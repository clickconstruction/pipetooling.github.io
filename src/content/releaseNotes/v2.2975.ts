import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2975',
  date: '2026-09-06',
  title: 'Safety net under the team feedback prompt',
  kind: 'infra',
  highlights: [
    'Nothing changes in the app. The rules for when the clock-out feedback prompt may ask, which cycle an answer files under, and who counts as your manager for the rating — plus the crew deck\'s reads and writes and the tag-org audit signals — now have 32 tests pinning their behaviour.',
  ],
}

export default note
