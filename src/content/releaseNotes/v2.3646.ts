import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3646',
  date: '2026-09-20',
  title: 'Developers: the agent door learns the app’s questions — a job, a customer, a bid, and “view as”',
  kind: 'infra',
  highlights: [
    'A developer’s coding agent can now ask for a job, a customer or a bid by its number or name and get what the app’s own window shows — including a job’s profit and a customer’s lifetime value, worked out by the same calculations the screens use.',
    'It can also read as a role’s sample account or a named person, to check what they can and cannot see. Still read-only, still logged by name.',
  ],
}

export default note
