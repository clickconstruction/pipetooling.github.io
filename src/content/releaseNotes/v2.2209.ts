import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2209',
  date: '2026-08-23',
  title: 'Send a task to Dispatch: buttons in the expected order',
  kind: 'fix',
  highlights: [
    'On the "Send a task to Dispatch" modal, Cancel now sits on the left and Send to Dispatch on the right, matching every other modal in the app.',
  ],
}

export default note
