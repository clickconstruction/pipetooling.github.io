import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2239',
  date: '2026-08-24',
  title: 'HR Report: find people by typing',
  kind: 'fix',
  highlights: [
    'The HR Report card’s person picker is now searchable — start typing a name instead of scrolling the whole roster; each match still shows the person’s role.',
    'Tightened the card’s intro line.',
  ],
}

export default note
