import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1969',
  date: '2026-08-21',
  title: 'Partnerships works on a phone',
  kind: 'fix',
  highlights: [
    'On mobile the partner roster becomes a chip strip above the detail, the header stacks with a normal-sized View-as button, and the six tabs scroll sideways on one line instead of stacking into a tower.',
    'Desktop layout is unchanged.',
  ],
}

export default note
