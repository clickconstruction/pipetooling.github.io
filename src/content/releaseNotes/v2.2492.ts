import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2492',
  date: '2026-08-29',
  title: 'The last dashboard banner joins the "Needs you" card',
  kind: 'feature',
  highlights: [
    'The "someone tried to become a dev" alert now lives inside the Needs you card as a red item, with its Snooze / Dismiss links and the jump to account review.',
    'That completes the consolidation: every dashboard attention banner is now one item in one card, on the Dashboard and on Quickfill.',
  ],
}

export default note
