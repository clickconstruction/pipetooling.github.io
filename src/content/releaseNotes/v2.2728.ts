import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2728',
  date: '2026-09-03',
  title: 'Estimate terms read as a note, not an empty box',
  kind: 'fix',
  highlights: [
    'On estimates — the customer’s accept page and every signed record — the Terms now sit as a short pull-quote with an orange rule instead of a grey box, and the link to the company Terms and Conditions sits right under them instead of at the bottom of the page.',
  ],
}

export default note
