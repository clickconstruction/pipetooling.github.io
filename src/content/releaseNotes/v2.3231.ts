import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3231',
  date: '2026-09-10',
  title: 'Job window on a phone: the close button is back on screen',
  kind: 'fix',
  highlights: [
    'On a phone the job window (Job · Edit · Bill · Costs · History) now fills the screen, its tabs wrap to fit, and the ✕ sits at the top right where you can reach it. Before, the tab row ran wider than the screen and pushed the ✕ off the edge, so the window could not be closed by touch.',
  ],
}

export default note
