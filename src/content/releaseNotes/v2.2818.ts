import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2818',
  date: '2026-09-05',
  title: 'People → Person: the rail agrees with Users about hours',
  kind: 'fix',
  highlights: ['On the Person tab rail, sessions waiting for approval still show as a count but no longer turn a person amber or list them under Needs attention — the same rule the Users tab follows.'],
}

export default note
