import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2261',
  date: '2026-08-25',
  title: 'Task cards name their roadmap stage — for everyone',
  kind: 'feature',
  highlights: [
    'Roadmap tasks on your checklist now carry a quiet purple line under the title naming the stage they belong to — context at a glance, on every task whatever its status.',
    'Field crews see it too: being assigned a roadmap task now lets you read that roadmap\'s stage names (they used to collapse to a bare "goal" chip).',
    'The line stays one line — long stage names trim with an ellipsis, and tapping it still opens "Where this task fits", which carries the roadmap name and the full picture.',
  ],
}

export default note
