import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2721',
  date: '2026-09-03',
  kind: 'fix',
  title: 'Pricing Workbench: "Solver ›" opens the solver',
  highlights: [
    'The blue button on the Pricing Workbench now reads Solver › — it opens the solver panel; the Solve button inside it is still what prices the rows.',
  ],
}

export default note
