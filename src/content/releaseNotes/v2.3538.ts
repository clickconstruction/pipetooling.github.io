import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3538',
  date: '2026-09-16',
  title: 'Pipeline: the six sections share one wiring list',
  kind: 'infra',
  highlights: [
    'Nothing changes on screen. The six Pipeline sections used to each repeat the same fifty lines of wiring; they now read one shared list, so a change to what every row can do is made once instead of six times.',
  ],
}

export default note
