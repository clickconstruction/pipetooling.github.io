import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3041',
  date: '2026-09-07',
  title: 'Safety net under Person Desk lifecycle checks',
  kind: 'fix',
  highlights: [
    'The facts behind starting and ending someone’s employment on the Person Desk — pay set-up, open time, trucks, housing, leaders, sub balances and paperwork — now have 6 tests pinning what is checked for each kind of person and that an unreadable table never reads as all clear; no behaviour change.',
  ],
}

export default note
