import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3034',
  date: '2026-09-07',
  title: 'Safety net under Bid Board unread badges',
  kind: 'fix',
  highlights: [
    'The unread-note badge on each Bid Board card now has 6 tests pinning which notes count — other people’s, after you last looked, shared across a customer’s bids — and how they are fetched; no behaviour change.',
  ],
}

export default note
