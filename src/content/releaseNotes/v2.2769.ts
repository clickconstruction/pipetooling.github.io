import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2769',
  date: '2026-09-04',
  kind: 'infra',
  title: 'Takeoffs refresh: the groundwork',
  highlights: [
    'Behind the scenes: the pieces the new Takeoffs views stand on — fixture-name matching that sees "WC-12" and "wc" as the same fixture, coverage math that agrees with Pricing to the cent, and the book matching and learning rules. Nothing on screen changes yet.',
  ],
}

export default note
