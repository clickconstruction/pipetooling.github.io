import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3777',
  date: '2026-09-23',
  title: 'One lien notice for the jobs at a property, on purpose',
  kind: 'feature',
  highlights: [
    'Send the run offers a tick when two or more approved notices go to the same owner at the same address from the same original contractor: “Combine the jobs at one property into one notice” — one form, the claims summed, the months joined, the bills and pay codes of every job behind it.',
    'The record stays one per job: each job gets its own filing with its own share, the paper’s total beside it, on one packet; every desk item moves to Sent; a tracking number typed on the envelope lands on each.',
    'Off unless the office ticks it, because the form’s claim changes when jobs combine. The row under a combined notice lists the jobs and their shares.',
  ],
}

export default note
