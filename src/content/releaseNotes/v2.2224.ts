import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2224',
  date: '2026-08-24',
  kind: 'fix',
  title: 'Pipeline tools menu wears its icons',
  highlights: [
    'The hamburger menu on the Pipeline stage strip now shows each tool with the same icon its board button wears — the hard hat for GC Review, 💵 for Accounts Receivable, 📅 for Payment forecast, and so on — instead of bare text.',
  ],
}

export default note
