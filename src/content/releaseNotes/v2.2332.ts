import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2332',
  date: '2026-08-26',
  title: 'Invoice PDF payment history gets its box',
  kind: 'fix',
  highlights: [
    'The payment history on invoice PDFs now sits in a black-bordered card, right-aligned under the amounts — the same frame customers see on the invoice preview and in the email.',
  ],
}

export default note
