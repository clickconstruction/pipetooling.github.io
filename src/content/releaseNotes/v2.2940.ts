import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2940',
  date: '2026-09-06',
  title: 'Robot doctrine: a new rule must prove itself before it spreads',
  kind: 'infra',
  highlights: [
    'A lesson that would change how OTHER bids price is now proposed, tested against two or three more historical bids, and only then adopted — the multiplier that pushed two estimates 100% high went in from a single bid.',
    "An auditor's stated standing rule still banks immediately — the gate is for the robots' own generalizations, never for her rulings.",
  ],
}

export default note
