import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2067',
  date: '2026-08-21',
  title: 'Assigned Jobs: real cards on your phone',
  kind: 'feature',
  highlights: [
    'Each assigned job is now its own card on phones — like your My Schedule cards — instead of one long run-together list.',
    'A job with a report due wears the amber left rail, so it stands out before you even reach the button.',
    'The report count moved out from under the Leave Report button into its own labeled chip ("3 reports" — tap to read them), and every button is now a full-size tap target.',
  ],
}

export default note
