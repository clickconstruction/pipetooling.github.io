import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2201',
  date: '2026-08-23',
  title: 'Checklist Review: task rows that fit a phone',
  kind: 'feature',
  highlights: [
    'On phones, each task in Outstanding by person is now two clean lines: a proper ✓ button and the task title edge to edge, then the goal chip, age, notes, and delete tucked underneath — titles get ~85% of the screen instead of about half.',
    'The drag-to-reorder grip stays on desktop where it\'s actually used; nothing else moved.',
    '"+ Add task" becomes a quieter dashed button so the tasks read louder than the chrome.',
  ],
}

export default note
