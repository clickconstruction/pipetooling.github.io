import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2564',
  date: '2026-09-01',
  title: 'Portal link on every Money waiting row',
  kind: 'fix',
  highlights: [
    'Each customer row in the Pay speeds Money waiting list now carries their 🌐 portal button at the right edge — collapsed or expanded — so sharing or checking their portal is one click from the money conversation.',
  ],
}

export default note
