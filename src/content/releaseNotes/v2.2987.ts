import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2987',
  date: '2026-09-07',
  title: 'One company, part 5: adoption and sharing are now a rule, not a list',
  kind: 'infra',
  highlights: [
    'The old "who adopted whom" and "who shares with whom" lists are retired. Every leader is paired with every office person automatically, computed from the roster, so a new hire or a role change never needs a sync and nothing can drift out of date.',
    'Nothing changes on screen for any role; this is the last piece of plumbing behind "one company".',
  ],
}

export default note
