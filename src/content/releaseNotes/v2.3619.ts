import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3619',
  date: '2026-09-19',
  title: 'A new app mark: the gear and wrench, now a C',
  kind: 'feature',
  highlights: [
    'The browser-tab icon and the iOS home-screen icon are a new mark: the same gear, filled solid, with the wrench moved to the middle so its open jaw reads as a C for Click.',
    'It sits on the yellow tile that CountTooling and Takeoff Tooling use, so the three apps look like one family in a row of tabs.',
    'Nothing else changes: the wordmark, the orange in the app, and the share card are as they were.',
  ],
}

export default note
