import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1952',
  date: '2026-08-21',
  title: 'Checklist fits phone screens again',
  kind: 'fix',
  highlights: [
    'On phones, the Checklist page (Review tab included) no longer renders zoomed out with sideways scrolling — the tab row now wraps instead of forcing the page wider than the screen.',
  ],
}

export default note
