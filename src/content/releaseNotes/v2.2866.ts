import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2866',
  date: '2026-09-05',
  kind: 'infra',
  title: 'Work Orders: the sheet rail, groundwork',
  highlights: [
    'Behind the scenes: one drawing of where a sub sheet stands — Drafted · Sent · Signed, then the sub\'s own Work · Inspection · Customer pays · Paid — with a dashed red run when work is happening and nothing is signed, and a "Next" line that names the office\'s move (get it in writing, price and send, nudge after three days, re-offer, schedule the inspection, bill and collect, pay). Nothing on screen changes yet; the Work Orders and Sub Labor rows pick it up next.',
  ],
}

export default note
