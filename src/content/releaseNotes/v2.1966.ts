import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1966',
  date: '2026-08-21',
  title: "Money moves tile across wide screens",
  kind: 'improvement',
  highlights: [
    "Today's Money Opportunities now lays out as cards side by side on big screens — four across on a wide monitor — instead of full-width rows with empty space between the text and the button.",
    'On phones they stack one per row, same as before. The 90+ chase card keeps its red edge.',
  ],
}

export default note
