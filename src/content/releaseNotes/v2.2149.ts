import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2149',
  date: '2026-08-23',
  title: 'Partner ledger: "(in progress)" on its own line',
  kind: 'fix',
  highlights: [
    'On the current week the card header now reads "Week of Aug 23" with "(in progress)" underneath, instead of one long phrase that wrapped mid-word on a phone.',
  ],
}

export default note
