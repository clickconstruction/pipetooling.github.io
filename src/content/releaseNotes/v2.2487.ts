import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2487',
  date: '2026-08-29',
  title: 'Job follow-ups join the "Needs you" card',
  kind: 'feature',
  highlights: [
    'The "jobs are waiting on a follow-up" banner now lives inside the dashboard\'s Needs you card — same count, same stage breakdown, same one-tap Start review.',
    'One less loose banner on the dashboard; the card\'s Walk the list mode now includes your quiet jobs.',
    'Quickfill is unchanged — its dedicated Job follow-ups station keeps the count there.',
  ],
}

export default note
