import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1938',
  date: '2026-08-20',
  title: 'Pipeline section buttons line up',
  kind: 'fix',
  highlights: [
    'The three oversized section-header buttons on the Pipeline board (Ready to Bill notifications, the Paid chart, Paid in Full notifications) now match the quiet 28px style every other header button uses.',
    '"Paid In Full" is now capitalized like the section it points at ("Paid in Full") everywhere it appears.',
  ],
}

export default note
