import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3643',
  date: '2026-09-20',
  title: 'Developers: Dev MCP keys moved to Settings → Your account',
  kind: 'fix',
  highlights: [
    'The Dev MCP keys card moved from the Digital twins tab to Settings → Your account (developers only). A key is personal — it lets your coding agent read the app as you — so it sits with your other account settings, and the Digital twins tab goes back to being about the robots.',
  ],
}

export default note
