import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3072',
  date: '2026-09-07',
  title: 'Housekeeping: database types regenerated after the sub-portal door and the function sweep',
  kind: 'fix',
  highlights: ["No visible change. The app's generated database type file picks up the two new server functions from today's pushes."],
}

export default note
