import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2527',
  date: '2026-08-31',
  kind: 'feature',
  title: 'Robot Board and Audits merge into one 🤖 tab',
  highlights: [
    'The Bids page now has a single 🤖 tab holding both robot surfaces — Robot Board and Audits flip as lenses inside, like Followup.',
    'The 🤖 badge shows the number of audits waiting on you; the Robot Board count moved onto its lens.',
    'Opening the tab lands on Audits when any are pending, otherwise the Robot Board. Old links to either tab still work.',
  ],
}

export default note
