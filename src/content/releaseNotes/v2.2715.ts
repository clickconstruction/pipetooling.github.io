import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2715',
  date: '2026-09-03',
  title: 'Pricing: the "price a bid at a target margin" guide follows the brush',
  kind: 'fix',
  highlights: [
    'The help guide for pricing at a target margin now describes the Workbench\'s 🖌 Margin › brush — pick a margin, sweep across rows, Undo sweep — instead of the retired Old grid\'s Margin mode toggle.',
    'Housekeeping from the Old-view retirement: the per-row "Margin for …" picker that only the Old grid could open is removed. The brush and the ⓘ breakdown are unchanged.',
  ],
}

export default note
