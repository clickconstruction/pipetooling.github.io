import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2004',
  date: '2026-08-21',
  title: 'Count Sheet: quick add tucks behind a button',
  kind: 'feature',
  highlights: [
    'The quick-add panel on the Count Sheet (Bids → Counts → New) starts collapsed to a "+ Quick add" button, so the sheet itself is the first thing you see.',
    'Click the button to expand it — the count box is focused and ready — and "Hide" to put it away.',
  ],
}

export default note
