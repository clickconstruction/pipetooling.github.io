import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3157',
  date: '2026-09-08',
  title: 'The phone/desktop switch sits at the very bottom',
  kind: 'fix',
  highlights: [
    'On the phone People board, "Show the desktop view" now comes after the Expected manpower totals, so it really is the last thing on the page — and the week grid offers the way back in the same spot.',
    'While you are placing something, the switch steps aside so the Cancel bar has the bottom of the screen to itself.',
  ],
}

export default note
