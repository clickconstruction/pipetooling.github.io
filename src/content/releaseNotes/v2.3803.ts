import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3803',
  date: '2026-09-24',
  title: 'Robot kickoffs open with the no-Terminal way',
  kind: 'feature',
  highlights: [
    'The kickoff a robot operator pastes into Claude Desktop — the estimator’s and the pricer’s — now opens with the plainest setup: Add custom connector, the robot’s address, No sign-in, and the key under Request headers, which works on claude.ai, Claude Desktop and a phone with no Terminal and no Node.',
    'The two Terminal ways stay, and the kickoff says when they are for you: an account whose Add custom connector screen has no Request headers section.',
  ],
}

export default note
