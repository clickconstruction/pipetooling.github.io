import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1905',
  date: '2026-08-20',
  title: 'Estimate drafts: Customer lines up with everything else',
  kind: 'fix',
  highlights: [
    'The Customer section at the top of a draft now sits on the left edge with the rest of the document instead of floating centered.',
  ],
}

export default note
