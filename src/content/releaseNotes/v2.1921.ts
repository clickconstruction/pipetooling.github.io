import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1921',
  date: '2026-08-20',
  title: 'Section lines you can actually see in light mode',
  kind: 'fix',
  highlights: [
    'The rules between estimate sections — in the draft editor, on the customer\'s document, and in the copy-to-Docs change order — were nearly invisible on white. They\'re now a clearly visible gray in light mode; dark mode is unchanged.',
  ],
}

export default note
