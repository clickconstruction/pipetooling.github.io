import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2687',
  date: '2026-09-03',
  title: 'Session notes lives in the search bar now',
  kind: 'feature',
  highlights: [
    'On Jobs → Pipeline, the Session notes clock moved off the button row and into the search bar, next to the # jump chip. Hover it and it still says Session notes; it opens the same view.',
    'The button row is back to New Job, Follow-ups, and Forecast, and on a phone the search bar keeps everything on one line.',
  ],
}

export default note
