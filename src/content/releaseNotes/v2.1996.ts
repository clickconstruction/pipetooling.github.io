import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1996',
  date: '2026-08-21',
  title: 'Order stages & tasks saves as you drag',
  kind: 'feature',
  highlights: [
    'Every drop in the Order stages & tasks dialog saves itself — no more Save button, just drag and watch "✓ Saved" in the footer.',
    'The Map, Plan, and Timeline behind the dialog update live with each drop.',
  ],
}

export default note
