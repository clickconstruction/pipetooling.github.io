import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2491',
  date: '2026-08-29',
  title: 'Bulk-deletion alerts join the "Needs you" card',
  kind: 'feature',
  highlights: [
    'The red bulk-deletions banner now lives inside the Needs you card with its own red stripe — burst summary, Review deletions button, and the Snooze 24h / Dismiss links all carry over.',
    'Alert items can now carry small follow-up links in the card, so alerts keep their "I\'m done with this" controls.',
  ],
}

export default note
