import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1973',
  date: '2026-08-21',
  title: 'Recently added moves into the section tools menu',
  kind: 'feature',
  highlights: [
    'The "Recently added" view now lives at the top of the ☰ section-tools menu (Pipeline group) instead of floating beside the stage strip — one less button competing for space.',
    'While the recent view is open, a "Back to board" pill still shows by the strip so the way back is always one obvious click.',
  ],
}

export default note
