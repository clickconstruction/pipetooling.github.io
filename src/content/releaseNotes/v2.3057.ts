import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3057',
  date: '2026-09-07',
  title: 'Controllers and the Team board: confirmed working',
  kind: 'fix',
  highlights: [
    'An open question about controllers being refused when linking clock sessions from Jobs → Team is closed: the July controller permissions already cover it. Documentation corrected; nothing to change in the app.',
  ],
}

export default note
