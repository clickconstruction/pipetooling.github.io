import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2175',
  date: '2026-08-23',
  kind: 'feature',
  title: 'Primaries see only their own estimates',
  highlights: [
    'A primary’s Estimates list now shows only the estimates they created and the ones on jobs they are the Account Man for. Nothing changes for other roles. (Applies once the migration is pushed.)',
  ],
}

export default note
