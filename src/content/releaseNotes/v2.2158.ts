import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2158',
  date: '2026-08-23',
  title: 'Statement emails: tap the office number to call',
  kind: 'fix',
  highlights: [
    'In GC statement emails, "call the office at (512) 360-0599" is now a tap-to-call link — on a phone it dials straight away. Draft Message, Copy, Share all, and the scheduled sends all get it.',
  ],
}

export default note
