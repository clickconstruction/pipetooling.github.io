import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3187',
  date: '2026-09-09',
  title: 'Robot questions link to our bid too',
  kind: 'feature',
  highlights: [
    'When a robot\'s question names its own practice bid (the ZZ Twin copy), a small green "ours b214" link now sits beside it. That opens the real bid — the one with our counts and our price — so you land on your own row for context, not the robot\'s draft.',
  ],
}

export default note
