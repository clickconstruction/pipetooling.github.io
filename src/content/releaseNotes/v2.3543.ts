import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3543',
  date: '2026-09-17',
  title: 'Submittals: the reviewer who marks up the PDF instead',
  kind: 'feature',
  highlights: [
    'On a shared revision, Drop a reviewer\'s file keeps the architect\'s redlined PDF or forwarded email on the revision, in a card under the sheet strip. The review room never shows it.',
    'Edit on a row gains Their call · on behalf of a reviewer: pick who it came from (or name a reviewer not on the room yet), tap Approve / Revise / Reject with their note, and the record reads "Revise · Dana Whitfield · entered by Wendi". The room\'s thread gets a quiet line, the reviewer\'s trail counts it, and the Needs You card treats it like any other call.',
  ],
}

export default note
