import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3554',
  date: '2026-09-17',
  title: 'Estimates: the groundwork for approving more than one option',
  kind: 'infra',
  highlights: [
    'Nothing changes on screen yet. Behind the page, an estimate option can now be a choice (pick one, as today) or an add-on (tick any), and an acceptance can freeze several options at once — the customer page and the builder follow in the next two releases.',
    'The estimate email lists add-ons under the choices with a + price when an estimate carries them.',
  ],
}

export default note
