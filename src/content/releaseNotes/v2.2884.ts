import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2884',
  date: '2026-09-05',
  title: 'Sheet story: the job line sits under the sub\'s name',
  kind: 'fix',
  highlights: [
    'In the sheet story, the job number and address now sit on their own line under the sub\'s name instead of running alongside it, so long addresses no longer crowd the header.',
  ],
}

export default note
