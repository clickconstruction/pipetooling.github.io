import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2674',
  date: '2026-09-03',
  title: 'Overhead lenses — click to see the math',
  kind: 'feature',
  highlights: [
    'Each of the three overhead lens cards (per field hour, per revenue dollar, per labor dollar) now opens a window showing the exact arithmetic with today’s numbers: the pool itemized on top, the denominator and its rule underneath, and the result.',
    'A week-by-week chart of the rate across the 90 days, with a rolling 4-week line and the 90-day headline, shows whether this week is unusual or the quarter is drifting.',
    '"What moves it" sizes each lever at today’s numbers — approve pending field time, bill finished work, cut office parts — and "Watch-outs" says plainly how each lens can mislead.',
  ],
}

export default note
