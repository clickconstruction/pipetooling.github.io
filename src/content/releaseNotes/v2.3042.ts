import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3042',
  date: '2026-09-07',
  title: 'Safety net under the Customer Summary',
  kind: 'fix',
  highlights: [
    'The Customer Summary in Dispatch Mode — every note, report, schedule block, clock session and event across a customer’s jobs — now has 4 tests pinning how it is gathered, capped and ordered; no behaviour change.',
  ],
}

export default note
