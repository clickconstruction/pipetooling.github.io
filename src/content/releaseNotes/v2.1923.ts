import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1923',
  date: '2026-08-20',
  title: 'Pipeline job rows get clear separators in light mode',
  kind: 'fix',
  highlights: [
    'The lines between jobs on the Jobs Pipeline board were nearly invisible in light mode. They\'re now a clearly visible gray — matching the crisp separation dark mode always had. Dark mode is unchanged.',
  ],
}

export default note
