import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3310',
  date: '2026-09-11',
  title: 'Bids → Labor opens on New',
  kind: 'feature',
  highlights: [
    'The Labor tab now opens on the New view — the queue of rows the book could not answer, the crew rate, the bottom line — on every device that has not picked a view. A device that chose Old keeps Old; the pills still switch either way.',
  ],
}

export default note
