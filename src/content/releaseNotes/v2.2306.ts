import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2306',
  date: '2026-08-25',
  title: 'Pricing opens in Margin mode',
  kind: 'fix',
  highlights: [
    'Bids → Pricing now starts with Margin mode on — the per-row margin chips are there from the first look instead of behind the toggle.',
    'Turning it off still sticks on your device, exactly as before.',
  ],
}

export default note
