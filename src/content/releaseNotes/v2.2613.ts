import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2613',
  date: '2026-09-01',
  title: 'Share a job from Waiting and Working rows',
  kind: 'feature',
  highlights: [
    'Waiting and Working rows on Jobs → Pipeline now have the Share icon in their icon row, next to the wrench — text a job\'s number, name, address, and link without opening the job.',
    'Share was already on the billing stages, Job detail, and the phone card menu; now every Pipeline row has it, like the help guide always said.',
  ],
}

export default note
