import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2971',
  date: '2026-09-06',
  title: 'Safety net under the dispatch slider',
  kind: 'infra',
  highlights: [
    'Nothing changes in the app. The math behind the Add-block slider on the Schedule hub — where a new block lands, how it hops across busy stretches when you drag, how linked legs move together — plus Expected Manpower totals and the clock-session bands under the dispatch strip now have 54 tests pinning their behaviour.',
  ],
}

export default note
