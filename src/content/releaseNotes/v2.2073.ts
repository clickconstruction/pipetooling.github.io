import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2073',
  date: '2026-08-22',
  title: 'My Schedule cards: the job name gets its room back',
  kind: 'feature',
  highlights: [
    'The job name now spans the whole card — no more four-line slivers squeezed beside the icons.',
    'Your % done shows once, on the progress bar, instead of twice in two corners.',
    'Call and pictures are uniform tap-size buttons, the report count is a labeled chip ("3 reports" — tap to read), and Leave Report + Update % done share one full-width row.',
  ],
}

export default note
