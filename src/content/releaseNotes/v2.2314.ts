import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2314',
  date: '2026-08-25',
  title: 'Quick Estimate: pick the kind first, step back any time, real CO fields',
  kind: 'feature',
  highlights: [
    'The wizard now opens with one clear question — Change order or Estimate — before anything else.',
    'Every screen has a ‹ Back button; step backwards and forwards freely without losing anything you typed.',
    'The change-order screen now has the same fields as the office change order: description of change, reason, schedule impact, and answer-needed-by date.',
  ],
}

export default note
