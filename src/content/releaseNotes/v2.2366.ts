import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2366',
  date: '2026-08-26',
  title: 'Groundwork for pushed-back task markers',
  kind: 'feature',
  highlights: [
    'Behind the scenes: every change to a task\'s due date is now remembered — who moved it, from when to when. The visible feature — "pushed ×2" chips and a timeline that draws the slip — ships next.',
  ],
}

export default note
