import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2512',
  date: '2026-08-30',
  title: 'Estimating robots carry their full playbook with them',
  kind: 'feature',
  highlights: [
    'The complete takeoff protocol set — doorway calibration, line tracing gates, branch sweeps, keyed-note census — is now served to robots directly, no repo access needed.',
    'A robot on any machine reads the same doctrine the in-house runs proved out.',
  ],
}

export default note
