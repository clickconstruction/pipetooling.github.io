import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2389',
  date: '2026-08-27',
  title: 'Cover Letter: Mark sent for simple bids; Workbench columns line up',
  kind: 'feature',
  highlights: [
    'A bid with no send-packets now gets its own Mark sent today button on the Cover Letter — one click stamps today as the sent date and the letter amount as the bid\'s value. Before, only packet bids had the button and simple bids needed the Edit form.',
    'Workbench worksheet: the Revenue and Profit headers sit centered over their columns, and the Count column\'s numbers are centered too.',
  ],
}

export default note
