import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2294',
  date: '2026-08-25',
  title: 'Partnerships gets a handshake icon',
  kind: 'fix',
  highlights: [
    'The gear menu’s Partnerships entry now shows a handshake instead of a briefcase, so it no longer reads like Documents’ twin.',
  ],
}

export default note
