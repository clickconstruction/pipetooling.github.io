import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2277',
  date: '2026-08-25',
  title: 'Dispatch Customers: tighter header',
  kind: 'feature',
  highlights: [
    'On the dispatch Customers screen, the title now sits to the right of the sort buttons instead of on its own centered row — the customer list starts one row higher.',
  ],
}

export default note
