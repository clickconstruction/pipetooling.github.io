import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2648',
  date: '2026-09-02',
  title: 'Price requests: real contacts, not retyped emails',
  kind: 'feature',
  highlights: [
    'Supply houses now carry contacts — reps, inside sales, branches. Sending a price request means tapping chips: one To, anyone else as CC, all on the same email and link.',
    'Your existing house contact info seeded the list automatically, a typed address can be remembered with one checkbox, and the desk shows who exactly each request went to.',
    'Contacts are managed right on the supply house form — archive, never lose history.',
  ],
}

export default note
