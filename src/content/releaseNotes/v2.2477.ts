import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2477',
  date: '2026-08-29',
  title: 'Plan briefs attach to bids (agent groundwork)',
  kind: 'feature',
  highlights: [
    'A bid can now carry a structured "plan brief" — a machine-readable summary of its plan set (fixture schedule, scope notes, risks) that estimating tools and the digital-twin program read.',
    'No visible change in the app yet; a plan-brief panel in the bid window comes later.',
  ],
}

export default note
