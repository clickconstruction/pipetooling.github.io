import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2938',
  date: '2026-09-06',
  title: 'Guide: schedule a sub across every surface',
  kind: 'feature',
  highlights: [
    'A new Help guide walks the sub scheduling plan end to end with screenshots: the window on Jobs → Subs, the sub picking a start and seeing their days, the percent card, watchers, what dispatch sees, and the GC portal.',
    'A map table names every surface and who owns it, and a table lists each dispatch inbox line with its next step.',
  ],
}

export default note
