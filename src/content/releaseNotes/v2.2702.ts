import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2702',
  date: '2026-09-03',
  title: 'Help: watch a contract go out from the Pipeline',
  kind: 'fix',
  highlights: [
    'The "get a job contract signed" guide now opens with a short recording: the contract chip on a Pipeline row, the prefilled Contract modal, and Copy link minting the customer’s signing link.',
  ],
}

export default note
