import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1988',
  date: '2026-08-21',
  title: 'Portal requests email the people you choose',
  kind: 'feature',
  highlights: [
    'New "Portal requests" stream in Settings → Email streams: pick who gets an email whenever a customer submits a visit or bid request from their portal — add and remove recipients right on the card.',
    'Push notifications to the dispatch group fire regardless; the email list is the CC layer on top.',
  ],
}

export default note
