import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2649',
  date: '2026-09-02',
  title: 'Lien signature requests show at the top of the Dispatch inbox',
  kind: 'fix',
  highlights: [
    'The "Awaiting your signature" and "Signed — ready to send" lanes now sit at the very top of Dispatch Mode → Inbox — the first thing a master sees — instead of hiding below the Teams Inbox fold.',
  ],
}

export default note
