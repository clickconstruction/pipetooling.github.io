import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3385',
  date: '2026-09-14',
  title: 'A new job asks once: does it need a contract?',
  kind: 'feature',
  highlights: [
    'The moment you save a new job, one small question: Send our agreement, File a signed copy, Not needed, or Later. On a builder’s job the first button reads “File Summit GC’s subcontract” — their paper is the agreement.',
    'Jobs that are already covered (a bid-room signature, an accepted estimate) and jobs under the contract floor never ask.',
    'The contract count on the Pipeline and the Dashboard stops refilling from hand-made jobs.',
  ],
}

export default note
