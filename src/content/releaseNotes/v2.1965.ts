import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1965',
  date: '2026-08-21',
  title: 'Partner lens loses the footer strip',
  kind: 'fix',
  highlights: [
    'The "Hidden with the lens on" explainer strip is gone from the View-as-Partner lens — the amber banner already says everything the lens needs to.',
  ],
}

export default note
