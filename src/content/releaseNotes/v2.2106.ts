import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2106',
  date: '2026-08-22',
  title: 'Fleet chips you can work through',
  kind: 'feature',
  highlights: [
    'On Vehicles, "N need a reading" is now a button: it opens a catch-up list — every vehicle with its VIN tail and holder, a miles box on each row, Enter saves and jumps to the next. Rows turn green as you go.',
    '"N maintenance tasks" opens the open-task list the same way: vehicle, VIN, task, assignee, and ✓ Done (or Assign) right on the row.',
  ],
}

export default note
