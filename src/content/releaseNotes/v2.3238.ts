import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3238',
  date: '2026-09-10',
  title: 'Edit Job: the status strip is near the top now',
  kind: 'feature',
  highlights: [
    'The tappable Waiting → Working → Ready to bill → Billed → Paid strip, with the Collections flag, now sits right under the Stages line near the top of the Edit tab instead of at the very bottom. Same taps, same rules, no more scrolling past everything to reach it.',
  ],
}

export default note
