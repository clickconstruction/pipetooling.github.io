import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3266',
  date: '2026-09-10',
  title: 'Job Summary: a chip names the overhead that landed on jobs the HCP # floor hides',
  kind: 'fix',
  highlights: [
    'The office pool spreads over every approved field hour, including hours on the older imported jobs the HCP # floor keeps out of the table. Overhead charged counts shown jobs only, so it read lower than the dials strip with no explanation.',
    'A new chip in the hygiene strip says how much landed on how many jobs the list leaves out — charged, not shown. Lower the floor under the table to see them.',
    'Nothing about the allocation changed; the numbers on every row are the same.',
  ],
}

export default note
