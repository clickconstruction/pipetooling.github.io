import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2791',
  date: '2026-09-04',
  title: 'Timeline — the y-axis says what it counts',
  kind: 'feature',
  highlights: [
    'On Jobs → Job Summary → Timeline, the left axis now carries a title: "jobs running that day" on the daily chart and "jobs touched that week" on the weekly roll-up — so the numbers up the side read without guessing.',
  ],
}

export default note
