import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1975',
  date: '2026-08-21',
  title: 'Pin Mode works in Dispatch Mode',
  kind: 'fix',
  highlights: [
    'The Dispatch Mode pages (Dashboard, Schedule, Inbox, Customers, PO) are now pinnable — with Pin Mode on, the pin footer shows there too, and the pins land on the Dashboard quick bar as "Dispatch Schedule"-style shortcuts.',
  ],
}

export default note
