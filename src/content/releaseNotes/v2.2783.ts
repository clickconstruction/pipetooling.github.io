import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2783',
  date: '2026-09-04',
  kind: 'infra',
  title: 'Takeoffs refresh: generated types catch up with the fixture-history lookup',
  highlights: [
    'Behind the scenes: the database type file now knows the fixture-history lookup that shipped earlier today. Nothing on screen changes.',
  ],
}

export default note
