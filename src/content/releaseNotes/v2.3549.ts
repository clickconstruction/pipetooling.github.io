import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3549',
  date: '2026-09-16',
  title: 'Pipeline: the ☰ section-tools menu moves into its own file',
  kind: 'infra',
  highlights: [
    'Nothing changes on screen. The ☰ menu beside the jump strip — the section headers\' tools without scrolling — now lives in its own component with its own tests.',
  ],
}

export default note
