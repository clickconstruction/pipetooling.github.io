import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2254',
  date: '2026-08-24',
  title: 'Pay speeds reads well on a phone',
  kind: 'fix',
  highlights: [
    'The Pay speeds breakdown restacks on phones: each customer becomes a two-line card — name and median up top, payments, open dollars and the speed bar underneath.',
    'The charts keep their full legible size behind a sideways swipe instead of shrinking, and the sheet uses nearly the whole screen.',
    'Desktop is unchanged.',
  ],
}

export default note
