import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2381',
  date: '2026-08-27',
  title: 'Counts: see how much is counted before you click',
  kind: 'feature',
  highlights: [
    'The bid list on the Counts tab now leads each row with a subtle number — how many fixtures and tie-ins are counted on that bid.',
    'Bids with nothing counted yet show a dim —, so "which bids still need counting" is answered at a glance.',
    'Hover the number for the long form ("83 fixtures & tie-ins counted").',
  ],
}

export default note
