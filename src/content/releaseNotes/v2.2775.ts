import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2775',
  date: '2026-09-04',
  title: 'Timeline — a hover guide names the day under your cursor',
  kind: 'feature',
  highlights: [
    'On Jobs → Job Summary → Timeline, moving across the chart now lights the day (or week) under the cursor with a soft band, thin edge lines, and a small label with the date and the running count — so you can see exactly what you\'re about to click before opening its session notes.',
  ],
}

export default note
