import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2699',
  date: '2026-09-03',
  title: 'Days view — click a day for its session notes',
  kind: 'feature',
  highlights: [
    'On Jobs → Job Summary → Days, click any day to open Session notes pinned to that day and grouped by job — who was on what, and what they wrote — one click from the count.',
    'Inside the view a "Day: Wed Sep 2 ✕" chip replaces the window chips; clear it to go back to the last 30 days. Search, Booked to, pins, and Assign / Change all work the same.',
  ],
}

export default note
