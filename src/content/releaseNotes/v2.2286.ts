import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2286',
  date: '2026-08-25',
  title: 'Checklist cards lead with their group',
  kind: 'feature',
  highlights: [
    'On the Review board, a task’s roadmap group now shows as its own bar above the title — wrapping fully instead of truncating.',
    'The age chip, cost, and delete buttons no longer get pushed off the screen by long group names, and cards read context-first.',
  ],
}

export default note
