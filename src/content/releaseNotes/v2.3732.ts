import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3732',
  date: '2026-09-22',
  title: 'Day book: devs and controllers only, for now',
  kind: 'feature',
  highlights: [
    'The Day book is open to devs and controllers only for now, at the owner’s call. Masters, assistants and estimators no longer see the tab, the line on Crew Day, or the Day book door anywhere, and the server refuses the read for every other role.',
    'People → Who’s where is unchanged: every office role still has it.',
  ],
}

export default note
