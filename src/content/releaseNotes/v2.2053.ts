import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2053',
  date: '2026-08-21',
  title: 'Why we lost: filter to one estimator',
  kind: 'feature',
  highlights: [
    'A "bids by" select in the header scopes the whole lens to one estimator — the headline count, the builder rail, and the keyboard queue all become that person\'s Friday list.',
    'The estimator list comes from the lost bids on screen (with a "No estimator" bucket), and All estimators is one tap back.',
  ],
}

export default note
