import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2301',
  date: '2026-08-25',
  title: 'Prospects: pick your calling order',
  kind: 'feature',
  highlights: [
    'Follow Up gets a calling-order switch: Coldest first (as before) or Never called first — prospects nobody has ever called lead the queue, oldest entry first.',
    '"Never called" means no Didn\'t Answer / Answered on record — a site-visit note or email doesn\'t count as a call. Those prospects wear a "never called" badge.',
    'A "3 of 41 · show order" peek lists who\'s coming up, with ages and badges — tap a row to jump straight to it. Your order choice is remembered per device.',
  ],
}

export default note
