import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2730',
  date: '2026-09-03',
  title: 'Help: the signed-agreement recording shows the new signature block',
  kind: 'fix',
  highlights: [
    'The "get a job contract signed" guide’s second recording now scrolls to the framed signature block — the mark, record ID, name and time, and the consent line — before showing Share and Email a copy.',
  ],
}

export default note
