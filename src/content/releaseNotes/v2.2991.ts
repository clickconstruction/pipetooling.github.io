import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2991',
  date: '2026-09-07',
  title: 'Safety net under the Calendar clock chips',
  kind: 'fix',
  highlights: [
    'The clock-session chips on the Calendar grid — their label, tooltip, Central-time range and duration — now have 9 tests pinning how they are built; no behaviour change.',
  ],
}

export default note
