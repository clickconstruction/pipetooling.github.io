import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2320',
  date: '2026-08-25',
  title: 'Portal recap box itemizes each payment',
  kind: 'feature',
  highlights: [
    'The per-job recap box on the customer portal now lists every payment by the date it was received, with its amount — Billed to date, minus each payment, equals Balance, all in one box.',
    'The separate "Payments received" strip is gone; the recap box tells the whole story.',
  ],
}

export default note
