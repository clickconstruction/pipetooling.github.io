import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3651',
  date: '2026-09-20',
  title: 'Sign-in page: the row of ten marks is the button',
  kind: 'feature',
  highlights: [
    'The "More apps" button is gone. The row of ten small marks under CountTooling and Takeoff Tooling is now the button itself — press anywhere on it and the ten open as a list, each with its name and one-line description; press again to close.',
    'The heading over the strip reads "The Tooling family".',
  ],
}

export default note
