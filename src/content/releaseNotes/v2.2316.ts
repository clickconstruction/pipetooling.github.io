import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2316',
  date: '2026-08-25',
  title: 'Roadmap: "needs a name" is now "needs a person"',
  kind: 'fix',
  highlights: [
    'The Dashboard roadmap card and the Plan view\'s shortlist lane now say "needs a person" — the count was always about unassigned tasks, but "needs a name" read like the tasks were missing titles.',
  ],
}

export default note
