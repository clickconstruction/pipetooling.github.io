import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2293',
  date: '2026-08-25',
  title: 'Quick Estimate — write up a change order from the field',
  kind: 'feature',
  highlights: [
    'Field roles can turn on a ⚡ Quick Estimate button (Settings → Dashboard & alerts, off by default): pick the job you\'re on, say what changed, snap photos, ballpark it or skip — and Send to Dispatch.',
    'The write-up is a real change-order draft in Estimates from the first tap, marked "With Dispatch" so nobody double-works it; Dispatch gets an inbox note with an Open-the-draft button.',
    'The office finishes pricing in the normal editor, and the field author gets an email the moment the customer accepts.',
  ],
}

export default note
