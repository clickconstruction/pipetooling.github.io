import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3337',
  date: '2026-09-11',
  title: 'The robot icon key explains the sealed envelope',
  kind: 'feature',
  highlights: [
    'The ? beside the Bid # header now opens with a thirty-second answer to "why can\'t I see the robot\'s number": the robot bids in secret, seals its price, and the envelope opens when you send.',
    'The five-in-a-row-within-8% rule is quoted from the gate itself, so the key can never drift from the Scoreboard.',
  ],
}

export default note
