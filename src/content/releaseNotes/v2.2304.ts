import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2304',
  date: '2026-08-25',
  title: 'The task card, rebuilt for thumbs and sunlight',
  kind: 'feature',
  highlights: [
    'Everything you press on the roadmap task card now lives in one dock at the bottom — a big Mark done with Pin and 🗑 beside it — the same on phone and desktop.',
    'Crew members get a stripped card built for glare: big bold title, who\'s on it, the last note, one-tap 👍 On it / ⚠️ Problem replies, and one giant green DONE.',
    'DONE responds instantly and tells you plainly if a save didn\'t stick ("Tap to retry — not saved") — no more waiting on a spinner in the sun.',
    'Renaming is just tapping the title; delete finally has a real button with its two-step confirm, out of the assign flow.',
  ],
}

export default note
