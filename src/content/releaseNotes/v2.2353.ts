import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2353',
  date: '2026-08-26',
  title: 'Profit bar tells you what the colors mean',
  kind: 'feature',
  highlights: [
    'Hover any slice of "Where the profit lives" on the Pricing Workbench and it answers instantly — line item, profit dollars, and share of job. The slice you\'re on stays vivid while the rest fade, so even hairline slivers read clearly.',
    'Click a slice to pin a detail card: qty, unit cost, sale price, revenue, cost, profit, margin, share of job, and the row\'s book entry — plus "Jump to row" to land on that line in the worksheet.',
    'The caption below the bar is now a real legend — every color with its item and share. Hover a chip to light up its slice; the legend collapses with one click if you want it out of the way.',
  ],
}

export default note
