import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3486',
  date: '2026-09-15',
  title: 'Settings: the Field reports rows say where to manage them',
  kind: 'fix',
  highlights: ['The Field reports row on My email schedule, and the Field report emails card on Email streams, now point at Jobs → Reports → Report email recipients.'],
}

export default note
