import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3281',
  date: '2026-09-11',
  title: 'Standing discount: the field is on Edit customer now',
  kind: 'fix',
  highlights: [
    'Customers → ✎ Edit → Standing discount. The rate and reason you set there are offered on every new job and every bill for that customer, as promised in v2.3272 — the field had shipped into a page the app no longer opens.',
  ],
}

export default note
