import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3634',
  date: '2026-09-20',
  title: 'Robots: the connector has a short address — mcp.clicktooling.com/twin',
  kind: 'infra',
  highlights: [
    'Every robot setup button (Set up on this Mac, Copy Desktop setup command, the kickoffs on the Console and the Pricing robot card) now hands out mcp.clicktooling.com/twin instead of the long database address.',
    'Nothing to redo: a computer that was set up with the old address keeps working.',
  ],
}

export default note
