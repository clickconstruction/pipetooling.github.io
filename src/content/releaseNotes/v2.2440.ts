import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2440',
  date: '2026-08-28',
  title: 'clicktooling.com groundwork',
  kind: 'infra',
  highlights: [
    'Behind-the-scenes prep for the app’s future home at clicktooling.com: every place that names the app’s address now reads from one flip point, so moving day is a couple of settings — and nothing changes until then.',
    'pipetooling.com will keep working forever as a redirect once the move happens — no saved link or bookmark will break.',
  ],
}

export default note
