import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2704',
  date: '2026-09-03',
  title: '"Only my bids" on every bid workflow tab, on by default',
  kind: 'feature',
  highlights: [
    'RFI, Change Order, and Lien Release now have the same "Only my bids" button as Counts, Takeoffs, Labor, Pricing, and Cover Letter — next to the sort buttons when no bid is selected.',
    'The filter starts on when you open Bids, so each tab opens to the bids where you are the estimator or account manager. Tap it once to see everyone\'s bids; the choice carries across all eight tabs.',
    'When the filter hides everything, the list says so ("No bids you are the account manager or estimator for") instead of looking empty.',
  ],
}

export default note
