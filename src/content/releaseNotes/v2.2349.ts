import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2349',
  date: '2026-08-26',
  title: 'Groundwork for task due dates',
  kind: 'feature',
  highlights: [
    'Behind the scenes: one-off checklist tasks can now carry a separate due date. The visible feature — a "Due by" field, "due Friday" chips, and late markers — ships next.',
  ],
}

export default note
