import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2825',
  date: '2026-09-05',
  title: 'Needs you — open jobs sitting idle',
  kind: 'feature',
  highlights: [
    'A new Needs you card on the Dashboard: how many open jobs have had no field work for 21+ days, the contract dollars behind them, and how many are yours. "See them" opens the Job Summary Cycle view with the list, longest idle first.',
    'Job Summary now reads ?view= from the link, so a card, a message, or a bookmark can land on Months, Cycle, Timeline, or Days directly.',
  ],
}

export default note
