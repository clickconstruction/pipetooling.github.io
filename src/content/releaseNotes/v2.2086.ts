import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2086',
  date: '2026-08-22',
  title: 'Checklist review: delete moved out of the way',
  kind: 'feature',
  highlights: [
    'On Review → Outstanding by person, the delete button now sits at the far right of each row and is gray instead of red — no more red trash next to every task title.',
  ],
}

export default note
