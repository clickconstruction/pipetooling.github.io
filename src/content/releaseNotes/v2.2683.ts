import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2683',
  date: '2026-09-03',
  title: 'Session notes door is now a clock icon',
  kind: 'feature',
  highlights: [
    'On Jobs → Pipeline, the "Session notes" text button next to Forecast is now a blue clock icon. Hover it and it still says Session notes; it opens the same view.',
  ],
}

export default note
