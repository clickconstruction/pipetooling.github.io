import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2167',
  date: '2026-08-23',
  title: 'Goals ledger: tap a stage to see its tasks',
  kind: 'feature',
  highlights: [
    'On Review → Goals, each stage row now opens in place to show its tasks — number, title, who\'s on it, ★ pinned / ⚡ next up, and whether it\'s on a list, in review, or signed off. Tap a task to see where it fits on the roadmap.',
    'On phones the stage rows wrap to two lines — full title on top, chips and progress bar underneath — instead of squeezing the title to a single letter.',
  ],
}

export default note
