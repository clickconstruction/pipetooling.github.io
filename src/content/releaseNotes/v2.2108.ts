import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2108',
  date: '2026-08-22',
  title: 'Fleet catch-up lists, dressed up',
  kind: 'feature',
  highlights: [
    'The odometer and maintenance catch-up windows got a design pass: a proper header with a big done-counter and progress bar, tidy identity rows with monospace VIN tails and holder dots, and a ✓ medallion when a row is finished.',
  ],
}

export default note
