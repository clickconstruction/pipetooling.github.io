import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3566',
  date: '2026-09-17',
  title: 'Punch list: an Opinion column — build, later, drop or your call, and the one sentence that decides it',
  kind: 'feature',
  highlights: [
    'Every row on the Punch list now carries a reviewer\'s call — Build, Later, Drop or Your call — and one sentence naming the benefit or the cost.',
    'It is a line in the to-do file, so anyone with repo access can change it; the board and the index render it.',
    'All 38 open rows have one today: 18 build, 13 later, 4 drop, 3 your call.',
  ],
}

export default note
