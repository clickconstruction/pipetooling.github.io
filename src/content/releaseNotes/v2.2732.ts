import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2732',
  date: '2026-09-03',
  kind: 'feature',
  title: 'Preview the bid-room email in Settings',
  highlights: [
    'Settings → Email templates & testing → Outbound email catalog: the "Bid room link / proposal to GC" row has a Preview button that opens the letter in a new tab with sample data — signed by you, the way a GC would see it.',
  ],
}

export default note
