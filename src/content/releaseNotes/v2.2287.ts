import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2287',
  date: '2026-08-25',
  title: 'Estimates header slims down',
  kind: 'fix',
  highlights: [
    'The Accepted notifications button on Estimates is now just the ⚙ gear — same settings behind it, and the header buttons fit one row on a phone.',
  ],
}

export default note
