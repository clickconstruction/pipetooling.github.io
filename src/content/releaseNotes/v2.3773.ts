import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3773',
  date: '2026-09-23',
  title: 'Travel times remembered: a repeat press is free',
  kind: 'feature',
  highlights: [
    'Drives to the office are now remembered for a month. Press Travel times on the clocked-in map tomorrow and the stops that were routed today answer at once, with no new lookup; only a job at a new address is routed.',
    'The bid form’s Distance to Office auto-fill shares the same memory, so the same address asked twice is answered from it.',
    'Nothing changes on screen: the miles and minutes read the same, and an estimate is still marked ≈.',
  ],
}

export default note
