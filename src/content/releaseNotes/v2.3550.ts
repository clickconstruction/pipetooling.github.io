import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3550',
  date: '2026-09-17',
  title: 'Bids → Labor: the Labor book panel moves into its own file',
  kind: 'infra',
  highlights: [
    'Nothing changes on screen. The Labor book section and its two forms — add or rename a book, add or edit an entry — now live in their own component with their own tests.',
  ],
}

export default note
