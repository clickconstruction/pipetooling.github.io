import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3767',
  date: '2026-09-23',
  title: 'Lien desk: the owner’s call, counsel’s three piles, and the playbook grid',
  kind: 'feature',
  highlights: [
    'Record the owner’s call… on a sent notice takes the three answers every owner letter asks for — do they still owe the GC anything, did they reserve the statutory 10 percent and still hold it, when was their contract with the GC completed — plus a note. Typed once, read by the notice, the Affidavits tab and the grid.',
    'The Affidavits tab sorts each job into counsel’s piles from those answers: A the owner still owes the GC (trap it), B paid but never reserved the 10 percent (a shared reserved-funds lien), C paid in full and holds nothing (the property lien — file now, do not wait for the 15th). The pane also says whether a payment bond is on the project, with the door to set it.',
    'Put a GC on notice gains a fifth step, The grid: counsel’s spreadsheet with one row per job — every date, every fact, and a ? for each answer the office still owes it — printable landscape for counsel.',
    'The GC failure playbook is now built end to end; the punch-list folder is retired and counsel’s memo lives in the docs.',
  ],
}

export default note
