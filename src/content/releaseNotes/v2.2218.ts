import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2218',
  date: '2026-08-23',
  kind: 'feature',
  title: 'Who sends the bids (dev)',
  highlights: [
    'The dev-only Health block gains "Who sends the bids": each month (or week) as a full-width bar split by person, toggling between share of dollars and share of bid count, over the last six months.',
  ],
}

export default note
