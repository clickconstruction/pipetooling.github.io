import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2339',
  date: '2026-08-26',
  title: 'The dashboard banners become one "Needs you" card',
  kind: 'feature',
  highlights: [
    'The stacked notification banners (bank deposits, unsorted purchases, team purchases, lost-bid reasons) now live in one "Needs you" card with a count — each item keeps its one-tap action.',
    'Two ways to work the list: Cards shows everything at once; Walk the list takes you through one item at a time with a Skip button, call-mode style.',
    'The Cards / Walk toggle sits at the card\'s bottom-right and remembers your choice.',
  ],
}

export default note
