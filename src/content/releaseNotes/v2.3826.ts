import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3826',
  date: '2026-09-25',
  title: 'Put a GC on notice shows each owner their property’s bills',
  kind: 'feature',
  highlights: [
    'Step 4 of Put a GC on notice gains a fourth tick, on by default when the run has property owners other than the GC: “Owners’ portals: their own bills only → their property’s bills too”.',
    'Approving the run then shows each owner every open bill at their property on their portal — the same as the Pipeline’s “owner sees the bills” switch, for the whole run at once. The notice itself appears there on its own once the run is recorded.',
  ],
}

export default note
