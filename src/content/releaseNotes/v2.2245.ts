import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2245',
  date: '2026-08-24',
  title: 'Dispatch Mode: focused pipeline + HR Report',
  kind: 'feature',
  highlights: [
    'The Job Pipeline on the Dispatch Mode dashboard now opens with only Ready to bill expanded — Waiting, Working, Billed awaiting payment, and Collections start collapsed (tap any header to open it).',
    'The HR Report card now also lives at the bottom of the Dispatch Mode dashboard, so masters can send an observation to HR without leaving dispatch.',
  ],
}

export default note
