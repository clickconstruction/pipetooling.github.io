import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2021',
  date: '2026-08-21',
  title: 'Pricing Workbench: guided tour',
  kind: 'feature',
  highlights: [
    'A "?" beside the New pill starts a spotlight tour of the Workbench — the page dims and each part lights up in order: scenarios and the ★, the summary strip, the solver, and preview → Apply.',
    'Four short stops with Next/Back (arrow keys work too, Esc closes); the last stop links to the full Workbench guide.',
  ],
}

export default note
