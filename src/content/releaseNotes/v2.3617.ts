import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3617',
  date: '2026-09-19',
  title: 'What customers see: the signed copy is its own step',
  kind: 'feature',
  highlights: [
    'The homeowner journey gains a Signed copy step after Signed: the email the app sends the moment a customer signs on the page, with the signed PDF attached and the same link kept. Settings → What customers see renders the real email over the sample.',
    'On a real person\'s journey the card reads what happened: sent at signing for an electronic signature, Emailed <date> when the office clicked Share on a signed agreement, or Not emailed for a paper signature nobody shared yet — with Open the signed copy as the door.',
  ],
}

export default note
