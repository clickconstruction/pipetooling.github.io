import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2222',
  date: '2026-08-23',
  kind: 'fix',
  title: 'Primary dashboard tidy-up',
  highlights: [
    'Recent Reports no longer offers primaries a Job detail button (or clickable job name) for jobs they can’t open — reports still read in full.',
    'My Bids now sits in the same card style as Recent Reports and Assigned Jobs.',
  ],
}

export default note
