import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.1989',
  date: '2026-08-21',
  title: 'One-tap sign-off rail on Checklist review rows',
  kind: 'feature',
  highlights: [
    'Every row in Checklist review now has the green ✓ Dismiss rail on its left — the same one-tap pattern as the Dispatch inbox — so clearing good work is a single tap.',
    'Expanding a row still shows the full history, with Dismiss and Reopen-with-comment unchanged.',
  ],
}

export default note
