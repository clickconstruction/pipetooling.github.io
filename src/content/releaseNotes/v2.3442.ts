import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3442',
  date: '2026-09-14',
  title: 'Maintenance: database types refreshed',
  kind: 'fix',
  highlights: [
    'The app’s generated database types were refreshed after today’s demand-letter migration — no change to what anyone sees.',
  ],
}

export default note
