import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2169',
  date: '2026-08-23',
  title: 'Vehicles: a fleet strip that reads on a phone',
  kind: 'feature',
  highlights: [
    'The cloud of chips at the top of People → Vehicles is now two things: one quiet line of facts (vehicles, motor pool, weekly ins+reg, with Insurance plans right there) and a "Needs attention" card that lists what wants work — count, plain label, red before amber — so it reads top to bottom on a phone.',
    'Only the rows that open something (need a reading, maintenance tasks) carry a chevron; the search box runs full width; on a desktop the same rows flow into one line of tiles.',
  ],
}

export default note
