import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2290',
  date: '2026-08-25',
  title: 'Data health opens its transactions',
  kind: 'feature',
  highlights: [
    'Click the Data health strip in Pay speeds to see every payment behind the numbers — filterable by bucket, searchable by customer, job, or address.',
    'Exclude a payment from the pay-speed math with one tap (Include again is the undo — who and when are remembered), or open its job to fix dates and links.',
    'The undated-bills backlog gets its own lens in the same list.',
  ],
}

export default note
