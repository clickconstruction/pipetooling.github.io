import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2082',
  date: '2026-08-22',
  title: 'Easter eggs can visit any screen',
  kind: 'feature',
  highlights: [
    'Settings → Easter eggs grows an "+ add screens" picker: every screen in the app in one grouped list, with Bids openable down to its individual tabs.',
    'Screens a targeted person can\'t see get a quiet "hidden for …" note — safe to leave checked, the visit just never triggers there.',
  ],
}

export default note
