import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3045',
  date: '2026-09-07',
  title: 'Safety net under the Team board',
  kind: 'fix',
  highlights: [
    'The week of clock sessions, dispatch blocks, sub sheets and acknowledgements behind Jobs → Team now has 4 tests pinning what is loaded and how each row is labelled and linked; no behaviour change.',
  ],
}

export default note
