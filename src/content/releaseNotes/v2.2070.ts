import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2070',
  date: '2026-08-22',
  title: 'Assigned Jobs: fresh launch starts folded, one door to billing',
  kind: 'feature',
  highlights: [
    'Assigned Jobs starts collapsed every time you open the app — expanding it lasts for the session, not forever.',
    "Field crew no longer see Send to Billing on assigned jobs: marking the job 100% in a report already walks you through Ready to Bill with the same checkboxes, so there's just one clear way now.",
    'Office roles keep the Send to Billing button.',
  ],
}

export default note
