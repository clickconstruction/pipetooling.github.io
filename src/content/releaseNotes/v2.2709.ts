import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2709',
  date: '2026-09-03',
  title: 'Signed contracts open as a record you can share',
  kind: 'feature',
  highlights: [
    'Click a green ✍ Signed chip and you now see the signed agreement — who signed, when and how, the document with their signature, and where it was signed from — instead of the send form. Works the same for contracts you sent, paper copies, and estimates the customer accepted online.',
    'A Share menu on that view holds Copy link, Text link, Download PDF, Print, and Open estimate; Start a new agreement… is the door back to sending when the scope changes.',
  ],
}

export default note
