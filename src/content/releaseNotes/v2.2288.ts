import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2288',
  date: '2026-08-25',
  title: 'Pay speeds: every payment names its job',
  kind: 'feature',
  highlights: [
    'Expanding a customer in Pay speeds now shows each payment with the job it came from — name and address — instead of bare dates.',
    'Tap a payment to open that job\'s detail.',
    'The "+ Weekly hygiene task" link is gone from the Data health strip.',
  ],
}

export default note
