import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2740',
  date: '2026-09-03',
  title: 'Add task: a Send button up top as soon as you start typing',
  kind: 'feature',
  highlights: [
    'In the Add checklist item dialog (the ＋ Task button, anywhere in the app), a Send button appears in the header the moment the title has text — tap it to send the task without scrolling to the bottom.',
    'The bottom button now says Send instead of Save. Both do the same thing.',
  ],
}

export default note
