import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2577',
  date: '2026-09-01',
  title: 'Supply house form is shorter — labels beside fields',
  kind: 'feature',
  highlights: [
    'The Add / Edit Supply House form now puts labels beside their fields instead of above them, so the whole form — Save button included — fits on screen without scrolling on a laptop.',
    'On phones the form keeps the taller stacked layout and scrolls.',
  ],
}

export default note
