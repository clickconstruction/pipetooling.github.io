import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2777',
  date: '2026-09-04',
  title: 'What customers see: the contract email joins the sub strip',
  kind: 'feature',
  highlights: [
    'Settings → What customers see now shows the email a sub gets when you send them a contract to sign, right before the signing page it links to.',
    'It is built by the same code that sends it, from you, with the link\'s expiry date and the sample sub\'s portal address.',
  ],
}

export default note
