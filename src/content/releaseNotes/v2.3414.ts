import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3414',
  date: '2026-09-14',
  title: 'Sold in packs, not only sticks',
  kind: 'feature',
  highlights: [
    'Some parts only come in batches — a flare nut in fives, wax rings by the box. Sold in gains “per pack” beside sticks, coils, box and bundle, so 13 nuts needed buys 15 and the takeoff prices the two extra the same way it prices the sticks.',
    'Request quotes now words pieces as packs — “× 15 (13 needed · packs of 5)” — instead of borrowing the sticks wording.',
  ],
}

export default note
