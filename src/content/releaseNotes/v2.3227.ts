import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3227',
  date: '2026-09-10',
  title: 'Bid flow: Count & import opens the paste box for you',
  kind: 'feature',
  highlights: [
    'Click Count & import on the bid flow strip and Counts opens with the Import Counts box already open and the cursor in it, ready to paste from CountTooling. Cancel closes it if you only wanted to look.',
  ],
}

export default note
