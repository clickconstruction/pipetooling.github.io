import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2442',
  date: '2026-08-28',
  title: 'The app moves to clicktooling.com',
  kind: 'feature',
  highlights: [
    'PipeTooling’s new home is clicktooling.com. Every old pipetooling.com link, bookmark, and emailed statement keeps working forever — they redirect automatically.',
    'One thing needs a minute of your time: re-add the app to your phone’s home screen from clicktooling.com and re-enable notifications there — the old install can’t follow the move on its own.',
  ],
}

export default note
