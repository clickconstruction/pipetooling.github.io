import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3434',
  date: '2026-09-14',
  title: 'Inbox eligibility: one shared reading behind the dispatch and estimator inboxes',
  kind: 'infra',
  highlights: [
    'Housekeeping only — the Dispatch inbox, the Estimator inbox and the Customer Waiting banner now share one check of who is in each group. Nothing changes in what you see or can do.',
  ],
}

export default note
