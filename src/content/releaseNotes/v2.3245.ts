import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3245',
  date: '2026-09-10',
  title: 'Nudge a vendor from the bid',
  kind: 'feature',
  highlights: [
    'Edit Bid → Price requests: a request the app sent that is still waiting on the vendor now has a Nudge button on its row. It shows you the exact reminder first; nothing sends until you tap Send this nudge. Same 24-hour rest as the Pricing desk, and the row then reads "nudged ×1".',
  ],
}

export default note
