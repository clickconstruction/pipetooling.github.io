import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2336',
  date: '2026-08-26',
  title: 'My Inbox fits on one screen (desktop)',
  kind: 'feature',
  highlights: [
    'On desktop, My Inbox items are now compact one-line rows — "Reopened" and note counts show as small chips, and the add-note and forward actions sit quietly at the end of each row until you hover.',
    'Overdue and Due Today carry their counts in the header, so the size of the pile reads at a glance.',
    'On phones nothing changes — the big glove-friendly cards stay exactly as they were.',
  ],
}

export default note
