import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2262',
  date: '2026-08-25',
  title: 'Roadmap search spotlights its matches',
  kind: 'feature',
  highlights: [
    'Searching the roadmap Map now dims stages with no hit to 30% — they stay in place so the arrows and layout keep making sense — while matching stages stay full-strength.',
    'The exact text that matched glows amber inside each card, whether it hit the stage title, a task, or an assignee name, so you can see why every card lit up.',
    'Clear the search and the whole map fades back in.',
  ],
}

export default note
