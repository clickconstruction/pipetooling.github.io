import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2443',
  date: '2026-08-28',
  title: 'Click Tooling is now Plumbing Tooling',
  kind: 'fix',
  highlights: [
    'The test-report tool renamed to match its new home at plumbingtooling.com — the Pipeline button, wrench tooltip, and help guides now say Plumbing Tooling. Same tool, same click, same prefilled report.',
    'The Click Plumbing branding on the reports themselves is unchanged — that’s the company on the paperwork, not the app.',
  ],
}

export default note
