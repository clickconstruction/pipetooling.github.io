import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3134',
  date: '2026-09-08',
  title: 'Stage Plan: the guides, with recordings',
  kind: 'feature',
  highlights: [
    'Three short recordings in Help: the Bill tab setting a stage and the draws following, Edit Job → Stages with the eye and the customer preview, and the GC portal\'s Where the job is card.',
    'The hub guide "schedule a sub across every surface" starts from the plan on the Bill tab; "split a job into stages" and "show a GC the stages you plan" carry the clips.',
    'The customer preview drawer opens the GC\'s real portal link when one is minted (the sample page otherwise).',
  ],
}

export default note
