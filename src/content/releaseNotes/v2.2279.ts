import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2279',
  date: '2026-08-25',
  title: 'Dispatch Inbox & PO: tighter headers',
  kind: 'feature',
  highlights: [
    'The dispatch Inbox and PO Generator titles now sit to the right of their header rows — Open Gmail and the hold-to-sort hint — matching the Customers screen, so content starts a row higher.',
  ],
}

export default note
