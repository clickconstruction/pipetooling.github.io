import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2514',
  date: '2026-08-30',
  title: 'Robot takeoffs learn the third dimension',
  kind: 'feature',
  highlights: [
    'Traced runs now carry pipe sizes, so footage and fittings report the way estimators price them — per size, per system.',
    'Vertical allowances capture the drops, risers, and vent stacks a flat plan view cannot show — every height names its source.',
    'The robot price book gained 36 unit prices mirrored from a sent-and-won bid, named to match the takeoff rows exactly.',
  ],
}

export default note
