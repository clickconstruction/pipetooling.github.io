import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2765',
  date: '2026-09-04',
  kind: 'infra',
  title: 'Takeoffs: the refresh roadmap',
  highlights: [
    'Behind the scenes: the sequenced plan for the Takeoffs refresh — today\'s tab stays as Old while two new views arrive behind a toggle: a guided one-fixture-at-a-time pass with a book that learns from finished fixtures, and a cost-rail view that shows exactly what Pricing sees.',
    'First on the list: "Fill from book" will start working on Combined takeoffs, which is nearly every bid.',
  ],
}

export default note
