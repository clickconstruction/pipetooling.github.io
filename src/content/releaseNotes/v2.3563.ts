import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3563',
  date: '2026-09-17',
  title: 'Bids → Pricing: the Price book drawer moves into its own file',
  kind: 'infra',
  highlights: [
    'Nothing changes on screen. The right-hand Price book drawer — the book chips, Use on this bid, the frozen-copy caption, Add entry, the search and the entries table — now lives in its own component with its own tests.',
  ],
}

export default note
