import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3456',
  date: '2026-09-15',
  title: 'Owner of record: the care-of name shows only when it adds something',
  kind: 'fix',
  highlights: ['The job form and the Fix-ups list no longer print the owner’s name twice when the appraisal roll repeats it in the care-of field.'],
}

export default note
