import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2650',
  date: '2026-09-02',
  title: 'Waiting For folds into My Inbox',
  kind: 'feature',
  highlights: [
    'The Dashboard\'s "⏳ Waiting For" list now lives inside the My Inbox card as a quiet strip at the bottom, collapsed by default — it shows the count and a peek at the first blocker, and tapping it unfolds the full list in place.',
    'Checklist → Today keeps its own Waiting For section unchanged.',
  ],
}

export default note
