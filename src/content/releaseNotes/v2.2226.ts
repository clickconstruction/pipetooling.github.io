import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2226',
  date: '2026-08-24',
  title: 'Email the Payment forecast',
  kind: 'feature',
  highlights: [
    'New Email… button on the Payment forecast: send it to a teammate now, schedule it, or make it a weekly subscription — Monday 7 AM works great.',
    'The email mirrors the forecast exactly — buckets, pay speeds, and every bill with its expected date, follow-ups first — built fresh at send time.',
    'Scheduled sends show on Settings → My email schedule, and the email’s button opens the forecast right back up in the app.',
  ],
}

export default note
