import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2182',
  date: '2026-08-23',
  title: 'Edit roadmap tasks from "Where this task fits" — and mark them done from the card',
  kind: 'feature',
  highlights: [
    'In the "Where this task fits" window, tap any task in the stage box to open its card — rename, assign, ★ pin, add a note — right over the window. Close it and the window is already refreshed.',
    'The task card gains ○ Mark done (and ✓ done · reopen to undo) — the same as ticking the box on the Map, so the roadmap, Goals and Timeline agree instantly.',
  ],
}

export default note
