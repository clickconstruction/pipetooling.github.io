import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2374',
  date: '2026-08-26',
  title: 'The package map — every GC, version, and price in one tree',
  kind: 'feature',
  highlights: [
    'A 🗺 Map button now rides the end of the Send to strip on the bid workflow tabs — click it any time to see the whole bid as a tree: each GC packet, its versions, and every price option.',
    'Every price shows its value, its margin against the bid’s shared cost, and who sees it — ★ on their letter, alternate on letter, or only you.',
    'Sent dates, sent values, and won/lost ride each GC’s line; a "viewing" chip marks where you are.',
    'Click a price to open it on the Pricing tab — viewing only, it never changes what a GC sees.',
  ],
}

export default note
