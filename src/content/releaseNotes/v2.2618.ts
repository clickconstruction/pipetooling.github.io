import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2618',
  date: '2026-09-01',
  title: '"Copy fixtures for text" is now "Supply house list"',
  kind: 'feature',
  highlights: [
    'The Share ▾ item and its prepare screen are named for the document they produce — a scoped, price-free supply house list — matching the Supply Houses directory in Materials.',
    'Nothing else changed: same scopes, same inline code pinning, same live preview.',
  ],
}

export default note
