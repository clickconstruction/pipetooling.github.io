import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2399',
  date: '2026-08-27',
  title: 'Add GC: search instead of scrolling',
  kind: 'feature',
  highlights: [
    'The "Send this bid to another GC" window now finds the GC by typing — no more scroll hunt through the whole customer list.',
    'The Version dialog\'s "GC this version goes to" picker got the same search.',
  ],
}

export default note
