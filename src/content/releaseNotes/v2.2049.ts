import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2049',
  date: '2026-08-21',
  kind: 'feature',
  title: 'Roadmap Timeline: full task titles and numbered bars',
  highlights: [
    'In an expanded stage, each task’s title now runs past the left rail into the open lane — as far as it needs, stopping exactly at its own bar. Assignees ride along in gray.',
    'Every task bar wears its number (1.1, 1.2, …) centered inside — on the waterfall rows and, where a slot is wide enough, on the stage’s segmented bar too.',
    'Tapping a title or a bar still opens the task card.',
  ],
}

export default note
