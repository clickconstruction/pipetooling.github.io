import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2247',
  date: '2026-08-24',
  title: 'AR list shows line items without a click',
  kind: 'feature',
  highlights: [
    'The Accounts Receivable and Not Billed Out lists now show each item\'s line items expanded by default — no more tapping "N line items" on every row.',
    'A small chevron beside the first line collapses a stack back to the compact chip when you want the shorter view; the chip brings it back.',
  ],
}

export default note
