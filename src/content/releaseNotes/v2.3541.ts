import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3541',
  date: '2026-09-16',
  title: 'Pipeline: the row icons are drawn from one place',
  kind: 'infra',
  highlights: [
    'Nothing changes on screen. The Test report, Lien, Release of lien, AIA and Hazmat icons on Pipeline rows, and the notes panel that opens under a row, each had two or three pasted copies across the section tables; they are now one component each, so a change reaches every row.',
  ],
}

export default note
