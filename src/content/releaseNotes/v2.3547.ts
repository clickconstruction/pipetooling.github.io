import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3547',
  date: '2026-09-16',
  title: 'Bids → Pricing: the margin breakdown moves into its own file',
  kind: 'infra',
  highlights: [
    'Nothing changes on screen. The per-row Margin breakdown dialog on the Pricing grid now lives in its own component with its own tests.',
  ],
}

export default note
