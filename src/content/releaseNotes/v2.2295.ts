import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2295',
  date: '2026-08-25',
  title: 'With Dispatch chip shows on phone-size estimate cards',
  kind: 'fix',
  highlights: [
    'The amber "With Dispatch" marker on field write-ups now appears on the phone-size card list too, not just the desktop table.',
  ],
}

export default note
