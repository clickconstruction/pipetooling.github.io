import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3814',
  date: '2026-09-24',
  title: 'The pipe title as files, for use elsewhere',
  kind: 'feature',
  highlights: [
    'The ClickPlumbing.com pipe lettering from the sign-in page is saved as picture files you can drop into anything — a letterhead, a truck decal, a shirt: a white version for dark backgrounds and a dark version for light ones, each as a scalable SVG and a large PNG, plus a preview on a dark ground.',
    'Find them at clicktooling.com/brand/clickplumbing-pipe-white.svg (and -dark, .png, -preview.png).',
  ],
}

export default note
