import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2344',
  date: '2026-08-26',
  title: 'Preview email from View bill too',
  kind: 'feature',
  highlights: [
    'The View bill window (Documents) gains a Preview email button next to "Open PDF in new tab" — see the email any billed invoice produces, reconstructed from current job data. Nothing is sent.',
    'Same preview engine as Bill Customer’s Preview email button, so both doors show the same thing.',
  ],
}

export default note
