import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2118',
  date: '2026-08-22',
  kind: 'feature',
  title: 'Manage tab opens on One-offs',
  highlights: [
    'Checklist → Manage now opens on the One-offs view instead of All, so the open list is the first thing you see.',
    'All, Repeating, and Completed are still one tap away — the pills work exactly as before.',
  ],
}

export default note
