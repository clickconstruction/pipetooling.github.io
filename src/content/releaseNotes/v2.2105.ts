import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2105',
  date: '2026-08-22',
  kind: 'feature',
  title: 'The Call queue — Followup as one list',
  highlights: [
    'A new first lens on Followup: one queue of builders worth a call, each card carrying a plain To do / Done table — Chase, Loss reasons, Bid tabs — so what a call collects and what\'s already collected read at a glance.',
    'Click any row and it drops open in place: the pending bids with their one-tap chase chips, the lost bids with the six reason chips, the gettable tabs with the capture panel — collect one thing without leaving the queue.',
    'Filter chips (To chase · Need a reason · Tab gettable) narrow the list to one kind of gap; search finds a builder or bid; 📞 Start call jumps to the builder card for a full session.',
    'The old lenses are all still there, unchanged, right next door — nothing about your routine has to change until the new queue earns it.',
  ],
}

export default note
