import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2335',
  date: '2026-08-26',
  title: 'Invoice email payment box moves right',
  kind: 'fix',
  highlights: [
    'The payment history in invoice emails now sits in a bordered card on the right side — heading inside the frame — matching the invoice PDF and the on-screen preview instead of stretching across the page.',
  ],
}

export default note
