import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2599',
  date: '2026-09-01',
  title: 'Superintendents see Recent Reports on their dashboard',
  kind: 'feature',
  highlights: [
    'The Recent Reports section — field reports as they come in, with unread badges — now shows on the superintendent dashboard, scoped to reports on the projects you are assigned to.',
    'Reports were already reachable through Jobs → Reports; this puts the newest ones where superintendents start their day.',
  ],
}

export default note
