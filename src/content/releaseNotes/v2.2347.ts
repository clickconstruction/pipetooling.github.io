import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2347',
  date: '2026-08-26',
  title: 'Quickfill gets three new stations',
  kind: 'feature',
  highlights: [
    'Lost bid reasons, GC weekly review, and Job follow-ups join Quickfill as their own sections — the same one-tap cards from the Dashboard, now part of the office routine with their own "Mark up to date" buttons.',
    'Each section carries its count in the header and feeds the freshness tally, so the morning sweep shows whether these rituals are current.',
    'On a clean day the card hides but the section stays markable — the ritual can be stamped even when there is nothing to chase.',
  ],
}

export default note
