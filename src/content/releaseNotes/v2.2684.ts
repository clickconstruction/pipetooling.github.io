import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2684',
  date: '2026-09-03',
  title: 'Contracts: paper copies, the signed record, and the contract on every job screen',
  kind: 'feature',
  highlights: [
    'Already have a signed paper contract? Open the Contract modal and use Upload signed copy — who signed, when, and an optional scan — and the job reads "✍ On file · paper" with nothing sent to the customer.',
    'Every signed contract opens as a record: the document as signed, the signature, and who / how / when / from where, with Print / save as PDF.',
    'The contract now shows in the Bill Customer modal, the View bill panel, the Job window’s Edit tab, and Documents → Jobs — each with Send contract or View record right there. The Contract Book gains an Audience setting so you can write your own customer terms.',
  ],
}

export default note
