import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3517',
  date: '2026-09-16',
  title: 'Create the GC’s portal link from the stage preview',
  kind: 'feature',
  highlights: [
    'On Edit Job → Stages → See it as the customer, a GC who has never been given a portal link now gets a Create their link button at the foot of the drawer — the page goes live right there, and you still decide when to share it.',
    'Once created, the drawer’s door reads Open the portal instead of the sample.',
  ],
}

export default note
