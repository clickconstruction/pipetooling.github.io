import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.3653',
  date: '2026-09-20',
  title: 'Sign-in page: the bar of ten marks runs the width of the two tiles above it',
  kind: 'feature',
  highlights: [
    'The bar of ten small marks under CountTooling and Takeoff Tooling now spans the same width as those two tiles side by side, and the marks are bigger so the ten fill it side by side, with the arrow at the right end. On a phone it matches the stacked tiles.',
    'The CountTooling and Takeoff Tooling marks are now exactly the size of the ten below them, at every screen width.',
  ],
}

export default note
