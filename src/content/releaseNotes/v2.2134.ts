import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2134',
  date: '2026-08-22',
  title: 'Partnerships Ledger: explanatory footnote removed',
  kind: 'fix',
  highlights: [
    'The paragraph under the Ledger table ("Newest first; each row\'s balance is the running balance…") is gone — the table speaks for itself, and the row drill-ins still work the same way.',
  ],
}

export default note
