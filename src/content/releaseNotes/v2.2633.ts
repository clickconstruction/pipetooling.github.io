import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2633',
  date: '2026-09-01',
  title: 'Help: one guide for the whole supply house quote loop',
  kind: 'fix',
  highlights: [
    'New guide "get supply house prices on a bid" walks the full loop — send the list, plug the reply in (paste, link, or phone call), compare houses, pick the winners — with the honesty rules spelled out.',
    'The pricing-package guide now points there instead of burying the quote workflow at its bottom.',
  ],
}

export default note
