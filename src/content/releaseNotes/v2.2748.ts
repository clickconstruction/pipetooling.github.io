import type { ReleaseNote } from '../../lib/releaseNotes'

const note: ReleaseNote = {
  version: 'v2.2748',
  date: '2026-09-04',
  kind: 'fix',
  title: 'Signed agreements: every master hears about every signature',
  highlights: ['Masters on the Signed agreements list are emailed for every accepted estimate and signed proposal, whichever master owns the record.'],
}

export default note
