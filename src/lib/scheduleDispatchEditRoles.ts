/** Roles that can edit schedule blocks in Schedule Dispatch and Quickfill Schedule (parity). */
export const CAN_USE_SCHEDULE_DISPATCH_EDIT_ROLES = new Set([
  'dev',
  'master_technician',
  'assistant',
  'superintendent',
  'controller',
])
