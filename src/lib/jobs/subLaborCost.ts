// Moved to supabase/functions/_shared/subLaborCost.ts (v2.3638) so the dev-mcp server reads the same
// numbers the screens do. This path stays so no importer changes; the tests beside it still run.
export * from '../../../supabase/functions/_shared/subLaborCost'
