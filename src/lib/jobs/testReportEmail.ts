/** Client door to the test-report email builder (v2.3301) — one builder for the Send sheet's preview and the edge function. */
export { DEFAULT_TEST_REPORT_EMAIL_TEMPLATE, buildTestReportEmail, testReportEmailSubject, type TestReportEmail, type TestReportEmailInput } from '../../../supabase/functions/_shared/testReportEmail'
