// Moved to supabase/functions/_shared/peopleLaborJobItemLineCost.ts (v2.3638) so the dev-mcp server reads the same
// numbers the screens do. This path stays so no importer changes (the kernel has no dedicated test yet).
export * from '../../supabase/functions/_shared/peopleLaborJobItemLineCost'
