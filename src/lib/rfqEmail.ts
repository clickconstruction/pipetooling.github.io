/** Client door to the price-request email builder `send-rfq-email` uses (v2.3512), so Settings → What customers see renders the same email over the sample. */
export { buildRfqEmail, rfqNeededByWord, type RfqEmailInput } from '../../supabase/functions/_shared/rfqEmail'
