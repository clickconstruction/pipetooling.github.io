# Day book census — read-only, prod, 2026-09-16 (PR 0)

Run through the Management API against `yewfzhbofbbyvkvtaatw` before any code, as the plan required. Windows: last 30 days unless stated. Names are the office's real people; figures are counts, not judgments.

## 1. Attribution per source

| Source | Rows | No actor | Distinct actors | Reading |
|---|---|---|---|---|
| `job_activity_events` · `invoice_sent` | 49 | **46** | 1 | The Stripe and email send functions write `sent_to_customer_at` under the service role, so the trigger stamps no actor. HousecallPro-channel sends (in-app) are attributed. |
| `job_activity_events` · `invoice_billed` | 19 (106 in 90 d) | 0 (1 in 90 d) | 2 | **"Marked billed" is the office's attributed billing act.** 53 of 152 sent invoices in 90 days had no billed event at all (billed before June, or sent without the mark). |
| `job_activity_events` · `payment_added` | 415 | 50 | 2 | Deposits applied in AR are attributed; the 50 are imports/backfills. |
| `job_activity_events` · `status_change` | 375 | 30 | 5 | |
| `job_activity_events` · `field_edited` | 1,529 | 768 | 2 | Not a line (a field edit is a click, not an outcome). |
| `job_contract_events` · `sent` / `recorded` | 3 / 3 | 0 | 1 | Small but clean. |
| `clock_sessions` approvals | 336 | 58 | 1 | The 58 are `origin = salary_schedule` auto-approvals — the system's, excluded. |
| `hours_reviewed` | **0** | | | Nobody has used it in 30 days. The line exists in the kernel; it will read 0. |
| `dispatch_requests` closed | 48 | 1 | 2 | |
| `deleted_records_archive` | **7,091** | 92 | 5 | Bulk deletions dominate; aggregated per person, day and table in the payload. |
| `bid_version_sends` | 16 | 0 | 3 | Clean. |
| `bid_pricing_assignments` | 4,130 | **4,062** | 2 | Robot writes. The 68 attributed rows are the human "priced" touches. |
| `bid_rfqs` / `bid_quotes` | 6 / 1 | 1 / 0 | 3 / 1 | Tiny. |
| `bid_audit_notes` with a verdict | 24 | 0 | 1 | Clean. |
| `twin_questions` answered | 22 | 0 | 2 | Clean. |
| `bids_submission_entries` | 443 | **301** | 4 | The no-actor rows are the robot pipeline's own log lines (`[shadow LOCK]`, `[pipeline STG-5]`). Of the attributed 222 in 90 days, **62 have a `contact_method`** (60 Phone, 2 Email) — those are the human follow-ups; the rest are outcome-change notes. |
| `bid_best_efforts` | 0 | | | New feature; nothing yet. |

**Stop rule 1 tripped for `invoice_sent` (94 % no actor).** Resolution: the *Billed* line reads `invoice_billed` plus any attributed `invoice_sent`, deduped per invoice, and unattributed sends count as system rows on the day header. A follow-up PR (1b) should have the two send functions stamp `sent_by_user_id` on the invoice so the trigger can attribute the send itself — `send-stripe-invoice` already resolves the caller from the Authorization header.

## 2. Office days and the quiet rate

| | |
|---|---|
| Office people with clock time on the Office job | 7 |
| Office days (person × day) | 57 |
| Quiet days (clock time, no attributed outcome in any source) | **13 (23 %)** |
| Days with a clock-out note | **56 of 57** |

Stop rule 2 (quiet above 40 %) did not trip. The notes are real and human — "bids", "Office", "Walmart and office", "Schedule", "emails", "bids and such. talked to some people" — so reading the note on the row is worth it. They are also short, so the row shows them in one line.

## 3. The estimator strip by hand — Wendi, Aug 18 – Sep 16

| Measure | Value |
|---|---|
| Sent | 5 bids · $264,159 |
| After due date | **4 of 5** |
| Decided in range | 0 won · 2 lost ($94,945) |
| Hit rate, trailing 90 days by value | 0 % on 2 decided (no wins decided in the window) |
| Lost, no reason | 2 |
| No follow-up in 7 days | 2 of 5 |
| Prices asked → in | no RFQs in range |
| Robot delta | +44.2 % median on 1 sealed run |
| Bid clock hours in range | none recorded (31 bid sessions in 90 days, none in this window) |

Every measure computes from real rows. Two things the numbers say about the design: a 90-day hit rate on two decisions is not a rate, so the strip must show the denominator (*0 % · 2 decided*) and grey out under, say, five; and "after due date" is the most informative column on the strip, which argues for keeping it prominent.

## 4. The RPC body against prod

The migration's `SELECT` body, run read-only with literal values for Sep 8 – 16: 6 people, 16 office sessions, 1,123 attributed events (deleted 989, approvals 70, status 27, dispatch answered 24, deposits 10, contracts filed 2, billed 1), 25 jobs referenced, system rows on 5 days. The deletion volume is why the payload aggregates deletions per person, day and table.

## 5. Not measured

`EXPLAIN` on the range query — the 30-day event volume is small enough that no index was added; revisit when a 93-day range feels slow. HR file entries — `person_files` is dev-only data and is not read by the Day book.
