# AI Agent Instructions

> **Start here.** Read [AI_CONTEXT.md](./AI_CONTEXT.md) for the full project overview, code map, and documentation index. This file is the entry point; AI_CONTEXT.md is the source of truth.

---

## Quick Orientation

**PipeTooling** — Workflow management for master plumbers. React + TypeScript + Supabase. 8 roles, 4 major systems (Projects/Workflows, Bids, Materials, Checklist). ~50+ tables with RLS.

---

## Where to Look For...

| Need | Documentation |
|------|---------------|
| Database schema, tables, columns | `PROJECT_DOCUMENTATION.md` → "Database Schema" section |
| User role permissions | `ACCESS_CONTROL.md` → Page/Feature access matrices |
| Adding a new role | `ADDING_A_NEW_ROLE.md` → Step-by-step guide |
| Term definitions | `GLOSSARY.md` → All domain terms and concepts |
| **App crash / outage / Supabase load** (*find why the app crashed*, 503, timeouts; natural language e.g. “Supabase crashed”, “Postgres unhealthy”) | Cursor rule `.cursor/rules/supabase-incident-triage.mdc` (auto) → `docs/runbooks/AGENT_APP_CRASH_INVESTIGATION.md` → `docs/runbooks/SUPABASE_INCIDENT_RUNBOOK.md` → `./scripts/capture-supabase-incident.sh`; client Realtime mitigation `RECENT_FEATURES.md` **v2.454** |
| Recent changes and features | `RECENT_FEATURES.md` → Chronological updates |
| **Project Number** (auto-assigned `Project #N` on `projects.project_number`; org-global `projects_project_number_seq`; **`BEFORE INSERT`** trigger **`projects_set_project_number`** fills only when blank — manual values honored verbatim; **editable** in Edit Project modal as the first form field, **warn-but-allow** duplicates via [`projectNumberLabel.ts`](src/lib/projectNumberLabel.ts); displayed on Edit Project / Projects list / Workflow chip (`Project #N · {name}`) / Dashboard subscribed line (`Project #N: {name}`); other surfaces unchanged) | `RECENT_FEATURES.md` → **v2.557**; `GLOSSARY.md` → **Project Number**; `PROJECT_DOCUMENTATION.md` → `public.projects`; `MIGRATIONS.md` → **`20260519170221`**; [`src/lib/projectNumberLabel.ts`](src/lib/projectNumberLabel.ts), [`src/components/projects/EditProjectForm.tsx`](src/components/projects/EditProjectForm.tsx), [`src/pages/Projects.tsx`](src/pages/Projects.tsx), [`src/pages/Workflow.tsx`](src/pages/Workflow.tsx), [`src/pages/Dashboard.tsx`](src/pages/Dashboard.tsx) |
| **Ledger display prefixes** (jobs **HCP** / bids **bid #** per **`service_types`**: **`ledger_job_prefix`**, **`ledger_bid_prefix`**; dev **Settings** → Service types; null/blank → legacy **`J`** / **`B`**; **`search_jobs_ledger`** + **`search_bids_for_clock`** match **J/B** or configured prefix + digits; **[`ledgerDisplayPrefixes.ts`](src/lib/ledgerDisplayPrefixes.ts)**, **[`LedgerDisplayPrefixContext.tsx`](src/contexts/LedgerDisplayPrefixContext.tsx)**; Edge **`notify-dispatch-request`** / **`notify-estimator-request`**) | `RECENT_FEATURES.md` → **v2.432**; `PROJECT_DOCUMENTATION.md` → **Service Types**; `GLOSSARY.md` → **Bid Number**, **Ledger display prefix**; `MIGRATIONS.md` → **`20260430201832`**, **`20260430202750`**, **`20260430203800`** |
| **Jobs** **Job Summary** tab (dedicated all-status `jobs_ledger` load; optional **HCP #** min filter default **500**, `jobs_jobSummary_minHcpExclusive`; sort unnumbered HCP first; **Mercury** on filtered set; **cost breakdown** **print** — **`printJobSummaryCostBreakdown`**, **Print / Save as PDF**, **`printCostBreakdownJobId`**, supply invoice lines only under **Parts Cost** in print, no duplicate **Invoices** block before **Team Labor**; person-summary **cell drilldown** modals — **`JobSummaryCostCellDrilldownModal`**, **Print** / **Export CSV** in modal, **`domTableToCsv`**, **`.jobSummaryBreakdownInteractive`**; Mercury drilldown **Reassign** → **`MercuryTransactionAllocationsModal`** (**`JobSummaryDrilldownMercuryTable`**, **`canAccessBankingForParts`**) | `RECENT_FEATURES.md` → v2.406, v2.405, v2.404, v2.403, v2.395, v2.396; `PROJECT_DOCUMENTATION.md` → §6 Jobs **Job Summary Tab**; [`src/lib/jobSummaryHcpFilter.ts`](src/lib/jobSummaryHcpFilter.ts), [`src/lib/fetchJobsLedgerWithDetailsForStages.ts`](src/lib/fetchJobsLedgerWithDetailsForStages.ts), [`src/lib/jobSummaryDrilldownMercuryFilter.ts`](src/lib/jobSummaryDrilldownMercuryFilter.ts), [`src/lib/domTableToCsv.ts`](src/lib/domTableToCsv.ts), [`src/pages/Jobs.tsx`](src/pages/Jobs.tsx) |
| **User day schedule** modal (clock strip **name** → one-day **`job_schedule_blocks`** + clock preview, add block, **Dispatch** / **+** / **Close**; day chevrons on **8 / 12 / 4** row above track; responsive header) | `RECENT_FEATURES.md` → v2.399; [`src/components/UserDayScheduleModal.tsx`](src/components/UserDayScheduleModal.tsx), [`src/contexts/UserDayScheduleModalContext.tsx`](src/contexts/UserDayScheduleModalContext.tsx), [`src/components/Layout.tsx`](src/components/Layout.tsx), [`src/components/DashboardTeamActiveClockStrip.tsx`](src/components/DashboardTeamActiveClockStrip.tsx) |
| **Email schedule** (Dashboard clock strip, dev / master / assistant when enabled): **[`ScheduleDayEmailModal`](src/components/ScheduleDayEmailModal.tsx)** → **`schedule_day_email_requests`**; Edge **`schedule-day-email-dispatch`**; **Schedule** / **Queue soon**; dev **Send to** any non-archived user (**`20270523120000`**) | `RECENT_FEATURES.md` → **v2.523**, v2.522; `PROJECT_DOCUMENTATION.md` → Dashboard §8; `EDGE_FUNCTIONS.md` → schedule-day-email-dispatch; `ACCESS_CONTROL.md`; `MIGRATIONS.md` → `20270522120000`, `20270523120000` |
| **Currently In** (clock strip) **name** column: **min-width** from full **name + (s)**; **`stripCurrentlyInFirstCol`**, **`<colgroup>`** first **col**, **`stripCurrentlyInNameWithSuffix`**, horizontal scroll on overflow | `RECENT_FEATURES.md` → v2.400; [`src/components/DashboardTeamActiveClockStrip.tsx`](src/components/DashboardTeamActiveClockStrip.tsx) |
| **Banking** **Mercury** **Sorting** — **top-right** Banking header **Configuration** + **User Card Link** + **Nicknames** (in-page, scrolls with content); **search** field flexes between left toggles and **Refresh from Mercury** / **Reload table**; no visible **Search transactions** label (**`aria-label`** on input); **`mercury_debit_card_user_links.auto_assign_user_id`** + backfill (**`public.users`**, same roster as Tally) | `RECENT_FEATURES.md` → v2.401; `PROJECT_DOCUMENTATION.md` → §15 Banking; `MIGRATIONS.md` → **`20260424161028_mercury_debit_card_auto_assign_user.sql`**; [`src/pages/Banking.tsx`](src/pages/Banking.tsx), [`src/components/BankingUserCardLinkModal.tsx`](src/components/BankingUserCardLinkModal.tsx) |
| **Banking** **Mercury** **Drag Sort** — tab **`?tab=drag_sort`**; **Accounting Labels** sidebar + ledger **Accounting Label** column; **org-wide** **built-in Schedule C defaults** (**`is_system_default`**, **`default_key`**, [`dragSortDefaultLabels.ts`](src/lib/dragSortDefaultLabels.ts)) + DnD assignments (**`mercury_drag_sort_labels`**, **`mercury_transaction_drag_sort_assignments`**); **v2.474** naming + section **Collapse** / **Expand** for all label cards; **v2.472** optimistic bucket stats + memo rows + snappy **`pointerWithin`** / overlay DnD + fire-and-forget save with revert; **v2.473** Equipment Lease / Property Lease built-ins; **Quick label** fullscreen modal (**[`BankingMercuryDragSortFocusModal.tsx`](src/components/banking/BankingMercuryDragSortFocusModal.tsx)**) + [**`dragSortLabelBucketCard.tsx`**](src/components/banking/dragSortLabelBucketCard.tsx) (**`sidebar`** vs **`grid`**); **Undo** max **two** picks (**v2.483**); **v2.484** modal layout (**FocusTransactionCard** header + counterparty/bank wrap, title-row stats on grid tiles, **`Search labels…`** beside **`Remaining unlabeled`**, **tighter** vertical spacing); **Counterparty** header **Counterparty frequency** modal (**v2.506**, **[`MercuryCounterpartyFrequencyModal.tsx`](src/components/banking/MercuryCounterpartyFrequencyModal.tsx)**) | `RECENT_FEATURES.md` → **v2.506**, **v2.484**, **v2.483**, **v2.482**, **v2.481**, **v2.474**, **v2.473**, **v2.472**; `PROJECT_DOCUMENTATION.md` → §15; `MIGRATIONS.md` → **`20260502224616_mercury_drag_sort_org_wide_labels.sql`**, **`20260502202929_rename_drag_sort_rent_lease_builtin_names.sql`**, **`20260502183057_mercury_drag_sort_labels.sql`**, **`20260502191955_mercury_drag_sort_labels_schedule_c.sql`**, **`20260502193138_mercury_drag_sort_label_system_defaults.sql`**; [`BankingMercuryDragSortTab.tsx`](src/components/banking/BankingMercuryDragSortTab.tsx), [`BankingMercuryDragSortFocusModal.tsx`](src/components/banking/BankingMercuryDragSortFocusModal.tsx), [`dragSortLabelBucketCard.tsx`](src/components/banking/dragSortLabelBucketCard.tsx), [`MercuryCounterpartyFrequencyModal.tsx`](src/components/banking/MercuryCounterpartyFrequencyModal.tsx); [`src/pages/Banking.tsx`](src/pages/Banking.tsx) |
| **Banking** **Mercury** **Accounting** — tab **`?tab=accounting`**; **Sorting Ledger** **`Sorting Ledger (n)`** + **Counterparty** **`Name (k)`** counts in the visible list (**v2.503**, [**`bankingMercuryCounterpartyFrequency.ts`**](src/lib/bankingMercuryCounterpartyFrequency.ts)); **Counterparty** header opens **Counterparty frequency** modal (**v2.506**, shared **[`MercuryCounterpartyFrequencyModal.tsx`](src/components/banking/MercuryCounterpartyFrequencyModal.tsx)**, **`counterpartyFrequenciesAboveMin`**, **3+** txs per name); **More filters** (**Sorting Ledger filters** modal; **v2.489**–**v2.492**, **`excludeCounterpartyContains`**, **Exclude counterparty** — **v2.492**); **Approvals** (**`mercury_accounting_label_suggestions`**, **Approve all** bulk — **v2.487**); **Rules** — **`Search rules…`** (**v2.504**) + **Name** / **Label** header sort (**v2.505**, **`sortAccountingRulesForTable`** in [**`accountingRulesTableSearch.ts`**](src/lib/accountingRulesTableSearch.ts)); **New/Edit rule** [**`AccountingRuleFormModal.tsx`**](src/components/banking/AccountingRuleFormModal.tsx) **`SearchableSelect`** + **`list_mercury_drag_sort_label_assignment_counts`** (**`MIGRATIONS.md`** **`20260505231245`**); **Rules** + **Test** / **Apply** (**`mercury_accounting_label_rules`**, **`accountingLabelRuleMatch.ts`**, **`resolveAccountingRuleAmountBounds`** — **v2.486** inclusive **Min**/**Max**); ledger without drag (**[`bankingMercuryDragSortLedger.tsx`](src/components/banking/bankingMercuryDragSortLedger.tsx)**); shared label/assignment tables as Drag Sort; migration **`20260504011219_mercury_accounting_label_rules_and_suggestions.sql`** | `RECENT_FEATURES.md` → **v2.506**, **v2.505**, **v2.504**, **v2.503**, **v2.492**, **v2.490**, **v2.489**, **v2.487**, **v2.486**, **v2.485**; `PROJECT_DOCUMENTATION.md` → §15; `GLOSSARY.md` → **Accounting rules**; `MIGRATIONS.md` → **`20260504011219_mercury_accounting_label_rules_and_suggestions.sql`**, **`20260505231245_list_mercury_drag_sort_label_assignment_counts.sql`**; [`bankingAccountingLedgerFilters.ts`](src/lib/bankingAccountingLedgerFilters.ts), [`bankingMercuryCounterpartyFrequency.ts`](src/lib/bankingMercuryCounterpartyFrequency.ts), [`accountingRulesTableSearch.ts`](src/lib/accountingRulesTableSearch.ts); [`BankingMercuryAccountingLedgerFilterModal.tsx`](src/components/banking/BankingMercuryAccountingLedgerFilterModal.tsx); [`BankingMercuryAccountingTab.tsx`](src/components/banking/BankingMercuryAccountingTab.tsx), [`AccountingRuleFormModal.tsx`](src/components/banking/AccountingRuleFormModal.tsx), [`MercuryCounterpartyFrequencyModal.tsx`](src/components/banking/MercuryCounterpartyFrequencyModal.tsx); [`src/pages/Banking.tsx`](src/pages/Banking.tsx) |
| **Banking** **Mercury** **Team notes** — **Edit note** / **Hide edit** under **Amount**; **read-only preview** sub-row when Mercury / external / org note text exists (**v2.476**); org preview line **single-row ellipsis** (**v2.477**); **v2.478** — **auto-grow** org field from one line, **Save** closes editor, **Close** when draft empty, actions **right**; **v2.479** — **divider under** bank/note band (no seam above child row), **tight** spacing from summary row, **Drag Sort** optional **bank \| note** pipe line (**`mercuryBankDescriptionFromRaw`**); **v2.480** — preview/editor **`colSpan`** split so content **starts at Counterparty** ([**`bankingMercuryNotesSubRowColSpan.ts`**](src/lib/bankingMercuryNotesSubRowColSpan.ts), **`bankingMercuryNotesSubRowColSpan.test.ts`**); toggle under **Amount** only (no **Notes** column — **v2.475**); **`stopPropagation`** on edit control | `RECENT_FEATURES.md` → **v2.480**, **v2.479**, **v2.477**, **v2.476**, **v2.475**; `PROJECT_DOCUMENTATION.md` → §15; `GLOSSARY.md` → **Mercury organization notes**; `MIGRATIONS.md` → **`20260502232908_mercury_transaction_org_notes.sql`**; [`MercuryTxNotesDisclosure.tsx`](src/components/banking/MercuryTxNotesDisclosure.tsx); [`Banking.tsx`](src/pages/Banking.tsx); [`BankingMercuryDragSortTab.tsx`](src/components/banking/BankingMercuryDragSortTab.tsx) |
| **Jobs** **Parts** tab — **Unattributed** toolbar (all jobs with unattributed Mercury lines) + expanded **Cost by person** **Unattributed** (per-job list, **Assign** / **Add {user}**, **Card** / **Account** columns; [`mercuryAllocModalData`](src/lib/mercuryAllocModalData.ts), [`PartsUnattributedMercuryListModal`](src/components/jobs/PartsUnattributedMercuryListModal.tsx), [`PartsUnattributedAllJobsModal`](src/components/jobs/PartsUnattributedAllJobsModal.tsx), [`fetchUnattributedMercuryLinesForManyJobs`](src/lib/fetchUnattributedMercuryForManyJobs.ts)); expanded table omits **Job (no per-person split)** row (**`buildPartsPerPersonCostRows`**) | `RECENT_FEATURES.md` → v2.402, v2.401; [`src/lib/partsPerPersonCostSummary.ts`](src/lib/partsPerPersonCostSummary.ts), [`src/pages/Jobs.tsx`](src/pages/Jobs.tsx) |
| **AIA G702-G703** (**Stages** + **View bill**): **`showAiaG702G703`** (job or invoice **ready_to_bill** / **billed**; **`isStaffFullJobLedgerDetailRole`**); **`AiaG702G703Modal`** — **Change Orders** fields in collapsed **\<details\>** (`detailsGroupId` on template defs) | `RECENT_FEATURES.md` → v2.398; `PROJECT_DOCUMENTATION.md` → §6 Jobs **Stages Tab**; [`src/lib/aiaG702G703Eligibility.ts`](src/lib/aiaG702G703Eligibility.ts), [`src/components/jobs/AiaG702G703Modal.tsx`](src/components/jobs/AiaG702G703Modal.tsx), [`src/lib/aiaG702G703Template.ts`](src/lib/aiaG702G703Template.ts), [`src/pages/Jobs.tsx`](src/pages/Jobs.tsx), [`src/components/jobs/BilledBillViewModal.tsx`](src/components/jobs/BilledBillViewModal.tsx) |
| **Map** (`/map`): **`dev`** / **`master_technician`** / **`assistant`** / **`estimator`**; desktop **pin** / narrow **gear**; entity **table below** map + title-row **Filter**; **Geoman**; faster load — **`useMapPageData`** chunked **`geocode-address-batch`** + **Resolving addresses…**; **Debug** → **Review geocodes**; org **default view** (**Settings**, **`map_default_view_v1`**); **`address_geocodes`** (**`20270520120000_address_geocodes_estimator_map_access.sql`**) | `RECENT_FEATURES.md` → **v2.451**, v2.380; `PROJECT_DOCUMENTATION.md` → §16 Map, **Key Routes**; `MIGRATIONS.md` → **`20270520120000_address_geocodes_estimator_map_access.sql`**; `GLOSSARY.md` → Map page; `EDGE_FUNCTIONS.md` → **`geocode-one`**, **`geocode-address-batch`**; `src/hooks/useMapPageData.ts`; `src/components/map/MapPageView.tsx`; `src/components/settings/MapDefaultViewSettingsBlock.tsx`; `src/lib/map/mapEntitySearch.ts`; `src/lib/mapDefaultViewSettings.ts`; `ACCESS_CONTROL.md` → estimator Layout (mobile Dashboard in hamburger) |
| **Estimates** (`/estimates`) — **`≤640px`**: **`EstimateListCards`** (**`useNarrowViewport640`**); **`estimatesPageShellCss`** / **`estimateListTableScrollWrapStyle`**; Customer experience **Email** preview **`overflow-x`** + **`estimate-email-html-preview-root`** **`img`**; expanded draft **`CustomerNotesTable`** **`overflow-x`**; **`AcceptHeaderBrandPicker`** **`max-width: min(900px, 100%)`**; **Email when customer accepts** — **Notify me** + **`SearchableMultiSelect`** **Also notify** (role-grouped list + separator captions; draft **`accept_notify` `NULL`** → self + all **`master_technician`** on load) | `RECENT_FEATURES.md` → **v2.434**, **v2.430**; `PROJECT_DOCUMENTATION.md` → Estimates (Approach A); `MIGRATIONS.md` → **`20260430213314`**; `EDGE_FUNCTIONS.md` → **accept-estimate**; [`src/pages/Estimates.tsx`](src/pages/Estimates.tsx); [`src/components/SearchableMultiSelect.tsx`](src/components/SearchableMultiSelect.tsx); [`src/components/SearchableSelect.tsx`](src/components/SearchableSelect.tsx); [`src/components/estimates/AcceptHeaderBrandPicker.tsx`](src/components/estimates/AcceptHeaderBrandPicker.tsx) |
| Checklist **Review** (Outstanding by person above inboxes), **Manage** client-side **search** (title / assignee; v2.416), **Roadmap** (`?tab=roadmap` + **`roadmap=`**, multi-roadmap + **Members** — **`ChecklistTechTreeRoadmapBar`**, **`ChecklistTechTreeRoadmapMembersModal`**, **`ChecklistTechTreeTab`**, **`ChecklistTechTreeMapActionIconButtons`**, full-screen + **`.checklistTechTreeExitFs`** — v2.408 / v2.407), **Layout** header **Task** / **Task Dispatch** / **Estimator** for **subcontractor**/**helpers**/estimator (**v2.450**, **`headerTaskDispatchEstimatorEligible.ts`**), **ScheduleDispatch** router (**HubPage** / **JobWeek**, **`ScheduleDispatchAddBlockModal`**), **JobsListCache**, **Documents** `search` + `supply-invoices`, **Layout** gear **Banking** | `RECENT_FEATURES.md` → **v2.450**, v2.416, v2.408, v2.407, v2.380; `MIGRATIONS.md` → **`20270519120000_subcontractor_helpers_estimator_checklist_task_definitions.sql`**, **`20260501205038_fix_checklist_items_rls_recursion.sql`**, **`20270427120000_checklist_tech_tree_multi_roadmap.sql`**; `ACCESS_CONTROL.md`; `PROJECT_DOCUMENTATION.md` → Checklist differentiator, §6 Jobs, Documents page, §16 Map, Jobs **Data** schedule dispatch; `AI_CONTEXT.md`; `src/pages/Checklist.tsx`; `src/lib/headerTaskDispatchEstimatorEligible.ts`; `src/components/checklist/ChecklistReviewInboxes.tsx`; `src/components/checklist/ChecklistTechTreeTab.tsx`; `src/components/checklist/ChecklistTechTreeRoadmapBar.tsx`; `src/components/checklist/ChecklistTechTreeRoadmapMembersModal.tsx`; `src/components/checklist/ChecklistTechTreeMapActionIconButtons.tsx`; `src/pages/ScheduleDispatch.tsx`; `src/contexts/JobsListCacheContext.tsx`; `src/lib/documentsPageTab.ts` |
| Bids system | `BIDS_SYSTEM.md` → Complete workflow documentation |
| Edge Functions API | `EDGE_FUNCTIONS.md` → All Edge Functions with examples |
| Migration history | `MIGRATIONS.md` → All migrations by date and category |
| **Linked** DB: local vs remote migration history mismatch, **`repair`**, **`db push --include-all`** | [Migration history drift (linked project)](#migration-history-drift-linked-project) below |
| Company calendar / Central time (`America/Chicago`), instants vs naive wall time | [`TIME_AND_ZONES.md`](./TIME_AND_ZONES.md); canonical constant **`APP_CALENDAR_TZ`** in `src/utils/dateUtils.ts`; `npm run check:timezone` |
| Apply migrations / run SQL on linked Supabase (when Docker local unavailable) | Cursor **Supabase MCP** — read tool descriptors in `.cursor/.../mcps/` first; `apply_migration` for new files, `execute_sql` for ad-hoc queries; see [Supabase MCP](#supabase-mcp-cursor) below |
| Workflow features | `WORKFLOW_FEATURES.md` → Stage management, financials |
| Clock In/Out, pending sessions, Revoke, accountability, Quickfill Hours, Crew Jobs / Bids, unified job/bid search (**legacy J/B + configured `ledger_*_prefix`**, **`service_type_id`**, **`service_type_name`** + trade pills on jobs — **v2.433**; ledger prefixes — **v2.432**), Pay Report Jobs/Bids | `RECENT_FEATURES.md` → **v2.433**, **v2.432**, v2.100, v2.105, v2.114, v2.120; `PROJECT_DOCUMENTATION.md` → Dashboard, Hours, Quickfill, People; `GLOSSARY.md` → Clock Sessions |
| **Dashboard** **Job Mode** (gear-menu toggle, mobile-first focused view): per-user `localStorage` (**[`jobModeToggle.ts`](src/lib/jobModeToggle.ts)** + **[`useJobModeEnabled.ts`](src/hooks/useJobModeEnabled.ts)**); replaces top of Dashboard with **[`DashboardJobModeCard.tsx`](src/components/jobMode/DashboardJobModeCard.tsx)** — stacked HCP / Job Name / Address header, big **Leave Report** + **Next Job** buttons. Clock-driven advance: pure picker **[`pickCurrentAndNextScheduleBlock`](src/lib/jobModePickCurrentNext.ts)** (jobs-only `job_schedule_blocks`, **14** unit tests covering on-bid, off-schedule job, multi-window same job, last block); Next Job opens **[`JobModeAdvanceNotesModal.tsx`](src/components/jobMode/JobModeAdvanceNotesModal.tsx)** then calls **`applyUpdateFocusDirect`** on **[`UpdateFocusOpenerBridgeContext`](src/contexts/UpdateFocusOpenerBridgeContext.tsx)** (`ClockInOutButton` registers it — closes current `clock_sessions` row + inserts new one for next block, or clocks-in to first when no open session). Rest of Dashboard hidden until **Show full dashboard** (component-local, resets every load); gated by `canLeaveJobFieldReport(role)` | `RECENT_FEATURES.md` → **v2.545**; `PROJECT_DOCUMENTATION.md` → Dashboard **Job Mode**; `GLOSSARY.md` → **Job Mode** |
| Testing without credentials (dev login) | `EDGE_FUNCTIONS.md` → dev-login; `/dev-login?as=<existing-email>` when `import.meta.env.DEV`; email must exist in auth.users (e.g. robert@douglasmining.com); set `VITE_DEV_LOGIN_SECRET` and `DEV_LOGIN_SECRET` |
| Set password (dev) | `EDGE_FUNCTIONS.md` → set-user-password; Settings → Active Accounts → Set password for another user |
| Dev Ignored Tasks, Recently Completed Tasks, button icons | `RECENT_FEATURES.md` → v2.110, v2.111; `PROJECT_DOCUMENTATION.md` → Dashboard; `GLOSSARY.md` → Ignored section |
| approve_clock_sessions RPC 404 / client-side | `RECENT_FEATURES.md` → v2.125; `TROUBLESHOOTING.md` → RPC 404; `TROUBLESHOOT_404.md` |
| Project superintendent assignment, Projects page master/superintendents display | `RECENT_FEATURES.md` → v2.127, v2.128; `ACCESS_CONTROL.md` → superintendent section; `PROJECT_DOCUMENTATION.md` → projects, project_superintendents |
| Job–Project link, linking jobs to projects for multi-phase billing | `RECENT_FEATURES.md` → v2.129; `PROJECT_DOCUMENTATION.md` → Jobs, projects; `MIGRATIONS.md` → 20260320140000 |
| **Jobs** **New Job** **Import** (estimate/bid prefill): header **Import** centered between title and **Link to**; **`JobFormImportEstimateOrBidModal`** dialog title **Import from estimate or bid**; **`newJobFormHasBlockingContent`** / **`initialNewJobServiceTypeIdRef`**; **`search_estimates_for_nav`** + **`search_bids_for_clock`**; **`applyPrefillFromBid`** / **`applyPrefillFromEstimate`**; **Bids** **Copy Bid** → **Open Job** — **`openNewJob({ prefillBidId })`**, **`applyPrefillFromBid`** after **`initDone`** (**v2.493**) | `RECENT_FEATURES.md` → **v2.435**, **v2.493**; `PROJECT_DOCUMENTATION.md` → Jobs (New Job — Import); [`JobFormModal.tsx`](src/components/jobs/JobFormModal.tsx); [`JobFormImportEstimateOrBidModal.tsx`](src/components/jobs/JobFormImportEstimateOrBidModal.tsx); [`BidFormModal.tsx`](src/components/bids/BidFormModal.tsx); [`JobFormModalContext.tsx`](src/contexts/JobFormModalContext.tsx) |
| Workflow step card collapse, collapsed header, button styling | `RECENT_FEATURES.md` → v2.132; `PROJECT_DOCUMENTATION.md` → Workflow; `WORKFLOW_FEATURES.md` |
| Collapse old stages toggle, stage breadcrumb below buttons, no-wrap scroll | `RECENT_FEATURES.md` → v2.135; `WORKFLOW_FEATURES.md` → Collapse Old Stages Toggle, Stage Breadcrumb Layout |
| Workflow Approve/Previous work incomplete, private notes (dev/master/assistant/superintendent) | `RECENT_FEATURES.md` → v2.133, v2.134, v2.137; `ACCESS_CONTROL.md` → Workflow; `WORKFLOW_FEATURES.md` |
| Line Items For Office, supply house invoice linking | `RECENT_FEATURES.md` → v2.136; `WORKFLOW_FEATURES.md` → Line Items For Office; `PROJECT_DOCUMENTATION.md` → workflow_step_line_items |
| Jobs Edit billing comma fields; Workflow line `item_date` + clipboard bulk import | `RECENT_FEATURES.md` → v2.181; `WORKFLOW_FEATURES.md` → Line Items For Office; `PROJECT_DOCUMENTATION.md` → Jobs §6, workflow_step_line_items; `MIGRATIONS.md` → 20270329210000; `src/lib/parseWorkflowLineItemPaste.ts`; `src/components/MoneyDecimalAmountInput.tsx` |
| Client auth (**`AuthProvider`**, **`useAuth`**) | `src/hooks/useAuth.ts`; `src/main.tsx` wraps **`App`** |
| Clock In / Update Focus: assigned jobs auto-load; **no assigned jobs** info toast once per modal (ToastContext stable value + ref); modal field borders / focus; **Complete Clock In** empty-notes toast refocuses notes field (**v2.431**); **Update Focus** blank notes + session job/bid hydrate / **`showUpdateFocusAssociationChip`** (**v2.431**); **Clock In / Review before clock out** — job/bid **summary + Clear** primary path typed search (**`associationChipFromSearch`**); **Trade-specific** ledger prefixes on chip/search labels when **`service_type_id`** present (**v2.432**); **Trade** **`[plum]`**/**`[elec]`**/**`[hvac]`** pills on **job** and **bid** unified rows when **`service_type_name`** present (**`serviceTypeTagForUnifiedRow`**, **v2.433**); clock-out **Missing reports from today (click to make report)** → **`AdditionalReportModal`** (**`canLeaveJobFieldReport`**) | `RECENT_FEATURES.md` → **v2.433**, **v2.432**, **v2.431**, **v2.426**, v2.182, v2.191; `PROJECT_DOCUMENTATION.md` → Dashboard **Clock In/Out**, `ToastContext`; `src/components/ClockInOutButton.tsx`; `src/contexts/ToastContext.tsx` |
| GitHub Pages **GET /route 404** in Network (SPA), Hard Reload document request | `TROUBLESHOOT_404.md`; `src/lib/hardReload.ts`; `index.html`; `PROJECT_DOCUMENTATION.md` → Deployment |
| Jobs Stages + Workflow **job thread notes** (`jobs_ledger_thread_notes`); merged Dispatch **`job_schedule_blocks`** notes as read-only **Schedule** timeline rows (**v2.445**, **`jobThreadScheduleActivity.ts`**, **`fetchJobScheduleBlocksForJob`**); Realtime **`job_schedule_blocks`** refresh expanded thread; **Last activity** preview column; composer **Enter** / **Shift+Enter**; **Arrived** / **Leaving** stamps **Job Detail** modal only (**v2.446**, **`jobThreadStampActions`** / **`submitStamp`**, **`jobThreadNoteStampBody.ts`**); activity list **`activityListMaxHeight`** + scroll snap to newest; composer **auto-grow**; dropped `stage_notes`; **Billed Awaiting Payment** **Edit** beside **Open …** (`editJobIconBesideTimeOpen` in `renderUnifiedStagesTable`); **Bank payments** → **Accounts Receivable Sorting** (org-wide filter JSON in **`app_settings`** **`bank_payments_sorting_config_v1`**, dev writes; legacy per-user local only when no server row; counterparty/note excludes; **Kind** badges **`bank_payments_kind_badges_v1`**), `BankPaymentsModal` + `BankingSortingConfigModal`, `useMercuryLedgerNicknames`; **Accounts Receivable** button + modal **`billedRows`** from **`buildJobsStagesBoardLists(jobs, '').billedRows`** (**`bankPaymentsModalBilledRows`**, ignores Stages search; **Print** still filtered); **Allocations** UI (amount on second row, **Add allocation** footer; **Memo (optional)** on apply); **Edit Job** Mercury unlink via **`remove_jobs_ledger_payment_and_reconcile`** (v2.436 — frees deposit capacity for AR); Dashboard **Unallocated bank deposits** (`count_mercury_transactions_for_bank_payments`, `DashboardArBankUnallocatedBanner`, `useArBankUnallocatedCount`; **v2.494** org banner **`canRoleSeeArBankUnallocatedOrgNudge`** — **dev** / **master_technician** / **assistant**; **`primary`** still **Jobs** **Stages** + **`/accounts-receivable`** via hook) | `RECENT_FEATURES.md` → **v2.494**, **v2.446**, **v2.445**, **v2.444**, **v2.436**, v2.336, v2.334, v2.333, v2.332, v2.331, v2.330, v2.328, v2.183–v2.185; `PROJECT_DOCUMENTATION.md` → Jobs §6, Dashboard, §15 Banking; `GLOSSARY.md` → **Accounts Receivable Sorting**, **Payment unlink**; `MIGRATIONS.md` → **`20260501030427_remove_jobs_ledger_payment_and_reconcile.sql`**, `20260330023918`, `20260418061005`, `20260418063154`, `20260418073359`, `20260418074400`; `src/lib/jobThreadScheduleActivity.ts`; `src/lib/jobThreadActivitySort.ts`; `src/lib/jobThreadNoteStampBody.ts`; `src/components/JobThreadNotesPanel.tsx`; `src/components/jobs/DetailJobModal.tsx`; `src/components/jobs/BankPaymentsModal.tsx`; `src/lib/bankingSortingConfig.ts`; `src/lib/bankPaymentsKindBadges.ts`; `src/hooks/useJobThreadNotes.ts`; `src/hooks/useJobThreadNotesForModal.ts`; `src/hooks/useArBankUnallocatedCount.ts`; `src/pages/Jobs.tsx`; `src/pages/Workflow.tsx`; `src/pages/Dashboard.tsx` |
| Jobs **Stages** **Combine / Separate** (toolbar **far right**; **dev** / **master_technician** / **assistant**): **[`JobsCombineSeparateModal.tsx`](src/components/jobs/JobsCombineSeparateModal.tsx)** — **Combine** **`migrate_job_ledger_costs_and_delete`**; **Separate** **`split_job_ledger_fixtures_to_new_job`** (**`20260511012922`**) | `RECENT_FEATURES.md` → **v2.516**; `PROJECT_DOCUMENTATION.md` → Jobs §6 Stages; `MIGRATIONS.md` → **`20260511012922`**; `ACCESS_CONTROL.md` → master / assistant **People** Stages bullets; `src/pages/Jobs.tsx` |
| Jobs **Sub Labor** New/Edit modal: **Search for crew** (centered field, **aria-label**); non-empty search hides **External**/**Internal**/**Office** blocks with no matches + **No crew match this search** when all empty; **Add Sub** (was Add Subcontractor); **Add line item** below **Specific Work** table; muted **Itemize hours and rate**; **Internal Subs**/**Office Team** headers centered when collapsed; footer **Cancel** → **Print** → **Save** → **Required** (right of Save) → **Delete** | `RECENT_FEATURES.md` → v2.305; `PROJECT_DOCUMENTATION.md` → Jobs §6; `src/pages/Jobs.tsx` |
| Job Detail modal (**v2.447** — **title row** trade pill **`buildServiceTypeTradePill`** / **`serviceTypeTradePill.ts`** + **Edit** when allowed; **Close** bottom footer; **limited** notices centered; no **Service type** **DetailRow**): **`DetailRow`** **`softBox`** on the three date rows; **Status** centered below dates; **Customer Files** / **Job Plans** via **`DetailJobModalFilesPlansRow`** (omit row when both links empty; each column only when that link is set); **Assigned Team** (`team_members`): read-only list; **Last bill date** row = invoice/payment activity (`deriveRecordedBillingActivityDetail`); **Last manual bill date** = `last_bill_date`; `formatJobDetailModalDateYmd`; `JobLedgerStatusPipeline`; **Edit Job** stacked (`JobFormModal` z-index over Detail); materials (`fetchJobMaterialsCostSnapshot`, `JobDetailMaterialsCostSection`, `canExpandJobDetailMaterials`); Mercury **Posted** (`formatMercuryCardChargesPostedDate`) + **Card** (`mercuryDebitCardIdFromRaw`, `useMercuryLedgerNicknames`); **Specific Work** **`[1]`**, **`[2]`, …** + name × count; **Job Total** below Specific Work (full) or below materials (limited), **hidden** for **`subcontractor`** (`showJobDetailJobTotal`); **Job thread notes** (**`JobThreadNotesPanel`** **`activity`**, inner scroll — v2.444; merged **`job_schedule_blocks`** notes — v2.445; **Arrived** / **Leaving** stamps — **Detail** only — v2.446); **Edit** gear + `onEditJobSaved` | `RECENT_FEATURES.md` → **v2.447**, **v2.446**, **v2.445**, **v2.444**, v2.338, v2.337, v2.278, v2.277, v2.276; `PROJECT_DOCUMENTATION.md` → Jobs §6; `GLOSSARY.md` → Last bill date (Job Detail), Other job charges; `src/components/jobs/DetailJobModal.tsx`; `src/lib/serviceTypeTradePill.ts`; `src/components/JobThreadNotesPanel.tsx`; `src/hooks/useJobThreadNotesForModal.ts`; `src/lib/jobThreadScheduleActivity.ts`; `src/components/jobs/JobFormModal.tsx`; `src/lib/fetchJobMaterialsCostSnapshot.ts`; `src/lib/jobDetailModalRole.ts`; `src/lib/formatMercuryCardChargesPostedDate.ts`; `src/lib/mercuryRawDebitCard.ts`; `src/hooks/useMercuryLedgerNicknames.ts`; `src/lib/stagesJobReferenceDates.ts`; `src/lib/formatJobDetailModalDateYmd.ts`; `src/lib/jobsLedgerStatusPipeline.ts`; `src/components/jobs/JobLedgerStatusPipeline.tsx` |
| **Invoice / Update** (Ready to Bill): Linked **`jobs_ledger.customer_id`** required (Jobs: toast + Edit Job **`billingCustomerHighlight`**; Dashboard: toast); Stripe **`create-stripe-invoice`**, **`preview-stripe-invoice`** (pre-create totals; **multi-line** from billable **`jobs_ledger_fixtures`** when Bill Customer **Line on bill** is blank; body **`line_description`** forces **single** line); **`invoice_items`** in **`jobs_ledger_fixtures.sequence_order`** ascending (**v2.527**, **`stripeInvoiceItemsFromFixtures`**); **`lines`** shown in app (**`preview-stripe-invoice`**, **`invoice_preview`** after create, **`get-stripe-invoice-details`**) normalized with **`stripeInvoiceLinesDataForFixtureOrderDisplay`** so **multi-line** order matches **invoice.stripe.com** (**v2.528**, **`EDGE_FUNCTIONS.md`**); **`send-stripe-invoice`** (email from Stripe; **`sent_to_customer_at`** = latest send; **`jobs_ledger_invoice_stripe_email_sends`** append log for modal history), + **`stripe-webhook`**; **`get_jobs_ledger_by_status`** includes **`customer_id`** (`20260330065236`); **`BillCustomerModalProvider`** + **`SendRecordInvoiceModal`** (Jobs, Dashboard, Edit Job **Preview / Stripe bill…**); **Undo out-of-band payment** **`reverse-stripe-invoice-out-of-band-payment`** + RPC **`revert_stripe_oob_invoice_payment`**; **`credit_note.created`** in **`stripe-webhook`**; **`onAfterOobUnwindSuccess`** / **`refreshEditingJobAndHydratePayments`** after unwind (**`RECENT_FEATURES.md`** v2.362–v2.363); **HouseCall Pro** / **Physical invoice** tabs; **Physical** **`send-physical-invoice-email`** (persist invoice + email); **`maybePromoteJobToBilledAfterCustomerInvoice`** (**`promoteJobToBilledIfFullyInvoiced.ts`**) after **Stripe** / **HCP** / **Physical** success — **job** → **billed** when fully invoiced out (**`RECENT_FEATURES.md`** v2.366); collapsible **Line on bill** / **Memo** (default collapsed); memo presets **`billCustomerMemoPresets`** + **`normalizePhysicalInvoiceFooterPlainText`**; **physical** customer email **`buildPhysicalInvoiceEmailBodies`** (bold tagline; summary omits Service date + Issuer block); Jobs **Stages** **Last activity** **Resend invoice email** when one billed Stripe line has a recorded send (**`StripeInvoiceSendFromStripeButton`**); Ham mode instant billed on Jobs (customer-gated); Edit Job Specific Work **(n/500)** Stripe length + scope disclosure | `RECENT_FEATURES.md` → **v2.528**, **v2.527**, v2.366, v2.363, v2.362, v2.325, v2.313, v2.304, v2.303, v2.283, v2.282, v2.190, v2.187; `EDGE_FUNCTIONS.md` → **create-stripe-invoice**, **preview-stripe-invoice**, **get-stripe-invoice-details**, **send-stripe-invoice**, **send-physical-invoice-email**, **reverse-stripe-invoice-out-of-band-payment**, **stripe-webhook**; `MIGRATIONS.md` → `20260330045018`, `20260330065236`, `20260420220523_revert_stripe_oob_invoice_payment`; `src/contexts/BillCustomerModalContext.tsx`; `src/components/jobs/SendRecordInvoiceModal.tsx`; `src/components/jobs/HostedStripeBillPanel.tsx`; `src/components/jobs/UnwindStripeOobPaymentModal.tsx`; `src/components/jobs/StripeInvoiceSendFromStripeButton.tsx`; `src/components/jobs/JobFormModal.tsx`; `src/lib/promoteJobToBilledIfFullyInvoiced.ts`; `src/lib/billCustomerMemoPresets.ts`; `src/lib/physicalInvoiceDocument.ts`; `src/pages/Jobs.tsx`; `src/pages/Dashboard.tsx`; `supabase/functions/_shared/stripeInvoiceItemsFromFixtures.ts`; `supabase/functions/_shared/stripeInvoiceLinesForFixtureOrderDisplay.ts` |
| Edit Job **Billing** accordion: **Partial invoice** (above **Outstanding billing**); **Outstanding billing** (**Date** + **`(+n)`**, **See in Stages**/**Bill**, **`StripeInvoiceSharePanel`** icon toolbar); **Payments received** (**Date**, **Amount ($)**, **Memo**; Stripe-linked row **inset** only—no **thead**-colored body; **`paymentRowsFromJob`** / **`refreshEditingJobAndHydratePayments`** after Stripe **Undo OOB** — v2.363); Mercury **Unlink and remove** + persisted **non-Stripe** invoice payment removal via RPC **`remove_jobs_ledger_payment_and_reconcile`** (**invoice** **`paid`/`billed`** reconcile; rejects Stripe-hosted **`stripe_invoice_id`**; sole row → empty line after refresh — v2.436); read-only **Ref** abbrev + copy (**`abbreviatePaymentReference.ts`**); footer **Delete** uses nested **confirm** modal (not **`window.confirm`**) | `RECENT_FEATURES.md` → v2.436, v2.363, v2.344, v2.336, v2.285; `PROJECT_DOCUMENTATION.md` → Jobs §6; `MIGRATIONS.md` → **`20260501030427_remove_jobs_ledger_payment_and_reconcile.sql`**; `GLOSSARY.md` → **Payment unlink (Edit Job)**; `src/components/jobs/JobFormModal.tsx`; `src/components/jobs/BilledBillViewModal.tsx`; `src/components/jobs/StripeInvoiceSharePanel.tsx`; `src/lib/abbreviatePaymentReference.ts`; `src/lib/invoiceCreatedRelative.ts`; `src/utils/dateUtils.ts` |
| **Banking** (`/banking`): **`product` + `tab`** URL (Mercury Ledger/Sorting vs Stripe Invoices/Data, **Stripe dev-only**); **`BankingStripeInvoicesPanel`** + **`BankingStripeWebhookEventsPanel`**; Jobs **Stages**: **`customerParamForJobsReload`**, thread stats **chunk 200** + **generation** guard, **320ms** debounce | `RECENT_FEATURES.md` → v2.284; `PROJECT_DOCUMENTATION.md` → §15 Banking; `ACCESS_CONTROL.md` → Banking; `MIGRATIONS.md` → `20270410130300`; `src/pages/Banking.tsx`; `src/components/BankingStripeInvoicesPanel.tsx`; `src/components/BankingStripeWebhookEventsPanel.tsx`; `src/pages/Jobs.tsx`; `src/hooks/useJobThreadNotes.ts` |
| Settings **Templates & testing** (dev): **Workflow email (Edge Function)** smoke test for **`send-workflow-notification`**; **`test-email`** / **`send-workflow-notification`** gateway **`verify_jwt`** in `supabase/config.toml` | `RECENT_FEATURES.md` → v2.186; `EDGE_FUNCTIONS.md` → **send-workflow-notification**, **test-email**; `WORKFLOW_EMAIL_TESTING.md` → Quick smoke test; `src/pages/Settings.tsx` |
| Settings **Salaried workday** (collapsible); **`people_pay_config`** self-read for salary Settings; Dashboard **Currently In** scope toggles without overlay frame | `RECENT_FEATURES.md` → v2.206; `MIGRATIONS.md` → `20270331160000`; `src/pages/Settings.tsx`; `src/components/SalaryWorkScheduleSettings.tsx`; `src/components/DashboardTeamActiveClockStrip.tsx` |
| **Impersonation** exit: [`Layout`](src/components/Layout.tsx) mobile **Back**, desktop short **Back** with **`title`/`aria-label`** full phrase; [`Settings`](src/pages/Settings.tsx) mobile **Back to my Account**, desktop **Back** with same a11y | `RECENT_FEATURES.md` → v2.231; `PROJECT_DOCUMENTATION.md` → Impersonation flow; `EDGE_FUNCTIONS.md` → **impersonate-user** |
| Salaried workday **end-time hints** (Day end / session ends, `+1 day`), **split** first-block default; **`salary_sync`** continuous duplicate INSERT guard (`20270402100000`); **continuous** indexed fragments close at **`t_end`** (`20270516120000`); indexed-slot **split RPCs** + split-mode **overlap** guard (`20270403180000`); split sync overlap uses **`work_date`** or clock-in date in template TZ (`20270408153000`); runbook **`SALARY_CLOCK_SESSIONS.md`** | `RECENT_FEATURES.md` → v2.419, v2.249, v2.229, v2.228; `MIGRATIONS.md` → `20270516120000`, `20270402100000`, `20270403180000`, `20270408153000`; `SalaryWorkScheduleSettings.tsx`; `salaryScheduleEndTimeDisplay.ts` |
| Dispatch dismissals, closed note, inbox thread notes; **Dispatch / Estimator inbox** narrow layout (**Expand for thread**, **Dismiss** beside stats — **v2.452**); **My Schedule** photo icon → **`dispatch_requests`** with **`pending_action = 'link_job_pictures'`** (dedupe per **`(job_ledger_id, pending_action, status='open')`**, inbox row renders **Add Customer Pictures URL** → **`useJobFormModal().openEditJob(jobId, { jobPicturesLinkHighlight: true })`** scroll/focus/flash Customer Pictures input, save URL auto-closes the request — **v2.556**) | `RECENT_FEATURES.md` → **v2.556**, **v2.452**, v2.169, v2.136; `MIGRATIONS.md` → **`20260519171140_dispatch_requests_pending_action`**; `GLOSSARY.md` → **Task Dispatch**, **Link Customer Pictures dispatch action**; [`DispatchInboxSection.tsx`](src/components/DispatchInboxSection.tsx); [`EstimatorInboxSection.tsx`](src/components/EstimatorInboxSection.tsx); [`JobFormModal.tsx`](src/components/jobs/JobFormModal.tsx); [`JobFormModalContext.tsx`](src/contexts/JobFormModalContext.tsx); [`Dashboard.tsx`](src/pages/Dashboard.tsx) |
| Superintendent Jobs: Reports + Sub Ledger only (no Stages, Billing) | `RECENT_FEATURES.md` → v2.138; `ACCESS_CONTROL.md` → superintendent; `MIGRATIONS.md` → 20260623190000 |
| Dashboard **Assigned Jobs** / **Superintendent Jobs** (title → **Job Detail**), **Send to Billing** outlined (**`#2563eb`**, **Leave Report** primary blue, v2.409) — **`helpers`** omit **Send to Billing**; **`my_last_report_at`** + **Leave Report** schedule nag (**[`leaveReportScheduleReminder.ts`](src/lib/leaveReportScheduleReminder.ts)**; **12h** silence — v2.411, **`20270507120000_dashboard_my_last_report_at.sql`**, **`20270506120000_update_job_status_disallow_helpers_send_to_billing.sql`**); **View Reports** (**[`JobReportsModal.tsx`](src/components/JobReportsModal.tsx)**) — **`ReportDetailBody`** **`fieldLayout="inline"`** + **`ReportLocationMapsLink`** when list RPCs return **`reported_at_lat/lng`**; superintendent **`reports`** anchor + **`list_reports_*`** parity (**`superintendent_report_job_anchor_allowed`**; **`20270511120000`** … **`20270515120000`** — v2.418); **team Ready to Bill** (`list_ready_to_bill_assigned_jobs_for_dashboard`), subcontractor **Collect Payment** (hosted Stripe invoice after staff **Approve for payment**; Step 1 Job Book always on + search + collapse; Step 2 **Call Dispatch** in footer; **`update-collect-payment-stripe-customer-email`**) + staff **Field: Waiting for Approval** queue (**Line Items** vs Stripe, red mismatch rows, **Prepare Bill** blue/green, **Add Line Items** → Edit Job highlight; Realtime **`jobs_ledger_invoices`**), **Collect Payment** modal centered step titles, in-progress stage banner, Choose from my jobs | `RECENT_FEATURES.md` → v2.418, v2.411, v2.409, v2.344, v2.343, v2.341, v2.339, v2.338, v2.142; `PROJECT_DOCUMENTATION.md` → Dashboard; `MIGRATIONS.md` → **`20270515120000_report_list_rpc_include_coordinates`**, **`20270514120000_list_reports_rpc_superintendent_job_anchor`**, **`20270513120000_superintendent_report_anchor_team_assignment`**, **`20270512120000_superintendent_report_anchor_row_security_off`**, **`20270511120000_superintendent_reports_job_anchor_security_definer`**, `20270507120000_dashboard_my_last_report_at`, `20270506120000_update_job_status_disallow_helpers_send_to_billing`, `20260419180746_collect_payment_complete_on_invoice_paid`, `20260419161731_job_collect_payment_flows`, `20260419154440_list_ready_to_bill_assigned_jobs_for_dashboard`, 20260624000000_allow_superintendent_send_to_billing |
| Dashboard **My Bids** title **My Bids (`n`)** after load (**`myBidsVisibleCount`**); **`primary`**: **My Bids** + **Recent Reports** **collapsed** by default and **no** auto-expand **Recent Reports** on unread (**`RECENT_FEATURES`** **v2.494**) | `RECENT_FEATURES.md` → **v2.494**; [`Dashboard.tsx`](src/pages/Dashboard.tsx) |
| Dashboard **Unallocated bank deposits** — **Dashboard** banner **dev** + **assistant** only (**`canRoleSeeArBankUnallocatedDashboardBanner`**); **master_technician** uses Quickfill / route / Jobs **Stages** paths (**`RECENT_FEATURES`** **v2.497**) | `RECENT_FEATURES.md` → **v2.497**; [`useArBankUnallocatedCount.ts`](src/hooks/useArBankUnallocatedCount.ts); [`Dashboard.tsx`](src/pages/Dashboard.tsx) |
| Dashboard **Lost bids need a reason** — amber banner when **lost** **`bids`** (**`estimator_id`** / **`account_manager_id`**) have blank **`loss_reason`**; navigates to **`/bids?tab=bid-board&lostSummary=1&lostSummaryTab=…`**; **`BidBoardLostSummaryModal`** **`initialStaffTabUserId`** (**`RECENT_FEATURES`** **v2.496**) | `RECENT_FEATURES.md` → **v2.496**; [`DashboardLostBidsMissingReasonBanner.tsx`](src/components/DashboardLostBidsMissingReasonBanner.tsx); [`Dashboard.tsx`](src/pages/Dashboard.tsx); [`Bids.tsx`](src/pages/Bids.tsx); [`BidBoardLostSummaryModal.tsx`](src/components/bids/BidBoardLostSummaryModal.tsx) |
| Remove specific pins, Page pins for assistants | `RECENT_FEATURES.md` → v2.147; `PROJECT_DOCUMENTATION.md` → Settings, Dashboard |
| Bids **Unsent/Working** Kanban (**`working`** column, Clock In quick picks), Bid Board **Unsent / Working Bids**, tab strip **\|** around **Submission & Followup**; **Archive from board** only in **Edit bid** footer (**`BidFormModal`**, **`workingBoardArchiveEligibility.ts`** — **v2.518**) | `RECENT_FEATURES.md` → **v2.518**, v2.369; `MIGRATIONS.md` → `20260422001732_bid_working_board_working_column.sql`, **`20260511015410`**; `src/pages/Bids.tsx`; `src/components/bids/BidsWorkingBoard.tsx`; `src/lib/fetchWorkingBoardClockBidPicks.ts`; `src/components/ClockInOutButton.tsx`; `src/components/bids/BidFormModal.tsx` |
| Bid Board **Bid #** (**trade prefix** + **`{n}`** from **`service_types.ledger_bid_prefix`**, fallback **`B`** — **v2.432**; **Bid Board** **`BidBoardBidNumberMark`** full-prefix **`0.7em`** vs digits — **v2.498**) → **Bid preview**; workflow tab **`h2`** same pattern → preview; Submission notes toolbar row + mobile centered; **`notify-dispatch-request`** `verify_jwt` in **`config.toml`** | `RECENT_FEATURES.md` → **v2.498**, **v2.432**, v2.279; `EDGE_FUNCTIONS.md` → notify-dispatch-request; `src/pages/Bids.tsx` (**`BidWorkflowTabTitleWithPreview`**); `src/components/bidNotes/BidNotesTable.tsx`; `src/components/customerNotes/CustomerNotesTable.tsx` |
| **Bids** **Pricing** — **`bid_count_row_submission_hides`** (omit fixtures from Cover Letter / Approval lists per **`price_book_version_id`**, totals unchanged; migrations **`20270521120000`**, **`20270521120100`** — **`duplicate_bid_to_service_type`** copies hides); **`% of bid revenue`** click-toggle (**eye** only when hidden); **`GenerateUnitCostModal`** + **`unitPriceFromTargetPctOfTotal`** ([`unitPriceFromTargetPctOfTotal.ts`](src/lib/unitPriceFromTargetPctOfTotal.ts)) → **`updateUnitPriceOverride`** (**`RECENT_FEATURES`** **v2.499**); modal preview (**New row total**, hide redundant unit line when count is 1, bold **N**) — **v2.500** | `RECENT_FEATURES.md` → **v2.499**, **v2.500**; `MIGRATIONS.md` → **`20270521120000`**, **`20270521120100`**; `BIDS_SYSTEM.md` → Bid Pricing Assignments / schema; `src/pages/Bids.tsx`; `src/components/bids/GenerateUnitCostModal.tsx`; `src/lib/bidPricingRowCalculations.ts` |
| Bid Board **Weekly bids sent** pivot + **dev** **Estimator labor cost** ( **`clock_sessions`** × **`people_pay_config`** vs sent count / **`bid_value`** ): **`bidBoardWeeklyEstimatorLaborCost`**, **`BidBoardWeeklyEstimatorLaborDevSection`** | `RECENT_FEATURES.md` → **v2.442**, v2.437–v2.441; `BIDS_SYSTEM.md` → Bid Board Weekly bids sent; `src/lib/bidBoardWeeklySentStats.ts`; `src/lib/bidBoardWeeklyEstimatorLaborCost.ts`; `src/components/bids/BidBoardWeeklySentSection.tsx`; `src/components/bids/BidBoardWeeklyEstimatorLaborDevSection.tsx`; `src/pages/Bids.tsx` |
| Bid Board All notes (unified timeline), customer notes cards, `customer_contacts.contact_method` | `RECENT_FEATURES.md` → v2.148; `PROJECT_DOCUMENTATION.md` → Bids; `MIGRATIONS.md` → 20260324120000 |
| Bids **Cover Letter** combined doc: **`buildCoverLetterHtml`** (single **`<p>`**, **`pre-wrap`**, **`line-height:1`**), **Copy** **`text/html`** full document only for Google Docs; **Submission** URL pending ref; bid/customer **`datetime-local`** via **`toDatetimeLocal`** / **`fromDatetimeLocal`** | `RECENT_FEATURES.md` → v2.329; `src/pages/Bids.tsx`; `src/components/bidNotes/BidNotesTable.tsx`; `src/components/bidBoard/UnifiedBidCustomerNotes.tsx`; `src/utils/datetimeLocal.ts` |
| Clock sessions table (time/location, merged notes+job, accountability lines), pending Approve/Reject/Edit order, My Roles Goals gate, `user_dashboard_goals` / `user_daily_goals_ack` | `RECENT_FEATURES.md` → v2.149; `PROJECT_DOCUMENTATION.md` → Dashboard, People Hours, Quickfill, Settings; `MIGRATIONS.md` → 20260329120000; `GLOSSARY.md` → Clock Sessions |
| Job Parts Tally **Transactions** tab: **search** ([`tallyTransactionSearch.ts`](src/lib/tallyTransactionSearch.ts)); **Mercury note** icon ([`MercuryTransactionNoteIcon.tsx`](src/components/icons/MercuryTransactionNoteIcon.tsx)); **`parseTallyJobSplitsJson`** ([`tallyJobSplits.ts`](src/lib/tallyJobSplits.ts)); [`TallyJobTransactionsModal.tsx`](src/components/tally/TallyJobTransactionsModal.tsx); **Clock Out** unlinked Mercury gate [`TallyPreClockOutModal.tsx`](src/components/tally/TallyPreClockOutModal.tsx) + [`mercuryTxRowFromTally.ts`](src/lib/mercuryTxRowFromTally.ts); **MercuryTransactionAllocationsModal** **Transaction's Job Assignment** (**v2.521**) | `RECENT_FEATURES.md` → **v2.521**, **v2.519**, v2.225; `PROJECT_DOCUMENTATION.md` → Jobs §6a |
| Quickfill **section** **`h2`** titles + **dividers**; **Banking sorting** snapshot **Link…** in Person/Jobs (no Link column); **People Hours (new)** — **11-day mini calendar** + unapproved counts (**`PeopleHoursClockStripMiniCalendar`** — **v2.508**); assistance notice + **mobile** day nav (≤640px: date line + **Previous day \| Next day \| Today**; **`peopleHoursClockStripSelectedDay`** **`shiftWorkDateYmd`** — **v2.453**); **Jobs Billing** **Min HCP** filter | `RECENT_FEATURES.md` → **v2.508**, **v2.453**, v2.289, v2.224; [`Quickfill.tsx`](src/pages/Quickfill.tsx); [`BankingSortingSnapshotSection.tsx`](src/components/quickfill/BankingSortingSnapshotSection.tsx); [`QuickfillPeopleHoursNewSection.tsx`](src/components/quickfill/QuickfillPeopleHoursNewSection.tsx); [`PeopleHoursClockStripMiniCalendar.tsx`](src/components/people/PeopleHoursClockStripMiniCalendar.tsx); [`JobsBillingReminderSection.tsx`](src/components/quickfill/JobsBillingReminderSection.tsx); [`peopleHoursClockStripSelectedDay.ts`](src/lib/peopleHoursClockStripSelectedDay.ts) |
| Quickfill **jump** row (**`h1`** below): compact **last-marked** subline (**`formatJumpMarkSublineRelative`** + marker **name**; **`title`** / **`aria-label`** = **Last marked …**); **Difficult people** minimal body copy (**`QuickfillDifficultPeopleSection`**) | `RECENT_FEATURES.md` → **v2.513**; [`Quickfill.tsx`](src/pages/Quickfill.tsx); [`QuickfillDifficultPeopleSection.tsx`](src/components/quickfill/QuickfillDifficultPeopleSection.tsx) |
| Quickfill **Schedule** header (no N open / no Mark history), conflicts prompt, `DispatchAddBlockTimeRange` session strip sizing; **Office** dev edit checklist; **Email** / **Texts** / **Physical** intro row + no outer border; stale tally **Assign** modal split seeding | `RECENT_FEATURES.md` → v2.310; [`QuickfillScheduleSection.tsx`](src/components/quickfill/QuickfillScheduleSection.tsx); [`QuickfillOfficeSection.tsx`](src/components/quickfill/QuickfillOfficeSection.tsx); [`MercuryTransactionAllocationsModal.tsx`](src/components/MercuryTransactionAllocationsModal.tsx) |
| Quickfill **Physical inbox** (note + mark); header-aligned **Task** / **Task Dispatch** / **Estimator Inbox**; Schedule Dispatch hub **Day** tab (`hubTab=day`, `day=` URL) | `RECENT_FEATURES.md` → v2.309; [`QuickfillPhysicalInboxSection.tsx`](src/components/quickfill/QuickfillPhysicalInboxSection.tsx); [`ScheduleDispatchHub.tsx`](src/components/schedule/ScheduleDispatchHub.tsx); [`scheduleDispatchColumnFocus.ts`](src/lib/scheduleDispatchColumnFocus.ts) |
| **Task Dispatch** / **Estimator Inbox** **send** modals: **Reference Job or Bid** + tag toggles + [`buildClockBidsSearchParams`](src/lib/clockBidsSearchParams.ts) + [`BidServiceTypeSearchToggles`](src/components/BidServiceTypeSearchToggles.tsx); **trade** pills on **job** and **bid** search rows (**v2.433**); two-column **Attach** / **Links**; full-width URL rows; **Clock In** job/bid row uses the same toggles | `RECENT_FEATURES.md` → **v2.433**, v2.370; [`DispatchTaskModal.tsx`](src/components/DispatchTaskModal.tsx); [`EstimatorTaskModal.tsx`](src/components/EstimatorTaskModal.tsx); [`ClockInOutButton.tsx`](src/components/ClockInOutButton.tsx) |
| Quickfill **Banking sorting** snapshot: parallel Mercury relations + nickname maps; **Total available** count | `RECENT_FEATURES.md` → v2.222; `PROJECT_DOCUMENTATION.md` → Quickfill; [`BankingSortingSnapshotSection.tsx`](src/components/quickfill/BankingSortingSnapshotSection.tsx) |
| Quickfill **Stages: customer link & customer pictures** (`no-customer-stages`, **`useQuickfillStagesJobsWithoutCustomer`**, **`buildStagesWorkingJobsWithoutPicturesList`**, union metric; **Jobs** → **Stages** pipeline **No customer pictures** — **`stagesWorkingJobsWithoutPicturesFromWorking`**, **`StagesAlertJobListModal`**) | `RECENT_FEATURES.md` → v2.413, v2.415; `PROJECT_DOCUMENTATION.md` → §6b Quickfill, Jobs Stages; `ACCESS_CONTROL.md` → Quickfill; [`QuickfillStagesNoCustomerSection.tsx`](src/components/quickfill/QuickfillStagesNoCustomerSection.tsx); [`Jobs.tsx`](src/pages/Jobs.tsx); [`jobsStagesBoard.ts`](src/lib/jobsStagesBoard.ts) |
| **Quickfill** **Prospects** section: warmth **0–3** + **4+** (`prospectWarmthCounts`), Team last **30** days as **line chart** (**recharts**, **Y** = **Marked + Updated**; `prospectTeamActivityChartData`, **`ProspectTeamActivityLineChart`**) for **dev** / **master_technician** / **assistant**; **`/prospects?tab=prospect-list`**; **`section_id` = `prospects`**; Prospects page **Team** tab: tables + same `loadProspectTeamActivity` | `RECENT_FEATURES.md` → v2.381 / v2.382; `PROJECT_DOCUMENTATION.md` → §6b Quickfill; `ACCESS_CONTROL.md` → Quickfill; `QuickfillProspectsSection.tsx`, `prospectTeamActivity.ts`, `prospectWarmthCounts.ts` |
| **Crew Jobs / Bids** Realtime in **`CrewJobsBlock`**; **`clock_sessions`** trigger syncs crew rows on job/bid assign; **inherit-from-crew-lead deprecated** (**v2.538**) — Crew column / picker removed from **`CrewJobsBlock`**, follower rows materialized to their own assignments by **`20260516154601_freeze_crew_lead_inheritance.sql`**, **`sync_crew_jobs_from_clock`** / **`sync_crew_bids_from_clock`** rewritten to always write the computed assignments and keep **`crew_lead_person_name = NULL`** (**`20260516162434_drop_crew_lead_inheritance_from_sync_rpcs.sql`**) | `RECENT_FEATURES.md` → **v2.538**, v2.223; `PROJECT_DOCUMENTATION.md` → Quickfill, People **Team Costs Tab**; `GLOSSARY.md` → **Crew lead inheritance (deprecated)**, **Clock Sessions**; `MIGRATIONS.md` → **`20260516162434`**, **`20260516154601`**, **`20260402120000_clock_sessions_sync_crew_assignments_trigger.sql`**; [`CrewJobsBlock.tsx`](src/components/CrewJobsBlock.tsx) |
| Dashboard My Team: Clock activity above Active/Pending, plain "Clock activity" label, pending banner full-bar jump + expand | `RECENT_FEATURES.md` → v2.153; `PROJECT_DOCUMENTATION.md` → Dashboard |
| Dashboard Currently clocked in strip (below pins): Today hours; **Everyone / Organization** toggle (**default Organization** when `dashboard_clock_strip_scope` unset — **`readClockStripScopeFromStorage`**, **`stripScopeEligible`** in **`Dashboard.tsx`**, shared **`dashboardClockStripScopeStorage`** with **People → Hours** strip — **v2.453**); **`useDashboardMyTeamSectionState`** keyed job ledger IDs for strip report hints reduce **Jobs worked today** missing-report flicker (**v2.495**); Materials supply house `website_url` + Open website | `RECENT_FEATURES.md` → **v2.495**, **v2.453**, v2.429, v2.163; `PROJECT_DOCUMENTATION.md` → Dashboard §8; `src/pages/Dashboard.tsx`; `src/lib/dashboardClockStripScopeStorage.ts`; `src/hooks/useDashboardMyTeamSectionState.ts`; `src/components/DashboardTeamActiveClockStrip.tsx`; `src/components/people/PeopleHoursDashboardClockStrip.tsx` |
| **Materials** **PO Generator** tab + **`material_po_generator_entries`**; **Supply Houses** invoice **PO #** vs ledger (house **or** null **`supply_house_id`**); **`parsePoGeneratorCodeFromPurchaseOrderName`** (`NNNNN-N` not a generator token) | `RECENT_FEATURES.md` → v2.412; `MIGRATIONS.md` → **`20260428231416_material_po_generator.sql`**, **`20260428232212_material_po_generator_supply_house_optional.sql`**; [`Materials.tsx`](src/pages/Materials.tsx), [`SupplyHousesTab.tsx`](src/components/SupplyHousesTab.tsx), [`parsePoGeneratorCodeFromPurchaseOrderName.ts`](src/lib/parsePoGeneratorCodeFromPurchaseOrderName.ts); `PROJECT_DOCUMENTATION.md` §11 |
| Layout **header** shared **height** (Task Dispatch, Estimator Inbox, Task, **Bid**); Dashboard strip **Assign** optimistic job/bid + **`loadPending` Promise.all**; **Calendar** month grid bottom chips **centered** | `RECENT_FEATURES.md` → v2.214; `PROJECT_DOCUMENTATION.md` → Dashboard **Currently clocked in**, Calendar §7, `Layout.tsx` component notes; `src/components/Layout.tsx`; `src/components/clock-sessions/AssignSessionJobPopover.tsx`; `src/hooks/useDashboardMyTeamSectionState.ts`; `src/components/DashboardTeamActiveClockStrip.tsx`; `src/pages/Dashboard.tsx`; `src/pages/Calendar.tsx` |
| Dashboard **Jobs worked today** (below Clocked in today): collapsible by job; **two-column** table—job link + inline **`[ hours • people ]`**, address on second line; **No job or bid** aggregate row first for unassigned sessions (**`JOBS_WORKED_TODAY_UNASSIGNED_ID`**, muted, no link — **v2.367**); merged orange **`thead`** with **Clocked in today** (**≤640px** single-line titles + horizontal scroll — **`mergedJobsHeaderTitlesOverflowWrap`**; **v2.429**); per-job session detail **`colSpan` 2; `jobsWorkedTodayStripRows`; session **duration** opens strip **Edit time** (`onOpenStripMyTimeEditor`); **Overlap** badge when `hasIntervalOverlapToday`; missing-report **`fa-exclamation`** stable after approving sessions (**`jobLedgerIdsForReportsLookupKey`**, **`useDashboardMyTeamSectionState`** — **v2.495**) | `RECENT_FEATURES.md` → **v2.495**, v2.429, v2.367, v2.231, v2.203, v2.202; `PROJECT_DOCUMENTATION.md` → Dashboard §8; `src/components/DashboardTeamActiveClockStrip.tsx`; `src/hooks/useDashboardMyTeamSectionState.ts` |
| Dashboard **Clocked in today** **Mix** (copy day job %): **`enableCopyDayJobMix`** (dev / master / assistant); per-row copy; **[`CopyDayJobMixModal`](src/components/day-job-mix/CopyDayJobMixModal.tsx)** + **[`dayJobMixPercentages.ts`](src/lib/dayJobMixPercentages.ts)** / **[`dayJobMixApply.ts`](src/lib/dayJobMixApply.ts)**; **[`copyDayJobMixTargetGate.ts`](src/lib/copyDayJobMixTargetGate.ts)** (salary **`origin`** day must be all **closed**); **`leader_replace_clock_session_cluster_mixed`** ([`leaderClockSessionSplit.ts`](src/lib/leaderClockSessionSplit.ts)); **`clockStripWorkDateYmd`** hook field; **Mix** chip matches **Show all** / **Needs attention** height (**`stripClockedInChromeBtnLayout`**, **`RECENT_FEATURES`** v2.428) | `RECENT_FEATURES.md` → v2.281, v2.428; `PROJECT_DOCUMENTATION.md` → Dashboard §8; `src/components/DashboardTeamActiveClockStrip.tsx`; `src/pages/Dashboard.tsx` |
| Dashboard / **Clock In** **View today’s time**: blue clock icon when not clocked in but today has sessions → **[`DashboardMyTimeDayEditorModal`](src/components/DashboardMyTimeDayEditorModal.tsx)** **`clockTimesReadOnly`** (timeline/assign/save-on-close; punch actions off; title **punch times locked**); [`ClockInOutButton.tsx`](src/components/ClockInOutButton.tsx); [`Dashboard.tsx`](src/pages/Dashboard.tsx) **`openMyTimePreviewFromClock`** | `RECENT_FEATURES.md` → v2.281; `PROJECT_DOCUMENTATION.md` → Dashboard **Clock In/Out**, **My Time** |
| Dashboard **My Time** / **Edit time** (this-week-only save, Form vs Visual defaults, timeline + form grid, merge + job-choice modal, `segmentJobOverrides`; **Option A** `can_edit_clock_sessions_for_user` for master/assistant/superintendent; merge-after-split reducer **v2.216**; **pairwise overlap** splits timeline into **one card per session** (`splitClustersWithPairwiseOverlap` / `expandClustersSplitPairwiseOverlaps`); Form **overlap** double border + cluster separators; compact list **no** session-frame border; prior-week gate footer trim; **Overlapping clock times** / **Multiple jobs/bids in this span** / `hasPairwiseClockIntervalOverlap`; coalesced mixed-cluster **Save** per-row **`notes`** — **`partitionMixedClusterEditorSegmentsToRowNotes`**, **v2.371**) | `RECENT_FEATURES.md` → v2.371, v2.289, v2.231, v2.216, v2.193, v2.192, v2.179; `ACCESS_CONTROL.md` → Dashboard matrix; `MIGRATIONS.md` → `20260401190823`, `20260328220000`; `src/components/DashboardMyTimeSection.tsx`; `src/components/DashboardMyTimeDayEditorModal.tsx`; `src/components/my-time-day-editor/`; `src/lib/myTimeDayTimeline.ts`; `src/lib/myTimeDaySavePlan.ts`; `src/lib/myTimeMixedClusterSingleSegmentPartition.ts` |
| Dashboard **NCNS** (team My Time from clock strip): **`record_ncns_and_reject_sessions_for_day`**, **`attendance_incidents`**; extra confirm if **approved** sessions (payroll + trust) | `RECENT_FEATURES.md` → v2.209; `ACCESS_CONTROL.md` → Dashboard matrix; `MIGRATIONS.md` → `20260331232529`; `src/components/DashboardMyTimeDayEditorModal.tsx`; `src/pages/Dashboard.tsx` |
| People **Writeups** unified timeline (**writeups** + read-only **NCNS** from **`attendance_incidents`**) | `RECENT_FEATURES.md` → v2.210; `ACCESS_CONTROL.md` → People matrix; `src/components/writeups/WriteupsContractsSubTab.tsx`; `src/components/writeups/NcnsDetailModal.tsx`; `src/components/writeups/writeupsTimelineTypes.ts`; `src/pages/People.tsx` |
| **Calendar** **My Day** card (both viewports) + **Show weekends** viewport default (no persistence) + dispatch notes on planned chips + mobile compact mini-chip format: **`renderPlannedWorkChips`** shared between My Day card and day-detail modal (long-form `573 · Johnny Ingram` / `8:00 AM–12:00 PM` + muted-indigo `note` subline); **`formatMyDayHeadingLabel`** (Today / Yesterday / Tomorrow / weekday); **`shiftYmd`** day math on the `YYYY-MM-DD` key (no TZ); auto-bump **`currentMonth`** when `myDayKey` exits the visible grid; **`showWeekends = useState(() => !mobileCalendarLayout)`** with no `localStorage` hydration / persistence (mobile starts unchecked, desktop checked, resets every open); **`showMyWorkday`** / **`showRecordedTime`** default `false` (still persisted per user); **`renderShowMyWorkdayToggle`** / **`renderShowRecordedTimeToggle`** / **`renderShowWeekendsToggle`** mount in the header row on desktop and a centered below-grid flex cluster on mobile (**`CALENDAR_MOBILE_CHROME_MQ = '(max-width: 640px)'`**); **`PlannedBlockRow.note`** + `note` column on the **`job_schedule_blocks`** SELECT; grid mini-chips use **`compactScheduleWindow`** (`8 AM – 12 PM`, drops `:00`, en-dash) + space-separated label on mobile only; **No planned work.** centered; Dashboard My Schedule `Today` / `Tomorrow` `<h3>` + **No blocks scheduled.** `<p>` centered | `RECENT_FEATURES.md` → **v2.558**; `PROJECT_DOCUMENTATION.md` → §7 Calendar; [`src/pages/Calendar.tsx`](src/pages/Calendar.tsx); [`src/pages/Dashboard.tsx`](src/pages/Dashboard.tsx); [`src/contexts/JobDetailModalContext.tsx`](src/contexts/JobDetailModalContext.tsx); [`src/lib/jobScheduleChicago.ts`](src/lib/jobScheduleChicago.ts) (`scheduleFormatTimeHm`); [`src/hooks/useMatchMedia.ts`](src/hooks/useMatchMedia.ts) |
| **Calendar** NCNS + **recorded time** + salary (**scheduled** forward-only after today; **PTO** all days; `showScheduledSalaryProjectionForYmd`) | `RECENT_FEATURES.md` → v2.212; `ACCESS_CONTROL.md` → Calendar matrix; `MIGRATIONS.md` → `20260401004452`; `src/pages/Calendar.tsx`; `src/lib/calendarClockedHoursByDate.ts` |
| Bids New/Edit modal: **`SearchableSelect`**, responsive `bid-form-top-fields`, Address + Distance/Plan Pages row, **720px**; **Estimator** / **Account Man** lists — **`loadEstimatorUsers`**: non-archived, not **`helpers`**, not name **delete** (**v2.449**); **Archive from board** when eligible (**v2.518**); Layout **Bid** header alignment | `RECENT_FEATURES.md` → v2.180, **v2.449**, **v2.518**; `BIDS_SYSTEM.md` → Edit Bid Modal; `PROJECT_DOCUMENTATION.md` → Bids; `src/components/SearchableSelect.tsx`; `src/pages/Bids.tsx`; `src/components/bids/BidFormModal.tsx`; `src/components/Layout.tsx` |
| Team feedback (dev): **People → Feedback** (`?tab=feedback`) or **Settings → People & accounts** — **`TeamFeedbackDevSettingsBlock`** (Enabled DB persist, Settings/Eligibility modals, raw detail modal, CSV, dev delete); eligibility overview per-user Reset, submit `reviewer_user_id` from session, `team_feedback_submissions_select_own` migration | `RECENT_FEATURES.md` → v2.162, v2.290; `MIGRATIONS.md` → 20270329140000; `src/lib/teamFeedback.ts`; `src/components/team-feedback/` |
| Settings: Sharing and Adoption merged into **People & accounts** (`settings-people`); no `settings-sharing` jump | `RECENT_FEATURES.md` → v2.165; `PROJECT_DOCUMENTATION.md` → Settings §9; `ACCESS_CONTROL.md` → Settings matrix |
| People **Payroll** (`?tab=pay_stubs`; UI **Payroll**, formerly Pay History): Ledger **Less** + **Additional** (qty × rate modal; **prevailing wage** from approved sessions, **`source_clock_session_id`**, human **`description`**; legacy **`[pw:uuid]`** stripped in UI/HTML — v2.345) + **Net Pay** (gross − Less + Additional); **Partial** installments (`pay_stub_payments`) capped at net; full-width ledger name search; **Print** in ledger; dev delete as red trash icon | `RECENT_FEATURES.md` → **v2.514**, v2.345, v2.330, v2.170, v2.172, v2.173, v2.174; `PROJECT_DOCUMENTATION.md` → People; `MIGRATIONS.md` → `20260420051645`; `GLOSSARY.md` → pay_stub_payments, pay_stub_additional_lines; `src/lib/payStubPayments.ts`; `src/lib/payStubDeductions.ts`; `src/lib/payStubPrevailingWageLine.ts`; `src/components/pay/PayStubLessModal.tsx`; `src/components/pay/PayStubAdditionalModal.tsx`; `src/components/pay/PayStubDeleteIcon.tsx` |
| **Draft Payroll** (prior week default, crew merge): **Cash Due** column; grey row **View**; **Hours** drilldown **`DraftPayrollPersonHoursBreakdownModal`** / **`draftPayrollPersonBreakdown.ts`** / **`payReportAssignmentsBreakdown.ts`**; **Print** (**Week N**, **`N of M paid · Total · Left`**); **dev** stub delete in modal (**`Z_PEOPLE_PAY_MODAL_NESTED`**); **`employee_credit`** on **`person_offsets`**, **Record payment** capped at balance + optional credit | `RECENT_FEATURES.md` → **v2.514**, v2.252; `MIGRATIONS.md` → `20270408163000`, `20270408160000`, `20270408161000`; `GLOSSARY.md` → person_offsets; `PROJECT_DOCUMENTATION.md` → People; `src/pages/People.tsx`; `src/components/pay/DraftPayrollModal.tsx`; `src/components/pay/DraftPayrollPersonHoursBreakdownModal.tsx`; `src/components/pay/PersonOffsetFormModal.tsx`; `Jobs.tsx` (**`get_invoice_allocation_lines_for_jobs`**) |
| People **Housing** tab (`housing_units`, `housing_possessions`); pay stub HTML **Housing** after vehicles | `RECENT_FEATURES.md` → v2.177; `ACCESS_CONTROL.md` → People; `MIGRATIONS.md` → 20270329180000; `PROJECT_DOCUMENTATION.md` → People; `src/pages/People.tsx` |
| People roster **`primary`** / **`superintendent`** on `people`; Pay/Hours via `allRosterNames`; backfill `20260329042321` | `RECENT_FEATURES.md` → v2.178; `MIGRATIONS.md` → 20260329042321; `PROJECT_DOCUMENTATION.md` → People; `src/pages/People.tsx`; `src/pages/Jobs.tsx` |
| **People → Teams** (`?tab=teams`) — **`team_leader_assignments`** tree (**[`PeopleTeamsTab.tsx`](src/components/people/PeopleTeamsTab.tsx)**); dev / master / assistant; **Leader dashboard** dev-only — parity with Settings **Team Hours Sharing** | `RECENT_FEATURES.md` → v2.424; `ACCESS_CONTROL.md` → People Management, Settings matrix; `PROJECT_DOCUMENTATION.md` → **`team_leader_assignments`** |
| **Jobs Reports** **Recurring Email Reports** — **`[RecurringEmailReportsModal.tsx](src/components/jobs/RecurringEmailReportsModal.tsx)`** schedules + recipients; per-recipient **`include_costs`** (digest **Cost** = **`hours × people_pay_config.hourly_wage`** on **`trim(users.name)` ↔ `person_name`**; **—** when missing); Edge **`recurring-job-report-preview`** / **`test-send`** / **`dispatch`**, **`[recurringJobReportCore.ts](supabase/functions/_shared/recurringJobReportCore.ts)`** | `RECENT_FEATURES.md` → v2.425, v2.420; `EDGE_FUNCTIONS.md`; `MIGRATIONS.md` → **`20260430071645`**, **`20260430054614`**; `PROJECT_DOCUMENTATION.md` → Jobs **Reports tab** |
| **People → Users** account field **`users.notes`** (UI **Full name and title**; **Full name, title, and phone** modal with **`users.phone`**); **narrow ≤640px**: email/phone on second line (**`usersTabContactRowStyle`**, **v2.416**); **Search**: hide roster sections with no hits; global **No matches.** under search when all sections empty (**v2.443**, **`usersTabSectionHasVisibleRows`**) | `RECENT_FEATURES.md` → **v2.443**, v2.416, v2.398; `PROJECT_DOCUMENTATION.md` → §5 People **Users**; `MIGRATIONS.md` → `20260229000004_add_users_notes` |
| People Hours: **v2.455** — **Pay** merged into **Hours** (`?tab=pay` / `team_costs` → `hours`); **v2.495** — **`people-hours-sections-nav`** (**`jumpToHoursTabSection`**) **first** below **`people-hours-pay-tools`**; **Week range** flat (**no** bordered card shell, scroll-only anchor **`people-hours-week`**, never collapsible); collapsible bordered sections elsewhere; **`#cost-matrix`** / **`?section=rejected`**; **Dashboard-style clock strip** (**`PeopleHoursDashboardClockStrip`**, **`PeopleHoursClockStripMiniCalendar`**, **`peopleHoursClockStripSelectedDay`**, **`dashboard_clock_strip_scope`** — **v2.453**, **v2.508** Quickfill uses same mini calendar); Correct-day audit **Edit** (crew + clock + add session); **ClockSessionEditSplitModal**; **Highlight by job** on grid; **manual cell blur → My Time** proportional scale of closed sessions or **draft** (`peopleHoursProportionalScale.ts`, `peopleHoursManualDraftSession.ts`; open session → fetch modal); **`saveHours(0)`** after save, **NCNS** off for manual-blur path; **v2.533** — draft sessions persist on **Close** even without a job/bid (**`draftClusterIds`** merged into **`effectiveDirty`** in **`DashboardMyTimeDayEditorModal.requestClose`**, toast copy clarifies *"Close will save it as a pending session"*) | `RECENT_FEATURES.md` → **v2.533**, **v2.495**, **v2.455**, **v2.453**, v2.297, v2.291, v2.171; `PROJECT_DOCUMENTATION.md` → People §5, Quickfill; `ACCESS_CONTROL.md`; `src/components/people/PeopleHoursDashboardClockStrip.tsx`; `src/lib/peopleHoursClockStripSelectedDay.ts`; `src/lib/dashboardClockStripScopeStorage.ts`; `src/components/PeopleHoursDayAuditModal.tsx`; `src/pages/People.tsx`; `src/lib/peopleHoursProportionalScale.ts`; `src/lib/peopleHoursManualDraftSession.ts`; `src/components/DashboardMyTimeDayEditorModal.tsx` |
| **People → Hours** **pending vs payroll visibility** — amber **`! n`** badge in any grid cell where pending closed clock sessions sum to **more** than saved **`people_hours`** (so payroll undercounts that day); cell tinted **`rgba(254,243,199,0.55)`** with inset amber outline; click → **[`PeopleHoursPendingCellPopover`](src/components/people/PeopleHoursPendingCellPopover.tsx)** (per-row reject + **Approve all (n)** → **`approve_clock_sessions`**, **View in My Time** → **`DashboardMyTimeDayEditorModal`**); week-strip roll-up banner **`Pending: N people · H h not yet in payroll`** + **Review & approve** → **[`PeopleHoursBulkApprovePendingModal`](src/components/people/PeopleHoursBulkApprovePendingModal.tsx)**; day-column header amber dot (**`workDateHasAnyPendingExcess`**); person row **`+X.XX pending`** subline (**`personPendingExcessHours`**); pure helpers **[`peopleHoursPendingByCell.ts`](src/lib/peopleHoursPendingByCell.ts)** (**`buildPeopleHoursPendingByCellMap`** / **`summarizePeopleHoursPendingByCell`**, salary-only skip via **`isSalaryOnly`** predicate, closed sessions only excluding rejected/revoked, **`> peopleHoursValue + 1e-9`** so equality never produces noise) — **15 unit tests** in **`peopleHoursPendingByCell.test.ts`**; gated on **`canAccessHours \|\| canAccessPay`**; **v2.537** — grid cell display sum (**`sumClosedPendingClockHoursForCell`**) and cost-matrix **Unapproved** column (**`pendingUnapprovedCountsByWorkDate`**) **also** filter **`rejected_at \|\| revoked_at`** so revoked rows drop off immediately (revoke clears **`approved_at`** but leaves **`rejected_at`** null, so **`pendingClockSessions`** still loads them via the **`approved_at IS NULL AND rejected_at IS NULL`** query — without the explicit filter the cell value and the column count stayed high after **`revoke_clock_sessions`** had already subtracted the hours from **`people_hours`**) | `RECENT_FEATURES.md` → **v2.537**, **v2.533**; `PROJECT_DOCUMENTATION.md` → People §5 **Hours tab**; `GLOSSARY.md` → **Clock Sessions / Pending Clock Sessions**; [`peopleHoursPendingByCell.ts`](src/lib/peopleHoursPendingByCell.ts); [`PeopleHoursPendingCellPopover.tsx`](src/components/people/PeopleHoursPendingCellPopover.tsx); [`PeopleHoursBulkApprovePendingModal.tsx`](src/components/people/PeopleHoursBulkApprovePendingModal.tsx); [`approveClockSessions.ts`](src/lib/approveClockSessions.ts); [`People.tsx`](src/pages/People.tsx) |
| **Quickfill** **Unassigned field time** (`quickfill_section_marks.section_id = 'unassigned-field-time'`, dev / master_technician / assistant) — per (person, work_date) cells where field-type time is paid but the team summary cannot tie it to a job. **v2.546 — approved-clock-only sourcing**: pure helper **`buildApprovedClosedHoursByPersonByDate`** sums approved-closed `clock_sessions` hours per (person, work_date) across every bucket (office / bid / field / unassigned); `computeUnallocatedFieldRows` uses that map as the **source of both candidate keys and `dayHoursRaw`** instead of the legacy `people_hours.hours` lookup, so manual grid overrides can't produce phantom rows. **Salary days require approved clock**: `dayHoursRaw = 8` only when the salary person has at least one approved closed session that weekday (no clock → no row; PTO / no-show salary days no longer show as 8h unallocated). **Pending sessions excluded** — caller's Supabase query already filters `approved_at IS NOT NULL` and the helper re-checks; the Pending Sessions UI (v2.537) is the canonical home for not-yet-approved closed sessions. Pure compute in **[`peopleHoursUnallocatedRows.ts`](src/lib/peopleHoursUnallocatedRows.ts)** (**`computeUnallocatedFieldRows`** mirrors **`derivePersonTeamSummary`** math: **`fieldHours = max(0, dayHoursRaw - overheadOnDay)`**, then **`crewAttributedHrs = dayHoursRaw × pct/100`** — Convention 1, share-of-total-day, matches the **`sync_crew_jobs_from_clock`** trigger and **`teamLabor.ts`** / **`payReportAssignmentsBreakdown`** (**v2.539**); subtracts that and any **`subLaborHrs`** from **`fieldHours`**; emits rows where remainder exceeds threshold) — **21 unit tests** (v2.546 added `buildApprovedClosedHoursByPersonByDate` helper tests + salary-no-clock + pending-session + uses-approved-clock gates; Paige-shaped Office + non-Office crew regression from **v2.539** stayed green). **[`QuickfillUnassignedFieldTimeSection.tsx`](src/components/quickfill/QuickfillUnassignedFieldTimeSection.tsx)** windows **3 / 7 / 14 / 30** days (default **14**) and threshold **≥ 0.25 / 0.5 / 1 / 2 / 4 h** (default **1 h**), persisted to **`localStorage`** (**`quickfill_unassigned_field_window_days`**, **`quickfill_unassigned_field_threshold`**); Realtime on **`people_crew_jobs`** / **`people_crew_bids`** / **`clock_sessions`** (v2.546 dropped the no-longer-relevant **`people_hours`** subscription and its parallel Supabase query — manual grid edits don't trigger reloads of a section that doesn't read them); each row **Open day audit** mounts **`PeopleHoursDayAuditModal`**. Audit modal **v2.537** upgrades — read-only **Dispatch** panel above **Clock sessions** (**`usePersonDayScheduleData`** + **`QuickfillScheduleUserRow`** strip with **`clockSessionsToDispatchSecondaryBands`** for recorded bands; plain-text block list with **`time_start–time_end · job/HCP · note`**; **Open in Schedule Dispatch** deep-link via **`companyWeekStartSundayContaining`** / **`getDefaultWeekRange`**); per-row **Approved** / **Pending** / **Open** status badge with explanatory tooltips; inline **Approve** button next to **Edit** for closed pending sessions when **`canEditCrewJobs`** (calls **`approveClockSessions`**, **`approvingSessionIds: Set<string>`** so only the in-flight row disables, refreshes sessions and bubbles **`onCrewSaved`** so Quickfill drops the row from the list); pending-approval banner in **Job / bid assignments** when no crew assignments yet but pending closed sessions link to a job/bid (**`pendingLinkedSessions`**, labels via **`formatJobLedgerShortLine`** / **`formatBidLedgerShortLine`** + trade prefix map) with **Approve all (N)** for **2+** sessions; **Clock sessions** list grows naturally (no inner **`maxHeight`** / scrollbar — modal **`90vh`** outer scroll handles overflow). Audit modal **v2.545** per-clock-session **`Assign`** popover — every row whose `job_ledger_id` and `bid_id` are both null shows the shared **`AssignSessionJobPopover`** (`src/components/clock-sessions/AssignSessionJobPopover.tsx`) trigger alongside **Approve** / **Edit** in the right-side actions cluster, gated on `canEditCrewJobs && !sessionsUserMissing && !!s.user_id && !s.job_ledger_id && !s.bid_id`; portal `popoverZIndex={1110}` (above modal `1002`), Dispatch quick-picks seeded from `dispatchScheduleAssigneeUserId={s.user_id}` + `dispatchScheduleWorkDateYmd={workDate}`. On save it `UPDATE`s `clock_sessions.job_ledger_id` → **`clock_sessions_sync_crew_assignments_after_job_bid`** trigger (**`20260402120000`**) → **`sync_crew_jobs_from_clock`** auto-populates **`people_crew_jobs.job_assignments`** (per v2.538 the RPC always rewrites); `onSaved` calls `refreshSessions()` + `onCrewSaved?.()` so the Quickfill Unassigned section re-runs `loadAll` + `computeUnallocatedFieldRows`. **This is the canonical fix** for *"session was never linked to a job"* — replaces the v2.543 `Assign a job or bid` CTA's direct `people_crew_jobs` write as the preferred path (CTA remains for the rarer override case). Audit modal **v2.543** UX fix — inline **`Assign a job or bid`** blue-outline button beside *No job or bid assignments for this day.* when **`canEditCrewJobs && !isEditMode`** flips `isEditMode = true` + opens `jobSearchOpen` with empty text in one click (now positioned as the override-only path; per-row **`Assign`** popover from v2.545 is the canonical fix); view-mode subtitle now matches reality — editing keeps the existing copy, **`!isEditMode && canEditCrewJobs`** says *“Click Edit to change assignments or sessions.”* (points at the top-right Edit button), **`!canEditCrewJobs`** says *“View only — you don't have permission to edit this day.”*; the misleading hardcoded *“This day is marked Correct (view only).”* line is gone (modal never actually consulted **`hours_reviewed`**) | `RECENT_FEATURES.md` → **v2.546**, **v2.545**, **v2.543**, **v2.539**, **v2.537**; `PROJECT_DOCUMENTATION.md` → §6b Quickfill, People §5 **Hours tab** (Pending vs payroll); `GLOSSARY.md` → **Unassigned field time / Open day audit**, **Clock Sessions / Pending Clock Sessions**; `ACCESS_CONTROL.md` → Quickfill matrix; [`peopleHoursUnallocatedRows.ts`](src/lib/peopleHoursUnallocatedRows.ts); [`QuickfillUnassignedFieldTimeSection.tsx`](src/components/quickfill/QuickfillUnassignedFieldTimeSection.tsx); [`PeopleHoursDayAuditModal.tsx`](src/components/PeopleHoursDayAuditModal.tsx); [`approveClockSessions.ts`](src/lib/approveClockSessions.ts); [`Quickfill.tsx`](src/pages/Quickfill.tsx) |
| **People → Review** **Team Summary** (dev-only) — embedded iframe + **Open in print view** popup; per-person table built by **`derivePersonTeamSummary`** from one shared **`TeamReviewUnion`** (`loadTeamReviewUnion`); **11 columns** sorted by `r.profit` desc: **Name** · **Hours** · **Overhead hrs** · **Overhead labor** · **Field hrs** · **Gross Revenue** · **Net Revenue** · **Profit (after overhead)** · **Gross/hr** · **Net/hr** · **Profit/hr**; click-to-drilldown via `data-type` modals; **`@media print`** hides modal chrome. **v2.539 (Convention 1)** — crew labor multiplies crew percentages by **`dayHoursRaw`** (share of total session hours, including configured Office overhead job), matching `sync_crew_jobs_from_clock` trigger and `teamLabor.ts` / `payReportAssignmentsBreakdown.ts`; print footnote rewritten ("day total × pct, Office filtered as overhead"); fixes Paige-shaped 1.91h phantom Unassigned field time. **v2.540 (Overhead labor column)** — between Overhead hrs and Field hrs; per-person `-((officeHours + bidHours) × people_pay_config.hourly_wage)` stored as a **negative dollar amount** so it renders `-$X` red via `negStyle` and the team total is negative; field labor intentionally excluded (already subtracted at the per-job level inside Net Revenue, would double-count); drilldown `buildOverheadLaborBody` shows source / wage / Office / Bid split + greyed **For context: this person's field labor** memo row; pure field workers render `—`; profit columns unchanged. **v2.541 (a11y + interactivity polish)** — clickable cells become keyboard-focusable (`tabindex="0"` / `role="button"` / descriptive `aria-label` / Enter+Space activation, `:focus-visible` outline, modal-open captures last trigger and modal-close returns focus to it); auto-refresh defers while a drilldown modal is open (iframe posts `team-summary-modal-open` / `team-summary-modal-close`; parent's `teamSummaryModalOpenRef` + `teamSummaryRefreshPendingRef` + `teamSummaryDrainTick` orchestrate "skip while open, drain on close"); tools row above the table adds substring **Search by name** + per-column click-to-sort (`<th data-sort tabindex="0" role="columnheader" aria-sort>`, `▲`/`▼` indicator, null values sort to bottom, default Profit-after-overhead-desc, `Reset sort` button); footer label switches to **Filtered total · k of N** under search and a persistent caption flags that archived/external-only workers contribute to job revenue but aren't in this table (silent-undercount transparency); `@media print` hides the new tools row + sort indicators; row HTML and footer HTML are now built **inside the iframe IIFE** (`renderTable()` from a single `breakdowns` payload that gained `idx`, `totalHours`, `gross`, `net`, `profitAfterOverhead`, `revPerHour`, `netPerHour`, `profitPerHourAfterOverhead`) — pre-v2.541 parent-side cell builders + `tableRows.join('\n')` + `footerRow` are gone. **v2.542 (popup cache + period selector + modal print)** — popup no longer double-fetches; `teamSummaryDataCacheRef` (a `useRef<{ rows, cacheKey }>`) holds the inline iframe's most recent rows, `buildTeamSummaryCacheKey()` joins `[start, end]` + `reviewOnlyPaidInFull` + sorted `showPeopleForReview` + payConfig wage signature, auto-refresh effect clears unconditionally above the early-returns, popup-only short-circuit checks `cached.cacheKey === buildTeamSummaryCacheKey()` and resolves `Promise.resolve(cached.rows)` (microtask paint, no Supabase, no `Loading…` toast); button renamed **Open in print view** → **Open in new window**. `ReviewPeriod` gains `'this_week'` (Sun→today running), `'last_30_days'` (renamed from `last_month`, same 30-day rolling semantics), `'last_90_days'`, `'this_year'` (Jan 1→today), `'custom'` (two `<input type="date">` From/To with cross-bounding `min`/`max`, range swap on out-of-order entry, *"Pick both dates to set the range."* hint when empty, seeded from current effective range on first switch); `getReviewDateRange` + `getReviewPeriodLabel` learned all 5; both data-loading effects include `reviewCustomRangeStart` + `reviewCustomRangeEnd` in deps; auto-refresh skips loads while custom pair is half-finished. Drilldown modals printable — Print button added to modal header (`.modal-print` + `.modal-header-actions`); click adds `body.printing-modal`, calls `window.print()`, `afterprint` strips the class; `@media print` splits into `body:not(.printing-modal)` (default whole-table) and `body.printing-modal` (hide `<h1>`, `.meta`, `.meta-sub`, `.tools`, `> table`, `.footer-caption`, `.modal-backdrop`, `.modal-print`, `.modal-close`; modal becomes static full-width no-shadow); `closeModal` + Escape handler defensively strip the class. **v2.547 (drilldown modal layout & visibility polish)** — uniform pass across all eight drilldown modals: **Hours breakdown** day list now includes Office and Bid allocations (the modal-only `crewByDateForPerson` builder used to filter out the configured Office job and never read `people_crew_bids` — it now iterates `union.periodCrewRows` (no Office filter) plus a new `union.periodCrewBidRows`, so days that were 100% Office or 100% on a bid no longer surface as `No crew assignment`; field-revenue math is unchanged because that path still uses `crewJobsWithLeadFiltered`), each allocation line gets the **address** appended (`(percent) Job # | Job Name - 12921 FM 20 Kingsbury TX`; widened `HoursBreakdown.dailyRows[].crewAllocations` from `{ hcp, pct, hours }` to `{ hcp, jobName, address, pct, hours }`), running total moved into the title (`Hours breakdown — Abraham · 50.8 hrs`), redundant top + bottom `Total: N hrs` lines + `Daily hours` `<h3>` + `Total = sum of daily hours…` legend caption removed, sub-labor hint now always rendered (was previously gated on `subLaborRows.length > 0`, hiding it precisely when discoverability mattered most). **Overhead hours breakdown** replaces its static *Bucket | Hours | Share* table with a hierarchical session-level layout matching the Hours breakdown idiom — `Office · 17.7 hrs` and `Bids · 12.9 hrs` centered section headers, per-day blocks underneath (`{DOW} {YYYY-MM-DD} · N.N hrs`), per-session lines indented (Office: `(pct) 8:00 AM → 5:30 PM · N hrs`, Bids: `(pct) B249 | Project Name - address · N hrs`); plumbed via new `TeamReviewUnion.overheadSessionsByPerson` (period-only, approved-closed, populated during the existing `overheadSessionsAllTime` iteration in `loadTeamReviewUnion`), new `OverheadSessionLine` type + `TeamSummaryRow.overheadSessions`, `derivePersonTeamSummary` formats `clocked_in_at`/`clocked_out_at` via `Intl.DateTimeFormat` in `APP_CALENDAR_TZ` and resolves bid display fields via `union.bidsById` (period-only `people_crew_bids` fetch + conditional `get_bids_by_ids` RPC keyed on the union of bid IDs), new iframe helper `buildOverheadSessionsSection` reuses the existing `.hours-day-section` / `.hours-day-header` / `.hours-day-allocs` / `.alloc-pct` / `.alloc-jobnum` / `.alloc-jobname` / `.alloc-address` styles; running total in the title (`Overhead hours breakdown — Abraham · 30.5 hrs`), intro paragraph moved to bottom as `.caption`, *Where overhead fits in this person's work* `<h3>` hidden, **Share of total work** column header + cells left-aligned within rightmost column. **Overhead labor breakdown** intro paragraph moved to bottom, summary line `Overhead labor: -$X (X hrs × $Y/hr)` centered, **Share** column left-aligned within rightmost column, *Where it shows up* cell on Field row trimmed to `Already in **Net Revenue**.` (long version still in bottom captions). **Field hours breakdown** running total in title (`Field hours breakdown — Abraham · 17.9 hrs`), `<h3>How field hrs is computed</h3>` collapsed into the first column header (was `Step`), `Where the field hrs went` `<h3>` centered, **Share of field hrs** column left-aligned within rightmost column. **Gross Revenue breakdown** every column header + body cell centered (`tabular-nums` preserved via `class="num"`), running total in title (`Gross Revenue breakdown — Abraham · $872`), explanation paragraph moved to bottom. **Net Revenue breakdown** running total in title (`Net Revenue breakdown — Abraham · $1,766`), explanation moved to bottom. **Profit (after overhead) / Gross/hr / Profit/hr breakdowns** explanation paragraphs moved to bottom (no math change). **Cross-cutting typography** — `· N hrs` muted suffix on per-day headers (`Mon 2026-05-04 · 6.8 hrs`) and Overhead-hours section headers (`Office · 17.7 hrs`) now inherits parent color/weight/size verbatim (only `margin-left: 0.5rem` retained); pre-v2.547 `.day-hours` shrank the suffix to a smaller muted style, splitting one composite metric into two unrelated typographies | `RECENT_FEATURES.md` → **v2.547**, **v2.542**, **v2.541**, **v2.540**, **v2.539**; `PROJECT_DOCUMENTATION.md` → People §5 **Review Tab**; `GLOSSARY.md` → **Team Summary (People → Review)**, **Overhead labor (Team Summary column)**, **Unassigned field time / Open day audit**; [`src/pages/People.tsx`](src/pages/People.tsx) (`derivePersonTeamSummary`, `loadTeamReviewUnion`, `ReviewPeriod` type, `reviewCustomRange{Start,End}`, `getReviewDateRange`, `getReviewPeriodLabel`, `teamSummaryDataCacheRef`, `buildTeamSummaryCacheKey`, iframe `renderTable` + cell builders + sort/filter wiring + modal `Print` button + scoped `@media print`, `buildOverheadLaborBody`); [`src/lib/peopleHoursUnallocatedRows.ts`](src/lib/peopleHoursUnallocatedRows.ts); [`src/utils/teamLabor.ts`](src/utils/teamLabor.ts); [`src/lib/payReportAssignmentsBreakdown.ts`](src/lib/payReportAssignmentsBreakdown.ts) |
| People **Overhead** (`?tab=overhead`, **dev** / **master_technician**): **Simple** vs **Advanced** (`overheadTableViewStorage`); **Overhead office job** modal + **`overhead_office_job_ledger_id_v1`**; **Office Total** / **Overhead %** / **Field Total** (**Advanced**: Office Total after parts, no rule between parts and Office Total; **Overhead %** whole-number **%** — v2.501–v2.502); no **Detail** column — per-day session lines via **$** breakdown modals (**v2.465**–**v2.466**) | `RECENT_FEATURES.md` → **v2.502**, **v2.501**, **v2.466**, **v2.465**, v2.459–v2.463; `PROJECT_DOCUMENTATION.md` → People §5; `ACCESS_CONTROL.md`; `src/pages/People.tsx`; `src/lib/overheadDailyLabor.ts`; `src/lib/overheadTableViewStorage.ts`; `src/lib/overheadOfficeJobSettings.ts` |
| **Schedule dispatch** **Linked** / **+ → Linked copy** (`shared_block_group_id`), group time/note sync, overlap **`excludeIds`**, DnD solo cross-day/assignee + linked cross-day via **`move_job_schedule_block_group`**; **Add schedule block** occupied timeline (person-day drafts, gap-clamped new range, batch save); **block note** quick-edit (hub + job-week **Grid**, **`ScheduleDispatchBlockNoteModal`**, **Cancel** / **Clear** / **Save**; **`scheduleBlockActionChromeStyle`** / linked circular chip); **mobile** (≤640px): transparent sticky first column + white **name pills** on **Person** / **Team member** ([`scheduleDispatchMobileNamePill.ts`](src/lib/scheduleDispatchMobileNamePill.ts), [`ScheduleDispatchHub.tsx`](src/components/schedule/ScheduleDispatchHub.tsx) **`HubPeoplePanel`**, [`ScheduleDispatchGrid.tsx`](src/components/schedule/ScheduleDispatchGrid.tsx)); Banking **Mercury** **`replace_mercury_transaction_splits`**: modal assertion for nullable attribution args vs **`gen types`** | `RECENT_FEATURES.md` → v2.383, v2.378, v2.296, v2.258, v2.257; `MIGRATIONS.md` → **`20260407165443`**, **`20260407061043`**, **`20260407052651`**; `GLOSSARY.md` → Job schedule blocks; `PROJECT_DOCUMENTATION.md` → Jobs; `src/lib/jobScheduleBlocks.ts`; `src/lib/scheduleDispatchAddBlockTimeline.ts`; `src/lib/scheduleDispatchDragEnd.ts`; `src/lib/jobScheduleOverlap.ts`; `src/lib/scheduleBlockActionChromeStyle.ts`; `src/pages/ScheduleDispatch.tsx`; `src/components/schedule/DispatchAddBlockTimeRange.tsx`; `src/components/schedule/ScheduleDispatchGrid.tsx`; `src/components/schedule/ScheduleDispatchBlockNoteModal.tsx`; `src/components/MercuryTransactionAllocationsModal.tsx` |
| **Schedule dispatch** **Not coming in today** (Hub + JobWeek; **dev** / **master_technician** / **assistant** / **superintendent**) — picker footer **Not coming in today** + inline confirm on the **Add job to schedule** modal (`notComingIn` prop, single person + day intent only) → **`recordNotComingInForUserAsStaff`** (`pay_staff_bulk_insert_user_time_off`, single-day **`kind='unpaid'`** + **`note='Not coming in'`**) + bulk **`deleteJobScheduleBlock`** for the day. Cell **chip** (red **Not coming in** / amber **Off**) — centered when the cell is otherwise empty — drives **`cellHasTimeOff`** which hides **Add block** / `+` triangles, disables **`useDroppable`** (DnD bounces), greys cells during placement / picker flows, and ignores click-to-add; existing blocks still edit / delete. Click the red chip → **`ScheduleDispatchUndoNotComingInModal`** (Cancel / **Mark as coming in**) → **`removeNotComingInForUserAsStaff`** → SECURITY DEFINER RPC **`pay_staff_remove_not_coming_in_for_user_day`** (tightly scoped: only `start=end=p_work_date AND kind='unpaid' AND note='Not coming in'`) + post-delete **`sync_salary_clock_sessions_for_user_day`**. Pure helpers in **[`userTimeOffByCell.ts`](src/lib/userTimeOffByCell.ts)** (14 tests) + **[`notComingInTimeOff.ts`](src/lib/notComingInTimeOff.ts)** (`parseRemoveNotComingInResult`, 9 tests). | `RECENT_FEATURES.md` → **v2.535**; `MIGRATIONS.md` → **`20260515233801_pay_staff_remove_not_coming_in_for_user_day`**, **`20270331192000_pay_staff_bulk_insert_user_time_off`**, **`20270331170000_user_time_off`**; `GLOSSARY.md` → **Not coming in (Schedule Dispatch)**, **Job schedule blocks**; `ACCESS_CONTROL.md` → **Dispatch** matrix (`/schedule-dispatch`); [`ScheduleDispatchTimeOffChip.tsx`](src/components/schedule/ScheduleDispatchTimeOffChip.tsx); [`ScheduleDispatchUndoNotComingInModal.tsx`](src/components/schedule/ScheduleDispatchUndoNotComingInModal.tsx); [`ScheduleDispatchAssignJobPickerModal.tsx`](src/components/schedule/ScheduleDispatchAssignJobPickerModal.tsx); [`ScheduleDispatchHubPage.tsx`](src/components/schedule/ScheduleDispatchHubPage.tsx); [`ScheduleDispatchHub.tsx`](src/components/schedule/ScheduleDispatchHub.tsx); [`ScheduleDispatchJobWeek.tsx`](src/components/schedule/ScheduleDispatchJobWeek.tsx); [`ScheduleDispatchGrid.tsx`](src/components/schedule/ScheduleDispatchGrid.tsx); [`userTimeOffByCell.ts`](src/lib/userTimeOffByCell.ts); [`notComingInTimeOff.ts`](src/lib/notComingInTimeOff.ts) |
| **Schedule dispatch** **Hub fast first paint** — **`loadHub`** in [`ScheduleDispatchHubPage.tsx`](src/components/schedule/ScheduleDispatchHubPage.tsx) reshaped into 4 awaited phases (A: jobs ledger + week blocks + users-tab roster parallel; B: team-members for `jobIds`; C: names + archived + **`user_time_off`** parallel; D: **`Promise.allSettled`** salaried + `people_pay_config` wages parallel, both keyed by the already-fetched name map). [`fetchSalariedUserIdSetFromUserIds`](src/lib/salaryPayConfigGate.ts) accepts optional **`opts.nameByUserId?: ReadonlyMap<string, string>`** and skips its own `users` round-trip when provided (back-compat — all five existing callers unchanged). [`fetchUsersTabUserIdsForScheduleDispatchHub`](src/lib/scheduleDispatchHub.ts) + [`fetchUsersTabRosterForScheduleDispatchHub`](src/lib/scheduleDispatchHub.ts) collapse the dev-viewer branch into one **`.in('role', allowedRoles)`** via shared **`USERS_TAB_BASE_ROLES: readonly SupabaseUserRole[]`** (`['dev', ...USERS_TAB_BASE_ROLES]` when `includeDevUsers`). New **`hubUserTimeOffPrimedRef: useRef<{ weekStart, weekEnd, rosterKey } | null>`** records what `loadHub` seeded `hubUserTimeOffByCell` for; standalone time-off effect short-circuits when the triple matches, so first paint no longer issues a second `user_time_off` request immediately after `loadHub` already loaded it (effect still fires for later roster changes — quiet refresh, new assignee). **Net: sequential first-paint round-trips drop from ~7–8 to 4; dev viewers' `users` queries drop from 2 to 1; time-off chips paint in the same step as the People grid (no flash of empty cells, no double request).** No DB / migration / RLS / RPC / Edge changes; no user-visible behavior change | `RECENT_FEATURES.md` → **v2.561**; [`ScheduleDispatchHubPage.tsx`](src/components/schedule/ScheduleDispatchHubPage.tsx); [`salaryPayConfigGate.ts`](src/lib/salaryPayConfigGate.ts); [`scheduleDispatchHub.ts`](src/lib/scheduleDispatchHub.ts) (`USERS_TAB_BASE_ROLES`, `SupabaseUserRole`); [`userTimeOffByCell.ts`](src/lib/userTimeOffByCell.ts) (`fetchUserTimeOffForUsersInRange` / `buildUserTimeOffByCell` reused) |
| **Schedule dispatch** polish — **full-bleed table** (Hub people / Hub jobs / JobWeek scroll containers break out of `<main>`'s **`var(--app-main-pad)`** + page wrapper's **`1rem 1.25rem`** via **`marginLeft/Right: calc(-1 * (var(--app-main-pad) + 1.25rem))`**; WeekNav lifted out of **`scrollRootRef`** so only the table is full-width); salaried **`(s)`** suffix renders on its own line under the name (**`scheduleGridSalarySuffix`** / **`hubPeopleSalarySuffix`** → **`display: block`** + **`lineHeight: 1.1`**, works inside the mobile **`scheduleDispatchMobileNamePill`** because block children break the line regardless of **`white-space: nowrap`**); **archived users hidden from Dispatch** — new **`fetchArchivedUserIdSetForIds(ids)`** in **[`scheduleDispatchHub.ts`](src/lib/scheduleDispatchHub.ts)** (single non-`archived_at` query, empty-input short-circuit, errors return empty so a network blip can't hide everyone), Hub stores **`hubArchivedUserIds`** and filters **`hubAllPeopleRows`** (covers Hub people + Hub jobs surfaces; flows into **`hubUserIdsWithBlocksThisWeek`**), JobWeek runs the helper in **`Promise.all`** with **`fetchUserNamesForIds`** and filters **`rosterIds`** before building **`mergedRoster`** — covers all four roster sources (`fetchUsersTabUserIdsForScheduleDispatchHub`, `jobs_ledger_team_members`, `job_schedule_blocks.assignee_user_id`, embedded `users(name)` via `fetchScheduleJobContext`); **twin white-circle action chrome** — new **`scheduleBlockControlPlateBackgroundStyle`** extracted from **`scheduleBlockLinkedControlPlateStyle`** (so the linked badge composes the same base), edit-note button now **16×16** with **`<ScheduleDispatchBlockNoteIcon size={10} />`** + plate background + same subtle **`scheduleBlockActionLinkedIconButtonStyle`** filter as the linked badge; the heavy halation **`scheduleBlockActionIconButtonStyle`** was deleted (no callers); container **`gap: 0`** + zero **`marginLeft`** so the two perfect circles touch edge-to-edge for an `almost touching` look | `RECENT_FEATURES.md` → **v2.536**; [`ScheduleDispatchHub.tsx`](src/components/schedule/ScheduleDispatchHub.tsx); [`ScheduleDispatchHubPage.tsx`](src/components/schedule/ScheduleDispatchHubPage.tsx); [`ScheduleDispatchGrid.tsx`](src/components/schedule/ScheduleDispatchGrid.tsx); [`ScheduleDispatchJobWeek.tsx`](src/components/schedule/ScheduleDispatchJobWeek.tsx); [`scheduleDispatchHub.ts`](src/lib/scheduleDispatchHub.ts) (`fetchArchivedUserIdSetForIds`); [`scheduleBlockActionChromeStyle.ts`](src/lib/scheduleBlockActionChromeStyle.ts) (`scheduleBlockControlPlateBackgroundStyle`) |
| Bids **Confirm bid sent** attestation modal — optional **Adds to bid note:** → **`bids_submission_entries`** + **`last_contact`** on save ([`Bids.tsx`](src/pages/Bids.tsx)) | `RECENT_FEATURES.md` → v2.383; `BIDS_SYSTEM.md` → Edit Bid Modal, Confirm bid sent |
| **Bids** **Bid Board** **due-date sort** + **Win/Loss audit note** — **`compareBidsForBidBoardDueDate`** (dated ascending, unmarked last); **`insertOutcomeChangeBidNoteAfterSave`** + **`outcomeChangeBidNote.ts`** on **`saveBid`** / **`saveBidAndOpenCounts`** when **`outcome`** changes (**`profileName`** / email) | `RECENT_FEATURES.md` → **v2.507**; **`BIDS_SYSTEM.md`**; [`compareBidsForBidBoardDueDate.ts`](src/lib/compareBidsForBidBoardDueDate.ts), [`outcomeChangeBidNote.ts`](src/lib/outcomeChangeBidNote.ts); [`Bids.tsx`](src/pages/Bids.tsx) |
| **Bids** **Estimators tab** (`?tab=estimators`, viewable by **all roles**, sits right of **Bid Costs**) — days (last 30) × estimators pivot of **`clock_sessions`** linked to bids; each cell stacks bid chips **`N% — B{n} (Project...)`** where **`N = userHoursThatDay / bidAllTimeHours × 100`** (lifetime team denominator) — project clip = first 10 chars + `...`; **Cost mode** toggle (dev only) appends **`{bidValue × pct}k | {bidValue}k`** per chip, with **red** **"no bid value"** when **`bids.bid_value`** is empty; columns = users with **`role = 'estimator'`** **plus** an org-wide augmentation list **`bid_estimators_extra_users`** (dev / master_technician / assistant edit via **Manage columns**); two **SECURITY DEFINER** RPCs **`list_bid_estimators_window_hours`** + **`list_bid_estimators_all_time_hours`** so non pay-access roles can read the pivot; bid chips open **`useBidPreview`**. **Search bar** (**v2.534**) above the table matches each bid's ledger label (e.g. **`BE249`** — prefix or digits), **`project_name`**, and **GC/Builder name** (**`customers.name`** with legacy **`bids_gc_builders.name`** fallback); when a query is active, only day rows with at least one matching bid render, matching chips get an amber pill + bolded number, columns stay stable (no column hiding). Predicate **`bidEstimatorsBidMatchesSearch`** + **`normalizeBidEstimatorsSearchQuery`** covered by 12 unit tests. | `RECENT_FEATURES.md` → **v2.534**, **v2.531**; `MIGRATIONS.md` → **`20260515102040_bid_estimators_tab.sql`**; [`BidsEstimatorsTab.tsx`](src/components/bids/BidsEstimatorsTab.tsx); [`BidsEstimatorsExtraUsersModal.tsx`](src/components/bids/BidsEstimatorsExtraUsersModal.tsx); [`bidEstimatorsTab.ts`](src/lib/bidEstimatorsTab.ts); [`Bids.tsx`](src/pages/Bids.tsx) |
| **Bids** **Bid Board** **empty bid-value alert** — red filled-circle **`$`** button in the **Bid Value** column when a bid is in **"Not yet won or lost"** (**`bid_date_sent`** set + **`outcome`** not **`won`** / **`lost`** / **`started_or_complete`**) and **`bid_value`** is null / `0` / unparseable. Click opens **Edit Bid** (`openEditBid(bid, { focus: 'bidValue' })` → `setBidFormOpen(true)` + `pendingBidFormFocus`); modal effect focuses **`bid-form-bid-value`**, selects its contents, scroll-centers it, and pulses an amber outline + background for **1.6s** (auto-restored); `e.stopPropagation()` so it doesn't trigger row-level handlers. Focus union `'projectName' \| 'gcBuilder' \| 'bidValue' \| null`. Predicate is unit-tested for reuse by other bid surfaces. | `RECENT_FEATURES.md` → **v2.532**; [`bidBoardEmptyBidValueAlert.ts`](src/lib/bidBoardEmptyBidValueAlert.ts); [`bidBoardEmptyBidValueAlert.test.ts`](src/lib/bidBoardEmptyBidValueAlert.test.ts); [`Bids.tsx`](src/pages/Bids.tsx); [`BidFormModal.tsx`](src/components/bids/BidFormModal.tsx) |

---

## Critical Constraints (Non-Negotiable)

1. **Never edit existing migrations** — Append-only. Create new migration to change schema.
   - **Always create new migration files with the CLI** — Run `supabase migration new short_description_of_change` (snake_case description). Never invent timestamps, copy an existing migration file and tweak the name, or add a second file that shares the same `YYYYMMDDHHMMSS` prefix as another file in `supabase/migrations/` (one version number → one SQL file). Edit the generated file, then apply via `supabase db push` (or MCP `apply_migration` on that file).
2. **Always add RLS policies** — Every new table needs SELECT/INSERT/UPDATE/DELETE for all 6 roles.
3. **Update types after schema changes** — Regenerate **`src/types/database.ts`** using **`npm run gen-types:local`** or **`npm run gen-types:linked`** (see **`package.json`**). Manual equivalent: **`supabase gen types typescript --local > src/types/database.ts`** or **`supabase gen types typescript --linked > src/types/database.ts`**. The redirect **`>**` sends **stdout** (generated types) **only** into the file—**stderr** does **not** get written into **`database.ts`**; it prints to the terminal unless you redirect it separately. **Troubleshooting:** if generation fails or the file looks truncated, check terminal stderr (do **not** merge **`2>&1`** into **`>`**, which could mix streams into **`database.ts`**). Optional on a manual one-liner: **`2>/dev/null`** hides noisy CLI chatter but **also hides failures**—avoid it until you need maximum quiet output.
4. **No `any` types** — TypeScript strict mode. Use proper types or `unknown`.
5. **Wrap Supabase calls** — Use `withSupabaseRetry()` from `@/utils/errorHandling`
6. **Test all 8 roles** — dev, master, assistant, subcontractor, helpers (UI label **Helper**), estimator, primary, superintendent
7. **Company time zone** — Use **`APP_CALENDAR_TZ`** from [`src/utils/dateUtils.ts`](./src/utils/dateUtils.ts) (or Edge [`_shared/appTimeZone.ts`](./supabase/functions/_shared/appTimeZone.ts)); do not introduce new `'America/Chicago'` string literals in app/Edge TS (see [`TIME_AND_ZONES.md`](./TIME_AND_ZONES.md), `npm run check:timezone`).

---

## Migration history drift (linked project)

Use this when **`supabase migration list --linked`** shows **Local** and **Remote** columns out of sync (one side empty for a version), or **`supabase db push --linked`** errors with **Remote migration versions not found in local migrations directory** / **Found local migration files to be inserted before the last migration on remote database**.

### Inspect

```bash
supabase migration list --linked
```

Match each row: both columns should show the same **`YYYYMMDDHHMMSS`** for every applied version.

### Remote-only versions (no file in `supabase/migrations/`)

The remote **`supabase_migrations.schema_migrations`** table lists versions that do not exist in the repo (alternate timestamps, old applies, renamed files). They block **`db push`** until the history row is removed.

**Fix** — mark those versions **reverted** (updates the **history table only**; it does **not** run `DOWN` or drop objects):

```bash
supabase migration repair --status reverted VERSION [VERSION ...] --linked
```

The CLI often prints the exact version list when **`db push`** fails; paste that list into the command.

### Push local migrations

After repair, apply pending files:

```bash
supabase db push --linked
```

If the CLI says local files must be applied **before** the last migration on remote (out-of-order timestamps), rerun with:

```bash
supabase db push --linked --include-all
```

Use **`--yes`** in CI or scripts to skip the confirmation prompt.

### Push fails with “already exists”

The migration was **never recorded** in history but DDL is already on the database (e.g. applied manually via SQL or MCP **`apply_migration`**, which does not always write the same history row as **`db push`**).

**Fix** — record the version **applied** without executing SQL:

```bash
supabase migration repair --status applied VERSION --linked
```

Then run **`db push --linked`** again (with **`--include-all`** if needed again). Prefer resolving the root cause: use **`supabase db push`** on the linked project for repo migrations so history and schema stay aligned.

### MCP vs CLI

MCP **`apply_migration`** is for executing DDL when **`db push`** is not available. After using it, **`migration list`** may still show a local file as “not applied” until you **`repair --status applied`** for that version or re-apply via **`db push`** as appropriate. **`npm run gen-types:linked`** after the remote schema matches expectations.

### Verify

```bash
supabase migration list --linked
```

No row should have an empty **Local** or **Remote** column for committed migration files.

---

## Supabase MCP (Cursor)

When this workspace has the **Supabase MCP** server enabled, agents can apply new migration files and run SQL against the **linked** project via MCP (useful when local Docker / `supabase db reset` is not available). **Create the migration file first** with `supabase migration new …`, then edit it; use MCP `apply_migration` only for that generated path. **Always read each tool’s JSON descriptor** under the project’s `mcps` folder before calling — e.g. `execute_sql` for validation or reads, `apply_migration` to apply a file under `supabase/migrations/`. If **`migration list --linked`** and the repo diverge after MCP or manual SQL, use **[Migration history drift](#migration-history-drift-linked-project)**. This does not replace **Critical Constraints** item 3: after schema changes, still regenerate **`src/types/database.ts`** per **Critical Constraints** item 3.

---

## Next Steps

1. **Read [AI_CONTEXT.md](./AI_CONTEXT.md)** — Full overview, file structure, patterns, glossary
2. **Consult the table above** — For your task, open the relevant doc
3. **Review code** — `src/pages/` for UI, `supabase/` for backend
4. **App crash / Supabase outage triage** — [docs/runbooks/AGENT_APP_CRASH_INVESTIGATION.md](./docs/runbooks/AGENT_APP_CRASH_INVESTIGATION.md) (see table **Where to Look For**)
5. **Check RECENT_FEATURES.md** — For context on recent changes
6. **Ask before changing** — Clarify requirements if unclear

---

*Full documentation lives in [AI_CONTEXT.md](./AI_CONTEXT.md). Keep that file updated; this file stays minimal.*
