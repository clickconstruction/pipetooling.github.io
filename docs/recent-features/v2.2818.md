# v2.2818 — Person tab rail: hours never color the dot (2026-09-05)

PR 4 of 4 for the People → Users status column (artifact 747a0b27). The Person tab's roster rail uses the same `buildRailRow` kernel as the Users row, so the owner's decision from v2.2809 — hours are a queue, not an alarm — now holds on both surfaces.

- `deskRailAttention.buildRailRow`: sessions waiting still land in the badge ("23 · doc") and the reasons, and still emit a signal (now gray), but no longer set `attention = 'amber'`. Someone with only hours waiting is green and leaves the rail's **Needs attention** group.
- Tests updated (`deskRailAttention.test.ts`, `rowNeeds.test.ts`); help guide "open a person's desk" says so.
- Nothing else changes: the Users tab already read the grouped needs (v2.2809, v2.2815).
