# v2.2172 — Odometer history sheet on the vehicle card (2026-08-23)

Owner request (mock-up artifact 08714142, built as designed). The card's reading line (`229,950 mi · 9d ago`) is now a quiet button (dotted underline + ›, stops the card click) that opens **Odometer history**.

- **Kernel [`vehicleOdometerHistory.ts`](../../src/lib/vehicleOdometerHistory.ts)** (+5 tests): `sortOdometerEntries` (date asc, created_at tiebreak); `odometerHistoryRows` (newest first; per-row delta miles/days vs the previous reading; kind first / gain / dip / same; logger name); `odometerPace` — per month = (last − first) ÷ days × 30.44, per year × 365.25 (null until 2+ readings on different days with a non-negative span), last-90-days pace from in-window readings with a faster/slower/same trend vs overall; `formatMiles`, `formatSpanDays`, `odometerRowCaption` ("+1,230 mi in 12 days" · "first reading" · "↓ 120 below the previous reading" · "no change · same day").
- **[`VehicleOdometerHistoryModal.tsx`](../../src/components/people/VehicleOdometerHistoryModal.tsx)**: reuses the catch-up sheets' `ModalShell` (now exported from `VehicleCatchUpModals.tsx`); fetches the vehicle's `vehicle_odometer_entries` on open (limit 500); four pace tiles (2×2 on phones, 4 across from 560px), the method caption, an **Add reading** row (today's date, same insert as the catch-up list with `created_by`; Enter saves; refetches the list and calls `onAdded` → the tab's `loadFleet()` so the card's line follows), the reading list, Esc/✕/Close/backdrop to close.
- **[`PeopleVehiclesTab.tsx`](../../src/components/people/PeopleVehiclesTab.tsx)**: trigger on the card ("No reading yet" stays plain text), `odoHistoryVehicleId` state, modal mounted beside the catch-up sheets. Render test: opens the sheet from the trigger, sees the dialog + first reading, adds a reading (button enables on input, clears on save), closes.
- Help: `manage-company-vehicles.md`. Verified on the dev build at 375px (sheet opens without opening the panel; prod has one reading per vehicle today so the tiles show their honest "need one more reading" state). Client-only.
