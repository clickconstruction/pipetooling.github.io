# v2.2660 — Email wording PR 4: polish (2026-09-02)

- `gc-statement-email-dispatch` adopts `resolveServerEmailWording` (type `gc_statement_scheduled`, vars `{{date}}`/`{{default_subject}}`) — the "Click Plumbing open balances:" subject prefix baked into `render.ts` becomes editable through the template instead of an issuer-settings plumb-through (the other baked name, weekly-movement's subject, was already covered by its v2.2659 template). Redeploy with the stream batch.
- Templates tab: Test buttons on the Customer Emails + Digest Emails cards (same `openTestEmail` flow as User Management — sends to the chosen test target; catalog vars render as literal tokens in a test, which is the point: you see the wording).
- Dead wood: `login_as` template removed from the union/defaults/tab (editable since forever, read by no sender — any stored DB row is inert and harmless); `supabase/functions/test-email/index-with-resend.ts` (unreferenced duplicate) deleted.
- `EMAIL_TEMPLATE_DEFAULTS` now spans 25 types (test re-pinned with the arithmetic in a comment).
