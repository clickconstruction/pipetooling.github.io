# v2.2906 — Papercut batch B3: the vendor quote page keeps typed prices reachable after an error, saves no draft on a dead link, and says what it saved and what's missing (2026-09-05)

Journey map Tier-3 row **B3** (dossier `vendor-answers-rfq` — J23-2, J23-3, J23-4, J23-5, J23-6, J23-N1; cluster C54). Public page `/q/:token` only — [`src/pages/SupplyHouseQuotePage.tsx`](../../src/pages/SupplyHouseQuotePage.tsx). Client only; no migration, no edge-function change. `PrepareFixtureCopyModal`, `RfqDeskModal` and the estimate accept page untouched.

## Per item

| Item | Finding | Status |
|---|---|---|
| 1 | Render the form under the error/closed state and reset `error` (J23-3, J23-N1) | **done** — a submit failure no longer replaces the form. `error` split into `loadError` (404 / incomplete link / fetch failed — no page to show) and `submitError` (rides the sticky footer as a `role="alert"` notice; Send reads **Try again**; cleared at the start of the next attempt). A 410 on submit flips `page.status` to `closed` so the same closed screen handles both the cold-open and the under-the-thumb close. The closed and can't-load screens render a **typed-work recap** from the localStorage draft — "Your typed prices stayed on this phone — nothing was sent." + each line's `$412.50` / `can't supply` / note + the quote-level extras (freight, good-until, who). On a cold-open closed link there are no `page.lines`, so the recap comes from the draft itself (page order first, then anything left in the draft). |
| 2 | Don't write a draft key on 404/closed (J23-4) | **done** — `shouldPersistDraft({ token, pageStatus, done })`: the save effect runs only for a token with a loaded, non-closed page and nothing sent. Nothing here ever deletes a draft; only a successful submit does. |
| 3 | Promote "your entries save on this phone" out of the 12.8 px paragraph (J23-2) | **done** — clause removed from the intro paragraph; the sticky footer carries a third line: "Saves on this phone as you go" before anything is typed, "Saved on this phone · nothing is sent until you tap Send quote" once the draft has any work (freight alone counts). |
| 4 | Count freight / valid-until in the footer or mark their silence (J23-6) | **done** — footer line 2: `Freight $45.00 · good until 2026-10-03`, `No freight quoted · no expiry date`, or `Freight needs a number · …` when the freight box holds text the submit would silently drop. Page bottom padding grew to 9.5rem for the taller footer. |
| 5 | Note that the `/q/` lane flattens price basis while Rung E/G record lots (J23-5) | **done (docs) + one-line UI hint** — `docs/SUPPLY_HOUSE_RFQ_PLAN.md` Phase 2 and `docs/RFQ_ROUND2_PLAN.md` Rung G carry the divergence note. The hint: each price box's placeholder is `$ each` or `$ per ft` (`priceBasisHint(unit)` — footage units `ft / lf / feet / lin ft / '`). No basis picker, no lot capture on the vendor page (Rung G's "vendor-page lots later"). |
| — | A failed load (fetch error / 5xx) | **bonus** — offers a **Reload** button; 404 and incomplete-link do not (reloading a dead link changes nothing). |
| — | J23 P4 — render coverage | **done** — [`SupplyHouseQuotePage.render.test.tsx`](../../src/pages/SupplyHouseQuotePage.render.test.tsx): form + footer copy + per-ft hint; typing counts lines and freight and persists under `rfqQuoteDraft_<token>`; failed submit keeps the form and Try again succeeds; 410 → closed recap with the draft kept; cold closed link recaps an earlier draft / shows plain when none; 404 writes no key and no Reload; closed writes no key; failed load offers Reload. |

## Kernel

New [`src/lib/rfq/quotePageState.ts`](../../src/lib/rfq/quotePageState.ts) (+ `quotePageState.test.ts`, 23 tests): `quotePageView` (`loading | dead | closed | done | form` — there is deliberately no submit-error view), `quoteFooterVisible`, `shouldPersistDraft`, `typedQuoteWork`, `quoteFooterLines`, `countAnswered` / `lineAnswered`, `strToCents` / `formatCents` (moved out of the page), `priceBasisHint`.

## Docs

- Help guide `send-a-supply-house-a-quote-link.md` — vendor-side bullets: footer counts freight/good-until, save promise in the footer, failed send keeps the form, closed page recap; "When links die" names the vendor's wording ("Nothing needed — thanks for looking") and the recap.
- `docs/SUPPLY_HOUSE_RFQ_PLAN.md` — Phase 2 bullet for the page (B3 behaviour) + price-basis divergence note; `docs/RFQ_ROUND2_PLAN.md` Rung G — same note under the lots decision.

## Not done / out of scope

- No basis or lot capture on the vendor page (Rung G sequencing: office-side first).
- No per-visit telemetry (repeat opens still invisible; first-open `viewed_at` only) — J23 evidence gap, not in B3.
