# v2.2340 — Preview email button on Bill Customer (2026-08-26)

Owner ask ("Do we not have an email preview button?"): the invoice email was
assembled only at send time — nowhere to see it.

- `SendRecordInvoiceModal.tsx`: new **Preview email** button beside the PDF
  Preview in the physical-invoice tab. Builds
  `buildPhysicalInvoiceEmailBodies(physicalDocPreview)` — the same doc the
  send path uses, so the preview is byte-for-byte the send — and opens the
  HTML in a new tab (text/html blob, sync build so pop-up blockers stay
  quiet). The preview page wraps the body with a gray context line: "Preview
  — nothing has been sent · To · Subject · PDF rides along as an
  attachment"; object URL revoked after 60s.
- Help guide `ready-to-bill-pipeline.md` documents both preview buttons.
