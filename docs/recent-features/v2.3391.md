# v2.3391 — Drive pass: `DRIVE_CONTRACT_ROOTS`, the folder the signed contracts live in (2026-09-14)

Edge function `drive-contract-scan` changed (redeploy); no migration. Follows v2.3390 the same day: the owner shared `https://drive.google.com/drive/folders/1cOTvZrJFTUlxTiUMoESdMtTRvQgxft60` with the intake service account as Viewer, and it is not the jobs root the scan read.

- **`DRIVE_CONTRACT_ROOTS`** (project secret, comma-separated folder ids; set to that folder on 2026-09-14): the scan's roots are `DRIVE_JOBS_FOLDER_ID` plus these; each root's child folders are job folders; a file sitting directly in a root is matched by its own name (the address or job number is usually in it). Either secret alone is enough to run; neither → 503 with the setup pointer. The response gains `roots`.
- `docs/EDGE_FUNCTIONS.md` → drive-contract-scan updated.

Deploy after merge: `supabase functions deploy drive-contract-scan --project-ref yewfzhbofbbyvkvtaatw`, then the dev-run pass from the sweep's ⋯.
