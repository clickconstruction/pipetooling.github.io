# v2.2494 — Job-share links mint on share.clicktooling.com (2026-08-29)

The last user-visible pipetooling.com surface after the domain cutover: the
job-share front (owner report — shared jobs still texted pipetooling links).

- **Cloudflare (done via dashboard, no repo change)**: `share.clicktooling.com`
  added as a second custom domain on the `job-share-preview` Worker
  (clicktooling.com zone). The Worker rewrites upstream references to the
  request's own origin (`u.origin`), so it needed no code change — both
  domains serve identical cards. `share.pipetooling.com` stays attached
  forever for links already texted.
- **Client**: `JOB_SHARE_PREVIEW_BASE_URL` in `src/lib/jobShare.ts` →
  `https://share.clicktooling.com` (+ test). Comment-only touches in
  `ShareJobButton.tsx` / `appOrigin.ts`.
- **Verified before the flip**: `https://share.clicktooling.com/?t=probe`
  serves the Worker's 404 HTML with the app link pointing at clicktooling.com
  (the `APP_ORIGIN` edge-fn secret was already set at cutover), and a real
  token round-trip after the flip.
- Docs: `EDGE_FUNCTIONS.md` job-share blockquote (two-domain reality),
  `DOMAIN_CUTOVER.md` step-5 share bullet marked DONE.
