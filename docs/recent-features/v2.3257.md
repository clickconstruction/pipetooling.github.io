# v2.3257 — Bill-tab job-account note (2026-09-10)

The one placement from the job-account follow-up (v2.3160 job window, v2.3161 Dashboard) that hadn't shipped: a note inside the billing flow itself. Mockup-approved before build.

## `JobFormBillJobAccountNote` (new) — rendered by `JobFormModal`'s bill region

- Sits between `MoneyLifecycleBar` ("Remaining to bill") and `InvoicesSectionHeading`, embedded (job window) mode only — the standalone New Job form has no job yet. Same office-role gate as the header storefront icon (dev / master / assistant-like).
- Reuses `useJobAccountShares` (v2.3160) for the packet and the window's existing `supplyInvoiceLines` via `jobAccountSplitFromLines` for the flagged dollars — no new reads.
- Copy from the pure `billTabJobAccountNote()` (ledger kernel): headline "Job account on file with {house}" (house = contact label minus the desk suffix, `jobAccountHouseShortName`), "packet sent {date} by {name}", "If material invoices on this job go unpaid, {house} bills the property owner — not you.", plus "$X of this job's unpaid supplier invoices are on the account." only when flagged dollars exist. Null → renders nothing.
- "Costs →" button calls `onRequestRegion('costs')` when the window provides it (Costs tab holds the invoice detail).
- Deliberately quiet for the "flagged but no packet" mismatch — that is the Dashboard card's job, not the biller's.

## Tests / docs

- Kernel: `billTabJobAccountNote` (null, newest-send house, desk-suffix strip, flagged line gating, email fallback). Render smoke: three states + Costs link wiring (`JobFormBillJobAccountNote.render.test.tsx`).
- Help: job-window-tabs (Bill tab) + mark-an-invoice-on-a-job-account (where it shows) updated.
