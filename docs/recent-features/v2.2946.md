# v2.2946 — Pay-week links: Hours → Draft Payroll → Tally, and "Mark payroll" for every payroll-access role (2026-09-06)

Journey map **Tier 5 X3** (J7-9 gap — "the pay week is a 6-surface checklist with no home; Mark payroll on Tally is dev-only and nothing points Hours → Draft Payroll → Tally"). Tier 5 train T5-03. Will's call 2026-09-06: build. One migration (`20260906130000_tally_payroll_flag_payroll_access.sql` — **`supabase db push` after merge**); no edge function.

## What shipped

- **Kernel** [`src/lib/people/payWeekLinks.ts`](../../src/lib/people/payWeekLinks.ts) (+6 tests): `canMarkTallyPayroll({isDev, canAccessPay})`, `foldHoursApproved` / `hoursApprovedChipCopy` (the chip's count + the Draft Payroll default week from `previousCompletePayWeek`), `draftPayrollAllGenerated`, `TALLY_PAYROLL_HREF` (`/tally?tab=transactions`), the Tally pointer copy.
- **Link 1 — Hours → Draft Payroll** (`People.tsx` + new `HoursApprovedNudgeChip`): the two Hours approval surfaces that count (`PeopleHoursApprovalsQueueModal`, `PeopleHoursPendingCellPopover`) gain an optional `onApproved(count)`; the bulk-approve modal's `onApproved` folds in uncounted. After an approval, pay-access viewers see a green chip on the Hours tab — "6 sessions approved · Draft payroll for Aug 24 – 30 →" — that sets the period to that week, switches to Pay Stubs and opens Draft Payroll; open or dismiss clears it. Assistants (who approve but have no pay access) never see it.
- **Link 2 — Draft Payroll → Tally** (`DraftPayrollModal`): when every person with hours has a report for the period (`bulkMissingCount === 0` and ≥1 stub in range), a green status row under the buttons says what comes next with an **Open Tally →** link.
- **Gate — Mark payroll for payroll access** (`JobTally.tsx` + migration): `canMarkPayroll = isDev || usePeopleAccess().canAccessPay` gates the Mark payroll button, the Payroll ✓ / Unmark banner, the payroll total line and the confirm modal. DB: `mercury_tally_payroll_flags` policy `is_dev()` → `has_payroll_access()`; `set_tally_payroll_flag` authorization line likewise. **Payroll rules** (button, modal, `mercury_tally_payroll_rules`, `bulk_apply_tally_payroll_rule_flags`) stay dev-only, deliberately — configuration, not the weekly chore.
- **Guides**: new `run-the-pay-week` (Office · dev, master, controller); `tally-payroll-marking` opened to controller + master and reworded; `clear-the-hours-approvals-backlog` names the chip.

## Verify

Dev on People → Hours: approve one pending session → chip "1 session approved · Draft payroll for <last Sun–Sat> →" → tap → Pay Stubs tab, Draft Payroll open on that week. Generate Remaining until "No one needs a report" → green "Open Tally →" row. Impersonate a controller on /tally after the push: **Mark payroll** on an unsplit row → confirm → Payroll ✓; before the push it fails 42501 (the client gate is ahead of the DB by design — push right after merge). Assistant: no chip, no Mark payroll.
