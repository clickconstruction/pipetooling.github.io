# v2.1991 — Portal globe entry points (2026-08-21)

Portal train PR 4: the staff-facing half.

- `CustomerPortalGlobeButton` (self-contained button + modal, office-only —
  renders null otherwise): mint-or-fetch the link on open, Copy / Preview as
  customer / Rotate / Turn off, plus an As customer | As GC audience toggle.
  Confirms rotate/revoke through the app confirm dialog.
- Placements: Customers rows (commercial-typed customers default to the GC
  audience), the shared Pipeline customer line (`renderJobCustomerLine` —
  covers desktop rows, mobile cards, AND Job Detail in one edit; GC row gets
  its own globe), and Edit Job's Customer fact row.
- Migration [`20260821223000_portal_links_raw_token.sql`](../migrations/20260821223000_portal_links_raw_token.md):
  store the RAW token (v1 hash-only made links un-reshowable, killing the
  globe's core flow; same trust class as stored Stripe hosted URLs — the
  tradeoff is documented in the migration header). Mint v2 returns the
  existing active token; new `revoke_customer_portal_link` RPC. Legacy
  hash-only rows revoked. Edge fns look up token-first with hash fallback.
- Globe render smoke (+2). Help guide `share-a-customer-their-portal` (new);
  ACCESS_CONTROL updated.
