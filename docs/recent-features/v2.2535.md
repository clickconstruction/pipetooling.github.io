# v2.2535 — Audits tab: sectioned, anchored robot questions (2026-08-31)

Owner request: give the auditor more context on the 🤖 questions. Two tiers in
one PR (free rendering wins + the anchored-questions schema):

- **Section chips + grouping** (rendering-only): each question card leads with
  its `section` chip (Counts/Footage/Pricing/Scope/General — the field existed
  since v2.2517 but never rendered), and `threadAuditNotes` now orders
  questions by section (created_at within a section, via stable sort) so
  counts questions sit next to the Counts composer they'll feed.
- **Question anchors** (migration `20260831191112_bid_audit_note_anchors.sql`):
  nullable `sheet_ref` + `context` on `bid_audit_notes`. New kernel helper
  `questionContextLine()` renders the muted line under the question —
  "On P2.1 — 4 wet tables with rough-ins drawn but absent from the fixture
  schedule" — so the auditor knows where to look and what rides on the answer.
  Pre-migration/unanchored rows render exactly as before (helper treats
  absent/blank as null; the tab queries `select('*')` through the untyped
  client, so the columns are simply absent until `db push`).
- **Twin convention**: `docs/twins/FEEDBACK_LOOP.md` step 3 now instructs twins
  to anchor every question (`sheet_ref` + one-sentence `context`). No twin-mcp
  change — audit questions are direct PostgREST inserts under the existing
  twin RLS lane, and FEEDBACK_LOOP.md is not bundled into the twin-mcp briefs,
  so no redeploys.
- Deferred (needs CT-side page-param support on view links): per-question deep
  link into CountTooling at `sheet_ref`'s page.
- Kernel tests: section ordering + `questionContextLine` shapes (incl. the
  fields-absent pre-migration case).

Coordination: Audits surface belongs to the reverent-babbage stream (v2.2516–
2520, backtest program); noted in the sessions ledger.
