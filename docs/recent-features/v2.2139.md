# v2.2139 — Roadmap ★ pin: `pinned_at` column (2026-08-22)

Next-up phase 3, part 1 of 2 (part 2 = the client, v2.2140). Migration [`20260823042146_roadmap_task_pin.sql`](../migrations/20260823042146_roadmap_task_pin.md): nullable `checklist_tech_tree_group_tasks.pinned_at timestamptz` — the owner's "this one, now" override for the Plan's ⚡ Next up shortlist. A pinned task leads its lane (oldest pin first); stage numbers and arrows are untouched, which is the whole point of pick-don't-sort. No RLS change (the editors' existing UPDATE policy covers it), no CREATE TABLE. No client change in this PR — merge, `supabase db push`, then the client PR lands and `npm run gen-types:linked` regenerates `src/types/database.ts`.
