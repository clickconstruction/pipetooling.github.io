# v2.2051 — Bid-only quick log on builder cards (2026-08-21)

Owner request ("feedback on bid only without automatically including builder feedback"; mockup artifact 51b0a33a). The quick-log always wrote a `customer_contacts` row — so a bid-level fact faked a relationship contact, resetting the card's "Last contact" and moving the builder down the Oldest-first queue.

[`buildBuilderQuickLogWrites`](../../src/lib/bids/builderQuickLog.ts) gains `includeBuilderLog` (default true; false → `customerContact: null`, bid entries + `last_contact` stamps unchanged; +1 test). [`BidsBuilderReviewTab`](../../src/components/bids/BidsBuilderReviewTab.tsx): `submitQuickLog` takes `{ builderLog: false }` (skips the contact insert AND the fulfilled-promise clearing — a bid note doesn't cancel "call them Tuesday"); the bar gains a quiet outline button ("N bids only" / "this bid only", disabled at zero checked) beside the unchanged blue primary, with an adaptive caption underneath stating what each save touches ("the call queue doesn't move" / "the builder's last contact doesn't change"). New 📝 aim on unsent/pending rows (faint until row-hover via the `br-bid-note-aim` style block; always visible on touch): checks only that bid and focuses the note input. Placeholder simplified to "What did they say? Saved by whichever button you pick →" since the captions now carry the routing.

Verified live on the Michael Palmer card: 📝 collapsed the checkboxes to one, focused the note, flipped labels to "Log for builder + 1 bid" / "this bid only", and switched the caption to the singular story (no saves performed against prod). Help guide's quick-log section documents both buttons and the aim. Client-only — no migration.
