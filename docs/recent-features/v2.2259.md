# v2.2259 — Data-health line on the Pay speeds breakdown (2026-08-25)

Billing Truth Plan Phase 4 (owner-reviewed mockup). `get_billed_customer_pay_speeds` v6 (migration `20260825001052`) adds a `quality` block — `{payments12mo, measurable, unlinked, undatedInvoices, quarantined}`, all sharing the medians' 12-month payment window except the all-time undated-invoice backlog. Client: `PaySpeedQuality` + `asQuality` defensive parse in [`billedExpectedPay.ts`](../../src/lib/jobs/billedExpectedPay.ts) (nullable — pre-v6 payloads hide the line); [`PaySpeedsBreakdownModal`](../../src/components/jobs/PaySpeedsBreakdownModal.tsx) renders one quiet row under the tiles: a green measurability meter ("238 of 545 measurable (44%)"), amber pills for the two actionable counts (unlinked payments, undated bills), plain quarantined count, hover titles carrying the what-to-do. Mockup refinements that shaped it: good-number-leads (meter, not alarm panel), only actionable counts get amber.

**+ Weekly hygiene task** button on the line opens the global `ChecklistAddModal` via its existing `preset` support — title "Billing hygiene — link this week's unapplied payments to their bills", link `/jobs?forecast=1` — so task creation runs the real add flow (assignees, cadence, instance materialization) instead of duplicating it. Render smokes cover the line + pre-v6 hiding; parse tests cover the quality block. Help guide `see-when-a-customer-will-pay` gains the health-line bullet.
