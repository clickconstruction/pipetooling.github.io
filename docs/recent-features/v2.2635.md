# v2.2635 — Superintendent field-controls parity (2026-09-02)

Items 2 + 3 of the owner's follow-up list — two sub-like-only render gates widen to superintendent. Both were client-only: the servers already authorized supers.

- **Update % done** (`DashboardMyScheduleSection`): the v2.1806 field stepper on today's schedule cards now shows for superintendents. `set_job_pct_from_field` needed no change — its access check already passes schedule-block assignees and job team members (job-based, not role-based).
- **Clickable last-activity** (`DashboardTeamReadyToBillSection`): the last-activity line on Team Ready to Bill rows opens `SubcontractorJobActivityModal` for superintendents too. `list_my_accessible_job_activity_events` needed no change — its gate is `jobs_ledger_team_members` membership, which every row in the assigned-RTB list implies.
- ACCESS_CONTROL superintendent Dashboard bullets updated. (The remaining super-RTB gap — Collect Payment — ships separately.)
