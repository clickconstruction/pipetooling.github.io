# v2.2148 — Pricing Workbench: one-line solver bar; "round up to $5" removed (2026-08-23)

Owner request (mockup: claude.ai/code/artifact/80bb640d-ad23-4751-8356-c21c43c47014). `BidsPricingTab.tsx` `data-tour="workbench-solver"`:
- The 4-column grid (two long uppercase labels, checkbox, button) becomes one flex row that wraps: **Solver** eyebrow · **Margin** slider + % (`flex: 1 1 240px`; slider `title` carries "moves the N costed rows…, prices round up to $5") · 1px divider (hidden under 900px, `.wb-solver-sep` in `index.css`) · **or total** `$`-prefixed input (placeholder 42,000, Enter solves) + blue **Solve** + inline echo (`flex: 2 1 360px`) · **Price unpriced only** (`margin-left: auto`).
- `wbRound5` state + checkbox removed; `roundTo5: true` always (the prior default).
- Echo: "→ previewing $X" (+ "(rounded)" when it differs from the typed target); the "counts the $… already on no-cost rows" hint is gone — the amber no-cost banner below says it.
- Measured live on BP329: 1280px → one line idle and after a solve (34px); 600px → three lines, no horizontal overflow.
