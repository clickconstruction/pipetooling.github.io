# v2.2910 — Customers & Prospects papercuts: hiring users land on Hiring, one name for Team Review, office roster kinds, re-armable roster prompt, honest onboarding pointer (2026-09-05)

Journey-map **Tier-3 B15** residue (customer front desk J34 · hiring pipeline J25; clusters C39 stale names, C64 lying empty states). Skips what shipped earlier today on the same row: prospect clock (#18, PR #2638), Convert auto-select (#32, PR #2621), `chunkIds` reads (#3c, PR #2606), twin filters (#19, PR #2636). No migration, no edge function, no DB command.

## Per item

| # | Item | Status | Where |
|---|---|---|---|
| 1 | `/customers` reads no role — ACCESS_CONTROL note + estimator RLS recheck (J34-adj4) | done (code-level recheck + note + one honest label) | Recheck against main's migrations: `jobs_ledger`, `jobs_ledger_invoices`, `jobs_ledger_payments` SELECT policies (baseline) admit dev / master_technician / assistant / primary plus sub, team-lead and own-clock-session branches — **no estimator**; `can_access_project_row` (20260905100000) has no estimator branch; `customers`, `customer_contacts`, `bids`, `estimates` do admit estimators. So an estimator's list shows real customers with 0 jobs and $0 money. `docs/ACCESS_CONTROL.md` → estimator → Customers documents exactly that; `src/pages/Customers.tsx` gets its only role read, `moneyHiddenByRls(role)`, and `CustomerMoneyRail` renders "money not shown for estimators" instead of $0 / $0 / $0. Live impersonation check not run (no DB access in this session) — the read-side result follows from the policies. |
| 2 | Nav "Prospects" drops hiring users into the customer cold-call deck with a timer (J25-F1) | done | New kernel `src/lib/prospects/prospectsLanding.ts` (+ 4 tests): `resolveProspectsLanding({ canAccessFollowUp, teamProspectsAccess, remembered })` → Hiring for a holder who cannot work the customer deck; the last clicked top tab (per-browser `localStorage`, key `prospects:lastTopTab`) for a holder of both, calling deck by default; calling deck when no Hiring grant. `Prospects.tsx` `?tab=` effect now waits for `authLoading` (the grants arrive with the role) and writes the resolved tab on a missing `?tab=`; `setTab` / `openTeamTab` remember the click. Not changed: Layout still shows "Prospects" to estimators only with `estimator_prospects_access`, so an estimator holding Hiring alone still has no nav entry — a route-access change, out of this batch. |
| 3 | "Team → Review" stale at four sites incl. Person Desk + guide (J25-F2, WORSE since v2.2710) | done | One current label, **Hiring → Review**: `PersonDeskRecordsSection.tsx` ("Hiring → Review holds their rating history"), `TeamReviewCadenceSettingsBlock.tsx` ("Everyone with Prospects → Hiring access…"), `dashboardNeedsYou.ts` Needs-You action "Open Hiring → Review" (was "Open Team Review"), help guide `open-a-persons-desk.md` (Rate into Prospects → Hiring → Review). The feature names "Team reviews due" / "Team review cadence" stay — they name the reminder, not a tab. Code comments and frozen release notes untouched. |
| 4 | Add-to-roster prompt re-armable from the Hire card (J25-F4) | done | `TeamProspectsTab.tsx`: `openRosterHandoff(candidate)` extracted from `setStatus('hired')`; every Hire card gets an **Add to roster** button (green, before Edit) that opens the same modal; modal copy says it can be reopened from the card. The undiscoverable Back-to-Interview bounce is no longer the only recovery. |
| 5 | Roster kind offers only Sub / Helper while hires are Office Manager (J25-F5) | done | New kernel `src/lib/prospects/hireRosterKinds.ts` (+ 8 tests): `HIRE_ROSTER_KINDS` = every `people_kind_check` value except `dev` — Subcontractor, Helper, Office / assistant, Estimator, Superintendent, Primary, Master technician (`controller` is in the app's `PersonKind` union but **not** in the DB CHECK, so it is deliberately not offered — see "Noticed, not fixed"); `suggestRosterKind(roleName)` pre-selects from the role column ("Office Manager" → assistant, "Apprentice" → helper, trade roles → sub); `isHireRosterKind` guards the select. Modal copy no longer says every roster row lands under External. |
| 6 | "No onboarding items defined yet" beside a real hire, pointer dev-only (J25-F6) | done | Only the dev can open ⚙ Onboarding settings, so only the dev sees the sentence (with its pointer); other Hiring holders see the hire list with no legend and no empty-state sentence when no items exist. |
| 7 | Review deck nags to rate twins (J25-F7, rides #19) | verified | PR #2636 (v2.2893) routed both `useTeamReviewsDue.ts` (the nag) and `TeamReviewSection.tsx` (the Rate deck / Leaderboard) through `activeUsersQuery(..., { includeDev: true })` — twins, archived and test accounts drop out of both. Nothing to redo. |

## Noticed, not fixed

- `people_kind_check` (baseline) accepts `assistant, master_technician, sub, dev, estimator, primary, superintendent, helper` — **not `controller`** — while `PersonKind` / `KINDS` (`src/components/people/peopleUsersTabShared.ts`) include `controller` and People → Users' "They are a…" picker offers it. Adding a controller roster row would fail the CHECK. Needs a migration (`ALTER TABLE people DROP CONSTRAINT people_kind_check; ADD CONSTRAINT … 'controller'`) — not done here (no DB work in this PR).

## Docs touched

`docs/ACCESS_CONTROL.md` (estimator → Customers read set; Prospects → Hiring tab label, cold landing, roster hand-off, onboarding visibility), `docs/PROSPECTS_TABS_ARCHITECTURE.md` (router `?tab=` resolution; Hire handlers), `docs/GLOSSARY.md` (Team prospects heading names the Hiring tab), help guides `team-prospects.md` (landing sentence, roster kinds + pre-selection, "Missed the prompt?" example, onboarding note, keywords) and `open-a-persons-desk.md` (Rate label).
