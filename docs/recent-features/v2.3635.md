# v2.3635 — What customers see: the Agreement PDF step; a paper hand-off reads as one (2026-09-20)

The residual v2.3631 logged in `to-dos/what-customers-see-residuals.md`, closed the next day. Client only.

- **The step.** `job-contract-paper-email` in the homeowner journey (`customerJourneys.ts`), sample email id `job-contract-paper` → `buildSampleJobContractPaperEmail` (the sender's own `buildJobContractPaperEmail` over the sample job, as the link email does), and `share-job-contract` now claims it in `customerSurfaceRegistry.ts` beside the signed-copy step.
- **A real person's lane** (`personJourney.ts`; the loader selects `sent_channel`): `pdf_email` → the PDF step is *PDF emailed <day> · waiting for the signed copy* with **File the signed copy**, and the link-email step is n/a (*Went as a PDF to sign by hand*); `handed` → the email step reads *Handed over on paper <day> · waiting for the signed copy* with the same door and no link, the PDF step is *Handed over, not emailed*, and the page step is *No page — it went out on paper* instead of *Waiting n days · never opened* with an Edit & re-send door onto a link that does not exist; a link send leaves the PDF step *Went as a signing link*.
- Guide *see what customers see* carries the new chip and the wording.

Tests: the sample email, and one journey case walking all three channels. Seen on the dev server: the step renders on the tab. (That tab's console is noisy on the dev server for an old reason — forty sample iframes each load the unbundled app; production serves one cached bundle.)
