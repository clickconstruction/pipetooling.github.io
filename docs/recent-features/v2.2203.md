# v2.2203 — Workbench polish batch: who-sees-it footers, merged solver card, tabs, light-mode ground (2026-08-23)

A live-iteration batch built with the owner on the dev server (option picks from artifacts b94d615e / 8e510a77 flow); shipped as one PR.

- **Price-card footers (option 1)** ([`BidsPricingTab.tsx`](../../src/components/bids/BidsPricingTab.tsx)): each card ends in a full-width bar — green `★ The price on their letter` (the ★), blue `On their letter · alternate` (offered), gray `Only you see this` (private/unpriced). The bar carries the actions (*offer as alternate / stop offering*, *☆ make base*, with the existing confirm); the old button row and the "offered · alternate" corner tag are gone. Toggles dispatch `bid-version-picker-reload` so the strip count updates.
- **"gets N prices" pill** ([`BidVersionPicker.tsx`](../../src/components/bids/BidVersionPicker.tsx)): each GC group shows how many prices that GC receives (each version's ★ + offered alternates; one `price_book_versions` query keyed on the picker-reload event).
- **Merged summary + solver (1B)**: one sticky card — compact Revenue / Profit / Margin (our cost in the tooltip) on the same wrapping line as the margin slider, target input, Solve and "Price unpriced only"; preview turns the card amber-dashed with **Apply to {name} / Discard** inline. "Solver changes preview here first — Apply writes them." deleted. Tour anchors `workbench-summary` / `workbench-solver` both preserved.
- **Card state tabs**: blue `Viewing` and green `★ Submittal` pills overhang the card's top edge (side by side on the same card); the "★ base" corner chip, `View` buttons, "Viewing" chip and "View it to start pricing." hint are gone — the card itself is the click target. The ★ card is tinted green (`--bg-green-tint` + `--border-green`).
- **Explainer bar → ⓘ modal**: the This GC / Price options / Labor & cost bar renders inside a modal behind an ⓘ beside the bid title (portal slot `#pricing-info-slot`; Esc/click-away closes).
- **Old/New pills top-right**: portal from the Pricing card into `#pricing-view-slot` beside the Send-to strip (fall back in place when no bid is selected).
- **Buttons**: "＋ Another GC…" → **＋ Add GC** (two-line, plus centered; every tooltip mention renamed), Workbench door → **＋ Add price** (dialog "Add a price or GC"); the strip's "Send… →" button removed (`onGoToCoverLetter` prop kept unused).
- **Light mode ground**: `--bg-page` `#fafafa → #f0f2f5`, `--border` `#d8dce2 → #cfd4db`, `--border-strong` `#b9c0c9 → #aab2bd`, matching dark mode's surface-contrast model (supersedes v2.2192's values).
- **Phone fix**: GC group chips wrap internally (a 4-version group forced horizontal scroll at 414px).
- Help guides (`price-a-bid-with-the-workbench`, `bid-one-project-to-multiple-gcs`, `try-the-new-cover-letter-layout`) and the tour updated for the new names and footers.
