# v2.2495 — Portal short links target clicktooling + help-guide sweep (2026-08-29)

Post-cutover link audit (owner request, with #2201/v2.2494's job-share fix):
probed every link-minting surface for old-domain leftovers.

- **Audit results**: pipetooling.com wildcard 301 ✓ (incl. www,
  path-preserving) · all 84 edge functions deployed current, `APP_ORIGIN`
  secret set (email links flipped — verified via job-share's rendered origin) ·
  auth `site_url` = clicktooling ✓ · remaining offenders were the portal
  Worker's bounce target and one help guide.
- **Cloudflare Worker `portal-link-shell`** (clickplumbing.com zone): TARGET /
  CARD / ICON constants flipped to clicktooling.com — humans now bounce
  straight to `clicktooling.com/p/<slug>` (was a pipetooling hop through the
  301) and the OG card/icon load from the new origin. Deployed via dashboard;
  reference copy `scripts/cloudflare/portal-link-shell.worker.js` updated in
  sync.
- **Help guide `fix-the-app-when-it-wont-load.md`**: pipetooling.com/fix →
  clicktooling.com/fix (both forms).
- **Stale comments**: `portalShortOrigin.ts` (described the pre-v2.2033
  redirect rule) rewritten to describe the Worker; `DOMAIN_CUTOVER.md` portal
  bullet marked DONE, text-sweep line updated (twin docs + UA string still
  open).
- Client behavior unchanged — links still mint as `my.clickplumbing.com/<slug>`
  (the customer-facing brand domain, deliberately kept).
