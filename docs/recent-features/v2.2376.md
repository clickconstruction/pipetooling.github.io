# v2.2376 — Pricing header: Old/New beside the title, one "?" help card (2026-08-26)

Estimator feedback (Wendi): the Old/New pills floated top-right in the Send-to
strip row (a different neighborhood from the bid they switch), and two
look-alike icons split the help job — the (i) by the title opened a wordy
four-section modal while the (?) by the pills silently launched the tour.

Changes (`BidsPricingTab.tsx`, `Bids.tsx`):

- **Old/New pills move into the title row**, right after
  `BidWorkflowTabTitleWithPreview` — Cover Letter's placement and its bordered
  pill look (`pricingViewPillStyle`, kept in step with
  `BidsCoverLetterTab.tsx`'s `studioPillStyle`). The portal into Bids.tsx's
  `#pricing-view-slot` and the slot div itself are gone.
- **One help door**: the (i) button, its `#pricing-info-slot`, and the old
  four-paragraph modal are deleted. The "?" beside the pills (New view only)
  now opens a compact card — "How this page works" — four label+line rows
  (Type a price / Solve / This bid‑or‑This GC / Labor & cost), with
  **▶ Take the tour** (closes the card, starts the existing SpotlightTour) and
  **Read the guide →** (router Link to the workbench help guide) in the
  footer. The card renders at the top level of the component, so it works even
  on a bid whose Workbench prerequisites aren't set up yet (the old modal only
  rendered from the scenarios branch).
- Solo bids get the one-packet line ("one packet — {GC} sees {price}");
  multi-GC/multi-price bids get the ★-base line instead.
- Help guide (`price-a-bid-with-the-workbench.md`) retold: pills beside the
  title, "?" as the single help door, (i) references removed.

No data or behavior changes to pricing itself — header layout and help
surfaces only.
