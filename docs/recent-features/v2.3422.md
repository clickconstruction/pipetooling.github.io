# v2.3422 — Pipeline: `src/types/database.ts` regenerated after the crew-feed migration (2026-09-14)

Mechanical; no migration, no edge function. `npm run gen-types:linked` after `20260914200000_job_crew_position.sql` was pushed (drift 570/570). The `list_job_crew_position` Functions entry had been hand-added in v2.3418; this replaces it with the generator's output — the same fields, in the generator's order (generated files are regenerated, never hand-merged). Typecheck clean.
