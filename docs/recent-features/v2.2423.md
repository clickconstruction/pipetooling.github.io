# v2.2423 — MULTIPLE stat in the Workbench header (2026-08-28)

Owner-approved via the "Multiple in the Workbench" mockup. The Pricing (New) header strip
gains a fourth stat after MARGIN: **MULTIPLE** — `effRevenue / totalCost` to one decimal
with an × suffix ("2.2×"), margin's other dialect (multiple = 1 / (1 − margin)).

- Kernel `formatRevenueMultiple(revenue, cost)` in `src/lib/bids/bidFormatting.ts`
  (+ tests): null (renders "—") when cost or revenue is missing/zero.
- Rendered by the existing `stat()` helper in `BidsPricingTab`'s workbench strip, colored
  with the margin health color; tooltip spells the division with exact figures.
- Display-only — nothing stored, no other surface touched.
