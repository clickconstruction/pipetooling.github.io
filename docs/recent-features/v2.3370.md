# v2.3370 — Review's salaried rows use clocked hours, like the Bridge (2026-09-12)

Client only; no migration, no edge function. Owner decision after the v2.3360 side-by-side: "Review's salaried rows should use clocked hours like the Bridge does." Stacked on v2.3360 (step 1 of the fold).

## Why

The step 1 comparison for the pay week Aug 30 – Sep 5 agreed within half a percent for hourly people and disagreed only on the two salaried rows: Review credited Malachi 37.5 h and Abraham 40 h from a flat 8 h every weekday, while the Bridge read their sessions (38 h and 35 h). Salaried sessions are real rows (system-materialized from `salary_schedule`, auto-approved, landing in `people_hours` — v2.3179), so the assumption was no longer needed anywhere on Review.

## What shipped

- **Kernel [`derivePersonTeamSummary.ts`](../../src/lib/people/derivePersonTeamSummary.ts)**: the three salaried branches (crew day hours, `getHoursForDay`, `dayHoursForPerson`) read the hours map for everyone.
- **[`PeopleReviewTab.tsx`](../../src/components/people/PeopleReviewTab.tsx)**: ten sites — the lifetime labor / hours maps in both loaders, the per-person crew rows, `getPayForPersonDate`, and the two panel `getHoursForDay` closures — all read `people_hours` for salaried people (the `is_salary ? 8/0 : …` ternaries are gone).
- **Copy**: `reviewRanked.ts` drops the " assumed" hours tag, the "Hours are assumed: 8 h every weekday" watch-out, and `perHour.basis` is `'clocked'` for all; its assumed-% lever now says "count as half done" (stale since v2.3360). `PeopleReviewMathDrawer`, the inline drilldowns, and the popup (`buildTeamSummaryHtml.ts`) say "clocked hours, like everyone" wherever they said "8 hrs/weekday" or "salary days". Help guide `read-the-team-review.md` updated.
- `hoursBreakdown.source: 'salary'` and the **(s)** marker stay — they still say who is paid a salary; they no longer imply assumed hours.

## Verified

Kernel + ranked tests updated; dev server, Team Summary Aug 30 – Sep 5: Malachi and Abraham read their clocked hours and their Gross now matches the Bridge's Earned for the same week (see the PR). typecheck · lint · People suites.

## Notes for the next session

- A salaried person with a day off now shows no hours that day (the flat 8 used to fill it). The hygiene strip's line for salaried people points at that instead.
- Payroll surfaces (pay stubs, Draft Payroll, the Overhead pool) are untouched — they keep their own salary conventions.
