# v2.2399 — Add-GC and version-GC pickers are searchable (2026-08-27)

Wendi: the "Send this bid to another GC" modal's GC dropdown is a plain
`<select>` over every customer — "needs a search instead of a drop down list,
list is too long."

`BidVersionPicker.tsx`: both long GC dropdowns swap to the app's
`SearchableSelect` (`searchable` + `fillViewportHeight`; its portal default
z-1100 clears the Overlay's z-60):

- **Add GC modal** — "GC / builder" (placeholder still "Pick a GC…" /
  "Loading…"; Add GC stays disabled until one is chosen).
- **Version dialog** — "GC this version goes to", keeping the meaningful
  empty option ("The bid's GC (name)") via `emptyOption`.

The short "Start from" version select is untouched. No schema/RPC changes.
