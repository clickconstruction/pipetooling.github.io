# v2.3622 — Sign-in page: the Tooling family strip (2026-09-19)

Owner-approved mock, after the three app marks were redrawn on one yellow tile. Client only; no migration.

## The change

- **The strip** — [`components/ToolingFamilyStrip.tsx`](../../src/components/ToolingFamilyStrip.tsx): a `nav` under the sign-in card with the two sibling marks, CountTooling and Takeoff Tooling, as links (new tab, `noopener`). No "you're here" tile: on a sign-in page everyone knows which app it is (the owner's pick from three mocks on the real photo). Rendered by [`pages/SignIn.tsx`](../../src/pages/SignIn.tsx) in both of its states (password form, magic-link sent).
- **Sign-in only, on purpose** — `AuthPublicLandingLayout` also fronts the customer estimate/contract accept pages, so the strip lives in `SignIn.tsx`, not in the layout. A homeowner never sees estimating tools under their contract.
- **The marks** — [`public/tooling/counttooling.svg`](../../public/tooling/counttooling.svg) and [`public/tooling/takeofftooling.svg`](../../public/tooling/takeofftooling.svg) are the siblings' own favicons, copied in so the page fetches nothing cross-origin. When a sibling's mark changes, re-copy.
- **Styles** — appended to [`authPublicLanding.css`](../../src/components/authPublicLanding.css): frosted glass like the trade tiles above (the same `rgba(255,255,255,.14)` + `blur(10px)`), white text with a soft shadow, not a second white panel — the page keeps one language, the white card for the thing you do and glass for the things around it. Two tiles abreast; one per row under 480px.
- **Test** — `SignIn.render.test.tsx`: the two links, their targets and rel, and that nothing else is in the nav.
