# v2.3622 — Sign-in page: the Tooling family strip (2026-09-19)

Owner-approved mock, after the three app marks were redrawn on one yellow tile. Client only; no migration.

## The change

- **The strip** — [`components/ToolingFamilyStrip.tsx`](../../src/components/ToolingFamilyStrip.tsx): a `nav` under the sign-in card with the three marks. CountTooling and Takeoff Tooling are links (new tab, `noopener`); ClickTooling is a non-link tile with `aria-current="page"` and a "You're here" blurb. Rendered by [`pages/SignIn.tsx`](../../src/pages/SignIn.tsx) in both of its states (password form, magic-link sent).
- **Sign-in only, on purpose** — `AuthPublicLandingLayout` also fronts the customer estimate/contract accept pages, so the strip lives in `SignIn.tsx`, not in the layout. A homeowner never sees estimating tools under their contract.
- **The marks** — [`public/tooling/counttooling.svg`](../../public/tooling/counttooling.svg) and [`public/tooling/takeofftooling.svg`](../../public/tooling/takeofftooling.svg) are the siblings' own favicons, copied in so the page fetches nothing cross-origin; ours is `/favicon.svg`. When a sibling's mark changes, re-copy.
- **Styles** — appended to [`authPublicLanding.css`](../../src/components/authPublicLanding.css); the strip is a white panel like the sign-in box (same border, radius and shadow) so it sits on the photo background the same way; one tile per row, since the sign-in stack is 420px wide.
- **Test** — `SignIn.render.test.tsx`: the two links, their targets and rel, and the non-link current tile.
