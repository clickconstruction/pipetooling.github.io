# v2.3134 — Stage Plan PR 6: guides, recordings, and the reference docs (2026-09-08)

Last PR of the train in `to-dos/stage-plan/` — which this PR deletes, per the to-dos rule (the release notes and these fragments carry the record; the residuals moved to [`to-dos/stage-plan-residuals.md`](../../to-dos/stage-plan-residuals.md)).

## Recordings (`public/help/`, Playwright video → ffmpeg, 8 fps, light theme, desktop)

- `split-a-job-into-stages.gif` — Bill → ① Line Items: Final flips Any → Order, the badge and the strip follow, Still to bill. Guides: *split a job into stages and bill stage by stage* (under "Set up the stages"), the hub guide's new **0 · The plan — the Bill tab**.
- `show-a-gc-the-stages-you-plan.gif` — Edit Job → Stages: four eyes on, See it as the customer, one eye off while the drawer watches. Guides: *show a GC the stages you plan* (under "The eye"), the hub guide's **7 · The GC**.
- `show-a-gc-the-stages-you-plan-portal.gif` — `/portal?t=sample-gc`: Where the job is, Need other dates? opened and cancelled. Guide: *show a GC the stages you plan* (under the example).
- Recorded on a throwaway job as dev (created through the Multiple Segment Generator, deleted after) — no read-only training account exists yet; `docs/HELP_MEDIA_PLAN.md` status table gains the three rows.

## Docs

- `docs/GLOSSARY.md`: **Stage kind**, **Draw**, **The eye**, **Where the job is**; the Subs tab entry's GC chip wording.
- `docs/PROJECT_DOCUMENTATION.md`: `jobs_ledger_fixtures.stage_kind` / `shared_with_gc`; the Jobs paragraph points at the train.
- Hub guide *schedule a sub across every surface*: the map gains the Edit Job → Bill / Edit row; section 0 (the plan); section 7 re-shot.

## One code change

The drawer's **Open the portal ↗** resolves the GC's real link through `useGcPortalLinks` (loaded only while the drawer is open); the sample page stays the fallback.
