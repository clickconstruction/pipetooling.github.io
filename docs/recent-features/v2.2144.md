# v2.2144 — GC Review locks background scroll (2026-08-22)

Owner request: scrolling inside the GC Review modal also scrolled the Stages board behind it. [`JobsGcReviewModal.tsx`](../../src/components/jobs/JobsGcReviewModal.tsx) now calls the shared `useBodyScrollLock(open)` (ref-counted, iOS-safe fixed body, scroll offset restored on close — the same hook the report/inspection modals use). Client-only.
