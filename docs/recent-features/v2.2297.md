# v2.2297 — Quick Estimate: placement + drops the ⚡ (2026-08-25)

Owner requests: (1) remove the lightning bolt from all Quick Estimate surfaces (Dashboard button, wizard header, Settings toggle label, help-guide tokens — purple styling unchanged); (2) move the Dashboard button from the very top to **below the Clock In row, above My Schedule** — done by prepending it inside `DashboardPinnedQuickRow`'s `afterJobReportRow` slot ahead of `myScheduleSection` in `Dashboard.tsx` (the clock row and schedule both live inside that component). Verified live: Clock In → Quick Estimate → My Schedule.
