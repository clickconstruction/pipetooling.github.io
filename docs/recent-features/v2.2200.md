# v2.2200 — Remind gets an honest outcome toast (2026-08-23)

Owner request. The Review tab's 🔔 Remind fired `send-checklist-notification` fire-and-forget — indistinguishable success, no-devices, and failure. [`Checklist.tsx`](../../src/pages/Checklist.tsx) `sendReminder` now reads the function's existing `push_sent` count: >0 → success toast "Reminded <name> on N device(s)"; 0 → error toast "<name> has no notification-enabled devices — the reminder had nowhere to land. A text or call is the sure path."; invoke error → "Couldn't send…". No edge change (the function already returned `push_sent`). Client-only.
