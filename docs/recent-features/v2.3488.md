# v2.3488 — Submittals stage 4b: Needs You and the won bid's chip (2026-09-16)

Client only; no migration, no edge function. Stage 4's third PR ([`to-dos/submittals/README.md`](../../to-dos/submittals/README.md) → PR 4b): the office's four cards, and the Bid Board chip.

## What shipped

- **Kernel** [`src/lib/submittals/submittalNeedsYou.ts`](../../src/lib/submittals/submittalNeedsYou.ts): `summarizeSubmittalNudge` over rows the hook fetched — *not started* (won or started_or_complete, `outcome_at` 5–45 days ago — `updated_at` for bids marked won before v2.3354 stamped the date — no `bid_submittals` row), *unopened* (a room `open` and shared ≥ 3 days with no `view` event since the share; names the people who may decide and never opened), *sent back* (the newest shared revision carries Revise / Reject rows and no newer revision exists), *lead time* (an item's `lead_time_days` on the bid's newest revision lands after the job's earliest `job_stage_windows.window_end`; worst overrun first). Tests.
- **Hook** [`useSubmittalsNudge`](../../src/hooks/useSubmittalsNudge.ts): the last 120 days of won bids, every room and revision the caller can see, the view events and people per room, the items of each bid's newest revisions, the linked jobs' stage windows — nine reads, null when a table is not there yet.
- **Needs You** ([`dashboardNeedsYou.ts`](../../src/lib/dashboardNeedsYou.ts)): keys `submittal-lead-time` (red, rank 30) · `submittal-sent-back` (red, 40) · `submittal-unopened` (blue, 40) · `submittal-not-started` (amber, 50), kicker *Submittals*, each worded from the nudge's first case, each opening `/bids?tab=submittals&bidId=…`. Enabled for the office and the estimators (`DashboardPinnedQuickRow`).
- **The chip** ([`BidSubmittalChip`](../../src/components/bids/BidSubmittalChip.tsx), under the won bid's Job block beside the job-accounts row): *Submittals · Rev 2 · shared · waiting on Dana W.* / *3 rows sent back* / *Rev 1 · draft* / *room closed* / a dashed *none yet* (`describeBidSubmittal`, `useBidSubmittalSummary`); a door to the tab.
- **Guide**: `needs-you-card.md` names the four cards.

## Verified

`submittalNeedsYou` (4), the four cards in `dashboardNeedsYou.test.ts` (2), the chip's render smokes (2 + the words), typecheck, lint, theme, timezone. Live: the cards draw on the dev Dashboard once BP398's room sits three days unopened, or a row is sent back on it — the live gate 4 script covers the sent-back card.
