# v2.2721 — Pricing Workbench: the folded solver button reads "Solver ›" (2026-09-03)

**Why.** The blue button that unfolds the solver was labelled "Solve ›", which reads as a verb — press it and expect prices to change. It only opens the panel; the verb lives on the **Solve** button inside the ring. Owner asked for "Solver ›".

**What.** Label, its comments, the Workbench help text, and the `price-a-bid-with-the-workbench` guide say **Solver ›**. The inner **Solve** action, the ▾ menu, and the tooltip are unchanged. Frozen release-note history keeps the old wording.
