# v2.3409 — Sold in sticks, PR 3: Materials to order, ordered quantities on the RFQ, Refresh from the catalog (2026-09-14)

Client only; no migration, no edge function. Third and last of the train (PR 1 v2.3406 the rule, PR 2 v2.3407 the rounding). Brief: the *Sold in Sticks* artifact `0851b7b5`.

## What shipped

- **Kernel**: `orderListRows(rounding, partNameById, cap)` in [`takeoffOrderRounding.ts`](../../src/lib/bids/takeoffOrderRounding.ts) — the list, most expensive rounding first, capped with a remainder (+1 test). `rfqScopeForZeroPrice(queue, rounding?)` in [`takeoffCostRail.ts`](../../src/lib/bids/takeoffCostRail.ts) now sums the bid's **need** per part (quantity × fixture count — it had summed the per-fixture quantities) and quotes a part sold in packs at its **ordered** quantity: *• 3/4" Type L Copper × 120 ft (105 ft needed · 20 ft sticks)* (+1 test, one expectation corrected).
- **[`TakeoffOrderListPanel`](../../src/components/bids/TakeoffOrderListPanel.tsx)** (+4 render smokes): the rows (*needed → ordered · packs × increment · +$extra* or *exact*), the total, the empty state that says where a rule is set, a folded compact form, and **Refresh Sold in rules from the catalog** reporting *N lines updated, M cleared*. Mounted on the Sheet view's rail between *What Pricing sees* and *Needs a price*, and folded under the strip on One at a time (the tab passes `partNameById` and the refresh to both).
- **`refreshOrderIncrementsFromCatalog`** in [`useTakeoffRoughLines`](../../src/hooks/useTakeoffRoughLines.ts): re-reads every line's part through `orderIncrementsFor`, updates the lines whose snapshot differs (through the normal `updateTakeoffRoughPartLine` → persist path), clears a snapshot whose part lost its rule, leaves matching lines alone.
- **Part Prices modal** ([`TakeoffPartPricesModal`](../../src/components/bids/TakeoffPartPricesModal.tsx)): a *Sold in* block above the prices — the effective rule with its source (*from the Copper pipe type* / *this part's own*), a number + unit + Save writing the part's own rule — so a rule can be fixed without leaving the sheet. It notes that lines keep their snapshot until Refresh.
- Guide *price a takeoff in sticks* (new); *see what pricing sees on takeoffs* already carries the strip's paragraph.

## Verified

`takeoffOrderRounding.test.ts` (6), `takeoffCostRail.test.ts` (+1, one updated), `TakeoffOrderListPanel.render.test.tsx` (4), the rail / focus / tab suites, release-note + guide drift tests, typecheck, lint, theme check.
