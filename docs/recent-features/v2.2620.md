# v2.2620 — Lien-release record surfaces: Documents rows + activity registry (2026-09-02)

PR 3 of the signing loop (schema v2.2616, modal/sign flow v2.2619).

- `src/lib/jobActivityEvent.ts` — the five `lien_release_*` event types registered in `JOB_ACTIVITY_EVENT_RENDER` (tag "Release", billing bucket: issued blue, requested amber, signed/sent green, voided red). The events have been written by the 20260902001517 trigger since PR 1; this is the client half of the 3-step pattern.
- `src/pages/Documents.tsx` Jobs tab — minted (non-draft) `job_lien_releases` render as child rows under their job beside billed invoices: form label, amount, lifecycle chips, click → preview window regenerated from the snapshot with the typed signature + audit line. Voided rows stay listed (voided chip). Fail-soft load; RLS keeps them office-only.
- Kernels: `lienReleaseSnapshotToWaiverFields` (lienReleaseTracking — one snapshot→fields mapping for every re-render surface) and `lienReleaseChipColors` (lienReleaseLifecycle — shared chip palette).
- Help: guide's "Track what you issued" now covers Documents + the activity feed.

Not in this PR (deliberate): a clickable view-link inside activity entries — the feed renders the events as text; the document opens from Documents or the release modal. PR 4 = inbox lanes + send-to-customer.
