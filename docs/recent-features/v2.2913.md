# v2.2913 — Pipeline papercut batch B6: paid matches show on search, honest HCP caption, fair proration, call mode dials and emails, collections age, plural round waits (2026-09-05)

Journey map Tier-3 row **B6 — Jobs → Pipeline (Stages)** (private repo `pipetooling-journey-map`, `docs/JOURNEY_MAP.md`; dossiers J3 bill-a-customer, J4 get-paid-chase-ar, J5 weekly-money-close, J20 gc-gets-a-statement). Client + one shared edge helper; no migration. Builds on today's #2605 (bill-truth kernel), #2587, #2619 (Bill Customer write-after-commit — only the HCP caption line touched), #2630, #2638.

## Per item

| # | Finding | Status | What shipped |
|---|---|---|---|
| 1 | J3-3 (C64) — paid job searched on the Pipeline → every section (0), no hint | **done** | New kernel [`src/lib/jobs/stagesPaidSearchHint.ts`](../../src/lib/jobs/stagesPaidSearchHint.ts): `stagesPaidSearchHint` (paid matches → "N match(es) in Paid in Full — show it/them" button that scrolls to the new `#stages-paid` header; lean lookup in flight and nothing open → "Checking Paid in Full…"; finished with nothing → "No match anywhere — Paid in Full included") and `stagesPaidHeaderSearchCount` (header shows "N match(es)" / "checking…" during a search, never "Expand to load"). The data was already client-side (v2.1825 lean search merges paid hits; search forces the section open) — only the reveal was missing. Hint renders as the first row of the board (`role="status"`, `data-testid="stages-paid-search-hint"`). |
| 2 | J3-5 — HouseCall Pro channel is Date · Memo · Save with no record-only statement | **done** | One muted caption above the Date field in [`SendRecordInvoiceModal`](../../src/components/jobs/SendRecordInvoiceModal.tsx) (`data-testid="bill-customer-hcp-record-only-caption"`): *"Records the bill as sent through HouseCall Pro — ClickTooling emails nobody. The job moves to Billed Awaiting Payment; the customer sees nothing from here."* Nothing else in the modal touched (#2619). |
| 3 | J3-6 — remainder bill prorates 50/50 across segments the Edit Job bar marks payments-covered | **done** | [`invoiceScopedFixtures.ts`](../../src/lib/invoiceScopedFixtures.ts) `dropPaymentsCoveredRows` + the primary-bundle branch of `fixturesForInvoiceBill`; mirrored in `supabase/functions/_shared/stripeInvoiceItemsFromFixtures.ts` `scopeFixturesToInvoice`. Rule: for the PRIMARY remainder only, when its amount is smaller than the still-unlinked billable rows, the gap (`sum − amount`) is the payments/carve pool; it fills rows in order exactly like `dollarCoverageForSegments` — rows swallowed whole never re-list (same treatment as rows on other bills, v2.2589); the row the pool runs out inside and every row after it stay, and the builders' proration runs over those only. The live specimen ($370 job, two $185 segments, $185 paid) now lists the second segment at $185 instead of both at $92.50. Riders/extras that inflate the remainder above the row sum leave the pool empty (nothing dropped); a positive remainder always keeps at least the last row; dollar carves are untouched. **Edge deploy needed for Stripe parity** (`create-stripe-invoice`, `send-physical-invoice-email` import the shared helper) — until then previews/PDFs apply the rule and hosted Stripe invoices still prorate. |
| 4 | J5-5 (C39) — "📆 Weekly movement" vs "💸 Weekly money movement" coin flip | **done** | Menu label → **"Stage moves this week"** (`stagesSectionToolsMenu.ts`, key `weekly-movement` unchanged); the modal's `<h2>`/aria-label follow. The emailed report (`emailCatalog` "Weekly movement", Settings email streams/templates) keeps its name — a sweep across the email family is out of scope; the guide says so. |
| 5 | J4-6 — call mode shows raw phone digits, no email door | **done** | `formatChasePhone` (ten digits / 1+ten → `(555) 123-4567`; anything else as stored; `tel:` href keeps raw digits) and `chaseEmailHref` + `chaseBillsWereEmailed` in [`paymentChase.ts`](../../src/lib/jobs/paymentChase.ts): a `mailto:` with a subject naming the bill appears only when the customer has an address AND a bill actually went out by email (Stripe channel or `sent_to_customer_at`); HouseCall Pro records offer no mailto. |
| 6 | J4-7 — after 2 broken promises only a passive "open the bill on the board for the button" line | **done** | `collectionsCandidatesForChase` (one chip per distinct job) + `onMoveToCollections?` prop on `PaymentChaseModal`; the banner and the dispute pane offer **Move … to Collections…**, which opens the board's existing typed confirm over call mode (confirm z-index 60 → 80; call mode is 70). The session snapshot stays put; the existing `set_job_collections_flag` path owns the write. Gate = the same office pool as the section's button. |
| 7 | J4-10 — 0%-billed Collections shells can't age, no expected-pay chip, No-line chip skips them | **done (age)** / **skipped (chip scope)** | `stageRowBilledAgeReference` returns `{ ymd: <collections_at day>, handSet: false, source: 'collections' }` for a billed job-shell in Collections, so `stageRowBilledAgeDays`, the Who-owes-what sort and the customer breakdown all age it from the flag day; the row chip reads **"In Collections N days · no bill line"** with the flag date in its title. Not changed: the Billed header's "No line" chip stays Billed-scoped because its fixer (`FixBillLinesModal`) is built from `billedActiveRows`; collections shells are no longer un-ageable, which was the harm. Expected-pay chip still needs a bill date (unchanged). |
| 8 | J20-F7 — "1 statement round wait on sign-off", counts GCs not rounds | **done** | `heldRoundHeadline(count, totalLabel)` in [`gcStatementRounds.ts`](../../src/lib/jobs/gcStatementRounds.ts): "1 GC statement waits on sign-off — $12,700" / "5 GC statements wait on sign-off — $154,166"; `PipelineMoneyOpportunities` uses it. |
| — | J4-5 Mobile cards at phone width (#26), J4-8 dead `primary` AR gate (#29), J22-F5 `/accounts-receivable` naming (Tier 4) | **skipped** | Shipped or scheduled elsewhere today, per the B6 brief. |

## Tests

`invoiceScopedFixtures.test.ts` (coverage pool: whole rows drop in order, partial row + tail stay, exact-sum composition still wins, remainder ≥ sum drops nothing, primary-only, unpriced rows ride through, other-bill rows excluded first, edge mirror identical), `invoiceBilling.test.ts` (Collections shell ages from `collections_at`; non-Collections shell / stale flag / invoice rows unchanged), `gcStatementRounds.test.ts` (`heldRoundHeadline`), `paymentChase.test.ts` (`formatChasePhone`, `chaseBillsWereEmailed`, `chaseEmailHref`, `collectionsCandidatesForChase`), new `stagesPaidSearchHint.test.ts`.

## Docs / guides

`docs/BILLING_FLOWS.md` (composition rule + HCP caption), `docs/JOBS_STAGES_TAB_ARCHITECTURE.md` (Paid header during search, hint row), help guides `ready-to-bill-pipeline.md`, `chase-late-payments.md`, `see-what-moved-this-week.md`, `run-your-gc-statement-round.md`.
