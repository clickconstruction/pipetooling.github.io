# v2.2389 — Simple-bid Mark sent + Workbench column alignment (2026-08-27)

Wendi, two asks:

- **Cover Letter** (`BidsCoverLetterTab.tsx`): the `bidVersions.length === 0`
  branch (plain bids, no packets) rendered only the "One bid — the letter
  shows ★ …" line — no send button — while the headline caption (v2.2385)
  promises "Mark sent stamps this as the bid's value". New
  `markSentTodaySimple(bidId, amount)`: writes `bid_date_sent` = app-TZ today
  and `bid_value` = the letter's headline amount (no `bid_version_sends` rows
  — there are no versions), same toast/reload pattern as `markSentToday`.
  Button disabled until the letter has a priced amount; the caption shows the
  current sent date once stamped.
- **Workbench worksheet** (`BidsPricingTab.tsx`): Revenue + Profit header
  cells centered over their columns; Count header and values centered
  (values were right-aligned).
