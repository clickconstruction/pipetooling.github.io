# v2.1951 — Partner ledger: pending offsets interleave in the journal (2026-08-21)

Owner request on the v2.1939 layout: the separate "Pending — not yet on a statement" box above the journal reads better as rows *in* the journal at their dates. Kernel [`partnerLedgerJournal.ts`](../../src/lib/partnerLedger/partnerLedgerJournal.ts) grows `mergePendingIntoJournal(rows, pending)` → `JournalDisplayRow[]` (new `PendingJournalRow` variant: dated, signed amount via `pendingOffsetSignedAmount`, `balance: null`; same-date ties put posted rows first so the balance chain reads contiguous; 3 new tests — 11 total in the file). [`PartnershipLedgerTab.tsx`](../../src/components/partnerships/PartnershipLedgerTab.tsx): the pending box is gone; pending rows render inline with an amber "pending" chip, muted date, and a "—" balance cell (the Timeline tab's no-balance-impact convention). Headline (settle-up position, posted · pending parts) unchanged from v2.1939; footer explains the chip. Client-only — no migration.
