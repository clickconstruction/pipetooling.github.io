# v2.2654 — RFQ Round 2, Rung F: the desk on film (2026-09-02)

Two recordings close Round 2's guide work:

- `rfq-desk-and-compose.gif` (quote-link guide): the urgency-sorted desk
  with reason chips and contact names, then + New request → the compose
  with Ferguson's contact chip already To, the plans-link toggle, and
  the exact-email preview (recording stops before Send — no vendor was
  emailed).
- `plug-in-file-drop.gif` (full-loop guide): the drop-zone before/after —
  a vendor xlsx becomes visible extracted text ("dropped Ext Price
  column · unit prices only") and a matched grid with the package-wording
  hint.
- Plan doc: Rung G marked owner-approved (option (a)) — next build rung.
