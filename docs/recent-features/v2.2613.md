# v2.2613 — Share button on Waiting/Working Pipeline rows (2026-09-01)

User report (2026-09-01): "I seem to have lost the ability to share" on Jobs →
Pipeline. Nothing was removed — the v2.1452 share feature (PR #1154) only ever
mounted `ShareJobButton` in `JobsStagesUnifiedTable` (Ready to Bill / Billed
Awaiting Payment / Collections), the Job detail header, and the mobile card
⋯ sheet, despite the commit message and the help guide
(`share-a-job-with-a-teammate.md`) promising "every Pipeline row". With Ready
to Bill empty and the billing sections collapsed, a Waiting/Working-only
viewport showed no share icon anywhere.

- `JobsStagesTable.tsx` (the Waiting/Working renderer): `ShareJobButton`
  added first in the row-actions icon row under the Edit button — Share →
  Plumbing Tooling wrench → AIA (when eligible) → hazmat — mirroring the
  billing stages' Share-first order (v2.2576).
- Same `{ hcpNumber, jobName, jobAddress }` payload and share-sheet /
  clipboard-toast behavior as everywhere else (single `useShareJob` hook).
- No help-guide change: the guide already documents Share on every row; the
  app now matches it. Mobile Waiting/Working cards already had Share in the
  ⋯ sheet.

No schema changes.
