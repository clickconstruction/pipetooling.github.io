# v2.2202 — Vehicle check-ins gen-types catch-up (2026-08-23)

Follow-up to v2.2199 (PR #1904): after `supabase db push` applied
`20260823210023_vehicle_checkins.sql`, regenerated `src/types/database.ts`
(`npm run gen-types:linked`, +59 lines: the `vehicle_checkins` table) and dropped the three
interim `from('vehicle_checkins' as any)` casts + their eslint-disables in
[`QuickfillVehicleOdometersSection.tsx`](../../src/components/quickfill/QuickfillVehicleOdometersSection.tsx)
(insert) and [`PeopleVehiclesTab.tsx`](../../src/components/people/PeopleVehiclesTab.tsx)
(select + delete). The fail-soft try/catch around the Quickfill insert stays — it now guards
transient errors instead of the missing table. No behavior change.
