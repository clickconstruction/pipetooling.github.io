# v2.2408 — Alternates label: project name, not GC name; doubled halves collapse (2026-08-27)

Owner (BP392 ALSATIAN screenshot): "Under Alternates I think it should be
(project name) value engineered not (GC) value engineered." The automatic
label was `version.name · pricing.name`, and ＋ Add GC names packets after the
GC — so a MERIT packet's VE scenario printed "MERIT GENERAL CONTRACTORS value
engineered · MERIT GENERAL CONTRACTORS value engineered" on the letter.

- New kernel helper `customerFacingAlternateName(name, gcName, projectName)`
  in `coverLetterSamePage.ts`: each ` · ` half that starts with the GC's name
  (case-insensitive) swaps it for the project name (drops it when no project
  name), and adjacent identical halves collapse to one. Names without the GC
  pass through untouched.
- `buildAlternatesBlock` gains an optional `naming` arg; both letter builders
  in `BidsCoverLetterTab` pass `{ gcName: letterCustomerName, projectName }`.
  Applies to AUTO labels only — estimator-edited wording
  (`cover_letter_alt_texts`) is untouched, as are the internal names on the
  Pricing tab / Send-to strip.

16 kernel tests (5 new). Verified live on BP392: the Alternates line reads
"ALSATIAN value engineered: $73,765.31 (reduced $46,813.07)".
