# v2.2178 — Followup → By status per GC (Bids by GC, 2026-08-23)

Mockup: claude.ai/code/artifact/d447447e-e3c1-45fc-be33-bf4b8bf478ef. Same rule as v2.2171: one row per GC the bid went to, in that GC's bucket.

- `BidSubmissionFollowupTab`: takes `gcPacketsByBid` (from Bids.tsx's hoisted `useBidGcPackets`); `sectionRows` = `gcOutcomeRowsForBid` per filtered bid → `SectionRow { bid, gc, rowKey }`. Pending / Won / Started or Complete / Lost lists are rows (`Started or Complete` = the bid's own state on its primary-GC row; a packet win for another GC lists under Won). Unsent stays the bid-level Working-board rule. Section counts count rows.
- GC column → `renderRowGcCell`: the row's GC (↗ opens that builder on By builder; the name opens the bid's party for single-GC rows) plus "also to X won, Y waiting" on multi-GC rows ("same letter · also to …" for Also-sent-to rows).
- Lost list: the reason chip / note come from the row (`gc.lossCategory` / `gc.lossNote`, `auto` tag when inferred); `BidLostQuickPopover` gains `packet?: { versionIds, gcName, lossCategory, note }` and writes `bid_versions.outcome/loss_category/outcome_note` in that mode (toast "Lost with {GC} — reason recorded."); panel toggles keyed by `rowKey`.
- Selected-bid prev/next nav dedupes rows to bids; only the primary-GC row carries `id="submission-row-{bid}"` so scroll-to-selected still lands.
- Printed follow-up sheet and per-manager counts untouched (per bid). No migration.
