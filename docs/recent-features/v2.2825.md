# v2.2825 — Needs you: open jobs sitting idle (2026-09-05)

PR 5 of the Job Summary analysis plan (artifact 9c02d29a) — the first push card. Stacked on v2.2823 (Cycle).

## Client only — no migration

- [`dashboardNeedsYou.ts`](../../src/lib/dashboardNeedsYou.ts): new key `jobs-stale-open` (tier 40, amber, kicker "Jobs"); inputs `staleOpenEnabled` / `staleOpen: { count, total, mine, minIdleDays } | null` (optional, so Quickfill's builder call is untouched). Title "N open jobs have sat idle 21+ days", detail names the contract dollars and "— N are yours", action "See them". One test.
- [`useStaleOpenJobsNudge.ts`](../../src/hooks/useStaleOpenJobsNudge.ts) (new): one scan of `jobs_ledger` where `status not in (billed, paid)` folded by the pure `staleOpenJobs` kernel at 21 days (the same rule as the Cycle view, so the card and the list agree); `mine` = rows whose `master_user_id` is the signed-in user; null while loading, zero on error; refreshes on `job-status-changed` / `clock-session-changed`.
- [`DashboardPinnedQuickRow.tsx`](../../src/components/dashboard/DashboardPinnedQuickRow.tsx): hook gated like the contract nudge (office roles, not hidden); dispatch → `/jobs?tab=job-summary&view=cycle`.
- [`jobSummaryCycle.ts`](../../src/lib/jobs/jobSummaryCycle.ts): `staleOpenJobs` now skips "waiting" jobs that have never seen a field day — queued, not stale (test extended).
- [`useJobSummaryView.ts`](../../src/hooks/useJobSummaryView.ts) + [`Jobs.tsx`](../../src/pages/Jobs.tsx): `initialView` from `?view=` (validated through the prefs reader), applied once; the pref owns it after. Ground rule from the plan: the URL carries the view.
- Help guide "see how fast jobs turn into cash": the Needs you paragraph.
