# v2.2453 — Prospect List: pipeline sections replace warmth grouping (2026-08-28)

Second slice of the Prospects-refresh arc (after v2.2452). Warmth grouping rendered as one giant bucket (238 of 268 rows at warmth 0); live counts after this change tell the real story: Never called 3 · Recent 24 · Going cold 70 · Cold 171.

- **New kernel [`src/lib/prospects/prospectListGrouping.ts`](../../src/lib/prospects/prospectListGrouping.ts)** (+11 tests): `filterProspectsForList` (search over company/contact/phone/email), `groupProspectsForList` (sections `never_called → recent <30d → going_cold 30–90d → cold 90d+ → converted → cant_reach → not_a_fit`; "called" = has a `didnt_answer`/`answered` comment, matching v2.2301's queue definition, so a noted-but-never-called prospect stays in `never_called`; called rows bucket by `last_contact` age, null → cold), `lastTouchLabel` ("answered 3d ago" / "didn't answer today" / "noted Nd ago" / "added Nd ago"), plus `LIST_SECTION_*` constants (labels, chip labels, order, default-open set — never_called + recent open, rest collapsed).
- **[`Prospects.tsx`](../../src/pages/Prospects.tsx)** Prospect List tab:
  - `loadProspectListProspects` also loads each prospect's latest call (`prospect_comments` in `CALL_INTERACTION_TYPES`, newest-first, first-row-per-prospect) into `prospectLastCallMap`; `calledIds` derives from its keys.
  - Warmth-keyed section state (`Record<number, boolean>`, sentinel keys −1/−2/−3) replaced by `ListSectionKey`-keyed state; toggle honors per-section defaults; new `jumpToProspectListSection` powers the count-chip row (chips for the four active sections + Converted; disabled at 0).
  - Table columns now Company (with 🔥N warmth chip when >0) | Contact | Phone | Added (`created_at` date) | Last touch (label + ` · MM:SS` ledger time when >0; full `last_contact` datetime in the title tooltip) | Email / Links | Actions. Address and Time columns dropped (address stays on the workstation card, Edit modal, and mobile cards). Terminal sections keep Edit / Send back / Not a fit / Delete; active sections get **Call now →** (same as row click → `selectProspectForList`).
  - The `?prospect_id` deep-link effect now opens the section the kernel puts the prospect in (was: hardcoded −1/−2).
  - Mobile cards: Time row → Last touch row; everything else unchanged.
- Never-called section sorts oldest-entry-first to match the calling queue; all other sections keep the old newest-touch-first order.
