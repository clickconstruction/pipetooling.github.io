# v2.2926 — Role sweep migration: functions with no role literal are skipped, not fatal (2026-09-06)

Follow-up to v2.2920 (journey-map Tier-2 #28). The first production `supabase db push` of `20260906010000_role_sweep_predicates.sql` aborted inside the Banking-RPC DO block:

```
ERROR: role sweep: list_unlabeled_mercury_transactions(oid 340152) carries no dev/master/assistant literal and no controller — review manually (SQLSTATE P0001)
```

`list_unlabeled_mercury_transactions` is a plain `SECURITY INVOKER` SQL function with no role check — RLS on `mercury_transactions` and the drag-sort assignments table gates it — so there is nothing to widen. The block's "no literal, no controller" branch now `RAISE NOTICE … RLS-gated, skipped` and continues; the "function not found" branch still aborts. The migration had no ledger row anywhere (the failed push rolled back), so the file is edited in place rather than followed by a second migration.

Nothing else in the sweep changed. Re-run `supabase db push` after this merges, then deploy `mercury-reconcile` and `get-mercury-account-balances` as v2.2920's fragment says.
