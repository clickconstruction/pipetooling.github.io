# v2.3071 — Sub portal reads the sheet → job link (2026-09-07)

Conversion train PR 5 — section B of [`to-dos/sub-sheet-job-link-followups.md`](../../to-dos/sub-sheet-job-link-followups.md). Edge functions only.

- [`sub-portal`](../../supabase/functions/sub-portal/index.ts): the sheet select carries `job_ledger_id`; the plans lookup is `jobs_ledger … .in('id', links)` (was `.in('hcp_number', numbers)`), and the same read fills `jobNumberById` so **Your days** shows the linked job's number (HCP, else click) for office-set sheet days instead of the raw sheet text. `SubSheetRow` (`_shared/subPortalStatement.ts`) gains `job_ledger_id`.
- [`submit-sub-portal`](../../supabase/functions/submit-sub-portal/index.ts): `mark_work_done` and `progress` read `job_ledger_id` for the watcher notification and the `sub_progress` activity event; the two case-sensitive `.eq('hcp_number', …)` lookups are gone.
- `docs/EDGE_FUNCTIONS.md` updated. **Deploy**: `supabase functions deploy sub-portal` and `supabase functions deploy submit-sub-portal` after merge (`npm run check:edge-drift` shows both until then).
