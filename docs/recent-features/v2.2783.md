# v2.2783 — Regenerate database.ts after the takeoff_fixture_history push (2026-09-04)

Follow-up to v2.2774: `npm run gen-types:linked` after `supabase db push` of migration `20260904202321_takeoff_fixture_history`. The only change is the new `takeoff_fixture_history` entry under `Functions` (22 lines); `takeoff_fixture_key` is not exposed (it takes a scalar, which PostgREST does not list). The manual interfaces in `src/types/database-functions.ts` stay — the client wrapper is typed through them. Generated file, regenerated never hand-edited (see `CLAUDE.md`).
