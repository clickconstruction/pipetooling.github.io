# v2.2442 — Domain cutover: the app moves to clicktooling.com (2026-08-28)

Cutover day (runbook `docs/DOMAIN_CUTOVER.md`; prep v2.2440, report-app evacuation
v2.2441, all prerequisites verified beforehand — DNS pre-wired, all three domains
GitHub-verified, allowlist live). This PR is the repo half of the flip:

- `CNAME` → `clicktooling.com` (GitHub Pages serves the app there; pipetooling.com
  becomes a permanent path-preserving 301 via Cloudflare).
- `src/lib/appOrigin.ts` `APP_ORIGIN` → `https://clicktooling.com` (the v2.2440 client
  flip point).
- `supabase/config.toml` auth `site_url` → clicktooling.com (pipetooling.com stays in
  the allowlist; applied via config push post-merge).

Post-merge, non-repo steps (same day): `supabase secrets set
APP_ORIGIN=https://clicktooling.com` (flips all edge-function email links, no
redeploys), config push, Cloudflare pipetooling.com 301 rule (kept FOREVER) + portal
short-link rule retarget (`my.clickplumbing.com/*` → `clicktooling.com/p/*`),
Enforce HTTPS for the new domain, full smoke. `share.pipetooling.com` deliberately
kept as-is. Crews re-install the PWA + re-enable push from clicktooling.com
(origin-bound — the release note says so).
