# v2.2061 — GC statement Email…: Preview button (2026-08-21)

Owner request after finding the Email… flow: a Preview that opens the report in a new window showing what the recipient will see. Client-only.

- **Kernel** ([`gcStatementEmail.ts`](../../src/lib/jobsDocuments/gcStatementEmail.ts)): `buildGcStatementEmailPreviewHtml(group, subject, opts)` — a standalone light-page document wrapping the **exact** `buildGcStatementEmailHtml` body (same builder the send pipeline uses), headed by "Preview — what the recipient sees" and the subject line (escaped). +2 tests.
- **Glue** ([`printWindow.ts`](../../src/lib/jobsDocuments/printWindow.ts)): `openHtmlPreviewWindow` — `openHtmlPrintWindow` without the `print()` call; same popup-blocked boolean contract.
- **UI** ([`JobsGcReviewModal.tsx`](../../src/components/jobs/JobsGcReviewModal.tsx)): a left-aligned **Preview** button in the Email… dialog footer. Uses the live subject field (falling back to the default subject) and the current group-by, so what you preview is what Send statement would send. Popup blocked → inline "Allow pop-ups to preview the statement." error (verified — the browser-pane test hit exactly this path). Nothing sends and nothing is audited to `gc_statement_emails` on preview.

The Share-all dialog was left without a preview for now — same pattern applies (`buildGcReviewShareAllEmailHtml`) if wanted later.
