# v2.2723 — Bridge link in the dev nav (2026-09-03)

Owner ask after v2.2677 shipped: put The Bridge in the dev menu. `Layout.tsx` gains a compass `bridgeIcon` (640-grid, same style as the other nav glyphs) and a dev-only `/bridge` NavLink in both places the dev links live — the header icon row (after Review) and the ☰ dropdown (after Moneyfill). Gate `role === 'dev'`, matching the page's own redirect. Help guide `the-bridge.md` names the icon and menu entry. Client-only.
