# v2.3324 — Test reports: the pinpoint paper dates its results section (2026-09-11)

Kernel only (`supabase/functions/_shared/testReport.ts` → `buildTestReportBlocks`); both renderers (jsPDF in the browser, pdf-lib on the server) and the modal preview follow because they walk the same blocks. No migration, no deploy strictly required — `auto-send-test-reports` and `send-test-report` bundle the kernel at deploy time, so redeploy both for server-rendered pinpoint papers to match (the office sends pinpoints by hand, so the browser is the usual renderer).

Owner, 2026-09-11: "In the pinpoint test report, the date should be moved to the test results section."

- Pinpoint: the *Date* row leads **Test results** (Date · Pinpoint location · Test method · Findings); the location column no longer carries it.
- Gas keeps its date under the location; hydrostatic keeps it in Test details. Tests pin all three.
