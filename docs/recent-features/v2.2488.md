# v2.2488 — Needs You: team reviews item (banner migration 2/6) (2026-08-29)

Second v2.2339 follow-up: `DashboardTeamReviewsDueBanner` (v2.960) folds into
the Needs You card. Stacked on v2.2487.

- **New hook `src/hooks/useTeamReviewsDue.ts`**: the banner's gate query
  (`users.team_prospects_access`) + roster/stamps/cadence loads +
  `overdueReviewSubjects`, extracted verbatim. Empty on no-access or load
  error — the item self-gates exactly like the banner.
- **Kernel**: inputs `teamReviewsOverdue` / `teamReviewCadenceDays`; item key
  `team-reviews` (blue, kicker "Team reviews", action "Open Team Review" →
  `/prospects?tab=team&stage=review&rate=<first-due-id>` — keeps the v2.1564
  land-on-first-person deep link). Inserted between `lost-bids` and
  `job-followups` to preserve the old banner-stack order.
- **Both surfaces**: Dashboard quick row and Quickfill's Needs You station
  wire the hook — Quickfill has no dedicated team-reviews station, so unlike
  job-followups this item shows on both pages.
- **`DashboardTeamReviewsDueBanner.tsx` deleted** — no remaining mount sites.
- Tests: 2 new kernel tests (ordering + preview/+N-more copy, singular +
  empty gate). Help guide `needs-you-card.md` updated.
