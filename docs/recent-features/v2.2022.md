# v2.2022 — Pay-speeds breakdown behind the forecast strip (2026-08-21)

Owner-approved mockup ("Pay Speeds Drilldown" artifact), built as presented. The Payment forecast's pay-speeds strip becomes one clickable box ("See the breakdown ›", hover ring) that opens [`PaySpeedsBreakdownModal`](../../src/components/jobs/PaySpeedsBreakdownModal.tsx) (z 70, above the forecast):

- **Three centered tiles** echo the strip (Company / Res / Comm medians + sample counts).
- **Where customers land** — two chart variants behind pills: *Every customer a dot* (days axis, dot size = open $, dashed company-median line, slowest customer direct-labeled, native-title tooltips) and *Count by speed bucket* (stacked Res/Comm counts, 0–7d … 45d+). Res `#3b82f6` / Comm `#d97706` validated for CVD + contrast on both surfaces.
- **By customer — slowest first** — ranked rows with median (red past 30d), payment count, open $ now, and a proportional speed bar; **thin history** customers (< `PAY_SPEED_MIN_SAMPLES`) sit in a muted tier since their forecasts use the company median.
- **Kernel**: [`paySpeedsBreakdown.ts`](../../src/lib/jobs/paySpeedsBreakdown.ts) (`buildPaySpeedsBreakdown` groups the same `StageRow[]` the forecast reads by customer — zero extra fetches; `bucketPaySpeeds` for the histogram; 4 tests). Render smoke added to the forecast modal's suite (strip click → dialog, variant flip, close).
- Help guide `see-when-a-customer-will-pay.md` gains "The pay-speeds breakdown" section.

Client-only — no migration, no edge functions. Next up (owner): a payment follow-up loop connecting promised dates, the forecast's Past-expected bucket, and a Money Opportunities card — plan + mockup in discussion.
