# v2.2349 — Groundwork: checklist_items.due_date (2026-08-26)

Tier 2a of the start/due-date design — migration only, no behavior change.

- **Migration `20260826171756`**: `checklist_items.due_date date NULL` +
  `checklist_items_due_after_start` check (due ≥ start, idempotent add).
  Details: `docs/migrations/20260826171756_checklist_items_due_date.md`.
- Semantics (implemented in Tier 2b): start = appears, due = late; NULL =
  legacy; one-offs only v1; occurrences still materialize on `start_date`.
- Tier 2b (separate PR after `db push` + `gen-types:linked`): Add/Edit modal
  "Due by" row, chip states ("due Fri, Sep 4" / "due today" / "N days
  late"), Dashboard My Inbox T-day source, Today-list sort, History/Review
  "done late" annotation, and `send-scheduled-reminders` re-keying (due
  bucket, day-before, escalation = days past due).
