# v2.2564 — Portal globe on Money waiting rows (2026-09-01)

Owner ask: the customer's portal link in the corner of every Money waiting
row, both states.

- `PaySpeedsBreakdownModal`: the row grid gains a fifth `auto` column holding
  the existing self-contained `CustomerPortalGlobeButton`
  (`customerId`/`name` from `MoneyWaitingRow`), wrapped in a
  stopPropagation span so it doesn't toggle the row. The header row stays
  visible when expanded, so one placement covers both states. Renders
  nothing for non-office roles (the button's own gate).
- Smoke tests stub the globe (it needs Toast/Confirm/Router providers and
  has its own surface).
