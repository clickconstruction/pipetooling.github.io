# v2.2724 — Signed records close with a framed signature block ("Option A") (2026-09-03)

Owner finding: the signed estimate record ended in the greyed-out acceptance form — future-tense disclosure, disabled checkbox, a Full-name field that looked broken. Mockup artifact 0f4c0aee; the owner picked A (the DocuSign-style framed mark).

- **`SignedSignatureBlock`** ([`components/SignedSignatureBlock.tsx`](../../src/components/SignedSignatureBlock.tsx)): the mark (cursive typed name or the drawn image) in a thin orange frame tagged *Signed electronically* / *Signed on paper*, the short record ID on the frame's bottom edge, printed name + time (CT) to the right, and one facts line — ✓ *Consent recorded · agreed to…*, method (typed/drawn on the estimate page, on their phone, in person, on paper), IP · device. Theme tokens throughout.
- **Record ID kernel** [`lib/signedRecordId.ts`](../../src/lib/signedRecordId.ts) (+tests): `E84-9F3A2C` / `J922-1B0C4D` = prefix + number + six hex of the row id; `describeUserAgent` (iPhone · Chrome); method labels. Deno twin in `_shared/jobContract.ts`.
- **Where it replaced the form**: `EstimateAcceptBody`'s read-only branch (`staffAcceptedRecord` gains `ip` / `userAgent` / `recordId`; the Estimates detail page and `CustomerAcceptanceRecordBody` pass them), the customer's contract page after signing, the contract HTML builder (office preview / Print — framed `.frame` with tag + id + name/time), and the PDF builder (`drawRectangle` frame, tag and record ID on the edges, name + time right, audit line below). `sign-job-contract` and `share-job-contract` stamp the record ID and a `whenLabel`; the audit line becomes *Consent recorded · typed · Sep 2, 2026, 7:14 PM CT · ip*.
- Dropped from records: the pre-signing disclosure paragraph, the disabled checkbox, the name field. The live accept pages keep their forms.
