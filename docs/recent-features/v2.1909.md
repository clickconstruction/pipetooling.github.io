# v2.1909 — Pipeline refresh part 2: Sent urgency + the accepted nag (2026-08-20)

Second car of the owner-approved Pipeline Refresh prototype. [`estimatePipelineRefresh.ts`](../../src/lib/estimatePipelineRefresh.ts) gains `computeSentWait` (+3 tests, `nowMs` injected): day-granular waiting time from `estimates.sent_at` — neutral under 7 days, `warn` at 7+ ("sent 9d ago — nudge?"), and `overdue` (red) the day after a CO's `change_order_fields.response_requested_by` passes — overdue beats age even on a fresh send. In [`Estimates.tsx`](../../src/pages/Estimates.tsx) the Sent rows' Status cell (table + narrow cards) renders the wait label in muted/amber/red; accepted rows without `job_ledger_id` get the amber "not on a job yet" chip beside the existing Create job / Apply to job button (fragment-wrapped into both ternaries). Verified live against prod: "sent today" on the fresh sent row, two nags on the unlinked accepted rows, linked rows' green Job # untouched. Ledger money view is the prototype's last car. Client-only — no migration.
