# v2.1937 — Goals strip "now:" list truncation (2026-08-20)

Follow-up to v2.1936 (centered Review column): a goal with a wide work
front (15 current stages on the prod Default roadmap) wrapped its card to
four lines.

- New kernel `goalsStripNowSummary(currentStages, limit = 3)` in
  `src/lib/roadmapBridge.ts` (+ tests): joins the first `limit` stage
  titles and folds the rest into `… +N more`.
- Review tab's Goals card (`src/pages/Checklist.tsx`) renders
  `now: <summary>` through it; the "all stages complete 🎉" fallback and
  the "· N assigned tasks open" suffix are unchanged.
