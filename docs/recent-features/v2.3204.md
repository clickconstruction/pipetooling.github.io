# v2.3204 — The review caption names the reviewer (2026-09-09)

Client only. Found on the first live Mark reviewed (ZZ Test, right after the v2.3201 push): the strip read *Reviewed Wed 9/9* with no name. The stamp was right (`reviewed_by` was written); the caption's name lookup never re-rendered.

- [`src/hooks/useUserDisplayNames.ts`](../../src/hooks/useUserDisplayNames.ts): the hook fetches missing names into a module cache and bumps a tick, but the memo that builds the `id → name` map depended only on the id set, so the first render's empty map stuck until the ids changed. The tick is now a dependency; the caption reads *by Robert Douglas · Wed 9/9* once the one chunked `users` read lands (a re-render later, no flicker on the cached path).

Help unchanged. Release note `v2.3204.ts`.
