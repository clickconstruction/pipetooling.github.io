# v2.2244 — fix(jobs): Edit Sub Labor persists the job pick instantly (2026-08-24)

Reported live: a sheet stuck on a dead number ("944" — no such job in the
ledger) was re-pointed at J888 · Heron Construction in Edit Sub Labor, but the
modal was closed without pressing Save — and the pick silently vanished. In
edit mode only the Save button wrote `people_labor_jobs`, while `closeLaborModal`
discarded everything; nothing warned about the unsaved job change.

- **`persistPickedJobForEdit`** (JobsSubLaborFormModal): in edit mode,
  `onPickJob` now writes `job_number` + `address` to the sheet immediately —
  the same instant-save contract the invoice-link field already had
  (`saveLaborInvoiceLinkDraft`). Success shows a toast ("Sheet moved to
  J888 · Heron Construction — saved"); failure surfaces via the form error.
- Then `loadLaborJobs()` refreshes the ledger, and the existing
  `onLaborJobsReloaded` sync swaps the open modal's row.
- New mode is unchanged (nothing exists to update until first Save), and the
  Save button still persists crew/items/date edits as before.
- Render smoke: picking a job in edit mode fires the instant save
  (asserted via the confirming toast).
