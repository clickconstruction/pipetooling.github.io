# v2.2976 — One company, Phase 3b: "master" → "leader" in the copy, and the anon revoke (2026-09-07)

Phase 3b of [`docs/ONE_COMPANY_PLAN.md`](../ONE_COMPANY_PLAN.md) — Will's decision 4: the role name "master" becomes **"leader"** in copy only; the enum value `master_technician`, every identifier and every table name stay. Plus one migration (`20260906210000_company_owner_rpc_revoke_anon.sql` — **`supabase db push` after merge**).

## What shipped

- **`scripts/leader-rename.py`** (new): word-boundary rewrite of master / masters / Master / Masters / master technician(s) / master tech(s) → leader forms; never inside identifiers (`master_technician`, `master_user_id`, `masterUserId`, `pinAPMasterIds`), never before *subcontract*, *plumber*, *secret*, *record*, *key*, *copy*, *list*, *bath*, never on fixture ids like `master-1`. 239 replacements: every `src/content/help/*.md`, the role label maps (`roleLabels.ts` + its Deno twin `supabase/functions/_shared/roleLabels.ts` → **redeploy `invite-user`**, `roleGuideLens`, `bankingAttributionOptions`, `usersTabRosterRoleSections`, `peopleUsersTabShared`, `deskRailAttention`, Person Desk / Quick Sheet kinds, the work-order assembler map, Crew Review Deck, feedback rows), and the toasts / placeholders / headers on Settings (People matrix, Pay Approved Leaders, Convert leader, Dashboard pins), People → Users, the share-report modals, Paid-in-full emails, recurring reports, workflow step search, stages payments, lien inbox, capacity view, the sheet rail hint.
- **Hand edits after the sweep**: `BidsEstimatorsExtraUsersModal` keeps its `case 'master'` data key; `how-the-app-works` describes one company ("The company has Customers — nobody owns a customer", a "One company" section in place of "Sharing"); `start-here-as-a-master` keeps its slug (deep links) with the title "get started as a leader".
- **Developer docs**: `GLOSSARY.md` (`master_technician (Leader)`), `ACCESS_CONTROL.md` role list + heading, `PROJECT_DOCUMENTATION.md` role line, the plan doc's progress (Phase 4 next). Technical prose keeps "master" where it names the enum or a column.
- **Migration**: `REVOKE EXECUTE ON FUNCTION public.company_owner_user_id() FROM PUBLIC, anon` — the publishable key could read the company owner account id with no session (found 2026-09-07 while verifying the Phase 1 push).
- Tests updated: `roleLabels`, `roleGuideLens`, `teamLeaderboard`, `deskRailAttention`.
