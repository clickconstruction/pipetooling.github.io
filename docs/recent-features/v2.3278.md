# v2.3278 — Bids: the flow pill stacks ticks over words; the unfolded strip centers its steps (2026-09-11)

Client only; no migration. Owner, with two screenshots of BP403's Pricing tab: "on bids when this is expanded and showing, I think that it should be centered," then on the inline pill: "I don't think this should be side by side. I think the bars should be on top of or below the three of ten done next review."

## What changed

- **The inline pill stacks** ([`BidFlowStrip.tsx`](../../src/components/bids/BidFlowStrip.tsx), `inline` variant): a column — the ten ticks on the top line, each `flex: 1` so the bar spans the pill; the summary, review stamp and chevron centered underneath. Radius 10 instead of a full pill. Both pill buttons drop the `font` shorthand for `fontFamily` (the v2.770 shorthand-reset gotcha; this button re-renders on every fold).

- **The unfolded strip centers**: the step grid (ten columns, `maxWidth` = steps × 88px) gets `margin-left/right: auto` when the strip renders bare (`hideHeader`, the unfolded drawer under the title row on Counts, Takeoffs, Labor, Pricing and Cover Letter — v2.3241). The header-bearing full strip on the Bid Board's expanded row keeps its left alignment under "Where this bid is".

## Testing

`npm run typecheck`, `npm run lint`, `npm test`, `npm run build`. A one-property layout change on a grid with an explicit max width; auto margins center it inside the card's block box.
