# v2.3133 — `/estimate/terms` paints its own light ground (2026-09-08)

Found by the v2.3118 live screenshots taken after dark: the public Terms page wraps itself in `data-theme="light"` (customer-facing surfaces stay light) but never painted a background, so the app's time-of-day dark body showed through behind light-theme ink — the terms body and the new **Electronic signatures and records** section were invisible in the evening (headings survived only because `h2` takes the body's colour). [`EstimatePublicTerms.tsx`](../../src/pages/EstimatePublicTerms.tsx) now gives the wrapper `background: var(--surface)`, `min-height: 100vh` and `color: var(--text-strong)`, the way `AuthPublicLandingLayout` does for the other public pages. Client only.
