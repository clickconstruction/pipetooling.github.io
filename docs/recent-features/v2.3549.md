# v2.3549 — Stages tab decomposition, PR 11: the ☰ section-tools menu is its own component (2026-09-16)

Client only, behavior-preserving. PR 11 of the train in `to-dos/engineering-hygiene.md` — the single-opener region the map named after the jump strip left in v2.3534.

## What moved

[`JobsStagesSectionToolsMenu.tsx`](../../src/components/jobs/JobsStagesSectionToolsMenu.tsx) — the ☰ trigger beside the jump strip and its grouped menu (v2.1419, in-strip since v2.1421): the stage section headers' action buttons, reachable without scrolling the board. The items still come from the tested kernel `buildStagesSectionToolsMenu`; the component takes that kernel's `inputs` (role, the unfiltered billed and collections counts, the AR unallocated count, the capable-to-bill figure, the recent-view flag, the lien-desk count) and an `onSelect` record with the **fourteen doors**, which stay in the tab as `sectionToolsOnSelect` because every one of them sets a tab-owned modal flag. The menu closes itself before it calls the door picked, exactly as before.

**The open flag moved with the menu.** Unlike the ⋯ tools menu (whose open state the board's whole-row load condition reads, v2.3532), nothing outside this block read `stagesSectionToolsMenuOpen`, so per the playbook it is now the component's own `useState`. The tab has one less state hook and two fewer imports.

Numbers: `JobsStagesTab.tsx` 4,861 → 4,752 lines.

## New coverage

[`JobsStagesSectionToolsMenu.render.test.tsx`](../../src/components/jobs/JobsStagesSectionToolsMenu.render.test.tsx): closed until the trigger is pressed and then the kernel's items for a dev with the Accounts Receivable badge and GC Review disabled while both billed groups are empty; a pick closes first and calls its door; the backdrop closes without calling anything.

## Verified

- Typecheck, lint (0 errors; the same five pre-existing warnings), theme check, full suite.
- Live on prod data at Jobs → Pipeline as dev: the trigger reads closed, opens the menu with all fourteen items (Recently added, Stage moves this week, Weekly money movement, Capable of Being Billed: $41,286, …, Lien desk with its count of 11), and Accounts Receivable closes the menu and opens its dialog.
