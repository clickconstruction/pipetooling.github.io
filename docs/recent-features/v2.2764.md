# v2.2764 — GC Review: Include Collections on by default, left of Share all (2026-09-04)

Owner ask, screenshot of the modal header: "Include Collections (8 · $25,517.97) should be selected by default and to the left of Share all and Print All."

- [`JobsGcReviewModal.tsx`](../../src/components/jobs/JobsGcReviewModal.tsx): `includeCollections` starts `true`; the checkbox moved out of its own centered row into the Share all / Print all row, first in line (the row always renders now; the two buttons still need at least one group).
- **Certification pinned to active-only groups.** Certify status compares a GC's rows against the certified snapshot, so a displayed group that gained collections rows would have read "Changed since certified" the moment the default flipped — and a fresh certification would have snapshotted collections rows. New `certGroupByGc` (from the existing active-only `roundRollup`) is now the basis for the header chips, the Certify / Re-certify buttons (the checklist opens on the active-only group), and the week strip's `gcReviewWeekProgress`. A GC whose only outstanding jobs are in collections shows no certify chip or button — nothing to certify, nothing in a round. This also fixes the pre-existing quirk where ticking the box flipped certified GCs to "changed".
- Help guide [`track-a-general-contractor-on-a-job`](../../src/content/help/track-a-general-contractor-on-a-job.md): the bullet now says default-on, where the box is, and that certification / rounds ignore it.

Client only — no migration.
