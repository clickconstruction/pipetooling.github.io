# v2.2566 — EMAIL_FROM sender is ClickTooling (secret + repo mirrors) (2026-09-01)

Closes out the last "PipeTooling" sender identity from the DOMAIN_CUTOVER text
sweep (v2.2525 → v2.2526 → v2.2561):

- **`EMAIL_FROM` function secret** flipped to
  `ClickTooling <team@noreply.clicktooling.com>` (was display name
  "PipeTooling") via `supabase secrets set` on 2026-09-01. Running functions
  pick it up on next cold start.
- **Repo mirrors updated in the same breath** (the two spots that hard-code the
  sender string):
  - `supabase/functions/_shared/emailFrom.ts` fallback + doc comment — needs
    redeploy of its importers (run `npm run check:edge-drift` after merge for
    the exact list).
  - `ESTIMATE_EMAIL_FROM_LABEL` in `src/pages/Estimates.tsx` (the "From:" line
    shown in the estimate email preview).
- **Supabase Auth SMTP sender** renamed "Pipe Notifications" →
  **"Click Notifications"** by the owner in the dashboard (2026-09-01, project
  auth → SMTP settings; address already `team@noreply.clicktooling.com`).
  Recorded here because v2.2525/v2.2526 listed it as outstanding.

With this, all sender identities are ClickTooling. Remaining deliberate keeps
are unchanged from v2.2526 (functional identifiers, pipetooling.com redirect
URLs, twin-doc surface).
