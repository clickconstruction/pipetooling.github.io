# v2.1961 — Fix-ups strip on Today's Money Opportunities (2026-08-21)

The three conditional data-gap alert buttons that floated at the right of the
stage-jump strip row — **No customer** / **No customer pictures** (red) and
**No email** (amber) — now dock as a quiet **Fix-ups** footer strip inside the
Today's Money Opportunities card on the Pipeline **New** view.

- New kernel builder `buildPipelineFixups({noCustomer, noPictures, noEmail})`
  in `src/lib/jobs/pipelineOverview.ts` (+2 tests): pill labels/tones/titles;
  zero counts drop, all-clean returns `[]` and the strip hides entirely.
- `PipelineOverview` gains `fixupCounts` + `onFixup(key)` props and renders
  the strip after the move rows (bg-subtle footer, pill chips in the alerts'
  existing red/amber palette, "missing data blocks billing — click to fix"
  hint). The strip renders even when the opportunities queue itself is empty.
  Render smoke added (`PipelineOverview.render.test.tsx`, 3 tests).
- `JobsStagesTab` passes the three existing counts and routes clicks to the
  same `StagesAlertJobListModal`s; the classic strip-row buttons hide while
  the New view is active (Old view unchanged — no duplication).
- **Bug fix riding along**: the strip-row wrapper's render gate only checked
  the first two counts, so a lone No-email alert never rendered; the gate now
  includes it.
- Help guide `read-the-pipeline-money-view` documents the strip.
