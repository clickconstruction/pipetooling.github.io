# v2.2715 — Margin guide follows the brush; dead per-row margin picker removed (2026-09-03)

Follow-up to v2.2707 found while verifying the live bundle: the help guide `price-a-bid-by-margin.md` still taught the Old grid's **Margin mode** toggle ("next to Apply Matching Price Book Entries"), and the Workbench guide's brush paragraph still said the recents were "shared with the Old grid's Margin-mode chips".

## Client only — no migration

- [`BidsPricingTab.tsx`](../../src/components/bids/BidsPricingTab.tsx): the per-row **Margin for …** picker modal, its `marginPickerRow` / `marginPickerCustom` state, and `applyMarginToSingleRow` are removed — the only opener (the Old grid's "…" cell) went in v2.2707, so `setMarginPickerRow` was only ever called with `null`. The brush (`armBrush`, sweep batch, Undo sweep), `recentMargins`, and the `unitPriceForTargetMargin` / `updateRecentMargins` kernel calls it shares stay.

## Docs

- `price-a-bid-by-margin.md` rewritten around the brush: pick up (🖌 Margin › on the totals strip, recents chips), sweep (📌 / fixed / uncosted rows never painted, batch save, ↩ Undo sweep, ‹ / Esc), and the same cost ÷ (1 − margin) math.
- `price-a-bid-with-the-workbench.md`: the brush paragraph no longer references the Old grid.
