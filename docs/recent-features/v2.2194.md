# v2.2194 — Review tab tiles: centered, captioned, tappable (2026-08-23)

Owner-approved mock-up (artifact 7a5678e4, Option A + centered text). [`Checklist.tsx`](../../src/pages/Checklist.tsx) Review tab:

- **Kernel [`checklistReviewTiles.ts`](../../src/lib/checklistReviewTiles.ts)** (+4 tests): `signOffTileCaption` / `outstandingTileCaption` ("one-offs · 7 people · oldest 118 days", singulars handled) / `missedTileCaption` (weekday list in week order from the missed instances' dates) / `reviewTileTone` (zero = quiet grey; blue sign-off, red outstanding, amber missed).
- `boardTile` (static div) → `reviewTile`: a centered `<button>` (label, tabular number, caption, corner ›), blue outline on the tile whose list is showing (Outstanding for the one-offs/next pills, Missed for the missed pill, To sign off while the fold is open). Taps: sign-off opens the fold and scrolls (`scrollMarginTop`), Outstanding scrolls to the by-person board (restoring one-offs when on missed), Missed switches the pill + scrolls.
- Missed-this-week query upgraded from count-only HEAD to `scheduled_date` select (limit 500, count exact) to feed the weekday caption.
- Live check (375px, prod data): One-offs → Outstanding 13 lit; tap Missed → week tile lit and the middle tile mirrors the missed board (543 all-time · 8 people · oldest 187 days — the backlog number the old strip never surfaced); tap Outstanding → back. Help: `review-my-teams-completed-tasks.md`. Client-only.
