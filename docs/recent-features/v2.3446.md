# v2.3446 — `src/types/database.ts` regenerated after the owner-confirm push (2026-09-15)

Mechanical; no migration, no edge function. `npm run gen-types:linked` after `20260914260000_owner_confirm_list.sql` (v2.3447) was pushed (drift 578/578). The hand-edited entries that PR carried — `customer_addresses.owner_confirmed_at / owner_confirmed_by` and the `list_jobs_owner_to_confirm` RPC — are replaced by the generator's output, same fields in the generator's order (generated files are regenerated, never hand-merged). Typecheck clean.
