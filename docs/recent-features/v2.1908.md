# v2.1908 — Pipeline refresh part 1: the sweep + the Readiness column (2026-08-20)

Owner-approved prototype (Estimates Pipeline Refresh artifact), first train car. New kernel [`estimatePipelineRefresh.ts`](../../src/lib/estimatePipelineRefresh.ts) (+6 tests): `isEmptyEstimateDraft` (draft with no customer, no title, no meaningful lines — the legacy "Custom Service Visit" stub in either shape and blank rows don't count — no CO narrative, no terms) and `computeEstimateListReadiness`, which maps a list row onto the v2.1865 `estimateDraftSteps` kernel (customer email read from the embedded `customers.contact_info`; notify defaults to 1 and attachments to absent, so the list can only understate readiness). In [`Estimates.tsx`](../../src/pages/Estimates.tsx): empty drafts are filtered out of the Unsent section entirely and surface as one header button — "🧹 Clean up N empty drafts" — behind an in-app danger confirm (v2.1874 dialog), deleting via `.in('id', …).eq('status','draft')`; the Status cell for drafts (table + narrow cards) becomes the readiness line (green ● done / ○ remaining dots + the rail's short tokens, "ready to send" in green when nothing's left); and line-less drafts render "—" instead of $0.00 in both Total cells. Verified live against prod: Unsent collapsed 19 → 4 real rows behind a "Clean up 14 empty drafts" button (confirm shown, canceled — deletion left to the owner), readiness lines correct per row. Sent/Accepted urgency and the Ledger money-view are the remaining prototype cars. Client-only — no migration.
