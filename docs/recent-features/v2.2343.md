# v2.2343 — Email payment card: real rounded corners + inset rule (2026-08-26)

Follow-up to v2.2338 (the queue locked its branch mid-merge, so this shipped
separately). Two rendering fixes to the email card:

- `border-collapse:separate` on the card table — collapsed borders make
  browsers and mail clients drop `border-radius`, so the "rounded" corners
  were rendering square.
- The rule above Balance due becomes an inset bordered-div row (padded cell)
  instead of cell `border-top`s, so it stops short of the card's edges.
- v2.2338's fragment updated to describe the final card.
