# v2.3338 — Test reports PR 15: email yourself a sample at any time (2026-09-11)

Owner, 2026-09-11: "is there a way for a dev to send a test of any of these reports at any time? if not I would like to add this." The sample card could open the four PDFs (v2.3296) but nothing emailed them. Edge function `send-test-report` changed (redeploy); no migration.

- **`send-test-report` sample mode**: `{ sample: true, subject, email_text, email_html?, pdf_base64, pdf_filename?, report_label? }`. Dev only (`users.role` via the service role), recipient forced to the caller's login email, subject forced to start with `[Sample] `, no row read, no storage, no stamp, no activity line; the `email_send_log` line is written like any test-report send.
- **Kernel** `testReportSampleEmail(id, settings, today)` in [`testReportSample.ts`](../../src/lib/jobs/testReportSample.ts): the sample data + job, the send sheet's email built from the current Settings with a dead Stripe-shaped pay link (`TEST_REPORT_SAMPLE_PAY`) so the link paragraph renders, the attachment name. Test pins the subject and the link.
- **Client door** `sendTestReportSample(id, settings)` in [`sendTestReport.ts`](../../src/lib/jobs/sendTestReport.ts): jsPDF render, base64, invoke.
- **Card** [`TestReportSampleCard`](../../src/components/settings/TestReportSampleCard.tsx): a second row of ✉ pills under the open-PDF pills, with the signed-in email named; success line / error line inline.
- Guides: `see-what-customers-see.md` gains **Email yourself a test report**; `file-a-test-report.md` Notes point at it. `EDGE_FUNCTIONS` + `ACCESS_CONTROL` updated.

Verify: `npx vitest run src/lib/jobs/testReportSample.test.ts`; after `supabase functions deploy send-test-report`, Settings → What customers see → ✉ Gas test → the inbox shows *[Sample] Gas Test Report — 112 Seidel St* with the PDF; a non-dev JWT gets 403.
