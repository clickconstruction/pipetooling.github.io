# v2.2608 — Division 22 audit: full-height section picker (2026-09-01)

Owner request after using the audit live: the section dropdown showed ~3
rows of the 17-section catalog. One-prop adoption of SearchableSelect's
existing `fillViewportHeight` (grows a downward-opening portaled list to
the viewport bottom, never below the fixed cap; flip-above keeps the
cap) on the audit modal's picker — with `portalZIndex` (v2.2607) it now
extends past the modal chrome and shows all sections in one open list.
Verified visually on dev: list ran 293→1110px, all 17 options rendered,
no inner scroll.
