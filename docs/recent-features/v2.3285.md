# v2.3285 — "Their Word" PR 4: payment terms on the customer — the lever — on Edit customer, Customer review, New Bid, New Job, and the demand letter (2026-09-11)

Migration `20260911055837_customer_payment_terms.sql` ([`docs/migrations/20260911055837_customer_payment_terms.md`](../migrations/20260911055837_customer_payment_terms.md)); no edge function. Last of the four-PR train (v2.3280 → v2.3283 → v2.3284 → this). Ships the regenerated `src/types/database.ts` for all three migrations.

## Why

A number nobody acts on is a dashboard. The owner's stated goal was to work difficult customers out of the book; the record (PR 3) only has to be good enough to prompt that conversation, and the **terms flag** carries the decision onto every screen where the office would otherwise say yes by habit.

## What shipped

- **`customers.payment_terms`** (`standard` · `deposit_required` · `no_new_work_past_promise` · `winding_down`, default standard, CHECK), **`payment_terms_note`**, **`payment_terms_set_by/at`**. Additive; the table's existing RLS governs writes (masters own, adopted assistants, estimators — the UI shows the control to dev / master / assistant-like only).
- **Kernel [`src/lib/customerPaymentTerms.ts`](../../src/lib/customerPaymentTerms.ts)** (+7 tests): the four terms with labels and hints, `parseCustomerTerms` (missing column → standard), and `customerTermsWarning(terms, record)` → `{ severity: 'warn' | 'stop', headline, detail, note } | null` — silent for standard terms with nothing broken; **warn** for deposit required, no-new-work standing, or a promise broken right now on standard terms; **stop** for winding down, or no-new-work while a promise is broken. `detail` is PR 1's record line plus "N bills open past promise".
- **Hook [`useCustomerTermsWarning`](../../src/hooks/useCustomerTermsWarning.ts)** — fetches the customer's terms columns (with the setter's name) and the promise record (office roles) for one customer id, fail-soft; `refreshKey` reloads after an edit.
- **[`CustomerTermsBar`](../../src/components/customers/CustomerTermsBar.tsx)** on **New Bid / Edit Bid** (`BidFormModal`, above the GC recipients row, payer = `gcCustomerId || editingBid.customer_id`) and **New Job / Edit Job** (`JobFormModal`, above the customer fact rows, payer = `gcCustomerId || customerId`), each with a *Terms* button (office roles) that opens **[`CustomerTermsModal`](../../src/components/customers/CustomerTermsModal.tsx)** — four radio cards + note, writes the columns with `set_by/at`, shows the record line as context.
- **Edit customer** (`EditCustomerForm`): a *Payment terms* select + note under the standing discount, office roles only; written only when changed, stamped.
- **Customer review** (`BidBoardCustomerReviewModal`): three columns after the name — *Pays in* (`paySpeedSpread` over the pay-speed receipts, from `get_billed_customer_pay_speeds`), *Their word* (`keeps N of M · slips ~Nd`, office roles, from `list_payment_promise_records`), *Terms* (chip, amber when non-standard, note on hover, `set terms…` / `edit terms…` → the modal). Loads fail soft; the footer spans the three.
- **Demand letter** (`LienInstrumentsModal`): the dated notice history gains one line per live promise on the job — *Payment promised by Sep 12 — by the customer, in writing, from their statement page* / *by Tanya by phone to Taunya* — from `list_job_payment_promises`, fail-soft.
- Help guide `know-whether-a-customer-keeps-their-word`: new **Deciding what to do about it** section; `ACCESS_CONTROL.md` and `GLOSSARY.md` entries.

## Verified

Migration replayed twice on the scratch Postgres 16 (idempotent; CHECK refuses `'bogus'`). Dev server: New Bid shows the bar for a customer set to Deposit required and stays silent on standard; Customer review renders the three columns and the terms modal round-trips. typecheck · lint · `npm test` · theme check.

## Migration push note

All three train migrations (20260911051414, 20260911052931, 20260911055837) were applied to prod at 05:59 UTC on 2026-09-11 by one `db-push.sh` run intended for PR 1 only — `git checkout origin/main -- supabase/migrations` restores main's files but does not remove branch-only ones. PR 2 and this PR merge with the exact filenames, so the ledger reconciles; recorded in memory as the db-push-from-a-feature-worktree gotcha. Do not repeat.

## Notes for the next session

- `no_new_work_past_promise` **warns, it does not block** — a hard block on New Job / New Bid is an owner decision (the mock-up said "blocked"; the bar says "No new work — a payment promise is broken" in red instead).
- The job-form bar keys the payer as `gcCustomerId || customerId`; the Stages reliability line keys the same way; pay-speed receipts still key on `customer_id` (PR 3 note).
- Deferred from the plan, on purpose: the "Their word" six-column lens, Bid Board numbers, the Needs You card — revisit once a month of promises exists.
