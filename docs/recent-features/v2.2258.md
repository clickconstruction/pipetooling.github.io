# v2.2258 — Cost lens opens to the controller (2026-08-24)

v2.2250 shipped task cost estimates dev-only; the controller already reads the
payroll wages the estimates derive from, so the lens opens to them.

- **Migration `20260824235336_checklist_item_costs_controller.sql`** — the
  `checklist_item_costs` policy widens to `is_dev() OR is_controller()`.
  Deliberately NOT `has_payroll_access()`: that also covers pay-approved
  masters, who don't get the cost lens yet.
- **One shared client gate** — new `canSeeTaskCosts(role)` in
  `src/lib/checklistCostEstimate.ts` (+test) mirrors the RLS. Swept every
  `isDev` cost gate: Review rows' 🖩/chip + person totals + Goals-ledger stage
  costs (`Checklist.tsx`, threaded as a `canSeeCosts` prop), the estimates
  hook's `enabled`, and the tech-tree tab's Plan-view pass (prop renamed
  `isDev` → `showTaskCosts`).
- The estimator modal needs no change: `people_pay_config` reads are already
  controller-visible via `has_payroll_access()`.
- `docs/ACCESS_CONTROL.md` controller section updated. New help guide *put a
  cost estimate on a task* (roles: dev, controller).
- Verified: `canSeeTaskCosts` unit-tested over all 9 roles; controller-session
  RLS not exercisable via dev-login (fixed identity) — policy reviewed against
  the v2.2250 original.
