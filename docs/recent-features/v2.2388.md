# v2.2388 — Solver: "or total" + Solve as one wrap unit (2026-08-27)

Estimator feedback (Wendi): Solve fires the target-total solve, but sat as a
separate flex child — the ring's 0.8rem column gap held it away from the
field, and a narrow ring could wrap Solve ▾ onto its own line without the
field it belongs to.

`BidsPricingTab.tsx`: the `solveMenuRef` span (Solve + ▾ menu) moves inside
the "or total" unit span, so the unit's tighter 0.45rem gap spaces field →
Solve and the whole "or total $___ Solve ▾" wraps as one piece. Layout only —
handlers, menu, and Enter-to-solve unchanged.
