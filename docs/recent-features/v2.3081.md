# v2.3081 — Sheet form leftovers: edit-open pick and work-order trade read the link (2026-09-08)

Two sites in [`JobsSubLaborFormModal`](../../src/components/jobs/JobsSubLaborFormModal.tsx) the v2.3065 pass missed, found by re-grepping main after the conversion train: the edit-open seed (`setLaborPickedJobId` from `resolveSubLaborJobByNumber`) and the `SubSheetWorkOrderPanel` `defaultServiceTypeId` fallback (`jobs.find(hcp === job_number)`). Both now read `job_ledger_id` first and keep the number only for a sheet with no link. Client-only. With this, every reader in `to-dos/sub-sheet-job-link-followups.md` is on the link; only the dated cleanup remains.
