# v2.1975 — Pin Mode covers the Dispatch Mode pages (2026-08-21)

Follow-up to v2.1972 (owner request "make it work in dispatch mode"): the pin footer never appeared inside the dispatch shell because none of its routes were in `PINNABLE_PATHS`. [`pinnedTabs.ts`](../../src/lib/pinnedTabs.ts) adds `/dispatch-mode`, `/dispatch-mode/schedule`, `/dispatch-mode/inbox`, `/dispatch-mode/customers`, `/dispatch-mode/po` with "Dispatch …" labels; `getTabFromPath` already returns undefined for tab-less paths and Layout's footer gate needed no change. Verified live in the dispatch shell: Pin Mode on → footer on `/dispatch-mode/schedule` → pin wrote `{path, label: 'Dispatch Schedule'}` to `user_pinned_tabs` → unpin removed it. Client-only — no migration.
