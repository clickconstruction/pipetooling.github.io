# v2.1982 — Customer portal spine + statement page (2026-08-21)

Portal train PR 1 (plan: claude.ai artifact "Click Customer Portal"). The
no-login customer/GC portal behind a minted capability link.

- Migration [`20260821213000_customer_portal_links.sql`](../migrations/20260821213000_customer_portal_links.md):
  `customer_portal_links` (customer_id, audience customer|gc, token_hash
  sha256 — raw shown once, estimate-view model; partial-unique active link
  per customer+audience) + `mint_customer_portal_link(p_customer_id,
  p_audience, p_rotate)` (office writers; returns raw token once, or
  `exists`+`activeSince` when active and not rotating). pgcrypto extension
  ensured for `digest()`.
- New edge fn `customer-portal` (`verify_jwt=false`): token → letterhead
  block (`_shared/portalCompany.ts` constants), open billed lines (invoice
  remainders via invoice-linked payments; line-less billed jobs fall back to
  job remainder), Stripe pay URLs, total due, requestable jobs. Deployed
  with this PR's merge.
- `/portal` route + `CustomerPortal.tsx`: the approved "account statement"
  design — letterhead with license line, ruled ledger rows (tabular
  numerals), accounting double-rule total, PAY ONLINE ink-block vs
  check-reference chip, all-paid and revoked-link states. Deliberately
  single-theme light (customer-facing pins light).
- Kernel `src/lib/portal/portalPayload.ts` (+6 tests): defensive payload
  parse (drops non-https pay urls, recomputes totals) + statement date/money
  formatters. Page render smoke (+4 tests).
- Help guide arrives with the globe entry-point PR (that's the staff-facing
  flow); request forms + notifications are the next train cars.
