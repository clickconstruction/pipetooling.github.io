# v2.3636 — Sub portal in Español: the signature form's own words (2026-09-20)

Item 2 of [`to-dos/subs-residuals.md`](../../to-dos/subs-residuals.md) (deferred since v2.2667). Client only.

The to-do named `SignedSignatureBlock.tsx` / `ReportTemplateSignatureField.tsx`; the sub portal uses neither. Its signing step renders [`ContractAcceptSignatureForm`](../../src/components/contracts/ContractAcceptSignatureForm.tsx) over [`SignatureTypeOrDrawInput`](../../src/components/contracts/SignatureTypeOrDrawInput.tsx). The portal already passed Spanish for the heading, the disclosure, the consent, the agree label and the submit label — the words **inside** the two components were English: *Your name*, the placeholder, *Type* / *Draw*, *Sign below (use your finger or mouse)*, *Clear signature*, *Submitting…*, two aria labels, and three of the four field hints (the consent hint alone had an inline `es` branch).

- [`signatureFormStrings.ts`](../../src/lib/signatureFormStrings.ts) — one `en` / `es` table and `signatureFormStrings(lang)`; anything but `es` is English. A test walks every key: none blank, none left equal to the English.
- Both components take `lang?`. The form defaults to its consent's language, so a caller that already passes a Spanish consent gets a Spanish form without a second prop; `SubPortal` passes `lang` explicitly. The inline consent-hint branch is gone — it reads the table like the rest.
- Lien release signing, the customer's agreement page and the sub contract page pass no `lang` and render exactly as before.

Tests: the strings table; a render test (Spanish label, placeholder, group label, Dibujar → the draw hint and Borrar firma, the name hint on an empty submit; English default). The form's existing *Spanish surface end to end* test had been typing into the **English** placeholder — it now finds the Spanish one.

Left in the to-do: persisting the derived sheet stage, the portal's stage parity, item 3's chips and templates, the owner's roster hygiene.
