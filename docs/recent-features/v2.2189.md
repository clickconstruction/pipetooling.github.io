# v2.2189 — Quickfill: default banner questions, Warnings dedupe, "(Old)" retired, open legend (2026-08-23)

PR C of the Quickfill tune-up (mockup https://claude.ai/code/artifact/50741c3c-293e-4eae-a897-83076baad9aa, screen C).

- **Banners**: new [`src/lib/quickfill/sectionBanners.ts`](../../src/lib/quickfill/sectionBanners.ts) — `QUICKFILL_DEFAULT_SECTION_BANNERS` (one question per section id) + `defaultQuickfillSectionBanner()`. `effectiveQuickfillSectionBanner` = stored custom banner (app_settings) else the default; the dev panel's placeholder shows "Default: …" for any section. The three ad-hoc constants (Schedule / Tomorrow / Jobs Cleanup) moved into the map.
- **Warnings**: the `DashboardArBankUnallocatedBanner` + its `ar-bank-unallocated` metric leave Quickfill (now the "Allocate N bank deposits" card in Jobs Cleanup); `useArBankUnallocatedCount` is no longer called from this page.
- **Registry**: `hours` ("People Hours (Old)") removed with its wiring case; `HoursSection` stays (People → Review uses it). Past marks remain in `quickfill_section_marks`.
- **Legend** under the search box: "open = items waiting on someone in the office · — = this section has no count".
- Verified locally at 1280: 19 banners (every rendered section), Warnings shows only the stale-purchases banner, no "(Old)" chip.

Client-only.
