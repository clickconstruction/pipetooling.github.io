# v2.2375 — Manage Timeline Gantt strip (Tier B) (2026-08-26)

The calendar-true Gantt of the pushed-back design (ledger v2.2366, markers
v2.2371) — the roadmap Timeline's deliberate opposite (that one stays
sequence-axis and untouched).

- **Kernel `src/lib/checklistGanttRows.ts`** (+5 tests):
  `checklistGanttAxis` (week before earliest start → 30-day runway or past
  the latest due +14d; month labels; Saturday-anchored weekend bands; YMD
  string math) and `checklistGanttRows` (dated one-offs only; done rows
  kept 14 days; effective-due-ascending, done last; trail/tick/badge from
  `summarizeDuePushes`).
- **`ChecklistManageTimeline`** (new component) behind a **📅 Timeline**
  pill on Manage (count = dated open one-offs): solid start→due bars
  (green when done), hollow amber tick at the original promise, hatched
  slip trail, "→ pushed ×N · +Md" badge, red today line, weekend shading,
  horizontal scroll with a fixed name column; rows open the edit modal;
  empty state invites setting a Due by.
- Help guide *schedule a task or make it repeat* → "See dated tasks on a
  calendar" section.
