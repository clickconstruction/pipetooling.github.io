# v2.2507 — Fitting derivation: tees/wyes/elbows from run geometry (2026-08-30)

Owner ask: capture the fittings joining the traced systems. Insight: they're free —
derivable from the polylines already traced, no vision pass.

- **Kernel** (`src/lib/takeoffPlacement.ts`, +4 tests → 19): `deriveFittings` — interior
  vertices turning ≈90°/≈45° = **ell90/ell45**; a run endpoint on another same-system
  run's body at ≈90°/≈45° = **tee/wye**; axial end-to-end joins = couplings (uncounted);
  outside ±20° of 90/45 = **odd-turn/odd-branch**, flagged with the angle, never binned.
  Snap radius 2 ft via the page's doorway-calibrated scale; unscaled pages skipped and
  reported. Branch beats turn in co-located dedupe (the tee IS the joint).
  `fittingSummary` (per system+kind rollup) and `materializeFittings` (fittings become
  visible counters — "CW · Tee" — with markers at the joints, reviewable in the app).
- **Assembler** prints the fitting rollup + every odd-angle flag on each run;
  `"materializeFittings": true` bakes them in.
- **PLACEMENT.md** fittings chapter: vertex-at-the-true-corner and end-branches-on-the-
  main trace consequences; sizes/reducers deferred to the keyed-note hexes.
- **Live on the M6 takeoff**: 20 fittings — CW 6×90ell + 1×45ell + 2 tee; HW 3×90ell;
  Gas 1×90ell; SAN 1×90ell + 4 tee + 2 odd flags at the DF-4 spur (real shallow-angle
  branch, now visible for the reviewer). Re-imported: 47 counters (27 fixtures + 20
  fittings) + 14 runs, review lane still `ready`.
