# v2.2531 — 🤖 tab pending count becomes the red inbox badge (2026-08-31)

Owner request, same day as the v2.2527 Robots group-tab merge: the tab showed
`🤖 · 7` inline while Unsent/Working shows its inbox count as a red pill —
both are "needs you" counts, so they now share the visual language.

- `src/pages/Bids.tsx` top tab strip only: the Robots group button is wrapped
  in the same relative-`span` + absolute red pill markup as the Unsent/Working
  tab (`data-tabkey` moves to the wrapper span, matching that precedent). The
  pill shows `auditGate.pending`, renders only when > 0, caps at `9+`, and the
  button carries an `aria-label` naming the pending count. Tab label is the
  bare 🤖 again; landing/lens behavior untouched.
- Coordinated via the sessions ledger with `claude/reverent-babbage-8894d7`
  (owner of the v2.2527 tab-strip work; v2.2530 outstanding at edit time).
