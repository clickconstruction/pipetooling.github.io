# v2.2542 — Robot request queue: yellow asks, green jumps the line (2026-08-31)

Owner rethink of v2.2530's yellow-click prompt (it only worked for someone with
twin-mcp context): the prompt leaves the estimator-facing board entirely.

- **Icon states**: yellow click now REQUESTS a robot bid (stamps
  `bids.robot_requested_at/_by`, migration `20260831230000_bids_robot_request`) —
  icon turns green with a success toast; green click withdraws. Grey and colorful
  behavior unchanged. Optimistic patch + rollback on write error.
- **Queue lens** (`BidsRobotQueueTab`, dev-only third lens in the 🤖 group,
  `robot-queue` tab key with a non-dev URL redirect guard): requested bids first
  (oldest ask on top, requester name + age), then ready bids by due date. The
  **Copy robot prompt** button moved here. Kernel `buildRobotQueue`
  (`src/lib/bids/robotQueue.ts`, 4 tests): unsent + undecided + robot-ready + no
  twin + not stale (due date ≥ today−30d) — deliberately the same shape as
  twin-mcp `get_shadow_queue` (v2.2539) so a follow-up can feed requested-first
  priority straight into shadow dispatch.
- `database.ts` hand-updated; regenerate after push. Help guide robot-icon
  section rewritten.
