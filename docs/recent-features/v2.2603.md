# v2.2603 — Crew Day email stream (2026-09-01)

Part 3 of the superintendent visibility stack: the Crew Day section (v2.2602) becomes Report Subscriptions stream `crew_day` — an end-of-day "what everyone did today" email.

- **Migration** `20260901220804_crew_day_email_stream.sql`: `crew_day_email_requests` + `get_crew_day_payload_for_user(p_user_id, p_day)` (service-role; recipient-scoped — superintendents get their `project_superintendents` assignments) + pg_cron `crew-day-email-dispatch` (:04 lane co-ride) + `crew_day` branches in `get_my_email_schedule` / `get_global_email_schedule` (bodies verbatim from 20260901120000). See `docs/migrations/20260901220804_crew_day_email_stream.md`.
- **Edge function** `crew-day-email-dispatch` (money-waiting skeleton): preview / test_send / send_now / cron drain; per-recipient payload rebuilt at send time for the send's Chicago day; groups **by job** (site-diary read) with people rows, report excerpts, % movement, and the section's flags as red/amber lines. Quiet days still send. Renderer in `render.ts`.
- **Client**: `src/lib/crewDayEmailClient.ts` (protocol clone of `moneyWaitingEmailClient.ts`) + `CrewDayEmailModal` (verbatim sibling of `MoneyWaitingShareModal`: recipient picker of eligible users, Send now / Schedule… with date + Central time + Repeat weekly, Preview / Email me a test, pending-sends list with Cancel), opened from an ✉ button on the Crew Day card. Superintendents can schedule for themselves; the server enforces sender/recipient eligibility either way.
- Settings → My email schedule: `crew_day` tone + "Crew Day" label in the grid (`emailScheduleWeek.ts`, `SettingsMyEmailScheduleSection.tsx`).
- Help guide: `src/content/help/get-a-daily-crew-email.md`.
- Deploy note: `supabase functions deploy crew-day-email-dispatch --no-verify-jwt` after `db push`.
