# v2.3065 — Sheet → job maps use the link (2026-09-07)

Conversion train PR 3 — section D of [`to-dos/sub-sheet-job-link-followups.md`](../../to-dos/sub-sheet-job-link-followups.md), all but People → Review (its own PR).

- `LaborJob` carries `job_ledger_id`; [`useSubLaborLedger`](../../src/hooks/useSubLaborLedger.ts) selects it and resolves job names with `jobs_ledger … .in('id', links)` — the `get_jobs_ledger_by_hcp_numbers` RPC call and the number-keyed `laborJobNamesByHcp` map are gone; `laborJobNamesByJobId` (`buildLaborJobNamesById` in [`subLaborLedgerNames.ts`](../../src/lib/subLaborLedgerNames.ts)) replaces it through `Jobs.tsx`, `JobsSubLaborTab` (name cell, search, `spineFor`) and `subLaborJobMatchesSearch`.
- New `sheetJob(sheet, jobsById, jobsByNumber)` in [`sheetsNeedingWorkOrder.ts`](../../src/lib/subWorkOrders/sheetsNeedingWorkOrder.ts): the link first, the number only for a sheet with no link. Used by `sheetsNeedingWorkOrder` and [`buildWorkOrderBoard`](../../src/lib/subWorkOrders/workOrderBoardRows.ts); `JobsSubsWorkView` selects `job_ledger_id`. Tests: linked-by-id sheet with a stale number lands on the linked job; link-less sheet still resolves by number.
- [`JobsSubLaborFormModal`](../../src/components/jobs/JobsSubLaborFormModal.tsx): the assembler's job and the edit-mode pick seed read the sheet's link first.
- [`subLaborUnlinked`](../../src/lib/jobs/subLaborUnlinked.ts) (Quickfill → Jobs Cleanup): a sheet with a link is never "unlinked"; the number rule stays only for link-less sheets. [`JobsCrewPnlTab`](../../src/components/jobs/JobsCrewPnlTab.tsx): link first.

Client-only. No migration.
