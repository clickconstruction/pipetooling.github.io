# v2.2506 — M6: the pipes — legend extraction, substrate v0.5, first traced takeoff (2026-08-30)

The line leg ran end to end the same day its tooling (v2.2505) landed.

- **Legend + keyed-note extraction (P001/P201)**: the PLUMBING LEGEND (CW/HW/HWR/W/V/
  RWL/OD/SW/O2/SC line styles + valve/cleanout symbols), the PLUMBING MATERIALS SCHEDULE
  (system → material), and the load-bearing decode: **the hexagon tags are KEYED NOTES on
  the sheet itself** (G1 = 2" gas to roof; P26 = 2" vent up; W1/W2/W4/W23/W32 = sized
  CW/HW drops) — not fixtures, not a P001 table. The P201 gas sizing note's printed
  140'-0" total recorded as a cross-check figure.
- **Substrate v0.5** attached to b403 — by the twin's own session through the write fence
  (line_legend, materials_schedule, keyed_notes, gas_sizing, doorway scale_calibration;
  supersedes v0.4).
- **Mission M6 written** (trace the runs: sanitary from P200, water+gas from P201, vent
  riser-reconciled; doorway calibration mandatory per page; whole-run polylines; RFI at
  ambiguous continuations) — briefs bundle M1–M6, twin-mcp redeployed.
- **First M6 run (results row appended)**: 14 runs · SAN 175.6 ft / CW 166.7 / HW 44.6 /
  Gas 17.5 · P201 5.22 px/ft + P200 5.20 px/ft (doorways, 0.4% agreement) · gas printed
  total honestly reconciled (offsite + riser vs on-plan) · the connectivity check caught
  3 missed spurs live (corridor S-1, reception WB-1, treatment row) which were fixed
  pre-import · 3 cross-page FD-2 flags explained (waste lives on P200) · 2 new RFIs
  (HWR extent; P200's own P1/P2 key table) · re-import replaced, counters intact,
  review lane still `ready`.
- Standing gaps: cloud human reference for the eval diff (M4's gap, applies to M6);
  connectivity v2 should map counter→system+sheet to stop flagging legitimate
  cross-page drains.
