# v2.3381 — Accounts Receivable refresh, PR 3: the matches as a list (2026-09-13)

Client only; no migration, no edge function. Third of the train in [`to-dos/accounts-receivable-refresh/`](../../to-dos/accounts-receivable-refresh/README.md).

## What shipped

- **[`ArPayerMatches`](../../src/components/jobs/ar/ArPayerMatches.tsx)** — the matched payer's open bills as rows (amount · `bankPaymentTargetPrimaryLabel` · `bankPaymentTargetDetailLead` · a Stripe tag when `stripeHosted`), the deposit-amount match tinted green and first with *✓ matches this deposit*; the one-check-several-bills combo as a dashed row (*2 bills = $4,091.50 · fills 2 allocation lines*); amount-only matches from other customers under *Also matches the deposit amount* (or *Matches deposit amount* when no payer was read). Lifted out of the first allocation line, where the same data was a row of chips gated on that line being untouched. Every row keeps its *Apply allocation: …* / *Fill N allocations: …* name, so the three render suites pass unchanged.
- **Picking**: `pickTargetIntoLines` in the modal fills the first empty billed line, or appends a line when every line is taken — the same amount math as the row's own picker (`allocationAmountStrForTargetChange`). Bills already on a line are filtered out of the list; the combo row shows only while the pane has its one untouched line.
- The picked-target detail and the *This payment may already be recorded* guard stay under the row they describe.
