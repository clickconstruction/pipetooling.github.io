# v2.1978 — Partner Full ledger shows charges at their date (2026-08-21)

Owner request completing v2.1967 (charges-at-date on the office ledger): the partner's own Full ledger was statement-rows only, so Bryan read $967.60 while the office read −$1,008.13. Now both sides show the same journal.

**DB** (migration `20260821200000_partner_ledger_offsets.sql`): `partner_ledger_payload` gains a top-level `offsets` array (the partner's `person_offsets` — all their own money per §4b) and `person_offset_id` on each deduction object; body otherwise verbatim from 20260821180000. Both resolvers inherit.

**Client**: [`partnerWeeks.ts`](../../src/lib/partnerLedger/partnerWeeks.ts) — new `parsePartnerLedgerOffsets`; `partnerStubsToJournal(stubs, offsets)` now applies the office tab's exact charges-at-date rules (charge-type offsets book at `occurred_date`, mirroring statement deductions excluded, positive-type/manual deductions unchanged; 3 new tests — 14 in file). [`DashboardPartnerLedgerSection`](../../src/components/dashboard/DashboardPartnerLedgerSection.tsx) passes the parsed offsets into the Full ledger and updates the footer. Fail-soft: an old payload has no `offsets` → parses to `[]` → prior behavior. Help guide updated.
