# v2.3161 — Needs You job-account gap cards + Job Accounts filters (2026-09-08)

Second half of the job-account follow-up (first half: v2.3160, job window). Owner asked for a Dashboard todo to "review job accounts"; per the Needs You rule (v2.2339 → v2.2493: every item gated on a concrete condition, ranked worst-first) this ships as two self-clearing hygiene items instead of a standing reminder.

## RPC (migration `20260908120000` — fragment in docs/migrations/)

- `count_job_account_flag_gaps()` — see the migration fragment. Gate inside; office roles only.

## Needs You — `dashboardNeedsYou` kernel + `DashboardPinnedQuickRow`

- New hook `useJobAccountFlagGapsNudge(enabled)` (`(supabase as any).rpc` precedent; null while loading / disabled / both-zero; refetches on window focus).
- New inputs `jobAccountGapsEnabled?` / `jobAccountGaps?` (optional — Quickfill's twin passes nothing).
- Items `job-account-unflagged` ("N jobs with supply house job accounts have unflagged invoices" — $ of unpaid unflagged invoices on shared jobs) and `job-account-no-packet` ("N invoices are flagged on job accounts with no packet on record"). Both `gray`, tier 60 (hygiene, beside d22-uncoded); figure = count; action "Review them".
- Dispatch: `/materials?tab=job-accounts&filter=needs_flag` / `&filter=no_packet`. Enabled for the office set (`!hideBanners && authUserId && officeEligible`).

## Materials → Job Accounts — `MaterialsJobAccountsTab` + `jobAccountsFlow` kernel

- Kernel: optional 7th arg `sharedJobIds` → `row.hasJobAccountShare`; view totals `needsFlagJobs` (share on file AND unpaid unflagged dollars) / `noPacketJobs` (no share AND flagged dollars).
- Tab loads `supply_house_job_accounts.job_id` (all rows, `.catch → []` so a read failure never blanks the tab) and passes the set.
- New filter chips "Packet on file, unflagged" / "Flagged, no packet" — shown when their count is nonzero *or* the filter is active (so a deep link to an empty queue still shows the chip + "No jobs match this filter"). `?filter=` deep link read on mount and on param change.
- Expanded rows: teal "Job account packet on file" chip in the customer bar; one-line hint in the footer for each gap state (teal for packet-on-file/unflagged, amber for flagged/no-packet).

## Tests / docs

- Needs You kernel: both items, gating, singular forms. Job Accounts kernel: share marking + both gap counts + default-arg behavior. Full suite green.
- Help: see-which-paid-jobs-still-owe-supply-houses (filters + chip) and mark-an-invoice-on-a-job-account (Dashboard cards) updated.

## Deferred

- `supply_house_job_accounts.supply_house_id` (share rows carry only a contact label) — would make "packet on file" house-specific rather than job-level. Contacts already carry `supply_house_id`, so it's a nullable column + two insert sites (share modal, `send-supply-house-job-account` edge fn) when wanted.
