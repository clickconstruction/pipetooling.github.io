# v2.2709 — Signed agreement view: the green chip opens the record, with Share (2026-09-03)

Owner finding: clicking {{chip:green|✍ Signed · estimate #84}} on Kimberly Coe's job opened the Contract *send form* — wrong for a job that is done, and with no way to see her signature. Mockup artifact d69c8093 (PR A of two).

- **`JobSignedAgreementModal`** ([`components/jobs/JobSignedAgreementModal.tsx`](../../src/components/jobs/JobSignedAgreementModal.tsx)): one shell for every signature source. Green banner (signed/accepted by · when · how · sent-to/opened or *via Quote #84*), a **Share ▾** menu (Copy link → the customer's durable page [contracts], Text link, Download PDF [stored], Print / save as PDF, Open uploaded copy [paper], Open estimate/proposal #N [estimate-sourced]; *Email a copy…* is PR B), **Open job**, and the deliberate **Start a new agreement…** door to the send form. Below: the document — the contract as signed (`JobContractRecordBody`) or the estimate's acceptance record (`CustomerAcceptanceRecordBody`) — then the audit facts (how, consent, IP + map, device, document/rev).
- **Bodies split from their modals** so both hosts share them: `CustomerAcceptanceRecordBody` (new; `CustomerAcceptanceRecordModal` on Estimates now mounts it — same look) and `JobContractRecordBody` + `useJobContractRecordUrls` + `buildJobContractRecordHtml` exported from `JobContractRecordModal.tsx`.
- **Coverage carries `estimateId`** (`SignedEstimateLike.id`; the three estimates selects include `id`), so an estimate / bid-room signature can open its acceptance record.
- **Routing**: the Pipeline chip and ✍ quick action, `JobContractStrip` (Job window · Bill Customer · View bill — chip and *View record* for every signed source), the Contract modal's history rows, and Documents → Jobs rows all open the signed view when a signature exists; unsigned rows keep the send form / HTML preview.
- Help guide gains "View the signed record".
