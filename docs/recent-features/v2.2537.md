# v2.2537 — Avg / week stat on the Estimating Health pulse (2026-08-31)

New StatCard in `BidBoardEstimatingPulseSection` between Sent and Last 4 weeks:
`stats.sentDollars / windowWeeks` (the full 26-week window — zero-send weeks count,
so it reads as the honest run rate, not an active-week average). Sub-line names the
window. Help guide `read-estimating-health.md` stat-card list updated (five → six).
