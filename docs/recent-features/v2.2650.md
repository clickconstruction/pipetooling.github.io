# v2.2650 — Waiting For becomes a collapsed My Inbox footer strip (2026-09-02)

Owner request (mockups approved on the design canvas — "Option C, quiet footer
strip"): the Dashboard's "⏳ Waiting For" roadmap section should read as part
of My Inbox and start collapsed, instead of floating below the card.

- `ChecklistComingUpSection.tsx` split into `useComingUpWaitingGroups(authUserId)`
  (the fetch — unchanged queries/grouping) and `ComingUpWaitingGroupList`
  (dashed group cards + show-more, new optional `groupBackground`);
  `ChecklistComingUpSection` recomposes both so Checklist → Today renders
  exactly as before.
- `DashboardMyInboxCard.tsx`: the free-floating section render is gone. The
  card gains a footer strip (`--bg-subtle`, dashed `--border`, radius 8) as its
  last child — collapsed by default (plain state, like Recently Completed's
  corner link): ⏳ + **Waiting For** + muted count pill + one-line peek at the
  first blocker with ellipsis; tapping unfolds the header ("yours, once the
  step ahead clears") plus the full group list (`groupBackground:
  var(--surface)` so the dashed cards sit on the subtle strip). The card also
  shows when Waiting For is the only content (`showMyInboxCard` gains
  `waitingGroups.length > 0`).
- New kernel `src/lib/dashboardWaitingForStrip.ts`
  (`buildWaitingForStripSummary`: total task count + `after "<44-char clipped
  blocker>" — <names | not staffed yet>`; 6 unit tests).
- Help: `dashboard-my-inbox.md` gains the Waiting For bullet;
  `check-off-my-daily-tasks.md` §Waiting For notes the Dashboard placement.
