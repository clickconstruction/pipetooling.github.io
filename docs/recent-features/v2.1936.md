# v2.1936 — Checklist Review centered on wide screens (2026-08-20)

Cosmetic layout fix requested from a wide-monitor screenshot: the Review
tab's full-bleed cards read poorly past ~1400px.

- `ChecklistOutstandingTab`'s root div (in `src/pages/Checklist.tsx`) now
  carries `maxWidth: 1100, margin: '0 auto'`, so the summary tiles, GOALS
  cards, fold sections, and Outstanding-by-person list render as a centered
  column on very wide screens. No behavior change; other Checklist tabs
  untouched.
