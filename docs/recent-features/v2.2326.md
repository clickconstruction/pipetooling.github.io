# v2.2326 — Quickfill "Missing bill dates" station (2026-08-26)

Owner ask: put the missing-bill-date work in front of the assistants, scoped
to the No Count Date. Mockup: https://claude.ai/code/artifact/a1729dbd-db45-44e7-b6e7-7e82d9c6509c

- **Migration `20260826050000`** (details in `docs/migrations/`): new RPC
  `get_undated_bill_worklist()` — undated billed/paid bills still relevant
  past `pay_speed_no_count_date_v1` (a payment on/after the floor, or
  paymentless and created on/after it), with per-bill clue data (HCP,
  payment dates, job identity). Gate: dev / assistant-like / master.
- **New section** `undated-bills` ("Missing bill dates"), slotted after
  Complete, no Total Bill via `SECTION_INSERT_AFTER`; dev / master /
  assistant-like eligibility; standard mark chrome; count in the metrics
  context. Body `QuickfillUndatedBillsSection`: scope line naming the No
  Count Date, search (customer / job / address / HCP), rows with clue chips
  (`paid 08/24 + 1 more` / `billed, unpaid · created 08/12`), inline
  MM/DD/YY save (row clears, toast), `Open job ›` →
  `openEditJob(..., { initialTab: 'bill', onSaved: reload })`.
- **Shared editor extracted**: `InlineBillDateEditor` + `AddBillDateButton`
  (`src/components/jobs/InlineBillDateEditor.tsx`) now serve both this
  station and the Data health drill-down (which was refactored onto them —
  the editor owns its draft state). One behavior addition on both surfaces:
  **future dates are refused** (amber border + hover explanation, Save
  disabled) — a bill date after today is a typo.
- Kernel `src/lib/quickfillUndatedBills.ts` (parse / clue / search) + tests;
  render suite for the section (save payload, scope line, fail-soft
  pre-migration message); help guide `quickfill.md` gains the station.
