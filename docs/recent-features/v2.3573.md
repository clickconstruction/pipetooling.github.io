# v2.3573 — Price requests, asked to priced — PR 4: Price with robot on the panel, and the Needs You card (2026-09-17)

PR 4, the last of `to-dos/price-requests-loop/README.md`'s train (PRs 1–3: v2.3495, v2.3526, v2.3572); drawn in `before-after-pr4.html`. Client only. What the to-do still holds is the owner's: the usual-houses rule and two wording questions.

## The header button

[`BidPriceRequestsTable.tsx`](../../src/components/bids/BidPriceRequestsTable.tsx): once the summary counts a quote in, the panel header carries **Price with robot · N quotes in** (the table's blue button, a small robot glyph) and the desk link shortens to *Open the desk ↗*. The button is a link to the bid's Pricing tab with `robot=price`; [`BidsPricingTab`](../../src/components/bids/BidsPricingTab.tsx) reads that flag once the bid is in hand, opens the same `PriceWithRobotModal` its own button opens (when the viewer may package and send pricing), and drops the flag so a reload does not reopen it. One door, one modal — the modal's context (count rows, the active request, the compare) stays on Pricing where it lives, and the table is not made to carry it.

## The Needs You card

[`usePriceRequestsLateNudge`](../../src/hooks/usePriceRequestsLateNudge.ts): `bid_rfqs` with `needed_by` before today, no `quote_url`, status not closed / draft / quoted, on a bid with no outcome yet (unsent or pending); a quote plugged in on Pricing against the request counts as in even with no link on the row. The card ([`dashboardNeedsYou.ts`](../../src/lib/dashboardNeedsYou.ts) `price-requests-late`, amber, the revenue-chasing tier beside the robot's *ready* card, the same audience — office roles and estimators): *National Wholesale is 7 days past the date you asked for on BP398 ZZ Test, with nothing in*, or *3 price requests are past their date with nothing in — longest …*; **Open Price requests** lands on that bid's Pricing tab, where the desk lists every request with Nudge and the Edit-bid table has the paste box.

## Coverage

Three card tests; the table's status smoke gains the header button (href carries `robot=price`, the desk link shortens). Live on the dev server: the Dashboard shows the card for BP398's Click Plumbing Supply request (late since Sep 9); `/bids?tab=pricing&bidId=…&robot=price` opens the robot sheet on Pricing and the flag drops from the address.
