# v2.2632 — RFQ Phase 3: price memory pays off (2026-09-01)

Phase 3 of `docs/SUPPLY_HOUSE_RFQ_PLAN.md` — the last planned phase
(lane B and the other Deferred items stay deferred).

- **Prepare-screen recency hint** (`PrepareFixtureCopyModal`): picking a
  house in the quote-link strip fetches its `supply_house_fixture_prices`
  and renders coverage over the CURRENT selection — "Moore Supply has
  last-quoted prices for 2 of these 83 items · newest today" (or "hasn't
  quoted any of these items before"). Keys match on the generated
  `fixture_key` normalization (lower/trim).
- **Compare expiry-vs-needed-by warning** (`QuoteCompareModal`): the
  bid's nearest `bid_rfqs.needed_by` loads with the quotes; any house
  whose LATEST quote's `valid_until` lands before it gets an amber
  header strip — "Moore Supply's quote expires before the needed-by
  (2026-09-10) — worth re-asking before ordering."
- The plan's third Phase 3 item — the "Last quoted" compare column —
  shipped early inside v2.2630's compare.
- Help: quote-link guide notes the recency hint; the pricing-package
  guide's compare section notes the expiry warning.
- Live-tested on BP339 (hint: 2 of 83 · newest today) and B429
  (warning appeared with valid-until backdated below needed-by,
  restored after).
