# v2.2832 — Job Summary: leakage flags (2026-09-05)

PR 10 of the Job Summary analysis plan (artifact 9c02d29a), stacked on v2.2831 (Rework).

## Client only — no migration

- [`jobsLedgerEmbedSelects.ts`](../../src/lib/jobsLedgerEmbedSelects.ts): the invoices embed now carries `agreed_write_down_at` and `agreed_write_down_previous_amount`.
- [`jobSummaryLedgerView.ts`](../../src/lib/jobs/jobSummaryLedgerView.ts): rows gain `writeDownUsd` (Σ previous amount − amount over written-down invoices) and `inCollections` (`collections_at` set and not paid) with flags `write-down` / `collections`; totals gain `writeDownUsd` / `writeDownJobs` / `collectionsJobs` / `collectionsUsd`. One test.
- Toolbar: two chips in the hygiene row — "✂ $X written down on N jobs" (money roles) and "⚑ N in collections · $Y" (red). Table: chips beside the job name.
- **Ahead correction** (found by the data check for Bid vs actual): no job on the ledger carries a `bid_id`, and no estimate links a bid to a job, so the hand-off signal the office actually uses is the bid's outcome (`won` → `started_or_complete`, 33 vs 11 today). The tile and list now read "Won, not marked started" and the help guide says so.

## Plan bookkeeping

- **Travel on Days** — deferred: `job_travel_times` holds 113 pairs across 55 jobs (on-demand from the schedule), and the day ledger has no per-session start times, so a per-day windshield figure can't be honest yet.
- **Bid vs actual** — blocked on the same finding: jobs don't link to bids. Cost estimates exist for 259 bids (6,805 labor rows), so the view is cheap once a "job from bid" flow writes `jobs_ledger.bid_id`.
