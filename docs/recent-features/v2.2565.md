# v2.2565 — Money waiting weekly email (2026-09-01)

Owner-approved mockup (https://claude.ai/code/artifact/16d8e5ad-8711-41a9-8d07-11774666a4da),
built on the payment-forecast email rails (v2.2225 skeleton).

- **Migration `20260901120000`** (details in `docs/migrations/`): requests
  table + pg_cron + `get_money_waiting_email_payload()` (forecast rows +
  `job_address`, v10-rules pay-speed mirror) + `money_waiting` branches in
  both schedule RPCs.
- **Edge function `money-waiting-email-dispatch`** (section + TOC in
  `docs/EDGE_FUNCTIONS.md`): preview/test_send/send_now + cron drain;
  renderer mirrors the list — legend, per-customer bar + "Nd waiting /
  usually ~Nd", every bill as a row (tone dot, job · FULL address with city,
  dollars, wait), "+ N more jobs" fold at 8, on-pace footer, all-on-pace
  one-liner. Grouping via `_shared/moneyWaitingCore.ts`, a Deno port of
  `src/lib/jobs/moneyWaiting.ts` (source of truth; keep in sync).
- **Client**: `moneyWaitingEmailClient.ts` + `MoneyWaitingShareModal`
  (clones of the forecast pair), "✉ Email this weekly…" in the Money waiting
  header (`canEmailMoneyWaiting`: dev/master/assistant-like), `money_waiting`
  stream registered in `emailScheduleWeek` + schedule section tones; help
  guide section added to *see when a customer will pay*.
- Deploy after merge: `supabase db push` + `supabase functions deploy
  money-waiting-email-dispatch --no-verify-jwt`.
