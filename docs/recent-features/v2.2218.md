# v2.2218 — "Who sends the bids": dev-only share-of-sent view (2026-08-23)

Owner: show what percentage of bids — by count and by $ — are sent weekly and monthly, by
whom, for the last six months, in the dev-only Health block. Mockup (approved shape):
claude.ai/code/artifact/bdda4014-20fd-4b36-89c0-b2689aac1f43

- Kernel [`sentShareByPerson.ts`](../../src/lib/bids/sentShareByPerson.ts) (+3 tests):
  windows sent bids (`bid_date_sent`) to six calendar months; tallies per person (bid's
  estimator; none → Unassigned) per calendar month and per company Sunday week (W-labels from
  the week's Thursday ISO number — the Pulse's convention); ranks people by window $, folds
  past five into **Other**; every row carries count/$ totals and per-segment shares; empty
  months/weeks stay as rows.
- Section [`BidBoardSentShareDevSection.tsx`](../../src/components/bids/BidBoardSentShareDevSection.tsx),
  rendered under the estimator labor table (dev only), fed the board's filtered bids so the
  service-type tabs scope it: 100%-stacked rows, **Monthly/Weekly** and **By $/By count**
  pills (sticky, `bid_board_sent_share_view_v1`), legend with window share · $ · count per
  person, in-bar % labels when ≥12% wide, per-segment tooltips, dashed ghost rows for quiet
  periods.
- **Palette:** slots 1–5 of the dataviz reference palette (dark-stepped set), validated with
  the skill's checker on BOTH surfaces (adjacent-pair CVD ΔE ≥ 8.4, normal ΔE ≥ 19.3; the one
  sub-3:1 relief case is covered by the legend + in-bar labels); person→color fixed by
  window-$ order across every toggle; Other/Unassigned wear a custom gray.
- Verified live on Bryan-era data: Aug = Wendi 96.8% of $18.1M across 12 bids; weekly view
  26 rows. Client-only — no migration. Stacked on v2.2215 (#1918).
