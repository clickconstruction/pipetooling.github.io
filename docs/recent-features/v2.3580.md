# v2.3580 — Apple Pay as a pay source, and the part memo: one send on several weeks says so on every row (2026-09-17)

The owner, 2026-09-17, after paying Tristen $20 and then $1,067.23 by Apple Pay from Mercury checking 8103 against two open weeks ($620.06 for 07-19, $467.17 for 09-06 — the two sends settle him to the cent): *"How should I record in the notes that both of those payments were a part of '1,067.23'? I want a robot to always do this, if they're adding a transaction across multiple."* Built, then recorded as the test.

## The convention

When one send lands on more than one report row, every row's memo ends with its part and the whole:

```
Apple Pay "Tristen" · 1 of 2 from $1,067.23
Apple Pay "Tristen" · 2 of 2 from $1,067.23
```

A leftover that goes to an advance offset says `· $323.81 of $1,500.00 ahead`. A single-report send wears no suffix. The memo is for eyes; a robot groups rows by `source_kind` / `source_id` and never parses it.

## Migration [`20260917210000_pay_sources_parts.sql`](../migrations/20260917210000_pay_sources_parts.md)

`apple_pay` as a kind (source = the Mercury card row, like Cash App's; allowed without an id until the row posts); `pay_send_part_memo` / `pay_send_strip_part` / `pay_send_total` / `pay_send_stamp_parts`; `record_pay_send` stamps the rows it writes and words the offset; `link_pay_send` restamps a source's rows after each link, so the backfill's five `1200 sent` rows read `1 of 5 … 5 of 5`.

## Client

- [`paySources.ts`](../../src/lib/people/paySources.ts): the fifth kind, `paySendPartMemo()` byte-equal to SQL, `parsePaySendPart()`, `stripPaySendPart()`.
- The Payments view chip reads *Apple Pay* from the memo ([`payRunPayments.ts`](../../src/lib/people/payRunPayments.ts)); the guide *see where someone stands on pay* names the chip and the part memo.
- `npm run pay:record -- --person <name> [--kind … --amount … --date … --source … --note … --apply]` ([`scripts/pay-record.ts`](../../scripts/pay-record.ts)): one send through `record_pay_send` from a terminal — dry run first, position printed after — or just where someone stands. The sign-in moved to `scripts/lib/paySession.ts`, shared with `pay:backfill`.

## The test recording

After the push: `pay:record` for Tristen, $20 then $1,067.23, kind `apple_pay`, sources the two Apple Wallet card rows on checking 8103, note "Tristen". Expected: the $20 on 07-19; the $1,067.23 as $600.06 finishing 07-19 (`1 of 2`) and $467.17 on 09-06 (`2 of 2`); open total $0.00. The outcome is in the PR.

## Not in this PR

Apple Wallet card rows carry no recipient, so the backfill matcher cannot resolve their person; an Apple Pay send is recorded by hand with `pay:record` (or the modal once PR 3b moves it onto the function). Tally: mark the two card rows payroll, or add one rule if Apple Cash will only ever carry pay.
