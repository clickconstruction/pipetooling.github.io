# v2.2337 — The billed-after-paid guard (2026-08-26)

Owner call after the 2026-08-26 data repair (37 fabricated bill dates across
history — retro-created invoice records stamped with their creation day; the
old `GREATEST(0, …)` clamp turned each into a fake +0d sample).

- **Migration `20260826150000`** (details in `docs/migrations/`): three
  functions gain the guard —
  `get_billed_customer_pay_speeds` **v11** (samples require
  `paid_on >= bill date`, so medians/receipts never see the pair),
  `get_pay_speed_transactions` **v5** (those rows bucket as `unlinked` with
  `gapDays` NULL), `get_undated_bill_worklist` **v2** (billed-after-paid
  bills join the Quickfill worklist; payload gains `billedYmd`).
- **Client**: kernel `isBilledAfterPaid` + third `missingInfoLabel` case
  `billed after paid`; the drill-down offers the inline date editor on those
  rows (replacing the wrong date) and the hover explains why the recorded
  date can't be right. Quickfill clue chip names the contradiction:
  `billed 08/19 after paid 08/14` (earliest contradicted payment).
- Fail-soft both directions: pre-v5 payloads simply have no such rows; the
  new client renders old payloads unchanged.
