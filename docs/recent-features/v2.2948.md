# v2.2948 — History tab on the job window: the Projects day-grid for one job (2026-09-06)

Journey map **Tier 5 X13** (J31-adj3 keep — "Job History's day-grid is a per-job density view nothing else offers; rehome under Jobs"). Tier 5 train T5-05. **Will's call 2026-09-06: build — decision 12 stays parked for Projects (nothing demoted); unparked for the rehome only.** Client only — no migration, no edge function.

## What shipped

- **Kernel** [`src/lib/jobs/jobHistoryTab.ts`](../../src/lib/jobs/jobHistoryTab.ts) (+4 tests): `JOB_WINDOW_TABS` / `JOB_WINDOW_TAB_LABELS` (job · edit · bill · **history**), `jobWindowFormPaneHidden(tab)`, `singleJobHistoryRange(today)` (180 d), `jobHistoryChannelName(user, jobId)` (a job-window mount must not share Projects' realtime channel).
- **`ProjectsJobHistoryTab` single-job mode** (`jobId` prop): reads that one job whatever its status (Projects keeps the working set), 180-day default range that is **not** persisted (Projects' saved range/prefs stay theirs), project filter off and its checkbox + "N of M linked" line hidden, realtime filtered to that job. The Projects mount is byte-for-byte unchanged (`jobId` absent).
- **`JobWindowModal`**: a fourth tab **History**; the pane lazy-loads the tab (`React.lazy` + `Suspense`, so the window bundle does not grow and the Projects → context → window import ring stays one-directional) on first visit and stays mounted after, like the form pane; the Edit/Bill form pane hides on History via the kernel predicate. `JobWindowTab` now derives from the kernel (`JobDetailModalContext.initialTab` accepts `'history'`).
- Render test: History mounts the grid on first click and survives a tab switch. Guide `job-window-tabs` → "four tabs" + "The History tab".

## Verify

Dev: open any job → **History** → the day grid for that job (rows only for days with sessions), range chips work, Day modal opens on a day. A job with no project shows the same. Projects → Job History: unchanged, including its saved range. The Projects tab and an open job window both live-refresh on a clock_sessions change without clobbering each other.
