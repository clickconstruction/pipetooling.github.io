# v2.3236 — Owner memo: Post is safe to retry (2026-09-10)

Client only — no migration, no edge function. The first live Post on the Console's owner memo (v2.3232, question 836b6c22) inserted its four one-tap questions and then lost the dev session before retiring the original; the memo came back showing the same ask, and a second Post would have written the four again. The original was retired by hand with Dismiss that day.

## What changed

- [`legacyAskMemo.ts`](../../src/lib/bids/legacyAskMemo.ts) (+1 test): `splitMissionTag(sourceId)` is the `split from <id8>` stamp every posted split carries in its mission (it always did; now named); `alreadySplitRows(rows, sourceId)` picks the rows carrying it.
- [`TwinOwnerMemoCard.tsx`](../../src/components/bids/TwinOwnerMemoCard.tsx) (+1 render smoke): **Post** first reads `twin_questions` for rows stamped for this ask (`ilike` on the mission). If any exist it skips the insert and only retires the original, with the toast *Found N questions already posted from this ask — retired the original.* If the retirement itself fails after a successful insert, the error says *Press Post again — it only retires now.*

## Testing

`npm run typecheck`, `npm run lint`, `npm test`, `npm run build`. The retry path is covered by the render smoke (prior rows present → no insert, one retirement); nothing to walk live — the only pre-rule ask on record was retired on 2026-09-10.
