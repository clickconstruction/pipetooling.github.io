# v2.2544 — Shadows lens: the sealed-envelope story (2026-09-01)

Owner-approved mockup. New `Shadows` lens in the 🤖 group (all staff; Queue stays
dev-only), the deferred v2.2539 scoreboard surface wearing plain language:

- **Kernel** `src/lib/bids/shadowStory.ts` (6 tests): `shadowStorySteps` maps a
  run to the five-step journey (requested-by / picked-up → estimated blind →
  🔒 sealed → waiting on our bid → scored with delta chip, "close ✓" within
  ±8%); `shadowGateProgress` computes per-axis Gate-B pips (last 5 scored,
  consecutive-streak, gateMet at 5).
- **Component** `BidsRobotShadowsTab` — collapsible "How shadow bids work (30
  seconds)" explainer, one stepper card per run, reference bid number
  deep-links to the board. Reads `list_shadow_runs()`.
- **The seal is enforced at the API** (migration
  `20260901010000_shadow_runs_staff_rpc`): the v2.2539 blanket staff SELECT on
  `twin_shadow_runs` is dropped; staff read through the security-definer RPC,
  which nulls `locked_total`/`reference_value`/`delta_pct` until
  `status='scored'` — a sealed robot number visible pre-send could anchor the
  human estimate.
- Tab key `robot-shadows`; help guide 🤖 section updated.
