# v2.2204 — Pricing Workbench polish: gray tray, typed margin, self-sizing total, Apply on its own line (2026-08-23)

Owner iteration on the v2.2203 strip (from screenshots):

- **Price options sit in one quiet tray**: the scenario-card row (`data-tour="workbench-scenarios"`) gets `--bg-subtle` + border + radius + padding, so "all the pricing options" read as one group.
- **Margin is typeable**: a numeric input (1–95, Enter/blur solves) next to the slider replaces the static `42%` label; the slider still solves on release.
- **The target-total box sizes to its contents**: `width: max(len, 6)+1 ch` — "42,000" wide by default, growing as a bigger number is typed (wrapper no longer flex-grows).
- **Apply/Discard moved to their own second line** inside the sticky summary: appears the moment the solver previews anything (slider, typed margin, Solve, Price unpriced only) with "Previewing N prices — nothing saved yet.", disappears on Apply or Discard.
- No behavior changes to the solver itself. ("Price unpriced only" = run the solver at the current margin but only fill rows whose Sale price is still empty; every already-priced row is held exactly as-is — explained to the owner, tooltip already says it.)
