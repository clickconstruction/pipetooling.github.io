# v2.1921 — Section rules get light-mode contrast (2026-08-20)

Owner feedback: the v2.1892/v2.1900 section-separator rules used `var(--border)` (#e5e7eb), close to invisible on white. New token in [`index.css`](../../src/index.css): `--border-rule` — #b8bec8 in light (clearly visible on white), #374151 in dark (identical to today's `--border`, so dark mode doesn't change). Applied to the draft editor's `est-region-ruled` ([`EstimateDraftStepRail`](../../src/components/estimates/EstimateDraftStepRail.tsx) CSS), the three section rules in [`EstimateCustomerDocument`](../../src/components/estimates/EstimateCustomerDocument.tsx), and the pasteable CO doc's inline rules ([`estimateChangeOrder.ts`](../../src/lib/estimateChangeOrder.ts), #d1d5db → #9ca3af — email-client-safe literal). Verified live in light mode (computed #b8bec8 on the cost-section rule). Client-only — no migration.
