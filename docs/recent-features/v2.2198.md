# v2.2198 — Pricing header: split Share button (option A) (2026-08-23)

Owner: "move these three buttons into this share menu… tastefully." Options mockup claude.ai/code/artifact/df8daa33-ef86-43bb-9090-eacbcd4f8020; A (split button) chosen.

- New [`PricingShareMenu.tsx`](../../src/components/bids/PricingShareMenu.tsx) (+4 render tests): green **Share** (one click, same disabled gate + tooltip, still `requestWithStarCheck('share')`) with an attached **▾** opening a `role=menu` popover — *Print* ("the price you're viewing") · *Download CSV* (disabled with today's tooltip when pricing/counts/labor missing) · divider · *Print all prices — review* ("every price option in one document", the old orange Review). Esc / click-outside close; arrow keys move focus; picking closes then fires the existing handler. Without `canPackageAndSendBidPricing` the control renders as a single **Export ▾** over the same menu.
- `BidsPricingTab.tsx`: the four header buttons (Share / CSV / Print / Review) replaced by the one control; handlers unchanged.
- Phone fix found in live test: the right-anchored popover clipped off the screen's left edge when the button sat left of ~16rem — the menu flips to left-anchored there (`alignLeft`), with `maxWidth: calc(100vw - 1rem)`.
- Verified on the dev server: desktop (menu contents, Print fires via stubbed `window.open`, closes on pick), phone 414 (anchor flip, fits), tablet 820 + dark mode.
