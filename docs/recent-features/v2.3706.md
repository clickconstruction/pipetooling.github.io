# v2.3706 — Contract sweep: the signing ways as one row, and a ⋯ More in the footer (2026-09-22)

PR 2 of the last four pieces in [`to-dos/contract-sweep-refresh`](../../to-dos/contract-sweep-refresh/README.md) — the two cosmetic pieces the refresh (v2.3669) drew and left, redrawn in `final-pass.html`. Client only, no migration.

## The row of ways — [`contractSigningWays.ts`](../../src/lib/jobs/contractSigningWays.ts) · [`JobsContractSweepModal.tsx`](../../src/components/jobs/JobsContractSweepModal.tsx)

- *How this one gets signed* was three tall radio cards, each with its own explanation — a third of the pane. It is now **one row of three segments** (`role="radiogroup"`, each segment a label around a radio the eye never sees, so the group's semantics and the render test's `getByRole('radio')` are unchanged), with **one line under it** for the chosen way's detail. New kernel `signingWayDetailLine(plan, way)` → the detail, and a note naming the ways that are out and why (*Email the PDF to sign by hand and Email a signing link: needs a signer email — type one in To above*), so a dimmed segment never has to be hovered.
- A builder's row is one segment (*File Summit GC's subcontract*) with **Send ours anyway ▸** under it; opening it adds a second row of the three demoted ways under a small heading, as before.

## The footer

- **One primary.** `fastPath` is true when a row follows and this one is Ready (for Download to print: when its scope is more than one line); then the blue button is *& next* and the one-job send goes under ⋯ More as *Email the PDF — stay on this job*. Otherwise the one-job send **is** the primary — a row that is not Ready no longer shows a dimmed *& next* beside an enabled one-job button; it shows the one that works, and the sentence still says what is unusual.
- **⋯ More** at the left — [`ArHeaderMenu`](../../src/components/jobs/ar/ArHeaderMenu.tsx) gained `label` ("⋯ More"), `openUp` and `align="left"` for a footer — holds *Open the full editor* and the one-job send while the fast path is showing. *Preview PDF* stays in reach, and **Fix email on the job** stays out in the open on a row with no email: the mock-up's first pass had it in the menu, and the critique took it back out — a fix should not hide.

## Coverage

`contractSigningWays.test.ts` — the detail line and the out-note. The sweep's render tests: the detail line follows the pick; a not-Ready row has the one-job send as its only primary; a no-email row names the two ways that are out and offers *Download & next* with the one-job download under More; ⋯ More opens the full editor; a thin row becomes Ready and the fast path appears.

Left in the train: PR 3 (the amount follows the line items, on the sweep and in the full editor).
