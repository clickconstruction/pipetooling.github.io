# v2.2808 — Contract library rows: name | buttons, chips + version below (2026-09-04)

Owner: "I don't love the order of things and I feel like it pushes them around" (the Contract library → Documents list on desktop). Mockup artifact 9102bce7; owner picked **B** with one tweak — the version text may wrap under the button row.

- `ContractBookModal.tsx` desktop row header is now a two-column grid: the document name (col 1, `overflowWrap: anywhere`) and the action buttons (col 2, `justifySelf: end`, `flexWrap: nowrap`) on line one; a full-width second line (`gridColumn: 1 / -1`, flex-wrap) carries the packet/audience/tag/Link chips followed by the version text ("· Version date: … — set manually · sent to N people") as an inline muted span, so it flows under the buttons when the chips take the width.
- The version line is computed once per row (`versionText` / `versionBlock`) and rendered as its own block only on phones (the v2.1413 stacked layout, unchanged) and while editing.
- No kernel change, no migration.
