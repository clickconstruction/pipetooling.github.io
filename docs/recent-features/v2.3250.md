# v2.3250 — Pipeline activity box: the report pill to the right of + Add, same height (2026-09-10)

Client only. The second half of the v2.3197 work: PR #2906 shipped the report pill **left** of + Add; the owner's call the same afternoon was "to the right of + Add, same height", committed on the original branch but never merged (the branch carried a duplicate v2.3197 fragment, so a plain merge would have failed the changelog drift test). Cherry-picked onto today's main during the 2026-09-10 branch audit (artifact *Two Leftover Branches*: before/after screenshots from the dev server).

- [`JobsStagesActivityBox`](../../src/components/jobs/JobsStagesActivityBox.tsx): the two floating pills render in one flex row (`alignItems: stretch`, 6px gap) — **+ Add** first, then the report pill — so they share a height (22px) and a bottom edge. Before, the report pill was absolutely positioned left of + Add and 2px taller. Behavior unchanged: the pill opens New report on that job.
