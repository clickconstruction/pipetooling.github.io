# v2.3352 — Bid Costs: the rail says what it sums; search box fills its line (2026-09-11)

Client only. Owner: "By estimator · year — of what?" The tail was the window segment's label lower-cased, which read as a calendar year and never named the number.

- [`bidPursuit.ts`](../../src/lib/bids/bidPursuit.ts): `PURSUIT_WINDOW_WORDS` — `90d` → "last 90 days", `year` → "last 12 months", `all` → "all time" (tested).
- [`BidsBidCostsTab.tsx`](../../src/components/bids/BidsBidCostsTab.tsx): rail headings read "Spent bidding by estimator · last 12 months" and "Spent bidding by outcome · last 12 months" ("Hours bidding …" for the hours-only roles); the first tile's sub-line and Cost to win's Everyone row use the same phrase. The Search bids input gets `flex: 1 1 180px` so it takes the rest of the controls line.
