# v2.2092 — Why we lost clarity pass: the sibling treatment (2026-08-22)

Owner: "Why we lost / Waiting to hear look so similar but different." The
v2.2080 clarity grammar, applied to the Why we lost lens (mockup approved).
Chrome and language only — recording flow, suggestions, per-GC copies, and
bid-tab capture unchanged. All in `BidsWhyWeLostLens.tsx`.

- **Header summary line** beside the red pill: `of N lost · $X unexplained ·
  loss rate N%, excluding GC-lost N%` (the rollup's rates, surfaced).
- **Search input** (parity with Waiting to hear): bid #, project, GC/Builder,
  address, estimator — narrows rail + queue via a `searchedLensBids` memo
  between `lensBids` and recipient expansion; the headline/rollup stay
  whole-queue. Own empty state.
- **Explainer line** replaces the right-aligned "Keys 1–6 tap a reason ·
  Enter takes a suggestion · arrows move between bids" caption.
- **Rail** capped at `min(72vh, 42rem)` with its own scroll (page was ~3.5k px
  with 44 builders; ~1.45k after); done-state "clear" → **"all explained"**.
- **Panel count** "2 of 14 done" → red **"12 of 14 need a reason"** / green
  "all explained".
- **"Their lost bids"** label on the pill row; **"Why did we lose it?"** label
  + inline keyboard hint over the reason chips.
- **Card contrast**: `cardStyle` gets the v2.2083 surface + border-strong +
  shadow treatment (was bg-page on bg-page).
- Help guide `follow-up-with-builders.md`: search + header-summary sentences.
