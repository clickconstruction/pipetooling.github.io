# v2.3413 — Sold in sticks: the guide's screenshots (2026-09-14)

Docs only. The guide *price a takeoff in sticks* (v2.3409) gains six `{{gif:…}}` illustrations in `public/help/` (single-frame GIFs, lazy-loaded, outside the SW precache): the Part Types list and form, Add Part, the line chip, the Sheet view with the tile and the rail, Materials to order, the Part Prices window. Captured headlessly on the worktree dev server against the ZZ Test bid (BP398) with two throwaway "(demo)" part types and four parts, removed after the capture (prod left at its 24 lines, no demo rows). The estimator-facing walkthrough with the same screenshots was published as the artifact *Pricing a Takeoff in Sticks* (`cd12dd98`).
