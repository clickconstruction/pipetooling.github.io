# v2.2633 — Help: the supply house quote loop gets its own guide (2026-09-01)

Guide-only PR (user request after the RFQ system shipped v2.2629–v2.2632).

- New `src/content/help/get-supply-house-prices-on-a-bid.md`: the full
  loop in one searchable place — send the list (both copy lanes), plug
  the reply in (paste/link/phone), compare + pick, and the honesty rules
  (expiry graying, needed-by warning, common-lines totals, latest-wins).
- `send-a-bid-pricing-package.md`: the "Plug the vendor's reply back in"
  and "Compare quotes" sections collapse to a pointer section ("When the
  prices come back") — the packaging guide stops burying the quote
  workflow at its bottom.
- `send-a-supply-house-a-quote-link.md`: compare-view cross-reference
  retargeted to the new guide.
- Follow-up (same day): the guide gains a screen recording —
  `public/help/get-supply-house-prices-on-a-bid.gif` (paste → match →
  save → compare → pick), recorded against BP339 and cleaned up after.
- Second follow-up: recordings for the other two guides —
  `supply-house-list-prepare.gif` (scope → live preview → copy) in the
  pricing-package guide, and `send-a-supply-house-a-quote-link.gif` +
  `supply-house-quote-page.gif` (mint the link / the vendor's page) in
  the quote-link guide.
