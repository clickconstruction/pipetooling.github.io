# v2.2048 — Waiting to hear lens gains search (2026-08-21)

Owner request (it was the only Followup lens without one). [`BidsWaitingToHearLens`](../../src/components/bids/BidsWaitingToHearLens.tsx): new `chaseSearchQuery` filters the windowed bids (`searchedLensBids`) on project name, builder name, ledger label, address, estimator name, and `bidNumberMatchesQuery` — feeding the per-GC expansion, builder groups, and selection; typing resets the selected builder/bid. Deliberate scope: the rollup ("N sent bids to chase") stays computed from the un-searched window — search is navigation, the chip is status. Input sits in the header row between the window pills and the session counter (standard "Search bids (bid #, project name, or GC/Builder)…" placeholder per the search-standardization campaign); dedicated no-match empty state. Verified live: "Palmer" → one builder + BP337 opened; "BP342" matched via ledger label → Knight Contracting; nonsense query → "No bids match" with the headline chip unchanged. Client-only — no migration.
