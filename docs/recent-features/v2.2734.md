# v2.2734 — Timeline: working jobs set the floor (2026-09-03)

Owner's idea after reviewing the v2.2711 Timeline ("what if I inverted the colors… made projects the bottom, then built on top of that, then paid on top"): the stacked area's order flips from paid → billed → working (bottom to top) to **working → billed → paid**. Same counts, same data; only the stacking order and the legend order change.

Why it reads better: the bottom band sets the baseline the eye follows. Most single-visit service calls are already paid, so with paid at the bottom the jumpy part bounced every band above it. With the still-working jobs at the bottom, the long-running carry draws as a calm floor and the day-to-day churn rides on top.

- [`components/jobs/JobSummaryTimelineView.tsx`](../../src/components/jobs/JobSummaryTimelineView.tsx): `order = ['working', 'billed', 'paid']`; legend lists in the same order.
