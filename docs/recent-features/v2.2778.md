# v2.2778 — Takeoffs New 1: one fixture at a time (2026-09-04)

PR 6 of the Takeoffs refresh train (`docs/TAKEOFFS_REFRESH_PLAN.md`; mockup B). The **New 1** pill (v2.2768) stops being a placeholder: a guided pass over a Combined takeoff. Old is untouched and still the default; New 2 remains a placeholder until its PR.

## Client only — no migration, no edge functions (reads the v2.2774 RPC)

- New [`TakeoffFocusView.tsx`](../../src/components/bids/TakeoffFocusView.tsx): the coverage strip (compact) with **Fill from book** and **Sheet view** (→ New 2); a sticky fixture rail (done / to-do / $0 dots via `focusRailItems`, per-fixture totals, a **book** hint on uncosted matches); the focused card — header (count · unit · lines · total · "n of N · ↑ ↓ move · Enter = Done"), **What a ‹key› usually gets** (the book card with **Apply** → `fillRowsFromAssemblies` for that row; up to three previous-bid cards from `takeoff_fixture_history` via `pickSameAsChips`, each **Use these lines** → `copyLinesToRow`), **Lines on this bid** (the tab's own line editor, handed in), and the footer — **Remember these lines for "‹key›" in the ‹book› book** checkbox, **Skip**, **Done · next uncosted**. Keyboard: Enter = Done, ↑/↓ move, only when nothing is being typed into (`isTypingTarget`). History loads once per bid (keys de-duplicated), failures degrade to "No other bid has costed this fixture".
- New shared [`TakeoffCoverageStrip.tsx`](../../src/components/bids/TakeoffCoverageStrip.tsx) (Workbench `stat` idiom; New 2 reuses it) and kernel [`takeoffFocus.ts`](../../src/lib/bids/takeoffFocus.ts) (`focusRailItems`, `moveFocus`, `initialFocusId`, `isTypingTarget`; 4 tests).
- [`useTakeoffRoughLines.copyLinesToRow`](../../src/hooks/useTakeoffRoughLines.ts) (plan decision 6): part lines re-price to today's lowest catalog price (overrides not carried), bundle lines to the assembly's lowest supply-house price.
- [`takeoffBookLearnWrite.ts`](../../src/lib/bids/takeoffBookLearnWrite.ts) (plan decision 3): the Remember writes — new `<key> · book` assembly + items (never in place), book entry create / alias, entry items (stage `rough_in`) — driven by `planRememberForBook`; the tab refreshes assemblies and entries afterwards.
- [`BidsTakeoffTab.tsx`](../../src/components/bids/BidsTakeoffTab.tsx): Old's whole Combined line editor (drag context, header, rows, add-line footer) is now `renderRoughLinesTable(rows)` — Old renders every fixture through it, New 1 one fixture — so the two views cannot drift; `takeoffCoverage` memo; `applyBookToFixture` / `useHistoryLinesOnFixture` / `rememberFixtureForBookFromRow`; a By Stage bid on New 1 shows `TakeoffByStageNotice` (plan decision 1).
- Help guide `cost-a-takeoff-one-fixture-at-a-time.md`.

Verified from a worktree dev server on the test bid B398 (Combined) in New 1, after the v2.2774 migration was pushed: rail dots and totals match the sheet; a temporary `WC-99` row showed three **Previous bids** cards (B363 · B393 · B392, key `wc`, from `takeoff_fixture_history` — whose `takeoff_fixture_key` agreed with the TypeScript `fixtureKey` on 17 names, zero mismatches) and **Use these lines** copied B363's bundle line re-priced at the assembly's lowest supply-house price (checked in the database); with lines on the row, **Remember** + **Done** created `wc · book` (2 items) and a `wc` entry with the new assembly linked in a temporary book, then Done reported every fixture costed; ↑ moved the focus along the rail. All temporary rows, lines, book, and assembly removed; the bid's book pointer restored. Render smoke + bids suites green; `tsc -b` clean; lint 0 errors; theme check green.
