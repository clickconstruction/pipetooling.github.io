# v2.2223 — Payment forecast email stream: schema + payload + cron (2026-08-24)

Migration `20260824133529_payment_forecast_email.sql` — REPORT_SUBSCRIPTIONS stream #10, part 1 of the train (dispatcher fn + client UI follow). `payment_forecast_email_requests` (billed-report clone with `repeat_weekly` from day one; staff-insert / creator-read / creator-cancel-unsent RLS). `get_payment_forecast_email_payload()` (service-role only): open billed invoice rows (the billed-report payload's invoice branch — job-shells excluded, matching the client kernel's skip) + inlined pay-speed medians (`get_billed_customer_pay_speeds` v2 mirror, gate removed — service role has no `auth.uid()`) + inlined promises map; `billed_at` ships raw so the dispatcher's kernel port slices the UTC date exactly like the client's `billedReferenceYmd`. pg_cron `payment-forecast-email-dispatch` at `4-59/5` — **co-rides the weekly-money lane**: the v2.1919 stagger filled all five */5 lanes, and weekly-money is the least-active co-tenant (documented in the migration + REPORT_SUBSCRIPTIONS.md). `get_my_email_schedule()`/`get_global_email_schedule()` rebuilt from the 20260820020000 live bodies + `payment_forecast` branches (recipient-scoped).
