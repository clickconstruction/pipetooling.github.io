# v2.2775 — Timeline hover guide (2026-09-04)

Owner ask on the Timeline chart: "as I run my cursor across it I would like to see a soft | | over the date being highlighted where the user can see the date they are about to click on." A hover guide on both the daily and the weekly chart.

## Client only — no migration

- [`JobSummaryTimelineView.tsx`](../../src/components/jobs/JobSummaryTimelineView.tsx): new `HoverGuide` SVG group (pointer-events off) — a soft band over the hovered column (`--text-strong` at 8%), two thin edge lines, and a label pill (`--surface` on `--border-strong`) reading "Thu Jul 23 · 21 running · click for notes" (daily) or "week of Mon Jul 27 · 18 jobs · 9 carried · 9 new" (weekly), clamped inside the plot. Daily: `hoverIdx` state set by `onMouseEnter` on the existing transparent hit rects, cleared on the SVG's `onMouseLeave`; weekly: `hoverWeek` on each week group. The native `<title>` tooltips stay for the full split.
- Help guide: one sentence in "Reading the chart".
