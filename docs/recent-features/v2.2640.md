# v2.2640 — Final Demand Desk: the demand letter in-app (2026-09-02)

Lien Instruments phase 2 (mockup: "Final Demand Desk" artifact, built via the
owner's think → mockup → self-review → revise → build loop).

- **Migration `20260902160748_job_demand_letters.sql`** — one row per sent
  letter: invoice_ids snapshot, amount, deadline_date (partial index for the
  watch), fields jsonb, recipient block, sent_method (certified_mail /
  traceable_courier / email / hand), tracking_number, sent_at, voided_at.
  Office RLS mirroring job_lien_releases; both read-only blocks.
- **Kernel `src/lib/jobsDocuments/demandLetter.ts`** (+8 tests): paragraph
  model of the lientooling.com letter upgraded with a DATED notice-history
  list and a Chapter 53 line that quotes the job's real filing window via
  `lienFilingDeadlineForMonth` (15th of 3rd/4th month per § 53.052,
  weekend-rolled per § 53.003 — holidays not modeled) + `addBusinessDays`
  for the +10-business-day default deadline. § 31.04 theft-of-services is a
  toggle, OFF by default pending the attorney package. HTML/text/print/PDF.
- **`LienInstrumentsModal`** — the orange lien icon's new home on Billed/
  Collections rows (grayed phase-3 stubs for the § 53.056 notice + affidavit;
  lientooling.com button falls back to the old `LienToolingPrefillModal`,
  which stays mounted for that purpose). Demand tab: billed-open bill-line
  chips, recipient from bill-to override → customer (+ customers.address),
  deadline with +10-business-days helper, escalation toggles, notice history
  assembled fail-soft from invoice sends + `jobs_ledger_invoice_stripe_email_sends`
  + `job_payment_chase_touches`, live serif preview, Print/PDF, and the
  record-send step (method chips + tracking + sent-on) inserting the row.
  History box with View (re-renders the snapshot) / two-click Void.
- **Watch**: `demandLettersOverdue` kernel (+5 tests) +
  `useDemandDeadlinesNudge` + red `demand-deadline` Needs You item (tier 40)
  on Dashboard and Quickfill; amber ring via `demandOutJobIds` on the lien
  icon (both unified-table rows + deck bundle).

Types hand-authored to generator shape; verified against `gen-types:linked`
during the merge window.
