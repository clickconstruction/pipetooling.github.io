# v2.2312 — Route loading screen: 10s escape hatch with countdown (2026-08-25)

Owner follow-up to the 2026-08-25 stuck-"Loading…" report (assistant stranded on a hung chunk fetch during deploy churn — DB exonerated, see v2.2307 investigation): "build the loading-screen escape hatch and make it 10 sec with a countdown." Mockup approved: https://claude.ai/code/artifact/47908c91-7635-4460-b2e2-0771a1cdfc39

- **New** [`RouteLoadingFallback.tsx`](../../src/components/RouteLoadingFallback.tsx) (+4 render tests, fake timers): 0–3s bare "Loading…" (normal navs unchanged); 3–10s adds "Taking longer than usual — more options in N" (aria-live); ≥10s swaps to "Still loading. This can happen right after an app update." + **Reload** button (`location.reload`) + **Fix the app** → `/fix-cache.html`.
- **[`Layout.tsx`](../../src/components/Layout.tsx)**: the route `<Suspense>` fallback (inside `RouteChunkBoundary`) uses it. Complements v2.811 chunk recovery, which only fires when a chunk fetch FAILS — this covers the hung-fetch case. The sign-in `AuthGateLoadingScreen` keeps its v2.1051 5s link.

Client-only.
