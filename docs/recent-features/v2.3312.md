# v2.3312 — Test reports PR 7: the portal card folds past five (2026-09-11)

Client only; no migration, no edge function. A residual from [`to-dos/test-reports/README.md`](../../to-dos/test-reports/README.md).

## What shipped

- `portalPayload.ts`: `PORTAL_TEST_REPORTS_CARD_LIMIT = 5`, `foldPortalTestReports(list, expanded, limit)` → `{ visible, hidden }`, `portalCertifierLine(name, license)` → *certified by Malachi Whites (#RMP41130)* (+2 tests).
- `CustomerPortal.tsx`: the **Test reports** card renders the fold with a *Show all N reports* / *Show fewer* link (screen-only, like the card itself); the inline job line and the card share the certifier spelling — the card used to rewrite `#RMP41130` as `RMP #41130`.
- Render smoke: seven sent reports → one inline line on the matching job plus five card rows, eight after *Show all*.

## Verified

Tests; typecheck · lint · theme check.
