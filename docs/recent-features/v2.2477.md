# v2.2477 — Plan substrates + twin agent reads (2026-08-29)

Estimator-twin pipeline Wave 1.4 + Wave 2.3/2.4 (docs/ESTIMATOR_TWIN_PIPELINE_PLAN.md;
data contract docs/twins/SUBSTRATE.md), shipped as one twin-mcp release:

- **Migration `20260829021122_plan_substrates`**: `bids_plan_substrates` — one immutable
  jsonb substrate per (bid, version), estimating-staff RLS mirroring the bids family,
  all three appliers (read-only ×2 + twin fence; the fence auto-covers it via `bid_id`).
  Deviation from the plan's storage-bucket default recorded in the plan status log:
  jsonb is simpler, queryable in-app, and fence-covered for free.
- **twin-mcp gains three read tools** (`supabase/functions/twin-mcp/index.ts`):
  `get_assignments` (bids where estimator = the twin — the work queue),
  `get_plan_brief(bid, full?)` (latest substrate; rollup by default), and
  `get_work_state(bid)` (composite resume: bid facts, stamped links, substrate version,
  counts-row count, audit-ledger tail; `not_yet_in_v1` names what later waves add).
  Both bid-scoped reads enforce assignment-is-the-grant (estimator or creator only).
- **Pipeline brief**: estimator brief section 8 — the stages as the twin must follow
  them (get_work_state-first, RFI: note convention, frozen-book-copy rule, hard stop
  before pricing-final/sending); briefs.ts regenerated.

Deploy after merge: `supabase db push`, then `supabase functions deploy twin-mcp`;
gen-types follow-up. First substrate attached: LIVSTE v0.4 on b403 (ZZ Twin LIVSTE).
