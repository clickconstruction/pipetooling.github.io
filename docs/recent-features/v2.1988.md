# v2.1988 — Portal requests notification stream (2026-08-21)

Portal train PR 3: the "whomever we want CC'd" layer.

- New app_settings key `portal_request_email_recipients_v1`
  (`APP_SETTINGS_KEY_PORTAL_REQUEST_EMAIL_RECIPIENTS`; paid-stream v1 JSON
  id-array format, parsed with the existing `parsePaidJobEmailRecipients`).
- Settings → Email streams gains a **Portal requests** card that is its own
  manager (every other stream defers to a home-surface modal; the portal has
  no staff surface yet, so the card carries an add-picker of office-capable
  users plus the usual remove chips). Empty-state copy notes dispatch-group
  push fires regardless.
- `submit-portal-request` now emails the configured list after the dispatch
  insert + push fan-out: subject "Portal visit/bid request — {Customer}",
  body with description/availability/phone/plans; best-effort
  (`sendEmailViaResend` shared helper), the inbox row is already safe.
- No migration. Redeploy `submit-portal-request` at merge (drift cron
  enforces).
