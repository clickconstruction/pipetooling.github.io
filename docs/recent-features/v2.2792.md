# v2.2792 — GC Review: the sender card — a round as its sender sees it (2026-09-04)

Owner ask: "If I made 'in Malachi's round' clickable so the user could see the status as Malachi might see it, how would I present this?" Card mockup approved; a read-only card rather than a "view as" impersonation (the overlay's buttons would forge his marks).

- [`GcSenderRoundCard.tsx`](../../src/components/jobs/GcSenderRoundCard.tsx) (new): **"Malachi's round this week · 0 of 2 sent"** — the sender's queue in Start-round order (kernel `senderRoundQueue` in [`gcStatementRounds.ts`](../../src/lib/jobs/gcStatementRounds.ts), +1 test: ready by amount, then held, then marks; assigned-only tally like `senderProgress`), each row with its state (ready · held, changed since certified / not certified yet · sent Wed · email ✎ · skipped), the clicked GC highlighted. Then **How he's being prompted**: Dashboard row (showing while anything is ready), round email (his chains, or "not subscribed" → set it up for him, which opens the v2.2771 block on his behalf), last activity (`latestGcStatementMarkBy`, new read in `gcStatementRoundIo.ts`). Actions: **Preview Malachi's email**, **reassign** (opens the assign picker), **undo** a mark — certifier roles only.
- [`JobsGcReviewModal.tsx`](../../src/components/jobs/JobsGcReviewModal.tsx): every rounds chip is now a button that opens the card for its sender (highlighting that GC); a **needs a sender** chip opens the assign picker instead; the per-sender tally in the panel header opens the same card. **Undo moved off the chip into the card** — the chip's click was the only undo door before.
- [`statement-round-email-dispatch`](../../supabase/functions/statement-round-email-dispatch/index.ts): `preview` accepts `recipient_user_id` for office callers (reads a colleague's round — the same data the panel shows them); `test_send` stays caller-only. Redeployed. Client: `fetchStatementRoundEmailPreview(recipientUserId?)`.
- Help guide [`run-your-gc-statement-round`](../../src/content/help/run-your-gc-statement-round.md): new "See a round the way its sender sees it" section; the chip-undo sentence retired.

Verified live as dev: the Knight chip opens "Malachi's round this week · 0 of 2 sent" with Knight highlighted as ready and RMC held · changed since certified; the prompting rows read Dashboard showing · 1 GC waiting, round email not subscribed, last activity loaded; Preview Malachi's email rendered his round via the redeployed function. Client + edge function; no migration.
