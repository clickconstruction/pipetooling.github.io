# v2.3823 — Pipeline: the money story reads before the search box (2026-09-25)

The owner, 2026-09-25: *"I want to move the money story above the row the search bar is on"* — and, on the first cut that moved the four cards alone, *"this section should be above the search"* of Today's Money Opportunities. Since v2.2012 the cards and the opportunities drew as one block under the map, which itself sat under the command bar (New Job · Follow-ups · Forecast · search · # · ⋯). Client only, no migration.

## The change

- **[`JobsStagesTab`](../../src/components/jobs/JobsStagesTab.tsx)**: the `PipelineOverview` JSX (the four cards + Today's Money Opportunities) is one local `renderPipelineOverview()`, called above `JobsStagesCommandBar` on the desktop board — before the search box — and inside the phone board's Overview fold at the bottom, where it was (v2.3762). The map stays under the command bar. The v2.3184 rule holds: a live search folds the block away. `PipelineOverview` itself is untouched.
- Guide *read the Pipeline money view* → the opening sentence.

## Verified

- `PipelineOverview.render.test.tsx` and `pipelineOverview.test.ts` unchanged and passing; typecheck, lint; live at 1024 px: the cards and Today's Money Opportunities sit above the command bar, then the search row, then the map; a search folds the block and clearing brings it back.
