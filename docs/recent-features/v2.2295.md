# v2.2295 — With Dispatch chip on estimate list cards (2026-08-25)

Follow-up to v2.2293 caught during pre-merge live verification: the two draft-status blocks in `Estimates.tsx` (desktop table vs `EstimateListCards`) differ by one line of indentation, so the `replace_all` that added `withDispatchChip(r)` only landed in the table variant. Added the same call to the card variant's readiness-dots span — verified live on the dev server (card shows WITH DISPATCH before the dots).
