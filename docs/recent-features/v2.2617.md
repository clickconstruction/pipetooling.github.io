# v2.2617 — Crew Day round two: office fold + restacked day nav (2026-09-02)

Owner-approved mockup (the Crew Day artifact, round two) built as speced.

- **Office fold (superintendent viewers only)**: office-role people (dev/master_technician/assistant/controller — `isCrewDayOfficeRole` in `src/lib/crewDay.ts`) drop out of the person list AND the summary chips; a dashed "+N office hidden" chip and a dashed fold row ("N office staff hidden (names…) — Show office staff") say where they went. The Show choice persists per device (`pipetooling_crew_day_show_office`); "Hide office staff" reverses it. Office viewers always see everyone. Role-based by design — an office person who worked a field job still folds. All-office days show "No field crew activity for this day." above the fold row.
- **Migration** `20260902001331_crew_day_payload_roles.sql`: both payload RPCs (`get_crew_day_payload`, `get_crew_day_payload_for_user`) rebuilt verbatim from their shipping migrations with one change — the `users` array gains `role` (additive; older clients ignore it).
- **Restacked day nav (all roles)**: ◀ ▶ sit together top-right with the label floating beneath; `crewDayNavWord` (kernel, tested) gives Today / Yesterday / "N days ago", with the short date as a muted suffix.
- Kernel also gains `crewDaySummaryFor(people)` (chips over any subset — buildCrewDayView now uses it too) and `CrewDayPerson.office`. +5 kernel tests, +2 render smokes (fold hidden/shown, office viewers unaffected).
