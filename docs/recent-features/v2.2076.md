# v2.2076 — My Team crew-lead section redesign (mockup A) (2026-08-22)

Owner screenshot review of Abraham's phone, round 3: the crew-lead approval section put ~600px of chrome (stacked date pickers, a 7-column roster table that clipped at "Manu…" with the Notify checkbox fully off-screen, an empty Active box) ahead of the only thing a lead opens it for — approving hours — and nothing flagged a suspicious 14.5h session. Mockup (Current + A/B/C) → variant A picked.

[`DashboardMyTeamSection`](../../src/components/DashboardMyTeamSection.tsx) + new kernel [`myTeamApprovals.ts`](../../src/lib/myTeamApprovals.ts) (tested):

- **Header chip**: amber "N to approve" rides the My Team title in both collapsed and expanded states ("nothing pending" otherwise).
- **Week pager**: one row — `‹ This week · Aug 16–22 ›` (`formatTeamWeekLabel`); tapping the label reveals the original exact Start/End date inputs.
- **Person cards** replace the roster table: name + `personWeekSummaryLine` ("46.4h this week — all waiting on you" / "38h — 30h approved · 8h pending") + a bell button for the clock in/out notify preference (previously an off-screen checkbox).
- **Pending approval card first** (keeps the `dashboard-my-team-pending-sessions` anchor for the banner's Go-to): **Approve all N · Xh** (existing `approveClockSessions` batch RPC + confirm), then per-session cards — day + hours headline, `⚠ long day` past 12h (`isLongSession`/`LONG_SESSION_HOURS`), times + `ClockSessionLocationCell` map links, job label + `AssignSessionJobPopover` Change, quoted note, and a 44px [Approve Xh][Reject][Edit] row (same handlers as before).
- **"On the clock right now"** box renders only when someone is actually clocked in (Force clock out kept); the **All clock activity** ledger (Detailed/Simple, Chronological/By person, people filter) is unchanged and sits last.

Verified live as Abraham (login-as-user, 402×874): chip, pager (label → pickers; ‹ shows "Aug 9–15" without the "This week" prefix), Paige's sentence card, Approve all 7 · 46.4h, and the ⚠ flag on the 14.49h Thursday. New help guide `approve-my-teams-hours.md`. Client-only — no migration.
