# v2.2854 — `scripts/twin-session.sh`: authenticated twin sessions in one command (2026-09-05)

The 2026-09-05 backfill session found the twin-mcp connector bootstrap machine-bound and manual: `.mcp.json` reads `$TWIN_ESTIMATOR_1_TOKEN`, MCP servers connect at session start, and a session launched without the variable cannot repair the connector mid-flight — a new machine meant mint → copy → export → restart, by hand.

- **`scripts/twin-session.sh`** — reads the key from `~/pt-twin-digest/twin.token` (override with `TWIN_TOKEN_FILE`), exports `TWIN_ESTIMATOR_1_TOKEN`, and execs `claude`. `--print-env` emits just the export line for `eval`. Helpful error with the mint-and-save recipe when the file is missing.
- **TWIN_HARNESS.md → "Bootstrapping a Claude Code session"** — documents the launcher, the one-file-per-machine key convention (mode 600, revocable per-key from Settings → Digital twins), and the mid-session HTTP fallback (`X-Twin-Token` + JSON-RPC), with the rule that keys are referenced by file path, never pasted into transcripts.
- `briefs.ts` regenerated (the harness guide is bundled) — **deploy `twin-mcp` after merge** so agents read the new section.
