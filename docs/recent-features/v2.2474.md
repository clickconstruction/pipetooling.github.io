# v2.2474 — Counts and Pricing default to their New views (2026-08-28)

Owner ask (clarifying the v2.2473 request): the Old/New pairs they meant are the per-tab view
pills on the bid workflow tabs. Inventory of all Old/New pill pairs:

- **Counts** (`BidsCountsTab.tsx`) — Old = classic sortable table, New = the Count Sheet.
  Defaulted **Old**. → now defaults **New**.
- **Pricing** (`BidsPricingTab.tsx`) — Old = classic grid, New = the Workbench.
  Defaulted **Old**. → now defaults **New**.
- **Cover Letter** (`BidsCoverLetterTab.tsx`) — already defaulted New since its pills shipped;
  untouched.
- **Takeoffs** — has no Old/New pills (single view); nothing to change.

Mechanism (the `roadmap_view_v2` pattern, v2.1913): each localStorage key bumps
(`bids_counts_view_v1` → `_v2`, `bids_pricing_view_v1` → `_v2`) with the default flipped to
`'new'` — every device's remembered view is deliberately dropped so the whole team lands on New
once; an explicit flip to Old sticks again under the new key. No migration, no edge changes.

Help guides `count-with-the-count-sheet` and `price-a-bid-with-the-workbench` now state the tab
opens on New.
