# v2.2772 — Estimate accept page: room on a phone (2026-09-04)

Owner saw the sample accept page in Settings → What customers see at phone width: the three-column line-item table left the name ~150 px, so "Remove existing water heater" wrapped one word per line; the photo hero and trade tiles filled the first screen before any estimate; the Approve button sat under the terms. Picked direction **B — Estimate first** from three mockups (artifact 8bcf83f6).

- **Stacked lines on phones** — [`EstimateCustomerDocument.tsx`](../../src/components/estimates/EstimateCustomerDocument.tsx) renders both the table (`.estimate-doc-lines--table`) and a new `EstimateLineItemsStack` (`.estimate-doc-lines--stack`: name, description, then *qty × unit* and the amount on one row); `index.css` shows the stack under 520 px, the table above it and in print. The document title drops to 1.5rem under 520 px.
- **Total first** — a `summary` slot under the meta rows; the accept body passes a card with the total (the selected option's when options are offered; signed for change orders), *Pricing is good through …*, and an Approve button that opens the same signature modal. Hidden on an already-accepted staff record. The card also shows in staff previews, matching the customer.
- **Compact hero** — `AuthPublicLandingLayout` gains `compact`; under 640 px the estimate accept shell renders a thin title band, no trade tiles, top-aligned. Desktop and the sign-in/contract pages are unchanged.
- **Bottom bar on phones** — `.estimate-accept-sticky` (interactive variant only): total + validity + Accept, fixed to the bottom under 640 px, hidden while the page's Approve button is on screen (IntersectionObserver), while the modal is open, and in print.
- **No update pill inside frames** — `UpdatePrompt` returns null when `window.self !== window.top`, so the What-customers-see thumbnails stop showing "A new version is ready".
- Render test: both line layouts present with *2 × $125.00* and the amount; the summary sits above the line items.
