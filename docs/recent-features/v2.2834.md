# v2.2834 — Statement round: fixes from the live run-through (2026-09-05)

Owner: "Test as much as you can live." Full pass against prod as the dev user, every write undone afterwards (marks deleted from the sender card / chip, the test assignment cleared; psql confirmed zero marks this week and no sender on Southern Post).

**Verified working**: assign a sender from the panel (tally + chip update); Share → Mark sent / spoke with them… → Spoke · Call · Cold + sentence + pay date → chip `spoke Sat · call · cold`, tally `0/1 sent · 1 spoke`, header pill `cold · Sat · Robert` with the sentence on hover, green `pays by Sep 12` chip, Temperature board row first with the red dot and the last word, send history `Spoke · Call · cold · pays 2026-09-12`, sender card `spoke Sat · call · cold ✎ undo`, round email preview `0 of 1 sent · 1 contacted`; undo from the sender card → board back to `no read`, Dashboard row **One GC is waiting on your statement** → Start round lands the overlay on that GC → Sent it → Statement sent · Text + note → Round done, chip `sent ✓ Sat by Robert · text ✎`, header `Sent Sep 5 · text`, week strip `1 sent`; a second cold read on a smaller GC moved it from 6th to 2nd on the Payment follow-up list (cold-first sort proven against late dollars).

**Fixed** ([`render.ts`](../../supabase/functions/statement-round-email-dispatch/render.ts), redeployed): the subtitle said "$0.00 outstanding across your accounts" when nothing was ready — it summed the ready GCs; now `book_total`, matching the scoreboard. ([`JobsGcReviewModal.tsx`](../../src/components/jobs/JobsGcReviewModal.tsx)): a marked GC with **no sender** had no undo door — the chip opened the assign picker; it now undoes the mark (the v2.2761 behaviour) and only opens the picker when unmarked. ([`PaymentChaseModal.tsx`](../../src/components/jobs/PaymentChaseModal.tsx)): the rail caption reads "coldest first, then biggest" whenever a cold or cool read is reordering the list.
