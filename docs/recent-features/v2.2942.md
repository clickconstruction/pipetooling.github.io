# v2.2942 — Holdout set: the flag, the Queue lens surfacing, and gate-math prep (2026-09-06)

`docs/twins/LEARNING_PLAN.md` lever 3 / build item 6, app side only. With ~130 usable references, gates measured on practiced bids overstate readiness — a holdout slice (target 20–25 clean A/B refs, spread across axes, owner-designated) is reserved for gate measurement: never run as practice, never quoted in doctrine, never named in audits. WHICH references go in is the owner's call later; the twin-mcp edge-function exclusions are the operator's separate change — this PR builds the flag and the surfaces.

- **Migration `20260906120000_bids_holdout.sql`**: `bids.holdout boolean not null default false` + partial index (`WHERE holdout`) + the column comment carrying the doctrine. Additive/idempotent, `SET lock_timeout = '3s'`; plain ALTER, so no read-only appliers needed. Apply order: client first, then push.
- **Queue lens** (`BidsRobotQueueTab`, dev-only): every backtest candidate row gets a Hold out / Release holdout toggle (writes `bids.holdout` via the untyped-client pattern until the post-push gen-types run). Held-out rows show a violet HOLDOUT badge, render "gate measurement only — no practice prompt" instead of the Copy backtest prompt button, and live in a new `holdout` list on `BacktestAxisGroup` — never in `eligible`, so practice slates and the candidate count exclude them. A counter under the section head: "Holdout: N references (target 20–25, spread across axes)" with the axes covered (`holdoutSummary` in `backtestCandidates.ts`); `starvationLine` distinguishes "all held out" from "nothing graded".
- **Gate-math prep** (`confidenceBoard.ts`): `buildAxisCards` takes optional `holdoutReferenceNumbers`; each axis card exposes `holdoutRuns` / `practiceRuns` / `streakHoldoutRuns` (runs matched by `reference_bid_number`, b-prefix-insensitive — `normalizeBidNumber` moved here from `backtestCandidates.ts`, still re-exported). **Current gate semantics untouched** — the streak still counts every eligible run; switching denominators to holdout-only is a future owner decision. Scoreboard axis cards show a muted "no holdout evidence yet" when a scored axis's streak contains zero holdout runs (the pills row is untouched — the shadow-coverage pill is a parallel PR).

No help-guide change: the Queue and Scoreboard lenses are dev-only and no existing guide covers them (checked `src/content/help/`).
