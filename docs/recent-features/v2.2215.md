# v2.2215 — Estimating Pulse is the Health section; the Old view retires (2026-08-23)

Owner, after seeing the Pulse live: "It looks great, we can now get rid of old."

- [`BidBoardEstimatingHealthSection.tsx`](../../src/components/bids/BidBoardEstimatingHealthSection.tsx)
  shrinks from 706 lines to a thin wrapper: the Pulse + the dev-only estimator labor table. The
  Old/New pills (v2.1918) and the `bid_board_health_view_v1` localStorage read are gone (stale
  keys are harmless).
- **Deleted with the Old view** (referenced nowhere else): `BidBoardWeeklySentSection.tsx`
  (classic weekly pivot), `BidBoardEstimatingHealthSliders.tsx`, `StaffOutcomeDrilldownCountCell.tsx`.
  Kept: `BidBoardWeeklySentCellModal` (the Pulse uses it), the `bidBoardStaffOutcomes` kernel
  (Followup uses it), `bidBoardWeeklySentStats` (Pulse + dev labor).
- [`BidsBidBoardTab.tsx`](../../src/components/bids/BidsBidBoardTab.tsx) drops the now-dead
  `bidBoardStaffOutcomeByRole` memo and the section's `staffOutcomeByRole` / `ledgerPrefixMap` props.
- Help guides `read-estimating-health.md` / `read-the-bid-board.md` no longer mention Old/New.
