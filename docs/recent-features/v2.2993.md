# v2.2993 — chore(types): regenerate `database.ts` after the one-company pushes (2026-09-07)

`npm run gen-types:linked` against prod after `20260906190000` → `20260907060000` (one company Phases 1–5 plus the opaque-columns fix). Adds `company_owner_user_id()` to Functions, moves `master_assistants` / `master_shares` from Tables to Views, adds the `retired_master_assistants` / `retired_master_shares` tables. 20,758 lines (+151 / −106) — the first regeneration after Phase 5 alone was 29,102 lines and broke the typecheck; [v2.2995](v2.2995.md) explains why and fixed it at the views.

Generated file — regenerated, never hand-merged: on a rebase conflict take main's copy and re-run the generator.
