# v2.2002 — Customer profile rails → informative lists (2026-08-21)

Round 2 of the profile rebuild (v2.1993 did Jobs; owner: pills of bare numbers aren't informative; mockup on the "Customer Profile v2" canvas). New kernel [`customerProfileRails.ts`](../../src/lib/customers/customerProfileRails.ts) (6 tests): outcome buckets (`started_or_complete` counts as won), `bidDisplayValue` (agreed_value once won, else bid_value, null hides), `bidClock` ("due in Nd"/"due today" from `bid_due_date`, "due Nd ago" overdue only when never sent, "sent Nd ago · undecided" from `bid_date_sent`, decided → "won/lost · {sent date}" — there is no outcome timestamp column), `sortProfileBids` (due-soonest → longest-waiting undecided → decided as fetched), `estimateStatusShortLabel`. [`fetchCustomerProfile`](../../src/lib/customers/fetchCustomerProfile.ts) widens the bids select with `address, bid_value, agreed_value, bid_date_sent, bid_due_date` (same single query). [`CustomerProfileModal`](../../src/components/customers/CustomerProfileModal.tsx): Bids = summary line + two-line rows (BP# link · project · value / address · clock), collapsed at 3 with show-all; Projects = full-name rows with step position/name/assignee + first attention flag; Estimates = #, title, status label, dollars (total_cents was fetched but never shown). Dead `Rail`/`pillStyle` removed. Verified live on Knight Contracting (49 bids: summary + 191d-waiting bid surfaced on top) and mobile-clean at exactly 375px (zero horizontal overflow). Client-only — no migration.
