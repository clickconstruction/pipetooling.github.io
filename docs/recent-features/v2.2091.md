# v2.2091 — History split: repeating grid + one-off ledger (2026-08-22)

Owner report: the History grid neither showed when a task was created nor separated recurring from one-off tasks — one-offs were rows of ~120 dashed boxes hiding a single ✓ (mockup artifact 7c1a9f69, owner-approved). New pure kernel [`checklistHistorySplit.ts`](../../src/lib/checklistHistorySplit.ts) (`splitHistoryItems`, 4 tests): `repeat_type` missing/`once` → one-off row (status done / done_by_other / missed / open, created + completed days via `calendarYmdInAppTzFromIso`), else a repeating grid row whose `sinceYmd` = the earlier of the item's `created_at` day and its first instance (backfills win). Desktop History in [`Checklist.tsx`](../../src/pages/Checklist.tsx): grid columns now come from repeating instances only; pre-birth cells render as empty `<td>`s (no dashed box) with a blue birth tick + "since M/D" under the title; one-offs render as a ledger below (chip + title + "created → done" meta), edit-mode chip cycling reusing `handleCycleStatus`. `handleCycleStatus` reads current status from `instances` directly (the old `byItem` map is gone). History select gains `repeat_type, created_at` on the item embed. Phone ledger view unchanged. Help guide section updated. Client-only — no migration.
