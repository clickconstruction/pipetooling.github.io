# v2.1974 — Edge drift content check + 3 stale functions redeployed (2026-08-21)

An audit found `check-edge-function-drift.mjs` only verified function
PRESENCE — `notify-dispatch-request`, `notify-estimator-request` (edited
Apr 30: trade-aware ledger-prefix labels in pushes), and
`send-report-notification` (edited Apr 22+28: Status Report rename, maps v1)
had run 4-month-stale bundles unnoticed. Both halves fixed:

- **Redeployed** all three on 2026-08-21 (versions bumped, `verify_jwt=false`
  preserved from config.toml for the two notify functions).
- **Script hardened**: for every deployed function it now compares the
  function dir's last commit date against the deployed `updated_at` and
  FAILS (exit 1) when the repo is newer, with a grace window (default 72h,
  `EDGE_DRIFT_GRACE_HOURS`) for the deploy-from-branch → squash-merge-later
  workflow. `_shared/` edits newer than the oldest deploy warn (bundled into
  importers, but flagging all 71 on any shared edit would be noise).
  `edge-function-drift.yml` gains `fetch-depth: 0` — a shallow clone would
  silently skip the git-date comparison.
- Verified: normal run OK (71/71 current); `EDGE_DRIFT_GRACE_HOURS=0` flags
  the same-day deploy-then-merge cases and exits 1.
