# v2.2175 — Primary scoping 2/3: estimates (2026-08-23)

Stacked on v2.2174 (bids). Migration [`20260823171822.sql`](../migrations/20260823171822.md): restrictive
policies on `estimates` + `estimates_thread_notes` — a primary sees estimates they created
or on jobs they're Account Man for. [`Estimates.tsx`](../../src/pages/Estimates.tsx): the list
is RLS-driven (no query change); the empty state tells a primary what they're looking at.
Finding recorded for later: "estimates they sent" is not representable today (no `sent_by`
column) — `created_by` is the proxy (D2).
