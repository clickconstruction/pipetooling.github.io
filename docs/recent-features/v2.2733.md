# v2.2733 — Wheels on Labor PR 1: vehicle arrangement + per-field-hour report (2026-09-03)

Owner picked Variant C of the "Wheels on Labor" proposal (artifact 8832d369): own-vehicle fuel belongs to the person's labor, company trucks get a per-field-hour line. PR 1 records the deal and shows the rates read-only; PR 2 wires Review.

## Migration

- `20260903233954_pay_config_vehicle_arrangement.sql` — `people_pay_config.vehicle_arrangement` (`none | own_fuel_paid | company`, default none, CHECK) + `vehicle_rate_override numeric(8,2)`. See `docs/migrations/`.

## Kernel

- [`lib/people/wheels.ts`](../../src/lib/people/wheels.ts) (+7 tests): `wheelsWindow` (90 days), `sumFuelByUser` (|amount| per attributed user), `fieldHoursByUser` (approved, closed, job-not-bid sessions), `truckRunningCost` (fuel + insurance-while-on-plan + registration pro-rated by days/7 + service, ÷ holder field hours), `ownVehicleFuelRate`, `buildWheelsRows` (one row per pay-config person: computed rate for the arrangement, override wins, a plain-English note), `wheelsComparison` (averages per deal).
- [`lib/people/wheelsData.ts`](../../src/lib/people/wheelsData.ts): `loadWheelsSnapshot` — transactions posted in the window classified with `categoryTagForCharge` against the fuel tag (`default_key = fuel_vehicle`, else a name match), `fetchAttributionsByMercuryTxIds` for the person, clock sessions by `work_date`, vehicles + possessions (`currentPossession`, motor pool excluded) + insurance periods (`currentInsurancePeriod`) + service events; `saveVehicleRateOverride`.

## Client

- **Pay config modal** ([`PeoplePayConfigModal.tsx`](../../src/components/people/PeoplePayConfigModal.tsx)): a **Vehicle** select per row (`VEHICLE_ARRANGEMENT_OPTIONS`). [`usePayConfig.ts`](../../src/hooks/usePayConfig.ts) reads both columns through a cast (`select('*')`) until `database.ts` is regenerated after the push, and carries them through the upsert. `PayConfigRow` gains the two optional fields.
- **Wheels section** ([`PeopleVehiclesWheelsSection.tsx`](../../src/components/people/PeopleVehiclesWheelsSection.tsx)) on People → Vehicles, dev-only like Review: comparison line (own avg vs company avg), people table (deal chip, truck, fuel 90d, field h, fuel/field h, rate, override input, why), company-truck running-cost table, unattributed-fuel call-out. `VehicleArrangementChip` is exported for PR 2's ranked bars.

## Docs

- Help guide `see-what-each-vehicle-costs-per-field-hour.md`; `GLOSSARY.md`.

## Next (PR 2)

- Review: own-vehicle fuel leaves the job parts share and lands on the person's labor line; company trucks get a Wheels line per field hour on the burden side; verdict bar gains the two segments; ranked bars carry the arrangement chip (Variant C).
