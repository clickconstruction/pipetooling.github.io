# v2.1911 — Pipeline refresh part 3: the Ledger money view — prototype complete (2026-08-20)

Last car of the owner-approved Pipeline Refresh prototype. [`estimatePipelineRefresh.ts`](../../src/lib/estimatePipelineRefresh.ts) gains `ledgerRowPasses` (kind filter estimate/CO via `doc_kind`, superseded+declined excluded unless toggled, `updated_at` within an N-day window) and `computeLedgerTotals` (accepted-this-UTC-month via `acceptor_consented_at`, outstanding `sent` value, accepted-without-`job_ledger_id` value) — +2 test blocks. The Ledger tab ([`Estimates.tsx`](../../src/pages/Estimates.tsx)) gains the filter chip row (All kinds / Estimates / Change orders, a 30/90/365/all-time range select defaulting to 90 days, "Include superseded & declined") and the totals footer (green / amber / red-when-nonzero), all computed from the filtered rows — so "change orders, last 90 days" answers itself. Verified live against prod: accepted-this-month $7,693.43 matched the three August acceptances; the unlinked total correctly excluded a 3-month-old row outside the default window. Client-only — no migration.
