# v2.2656 — Outbound email catalog + complete send log (2026-09-02)

PR 1 of the owner-approved email-wording plan (mockups: Email Catalog canvas; wording-only by decision — attached documents are never editable). Full 33-type inventory with file:line pointers was compiled 2026-09-02 (this fragment is its durable home going forward via `src/lib/emailCatalog.ts`).

- New `src/lib/emailCatalog.ts` (+tests): 32-row registry (33 composition paths; the 11 workflow templates ride one aggregate row, resends/reminders/[TEST] twins ride their parents) — id, group, audience, attachment, client/server-built, sender fn, editable status (`templates` / `estimate_settings` / `hardcoded`), subject example.
- New `SettingsEmailCatalogSection` mounted atop Settings → Email templates (dev): grouped rows, chips, live subject for single-template rows, per-type last-sent/30-day stats. Footer names the out-of-scope senders (Stripe invoice emails, Supabase auth emails).
- Migration `20260902183356_email_send_log_email_type.sql`: `email_send_log.email_type` + partial index — see `docs/migrations/20260902183356_email_send_log_email_type.md`.
- `_shared/logEmailSend.ts` takes `emailType`; `sendResendHtmlEmail` (recurringJobReportCore) now logs. The five previously-unlogged senders log with their type: `send-bid-pricing-package`, `recurring-job-report-dispatch`, `recurring-job-report-test-send`, `schedule-day-email-dispatch`, `ct-roster-audit`, `send-scheduled-reminders` — **all need redeploys** (deferred to the end of the stream; PR 3 redeploys most anyway).
- Next: PR 2 (client-built senders adopt `email_templates` + authenticated read policy), PR 3 (digest subjects/intros adopt server-side), PR 4 (polish: dead `login_as` template, dead test-email duplicate, hardcoded company names → issuer settings).
