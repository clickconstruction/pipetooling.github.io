# v2.2286 — Checklist Review rows: group context bar above the title (2026-08-25)

Owner (screenshot of a phone row where the "Shelve Abrahams #2 and #…" stage chip pushed the 1d/cost/trash icons off-screen): "the context of what group something is with at the top so these edit buttons are not pushed off the page … I would like the group context bar to wrap if need be."

- **Row layout** ([`Checklist.tsx`](../../src/pages/Checklist.tsx), `OutstandingByPersonSortableRow`): the `roadmapGoalChip` moves out of the `.obp-meta` icon cluster to a new `.obp-goalbar` (`flex: 1 1 100%`) rendered *above* `.obp-title`; `.obp-main` is now `flex-wrap: wrap` on desktop too (no-op for rows without a bar — the title's `min-width: 0` still absorbs pressure). The meta line is now age · 💬 · missed · cost · spacer · 🗑 only, so it always fits.
- **Chip** gains a `wrap` option: bar placement renders the full stage name with `white-space: normal` (no 230px ellipsis); inline placement elsewhere (Today tab, forward modal) is unchanged.

Client-only.
