# v2.2618 — Rename: "Copy fixtures for text" → "Supply house list" (2026-09-01)

Owner naming call (picked over "Parts house list" / "Fixture list — no
prices"): the v2.2612 prepare screen outgrew its verb-y v2.2574 label, and
the app's convention names menu items for the document they produce
(Release of Lien). "Supply house" matches the Materials → Supply Houses
vocabulary. Touched: `PricingShareMenu` item label/hint + caret
title/aria (+ render tests), `PrepareFixtureCopyModal` title/aria, help
guide wording + keywords ("copy fixtures"/"copy for text" kept as search
keywords). No behavior change.
