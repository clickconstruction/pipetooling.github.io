# v2.2798 — Contract Forms PR 4: the staff record (2026-09-04)

What staff see after a form is signed. Plan: `docs/CONTRACT_FORMS_PLAN.md`; spec: `docs/CONTRACT_FORMS.md`.

- **`PersonContractSignedRecordModal`**: selects the form columns; for a form row loads the template schema and renders a **Form answers** card — `formFacts` (`src/lib/forms/formRecord.ts`, tested): non-sensitive answers labelled in form order, exactly-one groups folded into one fact, sensitive boxes shown as `••••` + last four from `form_hints` with an "in the PDF only" tag, the source (filled on the signing page / keyed in from paper). **Open signed PDF** calls the new function and opens the short-lived link in a new tab; a 403 surfaces as the function's message.
- **New edge function `open-contract-form-pdf`** (JWT in body, `verify_jwt = false`): gate = dev, controller, or a `pay_approved_masters` row; the caller must also read the row through contracts RLS; mints a 5-minute signed URL into the private `contract-form-pdfs` bucket (download filename `<document> - <person>.pdf`) and inserts a `contract_form_pdf_opens` row. 404 when the row has no filed PDF.
- **Person Desk paperwork**: `PaperworkDocInput.form_template_id`; a form document's detail ends in `· form` so a filled W-9 reads differently from an uploaded link.
- Guide `open-a-signed-form.md` (dev, master, controller). `docs/ACCESS_CONTROL.md` gains the signed-form PDF gate.
- Deploy: `supabase functions deploy open-contract-form-pdf`. No migration (the opens table shipped in PR 1).
