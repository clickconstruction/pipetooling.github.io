# v2.2276 — Assistants' ready-to-bill banner under the header (2026-08-25)

Owner-requested (mockup-reviewed, two refinement passes: full alert card → 36px one-line bar; label copy → verb copy). [`AssistantReadyToBillBanner`](../../src/components/AssistantReadyToBillBanner.tsx) renders as the first child of Layout's `<main>` on **every** page: an orange bar (amber tint + solid-orange count pill, deep-orange bottom border seating it against the header) reading "N ready to bill — send them/it", whole-bar tappable → `/jobs?rtb=1`. Pure bits in [`readyToBillBanner.ts`](../../src/lib/readyToBillBanner.ts) (`canRoleSeeReadyToBillBanner` — **assistant role only**; `readyToBillBannerLabel` — null at zero hides the bar entirely).

- Count = RLS-scoped HEAD count of `jobs_ledger` `status='ready_to_bill'`; refreshes on route change, window focus, and a 5-minute tick.
- `JobsStagesTab` gains the `?rtb=1` one-shot deep link (chase/forecast pattern) → `focusStagesSection('readyToBill')` (opens + scrolls `#stages-ready-to-bill`), param stripped after consume.
- Note: the request said "a bill in Billed awaiting payment" but named and targeted Ready to Bill — built as the Ready to Bill count/destination (2 of 3 signals); flagged to the owner as a one-line swap if misread.
- Kernel + render smokes (role gate, zero-hide, singular copy). Help guide `ready-to-bill-pipeline` gains the banner paragraph.
