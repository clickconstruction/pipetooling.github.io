# v2.2604 — Who owes what: Stripe resend on the bill cards (2026-09-01)

Second of the three "PDF + send" PRs (round-2 mockup; v2.2600 = PDF tail, PR 3 = physical email resend). Note: v2.2601–2603 belong to other sessions; this stream also holds v2.2605 for PR 3.

- `src/lib/jobs/billedByCustomerBreakdown.ts` — `BilledBreakdownBill` grew the resend wiring: `customerId`, `customerEmail`, `stripeInvoiceId`, `stripePaid` (`stripe_invoice_status === 'paid'`), `sentAtIso` (`sent_to_customer_at`). All read from data the board rows already carry — no new fetches.
- `src/components/jobs/BilledByCustomerBreakdownModal.tsx` — cards for unpaid Stripe-billed invoices render `StripeInvoiceSendFromStripeButton` (`micro`/`unboxed`/`compact`, label "Never got it? Resend" — the PaymentChaseModal pairing), and a muted evidence line: "✉ Email sent <Mon D> · stripe" (or "Not emailed yet"). `onSent` records a best-effort `resend` chase touch via `addPaymentChaseTouch` when the job has a customer id — same write path as the chase surfaces (v2.2572).
- New modal prop `authRole` (threaded from `JobsStagesTab`) feeds `stripeModeForBillingFromRole` — dev may resend in Stripe test mode, everyone else live.
- Tests: kernel test covers the new fields (trimming, paid flag, nulls on non-Stripe/shell bills).
- Help: `read-the-pipeline-money-view.md` mentions the resend button on Stripe cards.
