# v2.2086 — Checklist review delete button placement (2026-08-22)

Owner annotation on Review → Outstanding by person: the dev-only per-row delete sat in the left action cluster (drag · ✓ · red trash) right against every task title. Moved in [`Checklist.tsx`](../../src/pages/Checklist.tsx) (row render inside `OutstandingByPersonSortableList`) to the end of the row — a sibling after the toggle-expand div, so it sits flush right past the age chip — and recolored `--text-red-700` → `--text-muted`; the destructive red stays on the confirm modal where the decision actually happens. Same handler/confirm flow, `isDev`-gated as before. Client-only, styling/placement only.
