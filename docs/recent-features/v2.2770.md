# v2.2770 — Takeoffs refresh substrate B: the T8 / T9 hooks leave the tab (2026-09-04)

PR 3 of the Takeoffs refresh train (`docs/TAKEOFFS_REFRESH_PLAN.md`). The two seams the architecture map has named since 2026-08 are extracted from `BidsTakeoffTab.tsx` **as is** — a behavior-equivalent move so New 1 / New 2 can call the same persistence engine Old uses. The tab drops from ~3,030 to ~2,650 lines; nothing visible changes.

## Client only — no migration, no edge functions

- New [`src/hooks/useTakeoffRoughLines.ts`](../../src/hooks/useTakeoffRoughLines.ts) (**T9**): `persistTakeoffRoughPartLine`, `setRoughPartLinePartAndCatalogPrice`, `resetRoughLineToCatalogPrice`, `updateTakeoffRoughPartLine` (the `queueMicrotask` persistence pair moved together, per the preserve-quirks list), `addTakeoffRoughPartLine`, `removeTakeoffRoughPartLine`, `handleRoughPartLinesDragEnd` + its `reorderingRoughPartLine` state, `applyRoughAddAssemblyTemplate` (expand-to-parts), `insertRoughBundleLine`, `applyRoughAddAssemblyBundle`, and the v2.2755 missing-part fallback effect with its tried-ids ref. Inputs are the engine's rows/setter, the bid + active version, the catalog list/setter, `setError` / `showToast`, the catalog-lowest refresher, and the Add-assembly modal's two UI callbacks (expanding flag, close) so those two handlers keep their exact toasts and close behavior.
- New [`src/hooks/useTakeoffPartsCatalog.ts`](../../src/hooks/useTakeoffPartsCatalog.ts) (**T8**): `takeoffAddTemplateParts` + its two paged loads (Combined active; any authoring modal open — the three flags stay separate inputs so the effect re-runs exactly as before), `supplyHouses` / `partTypes` + their mount-per-service-type loads, and the exact-model `takeoffTemplatePreviewCache` with its lazy fill.
- [`BidsTakeoffTab.tsx`](../../src/components/bids/BidsTakeoffTab.tsx): the two hook calls sit after the state block; `refreshTakeoffRoughCatalogLowest` (a `useCallback` on `showToast` only) moved up beside them so it can be passed in. The qty-numpad handlers, the remove-confirm modal, the PartForm-routed bridges, and the two post-PartForm catalog reloads stay in the tab (they are UI, and the map says so). Imports pruned.
- `docs/BIDS_TAKEOFF_TAB_ARCHITECTURE.md` decomposition log: T8 / T9 recorded.

Verified from a worktree dev server on the test bid B398 (Combined, Old view): add part line → pick a part → the row persists with its lowest catalog price; qty edit persists; delete removes the row; Add assembly (expand) inserts priced lines; render smoke + all bids suites green; `tsc -b` clean; lint 0 errors.
