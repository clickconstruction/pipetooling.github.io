# v2.3545 — Stages tab decomposition, PR 12: the confirm dialog nothing could open is gone (2026-09-16)

Client only. PR 12 of the train in `to-dos/engineering-hygiene.md`, and the one step of it that is a deletion rather than a move: the map's preserve-quirks list (item 5) recorded `confirmJobStatusJob` as *dead state + dead modal — no setter call sites* and reserved its removal for a PR of its own, so that no move would silently carry a behavior change.

## What went

The `confirmJobStatusJob` state (a job id, a target status of `billed` or `paid`, and a message) and the generic *Are you sure?* dialog it rendered in the modal tail. A repo-wide grep found the declaration and the dialog's own Cancel / Confirm handlers as the only references — no row action, handle method, or deep link ever set it, so no user could reach the dialog. The typed `updateJobStatus` path it would have called is unchanged and still reached by the live confirms (`StagesSendBackSimpleConfirmModal`, `StagesReadyForBillingConfirmModal`).

Numbers: `JobsStagesTab.tsx` 4,752 → 4,712 lines.

## Verified

- Typecheck, lint (0 errors; the same five pre-existing warnings), the tab's render smoke, full suite.
- Live on prod data at Jobs → Pipeline as dev: the board renders unchanged.
