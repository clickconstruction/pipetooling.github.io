# v2.2465 — Options picker in the rail-v2 Customer view (2026-08-28)

Estimate Options follow-up, found in the live end-to-end verification of v2.2462: the
draft editor's **Editing | Customer view** toggle (rail-v2, newer than the Estimates
architecture map) renders its own `EstimateAcceptBody` — a second instance the options
props never reached, so a multi-option draft's Customer view showed the recommended
option's document with no picker. Wired like the other two surfaces (CX Page tab,
new-tab Preview as customer).

Verified live on a throwaway draft against prod: picker cards with the Recommended
badge pre-selected, "Your selection — <name>" heading and total following the click,
options_snapshot + recommended-mirror persistence, and the Pipeline "· 2 options" badge.
