# v2.2322 — Portal recap payment rows drop the generic "Payment" suffix (2026-08-26)

Owner ask on v2.2320's itemized rows: "Paid Jul 31, 2026 · Payment" → drop
"Payment".

- `portalJobGroups.ts`: `PORTAL_GENERIC_PAYMENT_METHOD = 'Payment'` exported;
  `portalPaymentMethodLabel` unchanged in behavior (blank/`other` → generic).
- `CustomerPortal.tsx`: the recap's payment row renders the " · method"
  suffix only when the method is NOT the generic label — date-only for
  catch-all payments, while meaningful methods (e.g. "check · 1042") keep
  their suffix.
- Render smoke updated: generic row is date-only, check-method row keeps its
  suffix.
