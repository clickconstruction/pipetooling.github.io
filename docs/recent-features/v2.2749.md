# v2.2749 — Timeline opens on Working → Billed (2026-09-04)

PR 3 of the approved Timeline plan, on the owner's explicit yes: the Running = control defaults to **Working → Billed** (status spans from `job_status_events` — "open on the board") instead of first → last work (approved field days with the gap rule). Both definitions stay; only the opening state and the option order change.

## Client only — no migration

- [`JobSummaryTimelineView.tsx`](../../src/components/jobs/JobSummaryTimelineView.tsx): `definition` state initialises to `'status'`.
- [`lib/jobs/jobRunningTimeline.ts`](../../src/lib/jobs/jobRunningTimeline.ts): `JOB_RUN_DEFINITIONS` lists Working → Billed first.
- Timeline help guide marks the new default.
