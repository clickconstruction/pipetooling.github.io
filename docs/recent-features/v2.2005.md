# v2.2005 — Cover letter amount break is preview-only, after the colon (2026-08-21)

Correction to v2.2003 (owner: the break belongs after "in the amount of:", with words + figure together on the next line — and only for the estimator's on-screen preview, not the shipped document). Builders in [`coverLetter.ts`](../../src/lib/bidDocuments/coverLetter.ts) revert to the original single-line sentence (`… in the amount of: WORDS ($N)`), so clipboard copy, Print, and the approval PDF are back to pre-v2.2003 output. New pure transform `breakAmountOntoOwnLineForPreview(html)` (replace-all, so combined GC-packet documents break in every section; 3 tests) is applied only at the studio preview's `dangerouslySetInnerHTML` in [`BidsCoverLetterTab.tsx`](../../src/components/bids/BidsCoverLetterTab.tsx). Snapshots reverted to the inline form. Verified live on BP335: preview breaks after the colon; document HTML unchanged. Client-only — no migration.
