# v2.2648 — RFQ Round 2, Rung D: per-house contacts (2026-09-02)

Rung D of `docs/RFQ_ROUND2_PLAN.md` (canvas artboard 8; migration
`20260902170045`).

- **One contacts store, not two**: the v2.1605 jobs-side shortlist table
  was extended with house linkage instead of shipping a sibling table;
  estimators joined its write roles. Backfill seeded 6 real per-house
  contacts on day one (house contact fields first, then newest request).
- **Compose**: house cards are contact-chip pickers — tap cycles the
  FULL loop Off → CC → To (exactly one To; promoting demotes; tapping
  To un-sets it so a brand-new address can be To — a dead-end the live
  test caught). Free-text stays the escape hatch and offers "remember as
  <house>'s contact" (first contact becomes the default). One request /
  one link per house; CCs ride the same email (Resend cc + reply_to).
- **Everywhere else**: previews show To + CC; the desk row reads
  "Danny · danny@… (+1 cc)"; reminders and bounce-resends reuse the CC
  list; `bid_rfqs.sent_name`/`sent_cc` record exactly who was written.
- **SupplyHouseForm** gains the contacts editor (add / make default /
  archive — never delete, sent history references them).
- Live-tested: backfilled chips appeared for Ferguson/Moore/Reece; a
  custom-To self-addressed send worked after chip-off; a CC send stored
  sent_name/sent_cc and delivered (all recipients the operator's own
  addresses); test requests deleted.
