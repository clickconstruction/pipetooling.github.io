# v2.2146 — Pricing: drop the New-pill caption (2026-08-23)

Owner request: on Bids → Pricing, the text beside the {{New}} pill — "the Workbench — solver previews, Apply writes. Same data as Old." — is removed (`BidsPricingTab.tsx`). The ? tour button stays. Tour step 1 ("Scenarios — for you to compare") body is now: "Price scenarios are different prices for the same version. The ★ starred one is what the customer sees. Cover Letter, Share, Print, and the bid value all use it." The solver-preview strip no longer shows "Previewing N changed price(s) on <scenario> — nothing saved yet" (`previewCount` is still used for the gate); Apply / Discard and the amber border remain. Copy only.
