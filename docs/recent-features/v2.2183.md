# v2.2183 — Bid Board per-GC lines move into the GC cell (option A) (2026-08-23)

Owner picked option A from claude.ai/code/artifact/8e510a77-2990-4f31-b6f6-c604a746af6c ("I don't love how Southern Post / Burd look here").

- [`BidBoardGcRows.tsx`](../../src/components/bids/BidBoardGcRows.tsx): `BidBoardGcRows` (the full-width `<tr>`) is gone. `BidBoardGcLines` now renders one single-line entry per packet — name (ellipsizes) · `sent m/d` · `GcOutcomePill`; shared-letter packets read "same letter · sent m/d". New `GcOutcomePill` = the state pill (waiting / won / lost; colored when answered) that opens an inline popover with the three options (click-outside / Escape close; `role=listbox`). `fmtSentShort` exported.
- [`BidsBidBoardTab.tsx`](../../src/components/bids/BidsBidBoardTab.tsx): table GC/Builder cell renders the lines in place of the GC name button when `gcRowsWorthShowing`; cell max-width 250 (200 otherwise); the "+N GCs" chip only shows when the lines don't; the sub-row render site removed. Phone card: lines block under the project name; the GC name drops out of the meta line on those bids.
- [`BidPackageSendsDetails.tsx`](../../src/components/bids/BidPackageSendsDetails.tsx): Followup's "Sent to — by GC" uses the same pill instead of the select.
- Verified on the dev server against prod data (BP364 staged Burd won / SPC lost, then restored): desktop table row 63px tall with both lines, popover beside the pill, phone card lines.
