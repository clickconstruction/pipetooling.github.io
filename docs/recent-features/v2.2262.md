# v2.2262 — Roadmap canvas search spotlight (2026-08-25)

Searching the Map (the canvas search panel, v2.2156) now answers on the canvas
itself, not just with an outline (mockup artifact 8109112b):

- **Dim, don't hide**: stage nodes with no hit drop to `opacity: 0.3`
  (120 ms ease) but keep their position, so edges and layout stay legible
  while searching; clearing the query restores everything.
- **Substring marks**: the exact matching characters glow amber
  (`rgba(250,204,21,.45)` mark spans) inside stage titles, task titles, and
  assignee labels — in both the static and reorder task rows — replacing the
  old whole-title amber wash. The yellow card outline and the faint blue
  row tint stay.

Kernel: `splitTextForHighlight(text, normalizedQuery)` in
`src/lib/checklistTechTreeSearch.ts` (+tests) — case-insensitive, every
occurrence, original casing preserved. `GroupNodeData` gains `searchQuery`
(normalized), passed from the tab's flowNodes memo; `SearchMarkedText` in
`ChecklistTechTreeGroupNode.tsx` renders the segments. Panel behavior
(match count, Show on map pan) unchanged.
