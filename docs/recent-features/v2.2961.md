# v2.2961 — Docs drift, pass 2: the semantic layer — inventory re-measured, one access cell, one missing TOC entry (2026-09-06)

Follow-up to v2.2956 (links, paths, counts, changelog routing). This pass compared what the docs *claim* against the code on three surfaces that the first pass could not check mechanically. Docs only.

## Checked

- **`EDGE_FUNCTIONS.md` vs `supabase/functions/`** — 104 directories, 104 documented sections: complete. The table of contents listed 103; `plan-fetch` (between `drive-intake` and `ct-bridge`) had a section but no TOC line. Added.
- **`ACCESS_CONTROL.md` Page Access Matrix vs `src/lib/layoutRouteAccess.ts`** — every role's allow-list was walked against its column. One cell disagreed: the matrix said subcontractors ❌ on **Settings**, while `SUBCONTRACTOR_PATHS` has carried `/settings` and the page renders account, appearance, guides, release notes and My Reports for sub-like roles. The cell now reads ✅ limited and names the list. Every other cell matched, including the deliberate ones (estimators off People since v2.2920, primaries off Workflow since v2.2836, superintendents off Roadmap).
- **`PAGE_DECOMPOSITION_PLAYBOOK.md` inventory vs `wc -l`** — all 26 rows re-measured (the table was the 2026-07-29 sweep's numbers, flagged stale on 2026-09-05). The two that matter: `JobsStagesTab.tsx` 3,664 → 6,094 and `BidsPricingTab.tsx` 2,610 → 5,504 have roughly doubled since the sweep and are now the largest unfinished extracted tabs; `Estimates.tsx` 5,365 → 6,907 is the largest page. Shrunk: `Workflow.tsx`, `PeopleReviewTab.tsx`, `Banking.tsx`, `BidsTakeoffTab.tsx`. The stale-inventory banner is replaced by the measurement date; `to-dos/engineering-hygiene.md` item 1 is struck through and names the next two Stage-A targets.

## Not changed

The 63 backticked paths that do not exist (planned extraction targets in the architecture maps) and the architecture maps' own per-tab line ranges, which describe the July shape of each file and would need each map rewritten, not refreshed.

## Verify

`grep -c '(#plan-fetch)' docs/EDGE_FUNCTIONS.md` → 1. Impersonate a helper: the gear menu's Settings entry opens `/settings`. `wc -l src/components/jobs/JobsStagesTab.tsx` matches the playbook row.
