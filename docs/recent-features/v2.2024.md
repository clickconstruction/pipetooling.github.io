# v2.2024 — Count Sheet inline edits + page polish (2026-08-21)

Owner request ("edit every line" + whole-page polish; mockup artifact d32694ce). The Counts tab's New view ([`BidsCountsTab.tsx`](../../src/components/bids/BidsCountsTab.tsx)) gets quiet always-editable cells: uncontrolled inputs keyed by saved value (optimistic updates re-sync them), Enter/blur commits via `sheetSaveRowEdit` — the same single-row `bids_count_rows` update the Old view's editor does, optimistic + `refreshAfterCountsChange({ skipCountRows: true })`, full refresh on error — Esc reverts. Styling via a colocated `<style>` block (`.count-sheet-input` transparent until row-hover/focus, `--nopage` red dashed underline + red placeholder, `.count-sheet-trash` fades in on hover, always visible on touch via `@media (hover: none)`).

[`findDuplicateFixture`](../../src/lib/bids/countSheet.ts) gains `excludeId` (+1 test) so renames are checked against every row but their own — duplicate renames are rejected with the one-name-one-row toast and the input reverts. The plan-page column now renders in **both** modes (group headers colSpan bumped): editing a page inside By plan page hops the row to its new group live, and the "No plan page" bucket hint says to type the page right on the row. Toolbar merged onto one line (List/By-plan-page toggle, + Quick add, no-page filter note, edit hint); caption becomes "reorder still lives in Old". Help guide `count-with-the-count-sheet.md` gains "Edit any line in place". Verified live on BP298 with a temp row (add → count edit → blocked rename → page edit + group hop → delete; bid left untouched). Client-only — no migration.
