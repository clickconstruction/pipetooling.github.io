# v2.3811 — GC review: the Dashboard card counts the same GCs the modal does (2026-09-24)

Taunya, 2026-09-24: after certifying every group, the GC Review modal read *14 of 14 certified* and the Dashboard's **GC review is still due this week** card read *13 of 14 GCs certified*.

## What was wrong

The modal groups Billed rows by **who pays** (`gcThatPaysRow` → `effectiveInvoiceParty`, v2.3374 / v2.3403): J1045 *Faucet and Drains* ($712.50) names Johnny Ingram as its GC but is billed to the customer (`bill_to_party = 'customer'`), so the modal keeps it out of his group; his certification (one job, $142.50) still matched. The card reads `gc_review_week_status`, last changed in v2.2842 before the who-pays rule, which attributed every row by `gc_customer_id` alone — Johnny Ingram's live total read $855 and the latest attestation ($142.50) did not match, so 13.

## The change

- **`20260924110000_gc_review_week_status_who_pays.sql`** re-creates the RPC (v4) with the who-pays predicate on both `job_rows` branches — `public.job_bill_payer_customer_id(<invoice pick, else the job's bill_to_party>, customer_id, gc_customer_id) = gc_customer_id`, and never a bill addressed to a third-party email — mirroring the client kernel line for line. Same signature and keys. **Deploy:** `supabase db push` after merge.
- Replicating the new rule against prod rows as the dev gives Johnny Ingram $142.50 live, so the card reads 14 of 14 once pushed.

Not a cache problem: the card refetches on every Dashboard mount; it was reading a different rule.
