# v2.2335 — Invoice email payment box becomes a right-aligned card (2026-08-26)

Owner ask on v2.2324/v2.2332: the email's payment history was the last
full-width box — align it right like the PDF's and preview's cards.

- `physicalInvoiceDocument.ts` `paymentHistoryHtml`: the ledger table now
  renders inside a 340px card (1.5px ink border, heading row INSIDE the
  frame) pushed right by a two-cell wrapper table — the email-safe
  right-alignment (CSS float / margin:auto die in desktop Outlook). Both
  layouts share the function; the plain-text body is unchanged.
