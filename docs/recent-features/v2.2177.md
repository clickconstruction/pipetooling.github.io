# v2.2177 — Primary scoping 3/3: jobs + Documents; ACCESS_CONTROL corrected (2026-08-23)

Stacked on v2.2174/2175. Migration [`20260823172141_primary_scope_jobs.sql`](../migrations/20260823172141_primary_scope_jobs.md): restrictive
policies on `jobs_ledger` + 18 job children + `inspections` — a primary sees only the jobs
they are Account Man for. Reports deliberately untouched (D1; SECURITY DEFINER RPCs keep the
Reports tab and New-report picker whole). [`Documents.tsx`](../../src/pages/Documents.tsx)
Jobs tab adds the explicit `account_manager_user_id = me` filter for primaries.
`ACCESS_CONTROL.md` primary section rewritten — and corrected: the Jobs page has been
**Reports-only** for primaries in code (`Jobs.tsx` `primaryTabs`) while the doc still said
"Reports + Billing".

**Verification plan (after the three migrations are pushed):** imitate a primary account
(People → Users → "imitate (localhost)"), check Bid Board, Estimates, Jobs → Reports,
Documents; confirm a dev/master sees no change.
