# v2.2156 — Tech-tree tab sub-decomposition + the Roadmap architecture map catches up (2026-08-23)

Behavior-preserving refactor per `PAGE_DECOMPOSITION_PLAYBOOK.md` — the four sub-moves the `CHECKLIST_TABS_ARCHITECTURE.md` roadmap dossier had recommended since v2.1088, finally done after the 2026-08-20→23 feature train pushed `ChecklistTechTreeTab.tsx` to 2,825 lines. **2,825 → 1,837 lines**; every move is verbatim (git shows them as moves), no behavior change.

- **Data hook** → [`src/hooks/useChecklistTechTreeData.ts`](../../src/hooks/useChecklistTechTreeData.ts) (164): `useChecklistTechTreeData(authUserId, roadmapId, setError)`; exports `TaskView`. `blockingLoadOverlayRef` semantics kept.
- **Node cluster** → [`ChecklistTechTreeGroupNode.tsx`](../../src/components/checklist/ChecklistTechTreeGroupNode.tsx) (646): `GroupNode`, `TechTreeDndTaskRow`, `TechTreeEditableTaskTitle`, `TechTreeEmptyGroupDrop`, `TaskBridgeChipSpan`, `TaskNextUpSpan`, exported `GroupNodeData` type. The tab keeps the module-level `nodeTypes = { groupNode: GroupNode }` registry (no React Flow re-registration; the node file exports only components, so no fast-refresh warnings).
- **Canvas search** → [`ChecklistTechTreeCanvasSearchPanel.tsx`](../../src/components/checklist/ChecklistTechTreeCanvasSearchPanel.tsx) (176): `RoadmapCanvasSearchPanel`.
- **Pure helpers (Stage A)** → `getAddPrereqLinkBlockReason` into `lib/checklistTechTreeGraph.ts` (param typed structurally; +4 tests), `clientCoordsForConnectEnd` into new `lib/checklistTechTreeCanvas.ts` (+2 tests).
- One tiny real fix riding along: the `flowNodes` memo was missing `nextUpTaskIds` in its deps (v2.2138) — added.
- **Docs**: the roadmap dossier in `CHECKLIST_TABS_ARCHITECTURE.md` is rewritten for the current surface (three views, bridge, Next up / ⚡ / ★, unplanned roots, the new files, what's left — `useChecklistRoadmaps` / `useTechTreeMutations` / `useCanvasFullscreen` as optional later moves); summary table + counts + churn updated; `GLOSSARY.md` → Roadmap entry rewritten (stages/tasks/arrows/milestones/not-planned-yet/bridge/three views/Next up/★/visibility gate); `PAGE_DECOMPOSITION_PLAYBOOK.md` inventory row updated.
