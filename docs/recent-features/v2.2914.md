# v2.2914 — Copy papercut batch B7+B17: Job Summary's footer says what it hides; partner screens stop promising an acknowledgment button that's gone (2026-09-05)

Journey map Phase 4, **Tier 3 rows B7 (Job Summary) and B17 (Partner surfaces)** (`JOURNEY_MAP.md`; dossiers J6 job-health-check, J26 partner-checks-position). One PR, client only — no migration, no edge function, no telemetry. Items already shipped elsewhere are not here: chart end label (#5b/c, PR #2602), Edit Job deep-link gate (#17, PR #2630), placeholder jobs (#19 — no convention yet, Will), partner one-journal (#44, PR #2632), notes preview (decision 4).

## Item by item

| # | Finding | What changed |
|---|---|---|
| 1 | J6-7 J6-N1 (B7) | **Done — mechanism corrected.** The brief's draft wording assumed a "Finished + 90 days" default; Job Summary's hidden rows come from the **HCP # > 500 floor** (`useJobSummaryData` → `fetchJobsLedgerWithDetailsForStages({ minHcpExclusive })`), applied server-side before enrichment, so both lists the footer compared were already floored and "417 of 417" was true by construction. The fetch result now carries `hiddenByMinHcp` (rows the floor dropped — zero extra queries; the primary select already had them); the hook adds any client-side raise of the floor and exposes `jobSummaryHiddenByMinHcp`. New kernel [`jobSummaryFooterCopy.ts`](../../src/lib/jobSummaryFooterCopy.ts) (`jobSummaryFloorFooter`, 5 tests) builds the sentence: `417 shown · 398 older imported jobs (HCP # 500 and below) hidden by the default HCP # 500 floor` + a **show all** button (floor → −1, persisted); `417 jobs shown · nothing hidden by …` when the floor drops nothing; `All 815 jobs shown · no HCP # floor` at −1. Control relabelled "Hide older imported jobs with HCP # at or below [500]"; footnote "Jobs with no HCP # (or a non-numeric one) always show. Set the floor to −1 to show every job." Filter semantics unchanged. |
| 2 | J26-F1 (B17) | **Done — reworded at all three sites** (grep `acknowledg` across partner surfaces; the Statements-tab per-row chips the 2b note asked about do not exist in code — only the header comment claimed them). Deal tab row sub: `Sun–Sat, posted to the partner’s statement page (§4)` (was "mutual acknowledgment (§4, §9b)"); Statements close card: `…attaches pending offsets, and posts it to Bryan’s statement page` (was "stamps your acknowledgment" — the close still writes the company stamp, but the sentence promised a mutual step); Timeline statement rows: new `statementRowSub` in [`partnerTimeline.ts`](../../src/lib/partnerLedger/partnerTimeline.ts) → `on the partner’s statement page` for every week without a partner stamp (was "no acknowledgments yet" / "company ✓ · awaiting partner"); the one historic week keeps `acknowledged by both`. `partnershipConfig.ts` doc comment and the Statements-tab header comment updated. |
| 3 | J26-F7 (B17) | **Done.** `PartnerSummary` gains `status` (the RPC payload already returned `partnerships.status`; `parsePartnerSummary` now reads it). [`partnerStatementModel.ts`](../../src/lib/partnerLedger/partnerStatementModel.ts): `partnerSincePrefix(status)` → `draft since` only for `draft` (paused/ended keep "partner since" — the tenure is real), `partnerSinceLine(summary, cards)` prefers `started_on`, else the oldest week (+6 tests); `PartnerStatementPaper` uses it (render test added). Lens and partner page share the parser, so both read "draft since Mar 22, 2026" today. |

## Files

Kernels: `src/lib/jobSummaryFooterCopy.ts` (new, +test) · `src/lib/jobSummaryHcpFilter.ts` (exports `DEFAULT_MIN_HCP_EXCLUSIVE`) · `src/lib/fetchJobsLedgerWithDetailsForStages.ts` (`hiddenByMinHcp` on the ok result) · `src/lib/partnerLedger/partnerStatementModel.ts` (+test) · `src/lib/partnerLedger/partnerTimeline.ts` (+test) · `src/lib/partnerLedger/partnerWeeks.ts` (`PartnerSummary.status`, +test) · `src/lib/partnerLedger/partnershipConfig.ts` (comment).
Hooks/components: `src/hooks/useJobSummaryData.ts` · `src/pages/Jobs.tsx` (prop pass) · `src/components/jobs/JobsJobSummaryTab.tsx` (footer box) · `src/pages/Partnerships.tsx` (one cfgRow sub) · `src/components/partnerships/PartnershipStatementsTab.tsx` · `src/components/partner/PartnerStatementPaper.tsx` (+render test).
Docs: `JOBS_JOB_SUMMARY_TAB_ARCHITECTURE.md` (footer section). Help: `read-true-profit-on-job-summary.md` (new "Older imported jobs" section), `read-a-partners-balance-from-the-office.md` (Timeline statement rows), `see-your-partner-ledger.md` (letterhead line).

## Verify

1. Jobs → Job Summary, default floor: footer reads `417 shown · 398 older imported jobs (HCP # 500 and below) hidden by the default HCP # 500 floor — show all`; click **show all** → table reloads with every ledger job and the footer reads `All 815 jobs shown · no HCP # floor`; set the box back to 500 → the 398 return to hidden.
2. Partnerships → Deal: the Weekly statement row's sub reads "posted to the partner’s statement page (§4)"; → Statements: the close card ends "…posts it to Bryan’s statement page"; → Timeline: every statement row says "on the partner’s statement page" except the one historic "acknowledged by both".
3. Partnerships → 👁 View as Bryan (deal status `draft`): letterhead sub-line reads "draft since Mar 22, 2026 · field $… · estimating $… / h". Flip the deal to `active` on the Deal tab (then back) and it reads "partner since".
