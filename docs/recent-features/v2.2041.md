# v2.2041 — Statement job lines go trade-first (2026-08-21)

Owner request with format spec; built to the approved "Statement Job Lines" mockup (before/after + edge cases). Rows go from "Springtown- HVAC · Job 978 / full address" to **"HVAC 978 • 415 Springtown Way"** with the city on the quiet line — trade as colored TEXT (not a pill; pills read as buttons on a payment page), in the board's exact trade colors (`BID_SERVICE_TYPE_TAGS`: plum `#e17235`, elec `#EE9310`, hvac `#06b6d4`).

- **Edge** ([`portalMergedBills.ts`](../../supabase/functions/_shared/portalMergedBills.ts) + `customer-portal/index.ts`): job select gains the `service_types:service_type_id(name)` embed; `jobTradeTag` maps names → board tags; `PortalBillOut` gains `serviceTag` + bare `jobName` (+2 tests). **Requires `supabase functions deploy customer-portal`** — pre-deploy clients degrade to street-first lines with no tag.
- **Client** ([`portalPayload.ts`](../../src/lib/portal/portalPayload.ts) + [`CustomerPortal.tsx`](../../src/pages/CustomerPortal.tsx)): `PORTAL_TRADE_COLORS` (duplicated from `unifiedJobBidSearch` to keep the portal bundle lean), `splitPortalAddress` (first-comma split; comma-less stays whole; +1 test), parse of `serviceTag`/`jobName`. Fallbacks: no tag → number-only; no address → job name steps in; As-GC chip + owner line unchanged.

Client + edge redeploy — no migration.
