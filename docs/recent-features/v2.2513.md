# v2.2513 — Robot books: twins get their own takeoff/labor/price books (2026-08-30)

M5's first run showed the books matching 0/36 rows and the labor stage hard-blocked.
Owner call: robots get their own default books they can add to — the Robot Board
pattern applied to books.

- Migration `20260830180000_robot_books.sql` (see its fragment): `is_robot` flags,
  write-fence v2 (robot-book writes, robot assemblies, additive fixture_types, and
  the `cost_estimate_id` bid-child fix that unblocks the twin labor stage), and a
  seeded `🤖 Robot Default` takeoff/labor/price book per service type.
- No UI changes needed: the robot books appear in the existing book dropdowns under
  their 🤖 name.
- `docs/twins/estimator.md` §8 documents the twin-side contract (grow your own book,
  ground every entry, human books stay read-only); briefs regenerated.
