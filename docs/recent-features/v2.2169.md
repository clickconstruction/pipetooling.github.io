# v2.2169 — Vehicles fleet strip re-cut for phones (2026-08-23)

Owner screenshot (iPhone): the seven mixed-tone pills wrapped into a ragged cloud, facts beside to-dos in the same shape, two tappable chips indistinguishable from five that weren't, half-width search. Mock-up (artifact 99cea7fb) approved by the ask; built as designed.

- **Kernel [`vehicleFleetAttention.ts`](../../src/lib/vehicleFleetAttention.ts)** (+3 tests): `fleetFactsLine` (total · motor pool · $/wk ins+reg) and `buildFleetAttentionItems` (red — oil overdue, open problems — before amber — need a reading ›, not on insurance, unassigned, oil due soon, maintenance tasks ›; zero rows dropped; `action` on the two catch-up rows).
- **[`VehiclesFleetSummary.tsx`](../../src/components/people/VehiclesFleetSummary.tsx)**: facts as one muted line with the **Insurance plans ›** link riding on it (header keeps only + Add Vehicle); a **Needs attention** card (eyebrow + count; rows = tabular count in tone color, label, chevron only for tappable rows; button semantics + aria-label "8 need a reading" for the two actions); list on phones, `repeat(auto-fill, minmax(200px,1fr))` tiles from 720px; green "caught up" line when empty.
- **[`PeopleVehiclesTab.tsx`](../../src/components/people/PeopleVehiclesTab.tsx)**: strip replaced, search full width (max 440px). Cards/sections/hand-off/ledger untouched. Render test updated to the row structure (test ids + button names).
- Help: `manage-company-vehicles.md`. Verified on the dev build at 375px (four rows, search 359px wide) and 1280px (six tiles; "8 need a reading" opens the Odometer catch-up). Client-only.
