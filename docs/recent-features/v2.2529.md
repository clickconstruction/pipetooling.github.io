# v2.2529 — Sign-in headline reads ClickPlumbing.com (2026-08-31)

Owner request. `SignIn.tsx` passes `titleLinkText="ClickPlumbing.com"` (+
matching `titleLinkAriaLabel`) to `AuthPublicLandingLayout` at both call sites
(password form and the magic-link "Check your email" view). Scope is the
sign-in page only: the layout's default ("Click Plumbing") still serves
`BidRoom.tsx`, and `EstimateAccept.tsx` / `ContractAccept.tsx` keep their
existing "Click Plumbing and Electrical" overrides — customer-facing surfaces
stay on the company name. The heading's link target (clickplumbing.com) is
unchanged.
