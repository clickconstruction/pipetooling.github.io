# v2.2239 — HR Report person picker is searchable (2026-08-24)

Owner feedback on first live use of the v2.2235 HR Report card: the native
`<select>` roster was a scroll hunt.

- `DashboardPersonReportCard` now uses `SearchableSelect` with
  `searchReplacesTrigger` (the checklist task-assignment pattern) and
  `triggerMinHeightPx={0}` to match the sibling date field. Option labels keep
  the role, with underscores humanized (`master_technician` → `master
  technician`).
- Intro copy trimmed to "For anything worth remembering." (was "— good or
  bad. A dev folds it into their record.").
