# v2.2151 — GC Review × portal: the globe on the row, a portal card in the email (2026-08-23)

Owner-approved mock-up "Option A" (artifact a07c7770). Every GC already has a portal (the globe + modal, the GC-bills-only scoped view); GC Review just never mentioned it.

- **Kernel [`gcPortalLink.ts`](../../src/lib/portal/gcPortalLink.ts)** (+5 tests): `resolveGcPortalLink(customerId, links, slugs, origin)` — an active GC-scoped link (audience `gc`) wins (token URL, "GC bills only"); else the main link: the short custom address `my.clickplumbing.com/<slug>` when a slug exists, otherwise its token URL ("full account"); nothing active → null. Hook [`useGcPortalLinks`](../../src/hooks/useGcPortalLinks.ts) reads `customer_portal_links` + `customer_portal_slugs` once per open for the review's GC ids.
- **[`JobsGcReviewModal.tsx`](../../src/components/jobs/JobsGcReviewModal.tsx)**: `CustomerPortalGlobeButton` next to each GC name (GC grouping only); the per-GC **Share** menu gains a *Portal* section — **Copy portal link** with the view caption and the address (locks an unshared short address first, same rule as the globe's Copy link), or "No portal link yet — use the 🌐" when none; **Draft Message** gets an *Include portal link* checkbox (default on when a link exists; disabled under Schedule with a note that scheduled sends include it automatically) and threads `portalUrl` into preview/send; **Copy** (menu + certify checklist) passes it through `onCopyForEmail(group, groupBy, { portalUrl })` (parent in `JobsStagesTab`).
- **Email** ([`gcStatementEmail.ts`](../../src/lib/jobsDocuments/gcStatementEmail.ts)): `gcStatementPortalCardHtml` / `gcStatementPortalLineText` — a copper-ruled "Your account, any time" card under the table ("This statement stays current at …"); omitted entirely when no URL.
- **Scheduled sends** ([`gc-statement-email-dispatch`](../EDGE_FUNCTIONS.md#gc-statement-email-dispatch)): `resolveGcPortalUrl(admin, gcCustomerId)` mirrors the kernel's rules server-side; `renderGcStatementHtml/Text` take `portalUrl`; Share-all emails unchanged. **Redeploy required** (`supabase functions deploy gc-statement-email-dispatch`).
- Help: `run-your-gc-statement-round.md`, `track-a-general-contractor-on-a-job.md`, `share-a-customer-their-portal.md`.

Client + one edge redeploy; no migration.
