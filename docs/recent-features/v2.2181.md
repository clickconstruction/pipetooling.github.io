# v2.2181 — Documents restored for primaries; Documents → Jobs un-broken; migration-doc slug fix (2026-08-23)

Testing the primary-scoping train as Trace (the only primary) showed `/documents` bouncing to
`/dashboard`: PR #275 (2026-07-13) had removed `/documents` from `Layout.tsx`'s
`PRIMARY_PATHS` (`layoutRouteAccess.ts` and `ACCESS_CONTROL.md` still listed it — drift).
The owner's rule wants primaries on Documents for their Account-Man jobs, and v2.2177 scopes the
rows, so the path and the gear-menu link are restored for `primary`. **Also fixes a pre-existing bug found by the same test:** #1840 (v2.2137) had added
`.is('adopted_into_bid_id', null)` to the Documents → **Jobs** query — `jobs_ledger` has no such
column, so the tab returned a 400 that `withSupabaseRetry` swallowed and showed "No jobs in this
ledger" for **every** role since then. Removed. Also renames PR 2's
migration doc to `20260823171822_primary_scope_estimates.md` (it landed without its slug).

**Test results as Trace (REST, same session, before → after the three migrations):** bids
347 → 27 (= his estimator/AM/creator set), jobs 798 → 21 (= his Account-Man jobs), estimates
14 → 0 (created none; none on his jobs), reports 490 → 490 (D1), bid_versions 26 → 0,
bids_count_rows → only his bids'. UI: Bid Board shows only his bids (Pending 4 · Lost 3 in
Plumbing); Estimates shows the primary empty-state; Jobs `?tab=billing` rewrites to Reports,
Reports tab only, New report present and the reports list still spans all jobs.
