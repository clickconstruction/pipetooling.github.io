# v2.2300 — Quickfill "Field photos → Drive" handover (2026-08-25)

Owner-requested handover stage (long-term all customer photos live in Google Drive; Quick Estimate photos land in Supabase Storage because the field can't do Drive). Built on the v2.2298 `estimate_photo_handover` groundwork.

- Kernel [`fieldPhotoHandover.ts`](../../src/lib/quickfill/fieldPhotoHandover.ts) (+5 tests): `groupFieldPhotosByEstimate` (per-estimate groups, oldest-first, RLS-invisible estimates skipped), `handoverGroupLabel` ("CO #78 — Herber Custom Homes"), `isDriveLinkValid` (https URL).
- [`QuickfillFieldPhotoHandoverSection`](../../src/components/quickfill/QuickfillFieldPhotoHandoverSection.tsx), registered as Quickfill section `field-photos` ("Field photos → Drive", after Dispatch inbox): per-estimate cards with signed-URL photo thumbnails (open/download), a Drive-link input, and **Replace photos with this link** → upserts `estimate_photo_handover` (`moved_by` = the mover), then best-effort deletes the bucket objects + `estimate_field_photos` rows (the handover row is the source of truth; failed cleanup just leaves orphans). Cards vanish as worked; green check when empty.
- [`EstimateFieldPhotosStrip`](../../src/components/estimates/EstimateFieldPhotosStrip.tsx) now shows "📁 Field photos — moved to Google Drive" (the link) once a handover exists.
- Help guide `move-field-photos-to-google-drive.md` (Office; dev/assistant/controller).
- Types regenerated (`estimate_photo_handover`).
