# v2.2702 — Help: a recording for "get a job contract signed" (2026-09-03)

- `public/help/get-a-job-contract-signed.gif` (1.8 MB, desktop width, 7 frames): Pipeline row → the contract chip opens the Contract modal prefilled from Job 922 → **Copy link** mints the customer's signing link → the amber strip reads *Link copied … nothing emailed yet* with **Send by email** / **Text link** / **Sign in person** / **Void & redo**. Recorded in Chrome as dev; the minted link was voided right after, so the job holds only its draft.
- Wired into `src/content/help/get-a-job-contract-signed.md` under "Send a contract" via `{{gif:…}}`; `docs/HELP_MEDIA_PLAN.md` status table gains the row.
