# v2.2009 — Partner ledger card: settle-up headline only (2026-08-21)

Owner call on the partner "Your ledger" card (and its View-as-Partner lens mirror): the partner should never see the posted-only balance ($967.60 in Bryan's case) — only the settle-up position (−$1,008.13). Client-only — no migration, no RPC change.

**UI** ([`DashboardPartnerLedgerSection.tsx`](../../src/components/dashboard/DashboardPartnerLedgerSection.tsx)): the open week's headline is now `netPosition(card.closing, summary.pending_offsets.net)` — the same offset-inclusive settle-up convention the office Ledger tab has led with since v2.1939. The amber "with pending charges: …" sub-line is gone; the muted caption now reads "balance so far · includes N charge(s) waiting for the next statement · updates as hours approve" (charge count only — the number is already in the headline). With zero pending offsets the headline is unchanged (net = posted).

**Closed weeks are untouched**: navigating ‹ Older still shows that statement's closing balance with its opening chain — the paper record must keep matching the printed statement; pending charges belong only to the live week's number.

Help guide [`see-your-partner-ledger.md`](../../src/content/help/see-your-partner-ledger.md) rewritten to match (one balance, charges counted in).
