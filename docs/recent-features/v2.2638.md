# v2.2638 — Property ledger follow-ups: Job form picker + prefill wiring + county map settings (2026-09-02)

The three deferred items from v2.2614, shipped together:

- **Job form property picker** — `JobIdentityFormFields.customerAddressId`
  threaded through the identity autosave slice (`ca` in the slice json,
  `customer_address_id` in the update payload) and the create-path insert.
  `JobFormEditFactRows` gains a "Property record" fact row (customer sub-row
  family): picker over the job customer's + GC's `customer_addresses` rows,
  "matches job address" suggestion via `suggestCustomerAddressForJob`, ✓
  lien-ready state on the collapsed row. The modal loads candidates on
  customer/GC change and clears a link only after a successful load proves the
  row foreign (fail-soft on fetch errors); undo snapshot restores it.
- **External prefill county/legal wiring** — `LienToolingPrefillContext.property`
  (county + legal description from the linked row, fetched in
  `LienToolingPrefillModal`): mechanic's-lien `property-county` +
  `legal-description` and release `property-county` + `property-description`
  now fill from the property ledger instead of shipping blank.
- **Extensible county map** — `app_settings.tx_county_extra_mappings_v1`
  ("City = County" lines; extras override built-ins), hydrated in Layout beside
  the locality list, editable via the new `TxCountyMapSettingsBlock` in
  Settings → Jobs & dispatch (clone of `JobAddressCityListSettingsBlock`).
  Kernel: `parse/format/setExtraTxCountyMappings` in `txCountyLookup.ts`.

Tests: prefill property cases, county-extras parse/override/format, slice and
undo fixtures extended. No schema changes.
