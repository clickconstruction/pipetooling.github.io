# v2.1904 — Test-email toast trimmed to one line (2026-08-20)

Follow-up to v2.1891 (alert() → toasts): the test-email confirmation in [`useSettingsTemplatesEngine`](../../src/hooks/useSettingsTemplatesEngine.ts) kept the old alert's full multi-line body (subject + body preview) when it became a toast, which is too much for a toast. It now reads just `Test email sent to <name <email>>` — the subject/body preview is redundant anyway, since the test email itself lands in the recipient's inbox. Client-only — no migration.
