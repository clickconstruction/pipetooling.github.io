# v2.3320 — Bank transfer card: "ACH (direct deposit) • wire • check", one rule per row, the account kind beside the number (2026-09-11)

Client only; owner wording after the v2.3308 review screenshots. In [`PortalBankTransferCard`](../../src/components/portal/PortalBankTransferCard.tsx) the collapsed card's right-hand hint is now **ACH (direct deposit) • wire • check** (the bullet the statement's job lines already use), and the opened card's first line reads "ACH (direct deposit) and domestic wire use the same details." No kernel change; the render smoke passes unchanged.

**Row rules (same review):** the label, value and Copy cells each carried their own bottom border, so a two-line value (the account kind under the number, the bank note under the bank) left the label's underline higher than the value's — a rule that "deviates across the page". Each `Row` is now its own three-column grid (`84px · 1fr · auto`, the ledger's label width) that owns one `borderBottom`; the cells carry none. The account kind renders inline to the right of the number (`2025 1122 6605 · Business checking`), and the bank note leaves the grid to sit beneath it as a muted sentence. The owner changed the stored kind to "Business checking" at Settings → Company.
