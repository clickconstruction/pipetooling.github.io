# v2.2555 — twin-census T3 template matcher (2026-08-31)

Census Toolkit Plan tool T3 — the Class-C (full-raster scan) counter and
the last of the four census tools. `scripts/twin-census/template_match.py`:
pure-numpy FFT normalized cross-correlation with peak NMS and a circled
verification image; harvest a template from one confirmed symbol instance,
match, verify, count. No OpenCV dependency.

Validated on the exact sheet behind BT-14's count misses (Proud Mary K105
below-slab plumbing): 9/9 "FS" floor-sink callouts found in 5.6s with
clean threshold separation from lookalike bubbles (K30/K38A). Combined
with the TP-per-trap doctrine (9 FS + 1 FD = 10 trap primers), the whole
BT-14 quantity miss is now machine-covered. Toolkit status: T1 vector
census, T2 path census, T3 template match, T4 auto-scorecard — all four
shipped.
