# v2.2206 — Bids pricing: drop the "Healthy spread." verdict (2026-08-23)

Owner feedback on the Pricing tab's **Where the profit lives** card
([BidsPricingTab.tsx](../../src/components/bids/BidsPricingTab.tsx)): removed the green
"Healthy spread." span — when concentration is fine the card now shows just the title, the
profit bar, and the per-row share line. The amber ⚠ warning (top-2 rows > 60% of profit,
kernel `profitConcentration`) is unchanged, as is the "No profit yet — price some rows."
empty state.
