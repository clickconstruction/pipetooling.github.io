# v2.3070 — One company: the function sweep (2026-09-07)

The last maintenance item from [`docs/ONE_COMPANY_PLAN.md`](../ONE_COMPANY_PLAN.md). One migration, `20260907160000_one_company_function_sweep.sql` ([doc](../migrations/20260907160000_one_company_function_sweep.md)) — pushed by the agent after merge.

- New `is_office_or_estimator()`; 32 RPC gates rewritten on their **live** definitions (five canonical `EXISTS` forms → `is_office_or_estimator()` / `is_master_or_dev()`), 2 partially, 11 deliberately untouched (they test a user other than the caller and keep reading the views).
- **Found on the way**: `merge_user_accounts` would have failed since Phase 5 — it `UPDATE`s / `DELETE`s through a list of grant tables, and two of them are views now. The two tuples are removed; merging duplicate accounts works again.
- Plan doc: the sweep is recorded; nothing is left on the plan.
