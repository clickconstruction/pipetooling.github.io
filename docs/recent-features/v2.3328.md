# v2.3328 — Test reports PR 12: the help guide walks the flow end to end (2026-09-11)

Docs only: `src/content/help/file-a-test-report.md`. No code, no migration, no deploy. Plan: [`to-dos/test-reports/README.md`](../../to-dos/test-reports/README.md).

Owner, 2026-09-11: "Is there a guide on the app of how this works and how to use it? if not, please help me make one." The guide has shipped with the feature since PR 2 (v2.3298) and grown with every PR, but it read as a reference of the modal, not as an explanation of the process that replaced Taunya's Gmail + Stripe routine.

- New **How it works at a glance** section under the intro — four numbered steps: the clock-out Status Report drafts the report (v2.3303), the Dashboard's Needs you card counts it (v2.3301), the office glances and sends (v2.3301/3309), the GC sees it on the portal (v2.3304/3312). The rest of the guide is introduced as the detail of those steps.
- **Save, download, send** now states that the Send button is in the modal's bottom bar and gray until the report can go (v2.3326's fix for "it won't let me resend"), and that sending again to a different address is Send again → change To.
- **Fill in the test** names where the date prints per type (v2.3324's pinpoint move).
- Keywords gain "test reports", "send test report", "resend" so the Help search finds it from the words the office actually uses.

Verify: `npx vitest run src/lib/helpGuideContent.test.ts src/lib/releaseNotes.test.ts`; open **Help → Guides → file a hydrostatic, pinpoint or gas test report** (`/help?g=file-a-test-report`) and read the new section with its chips rendered.
