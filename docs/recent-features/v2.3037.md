# v2.3037 — twin_shadow_runs.status admits 'void' (2026-09-07)

Follow-through on v2.3032's `void_shadow`: its first real use (b480, the plumbing shadow opened on Electrical-division b378) failed on `twin_shadow_runs_status_check`, which only knew open/locked/scored. Migration `20260907110000_shadow_void_status.sql` widens the CHECK (drop-if-exists + re-add) and documents the column. No edge change — the verb already writes 'void'. **Push after merge, then void b480.**
