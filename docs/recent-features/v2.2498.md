# v2.2498 — Help guide sender-address correction (2026-08-29)

Post-Resend-migration sweep found one stale user-facing fact: the
`track-a-general-contractor-on-a-job` help guide named
`team@noreply.pipetooling.com` as the statement sender (two mentions) —
emails send from `team@noreply.clicktooling.com` since v2.2496. Both
mentions corrected. Found by the full-repo "any pipetooling left?" audit;
everything else remaining is internal identifiers (Stripe metadata keys,
`pipetooling-pins-changed` event, `pipetooling_` localStorage prefix, twin
fleet emails), deliberate fallbacks (APP_ORIGIN/EMAIL_FROM, auth
allowlist), or the runbook's listed cosmetic items (twin docs, UA string,
VAPID subject).
