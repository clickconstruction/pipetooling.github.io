# v2.1956 — Partner ledger: pending tag removed (2026-08-21)

Owner follow-up to v2.1951/v2.1953: the amber "pending" chip on inline ledger rows comes off in [`PartnershipLedgerTab.tsx`](../../src/components/partnerships/PartnershipLedgerTab.tsx). A not-yet-attached charge now renders as an ordinary journal row whose balance cell is "—" (muted date kept); the footer sentence carries the convention ("rows with a '—' balance are charges waiting for the next statement"). The v2.1951 date-interleave and `mergePendingIntoJournal` kernel are unchanged. Client-only — no migration.
