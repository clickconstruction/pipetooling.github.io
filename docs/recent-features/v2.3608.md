# v2.3608 — View as, PR 2: the gear-menu door — a role or a person, from any page (2026-09-19)

PR 2 of two for the View-as to-do (closed by this PR; PR 1 was v2.3606). Client only.

## The change

- **Gear menu → View as…** ([`Layout.tsx`](../../src/components/Layout.tsx); dev only, not while already imitating, not in Farm mode) opens [`ViewAsPanel`](../../src/components/layout/ViewAsPanel.tsx): the **Roles** list — every imitable role in `VIEW_AS_ROLE_ORDER`, backed by its sample account (`is_sample` rows read directly, since `activeUsersQuery` hides them) with `switchChipsFor` chips (*training mode · Hiring · estimator prospects*), or *no sample yet* pointing at Active accounts; and the **People** list — `activeUsersQuery` (no devs, no twins, no samples) with `filterViewAsPeople` (name, email or role, every word). Picking either calls `loginAsUser` — the v2.3606 landing on the current page, the amber Exit back to it.
- **Kernel** [`lib/viewAs.ts`](../../src/lib/viewAs.ts) gains the order, the chips and the search (tests); the panel has a render smoke (eight roles, *no sample yet*, the search, Close).
- **Guide** [`see-the-app-as-someone-else`](../../src/content/help/see-the-app-as-someone-else.md) (dev): the door, the two lists, coming back, the samples and their switches, the Hiring-pill example. `ACCESS_CONTROL` § Sample accounts and the glossary name the door.
- The to-do folder `to-dos/view-as/` is deleted; the board regenerated.

## Verified

- Live on prod through the dev server: gear → View as… listed the eight roles with their chips and the people search; *Assistant* from Prospects landed on Prospects as *Sample assistant* (the route guard sent the assistant to Follow Up — the answer); Exit returned to the page as the dev. A non-dev session has no entry.
