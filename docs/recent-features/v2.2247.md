# v2.2247 — fix(dashboard): AR/Not-billed line items expanded by default (2026-08-24)

The AR drill-down's per-row "N line items ▾" chip (v2.1595 "Variant B") made
every row a click, and once expanded the chip row just ate vertical space.
Requested flip in `DashboardFinancialsSection`:

- **Line items start expanded** for the AR and Not Billed Out modals. Expanded
  rows render the lines directly with a small **▾ chevron left of the first
  line** that collapses the stack; no chip row above them.
- **Collapsed rows** show the original compact chip, now reading
  "N line items ▸", which re-expands.
- State: new `collapsedArLineKeys` set (default empty = all expanded), separate
  from `expandedLineKeys`, which the AP modal's payroll person-groups keep —
  those still start collapsed.
- Both consumers of the shared `arLineItemsBlock` (phone sheet + desktop
  table) pick the change up together.
