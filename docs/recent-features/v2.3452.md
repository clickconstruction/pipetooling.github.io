# v2.3452 — Owner of record: the c/o line only when it adds a name; the to-do closes after the live pass (2026-09-15)

Client only; no migration, no edge function. Found in the 2026-09-15 live pass of v2.3452.

## Why

Several appraisal districts echo the owner into the roll's "name care" field, so the job form's Found box and the Fix-ups list printed *Khan Umar & Bangash Shazmeena · Khan Umar & Bangash Shazmeena*.

## What shipped

- `careOfLine(parcel)` in [`ownerConfirm.ts`](../../src/lib/jobs/ownerConfirm.ts) (+1 test): the c/o beside the owner only when, normalized, it says something the owner name does not (Sabra's *% Sabra Health Care REIT Inc* stays; an echo drops). `JobFormOwnerLookupBox` and `OwnerConfirmListModal` read it.
- `to-dos/owner-of-record-prompts/` deleted — PRs 1–3 (v2.3447, v2.3452, v2.3450) plus the two live-pass fixes (v2.3455, v2.3452) are on main and were live-tested on real jobs the same night; the index's Closed paragraph carries the record and the one residual (Bill Customer cannot open on a job with no customer row). `gc-on-notice/` unblocked.

## Verified

Kernel tests, the two render smokes, typecheck, lint. Live: J273's Found box read the owner once after the change.
