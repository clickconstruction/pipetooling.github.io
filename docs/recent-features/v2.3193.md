# v2.3193 — Job Mode card: manual Clock In in clock-in orange (2026-09-09)

Client only. Owner, after v2.3190 made the button work: "should I make the button orange like the clock in button elsewhere in the app?" Yes — orange is the app's clock-in accent (the visible Clock In button, the Clock In modal's top stripe and focus rings), and the card was the one place a Clock In was green, because it inherited the advance buttons' style.

## What shipped

- **[`lib/clock/clockColors.ts`](../../src/lib/clock/clockColors.ts)**: `CLOCK_IN_ACCENT_ORANGE` (`#ff6600`) moves out of `ClockInOutButton` so both surfaces read one constant.
- **[`DashboardJobModeCard`](../../src/components/jobMode/DashboardJobModeCard.tsx)**: a `manualClockInBtn` style — the big-button base in the clock-in orange — used only when the right button is `manual-clock-in`. Start First Job / Next Job / Switch to Scheduled Job keep green (`nextJobBtn`), Wrap Up Day keeps red, Last job of the day keeps the disabled grey. Smoke: with no blocks and no session the right button reads **Clock In** and carries the orange.
