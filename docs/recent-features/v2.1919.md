# v2.1919 — Email dispatcher crons staggered onto minute lanes (2026-08-20)

Part 2 of the OOM-crash-loop remediation (part 1: v2.1917). Migration
[`20260821010000_stagger_email_dispatch_crons.sql`](../migrations/20260821010000_stagger_email_dispatch_crons.md)
moves the five email dispatchers off the shared `:00/:05` pg_cron tick:
billed-report `:01`, gc-statement `:02`, weekly-movement `:03`, weekly-money
`:04` (5-min cadence each), paid-job `:07/:22/:37/:52`. `sync-salary-sessions`
keeps `:00`. Ends the five-simultaneous-`net.http_post` volley whose edge
functions queried back in the same second — several Aug 19–20 crashes landed
right after it. Docs: `EDGE_FUNCTIONS.md` cron lines updated (the three newer
dispatcher sections gained explicit **Cron** lines), `REPORT_SUBSCRIPTIONS.md`
now tells new streams to pick a free minute lane and records that no delivery
SLA is tighter than the cron tick. DB-only — no client change; apply with
`supabase db push` after merge.
