# v2.1932 — Header stats force-refresh on every loadJobs (2026-08-20)

Owner-reported v2.1917 regression: the header-stats TTL (60s) only gets bypassed by `scheduleLoadJobsAfterMutation()` (plain `updateJobStatus` moves), but the money movers — Bill Customer, Mark Paid (job + invoice), the Collections flag, send-backs, partial-invoice creation, Edit Job saves — refresh via bare `loadJobs()`, whose piggybacked stats refresh is non-forced and TTL-swallowed. Rows moved instantly; section headers ("Billed Awaiting Payment (N) - $X"), money story cards, and aging chips kept pre-move numbers for up to 60s. Fix: the `loadJobs` wrapper in [`Jobs.tsx`](../../src/pages/Jobs.tsx) now force-refreshes `refreshHeaderStats` after its fetch — one funnel point covering all ten mutation call sites; the TTL keeps deduping the visibility/scoped-load piggybacks it was built for (non-Stages tab-entry loads pick up one extra lean stats fetch alongside their full board fetch — negligible). Verified live via in-page fetch instrumentation: two lean stats fetches 17s apart across in-app tab switches, where the TTL previously skipped the second. Client-only — no migration.
