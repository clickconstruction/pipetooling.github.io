# v2.2641 — Lien releases: awaiting-signature rows resume in the modal (2026-09-02)

Live-test finding minutes after the v2.2619–v2.2621 signing loop merged: the modal resumed only `draft` rows, so once you closed it with a request out, the amber strip (Cancel request / Sign now) was unreachable — the request could only be voided.

- `src/components/jobs/LienReleaseModal.tsx` — the resume effect now adopts the newest live `awaiting_signature` row when no draft exists. Fields stay locked (editable = drafts only); the strip, footer latches, and cancel/sign affordances all come back. Signed/sent/issued rows still don't resume — the modal is then for the next release, with those in the history box.
