# v2.2478 — Bid room page: readable text on every element (2026-08-29)

Found by the owner's screenshot review pass: the room page renders inside the auth
landing shell, whose base text color is light-on-photo — every element without its own
color token (the title, option names and prices, section totals, the CO card heading)
inherited near-white and washed out against the white card. One-line fix: the pinned-light
wrapper now sets an explicit `var(--text-strong)` base. (main was at v2.2476/77 when this
was claimed; the intervening number belongs to a parallel branch.)
