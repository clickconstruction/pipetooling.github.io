# v2.2087 — Roadmap task context modal (2026-08-22)

Owner request on Review → Outstanding by person: tap a task's ⛰ roadmap chip → modal showing where the task fits in the greater roadmap (mockup artifact e554ba85, owner-approved shape). New pure kernel [`roadmapTaskContext.ts`](../../src/lib/roadmapTaskContext.ts) (`buildRoadmapTaskContext`, 4 tests): reuses `goalsStageRows` for stage states so the modal and Goals strip can't disagree; derives stage/task numbers (`7.1`), the focus stage's ordered task list, downstream unlocks from edges, and `blockedBy` for locked stages. New [`RoadmapTaskContextModal`](../../src/components/checklist/RoadmapTaskContextModal.tsx): fetches its own graph on open (task → groups → edges + tasks), renders the Goals-strip segmented bar with a blue ring + ▼ pin on the focus stage, a legend, the stage box with the clicked task highlighted, unlock/waiting-on lines, and "Open full roadmap →" (reuses the tab's `onOpenRoadmap` deep-link). `roadmapGoalChip` gained an optional `onOpen` and renders as a button on Review rows (threaded through `OutstandingByPersonSortableList`/`Row`); other chip sites stay static. Help guide `see-where-a-task-fits-in-its-roadmap.md` added; stale "delete on the left" line in `see-a-tasks-full-history-and-notes.md` fixed (v2.2086 moved it right). Client-only — no migration.
