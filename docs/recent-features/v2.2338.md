# v2.2338 — Payment card restyle: medium-gray rounded frame, no inner lines (2026-08-26)

Owner style pick after two mockup rounds ("a little more than frame one, not
as much as frame two"): frame = 1px #9ca3af rounded; interior rows separator-
free; the only other rule is the one above Balance due (same gray).

- `physicalInvoiceDocument.ts` `paymentHistoryHtml`: card border 1.5px
  #111827 → 1px #9ca3af; row `border-bottom`s removed; padding widened (rows
  4px 12px, heading 10px 12px 7px). The card table uses
  `border-collapse:separate` — collapsed borders make browsers/mail clients
  drop the border-radius (square corners). The balance rule is an inset
  bordered-div row (stops short of the card edges) instead of cell
  border-tops.
- `physicalInvoicePdf.ts` `drawPaymentHistory`: `rect` → `roundedRect`
  (2.5mm corners), stroke `PAYMENT_BOX_GRAY` (156,163,175) at 0.35mm, the
  balance rule uses the same gray, paddings bumped (PAD_X 5, PAD_TOP 6,
  PAD_BOTTOM 3.5, HEAD_GAP 6.5). Atomic page-fit behavior unchanged.
- Preview and portal untouched (preview is the reference; the portal keeps
  its ruled-paper identity).
