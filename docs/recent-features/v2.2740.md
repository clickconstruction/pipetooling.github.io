# v2.2740 — Add checklist item modal: header Send once typing starts (2026-09-03)

Owner ask: "Once someone starts typing, I would like a send button to appear at the header that they can click to send right away. It's the same as save at the bottom. I would like the word save at the bottom to become the word send."

- [`ChecklistAddModal.tsx`](../../src/components/ChecklistAddModal.tsx): the header row (title + "Go to checklist" icon) gains a right-side group with a pill **Send** button rendered only while `form.title.trim()` is non-empty; it calls the same `saveItem` as the footer, disables while `submitting`, and reads "Sending…" then. Footer button relabeled Save → **Send** / Sending…. The Tab cycle (title → people search → Send → Cancel) is unchanged — the header button is mouse/tap-reachable like the other extras.
- Render test: header Send absent on open, present after typing, absent again for a whitespace-only title; no "Save" button anywhere.
- Help guide `give-someone-a-task.md`: Save → Send in all three places, plus a sentence about the header button.

Client only.
