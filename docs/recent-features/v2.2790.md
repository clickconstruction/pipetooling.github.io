# v2.2790 — Sub work orders from sheets, PR 5: the Subs row and the Person tab (2026-09-04)

Last PR of the Sub Work Orders train (v2.2785 schema · v2.2786 sheet box · v2.2787 Scope tab · v2.2789 portal). Where a work order shows up afterwards.

- **People → Subs**: `buildSubsHqRows` takes `sheetLabel` on commitments and an optional `generalConditions` Book entry; rows gain `generalConditions: current | behind | unsigned | none` (best signed `person_contract_documents` copy by applied Book entry, then by name; `generalConditionsStanding`). The tab's commitments select adds `labor_job_id` + `offer_scope_snapshot`; step names are looked up only for step rows; sheet rows read the snapshot's sheet label (fallback: the sheet's `job_number · address`). Docs select adds `applied_contract_template_document_id` + `applied_version_date`; the sub-audience Book document is fetched once. Render: sheet work orders list as `J977 · 415 Springtown Way · sheet`; a **Gen. Cond.** pill (✓ / behind / unsigned) joins the compliance pills whenever the Book has a sub document. Tested in `subsHqRows.test.ts`.
- **Person Desk**: new [`PersonDeskWorkOrdersSection`](../../src/components/personDesk/sections/PersonDeskWorkOrdersSection.tsx) mounted for subs right after Portal & paperwork — every non-cancelled `step_commitments` row for the person (newest first, 50), labelled by step @ project or by sheet, with a status chip (Draft / Awaiting answer · sent … / Signed … / Accepted … / Settled … / Declined … + reason) and a door: `/workflows/<project>#step-<id>` or `/jobs?tab=sub_sheet_ledger&editLabor=<job number>`.
- Help: *review your subs in one place* gains a "Sheet work orders and General Conditions" section; *manage subs end to end* mentions the pill.

No migration, no edge functions.
