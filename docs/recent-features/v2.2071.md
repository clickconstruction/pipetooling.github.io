# v2.2071 — Goals strip: "stage N of M" label removed (2026-08-22)

Owner: "why does it say stage 2 of 24?" The header computed
`stagesComplete + 1` — a linear-progression fiction. Farm 1's one complete
stage is #20 (out of order) with a 17-stage parallel front, so "stage 2"
corresponded to nothing real; the v2.2050 numbered zones made the mismatch
visible. The label span is deleted (chevron stays); the segmented bar and
the tap-open stage ledger carry the truth. `GoalsStripRow.stagesComplete`
remains computed (used elsewhere in the ledger).
