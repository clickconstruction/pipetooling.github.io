# v2.2142 — Edit Sub Labor gets New's job search (2026-08-23)

Owner ask (mockup approved as built: https://claude.ai/code/artifact/d33a4a0d-df82-4518-b655-34984f3fc1b5): New Sub Labor (v2.1616–v2.1631) got the app-standard job picker, crew chips and the three-step rhythm; Edit still opened the classic form with a free-text **Job #** box and a typed Address — the one field that links the sheet to every rollup was the one you couldn't search.

[`JobsSubLaborFormModal.tsx`](../../src/components/jobs/JobsSubLaborFormModal.tsx):
- **Job field in both modes.** The picker button renders in Edit too (star only in New — Edit keeps "job optional" for old sheets). `openEditLaborJob` resolves the stored `job_number` via `resolveSubLaborJobByNumber(jobs, …)` → `laborPickedJobId`; an effect re-resolves once the jobs cache lands (a `?editLabor=` deep link straight to the tab). Resolving sets only the pick — the sheet's own address stays until the user picks a different job. Three states: **linked** (`J977 · name` + address subline, "change"), **typed but unmatched** (amber border, `#H-2291 No job with this number`, "Keeps the typed number until you link one", "link"), **empty** (placeholder + ⌕).
- **Address box only while unlinked** (`editingLaborJob && !laborPickedJobId`); the edit-mode Job # `<input>` is gone. Picking in Edit passes `keepAssigned: true` — the crew is never rewritten (decision D2). Storage unchanged (`people_labor_jobs.job_number` + `address` text), so every rollup that matches by number keeps working; no migration.
- **Header**: the "Required: …" paragraph is replaced by `editSummary` — contractor · $total · $due (same math as the Payments block); the Save button already spells out what's missing.
- **Copy**: "Subcontractors" → "Crew", "+ Add New Sub" → "+ Add Sub". **Footer**: Delete moves first, quiet (transparent, red text, `marginRight: auto`); Cancel · Print · Save stay right.
- **Picker copy**: [`ScheduleDispatchAssignJobPickerModal`](../../src/components/schedule/ScheduleDispatchAssignJobPickerModal.tsx) gains an optional `title` prop (default unchanged "Add job to schedule"); Sub Labor passes "Which job is this sub labor for?" + a subtitle and the New placeholder. Schedule / Quickfill / Dispatch / User Review callers untouched.
- Render test updated (typed-unmatched + linked states); help guide `record-sub-labor-on-a-job.md` "Editing an entry" rewritten.

Client-only.
