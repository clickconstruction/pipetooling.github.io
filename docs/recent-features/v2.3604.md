# v2.3604 — Supply houses: the summary table sorts by any column, Last Paid included (2026-09-18)

The May follow-ups from [`to-dos/supply-house-job-account-aging.md`](../../to-dos/supply-house-job-account-aging.md) item 3. Client only.

- **Last Paid sortable** — and every other summary column with it. Kernel [`lib/materials/supplyHouseSummarySort.ts`](../../src/lib/materials/supplyHouseSummarySort.ts) (6 tests): `sortSupplyHouseSummary` (nulls last in both directions, ties by name), `nextSupplyHouseSummarySort` (the same header flips; another header starts in its natural direction — names A→Z, money and dates biggest / newest first), `parseSupplyHouseSummarySort` / `serializeSupplyHouseSummarySort` (the remembered pick, `supplyHouses.accountsPayable.summary-sort` beside the tab's other remembered toggles), `ariaSortFor`. In [`SupplyHousesTab.tsx`](../../src/components/SupplyHousesTab.tsx) each `<th>` carries `aria-sort` and a button with a ▲ ▼ ⇅ mark; the rows render from `sortedSupplyHouseSummary`. The loader's own outstanding-first order is the default, so a device that never clicks sees the table it always saw.
- **The editable Paid On date** the to-do listed as unbuilt was already there: the Edit invoice modal's Status row carries a *Paid on* date beside the Paid radio (`paidAtPayload` writes `paid_at` only when the day changed). Nothing to build; the to-do is corrected.

Verified on prod through the dev server: the summary re-orders on each header, Last Paid newest first then the never-paid houses, the mark follows the pick, and a reload keeps it.
