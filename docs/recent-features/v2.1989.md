# v2.1989 — Checklist review dismiss rail (2026-08-21)

User request: match the Dispatch inbox's left-hand Dismiss for easy
clearing.

- `ChecklistReviewInboxSection` rows become flex rows with a full-height
  green ✓ Dismiss rail on the left (88px, `--bg-green-tint` /
  `--text-green-800`, same styling as `DispatchInboxSection`'s rail) that
  calls the existing `dismiss(row)` (stamps `reviewed_at`, logs 'accepted').
- Expanded panel unchanged (history + Dismiss + Reopen-with-comment); guide
  `review-my-teams-completed-tasks` updated to lead with the rail.
