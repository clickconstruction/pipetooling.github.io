# v2.2553 — Audit cockpit v2: name-matched diff, one-tap verdicts, sealed-shadow hold (2026-09-01)

Second pass on the audit cockpit (v2.2549 was pass one). The card stops showing "the robot's biggest rows" and starts showing an actual **diff** between the twin's draft and the reference bid.

- **`src/lib/bids/takeoffDiff.ts`** (+13 tests): pure kernel that parses count-row names from BOTH naming styles (`ft of 3/4" Cold Water` ≡ `3/4IN WATER`, incl. `11/2IN` = 1-1/2" and `MD` = med-gas shorthand) into signatures (kind + size + system / fitting type / fixture alias), joins the two takeoffs, and emits three buckets ranked by dollars: **missed** (our rows the robot lacks), **added** (robot-only rows), **gaps** (matched, quantities disagree beyond ±15%). Fittings match on size + type only (robot names them bare, human rows carry the system). Also `rollupSystems` (per-system ft + fixture ea, robot vs ours) and `buildVerdictDraft`/`entrySection` for verdict notes.
- **BidsAuditsTab** rework: system scoreboard table with ratio column → three diff buckets with one-tap verdicts (`✗ Robot's wrong` opens an editable drafted note tagged `[verdict:teach]`; `📋 Our record's off` → `[verdict:record]`; `✓ Both fine` posts `[verdict:ok]` immediately) → questions → single composer. Small rows collapse under a $1,500 floor (top 3 always shown, 8 max per bucket). The v2.2548 biggest-rows judge list remains as the fallback when there's no reference takeoff to diff against. New card lead: the robot's `self_assessment` ("🤖 Where I'm least sure"). New tab lead: a coaching-record strip (notes → digest outcomes + recent run deltas).
- **Sealed-shadow hold**: a pending audit whose reference bid hasn't been sent renders only as a 🔒 row ("opens after we send bNNN"), can't expand, and is excluded from the pending count (`useBidAuditsPendingCount` now joins through `twin_source_bid_id`). Rationale: pre-send, even the robot's takeoff *rows* could anchor the estimator — the earlier seal only hid the total.
- **Migration `20260901070000_bid_audits_self_assessment.sql`**: nullable `bid_audits.self_assessment`; twin-mcp `ct_finish_takeoff` (v1.3.3) gains a `self_assessment` param that stamps it (insert or refresh on re-finish).
- Verdict tag contract for the digest documented in `docs/twins/FEEDBACK_LOOP.md`: `[verdict:teach]` → doctrine/books, `[verdict:record]` → reference_quality repair, `[verdict:ok]` → bid_only.

Verified live against b405 vs b376 ($244,665 vs $346,881): med-gas rollup 0.56×, MISSED shrank 19→8 as the shorthand fixes landed, verdict tap drafts "[verdict:teach] ft of 1 1/2IN WASTE — robot missed this (ours ×270.1, $33,426)."
