# v2.2820 — Job Summary: Cut by + $/field hr (2026-09-05)

PR 2 of the Job Summary analysis plan (artifact 9c02d29a), stacked on v2.2817 (Compare to + Target).

## Client only — no migration

- [`jobSummaryLedgerView.ts`](../../src/lib/jobs/jobSummaryLedgerView.ts): pref `cutBy: 'none' | 'gc' | 'trade' | 'tech' | 'accountManager' | 'customer' | 'development' | 'billMonth'` + `JOB_SUMMARY_CUT_OPTIONS`. `JobSummaryLedgerRowInput.job` grows optional cut keys (`gc_customer_id` / `gcCustomer`, `service_type_id` / `serviceType`, `master_user_id`, `account_manager_user_id` / `account_manager`, `customer_id` / `customer_name`, `development_id` / `development`, `last_bill_date`) — all already on `JobWithDetails` from the ledger list embed. `jobSummaryCutKey(job, cutBy, ctx)` → `{ key, label }` with a "none" bucket per cut ("No GC (direct)", "Not billed yet"…); the lead-tech cut resolves `master_user_id` through `ctx.userNameById` (the page's users list). `groupJobSummaryRows(rows, cutBy, ctx)` → `JobSummaryGroup[]` (rows + `summarizeJobSummaryRows` subtotal) ranked by true profit (unknown last, then revenue); `'none'` → `[]`. `jobSummaryConcentration(groups, top = 3)` → the top groups' share of positive true profit (null with ≤ 3 groups). Rows and totals carry `revenuePerHourUsd`; sort key `revPerHour`. Four new tests.
- [`useJobSummaryView.ts`](../../src/hooks/useJobSummaryView.ts): new arg `userNameById`; bundle gains `groups` + `concentration`; the compare bundle gains `trueMarginPctByGroupKey` (the compare window grouped the same way) so subtotal rows can show Δ margin.
- [`JobSummaryCutByPanel.tsx`](../../src/components/jobs/JobSummaryCutByPanel.tsx) (new): the ranked bars (true profit green / loss red, revenue when money is hidden; label = $k · margin · jobs; red tick + label for groups under target; "top 3 = N% of true profit"), top 12 groups shown. Exports `JobSummaryGroupRow` — the subtotal row (15 columns, bold, subtle ground, Δ margin under True % when Compare to is on, red ▾ when under target).
- [`JobsJobSummaryTab.tsx`](../../src/components/jobs/JobsJobSummaryTab.tsx): the body now iterates `view.groups` (or one unlabeled group holding `view.rows`) and inserts a `JobSummaryGroupRow` above each; the panel renders above the table; new **$/hr** column after True % (header, cell, footer); detail-row `colSpan` 14 → 15. Nothing inside the expanded row moved.
- [`JobSummaryLedgerToolbar.tsx`](../../src/components/jobs/JobSummaryLedgerToolbar.tsx): **Cut by** segmented control (Jobs view). [`Jobs.tsx`](../../src/pages/Jobs.tsx): builds `userNameById` from `users` and passes it to the hook.
- Help guide "read true profit on Job Summary": new **Cut by** section + the $/hr column.
