# v2.2352 — Invoice payment card: air + no Billed echo (2026-08-26)

Owner Option B after a before/after mockup: the card looked scrunched next
to the portal's, and its Billed line duplicated the Amount due sitting right
above it on invoices.

- Email (`physicalInvoiceDocument.ts` `paymentHistoryHtml`): Billed row
  removed; portal proportions — rows 5.5px 16px, heading 13px 16px 9px,
  rule row 9px air, balance row 9px 16px 13px at 14px.
- PDF (`physicalInvoicePdf.ts` `drawPaymentHistory`): Billed row removed;
  rowH = bodyLineHeight × 1.15, PAD_X 5.5 / PAD_TOP 7 / HEAD_GAP 8 /
  PAD_BOTTOM 4.5, +2mm `PAYMENT_BOX_RULE_GAP_MM` before the closing rule.
  Atomic page-fit updated for the new geometry.
- Preview (`PhysicalInvoicePreview.tsx`): same spec — Billed row removed,
  card padding 0.85/1rem, rows 0.32rem rhythm, balance row larger with air.
- `billedFormatted` stays on `PhysicalInvoicePaymentTotals` — the portal
  recap still uses the concept (its own kernel) and the field is harmless.
- Deliberate divergence: the PORTAL recap keeps its Billed line — nothing
  above that card states the job total.
