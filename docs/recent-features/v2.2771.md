# v2.2771 — Statement round: Dashboard row, morning email stream, send from the app (2026-09-04)

Owner ask after walking the round: "when it says in Malachi's round, what is the prompt in his environment to send it?" — the only prompt was the Pipeline money-opportunity card, which nobody sees unless they open the board. "Build those three improvements, regarding the email in the GC Review I would like to add an email subscription like there is in settings Emails & reports."

**DB** (migration `20260904201238_statement_round_email_stream.sql`, applied via db push while this PR merged; [migration doc](../migrations/20260904201238_statement_round_email_stream.md)): `row_key` on the statement payload rows; `get_statement_round_for_user(p_user_id)` (service-role, server-side `buildStatementRound` — snapshot-diff certification, marks, standing sender → Account Man fallback) + self-scoped `get_my_statement_round()`; `statement_round_email_requests` + pg_cron (:02 lane) + `statement_round` branches on both schedule RPCs. Types regenerated.

**1. Dashboard Needs You row** ([`dashboardNeedsYou.ts`](../../src/lib/dashboardNeedsYou.ts) `statement-round`, rank 30, blue): "2 GCs are waiting on your statement — RMC- Dudley Mason, Knight Contracting · $96,257 certified and ready — a personal email from you." **Start round** → `/jobs?tab=stages&round=1`. Hook [`useStatementRoundNudge`](../../src/hooks/useStatementRoundNudge.ts) (one RPC); wired in [`DashboardPinnedQuickRow.tsx`](../../src/components/dashboard/DashboardPinnedQuickRow.tsx); the `?round=1` deep link in [`JobsStagesTab.tsx`](../../src/components/jobs/JobsStagesTab.tsx) opens GC Review with `startInRound`. Kernel shaping in [`statementRoundEmail.ts`](../../src/lib/statementRoundEmail.ts) (`parseStatementRoundPayload`, `statementRoundNudgeFromPayload`).

**2. `statement_round` email stream** (REPORT_SUBSCRIPTIONS inventory row + section): edge function [`statement-round-email-dispatch`](../../supabase/functions/statement-round-email-dispatch/index.ts) (preview / test_send with caller JWT; cron drain; per-recipient payload; empty round still sends a one-liner) + [`render.ts`](../../supabase/functions/statement-round-email-dispatch/render.ts) (GC rows with amount / jobs / age / certifier, total, **Start round →** button, held-on-certification line). **UI: "Email me my round…"** in the Weekly statement rounds panel ([`JobsGcReviewModal.tsx`](../../src/components/jobs/JobsGcReviewModal.tsx)): Mon–Fri chips + Central time → one `repeat_weekly` chain per weekday (kernel `groupStatementRoundChains` / `planStatementRoundChainEdit`, 6 tests — the v2.1430 standing-copies diff: a time change re-creates every chain, a weekday change touches only added/removed days, zero days = Stop emailing); Preview / Email me a test; Edit; certifier roles can set it up for another sender from a picker, and other senders' chains list read-only. Lists on Settings → My email schedule (`emailScheduleWeek.ts` union + label "Statement round"; blue tone). IO in [`statementRoundEmailClient.ts`](../../src/lib/statementRoundEmailClient.ts).

**3. Send from the app inside the round**: a **Send from the app…** button beside Copy for email in the round overlay opens the existing Draft Message dialog for that GC; the overlay steps aside while it is open (the dialog stacks under it) and returns when it closes. On a successful send-now, any GC in the round not already marked sent gets an automatic mark (`email` · "Sent from the app") — an app send from anywhere (Share → Draft Message included) now advances the round, where before only the pill moved.

**Help guide** [`run-your-gc-statement-round`](../../src/content/help/run-your-gc-statement-round.md): "Where your round finds you" (Dashboard row, morning email) + the app-send step.

**Fidelity**: `get_statement_round_for_user` was run read-only for the two live senders after the push and compared with the GC Review panel (see the PR thread / verification note below).
