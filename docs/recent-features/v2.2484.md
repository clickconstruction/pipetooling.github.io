# v2.2484 — R5 letter chip + the no-send guard (2026-08-29)

RFI_LOOP_PLAN R5 + master plan Wave 4.3 (+ 4.2's kernels):

- **`OpenRfiChip`** beside BOTH Mark-sent buttons on the Cover Letter tab (plain-bid and
  per-GC-packet branches): counts draft/approved/sent RFIs, hover lists them as the
  assumption lines they must become. Never blocks the button — names the debt.
- **Kernel `src/lib/bids/scopeSheet.ts`** (+3 tests): `substrateNotesToSuggestions`
  (substrate note flags → suggested exclusions/risks, suggested-never-auto) and
  `openRfiAssumptions` (open RFIs → required assumption lines) — the Wave-4 scope-sheet
  seam, consumed by the chip today and the drafting flow later.
- **Migration `20260829033440`**: `twin_no_send_guard()` triggers on `bids`
  (bid_date_sent/submitted_to/outcome), `bid_versions` (per-GC outcome/outcome_at), and
  `bid_version_sends` (any row) — sending and outcome-recording RAISE for
  `is_digital_twin()` sessions. The M3-era mission-text policy is now physics.

Deploy: client first, then `supabase db push` (no edge functions).
