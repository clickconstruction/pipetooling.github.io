# v2.2458 — Follow Up workstation rework (2026-08-28)

Fifth slice of the Prospects-refresh arc — the big one. The workstation led with a warmth counter (238 of 268 prospects sat at warmth 0) and scattered the call outcomes across two rows; converted work now leads with the call itself.

- **[`Prospects.tsx`](../../src/pages/Prospects.tsx)** Follow Up tab restructured into a two-column layout (main + desktop queue rail):
  - **Header card**: company name + contact/email/website line; due + never-called chips; tap-to-call `tel:` button. Outcome row labeled "How did the call end?" — Didn't Answer / Answered (moved here from the Comments header; same handlers, typed comment still attaches) / Can't reach / **Not a fit** (renamed from the red "No Longer a Fit"; neutral styling — it's a routine outcome) / Converted ✓. Utility row: Set callback + Edit prospect links, 🔥 warmth chip, scheduled-callback link, `this call` timer (still opens history) and `my day`.
  - **Auto-warm (v2.2458)**: `handleAnswered` bumps `warmth_count` by 1 with the `last_contact` update (both caches patched). The −1/Reset/+1 controls moved into the Edit modal (only when editing the current prospect). The standalone "all time" timer readout became a "Time on this prospect" line on the info card.
  - **Queue rail** (`.followUpQueueRail`, ≥1024px): the v2.2301 peek always visible — position ("N of M"), 12-row window via new component-level `renderQueueRows(limit, collapseOnJump)`, calling-order toggle via `renderCallingOrderToggle()` (labels shortened to Coldest / Never called). Mobile keeps "N of M · show order ▾" in the utility row + the 15-row peek (`.followUpQueueMobileStrip`); the old calling-order strip is gone.
  - **Next Prospect** is a full-width bar at the bottom of the main column (owner-requested placement), after the info card and Copy row.
  - Info card slimmed: company/contact/phone/email/website rows removed (all in the header now); keeps Last Contact, Last updated by, Last Successful Contact, Address, Time on this prospect.
- **[`index.css`](../../src/index.css)**: `.followUpQueueRail` desktop-only (270px), `.followUpQueueMobileStrip` hidden ≥1024px.
- **Help guide** [`work-the-prospect-calling-queue.md`](../../src/content/help/work-the-prospect-calling-queue.md) — closes the known guide gap flagged in v2.2301.
- Quirks preserved: auto-advance checkbox path unchanged; timer-event semantics unchanged; steal-on-view locks untouched; `handleDidntAnswer` still uses the typed comment.
