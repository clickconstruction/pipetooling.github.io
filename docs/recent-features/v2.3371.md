# v2.3371 — Edit tab: Collections on the status line, Customer Contract named (2026-09-12)

Client only. Owner: on the job window's Edit tab, Paid and Collections showed on two lines — "move the collections slider to the same line as Paid but have it be on the right hand side."

- [`JobStatusStepper.tsx`](../../src/components/jobs/JobStatusStepper.tsx): the Collections switch (and its "In Collections" / "applies once the job is Billed" words) moved from its own row under the rail into the rail's flex row as the last child with `marginLeft: auto`, so it sits at the right end past Paid. The rail already wraps, so on a narrow window the switch drops under the rail, still right-aligned. No behaviour change: same switch, same confirm flow, same Billed-only arming.
- The render smoke (`JobStatusStepper.render.test.tsx`) still finds the switch by role and its states.
- [`JobFormEditFactRows.tsx`](../../src/components/jobs/JobFormEditFactRows.tsx): the customer sub-row labelled **Contract** is now **Customer Contract** (owner ask, same pass); the narrow label column wraps it onto two lines.
