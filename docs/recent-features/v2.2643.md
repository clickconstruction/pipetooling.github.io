# v2.2643 — RFQ Round 2, Rung B: freight joins the compare (2026-09-02)

Rung B of `docs/RFQ_ROUND2_PLAN.md` (canvas artboard 6). Freight was
captured by both entry lanes but ignored everywhere it mattered.

- **Kernel** (`quoteCompare.ts`, +3 tests): `CompareQuote.freightCents`
  (null = not stated, NOT free), `HouseSummary.freightCents` +
  `commonWithFreightCents` (the number that ranks houses honestly), and
  `pickedFreight`/`pickedTotalWithFreightCents` — freight counts once
  per house you picked anything from.
- **Compare modal**: house chips grow "+ $85.00 freight = $903.00" /
  "free freight" / amber "freight not stated"; the footer's picked total
  becomes the split — "parts $580.00 + Ferguson freight $85.00 =
  $665.00". Stars stay unit-price only; expired quotes keep freight out
  with everything else.
- Live-tested on BP339 with all three freight states seeded (85.00 /
  0 / null); test quotes deleted after.
