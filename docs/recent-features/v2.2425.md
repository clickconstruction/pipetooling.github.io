# v2.2425 — Cover Letter sent-date wrap fix (2026-08-28)

Owner screenshot: at many viewports the checklist meta line "★ WENDI · $56,343.00 · sent
8/27" broke between "sent" and "8/27".

- `BidsCoverLetterTab` checklist meta line: each segment (★ name / $amount / send badge) is
  now a `white-space: nowrap` unit, so narrow panels break only at the " · " separators.
- `BidVersionPicker` chip send badge ("sent 7/7 · $X") gets the same nowrap.

Verified at 980px: the badge renders as a single rect.
