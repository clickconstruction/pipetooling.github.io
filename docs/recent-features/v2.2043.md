# v2.2043 — One-shot deep links + quick lost capture on Followup (2026-08-21)

Wendi's scroll report ("By builder takes you very far down to a recently accessed bid") + the loss-reason refinement round (mockups: landing fix cafca93d, loss flow 85e15844).

**One-shot deep links** ([`Bids.tsx`](../../src/pages/Bids.tsx)): `?bidId=` was never removed from the URL, and each tab's applied-guard ref resets on leave — so one jump replayed on every revisit and reload. New `consumeBidIdParam` (delete + `replace: true`) runs at each application site: `applyBidBoardDeepLinkToBid`, `applySubmissionFollowupDeepLinkToBid`, `applyBuilderReviewDeepLinkFromBid` (up front, so the no-customer/already-applied branches stop replaying their toasts too), and the Working-board branch of the routing effect. `selectBidsTab` also drops `bidId` — clicking a top tab means "take me to the page". Workspace tabs (counts/pricing/…) keep their `bidId` selection semantics untouched.

**Quick lost capture**: new [`BidLostQuickPopover`](../../src/components/bids/BidLostQuickPopover.tsx) — note box + the shared `BidLossCategoryChips` (suggestion ring from `suggestLossCategoryFromNote`); a reason tap writes `outcome/loss_category/loss_reason` in one update ("mark lost — don't know why yet" saves outcome only, feeding the Why-we-lost queue by design). Wired in [`BidsBuilderReviewTab`](../../src/components/bids/BidsBuilderReviewTab.tsx): "Lost…" on unsent/pending rows, and lost rows show the category chip (tap to change) or amber "why? →"; panel renders inline under the row. Same treatment on [`BidSubmissionFollowupTab`](../../src/components/bids/BidSubmissionFollowupTab.tsx)'s Lost table — reason column becomes chip/why? + the free-text note in italics, panel as a full-width row.

Verified live: deep link to a builder card scrolled once and the URL immediately cleaned; Bid Board → Followup round-trip landed at scrollY 0; 175 Lost… actions / 49 chips / 60 why? buttons rendered across both surfaces; panel prefills the note and a same-value re-tap saved and closed. Help guide updated. Client-only — no migration.
