# v2.2919 — Schedule papercut batch B10: honest undo copy, Escape closes Share, Share and Day-email preview what will send (2026-09-05)

Journey map Phase 4, **Tier 3 row B10** (`JOURNEY_MAP.md` → "Schedule Dispatch hub"; dossier J18 schedule-the-week). One PR, client only — no migration, no edge function, no telemetry. The B10 items that shipped earlier today are not here: `off` gated to the roles the RPC accepts (#23, PR #2633), Dispatch-settings footer honesty (#42, PR #2637), `?hubTab=day&day=` (#17, PR #2630), `/calendar` twin filter (#19, PR #2636), bid visits on the Jobs tab (#22, PR #2640).

## Item by item

| # | Finding | What changed |
|---|---|---|
| 1 | J18-F3 | **Done.** New [`scheduleDispatchNotComingInCopy.ts`](../../src/lib/scheduleDispatchNotComingInCopy.ts) (+3 tests): `undoNotComingInRestoresBlocks()` (false — the undo RPC deletes the `user_time_off` row only) drives `undoNotComingInCopy()`, which `ScheduleDispatchUndoNotComingInModal` now renders: lead line, an unconditional "This only clears the mark. Any schedule blocks removed when the day was marked off don't come back — add them again from the cell.", and the NCNS incident note. The modal cannot tell which door wrote the mark (empty-cell `off` removes nothing; the picker's "Not coming in today" and NCNS clear the day's blocks), so the line is unconditional. |
| 1b | J18-F3 (optional confirm) | **Done.** The empty-cell `off` button now opens `ConfirmDialog` with `markOffConfirmCopy()` — "Mark Paige as not coming in? · Wednesday, Sep 2 — records unpaid time off for the day. Nothing is scheduled for them that day, so no blocks are removed. Undo any time from the cell's chip." Confirm → the unchanged `markNotComingInForPersonDay`. `ScheduleDispatchHubPage.tsx`: `markOffConfirmTarget` state + `cancelMarkOffForCell` / `confirmMarkOffForCell`; the assign picker's own confirm is untouched. Still only armed for `canTimeOff` roles (v2.2892). |
| 2 | J18-F8 | **Done.** `ScheduleShareModal` registers with the Tier-2 #42 modal stack (`useModalStackEntry(open)`) and closes on Escape when topmost and not sending; a key the recipient `SearchableSelect` already consumed (`ev.defaultPrevented`) is ignored so Escape closes the dropdown first, the modal second. `ScheduleDayEmailModal` gets the same treatment. |
| 3 | J18-F9 | **Done.** New [`scheduleSharePreview.ts`](../../src/lib/scheduleSharePreview.ts) (+8 tests) mirrors `_shared/scheduleShareCore.ts`: `schedulePreviewLines(rows, dates)` (filters to the Include set exactly like the edge function's `fetchBoardBlocks`, `— · Job` / `(Unassigned)` fallbacks, `B<number>` bid rows visible), `schedulePreviewByPerson`, `schedulePreviewSummary(+Label)`. New presentational [`ScheduleEmailPreviewPanel.tsx`](../../src/components/schedule/ScheduleEmailPreviewPanel.tsx) ("What will send", `role="status"` headline, scrollable list). **Share** (Send now tab) calls `list_schedule_blocks_for_share(p_viewer = me, p_start, p_end)` — the same call the instant path makes server-side from the sharer's visibility — grouped by person with a caption that every recipient gets the same email. **Day email** calls `list_job_schedule_blocks_for_schedule_email(p_recipient, p_work_date)` for the chosen recipient (dev picker honoured), flat in time order like its email, captioned "built again at send time". Both RPCs were already `GRANT … TO authenticated` (baseline). The v2.1624 "bid blocks reach schedule emails" claim is now visible from the UI. |
| 4 | J18-K1 P6 | **Done (copy softened).** `/calendar` keeps its gear-menu entry and all four dispatch renderings stay; `Calendar.tsx` gains one muted line for roles that can open the hub (`CAN_VIEW_SCHEDULE_DISPATCH_ROLES`): "Putting people on jobs happens on the Schedule hub — this page is your own day and bid due dates." The brief's "This view is retiring" wording was not used: decision #16 is **keep**, so nothing retires and saying so would be the kind of dishonest copy this batch exists to remove. Subcontractors (who can reach `/calendar` but not the hub) see no line. |
| — | J18-F10 / F11 / F12 | **Skipped** — shipped today in PRs #2637, #2630, #2636. |

## Files

Kernels: `src/lib/scheduleSharePreview.ts` (new, +test) · `src/lib/scheduleDispatchNotComingInCopy.ts` (new, +test).
Components: `schedule/ScheduleEmailPreviewPanel.tsx` (new) · `schedule/ScheduleShareModal.tsx` · `ScheduleDayEmailModal.tsx` · `schedule/ScheduleDispatchUndoNotComingInModal.tsx` · `schedule/ScheduleDispatchHubPage.tsx` (off-cell confirm only) · `pages/Calendar.tsx`.
Docs: `SCHEDULE_DISPATCH_ARCHITECTURE.md`. Help: `schedule-dispatch.md`.

## Verify

1. Schedule → People grid, an empty cell → **off**: a confirm names the person and day and says no blocks are removed; Cancel writes nothing; Confirm → the time-off chip appears with the usual toast.
2. Click the chip → the undo modal shows the "don't come back" line; on an NCNS chip both the blocks line and the incident line show.
3. ⋯ → Share: press Escape → closes. Reopen, click the recipient box, type two letters, press Escape → the dropdown closes, the modal stays; Escape again → closes.
4. Share → Send now: "What will send" lists people and their blocks for today; tick **Rest of week** → the list grows and the headline reads `N blocks · M people · D days`; a week with a scheduled bid visit shows `B412 · <project>` under the estimator.
5. Dashboard clock strip → Email schedule: the panel lists that day's blocks for you (dev: for the chosen recipient); Escape closes it.
6. `/calendar` as assistant: one muted signpost line above My Day linking to `/schedule-dispatch`; as subcontractor: no line.

## Rollback

Revert the squash commit — client-only, no schema or edge-function change.
