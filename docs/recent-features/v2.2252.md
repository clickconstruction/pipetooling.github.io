# v2.2252 — Payroll: small-cents amounts (rollout of v2.2246) (2026-08-24)

Owner: "roll it out to the payroll ledger too." The Sub Labor small-cents treatment now covers People → Payroll.

- **`AmountSmallCents` moved to shared** [`src/components/AmountSmallCents.tsx`](../../src/components/AmountSmallCents.tsx) (from `components/jobs/`); `JobsSubLaborTab` import updated. Kernel `splitFormattedAmountCents` stays in `lib/jobs/jobFormatting.ts`.
- **Pay reports table** ([`PeoplePayStubsTab.tsx`](../../src/components/people/PeoplePayStubsTab.tsx)): header open-balance summary + upcoming button, Gross / Less|Additional / Net / Paid to date / Balance cells, Upcoming-payroll modal (subtitle, Est. Gross, total), Payment-details modal (installments, Total paid · Balance), calendar YTD line. Aria-labels, tooltips, and the copy-balance clipboard text stay plain full strings.
- **Ledger view** ([`PeoplePayLedgerView.tsx`](../../src/components/people/PeoplePayLedgerView.tsx)): local `MoneySC`/`SignedBalanceSC` wrappers over the shared component for the numeric display spots — We-owe/owed-to-us header, roster balances, the balance headline, the equation, journal Amount + Balance columns (desktop + stacked phone rows). The `money()`/`signedBalance()` string helpers remain for prose sentences (unpaid callout, partial chips, captions), which keep full-size cents deliberately.

Client-only; no schema changes.
