# v2.2228 — Billed-report email payload matches the board's row scope (2026-08-24)

Migration `20260824141336` — CREATE OR REPLACE of `get_billed_report_email_payload()` only. The RPC selected invoices only from `status='billed'` jobs, while the Stages board flatMaps billed invoice lines across ALL jobs (`jobsStagesBoard.ts` `billedInvoices` — progressive billing bills break-off lines on Working jobs; verified 2026-08-24 with jobs 878 Take 5-Seguin ~$16k / 650 ATI Schertz $26.8k / 967 Calvin Barrett Pump on the board but absent from email payloads), excluding only collections (`status='billed' AND collections_at`). New scope: every billed invoice line on any non-collections job; the merged `'Billed line'` detail is gated on the job itself being billed with one billed line, so a Working job's line labels `'Invoice #N'` like the board. Mirror of the payment-forecast fix (v2.2227, 20260824140029). Dates/aging/totals/JSON shape unchanged; no edge-function redeploy.
