# v2.3310 — Bids → Labor opens on New (2026-09-11)

Client only. Owner call after rungs 2–5 of the Labor refresh landed (v2.3291–v2.3307): "Between old and new, I want the user to have new as the default instead of old."

- [`laborView.ts`](../../src/lib/bids/laborView.ts): `parseLaborView` returns `old` only for a stored `old`; anything else (nothing stored, a broken storage, an unknown value) is `new`. A device that picked Old keeps it (the stored `old` still wins). Tests flipped accordingly.
- Help guide and the architecture map say New is the default; the to-do's status line records the call. Retiring Old (PR 6) is still the owner's decision.

Verified live: with the device key cleared the Labor tab opens on New; with `old` stored it opens on Old.
