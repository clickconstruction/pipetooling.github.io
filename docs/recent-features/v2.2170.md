# v2.2170 — Partner statement letterhead: company name + deal start (2026-08-23)

Follow-up to v2.2157. The statement's letterhead read only "Partner account" because the
summary RPC didn't carry the deal's company name; "partner since" was inferred from the oldest
week on file.

- Migration [`20260823163821_partner_summary_company_name.sql`](../migrations/20260823163821_partner_summary_company_name.md):
  `partner_summary_payload` returns `company_name` (null when blank) and `started_on`.
- [`partnerWeeks.ts`](../../src/lib/partnerLedger/partnerWeeks.ts): `PartnerSummary` gains
  `company_name: string | null` and `started_on: string | null`; the parser trims / nulls them
  (+ tests). Both optional in the payload, so the client works against the old RPC too.
- [`PartnerStatementPaper.tsx`](../../src/components/partner/PartnerStatementPaper.tsx):
  letterhead right-hand line reads **Partner account · Herber Electric** when a company name
  exists; the head's "partner since …" prefers `started_on` and falls back to the oldest week
  card (+ render assertions for both paths).

Stacked on v2.2165 (nav link) so the shared `PartnerSummary` fixtures move together.
