# v2.1958 — Order stages from both views; toolbar trailing right-aligned (2026-08-21)

Follow-up to the v2.1947 single-row toolbar (user request).

- The `viewMode === 'map'` gate on the trailing `TechTreeRoadmapToolbarActions`
  is gone — Order stages (and the empty-graph starters) work from Plan too;
  the modal was always view-independent.
- The Map/Plan toggle + editor actions wrap in a `marginLeft: 'auto'` group,
  so they sit on the right-hand side of the roadmap toolbar row (still
  flex-wraps on narrow screens).
- Help guide step updated ("either view works").
