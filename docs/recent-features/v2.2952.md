# v2.2952 — twin-mcp v1.3.9: holdout enforcement at the harness (2026-09-06)

Completes LEARNING_PLAN item 6\'s mechanism (flag + Queue lens + gate-math prep shipped v2.2942; this is the door that makes it structural). Edge only — deploy `twin-mcp` after merge; requires the `bids.holdout` column (pushed earlier today).

- **`openBacktestShell` refuses holdout references** unless `open_backtest(..., gate_run: true)` — pass it only on an operator-ordered gate run. `next_backtest` auto-skips holdout candidates through the shared refusal, so practice slates can never touch the measuring set.
- Remaining owner action: designate the ~20–25 holdout references in the Queue lens (Hold out toggle, spread across axes).
