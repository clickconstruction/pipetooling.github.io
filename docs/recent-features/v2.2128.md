# v2.2128 — Manage view pills: One-offs leads (2026-08-22)

Follow-up to v2.2118 (Manage opens on One-offs): with One-offs as the default it read oddly
for the pressed pill to sit second. Pill order is now **One-offs · All · ↻ Repeating · ✓ Completed**.

- [`Checklist.tsx`](../../src/pages/Checklist.tsx): the `manageView` pill tuple array is
  reordered; no other change (counts, rendering, default all unchanged). Client-only.
