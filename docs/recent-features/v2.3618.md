# v2.3618 — Sub Labor sheets: benched subs leave the crew lists (2026-09-19)

Item 4 of [`to-dos/subs-residuals.md`](../../to-dos/subs-residuals.md) (v2.2860's "not done"). Client only; no migration.

## The change

- **The rule** — [`lib/people/subBench.ts`](../../src/lib/people/subBench.ts) `isOnBench(endDate)`: the bench is `people.end_date` set (v2.2853's storage), so nothing new is stored.
- **The roster type** — `Person` on [`pages/Jobs.tsx`](../../src/pages/Jobs.tsx) and [`hooks/usePeopleRoster.ts`](../../src/hooks/usePeopleRoster.ts) gains optional `account_user_id` and `end_date`, and both `people` selects read them.
- **The form** — [`JobsSubLaborFormModal.tsx`](../../src/components/jobs/JobsSubLaborFormModal.tsx) `byKind('sub')` leaves out a benched roster row, and the account row (or email) a benched roster row is folded into; a name already on the sheet is untouched (+1 render test: *Sub Benched* is not offered, *Sub Sam* is).
- **The guide** — [`manage-subs-end-to-end.md`](../../src/content/help/manage-subs-end-to-end.md) *Active or on the bench* says so.
