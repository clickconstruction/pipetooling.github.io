# v2.2245 — Dispatch Mode: pipeline collapsed to Ready to bill + HR Report card (2026-08-24)

Two Dispatch Mode dashboard (`DispatchModeHome`) requests from Stephen:

- **Job Pipeline default**: all stage groups now start collapsed except
  **Ready to bill** — previously only Waiting started closed. The
  `collapsedStages` initial set is `waiting/working/billed/collections`;
  toggling is unchanged.
- **HR Report card**: `DashboardPersonReportCard` (v2.2235) renders below the
  Job Pipeline with the same role gate as the main Dashboard
  (dev + master_technician; the card itself stays collapsed by default and
  fail-soft). Help guide `send-an-hr-report-about-someone` mentions the new
  location.
