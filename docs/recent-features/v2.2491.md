# v2.2491 — Needs You: bulk-delete item, red severity + secondaries (banner migration 5/6) (2026-08-29)

Fifth v2.2339 follow-up: `DashboardBulkDeleteAlertBanner` (dev-only) folds
into the Needs You card. Stacked on v2.2490.

- **Card grows two capabilities**:
  - `NeedsYouSeverity` gains `'red'` — a destructive event to investigate,
    not a work queue (rail/action `#dc2626`, walk tint `--bg-orange-tint`).
  - `NeedsYouItem.secondary` + card prop `onSecondary` — small link-style
    follow-ups under the detail line (both modes), telemetry target
    `#<item-key>-<secondary-key>`.
- **New hook `src/hooks/useBulkDeleteNudge.ts`**: wraps `useBulkDeleteAlerts`
  with the banner's per-user localStorage snooze/dismiss state
  (`bulkDeleteAlertDismiss` kernel unchanged); returns `visibleAlerts`
  (null when hidden) + `snooze24h` / `dismissUntilCountIncreases`.
- **Kernel**: input `bulkDeleteAlerts`; item `bulk-delete` (red, kicker
  "Data safety", action "Review deletions" →
  `/settings?tab=settings-data#settings-recently-deleted`, secondaries
  snooze/dismiss). Multi-burst detail compresses the banner's per-burst lines
  into one: totals + actor count + newest burst.
- **Both surfaces** (dev-only via the RPC's own gating); banner deleted.
- Tests: 2 new kernel tests. Help guide notes the red stripe + snooze links.
