# v2.2402 — Workbench solve-landing chip: blue, terse, tucked (2026-08-27)

Wendi: the green landing chip ("95% on 15 costed rows → bid is $867,187 ·
96%") should become "95% on 15 costed rows" in the solver's blue, nested
closer to the Revenue/Profit/Margin strip it sits under.

`BidsPricingTab.tsx`, the `wbSolveLanding` chip:

- Text drops the "→ bid is $… · %" tail — those numbers sit directly above
  in the totals strip; restating them was noise.
- Colors move from green (`--text-green-700`/`--bg-green-tint`) to the solver
  family: `--text-blue-700` on `--bg-blue-tint` with the `#3b82f6` border.
- Wrapper `marginTop` 0.5rem → 0.15rem so it reads as a caption of the strip.

Display-only; the chip's show/hide logic (pending solve with un-vetoed
proposals) is unchanged.
