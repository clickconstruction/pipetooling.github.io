# v2.2279 — Dispatch Inbox & PO Generator: titles move onto their header rows (2026-08-25)

Owner follow-up to v2.2277 (Customers): "do the same for the inbox and po titles." Same grammar — small header content left, right-aligned `<h1>` (`margin-left: auto`, `nowrap`) on the same flex row, one row saved per screen.

- [`DispatchModeInbox.tsx`](../../src/components/dispatchMode/DispatchModeInbox.tsx): the **Open Gmail** pill (formerly self-centered) sits left; **Inbox** right.
- [`DispatchModePO.tsx`](../../src/components/dispatchMode/DispatchModePO.tsx): the faint "Hold down a name to sort it into Other" hint sits left; **PO Generator** right.

Client-only. All four dispatch tabs with titles now share the right-aligned treatment.
