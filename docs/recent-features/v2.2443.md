# v2.2443 — Plumbing Tooling rebrand, PT side (2026-08-28)

Owner-approved via the "Plumbing Tooling Rebrand" mockup artifact. The report app at
plumbingtooling.com renamed from "Click Tooling" to **"Plumbing Tooling"** (its repo
commit `b7555c1`: title tag + README; the Click Plumbing letterhead/certification on
the reports is deliberately untouched). This PR renames PT's user-visible strings:

- Stages card-menu action "Click Tooling report" → "Plumbing Tooling report"
  (JobsStagesCardList ×2 + render test), wrench tooltips/aria-labels in
  JobsStagesUnifiedTable ×2 and JobsStagesTable.
- Help guides `search-the-stages-board.md` and `ready-to-bill-pipeline.md`.
- Internal identifiers (`buildClickToolingUrl`, `showClickTooling`, `click-tooling`
  keys) deliberately keep the old name — no churn; noted in jobAddressUrls.ts.
- Frozen surfaces untouched: `releaseNotesArchive.ts`, shipped release note v2.2441.
