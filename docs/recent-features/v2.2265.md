# v2.2265 — Fix: v2.2264 sync errored at runtime (2026-08-25)

Migration `20260825032223`: the v2.2264 de-materialize sweep joined
`checklist_instance_events` on `checklist_instance_id`; the real column is
`instance_id`. plpgsql bodies aren't validated at CREATE, so
`sync_roadmap_to_checklist` failed on every call between the two pushes
(~40 minutes; the client swallows sync errors by design, so the only symptom
was no pruning/materialization). Function body otherwise identical.
Lesson recorded: smoke-run a replaced RPC immediately after `db push`.
