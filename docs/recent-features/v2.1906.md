# v2.1906 — Pricing gets Old/New views: the Workbench arrives behind a toggle (2026-08-20)

Owner-approved prototype ("Pricing Workbench" artifact), shipped like the Cover Letter studio was: the Pricing tab's selected-bid card gains **Old / New** pills (per-device `bids_pricing_view_v1`, default **Old** — the classic grid, untouched). **New** is the Workbench:

- **Sticky live totals** — Revenue · Our cost · Profit · Margin always on screen, tracking every edit.
- **Target-first solver** (new kernel [`pricingWorkbenchSolver.ts`](../../src/lib/bids/pricingWorkbenchSolver.ts), 10 tests): drag the **blended-margin slider** (20–65%) or type a **target total** and it prices the rows — revenue spread proportional to each row's cost with overhead pro-rata, matching the grid's Total-margin definition. **📌 session locks** and `is_fixed_price` rows are held; **uncosted rows are never priced** (same rule as `applyMarginPricing`); optional round-up-to-$5; "Price unpriced only" fills blanks around existing prices.
- **Preview → Apply** — the solver NEVER writes directly: changes land as amber "preview" prices with the totals reflecting them, then **Apply prices** commits each row through the existing `writeUnitPriceOverrideRow` path (one engine reload at the end) and **Discard** walks away. Manual per-row edits join the same preview.
- **Coverage meter** — "N of M costed rows priced — $X of cost has no sale price" with a show-unpriced-only filter (unpriced cells tinted); the uncosted-revenue margin warning carries over.
- **Where the profit lives** — profit-by-row concentration bar with a top-2 warning ("⚠ 72% of profit sits in X + Y — a VE cut there guts the job").

Plumbing: the old grid IIFE's totals/row-decoration compute hoisted into a shared `derivePricingWorkbench()` (both views single-sourced; old view byte-identical otherwise); the header's Cost/Price-Model toggle hides in New (no view-model there). Row click opens the same margin-breakdown modal. **Deferred to iterations 2/3** (per the prototype): the scenario strip (maps to named Pricings) and the win/loss history calibration strip (needs a history RPC). Verified live in the worktree: solve→preview→discard on B273 with uncosted rows correctly skipped. Client-only — no migration.
