# v2.2941 — Standing rulings panel + doctrine-at-stake audit triage (2026-09-06)

LEARNING_PLAN.md items 4 and 5 in one PR: the estimator's hours are the robot program's bottleneck, so the Audits tab now (a) surfaces the robots' parked questions where she already works, deduped so one answer settles a doctrine issue everywhere, and (b) orders her pending queue by what a verdict unblocks instead of oldest-first.

- **Standing rulings panel** (top of `BidsAuditsTab.tsx`, write-audience only — `canWriteBidAudit`): open `twin_questions` grouped by `topic` (the kebab-slug doctrine key robots stamp since PR #2684, e.g. `travel-bands`). Same-topic questions collapse into one ruling card — humanized topic chip, the newest phrasing as the canonical question, "asked N times across M bids" — with ONE answer box whose submit updates EVERY open question in the topic (`answer`, `status='answered'`, `answered_at`, `answered_by`; guarded `.eq('status','open')` so a race reports "already handled elsewhere"). Topicless questions list individually below. Header "📜 Standing rulings · N — fifteen minutes here unblocks every robot"; collapsible, collapsed by default only when N=0. Until now these questions surfaced only in the dev-only fleet console (Settings → System → Digital twins) — the fleet console lane (answer/dismiss/promote-to-RFI) is unchanged.
  - Kernel: [`standingRulings.ts`](../../src/lib/bids/standingRulings.ts) (+6 tests) — `groupStandingRulings` (open-only, newest-first canonical text, distinct-bid count, most-asked-first ordering), `topicLabel`, `rulingAskedLine`.
  - **No migration**: `20260829032237_twin_questions_heartbeat.sql` already grants SELECT to all staff roles and UPDATE (FOR ALL) to exactly the audit-writer set (dev, master_technician, assistant, controller, estimator). Reads use `select('*')` through the untyped client (BidRfiQueue pattern), so the panel works before and after the `topic` column's migration (`20260906110000`, PR #2684) is pushed; the panel hides entirely if the read fails.
- **Doctrine-at-stake triage** (kernel [`auditTriage.ts`](../../src/lib/bids/auditTriage.ts), +6 tests): PENDING audits order by `compareAuditStake` — open question count descending (the biggest lever), then |delta %| vs our number when priced (a −57% card teaches more than a −3%; unpriced/unpaired rank after any priced card), then age (older first). `orderPendingByStake` reorders only the pending block; done/digested keep `sortAuditsForTab`'s order. Wired in the tab via a `useMemo` over data it already loads (threaded notes, draft totals, refValue); auto-expand and finish-audit's "open the next one" follow the same order. A muted caption — "sorted by what your verdict unblocks" — shows when 2+ audits are pending.
- Render smokes extended (`BidsAuditsTab.render.test.tsx`): the panel groups duplicates and shows "Answer all 2" for dev; absent for superintendent (read-only mirror).
- Help guide `audit-a-robot-bid.md`: standing rulings section + the new queue order documented.

Client-only; no migration, no edge deploy.
