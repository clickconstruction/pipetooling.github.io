# v2.2294 — Gear menu: Partnerships icon is a handshake (2026-08-25)

Owner request with the exact SVG supplied (Font Awesome handshake-simple, 640 grid). `partnershipsIcon` in [`Layout.tsx`](../../src/components/Layout.tsx) swaps its briefcase path for the handshake path — the old briefcase read as a near-duplicate of the Documents icon directly below it. Same 1em/currentColor/aria-hidden wrapper; `fillRule` dropped (single path, not needed). Client-only, gear menu only (dev role sees the entry).
