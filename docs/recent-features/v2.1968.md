# v2.1968 — Review tiles side by side on mobile (2026-08-21)

Follow-up to the v2.1957 centered tiles (user-reported with phone
screenshot): `flex: 0 1 220px` made each tile's hypothetical size 220px, so
a 375px phone wrapped them into a three-high stack.

- `boardTile` becomes `flex: '1 1 96px', maxWidth: 220, minWidth: 0` — three
  tiles always share one row (labels wrap to two lines on phones), while
  desktop still renders the same centered 220px group.
