# v2.2163 — Bids by GC G4: words, tour, help; "Also sent to" folds into packets (2026-08-23)

G4 of the owner's "Bids by GC" model (proposal claude.ai/code/artifact/a2878573-eb4d-47d9-abaf-e4cbd942a811): the app now says the owner's sentence — **versions draft this bid for different GCs; price options send more than one price to the same GC** — everywhere the old "version / scenario / package" words were, plus the light fold of "Also sent to" GCs into the packet views.

- **Pricing Workbench words** (`BidsPricingTab.tsx`): door button "＋ Another price or GC…" → *Another price for {GC}* (offer as alternate or keep to compare) / *Another GC* (dispatches `bid-version-picker-open-add-gc`, which the picker handles by opening the GC-first "Send this bid to another GC" modal) / *Adopt an existing bid*; solo line "One GC · one price", "★ base · the GC sees this", "☆ Make base…"; "price option" replaces "scenario" in the delete modal, the copy-prices row, toasts and card titles; the ★ chooser says "Not what the GC sees".
- **Tour**: a new first stop on the **Send to** strip (`data-tour="send-to"` on the picker root) — "Versions draft this bid for different GCs: each GC gets its own packet…"; the prices stop reads "Price options are different prices for the same GC. The ★ base is what the GC sees… Offer another as an alternate…"; "scenario" → "price" in the strip/rows stops.
- **Send to strip** (`BidVersionPicker.tsx`): "+ version" tooltip and the Another-version modal explain "same GC, its own takeoff and prices — a VE; for another GC use ＋ Another GC…; for another price only, the Pricing page"; rename dialog "GC this version goes to" with "The bid's GC (name)" as the default option; "one packet" instead of "one bid".
- **"Also sent to" fold** (kernel `groupVersionsByGc` gains `recipients`; packets get `sharedLetter`): a `bid_gc_recipients` GC with no version of its own appears as a dimmed group on the strip ("same letter · sent m/d") with **track separately** (opens ＋ Another GC… prefilled), as a "same letter as {bid GC}" line under the Bid Board row and in Followup's "Sent to — by GC" (no outcome select — its answer is tracked with the bid). `useBidGcPackets(bids, recipientsByBidId)`; `gcRowsWorthShowing` needs at least one real packet. Edit Bid's "Also sent to" chips explain which GCs have their own packet. Kernel test added (+1).
- **Cover Letter New** copy: Old/New tooltip and the "Checked packets go in {GC}'s letter…" helper say packets, not bids.
- **Help**: `bid-one-project-to-multiple-gcs` rewritten end-to-end (add a GC, choose what each GC receives, write each GC's letter, track each GC's answer, "Also sent to"), roles widened from dev to the office roles; `price-a-bid-with-the-workbench` "GCs, prices, and the star"; `try-the-new-cover-letter-layout` (one letter per GC, Mark sent to {GC}, GC tabs); `read-the-bid-board` (per-GC lines). `GLOSSARY.md` entry "GC packet / Bid version ("Send to") / Price option" replaces the old "Bid Version / Price scenario" entry; `BIDS_SYSTEM.md` gains "Bids by GC (v2.2154–v2.2163)".
- v2.2162 fragment nit: the Bid Board line now reads "phone-width cards show the same lines" (the card variant shipped in that PR's live-test follow-up).

Words sweep is script-driven (`g4-words.py` in the session scratchpad — exact-string replacements); on a rebase conflict re-run it on fresh main rather than hand-merging.
