# v2.2062 — Activity box: chat order + "+ Add" (2026-08-21)

Owner request: the Pipeline row activity box read newest-first; now it reads like a chat. [`jobActivityBoxFeed.ts`](../../src/lib/jobs/jobActivityBoxFeed.ts) drops the final `.reverse()` — oldest first, newest at the bottom (numbers unchanged: 1 = oldest, still stable references). [`JobsStagesActivityBox.tsx`](../../src/components/jobs/JobsStagesActivityBox.tsx) pins the scroll to the bottom whenever the feed grows (initial lazy-load and each new entry, via a `feed.length` effect), and the floating "Post" pill becomes **"+ Add"** (aria-label follows). Composer submit keeps "Post". Kernel + render-smoke tests flipped to the new order. Verified live against prod (15-entry thread renders 1→15, scrolled to newest; 43 "+ Add" pills across the board). Client-only — no migration.
