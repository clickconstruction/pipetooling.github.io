# v2.2147 — Quickfill Billed Awaiting Payment: one row per bill (2026-08-23)

Found during the Quickfill walkthrough (owner: "fix the double count"). [`BilledAwaitingPaymentSection.tsx`](../../src/components/quickfill/BilledAwaitingPaymentSection.tsx) fetched `status='billed'` jobs and `status='billed'` invoices separately and pushed a **job-level row (revenue − payments_made) for every billed job AND an invoice row per line** — any billed job with a bill line (nearly all) counted twice, and Collections jobs were included. Live 2026-08-23: "122 lines · $488,035.34" against the Pipeline's 61 / $240,142.

- **Loader** now calls [`fetchStagesHeaderStats(null)`](../../src/lib/jobs/fetchStagesHeaderStats.ts) and reads `leanBilledRows` — the Pipeline's `billedActiveRows` (board kernel `buildBilledStageRows`: one merged row when a billed job has one line, invoice rows when it has more, job shell when none; Collections excluded) — then decorates with names (`get_jobs_ledger_by_ids`) + team members, which the lean spine lacks.
- **Kernel** [`quickfillBilledAwaiting.ts`](../../src/lib/jobs/quickfillBilledAwaiting.ts) (+ test pinning "one row per bill, shell once, no collections, total = Σ `stageRowBilledRemainingAmount`"; rows with nothing left to pay drop).
- Render unchanged (Job # · Job → Job Detail · Assigned · Remaining; "N lines · $X remaining"; View in Jobs Pipeline). Verified live: 60 lines · $240,142.48 — the Pipeline's total to the cent (its 61 includes one fully-applied $0 row this list omits).

Client-only.
