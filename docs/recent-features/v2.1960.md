# v2.1960 — Billed filter banner + money-move search clearing (2026-08-20)

Owner requests, both in [`JobsStagesTab`](../../src/components/jobs/JobsStagesTab.tsx). (1) The Billed section's filter-active notice ("Showing only 90+ day rows (N of M) · Show all", also the 30–90 and no-bill-line variants) was a muted left-aligned line — easy to miss, so a narrowed list could read as the whole section. It's now a full-width centered orange bar (`#f59e0b` — saturated status color stays literal — with `var(--text-on-amber-solid)` text; the "Show all" / "Fix bill lines…" links go dark-underlined to stay legible on orange). (2) The Pipeline New view's money-opportunity actions — Capable list, Show 90+, Accounts Receivable, Show them (no-bill-line), and the Waiting-on-customers card's breakdown — now `setStagesSearchQuery('')` before acting, matching the existing focusJob pattern ("a live search would filter out the row we're about to scroll to"); this also makes the Capable and Who-owes-what modals complete, since their rows come from the search-filtered board lists. Verified live: search seeded with a query cleared on Show 90+, banner computed orange/centered/full-width. Client-only — no migration.
