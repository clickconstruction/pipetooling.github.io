# v2.2121 — Roadmap task title editor wraps (2026-08-22)

Owner request: renaming a task from the Roadmap task card (Timeline / Plan / Map →
tap a task → ✏ Rename) edited in a one-line `<input>`, so a long title scrolled off
the right edge and you could only see the tail of what you were typing.

- [`ChecklistTechTreeTaskCardModal.tsx`](../../src/components/checklist/ChecklistTechTreeTaskCardModal.tsx):
  the inline title editor is now the shared [`AutoGrowTextarea`](../../src/components/AutoGrowTextarea.tsx)
  (`rows={1}`, grows with the text, no inner scrollbar). Behavior is otherwise unchanged:
  Enter saves (titles carry no newlines, so Enter is intercepted), Escape cancels, the ✓
  button submits the form; the ✓ now top-aligns beside a multi-line title. Render test
  updated to the textarea element type. Client-only — no migration.
