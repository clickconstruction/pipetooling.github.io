# v2.3060 — Per-job client reads use the sheet → job link (2026-09-07)

Conversion train PR 2 — section C of [`to-dos/sub-sheet-job-link-followups.md`](../../to-dos/sub-sheet-job-link-followups.md). Six readers, each now `.eq('job_ledger_id', jobId)` (or the sheet's own `job_ledger_id`) instead of an HCP text compare:

- [`useJobWorkOrderCoverage`](../../src/hooks/useJobWorkOrderCoverage.ts) — was `.ilike('job_number', …)`.
- [`JobChargesTimelineStandalone`](../../src/components/jobs/JobChargesTimelineStandalone.tsx) — was `.eq('job_number', hcp)`.
- [`JobFormModal`](../../src/components/jobs/JobFormModal.tsx) Edit Job sub-labor cost box — was a **full-table** `people_labor_jobs` read filtered client-side by HCP; the gate is now the job id, so a click-only job gets its cost too.
- [`useJobDetailSubLaborCost`](../../src/hooks/useJobDetailSubLaborCost.ts) (Job Detail profit band) — signature takes the job id; `laborJobMatchesHcp` retired from `jobProfitSummary.ts` (+ its tests).
- [`SheetStoryModal`](../../src/components/jobs/SheetStoryModal.tsx) — the sheet's `job_ledger_id` first (list lookup or `.eq('id')`), the number path kept only for a sheet with no link.
- [`offerNextStage`](../../src/lib/subs/offerNextStage.ts) — reads `job_ledger_id`; a sheet with no link has no job to offer on.

Client-only. No migration.
