# v2.2196 — "oldest N days" wraps as one unit (2026-08-23)

Owner screenshot: on phones the by-person row broke as "oldest 118 / days". The severity-colored span in [`Checklist.tsx`](../../src/pages/Checklist.tsx) gets `whiteSpace: 'nowrap'` so the phrase drops to the next line whole. Client-only.
