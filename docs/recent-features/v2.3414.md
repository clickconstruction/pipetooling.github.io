# v2.3414 — Sold in packs: the neutral unit for batches, and the words to match (2026-09-14)

Client only; no migration (`order_increment_unit` is free text — the kernel is the one reader). Owner, on the sticks train: "it isn't always sticks, sometimes it's batches … certain kinds of nuts in batches of 5 — did we cover these?" They were covered by *per box* / *per bundle*; this makes the batch case read right.

- **[`orderIncrement.ts`](../../src/lib/materials/orderIncrement.ts)**: a fifth unit, `pack` (*per pack* in the selects; chip *pack of 5*; sentence *packs of 5*), and the box / bundle sentences become plural (*boxes of 10*) to read as a rule, not a quantity. New helpers `orderIncrementPackWord` (sticks · coils · packs · boxes · bundles) and `orderIncrementIsFeet`.
- **`orderRoundingHover`** and the RFQ scope read the pack word off the unit — *13 needed on this bid → 15 (3 × 5 packs) · +2 extra*; *× 15 (13 needed · packs of 5)* — instead of the sticks wording (the scope had said "box of 10 packs").
- Guides: *price a takeoff in sticks* gains "It is not only pipe"; *add a part and its prices fast* lists per pack. Tests: +1 quote case, +1 hover case, the words.
