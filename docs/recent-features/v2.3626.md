# v2.3626 — Sign-in page: the ten More-apps links carry their marks (2026-09-19)

The owner, the day the marks were picked: *"I want all of these icons added to the landing page of ClickTooling."* Follows v2.3622 (the strip) and v2.3624 (the *More apps* disclosure, text-only). Client only; no migration, no edge function.

## The marks

Ten files in `public/tooling/` — `bidtooling.svg`, `plumbingtooling.svg`, `lientooling.svg`, `papertooling.svg`, `paytooling.svg`, `subtooling.svg`, `signtooling.svg`, `synctooling.svg`, `connecttooling.svg`, `govtooling.svg` — copied from the picks recorded in [`to-dos/tooling-icons/`](../../to-dos/tooling-icons/README.md) (`picks.json`; that to-do is PR #3448). House style: the `#e8c547` tile at `rx="112"` on a 512 box, `#161617` ink, two colours only, no `<text>`, images, filters or opacity. Two are from Todd's hand-off (plumbing, lien); eight were drawn in the picking sitting and are **not yet measured** by the hand-off's studio — if measuring changes a drawing, the file here is replaced with it.

## The change

- [`toolingFamily.ts`](../../src/lib/toolingFamily.ts): `icon` is now required on `ToolingApp`, and every `MORE_APPS` entry names its file.
- Six blurbs reworded by the owner the same day: *Plans count and measure* (CountTooling), *Electrical bid pricing* (Takeoff Tooling), *Liquid and gas test reports* (Plumbing Tooling), *Liens and releases* (LienTooling), *PDF stripper* (PaperTooling), *Certified payroll* (GovTooling).
- [`ToolingFamilyStrip.tsx`](../../src/components/ToolingFamilyStrip.tsx): each More-apps link is a row — a 26px mark, then the name and blurb. A **preview row** of the ten marks at 20px sits under the closed disclosure, as a sibling of `<details>` (content inside a closed `<details>` does not render), hidden by `.tooling-family__more[open] + .tooling-family__more-preview` once the list is open. Decorative (`aria-hidden`); the links keep their labels.
- [`authPublicLanding.css`](../../src/components/authPublicLanding.css): the link becomes a centred row; `tooling-family__mark--small`; the preview row.

## Tests

`toolingFamily.test.ts`: every app's `icon` is a unique `/tooling/<name>.svg` that exists under `public/`; the ten More-apps files are house marks (the tile rect, exactly the two colours, none of the banned elements).

## Verified

Dev server, signed out: the closed state shows the ten marks under *More apps 10*; open, the ten rows each carry their mark and the preview row is gone. Typecheck, lint, the suite above.

## Left

The rollout to each app's own repo (favicon set + head links) and the measurement pass — both in `to-dos/tooling-icons/`. Plumbing Tooling leaves this list when plumbingtooling.com retires (~2026-10-11, `to-dos/test-reports/`).
