# v2.2190 — Quickfill Billed Awaiting Payment → Who owes what (2026-08-23)

PR D of the Quickfill tune-up (mockup https://claude.ai/code/artifact/50741c3c-293e-4eae-a897-83076baad9aa, screen D). The v2.2147 fix made the flat table honest (one row per bill); this makes it actionable — the section now answers its new banner ("Who owes us, and who do we lean on first?") instead of listing 60 rows.

- [`BilledAwaitingPaymentSection.tsx`](../../src/components/quickfill/BilledAwaitingPaymentSection.tsx): rows from `fetchStagesHeaderStats(null).leanBilledRows` (the Pipeline's billed spine), decorated with `job_name` / `customer_name` / `customer_id` via a chunked `jobs_ledger` fetch, then grouped by the SAME kernel the Pipeline's WAITING ON CUSTOMERS modal uses — [`buildBilledByCustomerBreakdown`](../../src/lib/jobs/billedByCustomerBreakdown.ts) (v2.2130 age clock: hand-set est. date wins, else billed date, dot = hand-set).
- Render: Customer | Bills | Oldest | Owed, groups expandable to bills (oldest first) with **View in Pipeline →** (`?stagesInvoice=` / `?stagesJob=`); footer door `?stagesSection=billed`. Metric = bill count; subtitle carries "$X open · N customers". The interim `quickfillBilledAwaiting.ts` kernel (v2.2147) is superseded and removed — its "one row per bill" pin lives in `billedByCustomerBreakdown`'s own tests.
- Verified locally against prod data: "60 bills · $240,142.48 open · 23 customers"; Knight Contracting expands to 15 bills with 156d· leading.

Client-only.
