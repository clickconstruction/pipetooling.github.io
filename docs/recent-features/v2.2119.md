# v2.2119 — Count Sheet strip: plan pages + no-page count in one tile (2026-08-23)

Owner screenshot-annotation on the v2.2113 strip: the standalone red **No plan page** tile is struck out and **Plan pages cited** reads `1 (4 no pages)` with the parenthetical in red. [`BidsCountsTab.tsx`](../../src/components/bids/BidsCountsTab.tsx): the two tiles become one button — pages-cited count, plus a red `(N no page[s])` span only when `summary.noPageCount > 0`; the click-to-filter toggle (`sheetNoPageOnly`, `aria-pressed`) and the "Showing only rows with no plan page — click the tile again to show all" note are unchanged. Help guide wording updated. Client-only.
