# v2.2419 — Cover Letter copy trim (2026-08-28)

Owner request (2026-08-28): two captions in `BidsCoverLetterTab.tsx` said things the UI
already says.

- The single-GC explainer under the packet checklist ("Checked packets go in the letter,
  each at its ★ base price… Change prices on the Pricing tab.") is removed; the helper div
  now renders only for multi-GC bids (per-GC sentence + base/alternates summary) or the
  "Nothing checked" notice.
- The headline amount's " · Mark sent stamps this as the bid's value" suffix is removed —
  redundant with the Mark sent button's own caption ("…and sets the bid's sent date and
  value"), which is the one place that explanation now lives.
