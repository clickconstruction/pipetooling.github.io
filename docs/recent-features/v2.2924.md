# v2.2924 — Tier 4 developer docs: access matrix, edge functions, workflow, Crew P&L, migrations and surface index caught up to the Phase 4 merges (2026-09-06)

Journey map Phase 4, **Tier 4 row G22 — developer docs (C26)**. After the ~80 Phase-4 PRs of 2026-09-05/06, one docs-only pass to make the entry/specialist docs consistent with what shipped. No code, no migration, no edge function. Sibling PRs the same day: the role sweep (#28, v2.2920) owns the estimator People row and the controller Banking rows in `ACCESS_CONTROL.md` — deliberately not touched here; the three guide auditors own `src/content/help/*` and the money / bids / people glossary terms.

## Per-doc verdict

| Doc | Verdict | What was checked / changed |
|---|---|---|
| `docs/ACCESS_CONTROL.md` | **verified, no edit** | Superintendent project scope (v2.2836) + work-order scope (v2.2844), read-only Job pane for superintendent / estimator / controller (v2.2848), Roadmap page roles (v2.2916), training-at-invite (v2.2872), estimator `/customers` "money not shown" note (v2.2910), scheduled GC sends office-read (v2.2888) — every row present and consistent with the code. Re-checked after rebasing onto the role sweep (#2661, v2.2920): controller on Banking + every `jobs_ledger`-family policy, the estimator People door (nav line, Blocked line, redirection rule all agree), and the primary branch on the steps SELECT policy are all in #28's own hunks — nothing left for this pass to add. |
| `docs/EDGE_FUNCTIONS.md` | **fixed** | All 16 functions touched today have a section + TOC line and cite their Phase-4 version (`customer-portal` v2.2839/v2.2875/v2.2878, `sub-portal` + `get-bid-proposal-room` v2.2875, `accept-estimate` v2.2838/v2.2873, `get-estimate-for-customer` v2.2873, `send-estimate-to-customer` v2.2856, `create-stripe-invoice` v2.2846/v2.2878, `send-physical-invoice-email` v2.2846, `invite-user` + `create-user` v2.2872, `gc-statement-email-dispatch` v2.2874/v2.2888, `send-gc-statement-email` v2.2888, `notify-dispatch-request` v2.2880, `mercury-webhook` v2.2889, `get-rfq-quote-page` v2.2911, `sign-bid-room` v2.2838). **Missing and added:** the v2.2913 (B6) proration rule in the shared `stripeInvoiceItemsFromFixtures.ts` under `create-stripe-invoice` and its mirror note under `preview-stripe-invoice`, with the redeploy-together warning. |
| `docs/WORKFLOW_PAGE_ARCHITECTURE.md` | **fixed** | B21 (v2.2900) had already recorded the assignable roster (`WORKFLOW_ASSIGNABLE_USER_ROLES`, `buildWorkflowUserRoster`) and the notify defaults (`notifyAssignedDefaultsOnAssign`) in the StepFormModal and roster sections. Added the J31-N4 pointer: step readability is the `project_workflow_steps` SELECT policy's business — no primary branch until the role sweep (v2.2920, `20260906010000_role_sweep_predicates`) added one, route still off — and `ACCESS_CONTROL.md` is authoritative for the policy's branches. |
| `docs/CREW_PNL_DATA_FLOW.md` | **verified, no edit** | Dev-only tab statement (J8-D1), estimate-led rows + seam divider (`crewPnlRowIsEstimateLed`, v2.2912), loose name resolver, jobs-universe cache notice (`crewPnlJobsUniverse`), explicit-save sub rate (v2.2897) — all present. |
| `src/lib/bids/bidGcRecipients.ts` header | **verified, no edit** | B1 (v2.2905) rewrote it: outcome is per GC packet (`bid_versions.outcome`), `bids.outcome` is the roll-up (J14-F7). |
| `docs/MIGRATIONS.md` / `docs/migrations/` | **fixed (headers only)** | `MIGRATIONS.md` is frozen (2026-08-20); every 2026-09-05/06 migration has a fragment (20260905 090000 100000 110000 120000 130000 163807 170000 180000 190000 200000 210000 220000 230000 233000; 20260906 000000, plus 010000 from the role sweep and 030000 from v2.2922, both landed on main with their own fragments). The first four (Tier-1 #1 #4 #9 #4b) carried a bare `# <version>_<slug>` heading; brought to the README's `# <file>.sql (date, vN)` form. No fragment was missing. |
| `docs/PROJECT_DOCUMENTATION.md` | **fixed** | `/roadmap` route was already listed. Added one clause each: Data & recovery → **Draft bills on paid jobs** sweep (v2.2846); GC Review **Scheduled sends** / **What went out** (v2.2888) under Jobs; Materials **Price coverage** (v2.2903) under Materials and on the `public.get_supply_house_price_counts()` function row (the DB function's name did not change with the modal's rename). Edge-function count **57 → 104** (from `npm run check:edge-drift`). |
| `docs/GLOSSARY.md` | **fixed** | Farm Mode chip (v2.2916), Close week chip (v2.2898), Roadmap page were already there. Added **Draft bills on paid jobs**, **Scheduled sends / What went out (GC Review)**, and a **Price coverage** paragraph on the Supply House entry (plus the `is_insurer` picker flag, v2.2893). |
| `docs/journeys/` leak check | **clean** | No file, dossier text, or findings table from the private map repo lives under the public `docs/` or `src/content/` — there is no `docs/journeys/` directory and no `_FINDINGS.md` here. What does appear (14 `docs/recent-features/` fragments from 2026-09-04/06) are one-line provenance citations of the form "`pipetooling-journey-map` → `docs/journeys/<dossier>.md` F1" or "journey-map Tier-N #N" — pointers, not content, and within the allowed citation form. |

## Edge-function drift check (2026-09-06)

`npm run check:edge-drift`, run after rebasing onto the role sweep (v2.2920) and v2.2922:

```
warning: supabase/functions/_shared/ was edited 2026-09-06 after some functions last deployed — shared code is bundled at deploy time, so importers may be stale.

EDGE FUNCTION DRIFT: 2 function(s) were edited in the repo AFTER their last deploy (grace 72h — prod runs older code):
  - get-mercury-account-balances  (repo edit 2026-09-06, deployed 2026-06-03)
  - mercury-reconcile  (repo edit 2026-09-06, deployed 2026-06-04)
```

Both are #28's `ALLOWED_ROLES` widening (controller added to the Banking gate) — until they are deployed, the Balance Sheet cash line still 403s for controllers. Earlier the same morning (before v2.2920 merged) the script reported `OK: all 104 repo functions are deployed and current` with only the `_shared` warning; the repo holds 104 function directories, which is the count now written into `PROJECT_DOCUMENTATION.md`. Resolving the `_shared` warning by hand (importers of every `_shared` file changed since 2026-09-05 noon vs. each importer's deployed `updated_at`):

| `_shared` module (last commit) | Importers | Deployed after the edit? |
|---|---|---|
| `stripeInvoiceItemsFromFixtures.ts` (09-06 02:53Z, v2.2913) | `create-stripe-invoice` ✅ 03:06Z · **`preview-stripe-invoice` ❌ (09-01)** | **stale** — the Stripe preview composes the primary remainder the old way while the bill uses the new proration |
| `logEstimateCustomerEvent.ts` (09-05 21:34Z, v2.2873) | `accept-estimate` ✅ · `get-estimate-for-customer` ✅ · **`log-estimate-option-view` ❌ (09-03)** | **stale** |
| `customerSampleFixtures.ts` (09-05 17:14Z) | `customer-portal` ✅ · `get-bid-proposal-room` ✅ · `sub-portal` ✅ · `get-estimate-for-customer` ✅ · **`get-contract-for-signer` ❌ (09-04 23:36Z)** | **stale** (twin sample fixtures only) |
| `gcStatementSendDedupe.ts`, `accountingLabelAutoApprove.ts`, `customerPortalReturnUrl.ts`, `stripeInvoiceFooterPortalLink.ts`, `estimateDecline.ts`, `publicViewCounting.ts`, `roleLabels.ts`, `estimateLinkResend.ts` | all importers | ✅ current |

Follow-up for the deployer: `supabase functions deploy get-mercury-account-balances mercury-reconcile` (the script's two reds), then `preview-stripe-invoice`, `log-estimate-option-view`, `get-contract-for-signer` (the stale `_shared` importers the script cannot see).

## Findings for the app (not fixed here)

- Migration fragments `20260906000000_bid_room_tables_deleted_records.md` and `20260906030000_public_page_views_viewer.md` are dated 2026-09-05 in their headings while the filenames say 09-06 (both applied after midnight CDT) — cosmetic, but the README's heading form reads the date as the applied date.
- `npm run check:edge-drift` reports "current" per function directory only; an edit to `supabase/functions/_shared/*` never turns an importer red. Three importers are stale right now (table above) and the script cannot see it — worth mapping importers and comparing each one's deployed `updated_at` against the shared file's last commit.
- `docs/ACCESS_CONTROL.md` frontmatter still reads `last_updated: 2026-09-05` after #28's 2026-09-06 edits — cosmetic; left for #28's owner rather than touching that file from a second PR.
