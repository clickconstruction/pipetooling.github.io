# v2.2634 — My Bids loader stops fetching for superintendents (2026-09-02)

Resolves DASHBOARD_SECTIONS_ARCHITECTURE quirk #8 (preserved verbatim since the v2.718 extraction): `DashboardMyBidsSection`'s loader `hasBidsAccess` included `superintendent` while the render gate and dock entry excluded it — so every superintendent dashboard load ran the bids + read-state + submission-entries + customer-contacts query chain for a section that never rendered. The loader now matches the render gate (dev / master_technician / assistant-like / estimator / primary). No behavior change for any role that could see the section; quirk notes in the architecture doc flipped to RESOLVED.
