# v2.2666 — Email-wording docs catch-up + help guide (2026-09-03)

Docs-only PR closing the documentation gaps from the email-wording stream (v2.2656–v2.2662):

- **`docs/EDGE_FUNCTIONS.md`**: new top-level **Email Wording Overrides** section (+ TOC) — `_shared/emailWordingServer.ts` semantics (fail-soft, provided-keys-only substitution, `{{default_subject}}`), the 9+1 digest adopters, the client-composed pair (lien release / hazmat via `src/lib/emailWording.ts`), and `email_send_log.email_type` stamping through `_shared/logEmailSend.ts` / `sendResendHtmlEmail`.
- **`docs/ACCESS_CONTROL.md`**: entry for `email_templates_authenticated_read` (v2.2658, migration `20260902184612`) — table went dev/owner-read → all-authenticated-read (wording only, no secrets); writes and training/twin fences unchanged.
- **`docs/PROJECT_DOCUMENTATION.md`**: `email_templates` table entry refreshed (25 types, new RLS, catalog + preview UI pointers) — it still said "11 template types, dev-only RLS".
- **New help guide** `src/content/help/change-the-wording-of-an-outgoing-email.md` (dev role): catalog → edit → preview/test flow, digest subject+intro scoping, wording-only boundary.
