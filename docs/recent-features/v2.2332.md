# v2.2332 — Invoice PDF payment history framed in a card (2026-08-26)

Owner-approved Option A mockup:
claude.ai/code/artifact/d269f0f8-b626-4b29-a956-f792dfa7fa1f.

- `physicalInvoicePdf.ts` `drawPaymentHistory`: the v2.2324 ledger rows
  (Billed / green credits / red Balance due) now draw inside a right-aligned
  black-bordered card (88mm, heading inside, labels start at the card's
  edge) — matching the preview's and email's bordered box. The card is drawn
  atomically: height is estimated up front and the whole card moves to a new
  page rather than splitting across a break. Line width restored to the
  jsPDF default afterwards.
- No other surface changes; both PDF layouts (detailed + simple) share the
  function.
