# v2.2918 — Field papercut batch B11: "Ask estimating" everywhere, standing guidance when nothing is scheduled, "You are here" in Update Focus (2026-09-05)

Journey map Phase 4, **Tier 3 row B11** (`JOURNEY_MAP.md` → "Field — Job Mode, Clock In/Out, header"; dossiers J2 tech-works-a-day, J30 checklist-daily). One PR, client only — no migration, no edge function. The B11 items that shipped earlier today ride their own PRs and are not here: report modal current % (#5b, PR #2602), offline Retry (#6b, PR #2600), Job Mode Inbox push-log (#24+25, PR #2622), Job Mode default (#26, PR #2620). The clock-out four-screen stack (J2-F6) stays parked in Tier 4 behind decision #1 (training account).

## Item by item

| # | Finding | What changed |
|---|---|---|
| 1 | J2-F7 · J30-N3 · J30-adj-1 (C38) | **Done.** `HEADER_ASK_ESTIMATING_LABEL = 'Ask estimating'` in [`headerTaskDispatchEstimatorEligible.ts`](../../src/lib/headerTaskDispatchEstimatorEligible.ts) is the `title` + `aria-label` on both header pairs in `Layout.tsx` — the desktop text button (`estimating`) in the Job Mode Contact row and the mobile purple pencil icon. The visible `estimating` text is unchanged. New [`headerTaskDispatchEstimatorEligible.test.ts`](../../src/lib/headerTaskDispatchEstimatorEligible.test.ts) reads `Layout.tsx` and asserts the constant sits on exactly two title + two aria-label attributes and that no `Estimator Inbox` literal remains in the file. `showEstimatorInboxButton` keeps its name (the Quickfill physical-inbox tile and the Settings People "Estimator Inbox group" copy describe the real office inbox and are untouched). |
| 2 | J2-F8 | **Done.** The one-shot "No quick picks from your job assignments or Dispatch schedule for this day." info toast and its `noAssignedJobsInfoToastShownRef` are gone. The B2 quick-picks effect now sets `quickPicksEmpty` from [`clockInQuickPicksEmpty`](../../src/lib/clockInQuickPicksEmptyState.ts) (assigned ∧ dispatch ∧ working-board all empty). The Clock In sheet renders `renderClockInNoPicksGuidance()` — an amber `role="status"` panel above the search row: **Nothing on your schedule today, and no jobs assigned to you.** / *Search by job name or number below, or call dispatch at `<tel link>` and they will get you on a job.* — gated by `showClockInNoPicksGuidance` (hidden while loading, while typing, or once a job/bid is chosen). The number comes from `useFirstAssistantDispatchPhone(clockInModalOpen)` — the same source as My Schedule's "Schedule wrong? Call dispatch" button — fetched only while the sheet is open; with no number on file the sentence still says "call dispatch". 7 tests. |
| 3 | J2-F9 | **Done.** New kernel [`scheduledPicksYouAreHere.ts`](../../src/lib/scheduledPicksYouAreHere.ts): `orderScheduledPicksCurrentFirst(picks, currentJobId)` puts the pick matching `openSession.job_ledger_id` first and flags it; all other picks keep the incoming (job-number-desc) order; Clock In passes `null` (no open session) so nothing moves. `renderScheduledDispatchPicks` renders the flagged row with a green pill **You are here** (`--bg-green-tint` / `--border-green` / `--text-green-700`), `aria-current="true"`, and a "You are here — …" tooltip. The selected-pick style for the Update Focus and Clock Out review sheets (`focusOrReviewSelectedPickStyle`, shared by the dispatch and working-board pick lists) is now blue tint (`--border-blue` / `--bg-blue-tint`) instead of grey `--bg-muted`, matching those sheets' own search-result selection. 6 tests. |

## Files

Kernels: `src/lib/scheduledPicksYouAreHere.ts` (new, +test) · `src/lib/clockInQuickPicksEmptyState.ts` (new, +test) · `src/lib/headerTaskDispatchEstimatorEligible.ts` (+new test).
Components: `src/components/Layout.tsx` (two attribute pairs + import) · `src/components/ClockInOutButton.tsx` (B2 effect, `focusOrReviewSelectedPickStyle`, `renderScheduledDispatchPicks`, `renderWorkingBoardBidPicks`, new `renderClockInNoPicksGuidance`).
Docs: `CLOCK_SURFACES_ARCHITECTURE.md` (B2 / B3 / B4, quirk #1), `ACCESS_CONTROL.md`, `PROJECT_DOCUMENTATION.md`. Help: `job-mode-clocking.md`.

## Verify

1. Any role that sees the header buttons, at 375 px: long-press / inspect the purple pencil — tooltip and accessible name read **Ask estimating**; widen past the Job Mode Contact-row breakpoint and the `estimating` text button carries the same title. Protected first screen (Clock In on top, My Schedule, Call dispatch) unchanged.
2. Helper with no Dispatch blocks, no job assignments, no working-board bids → **Clock In**: the amber panel appears under "What job or bid should we change for your time?" and stays; the phone number dials; typing in the search box hides it; clearing the box brings it back; picking a result hides it. No info toast fires.
3. Same helper with blocks today: no panel; picks render as before.
4. Clocked-in tech with 3 blocks today → **Update Focus**: the current job is the first row, blue-tinted, with the green **You are here** pill; the other two rows follow in the old order and are tappable. Clock Out review shows the same ordering.
5. `npm test` — `headerTaskDispatchEstimatorEligible.test.ts` fails if either Layout pair regresses to a literal.
