# v2.2965 — chore(types): regenerate `database.ts` after the Tier 5 push (2026-09-06)

`npm run gen-types:linked` against prod after the six-migration push of 2026-09-06 (Tier 5's `20260906130000`–`170000` plus v2.2954's `180000`). Replaces the hand-typed blocks the Tier 5 PRs carried (`mercury_reconcile_runs`, `identity_aliases`, `org_defaults`, `user_dismissals`; RPCs `move_bid_schedule_blocks_to_job`, `is_office_staff`) and picks up other same-day schema the generator had not seen yet (`bids.holdout`, `job_stage_windows` columns, `sub_portal_visits` + its summary, `record_estimate_decline`, the accounting-label suggestion counters, `schedule_hidden_block_counts`, `superintendent_can_access_sub_work_order`). +206 / −123 lines; the file ends cleanly (the truncation gotcha checked); `tsc -b` green. The `'name' as never` RPC casts in the Tier 5 code can be dropped in a later tidy-up; they still compile.

Generated file — regenerated, never hand-merged: on a rebase conflict take main's copy and re-run the generator.
