# v2.2064 — Portal modal strip v2: statement mirror + pay links + preview controls (2026-08-21)

Owner iteration on v2.2054 ("Portal Job Shortcuts" mockup v2):

- **Statement-mirror rows**: `buildStatementJobLinks` (grouped, per-job,
  summed) → `buildStatementBillRows` in
  `src/lib/portal/portalStatementJobLinks.ts` — ONE ROW PER BILL, statement
  order preserved with duplicates, each row `date · trade · number · amount`.
  The v2.2054 per-job grouping was already in statement order, but the
  two-column chip wrap hid the correspondence; a dated column list makes it
  unmistakable.
- **Pay ↗** anchor per row (`hosted_invoice_url` from the payload's bills)
  opening that bill's Stripe pay page; italic "no pay link" placeholder keeps
  check-only rows aligned.
- **Preview controls** (header right, `CustomerPortalGlobeButton.tsx`):
  ⤢ Expand toggles the live preview 300px/0.62 → 560px/0.85 in place;
  **Full screen ↗** opens the portal page (token URL) in a new tab.
- Kernel tests reworked; help guide updated.
