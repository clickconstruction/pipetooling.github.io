# v2.2012 — Pipeline: the money view becomes the only view (2026-08-21)

Owner call, same shape as the Cover Letter retirement (v2.1895): "we can now keep the new and get rid of the old." The Old/New pills (v2.1915), the per-device `jobs_pipeline_view_v1` preference, and every `pipelineView` branch are removed from [`JobsStagesTab.tsx`](../../src/components/jobs/JobsStagesTab.tsx) — [`PipelineOverview`](../../src/components/jobs/PipelineOverview.tsx) (money story cards + Today's Money Opportunities) now renders unconditionally above the unchanged jump strip + board.

- **Old-only duplicate deleted**: the toolbar's red/amber data-gap alert strip (No customer / No customer pictures / No email, ~85 lines) — the New view already rebuilt these as the money card's Fix-ups strip (v2.1961), wired to the same modals. Its three hover states go with it.
- **Exposure widens by default**: Old was the per-device default, so primary and superintendent (who can reach `/jobs`) now see the money card without flipping a pill. Internal gating is unchanged — AR button `canOpenAr` (dev/master/assistant-like/primary), charts dev/controller, Collected card dev/master — and every figure comes from the RLS-scoped board rows the viewer already sees.
- The stale localStorage key is abandoned in place (Cover Letter precedent); Counts (`bids_counts_view_v1`) and Pricing Workbench Old/New pills are separate surfaces and stay.
- Help guide [`read-the-pipeline-money-view.md`](../../src/content/help/read-the-pipeline-money-view.md) retitled ("read the Pipeline's money view") and its two-views intro rewritten; comment sweep drops "New view" phrasing from `pipelineOverview.ts`, `stagesHeaderStats.ts`, `billedByCustomerBreakdown.ts`, `invoiceBilling.ts`.

Verified from a worktree dev server: no pills, money card renders directly on load, board/jump strip unchanged, fix-up chips open their lists. Client-only — no migration, no edge functions.
