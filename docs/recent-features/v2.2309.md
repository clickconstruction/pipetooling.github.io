# v2.2309 — Drill-down rows: sent → paid + line items (2026-08-25)

Owner asks on the Data health drill-down (mockup round two:
https://claude.ai/code/artifact/010cc1af-5227-4f3e-9c6e-955cdfb46ce5).

- **Migration `20260826010000`** (details in `docs/migrations/`):
  transactions RPC v3 adds `sentYmd`; new `get_payment_line_items` returns
  the fixtures behind one payment (bill's lines when linked + `billAmount`,
  job's lines as context when unlinked).
- **Rows** render `sent → paid` (muted sent) once `sent_on` exists; hover
  title spells it out.
- **Tap a row** → lazy-loaded line-items panel (cached per payment): name ×
  count — description, per-line amount, bill total when linked; honest
  captions for unlinked ("the job's line items, for context") and empty
  cases. Exclude / Open job stopPropagation so the row toggle doesn't eat
  them.
- Kernel: `sentYmd` parse + `parsePaymentLineItems`; new
  `PaySpeedDataHealthModal.render.test.tsx` (sent → paid rendering, linked
  and unlinked expansions).
