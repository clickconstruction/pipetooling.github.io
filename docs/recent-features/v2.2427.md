# v2.2427 — twin-login edge function (Phase E1) + estimator-only twin program (2026-08-28)

`docs/DIGITAL_TWINS_PLAN.md` gains the owner's 2026-08-28 decisions (estimator-only fleet,
one-directional write-fence with humans never fenced, assignment-is-the-grant, the Phase E
cloud plan and three safety rungs), and Phase E1 ships:

- **`supabase/functions/twin-login`** — dev-login's sibling for cloud harnesses against
  the DEPLOYED app: POST with `X-Twin-Login-Secret` → magic-link `action_link` → headless
  browser navigates to it. Four guards: own secret (`TWIN_LOGIN_SECRET`), fleet email
  pattern, `users.is_digital_twin = true`, role `estimator`. Mints log to the function
  log + a fail-soft `twin_runs` insert (table lands in E2).
- `config.toml`: `[functions.twin-login] verify_jwt = false` (secret-header auth).
- `docs/EDGE_FUNCTIONS.md` section + TOC row.
- **Deploy note**: `supabase functions deploy twin-login` +
  `supabase secrets set TWIN_LOGIN_SECRET=…` after merge (edge functions are manual).
