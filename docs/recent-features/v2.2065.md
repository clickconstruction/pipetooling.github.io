# v2.2065 — Required reason on RTB→Working send-backs, surfaced to the crew (2026-08-21)

Field-report investigation follow-up (Abraham, 2026-08-21): the office reverts Ready to bill → Working with no reason, no note, and no notification — from the tech's phone, their 100% report simply "didn't stick", and they redo Update % done. The send-back confirms even had an "I am going to call the Subcontractor and explain" checkbox, but the promise was never captured anywhere.

- **Kernel** [`jobSendBackNote.ts`](../../src/lib/jobs/jobSendBackNote.ts) (+ tests): `Sent back to Working — <reason>` note body compose/parse (same fixed-prefix pattern as the `N% complete` notes), `sendBackReasonError` validation (non-empty, ≥5 chars), and `sendBackLineForCard` (show while the job is Working and the send-back is ≤14 days old). IO wrapper [`postSendBackReasonNote.ts`](../../src/lib/jobs/postSendBackReasonNote.ts) rides `postJobThreadNoteBody`.
- **Office side** — required reason field ([`SendBackReasonField`](../../src/components/jobs/SendBackReasonField.tsx), shared) on the Send Job Back confirms in [`JobsStagesTab`](../../src/components/jobs/JobsStagesTab.tsx) and [`DashboardBillingPipelineSection`](../../src/components/dashboard/DashboardBillingPipelineSection.tsx); confirm stays disabled until valid. [`JobStatusStepper`](../../src/components/jobs/JobStatusStepper.tsx) intercepts the RTB→Working pill with an inline reason row (Collections-flow idiom). After a successful move the reason posts as a thread note (best-effort — failure toasts "add it in Job activity"). `stagesHamMode`'s direct flip deliberately keeps its no-confirm behavior.
- **Crew side** — [`DashboardMyScheduleSection`](../../src/components/dashboard/DashboardMyScheduleSection.tsx) gains `useMyScheduleSendBacks`: newest send-back note per scheduled job (with author name via the users embed), rendered as an amber "↩ Sent back by <name> — <reason>" line on the card. Additive: roles whose RLS hides notes/authors just get no line/no name.

Verified live on the Pipeline board (Shearer Pinpoint): reason field renders, confirm disabled until checkbox + ≥5-char reason, short reason shows the hint; Edit-tab stepper's Working tap opens the reason row without moving the job. Client-only — no migration. Push/email notification to the tech is a possible follow-up (would ride the notification streams infra).
