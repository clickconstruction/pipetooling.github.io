# v2.2004 — Count Sheet: quick add collapsed behind a button (2026-08-21)

Owner request: the quick-add panel (chips + count/fixture/page inputs, v2.1909) took the top of the Count Sheet before the rows. New `qaOpen` state in [`BidsCountsTab.tsx`](../../src/components/bids/BidsCountsTab.tsx) (default false): collapsed, the panel renders as a single "+ Quick add" outline button; expanding focuses the count input (`requestAnimationFrame` + existing `qaCountRef`), and a "Hide" text button on the panel's header row collapses it again. No other quick-add behavior changed (chips, Enter-keeps-going, duplicate merge). Help guide `count-with-the-count-sheet.md` gains a line on the toggle. Verified live both directions on BP335. Client-only — no migration.
