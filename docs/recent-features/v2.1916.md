# v2.1916 — Pipeline New view polish: collected card dev/master-only, queue sum removed (2026-08-20)

Owner feedback on v2.1915, same day:

- **Collected card gated** — `buildPipelineMoneyStory` gains `{ includeCollected }`; `PipelineOverview` passes `canSeeCollected = dev || master_technician` (new prop, threaded from `JobsStagesTab`). Other Pipeline roles see the three billing cards; the collected card was the one wage-adjacent cash-flow figure on the strip.
- **Queue header sum removed** — the "Today's money moves → $146,972" figure (Σ of the capable + 90+ dollar amounts named in the moves) read as unexplained noise; `buildPipelineMoneyMoves` now returns the moves array alone and the header renders without a total.

Tests updated (8 in `pipelineOverview.test.ts`, including the new includeCollected filter case); help guide notes the collected card's roles. Client-only.
