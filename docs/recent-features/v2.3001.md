# v2.3001 — Edit Job labor panel: "Team Labor" heading → "Team" (2026-09-07)

Owner, on the post-deploy walkthrough: "Yes, rename it to Team." Client only.

- `JobFormLaborCostPanel` heading **Team Labor → Team** (the in-house line above Sub Labor; hours · cost · people and the **Open on Jobs →** link unchanged).
- `roleGate.ts` `team-labor` subject **Team Labor → Team** (the "not for your role" message for `?tab=combined-labor`); test updated.
- Left alone on purpose: Job Summary's **Team Labor Cost** column and the cost-breakdown document's **Team Labor** heading — those name the cost concept (the team's labor, vs Sub Labor), not the tab.
- **Also** (found retesting v2.2996 on prod): after the deep link jumped weeks, the `?teamLaborJob=` param stayed in the URL — the focus effect's cleanup cancelled the 2.5 s consume timer when the board rebuilt (org defaults arriving). The timer is no longer tied to the effect's cleanup; the param is consumed after the flash as before.
