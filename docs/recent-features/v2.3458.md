# v2.3458 — Pricing: "What the bid is made of" moves below "Where the profit lives" (2026-09-15)

Client only; no migration, no edge function. Owner request 2026-09-15: "on Bids, Pricing, move the *What the bid is made of* section to below the *Where the profit lives* section."

## What shipped

- [`BidsPricingTab`](../../src/components/bids/BidsPricingTab.tsx): the `PricingCompositionBar` mount (v2.3239) moves from directly under the sticky summary strip to directly after the *Where the profit lives* card, at the foot of the Workbench. The bar's props, the kernel and the component are untouched; the order of everything else (summary strip → margin history → coverage → the table → profit bar) stays.
- `docs/BIDS_SYSTEM.md` → "What the bid is made of" names the new position.

## Verified

Typecheck, lint, the pricing-tab render smoke and the two bars' kernel tests, theme check.
