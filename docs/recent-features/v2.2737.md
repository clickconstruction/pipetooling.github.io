# v2.2737 — Help guide recording: move a schedule block by thumb (2026-09-03)

Follow-up to v2.2736 (PR #2472): the owner wanted a GIF of the two phone gestures to share with Taunya.

- New `public/help/schedule-dispatch-move-by-thumb.gif` (phone width, 500×1000, ~0.5 MB, 8 keyframes at 1.6–2.4 s each) embedded in `src/content/help/schedule-dispatch.md` under "Move a block from your phone" via `{{gif:…}}`.
- Captured with Playwright (390×780, `isMobile`, `themeOverride=light` via `addInitScript`) driving the local dev server signed in as dev, screenshots at scripted moments assembled with ffmpeg (`fps=4`, palettegen/paletteuse). The take moved Isiah's Thursday J878 block to Friday with the grip tap and back to Thursday with the Move sheet, so the schedule ended exactly where it started (verified in the DB after the take). Recipe + gotchas logged in `docs/HELP_MEDIA_PLAN.md` (status table row).
- Gotchas met on the way: the dev-login `to=` param must be URL-encoded or `&day=` is stripped; the board's grip lookup has to be scoped to one person's row and one day column (several people carry the same job number); a chosen target day must be free for that person — the group-move RPC refuses overlaps with the same "overlaps another block" message the drop shows.

No code change to the app.
