# v2.2197 — Jobs → Reports refresh: the feed (2026-08-23)

Owner: "give the job reports page a refresh while keeping it dead simple… one of its primary
viewports is mobile." Option A from the mockup (claude.ai/code/artifact/ce07da5e-70bd-4d20-b5f1-5941c80ea9ce),
approved with one amendment: **"Read report ›" sits bottom-right of every card.**

- New kernel [`reportsFeed.ts`](../../src/lib/reports/reportsFeed.ts) (+4 tests): day labels
  (Today / Yesterday / "Fri, Aug 21" / year when old), short times, `groupReportsByDay`, and
  `reportPreviewLines` (first 2 non-empty fields, 90-char clip, signature data-URLs read
  "✍ signed").
- New [`JobsReportsListView.tsx`](../../src/components/jobs/JobsReportsListView.tsx): the
  **Newest** feed (default) — day-grouped readable cards (template chip · job · Jnnn · by whom ·
  short time · two preview lines · Read report › bottom-right); tapping anywhere opens the full
  report in the existing `ReportViewModal`. **By job / By person** stay as restyled group cards:
  name + quiet subtitle ("J473 · last report Yesterday · Files not linked yet"), count right,
  and the four glyph buttons replaced by labeled **Files / Pictures / Edit job / Preview**
  buttons inside the open card (missing link = muted + dashed, toast on tap — never red).
  Dev-only Delete stays on the group rows.
- [`JobsReportsTab.tsx`](../../src/components/jobs/JobsReportsTab.tsx): toolbar reworked —
  full-width thumb-height **＋ New report** on phones, search, Newest/By job/By person chips;
  Recurring Email Reports + Templates tuck right (small links on phones). The whole tab caps at
  a centered 860px column so desktop reads like the phone.
- Same RPC, same rows, same permissions — presentation only. Verified at 375 / 768 / 1280:
  feed, card → full report, By job actions, By person groups.
