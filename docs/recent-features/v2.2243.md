# v2.2243 — HR Report person list fills the viewport (2026-08-24)

Owner feedback: the picker's 140px list showed ~4 names.

- `SearchableSelect` gains opt-in `fillViewportHeight`: when the list opens
  downward, its max-height grows to the viewport bottom (8px gutter), never
  below `listMaxHeightPx`; the flip-above fallback keeps the fixed cap. The
  dynamic height rides on `ListPosition` and recomputes on scroll/resize.
- `DashboardPersonReportCard` opts in.
