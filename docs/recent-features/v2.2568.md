# v2.2568 — Follow-ups corner badge + rtb deep-link re-pin (2026-09-01)

Owner asks: (1) the Follow-ups count should look like a notification badge on
the button's top-right corner, not an inline pill; (2) the assistants'
"ready to bill — send them" bar only landed on Jobs, never on the Ready to
Bill section.

- **Follow-ups badge** — `JobsStagesTab` toolbar: the button is wrapped in a
  `position: relative` span; the count moved out of the button content into an
  absolutely-positioned badge at `top/right: -0.5rem` with a 2px
  `var(--bg-page)` ring (works in both themes). `pointerEvents: 'none'` keeps
  the whole hit area on the button; the badge is `aria-hidden` — the button's
  `aria-label` already carries the count.
- **`?rtb=1` deep link fix** — the v2.2276 scroll waited for two layout reads
  300ms apart to match, but the page is also perfectly still *while the board
  query is in flight*, so it scrolled early and the Waiting/Working sections
  then filled in and pushed Ready to Bill back below the fold. The poll now
  keeps running after the first scroll and re-pins whenever layout settles
  with the section >40px from the viewport top; it stops once the section
  holds at the top (budget raised 50→100 ticks, still 300ms apart).
- Verified via in-page reproduction (dev login, `/jobs?rtb=1`): section
  expanded and pinned at viewport top after the board grew; two
  `scrollIntoView` calls observed (initial + one re-pin).
