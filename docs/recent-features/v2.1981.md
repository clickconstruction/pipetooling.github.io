# v2.1981 — Partner lens: statement action row hidden (2026-08-21)

Owner request: the lens's "Print / save PDF · Acknowledge statement (disabled) · the partner sees this button" row doesn't help the office. [`DashboardPartnerLedgerSection`](../../src/components/dashboard/DashboardPartnerLedgerSection.tsx) now skips the whole statement-actions row in lens mode (`asPartnershipId` set) — including the "waiting on your acknowledgment" partner-voice reminder; acknowledgment state remains visible office-side on the Statements tab's chips. The partner's own dashboard keeps Print + Acknowledge unchanged (§9b). The v2.1942 disabled-button treatment this replaces is retired. Client-only — no migration.
