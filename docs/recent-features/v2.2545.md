# v2.2545 — Reference grading: the sparse-era accommodation (2026-09-01)

Survey (2026-08-31, 344 human bids): 132 Tier-A (127 of them since March), 57 B,
34 C, 23 D, 98 with no plans (20 otherwise rich); only ~14 won Tier-A refs; 60 of
111 losses uncategorized; 95 round-to-$100 values.

- **Kernel** `src/lib/bids/referenceGrade.ts` (5 tests): `referenceGrade`
  (A/B/C/D/X from field presence) + `referenceQualityFlags` (roundValue,
  weakLoss = no_bid/project_died, lossUncategorized, stale >6mo, gateEligible).
  Blindness split: grade = presence-only (safe at open); flags read sealed
  fields (unseal-time only).
- **twin-mcp v1.3.2** (deployed): `open_backtest` returns `reference_grade` +
  `grade_note`; the STG-6 next-step text tells the twin to stamp grade+flags on
  the scorecard. Gate rule codified in FEEDBACK_LOOP.md: **gate denominators
  take only grade A/B, all-flags-clear references**.
- **Migration `20260901030000_digest_reference_quality`**: fifth
  `digest_outcome` bucket `reference_quality` — a disagreement judged to be the
  reference's fault files a repair task on the human bid (human approves the
  record change) instead of mis-teaching doctrine.
- Repair lists (20 plans-missing-but-rich, 19 value-missing, 60 uncategorized
  losses) delivered as a working artifact for the owner/estimator.
