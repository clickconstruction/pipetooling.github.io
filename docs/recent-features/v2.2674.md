# v2.2674 — Overhead lens modals: the math, the history, the levers (2026-09-03)

Owner ask: audit Methods A/B/C and make each lens clickable — explain the arithmetic, what moves it, and chart it over 90 days. Mock-up approved first ("Overhead Lenses, Explained" artifact), then built.

## Audit result (no math change)

The rate kernel (`overheadRateMethods.ts`) is pool ÷ denominator; correctness lives in the tab's 90-day effect. Verified: 90-day window anchored on the company calendar (`denverCalendarDayKey`, `start = today − 89`); pool = office labor + bid labor + office parts with internal transfers excluded; Method B revenue = invoices *sent* in-window, Chicago-bucketed, Stripe test-mode excluded; unapproved/rejected/revoked/open sessions excluded everywhere; zero-activity days count. **One structural edge found**: a session on a non-office job that also carries a `bid_id` is pulled by BOTH the office/bid query (`bid_id.not.is.null` → bucket `bid`, numerator) and the field query (`job_ledger_id` non-office, no bid exclusion → A/C denominators). Measured on prod: **zero such sessions** in the current window, so no rate is wrong today. Left as-is (the Review tab shares the classifier; changing accounting semantics is an owner call) and surfaced in every modal's Watch-outs with the live count.

## Client only — no migration

- Kernel [`lib/overheadLensSeries.ts`](../../src/lib/overheadLensSeries.ts) (+6 tests): `buildOverheadLensSeries` bins 7-day weeks back from the window end (oldest bin short, `days` says so) with each bin's own pool ÷ denominator, plus a rolling 28-day rate per day; `overheadLensSensitivity` (∂rate/∂pool = 1/d, ∂rate/∂denominator = −pool/d²) and `overheadLensRateWithExtraDenominator` (the "if pending hours were approved" what-if).
- Effect: after the rates, computes per-lens series from the maps it already holds (`totalsByDay` ÷ `fieldLabor.laborHoursByDay` / `revenueByDay` / `fieldLabor.laborUsdByDay`), `pendingFieldHours` (closed unapproved field sessions), and `overlapSessions` (approved non-office-job + bid rows). Zero new fetches.
- [`components/people/OverheadLensModal.tsx`](../../src/components/people/OverheadLensModal.tsx): formula strip with today's numbers, numerator itemized from the pool-trend totals, denominator rule, weekly-bar + rolling-line + dashed-headline SVG, sized levers (green ↓ / amber ↑), what-if panel, watch-outs (lens-specific caveat, pending hours, overlap count). Escape / backdrop / Close ✕ all close.
- Lens cards became `<button>`s with a "See the math ›" hint (same styling, `font: inherit`).
- Help guide `understand-overhead-numbers.md` gains "Click a lens to see its math".
