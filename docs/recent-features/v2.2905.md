# v2.2905 — Bids papercut batch B1: local-day last contact, honest sent/due columns, no flashing empty states, one name for an unnamed GC, labelled Labor inputs (2026-09-05)

Journey map Phase 4, **Tier 3 row B1** (`JOURNEY_MAP.md` → "Bids page — board, cover letter, pricing, labor"; dossiers J10 plans-to-bid-board, J11 takeoff-to-price, J13 send-the-bid, J14 chase-the-bid, J15 win-to-job, J35 gc-opens-bid-room). One PR, client only — no migration, no edge function. Items that ride the PARKED #20 "sent" count kernel (J13-F5, trade-scope label) and the #28 audience fix are not here.

## Item by item

| # | Finding | What changed |
|---|---|---|
| 1 | J10-F1 | **Done.** `toAppTzYmd(iso)` in [`datetimeLocal.ts`](../../src/utils/datetimeLocal.ts) (+3 tests) slices a `timestamptz` on the company calendar; `BidPreviewModal` uses it for Last contact (the other four dates are `date` columns and keep `slice(0, 10)`). A 10:22 PM CDT contact reads 8/27, not 8/28. |
| 2 | J10-F2 | **Done.** Pending still sorts by `bid_date_sent` (v2.1760) but now prints it: `bidBoardSentLabel` → `sent Wed 9/2` under the Due chip on every sent-undecided row, table and card variants ([`bidBoardDateCells.ts`](../../src/lib/bids/bidBoardDateCells.ts)). |
| 3 | J10-F4 | **Done.** `bidBoardNoDueDateParts(bid)` + `UNSENT_NO_DUE_DATE_RED_AFTER_DAYS = 14`: an unsent, undecided bid with no due date renders a dashed `No due date (+N)` chip (N = days since `created_at`), red once older than 14 days; null for dated / sent / decided rows (5 tests). Red = due passed OR undated-and-stale. |
| 4 | J10-F8 | **Done.** `Bids.tsx` gains `bidsLoaded` (set when the first `loadBids` settles, success or error) → `BidsBidBoardTab loading` prop → `role="status"` "Loading bids…" skeleton until then. Serves the Robot Board too. |
| 5 | J13-F3 | **Done.** `totalsResolved = coverLetterPricingRows != null || useCustomAmount`; both Print buttons and Copy & open in Google Docs are `disabled` + `aria-busy` with title "Pricing is still loading — the letter amount is not final yet" until it flips. Mark sent was already guarded. |
| 6 | J13-F4 J15-F9 J35-F4 | **Done.** New [`gcDisplayName.ts`](../../src/lib/bids/gcDisplayName.ts) (`General contractor`; dash-only strings count as unnamed; 4 tests) used at all four heads: `BidsCoverLetterTab` `customerName` (letterhead "To …", the Bid Room panel's `gcName`, and therefore the room payload / email "prepared for …"), `gcPackets.groupVersionsByGc` (was `'the GC'`), `BidRoom.tsx` "For:" (was `\|\| '—'`; old rooms published with `—` heal on read). |
| 7 | J14-F4 | **Done.** `compareCallQueueBuilders` in [`callQueue.ts`](../../src/lib/bids/callQueue.ts): `oldestContactMs` now folds in **pending** bids only (a lost bid's ancient note no longer ranks its builder); never-contacted builders tie-break by `chase.oldestQuietDays` descending before name (3 tests). |
| 8 | J14-F7 | **Done.** [`bidGcRecipients.ts`](../../src/lib/bids/bidGcRecipients.ts) header now states the per-packet outcome model (`bid_versions.outcome`, roll-up in `bids.outcome`). |
| 9 | J14-S1 | **Done (doc fix, no rename).** `denverCalendarDayKey` / `denverCalendarDaysBetweenInstantAndNow` doc comments name `APP_CALENDAR_TZ` (America/Chicago) and mark the `denver*` prefix historical. A rename would touch five schedule/jobs call sites for zero behaviour change. |
| 10 | J11-F2/N2 | **Done.** [`sharedBidPointer.ts`](../../src/lib/bids/sharedBidPointer.ts) (+4 tests): `setSharedBid` mirrors the id into `sessionStorage['bids.sharedBidId']`; the URL effect's new `!bidId && workflow tab && no selection` branch restores it. `selectBidsTab` still strips `bidId` (v2.2043 semantics untouched); the URL is never rewritten. |
| 11 | J11-F5 | **Done.** Workbench button reads `0 unassigned rows match the book` when there is nothing to fill (`BidsPricingTab.tsx`). |
| 12 | J11-F7 | **Skipped.** `BidsCountsTab.tsx` is in flight on open PR #2637 (import undo) — hands off per the batch brief. Re-run after #2637 lands. |
| 13 | J11-F8 | **Done.** [`laborCellSaveState.ts`](../../src/lib/bids/laborCellSaveState.ts) (+5 tests): `pending → saving → cleared` map transitions driven by the existing 1.5 s autosave effect; `laborCellAriaLabel`. All 25 numeric inputs in `BidsLaborTab.tsx` carry an `aria-label`; the autosaved ones spread `cellA11y(key, label)` (`aria-busy`, `data-save-state`, status title) and `cellSaveStyle(key)` (amber underline unsaved, blue while saving). Submit-form inputs (labor-book entry, add-missing-fixture) get labels only. |
| 14 | J11-F4 | **Done.** `MATERIALS_MODEL_CAPTION` (`bidTakeoffHelpers.ts`) rendered under the By Stage / Combined pills on Takeoffs and Labor and as the first paragraph of the switch-confirm modal in `Bids.tsx`. |
| 15 | J10-F10 | **Done.** The robots tab reads `🤖 Robots`; the red count pill is titled `N robot audits pending`. (#28's role-gating of the door is separate.) |

## Files

Kernels: `src/utils/datetimeLocal.ts` (+test) · `src/lib/bids/bidBoardDateCells.ts` (+test) · `src/lib/bids/gcDisplayName.ts` (new, +test) · `src/lib/bids/callQueue.ts` (+test) · `src/lib/bids/laborCellSaveState.ts` (new, +test) · `src/lib/bids/sharedBidPointer.ts` (new, +test) · `src/lib/bids/bidTakeoffHelpers.ts` · `src/lib/bids/gcPackets.ts` · `src/lib/bids/bidGcRecipients.ts` (comment) · `src/utils/dateUtils.ts` (comments).
Components: `Bids.tsx` · `bids/BidsBidBoardTab.tsx` · `bids/BidPreviewModal.tsx` · `bids/BidsCoverLetterTab.tsx` · `bids/BidsLaborTab.tsx` · `bids/BidsTakeoffTab.tsx` · `bids/BidsPricingTab.tsx` · `pages/BidRoom.tsx`.
Docs: `BIDS_SYSTEM.md`, `BIDS_TABS_ARCHITECTURE.md`. Help: `read-the-bid-board.md`, `price-a-bid-with-the-workbench.md`, `follow-up-with-builders.md`, `find-a-bid-on-the-workflow-tabs.md`, `try-the-new-cover-letter-layout.md`.

## Verify

1. Bids → Bid Board on BP398: Bid lens "Last contact" matches the board chip's date (8/27).
2. Not yet won or lost: every row shows `sent Wkd M/D` under its Due chip; top-to-bottom the sent dates descend.
3. Unsent: an undated bid older than two weeks shows a red dashed **No due date (+N)** chip; a fresh one shows it grey.
4. Hard-reload `/bids?tab=bid-board`: "Loading bids…" then rows — never "No bids yet".
5. Cover Letter on BP359 deep link: Print / Copy greyed for the first second, enabled once the amount appears.
6. Cover Letter on a GC-less bid: letterhead "To General contractor · …", Bid Room panel "one durable link for General contractor — …"; open the room link: "For: General contractor".
7. Followup → Call queue: the never-called band is ordered by quiet days, not alphabet.
8. Counts → pick a bid → click Labor → refresh: the bid is still selected, URL still `?tab=labor`.
9. Labor: tab to any number input — a screen reader names it; type — amber underline; ~1.5 s later blue, then gone.

## Rollback

Revert the squash commit — client-only; `sessionStorage['bids.sharedBidId']` is a harmless leftover.
