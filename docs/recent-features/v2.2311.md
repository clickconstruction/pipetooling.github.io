# v2.2311 — Data health "Open job" stacks + refreshes (2026-08-25)

Owner ask (mockup round three:
https://claude.ai/code/artifact/010cc1af-5227-4f3e-9c6e-955cdfb46ce5): keep
the drill-down alive under the job window and refresh it on edits.

- New `onOpenJobStacked(jobId, onSaved)` threaded JobsStagesTab →
  BilledPaymentForecastModal → PaySpeedsBreakdownModal →
  `PaySpeedDataHealthModal` (payment rows + undated-bill rows). It calls
  `tryOpenEditJob(jobId, { initialTab: 'bill', onSaved })` WITHOUT closing
  anything — the Job window (z 1010) naturally stacks above the drill-down
  (z 80), so closing it returns to the list with filters/search/scroll
  intact.
- `onSaved` (fires on every Edit/Bill save) re-pulls the transaction list
  and the pay-speed medians, so a just-linked payment flips from `unlinked`
  to its gap chip in place.
- The old close-everything `onOpenJobDetail` stays as fallback for callers
  that don't pass the stacked opener. Render test covers stacked call +
  refresh. No migration.
