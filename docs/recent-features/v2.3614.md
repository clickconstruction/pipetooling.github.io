# v2.3614 — Supervision, PR 4: Rate my crew — a supervisor's monthly sliders, by name, beside the office's (2026-09-19)

PR 4 of five in [`to-dos/supervision/README.md`](../../to-dos/supervision/README.md) (PR 1: v2.3611 · PR 2: v2.3612 · PR 3: v2.3613). One migration.

## The change

- **Migration** `20260919161056_supervisor_reviews.sql` ([doc](../migrations/20260919161056_supervisor_reviews.md)): `team_member_reviews.source` gains `'supervisor'`; `supervised_days_with(p_subject, p_month)` (SECURITY DEFINER) counts the job-days the caller shared with the subject that month while able to run a job; the INSERT / UPDATE policies gain the branch `source = 'supervisor' AND subject <> reviewer AND supervised_days_with(subject, review_month) >= 1`; SELECT admits the supervisor lane to prospects staff beside the office lane; `get_supervisor_review_deck(p_month)` returns everyone the caller supervised on **two or more** job-days, with days, jobs and whether a review exists.
- **Kernel** [`src/lib/people/supervisorReviews.ts`](../../src/lib/people/supervisorReviews.ts) (6 tests): the deck order (unrated first, most days together first), the summary (*2 to rate* · *all 3 rated*), the card line, the draft → row mapping, `nextUnratedCard`.
- **The deck** [`RateMyCrewDeck.tsx`](../../src/components/team-feedback/RateMyCrewDeck.tsx) (`team-feedback/`, shaped after the clock-out crew deck without its anonymity or cadence): a card per person with `RatingSliders` and the comment boxes, *Save · next* / *Skip* / *◀ Back*, a done screen; each save upserts `team_member_reviews` with `source = 'supervisor'` on the four-column key. **My crew** ([`DashboardSupervisorSection`](../../src/components/dashboard/DashboardSupervisorSection.tsx)) gains the *Rate my crew* line with this month's count and the button.
- **The Review stage** ([`TeamReviewSection.tsx`](../../src/components/prospects/TeamReviewSection.tsx)) loads office **and** supervisor rows; Reflect keys "every reviewer's latest" per lane (`reviewLaneKey`, [`teamMemberReviews.ts`](../../src/lib/prospects/teamMemberReviews.ts)) so a master who rates in both decks shows twice, the supervisor row wearing a green **supervisor** chip; the office Rate deck's rated / due bookkeeping reads the office lane only.
- Guides: `supervise-a-crew.md` (*Rate my crew, once a month*), `team-prospects.md` (Reflect), `say-who-can-run-a-job.md`. `ACCESS_CONTROL.md`, `GLOSSARY.md`, `DASHBOARD_SECTIONS_ARCHITECTURE.md`, the migration doc.

## Verified

Kernel tests; the deck's render smoke (the first unrated card, the save row, the done screen; the empty state); typecheck, eslint; the full suite. **Migration dry-run against prod in a rolled-back transaction**: a master listed this month gets their deck; their supervisor row about a person on it is accepted; a row about an assistant they never supervised is refused by the policy; a helper who needs supervision gets an empty deck and is refused.

## Not in this PR

PR 5 — retire Team leads.
