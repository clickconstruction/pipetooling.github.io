# v2.1977 — AR standing card + Fix-ups card (2026-08-21)

Two grid promotions in Today's Money Opportunities (New view):

- **Accounts Receivable is a standing card** for AR roles
  (dev/master/assistant-like/primary): `buildPipelineMoneyMoves` always emits
  `allocate-deposits` when `canOpenAr` — busy state keeps the classic
  "Allocate N bank deposits" claim and gains `badgeCount` (amber bubble
  matching the section-header button's badge); zero deposits yields an
  `idle` card ("Accounts Receivable · every deposit is applied — open to
  review payments and allocations", muted claim, action "Open"). AR is the
  daily payments surface, not just an alarm, so the door stays visible.
- **Fix-ups became a grid card** (was the v2.1961 footer strip): amber
  left edge, 🔎 "Fix-ups — missing data blocks billing", the data-gap chips
  inside (each keeps its own modal action, so no card button), gone when
  clean. The header's "pipeline is clean ✅" banner now also requires zero
  fix-ups.
- Tests: kernel (idle/badge states, canOpenAr:false still empty) + render
  smoke reworked for the card; 14 across both files. Verified live: AR card
  with badge 1 in the grid, footer strip gone, clean-state hides Fix-ups.
