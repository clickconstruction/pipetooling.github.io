# v2.2547 — Reference-grade badge on decided rows (2026-09-01)

Owner-approved mockup. On decided bids the robot icon repurposes from
"can a robot bid this?" to "can a robot learn from this?":

- **Badge**: A green / B–D amber / X grey letter over the robot glyph
  (`referenceGrade` kernel, v2.2545). Colorful-twin state still wins; live rows
  keep the v2.2530/2542 behavior untouched.
- **Modal** `RobotReferenceGradeModal`: grade meaning, presence checklist with
  fix hints, and the unseal-safe quality warnings (round value, weak/uncategorized
  loss, stale) — "Edit bid to fix" jumps to the form.
- **Migration `20260901050000_reference_presence_rpc`**: `list_reference_presence()`
  returns counts/pricing PRESENCE booleans for decided bids (the two grade inputs
  not on the bid row). Fetched once per board load.
- Help guide robot-icon section gains the decided-row grades.
