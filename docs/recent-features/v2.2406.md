# v2.2406 — Waiting to hear: the full picture on the call card (2026-08-27)

Wendi: "i still dont find the follow up page to be much more helpful than the
bid board as you can't see other communication here… when making calls or
logging follow up it's best to be able to see the full picture." Built from the
approved Followup Full Picture mockup.

- **Kernel** `src/lib/bids/followupStorySoFar.ts` (8 tests): `buildBidStory`
  (visible entries per the notes-popover GC rule + the letter-send anchor
  slotted chronologically, newest first, capped with a true total),
  `buildSiblingLines` (latest whole-bid entry per other pending queue row;
  spoken-to rows newest first, untouched rows oldest-send first),
  `storyMethodIcon`, `storyEntryVisible`.
- **`BidsWaitingToHearLens`**: the selected-bid region becomes a two-column
  layout (stacked under the actions on ≤640px). Right rail:
  - **The story so far · <bid>** — `bids_submission_entries` for the bid
    (one fetch per builder group, creator by-lines via
    `SELECT_BIDS_SUBMISSION_ENTRIES_WITH_CREATOR`, fail-soft), plus the send
    anchor with the bid's value; "show all N" expands. A chase tap prepends
    optimistically and the cache invalidates on save so the real row (with
    by-line) replaces it.
  - **With <builder> lately · other open bids** — the latest word per sibling
    pending row, amber "no contact since sent M/D" for untouched ones; a line
    click selects that bid on the card.
- Same store as the Bid Board's notes popover — entries flow both directions;
  GC-scoped rows follow `partitionNotesForGc` semantics (a GC row sees its
  scoped notes + whole-bid notes; other GCs' scoped notes stay on their rows).

No schema changes. Help guide `follow-up-with-builders.md` chase section
updated. Verified live: BP325's send anchor, real sibling entries under
Achilles-Austin, and a logged tap on the ZZ Test bid landing at the top of the
story with the saved by-line (one test entry left on BP398 ZZ Test).
