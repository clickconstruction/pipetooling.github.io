# v2.1998 — Partnerships lands on the Ledger tab (2026-08-21)

Owner request: `/partnerships` opened on the Deal tab (the config), but the day-to-day visit is about the money. The `activeTab` initial state in [`Partnerships.tsx`](../../src/pages/Partnerships.tsx) flips from `'deal'` to `'ledger'`; nothing else changes (tab choice still persists while switching partners within the visit). Client-only — no migration.
