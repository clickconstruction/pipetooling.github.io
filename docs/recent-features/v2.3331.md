# v2.3331 — Test reports PR 13: filed on Documents → Jobs (2026-09-11)

Owner, 2026-09-11: "let's also add the reports to the documents page." Plan: [`to-dos/test-reports/README.md`](../../to-dos/test-reports/README.md). Migration `20260911213000_job_test_report_docs_office_select.sql` ([`docs/migrations/20260911213000_job_test_report_docs_office_select.md`](../migrations/20260911213000_job_test_report_docs_office_select.md)); no edge function.

## What changed

- **Documents → Jobs child rows** ([`Documents.tsx`](../../src/pages/Documents.tsx)): after the job's invoices and contracts, one row per `job_test_reports` row on the listed jobs — *Sewer Pre-Test Hydrostatic test report · tested Sep 10 (· v2)* with a PASS/FAIL chip (hydrostatic only) and a Sent Sep 11 / Draft chip. Sent + `pdf_path` → a five-minute client signed URL on the `job-test-reports` bucket, opened outside the app (the exact bytes the GC got); otherwise the Test report modal (`useTestReportModalOptional` + `fetchJobWithDetailsById`), reloading the ledger on change. Fail-soft load like the lien releases and contracts.
- **Kernel** [`src/lib/jobsDocuments/testReportDocumentRow.ts`](../../src/lib/jobsDocuments/testReportDocumentRow.ts) (+ test): title ("Gas Test report", never "Gas Test test report"), detail, chips with tones, the door, and the search text the page filter matches.
- **Storage policy** (the migration): office-set SELECT on the bucket, gated by the same job predicate as the table's RLS. First client-side read of that bucket.
- Help guide `file-a-test-report.md` gains **Where they are filed**; `ACCESS_CONTROL`, `EDGE_FUNCTIONS` (send-test-report / open-test-report-pdf notes), `PROJECT_DOCUMENTATION` Documents section updated.

## Verify

`npx vitest run src/lib/jobsDocuments/testReportDocumentRow.test.ts`; typecheck · lint · theme check. After `db push`: Documents → Jobs → a job with a sent report → the row opens the PDF; a draft row opens the modal; before the push the sent row toasts the storage error ("Object not found" — RLS hides the object until the policy lands; nothing else is missing).
