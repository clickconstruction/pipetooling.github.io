# v2.2210 — Turnaway option in the New Report picker (2026-08-23)

Owner request (mock-up artifact f3567bec, built as designed). Turnaway stays "filed only through TurnawayModal" (report + dispatch alert + location) — this adds a second door to that one modal.

- **Kernel [`turnawayReportOption.ts`](../../src/lib/turnawayReportOption.ts)** (+1 test): `shouldOfferTurnawayInReportPicker(source, hasBlockToday)` — ledger jobs only — plus the option's label/sub copy.
- **[`NewReportModal.tsx`](../../src/components/NewReportModal.tsx)**: on job selection, one head-count of `job_schedule_blocks` (assignee = me, job, `work_date` = today via `denverCalendarDayKey`); when >0, an amber band renders under the report-type chips — ⚠️ **Turnaway — not ready / not home** · "on your schedule today · files the report and alerts Dispatch" — and opens `TurnawayModal` (wrapped in a z-1200 stacking context: the picker's overlay sits at 1100, the Job-Mode overlay at 65). `onSubmitted` closes both and fires `onSaved`. AdditionalReportModal (follow-ups) still hides Turnaway.
- Verified on the dev build against prod with a temporary schedule block (created + deleted): band absent on an unscheduled job, present on the scheduled one, opens the real Turnaway form on top of the picker with the right job, cancelled without filing; band gone after the block was removed. Help: `turnaway-trip-charges.md`. Client-only.
