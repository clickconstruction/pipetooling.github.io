# v2.2493 — Needs You worst-first ranking (2026-08-29)

The ordering follow-up deferred since v2.2339: with all ten sources migrated
(v2.2487–v2.2492), the card stops mirroring the old banner-stack order and
ranks worst-first.

- **Kernel `dashboardNeedsYou.ts`**: exported `NEEDS_YOU_RANK`
  (`Record<NeedsYouItem['key'], number>` — adding an item without picking a
  tier is a compile error) + `rankNeedsYouItems` (stable sort: rank tier, then
  figure descending, ties keep build order; `99+` parses above any two-digit
  figure). `buildNeedsYouItems` returns ranked items, so both surfaces get the
  order for free.
- **The tiers**: 0 alerts (bulk-delete, claim-dev — they were LAST in banner
  order, indefensible for "worst"), 10 money received not applied
  (ar-deposits), 20 the weekly GC deadline, 30 billing accuracy (self/team
  tally), 40 revenue chasing (job-followups), 50 people/planning
  (team-reviews, roadmap), 60 hygiene (lost-bids).
- Walk the list inherits the order — walking from the top is now "do the worst
  thing next".
- No wiring changes on either surface. Tests: ordering assertions updated +
  a full-house ranking test + a 99+/tie-stability test on `rankNeedsYouItems`.
- Help guide `needs-you-card.md` documents the ordering.
