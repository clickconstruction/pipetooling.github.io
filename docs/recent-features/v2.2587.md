# v2.2587 — Copy fixtures for text: Division 22 grouping (2026-09-01)

Phase 2 of the Division 22 ledger (schema/kernel: v2.2580, migration
20260901181000 — applied to prod 2026-09-01). The Pricing tab's "Copy
fixtures for text" now classifies each visible count row through the
org-wide `spec_section_match_rules` ledger and emits the grouped format
(option A from the "Division 22 Ledger" design canvas):

- **New kernel export `buildBidFixtureCountsTextGrouped`** (+ tests) in
  `src/lib/buildBidFixtureCountsText.ts`: section headers `code · title`
  in ascending MasterFormat order, rows keep input order within a group,
  unmatched + deliberate no-code rows in a "No code yet" tail, `Items: N`
  footer. Price-free guarantees unchanged and re-asserted in tests.
- **Wiring** (`BidsPricingTab.copyFixtureCountsForText`): reads rules +
  section titles at click time (two small selects, `withSupabaseRetry`);
  unknown `match_kind` values are skipped defensively. **Ledger
  unreachable → flat-list fallback** — the copy never blocks. Success
  toast reports how many names are still uncoded.
- **Repo tool `scripts/db-push.sh`**: the sanctioned apply-migrations
  sequence (link → `db push` → `gen-types:linked` → drift check) as a
  stable script, so agent sessions can be allowlisted to run it
  (`"Bash(bash scripts/db-push.sh)"` in `.claude/settings.local.json`).
- `src/types/database.ts` regenerated after the v2.2580 push (includes
  `spec_sections` / `spec_section_match_rules`).

Not built yet (deliberately): the ledger manager UI and the unmatched-
names audit screen — pinned names currently need a rules-table edit.
Gas/GPR/RH/EDF remain unseeded pending the owner's spec-book call.
