# v2.3597 — Labor refresh PR 6b: one book, the Hours-from chips, Reset to robot, who may recalibrate (2026-09-18)

Second of three for PR 6 of [`to-dos/bids-labor-refresh/`](../../to-dos/bids-labor-refresh/README.md), on the columns v2.3596's fold added. Client only. The drawing is [`before-after-pr6.html`](../../to-dos/bids-labor-refresh/before-after-pr6.html) §03–04; the owner's defaults hold — Robot's split won the fold, and an estimator's Set is a proposal until a leader confirms.

## What shipped

- **Kernel** [`lib/bids/laborEntryProvenance.ts`](../../src/lib/bids/laborEntryProvenance.ts) (11 tests): `entryProvenance` reads the five kinds — *robot* (with *picked over Default 2/3/2* off the fold's note) · *human · from Default* · *override · Wendi · Sep 18* · *learned · BP375 · Wendi* · *calibrated ×1.13 · 4 jobs · Sep 18* — off `origin`, `robot_*_hrs`, `set_by / set_at / set_note`; `laborBookRights(role)` (leaders set/reset anything and Set; estimators edit, reset their own, propose; assistant/controller read); `canResetToRobot`; `bookSummaryWords`; `laborBookForTrade` (the robot book, never an archived one); `calibrationSetPlan` (robot entries the jobs touched × the multiplier, to the quarter hour, from the robot's numbers so two Sets do not compound; a person's override untouched); `calibrationProposalWords`.
- **The panel** ([`BidsLaborBookPanel`](../../src/components/bids/BidsLaborBookPanel.tsx)) lost the book chips, *Add book*, *Edit book* and the book form; it names the trade's one book with `bookSummaryWords`, adds the **Hours from** column with the chip and **Reset to robot** (role- and owner-gated), gates *Add entry* and the pencil on `rights.edit`, and the entry form reminds an editor the robot's numbers stay underneath. The tab reads the bid's book, else the trade's, into the panel (`laborBookEntriesVersionId` follows it — the browse/applied split of quirk 15 is over), resolves `set_by` / `proposed_by` names from `users`, and owns `resetEntryToRobot` (the trigger clears the stamp).
- **The New view** lost its `<select>` and `onChangeBook`; the line reads *Labor book 🤖 Robot Default · Plumbing · 26 entries · 46 aliases · 9 overrides* (or *no labor book yet — the robot seeds one*). The **Book vs jobs** tile offers *Set ×1.13 on N entries* to a leader (writes the plan, clears any proposal) or *Propose ×1.13* to an estimator (writes `proposed_multiplier / proposed_by / proposed_at / proposed_note` on the version; the tile reads *proposed ×1.13 by Wendi · Sep 18* for everyone; a leader's *clear*). The drawer's per-entry Set is gated on `rights.edit` and writes `set_note = calibrated ×1.36 · 3 jobs`. The queue learns into the book only for roles that may write it; a new entry from the queue carries `set_note = learned from BP375`.
- **Loaders**: `useBidPricingEngine.loadLaborBookVersions` and the Jobs sub-labor modal's list read `archived_at IS NULL`, robot first; the Bids page's auto-pick is `laborBookForTrade`. The Jobs sub-labor modal lost *Add version* / *Edit version* and its form too.
- Guide [`estimate-labor-hours-on-a-bid`](../../src/content/help/estimate-labor-hours-on-a-bid.md): § One book per trade (the chips, Reset to robot, who may), the tile's Set / Propose, the entry form's reminder.

## Verified

- `npm test` — the kernel's 11, the panel's 4 and the New view's smokes (one book line, Set / Propose / read-only by role, a standing proposal named).
- On prod after 6a's push: BP403's Labor tab names *🤖 Robot Default · Plumbing · 26 entries · 46 aliases*; the panel's chips read *robot* on the seventeen robot rows, *robot · picked over Default …* on the three conflicts, *human · from Default* on the nine folded rows.

Left for PR 6c: retire the Old view (the pills, the L1 grid region, `bids_labor_view_v1`).
