# v2.2728 — Estimate documents: Terms as a pull-quote, with the Terms and Conditions link in context (2026-09-03)

Owner note on the Signed agreement view: "terms could look a little better". The estimate document (the live accept page and every read-only record — Estimates' acceptance modal, the Jobs Signed agreement view, the staff preview) rendered the terms inside a grey, bordered, rounded box — one sentence in what reads as an empty input.

- `EstimateCustomerDocument`: the terms body is now a quiet pull-quote (orange left rule, 0.95rem prose, `pre-wrap` kept for multi-line terms). New `termsPageHref` prop renders *"These apply together with our Terms and Conditions ↗"* directly under the body (or *"This estimate is subject to our …"* when the estimate has no terms text).
- `EstimateAcceptBody` passes `estimateTermsPageHref()` and drops the lonely centred *"Terms and Conditions."* line that used to hang below the footer; `EstimateTermsHeaderNotice` (its only user) is removed.
- No content change: the terms text and the linked page are the same; only where and how they sit.
