# v2.2816 — twin-mcp: amendable `score_backtest`, `stage_plan_pdf` for oversized sets, JSON `mint_session`, per-bid work folders (2026-09-05)

The first parallel round-2 batch (R2-BT-2 Take 5 Richmond −7.9 %, R2-BT-3 SAISD Davis MS −33.9 %, R2-BT-9 Bonilla Law Firm +27 %; three subagents, 25–48 min each) surfaced four harness stumbles. Edge only (deploy after merge); `briefs.ts` regenerated because the CountTooling guide changed.

- **`score_backtest` is amendable.** The scope-match check needs the reference rows, which only the first call unseals — so the honest first verdict was "unknown", and one run's `gate_eligible` went false for a structural reason. Protocol now: score without a verdict, line-compare, call again with the same `run_label` + `scope_verdict: 'pass'|'fail'` (+ `counts_note`/`note`); the row is patched, `gate_eligible` recomputed (grade and flags re-derived from the reference, verdict ≠ fail), and `[STG-6 SCORECARD amended]` stamped. Lenient parse (`PASS (pre-unseal)` → pass); anything else records `unknown` and says so in the response. `locked_total` never changes.
- **`stage_plan_pdf(bid, file_name?)`.** CountTooling refused the 113 MB school set and the takeoff shipped with no sheet under it. The verb returns a one-time signed upload URL into the existing `twin-plans-tmp` bucket (service-role signed, no storage policy needed) and the object's `public_url`; the twin trims locally, PUTs, and passes `public_url` as `ct_finish_takeoff(pdf_url)` — now a documented argument. The staged object is deleted after a successful import.
- **`mint_session` answers JSON** for both apps (`{ ok, app, email, action_link, note }`); two agents mis-parsed the prose form.
- **Kickoff**: two-call STG-6, the oversized-set path at STG-3, and one work folder per agent (plans, scratch, helper scripts) — two parallel agents shared a scratch folder and one found its helper rewritten under it. `COUNTTOOLING_BID_GUIDE.md` hard limits updated.

Not addressed here (this environment, not the harness): the agent-side permission classifier blocked one agent from adding 🤖 book entries; a session the owner starts does not hit it. Doctrine findings from the batch (small-TI reconnect tier, footage on protos, spec-literal tiers on district work) go through Wendi's audit and the digest, not this PR.
