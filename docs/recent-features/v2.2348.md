# v2.2348 — PDF payment card: balance rule at frame weight (2026-08-26)

Owner ask: the rule above Balance due should match the outside line.

- `physicalInvoicePdf.ts` `drawPaymentHistory`: the rule already used
  `PAYMENT_BOX_GRAY` but drew at the default hairline width, rendering
  thinner and paler than the 0.35mm frame. It now sets 0.35mm around the
  line (restored to the jsPDF default after). The email card already
  matched (1px #9ca3af for both frame and rule, v2.2343).
