# v2.2791 — Timeline y-axis title (2026-09-04)

Owner ask: "On the y-axis of the chart at the top I want to know what that means." The Timeline chart's left axis showed bare numbers.

## Client only — no migration

- [`JobSummaryTimelineView.tsx`](../../src/components/jobs/JobSummaryTimelineView.tsx): new `AxisTitle` — a rotated (−90°) muted label in the left gutter of both SVG charts; the gutter `L` widens 36 → 48 on the daily and weekly charts to make room beside the tick numbers. Daily reads "jobs running that day" (the stacked count for the day); weekly reads "jobs touched that week" (carried + new). The hover guide's plot-left clamp follows `L`, so nothing else moves.
- Help guide: one sentence in "Reading the chart".
