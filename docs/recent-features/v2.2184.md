# v2.2184 — Quickfill chrome: dock above the footer, phone jump strip, header ✓ Mark, inline stamps (2026-08-23)

PR A of the Quickfill tune-up (owner-approved mockup https://claude.ai/code/artifact/50741c3c-293e-4eae-a897-83076baad9aa, screens A1/A2).

- **Dock bug**: [`SectionDock`](../../src/components/SectionDock.tsx) rendered at `bottom: 12px`, z 900 — behind the 60px Dispatch/Job-mode footer (z 1000) whenever that mode is on, i.e. invisible for the owner on Quickfill and Dashboard. [`Layout.tsx`](../../src/components/Layout.tsx) now exposes `--app-bottom-chrome` (footer height + safe area, `0px` otherwise) on the content column; the dock's bottom is `calc(var(--app-bottom-chrome, 0px) + 12px)`.
- **Phone jump strip** ([`Quickfill.tsx`](../../src/pages/Quickfill.tsx)): under 640px the jump grid is a one-line `overflow-x: auto` strip (was 18 wrapped rows = 868px before the first section; now 371px) with a freshness line from the new kernel [`quickfillFreshnessSummary`](../../src/lib/quickfill/freshnessSummary.ts) (+ tests): "N of M fresh · K need a look · oldest Xd | some never marked"; personal (my-inbox) excluded. Desktop/tablet keep the wrapped grid.
- **Inline stamp**: the who·when stamp moved from an absolutely-positioned corner badge (overlapping the label's last letters at every width) to an inline span after the label.
- **Header ✓ Mark** in `QuickfillSectionWrapper` (expanded state, sections with the default mark button): same `onMarkUpToDate`, same freshness palette; sections with `omitDefaultMarkButton` (Texts / Email / Physical inbox — note-first marks; My Inbox — no mark) keep their own control only.
- Verified locally against prod data at 375 / 1280: dock bottom 72px above the nav (no overlap), strip + tally, 15 header marks, no horizontal overflow. Help `quickfill.md` updated.

Client-only.
