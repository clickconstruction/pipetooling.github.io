# v2.2782 — Sub sheet stages: the Activity trigger finds click-number jobs (2026-09-04)

Follow-up to v2.2767, caught on the first real move: the office advanced the sheet on job **1004** and the job's Activity feed stayed silent. Job 1004 is a **click-number job** — empty `hcp_number`, the number lives in `jobs_ledger.click_number` (92 such jobs, 8 sheets keyed on one today) — and the `people_labor_jobs_stage_to_activity` trigger matched on `hcp_number` only.

- Migration `20260904210406_sub_sheet_stage_trigger_click_number.sql`: `CREATE OR REPLACE` of the trigger function (same trigger). Resolution now mirrors the ledger's own join — `lower(btrim(hcp_number))` first, then `lower(btrim(click_number))`, newest job first, never an empty-string match on either side. Summary / actor / `detail` unchanged.
- No client change, no edge function change. The one lost line (1004's first move) is not backfilled — the sheet was stepped back and forward again after the push, so the feed carries the real history from there.
