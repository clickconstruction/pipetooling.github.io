# v2.2106 — Fleet chip catch-up modals (2026-08-22)

Owner request: click "8 need a reading" / "1 maintenance task" and get a modal list — vehicle name + VIN tail + who it's assigned to + the one entry field that clears it (mockup artifact 22ee9380, owner-approved). New kernel [`vehicleCatchUp.ts`](../../src/lib/vehicleCatchUp.ts) (`readingCatchUpRows`, 2 tests): mirrors `fleetSummary.staleReadings` exactly — non-fresh (>30d/never) across ALL vehicles, motor pool and unassigned included (deliberately unlike Quickfill's holders-only 7-day `staleOdometerCallList`); never-read first, then stalest, name tiebreak. New [`VehicleCatchUpModals.tsx`](../../src/components/people/VehicleCatchUpModals.tsx): shared shell, readings flavor (miles input per row, Enter saves with today's date and focuses the next empty box; rows snapshot at open and turn green via a saved overlay instead of vanishing as they go fresh) and tasks flavor (open tasks live from state; assignee chip or Assign button; ✓ Done runs `completeMaintenanceTask` with a new `skipServiceNudge` opt so the log-service modal doesn't stack). Both amber chips in [`PeopleVehiclesTab.tsx`](../../src/components/people/PeopleVehiclesTab.tsx) became buttons; counts tick down live via the existing `loadFleet` refresh. Help guide updated. Client-only — no migration.
