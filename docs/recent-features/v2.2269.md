# v2.2269 — ⏳ Waiting For: grouped, renamed; Upcoming dev-gated (2026-08-25)

Clarity pass on the v2.2264 waiting list (mockup-approved), following the
v2.2268 un-nesting fix. Owner feedback: Micah's Today page showed ~10 tall
repetitive cards.

## `ChecklistComingUpSection` (Today + Dashboard inbox card)

- **Renamed** "⏳ Coming up" → "⏳ Waiting For" (killed the near-synonym
  collision with the ▼ Upcoming section directly below it).
- **Grouped by blocker**: `sequentialWaiting`'s blocker is the stage's first
  open task, so waiting tasks group naturally under it. Each dashed group
  card says the blocker, its assignee chips (no more "is on it" — chips on
  their own line per owner feedback), and the stage once; the viewer's
  waiting tasks are ⏳ one-liners beneath, sort_index order, an italic
  *then* on tasks queued behind a waiting sibling.
- **Unknown assignees filtered**: a blocker with no resolvable names shows
  amber "not staffed yet" instead of a "…" chip.
- **Capped at 3 groups** with an "…and N more steps ahead ⌄" / "show fewer ⌃"
  toggle.
- Groups sort by size desc, then stage title.

## Upcoming dev-gated (`src/pages/Checklist.tsx`)

The ▼ Upcoming section (instances with `scheduled_date` after today) was
empty for nearly everyone and its name collided with the waiting list. It now
renders only for devs; `loadUpcoming` skips the query for everyone else.

## Guides

- `check-off-my-daily-tasks.md` § renamed + grouped description.
- `add-a-stage-and-its-tasks-to-a-roadmap.md`: two "Coming up" mentions
  renamed.

Verified live with Stephen's real 17 waiting tasks: 6 groups, 3 visible,
expander round-trip 3→6→3, "then" markers and chips render as mocked
(https://claude.ai/code/artifact/e2dbd89d-46a9-4c4d-baf6-9c939ae1f5e4).
