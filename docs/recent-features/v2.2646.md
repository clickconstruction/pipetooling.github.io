# v2.2646 — RFQ Round 2, Rung C: the ninety-second repeat quote (2026-09-02)

Rung C of `docs/RFQ_ROUND2_PLAN.md` (canvas artboard 7).

- **get-rfq-quote-page** returns `prior` — the requesting house's OWN
  last-quoted prices (memory rows ≤90 days, filtered to the request's
  scope; never another house's numbers, never our cost/sale data).
- **Vendor page**: a banner — "You priced 58 of these for us before
  (newest 6 days ago)" — with one deliberate tap: **Fill with last
  time's prices**. Filled inputs wear a blue "from last time" tag that
  flips off the moment the vendor edits the line; lines with no memory
  stay empty; a saved phone draft always wins over prefill. The review
  pass replaced the plan's auto-ghost prefill with this explicit-tap
  design — stale prices can never submit without the vendor choosing
  them, and a whole confirm-state machine disappears.
- Live-tested on a seeded B429 request: banner counted 2 of 3, one tap
  filled 179.00/6.10 (footer "2 of 3 answered"), editing dropped the
  tag. Seeds deleted after.
