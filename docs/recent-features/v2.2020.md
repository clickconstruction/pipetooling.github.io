# v2.2020 — Billed header: Accounts Receivable icon (2026-08-21)

Owner request: every button on the Billed Awaiting Payment header's quiet action tier wears an icon (GC hard-hat SVG, ⇪, 📊, 📅, ⚙) except Accounts Receivable. It now leads with 💵 — the same mark [`pipelineOverview.ts`](../../src/lib/jobs/pipelineOverview.ts) gives the allocate-deposits money move, so AR reads as one thing across the Pipeline. One-line addition in [`JobsStagesTab.tsx`](../../src/components/jobs/JobsStagesTab.tsx); the amber unallocated-count badge is untouched. Verified live from a worktree dev server. Client-only — no migration.
