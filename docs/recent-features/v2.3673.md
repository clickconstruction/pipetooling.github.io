# v2.3673 — Materials by stage, PR 3: the printed schedule of values and the letter section (2026-09-21)

Client only, on top of [v2.3672](./v2.3672.md). PR 3 of 4; PR 4 the book / assembly remembering.

## What shipped

- **Print → Schedule of values** — [`scheduleOfValues.ts`](../../src/lib/bidDocuments/scheduleOfValues.ts) (`buildScheduleOfValuesHtml`, 5 tests): page 1 is the three stages — the fixtures named under each (`½ of ft of 4IN WASTE`, `part of SK-1`), raw material, × the factor, the share, a total row — with a footer that says how much money still has no stage, which rows are not staged (allowances), and whose factor it is; page 2 lists every costed fixture with its stage in margin words (`fixtureStageText`: `3`, `1 + 2 (½ · ½)`, `mixed`) and the dollars per stage. An optional **Of contract** column (`contract` input, from `scaleToContract`) is built and tested but not yet offered from the tab, which does not hold the priced total. The panel's button (`onPrint`) lands in [`BidsTakeoffTab.tsx`](../../src/components/bids/BidsTakeoffTab.tsx) `printScheduleOfValues`, printing the rail's own `stageSummary`.
- **The Rough Takeoff print** ([`takeoffBreakdown.ts`](../../src/lib/bidDocuments/takeoffBreakdown.ts)) takes an optional `stageTextByRowId` and prints `stage 2` / `stage 1 + 2 (½ · ½)` beside each fixture's count — the paper Wendi marked by hand, marked for her.
- **The letter** — [`coverLetter.ts`](../../src/lib/bidDocuments/coverLetter.ts): a trailing `materialsByStage` param on both builders renders **Materials by stage:** (`Rough In — $29,227.36` …) after the payment schedule, before the closing (2 tests). [`BidsCoverLetterTab.tsx`](../../src/components/bids/BidsCoverLetterTab.tsx): the **Materials by stage** pill (`bids.include_materials_by_stage`, the same update-guard path as the payment schedule); when on, the rows come through `loadMaterialsByStageForBid` for the active version, so the letter says what the rail says. [`approvalPdf.ts`](../../src/lib/bidDocuments/approvalPdf.ts) reads the flag + the bid's factor fresh with the payment-schedule flag and bolds the heading.

## Not here

The book / assembly remembering a split (PR 4); "Scale to contract" as a choice on the tab; the payment schedule's "Use stage shares".

## Verify

`npx vitest run src/lib/bidDocuments` (the schedule builder, the letter section, the Approval PDF stand-in unchanged: a bid without the flag prints no section). Live: Takeoffs → Stages → Print schedule of values on a staged bid; Cover Letter → the pill → the preview gains the section.
