# v2.2193 — My Inbox rows become conversation cards (notes, thread, close-with-context) (2026-08-23)

Owner, from the phone: "When a user is looking at My Inbox, I would like for them to be able to drop down, leave comments and close cards." Mockup ("Inbox cards that talk") → built. The Due Today / Overdue rows in [`DashboardMyInboxCard`](../../src/components/dashboard/DashboardMyInboxCard.tsx) were checkbox + fwd only; they now render the shared [`ChecklistInstanceCard`](../../src/components/checklist/ChecklistInstanceCard.tsx) — the Today tab's card — so all four mounts (Dashboard, Job Mode Inbox, Dispatch Inbox, Quickfill) get the same behavior at once and there is exactly one card language in the app.

- **What each row can now do**: big ✓ Complete (44px, one-handed); ＋ Add a note → 💬 Notes N (instance thread + composer); tap the **title** → the task's full history (`ChecklistItemActivity` spine, all occurrences) with **✓ Post & complete** in the composer; live status chips (Reopened by … / Waiting on review). FWD, 🔕 mute, the `(T+3)` overdue tag, and the 🚗 vehicle chip are unchanged (`actions` slot / title suffix).
- **Engine**: the Today tab's events pattern copied in — `eventsByInstance` + actor names loaded in one query keyed on the visible instance ids (`checklist_instance_events`), `postCardComment` (+ local echo). Selects gain `reviewed_at` (instances) and `created_at, repeat_type` (items) — including the Dashboard boot query in [`dashboardBootQueries.ts`](../../src/lib/dashboardBootQueries.ts) that seeds `todayChecklist`, and the shared `ChecklistInstance` type. No migration.
- Help: `dashboard-my-inbox.md` Due-Today bullet rewritten.
- **Live testing note (transparent):** exercised on prod data with the dev account. A scripted tap accidentally completed the "does our plumbing tools…" instance (proving the complete path end-to-end, including its list refresh); it was restored with the app's own reopen recipe — an explanatory note event + clearing the completion — so its card now honestly shows "Reopened by You" + the note. A second harmless test note ("safe to ignore") remains on one card's thread.
