# v2.2283 — Reorder goal stages from Checklist → Review (2026-08-25)

Requested with mockup ("Goal Stage Reorder" artifact): extend the roadmap's
hold-and-drag stage reordering to the Review board's Goals section.

- **⇅ Reorder** button in the STAGES header (beside "Open roadmap →"), gated on
  `canEditRoadmapTasks` and >1 stage. Opens the existing
  `ChecklistTechTreeOrderStagesModal` (dnd-kit cards, saves as you go) fed from
  the goals data: stage meta ✓ done / 🔒 / "N of M", tasks orderable inline.
- Save path mirrors the roadmap's `saveStageOrder`: `computeStageOrderUpdates`
  + `computeTaskOrderUpdates` (diff-only `sort_index` writes to
  `checklist_tech_tree_groups` / `_group_tasks` with `withSupabaseRetry`), then
  `goalsReloadTick` refreshes the goals strip/ledger live behind the modal.
- New loader state keeps the raw per-roadmap `sort_index` baselines
  (`goalGroupOrder`, `goalRawTasks`) the diff needs.
- Help guide `review-my-teams-completed-tasks` gains the Reorder bullet.
- Verified live against Farm 1: dragged a stage down one slot (list behind the
  modal re-rendered in the new order), then restored the original order.
