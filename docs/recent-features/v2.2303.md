# v2.2303 — Sent dates, Bill-tab routing, No Count Date (2026-08-25)

Owner-approved mockup (https://claude.ai/code/artifact/93a6c26a-2cdf-4799-9cdf-76e33969dc11)
plus a mid-review addition (the No Count Date gear).

- **Migration `20260826000000`** (details in `docs/migrations/`):
  `jobs_ledger_payments.sent_on` + RPC floor on the dev-set
  `pay_speed_no_count_date_v1` app_settings key, reported as `noCountDate`
  in the transactions payload.
- **Bill tab routing**: the drill-down/receipt "Open job ›" now calls
  `tryOpenEditJob(jobId, { initialTab: 'bill' })` instead of the read view.
- **Payments received**: labeled **Sent | Received** date pair per row
  (`JobFormPaymentsTable`); Sent editable even on Stripe/Mercury-locked rows;
  Mercury rows offer "bank MM/DD →" one-tap fill (posted_at lookup,
  fail-soft); future Received dates get an amber border + warning line.
  `sent_on` rides the whole form pipeline (PaymentRow, jobFormRows, diff
  upserts, JOBS_LEDGER_PAYMENTS_EMBED, database.ts row types).
- **No Count Date** (dev-only ⚙ in `PaySpeedDataHealthModal`): date input +
  Save / "Clear — count everything", written to app_settings; medians and
  counts refetch immediately; non-gear viewers see a "Not counting payments
  received before X" footnote.
