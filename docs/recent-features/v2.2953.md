# v2.2953 — Fix: the Bridge died on `supply_house_invoice_job_allocations.id` (2026-09-06)

Found on the Tier 5 X17 walk of unwalked surfaces (P1, `pipetooling-journey-map` → `docs/journeys/weekly-money-close.md` BR-1). `/bridge` mounted its header and then failed the whole load: `loadBridgeData.ts`'s supply-allocation pager ordered by `id`, but `supply_house_invoice_job_allocations` has no `id` — its key is `(invoice_id, job_id)`. The other five pagers in the file order by real `id` columns (checked against `database.ts`). Fix: `.order('invoice_id').order('job_id')`. Client only; dev-only surface; no migration, no edge function.
