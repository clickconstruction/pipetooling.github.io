# v2.2610 — Deploy tooling: out-of-order db push + deploy-functions.sh (2026-09-01)

Tooling PR born from the crew-day deploy (v2.2602/v2.2603): a parallel session applied `20260901220520` between this session's two pending migrations, and `scripts/db-push.sh` failed on the CLI's out-of-order refusal.

- `scripts/db-push.sh`: plain `db push` first; when the CLI answers "Rerun the command with --include-all" (out-of-order pending migrations — the AGENTS.md break-glass case), it retries once with `--include-all` automatically. Any other failure still exits 1.
- New `scripts/deploy-functions.sh <name>…`: the sanctioned edge-function deploy counterpart — same `.env.local` auth pattern, `--no-verify-jwt` (repo-wide convention; functions self-gate via `auth.getUser()`), multiple names per call, `check:edge-drift` after.
- `src/types/database.ts` regenerated post-push (picks up `get_crew_day_payload` / `get_crew_day_payload_for_user` / `crew_day_email_requests`).
