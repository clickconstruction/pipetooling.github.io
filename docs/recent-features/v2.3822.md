# v2.3822 — Today's Money Opportunities: two-line cards (2026-09-25)

Punch list **#44** (`to-dos/money-opportunities-compact-cards/`). The owner, 2026-09-25, of the lien-notices and burn cards: *"these two cards are very wordy, can you come up with a proposal that would make them more compact while still maintaining useful value?"* — and *build it* on the mock-up. Three things made the cards tall: a body clause in the title (*the earliest closed Sep 15*, *all against an assumed budget*), prose where the facts were a list (four piles, three jobs), and a button on a row of its own; the **18** badge repeated the title's 18. Client only, no migration.

## The change

- **One card shell** in [`PipelineMoneyOpportunities.tsx`](../../src/components/jobs/PipelineMoneyOpportunities.tsx) — `OpportunityCard`: line one is the glyph, the claim (an optional muted tail and badge) and the **door as a link at the right end of the same line** (`data-testid="<card>-door"`); line two is `children`: the why, or a row of `OpportunityChip`s that are doors themselves (red · amber · plain · a dashed *quiet* caveat). Every card on the strip takes it — the money moves, the lien card, the burn card, the two statement-round cards, the payment chase, the fix-ups (its footer sentence is the card's hover). The contract-coverage card keeps its own component.
- **The lien card** — [`lienDeskMoneyCard.ts`](../../src/lib/jobs/lienDeskMoneyCard.ts) gains `title` (*18 lien notices due · $168,306*), `piles` (needs the owner · to draft · awaiting approval · approved for the run, only the non-empty ones, in the desk's order) and `deadline` (the earliest window as one chip: *closed Sep 15 · RMC- Dudley Mason* · *by Oct 15 · in 20 days · 6 notices* · *today* · *tomorrow*; the desk's tone; the old first sentence plus the mail-by warning as its hover; the pile it sits in). `claim` and `why` stay — `why` is the card's hover. `onOpenLienDesk` takes an optional `LienDeskPile`: a pile chip and the deadline chip open the desk on that pile (`JobsStagesTab` passes it through `setLienDesk({ …, pile })` → `LienDeskModal.initialPile`, which already existed for the Dashboard's Missed door); the *Lien desk →* door opens it plain. The badge is gone.
- **The burn card** — *4 jobs burning · $23,410 margin at risk*, then the worst three as chips (*◆ J878 Take 5 – Seguin · 88% spent at 60% done*), each opening that job's Costs tab, with the footing's meaning in the hover (◆ bid · ✎ typed · ≈ assumed); *+N more* opens Job Summary's list; *≈ N against an assumed budget* is a quiet chip with the fix in its hover. *Worst first →* is the door.
- Guide *read the Pipeline money view* (the burn and lien paragraphs), `GLOSSARY.md` → Lien desk (the Pipeline lien card's doors); the to-do moves to Close out.

## Verified

- `lienDeskMoneyCard.test.ts` (the title, the piles in order, the deadline chip's label · tone · hover · pile — closed, in N days, today, tomorrow, undated). `PipelineOverview.render.test.tsx` (the lien card's two lines and its three doors with the pile they carry, the old button gone; the burn card's chips open their job, *+1 more* the list, the assumed caveat, the door the worst). Quickfill's Jobs Cleanup station (the same component, no lien door) unchanged. Typecheck, lint.
- Live on the Pipeline (read-only): the two cards at two lines beside their neighbours; a pile chip opens the Lien desk on that pile.

## Left as is

- The money moves keep their kernel words (`buildPipelineMoneyMoves`): *Chase the 90+ tail — $39,490* over *5 bills waiting 90+ days* is still two lines, now without the button row. Folding the two into one line is a wording change for another day.
- The contract-coverage card (`PipelineContractCoverageCard`) is untouched; its chips already carry the stages.
