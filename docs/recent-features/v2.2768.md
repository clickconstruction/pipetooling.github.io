# v2.2768 — Takeoffs gets Old / New 1 / New 2 pills; Old is untouched (2026-09-04)

PR 1 of the Takeoffs refresh train (`docs/TAKEOFFS_REFRESH_PLAN.md`). The selected-bid card on Bids → Takeoffs gains **Old / New 1 / New 2** pills beside the bid title — the bordered-pill idiom Counts / Pricing / Cover Letter used for their parallel runs (v2.1906 / v2.1909). The choice is per device (`localStorage` key `bids_takeoff_view_v1`, values `old | new1 | new2`), and the default is **Old** until retirement.

## Client only — no migration, no edge functions

- New kernel [`src/lib/bids/takeoffView.ts`](../../src/lib/bids/takeoffView.ts): `TakeoffView`, `TAKEOFF_VIEWS` (labels + hover titles naming each mockup), `parseTakeoffView` (anything unknown → `old`), `readStoredTakeoffView` / `writeStoredTakeoffView` (storage failures never throw). 3 tests.
- New [`TakeoffViewPills.tsx`](../../src/components/bids/TakeoffViewPills.tsx): the pill row (`role="tablist"`) and `TakeoffNewViewPlaceholder` — the stand-in body for a view that is not built yet ("New 1 is on its way … Until it lands, Old has everything." + **Back to Old**). PR 5 and PR 6 replace the placeholder with the real views.
- [`BidsTakeoffTab.tsx`](../../src/components/bids/BidsTakeoffTab.tsx): `takeoffView` state + `switchTakeoffView`; the pills render after `BidWorkflowTabTitleWithPreview`; the whole Old body (materials-model toggle, book selector + Apply, both tables, the PO section) is wrapped in a `takeoffView === 'old'` branch, byte-for-byte otherwise. The header (title, pills, Print, ×), the takeoff-book admin section, and the materials summary section render for every view.

Verified from a worktree dev server on BP396: pills render; New 1 / New 2 show their placeholder; Back to Old and the Old pill restore the sheet unchanged; the choice survives a reload; render smoke unchanged.
