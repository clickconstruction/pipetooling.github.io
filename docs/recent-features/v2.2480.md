# v2.2480 — The RFI spine: bids_rfis + the queue (2026-08-29)

RFI loop Phase R1 (+ the PT half of R2's clipboard seam), per `docs/RFI_LOOP_PLAN.md`:

- **Migration `20260829023703_bids_rfis`**: per-bid numbered RFIs (trigger-assigned under a
  UNIQUE — a raced duplicate fails and retries, never renumbers silently), status machine
  draft→approved→sent→answered|withdrawn, `sent_to` jsonb (per-RFI GC pick), source
  manual|ct_note|substrate. Staff RLS (bid_proposal_rooms pattern) + **twin draft-only as
  RESTRICTIVE per-command policies** (INSERT/UPDATE/DELETE gated to status='draft' for
  `is_digital_twin` users — SELECT untouched) + all three house appliers.
- **Kernel `src/lib/bids/rfiFlow.ts`** (+9 tests): transition table (answered/withdrawn
  terminal; approved can fall back to draft), `parseCtRfiFlags` (CountTooling's Copy RFI
  Flags clipboard format), `rfiAuditNote` (the ledger stamps), `openRfisAtSend` (the R5
  letter-chip feed).
- **`BidRfiQueue`** on the RFI tab above the letter composer: quick-draft, paste-import of
  CT flags, Approve→GC checkboxes (default all bidding GCs from bid + `bid_gc_recipients`)
  + channel select, Mark sent, Record answer (+`answer_ref`), Withdraw. Updates guarded by
  the v2.2454 `updateApplied` pattern + a stale-status eq-guard; missing-table errors are
  fail-soft (client ships ahead of the migration). Ledger stamps are method-less bid notes
  (v2.2413: never move the chase clock).
- Help guide `track-rfis-on-a-bid`; CT capture half shipped in the counttooling repo
  (features/rfi-flags.js, same day).

Deploy: client first, then `supabase db push`; gen-types follow-up (component uses the
untyped-client cast until then).
