# v2.2637 — Collect Payment for superintendents (2026-09-02)

Item 1 of the owner's follow-up list — the last super-vs-sub gap on the Team Ready to Bill rows. Deliberately narrow: only role gates widened; every structural safeguard stays.

- **Migration** `20260902152109_collect_payment_superintendent.sql`: the three client-callable collect RPCs (`get_collect_payment_certify_payload`, `add_collect_payment_fixture_from_job_book`, `submit_collect_payment_certification`) rebuilt verbatim from the baseline with one change each — role gate `('subcontractor','helpers')` → `+ 'superintendent'`. The `jobs_ledger_team_members` requirement on the ready-to-bill job is unchanged (the Collect button only appears on assigned rows; no project-based path was added), and the office approval step (`approve_collect_payment_for_terminal`) is untouched.
- **Edge fns** (redeploy both): `send-stripe-invoice` — superintendents ride the strict field path (team member + `approved_for_terminal` flow), never the office RLS path; `update-collect-payment-stripe-customer-email` — role gate widened, same team-member + flow checks below it.
- **Client**: the Collect button gate in `DashboardTeamReadyToBillSection` widens to superintendent (`buttonVariant` plumbing untouched — note it's vestigial: no shipped RPC returns `collect_payment_button_variant`, so the button always renders the default style for every role).
- Docs: BILLING_FLOWS (flow step 1 + edge table), EDGE_FUNCTIONS (both function sections), ACCESS_CONTROL superintendent bullet; help guide `ready-to-bill-pipeline.md` roles/copy.
