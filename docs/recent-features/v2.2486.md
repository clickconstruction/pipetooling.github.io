# v2.2486 — drive-intake + file_plans (2026-08-29)

Estimator-twin pipeline Wave 4.4. `supabase/functions/drive-intake`: service-account Google
auth (RS256 JWT grant via WebCrypto — GOOGLE_SERVICE_ACCOUNT_JSON + DRIVE_JOBS_FOLDER_ID
secrets, setup runbook `docs/DRIVE_INTAKE_SETUP.md`), find-or-create the job folder named
after the bid's project, optional fetch-and-upload of `plans_url`, set-if-empty stamps on
`drive_link`/`plans_link`, and the `[pipeline STG-1]` audit note. Dual auth: X-Twin-Token
(assignment-is-the-grant enforced) or staff JWT (estimator+). Returns 503 with the setup
pointer until the secrets exist. twin-mcp gains the `file_plans` pass-through tool.

Deploy after merge: `supabase functions deploy drive-intake` + `deploy twin-mcp`
(the function answers 503-not-configured until the owner completes the runbook).
