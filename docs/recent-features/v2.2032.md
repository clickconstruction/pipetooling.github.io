# v2.2032 — Statements tab: collapsed charges + ack column removed (2026-08-21)

Owner request on [`PartnershipStatementsTab.tsx`](../../src/components/partnerships/PartnershipStatementsTab.tsx). Client-only — no migration.

- **Charges to put on this statement starts collapsed**: since charges book at their occurred date (charges-at-date, v2.1967) the attach list is bookkeeping detail. The header line now always shows "attaching N of M · ±$X" (net of the current selection) with a **show ▾ / hide ▴** toggle; expanding reveals the same checkbox list. Selection state and the below-zero rule are unchanged.
- **Acknowledgment column removed from the archive**: the "company — / awaiting partner" chips are gone; rows show week, hours, gross, and paid only. The `statement_acknowledgments` machinery (RPC, partner-side Acknowledge button) is untouched — this only stops surfacing the chips office-side; the closing footnote no longer promises acknowledgment tracking here.
- The `override guard (logged)` checkbox stays: `generate_partner_statement` blocks on unapproved sessions / unreviewed worked jobs in the week; the override generates anyway and is logged (partnership event + called out in the success message).
