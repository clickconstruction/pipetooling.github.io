# v2.2472 — The Bid Room, Phase 4: change orders join the room (2026-08-28)

The room becomes what it wanted to be — the GC's documents thread (plan rev 3; the
v2.1835 Bids→Estimates CO bridge already made COs signable, so this phase is routing +
visibility, no new signing machinery):

- **Migration `20260828230822`**: `estimates.bid_room_id` + the `document_published`
  room event.
- **Publish to bid room** on a bid-linked CO draft (beside Send to customer): freezes the
  CO (status sent, `bid_room_id` set — no email, no per-CO token; the room link is the
  credential), logs `document_published`, and the estimate's sent view says "In the bid
  room" instead of dangling accept-link copy. Room matching: the CO's customer's room,
  else the bid's only open room, else the own-GC room — ambiguous cases toast for a
  customer fix rather than guessing.
- **The room page grows a Change orders section** (mockup 4): each CO card shows the
  narrative (description/reason/schedule), signed cost detail (negative credits render
  −$), and its own state — pending cards carry **Review & sign** (name + consent +
  Approve, or Decline with an optional note). CO answers update the estimates row
  (customer_accepted / declined + acceptor fields + optional signature PNG) via
  `sign-bid-room`'s new `documentId` — and **never** touch the proposal's outcome or the
  bid's won/lost.
- **`get-bid-proposal-room`** returns `documents`; proposal-outcome resolution now
  ignores CO events (metadata.kind='change_order') everywhere — fetch, sign guard, and
  the `bidRoomState` kernel.
- **Chips count the thread**: `rev 2 · opened 3× · CO awaiting`, `✍ signed — Base bid
  $249,971 · 2 COs signed`; a pending CO keeps the chip amber even after the proposal is
  signed, across the Send-to strip, board GC lines, and Waiting to hear.

**Deploy after merge**: `supabase functions deploy get-bid-proposal-room sign-bid-room`.
