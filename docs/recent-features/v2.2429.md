# v2.2429 — per-twin credentials + rate limit + harness kit (2026-08-28)

The external-provider prerequisite from `docs/DIGITAL_TWINS_PLAN.md` (owner: offer twin
seats to other agent providers, e.g. Grok/xAI):

- **Migration `20260828080000`**: `twin_credentials` (twin_user_id, sha256 `token_hash`
  UNIQUE, label, revoked_at, last_used_at). Dev-only SELECT/INSERT/UPDATE (devs mint
  tokens client-side — plaintext shown once, only the hash stored); service-role for the
  edge function. All three appliers rerun (read-only ×2 + twin fence).
- **`twin-login` v2**: two auth paths — master `X-Twin-Login-Secret` (ops, any twin) or
  per-twin `X-Twin-Token` (sha256 → credential row; revoked → 401; the token can only
  mint its own twin, and a mismatched body email → 403). Guards unchanged (fleet email
  pattern + `is_digital_twin` + estimator). **Rate limit**: ≥6 mints in 60s per twin →
  429 with `retry_after_seconds` (counted via `twin_runs`). Mint log now records
  `via=token:<id>` vs `via=master`, and bumps the credential's `last_used_at`.
- **`docs/twins/TWIN_HARNESS.md`**: the provider-agnostic onboarding kit — endpoint +
  auth, session-expiry semantics, what to load into the agent, rules of engagement
  (browser-only, safety rungs, human-send gate, data-sensitivity note), scoring, and the
  owner-side runbook (mint twin, issue/revoke token, graduate rungs, audit).
- Deploy after merge: `supabase db push` + `supabase functions deploy twin-login`.
