# v2.3794 — The “signed” email subject loses its dash (2026-09-23)

The owner's pick after v2.3793: `Dana Ruiz signed $4,250 · Second-floor rough-in` — the em dash between *signed* and the amount goes.

- [`signedAgreementEmail.ts`](../../supabase/functions/_shared/signedAgreementEmail.ts) and its app twin: `${who} signed ${amount}[ · ${work}]`.
- [`emailLogStreamLink.ts`](../../src/lib/emailLogStreamLink.ts) — the Signed agreements pattern takes the dash as optional, so v2.3793's rows still link.
- Catalog example, guide *get notified when a bid or estimate is signed*, `EDGE_FUNCTIONS.md`, `ACCESS_CONTROL.md`.

Deploy: `supabase functions deploy accept-estimate sign-bid-room`. No migration.
