# v2.2426 — Digital twin identity, Phase T1+T2-lite (2026-08-28)

First build phase of `docs/DIGITAL_TWINS_PLAN.md` (role-impersonating agent accounts that
test and eventually work the app).

- **Migration `20260828060000`**: `users.is_digital_twin boolean not null default false`
  (additive; comment documents the contract). No new table — the `twin_runs` ledger is the
  next phase.
- **Banner**: `useIsDigitalTwin` hook (separate fail-soft query — a missing column means
  "not a twin", never a broken app) + a persistent violet "🤖 DIGITAL TWIN — <name>"
  strip in `Layout`, sitting beside the training-mode banner.
- **Active Accounts**: flagged rows get a 🤖 digital twin chip (fail-soft id fetch).
- **Dev-login twin alias**: `?as=twin:<role>` / `?as=twin:<role>:<n>` →
  `twin-<role>-<n>@twins.pipetooling.local` (kernel `src/lib/twinLogin.ts` + tests). The
  dev-login edge function already accepts any email behind the secret — no redeploy.
  Every other `as` value keeps the fixed dev account.
- Twin accounts are minted like any user (per-instance emails — twins are a fleet); they
  start `read_only = true` per the plan's safety ladder.
- Deploy order: client first (everything reads the column defensively), `supabase db push`
  after merge.
