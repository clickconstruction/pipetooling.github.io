# v2.3506 — Supply house credits: close-out (2026-09-16)

Client only, one string. Closes the four-PR train from `to-dos/supply-house-credits/` (v2.3500 the readers, v2.3501 the two refused edges, v2.3502 the migration, v2.3503 the form).

## What changed

- [`SupplyHousesTab.tsx`](../../src/components/SupplyHousesTab.tsx) — the delete confirmation reads *Delete this credit?* on a credit memo. The live pass caught it still saying *invoice*, which broke v2.3503's promise that nothing on screen calls a credit memo an invoice. It reads the stored row through `documentKindFromRow`, the same way the edit form does.
- The to-do itself was retired in #3262 (another session's close-out), which left `to-dos/supply-house-credit-residuals.md` for the two residuals: pairing a credit to its invoice, and the separate card-refund sign bug on the Mercury side.

## Live round-trip, on prod, after the migration push

Signed in as dev on the local app against the production database: **Materials → Supply houses → Reece → Add Invoice**, picked *Credit*, credit number `TEST-CREDIT-DELETE-ME`, $888.10, job J878.

- The form relabelled itself (*Add credit*, *Credit #*, *Credit date*, `− $`, *Applying it*, *Open — still on the account*, *Save credit*) and the effect line read *Takes $888.10 off what we owe Reece, and $888.10 off J878's parts cost.*
- After Save: the aging table grew a **Credits open** column with −$888.10 on Reece and in the footer, the last column read *Owed*, and the owed total stayed at **$114,543.96** — a credit never joins an owed figure. Reece's invoice list showed the row in green with the *credit* tag.
- Database: one row at `-888.10`, `document_kind = 'credit'`, allocated 100% to J878; the new `amount_check` constraint live.
- Opening the row read it back as *Edit credit* / *Open — still on the account*. Delete removed it and its allocation; the column disappeared; 466 rows, 0 credits, as before.

The two email renderers (`paid-job-email` v37, `weekly-money-email-dispatch` v27) were redeployed after v2.3500 merged.
