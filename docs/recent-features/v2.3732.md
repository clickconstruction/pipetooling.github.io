# v2.3732 — Day book: devs and controllers only, for now (2026-09-22)

The owner, on the finished train (punch list #20): *"No one should have access to day books except for the devs and controllers at the moment."* Decision 2 of [`to-dos/day-book`](../../to-dos/day-book/README.md), answered. One migration (the RPC's gate), client.

## The change

- **[`20260922170000_day_book_access_dev_controller.sql`](../migrations/20260922170000_day_book_access_dev_controller.md)** — `get_day_book_payload` re-created on PR 4's body with the gate narrowed: `is_dev()` or `role = 'controller'` → everyone, with amounts and the person pick; every other role → `{'error':'forbidden'}`. The pay-approved-master and self-only branches are gone.
- **`usePeopleAccess`** — `canSeeDayBook` / `canPickDayBookPerson` are true for dev and controller only. Who's where used to share the Day book flag; it gets its own `canSeeWhosWhere` (every office role, unchanged) so narrowing one did not hide the other.
- **Crew Day** — the office person's outcome line is read for dev and controller viewers only.
- Docs: `ACCESS_CONTROL.md` (the People row), `PEOPLE_TABS_ARCHITECTURE.md` (the two flags), the guide's roles and *Who sees what*, the to-do's decision 2.

## Verified

- The Day book render smoke (the self-only viewer case still exercises the payload shape the RPC can answer with), the Crew Day smoke, `releaseNotes`, `helpGuideContent`, `check:todos`, `check-migrations`; typecheck, eslint.
- After the push: as the dev the tab reads as before; the `forbidden` branch is the RPC's own (not staged with another role from this session).

## Next

PR 6b (the Crew Day email's line), PR 4b (the Bids door), decision 6 (the nightly snapshot under the owner's account) — all gated the same way.
