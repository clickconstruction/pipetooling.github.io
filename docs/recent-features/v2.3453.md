# v2.3453 — `src/types/database.ts` regenerated after the job-account migrations (2026-09-14)

Mechanical; no migration, no edge function. `npm run gen-types:linked` after `20260914280000_estimator_job_accounts.sql` was pushed (drift 580/580). The entries hand-added across v2.3423–v2.3451 (`job_supply_house_accounts`, `supply_houses.job_accounts`, `supply_house_contacts.role/phone`, the send log's `supply_house_id/account_id/bid_id`, and the RPCs `list_job_account_strip`, `count_job_account_evidence_gaps`, `list_job_account_evidence_gaps`, `job_account_job_identity`, `list_bid_job_account_strip`) are replaced by the generator's output — the same fields in the generator's order (generated files are regenerated, never hand-merged). Typecheck clean.
