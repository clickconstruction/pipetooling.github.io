# v2.2532 — Robot bid comparison modal (2026-08-31)

PR-2 of the robot readiness icon (PR-1: v2.2530). The colorful 🤖 click now opens
`RobotBidComparisonModal` instead of deep-linking straight to the Robot Board:

- **Comparison table**: bid total (real `bid_value` when set, else draft-priced with the
  Audits-tab rules — override beats book price, active version rows only), count rows,
  fixtures/equipment (ea), and total footage with a robot/ours ratio.
- **Kernel**: `src/lib/bids/robotBidComparison.ts` — `classifyCountRow` (footage /
  fitting / fixture by name pattern), `summarizeDraftForComparison`, `percentDelta`;
  unit-tested (6 tests).
- **Jump chips**: robot counts/pricing and our counts/pricing via `selectBidAndSyncUrl`;
  robot CountTooling takeoff via the twin bid's latest `bid_audits.ct_view_url`
  (untyped-client cast, BidsAuditsTab pattern). "Open on Robot Board" keeps the PR-1
  deep-link behavior.
- Board prop change: `robotReadiness.onOpenTwinBid(twin, source)` now carries the
  source bid so the modal has both sides.
