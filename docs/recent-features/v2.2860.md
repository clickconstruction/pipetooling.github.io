# v2.2860 — The bench reaches the pickers and the nags (2026-09-05)

PR 2 of the Subs bench (v2.2853, owner pick C). Stacked on `claude/subs-bench`.

- **Work order assembler** (`WorkOrderAssemblerModal.tsx`): the roster select adds `end_date`; the Sub step lists active subs and hides benched ones behind a "+ N on the bench" pill (`showBench`). Benched subs stay pickable (a gray "bench" marker, muted text, a tooltip saying to Reactivate on People → Subs once they accept) — offering work is how a sub comes back, and the Subs tab's `reactivate` nudge fires once the offer is accepted.
- **Subs tab Assign… picker** (`PeopleSubsTab.tsx`): the roster state carries `benched`; the select renders `<optgroup>` Active / On the bench when anyone is benched.
- **Rail facts** (`loadRailFacts.ts`): `benchPersonIds` from `people` (kind sub, `end_date` not null). **Attention kernel** (`deskRailAttention.ts`, +test): a benched person gets one gray `bench` signal ("on the bench") and returns early — unsent / expiring / expired paperwork and the no-roster-row nag are skipped ("nags paused"); `portal on` still shows; sessions waiting are pushed before the early return, so money still counts and attention stays amber for them. Feeds the Users tab row chips and the Person tab rail.
- Guide `manage-subs-end-to-end.md` bench section gains the picker and nag lines.
- **Not done (needs the roster `Person` type widened):** the Jobs → Sub Labor sheet form's crew name lists (`JobsSubLaborFormModal.tsx` `byKind('sub')`) still mix benched subs in alphabetically. Bench / Reactivate from the Person Desk is also a follow-up.
- No migration.
