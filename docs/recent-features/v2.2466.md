# v2.2466 — Silent no-op guard sweep: every bids UPDATE checks affected rows (2026-08-28)

Mechanical sweep (merge-alone, per the CLAUDE.md sweep rule) completing v2.2454/v2.2461: an
RLS-filtered UPDATE on `bids` (digital-twin write fence, bid deleted mid-edit, primary-scope
policies) returns success with **zero affected rows**, so unguarded call sites silently dropped
the user's change. v2.2454 guarded the two Edit Bid save paths and v2.2461 the Lost-summary
loss-reason door; this sweep applies the same pattern — append `.select('id')`, check
`updateApplied(rows)` (from `src/lib/bids/updateGuard.ts`), surface
`BID_UPDATE_NOT_APPLIED_MESSAGE` — to the **24 remaining `from('bids').update` sites**:

- `src/pages/Bids.tsx` — archive-working-board, submission-link quick add, notes modal (3)
- `src/components/bids/BidsCallQueueTab.tsx` — call outcome, loss reason, bid tab (3)
- `src/components/bids/BidsCoverLetterTab.tsx` — alt texts, payment-schedule toggle (with
  optimistic-state revert), mark-sent simple, mark-sent roll-up (4)
- `src/components/bids/BidsCountsTab.tsx` — count source link (1)
- `src/components/bids/BidGcSentPanel.tsx` — per-GC sent roll-up (1)
- `src/components/bids/BidLostQuickPopover.tsx` — quick lost capture, bids branch (1)
- `src/components/bids/BidWorkingBoardArchivedModal.tsx` — un-archive (1)
- `src/components/bids/BidsWaitingToHearLens.tsx` — chase outcome, remove bid tab (2)
- `src/components/bids/BidsWhyWeLostLens.tsx` — bid tab, loss category (2)
- `src/components/bids/BuilderCallSessionModal.tsx` — outcome + bid-tab loops (2)
- `src/components/bids/BidsLaborTab.tsx` — distance from office (1)
- `src/pages/Documents.tsx` — add bid link modal (1)
- `src/hooks/useBidPricingEngine.ts` — takeoff/labor/price-book/bid version pickers +
  materials-model switch (5)
- `src/lib/bids/gcPacketOutcome.ts` — the two bid-level outcome roll-ups (these previously
  ignored even hard errors; both now flow through the function's `{ error }` return) (2)

Feedback rides each surface's existing channel: sites inside a `try/catch` with a
toast/`onError` handler `throw new Error(BID_UPDATE_NOT_APPLIED_MESSAGE)`; direct
`{ error }`-style sites add an `updateApplied` branch beside the error branch (`setError` /
`showToast`). No behavior redesign — success paths are unchanged.

**Deliberately skipped:** `JobFormModal.tsx`'s two `agreed_value` backfills filter on
`.is('agreed_value', null)`, so zero affected rows is the *expected* "already set" outcome —
guarding them would false-alarm.

**Out of scope / follow-up:** UPDATE sites on twin-fenced bid child tables
(`bid_versions`, `bids_count_rows`, `bids_submission_entries`,
`bids_takeoff_rough_part_lines`) have the same silent-no-op exposure (their INSERTs error
loudly; UPDATEs do not) and would want a table-appropriate message rather than the bid-specific
one — left for a separate sweep.
