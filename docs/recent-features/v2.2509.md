# v2.2509 — Dash-aware tracing: density.ts + per-run registration bars + the east wing (2026-08-30)

Finishes what v2.2508's honesty exposed: the east-wing water distribution was RFI'd as
untraced because its dash-dot styles defeat single-row scanlines.

- **`scripts/placement-engine/density.ts`** — the dash-aware line finder: integrates
  ink ALONG the candidate run direction over a ±1 px band; a dashed line surfaces as a
  fill band at its exact coordinate with its duty cycle (solid ≈90%+, dash-dot 25–70%,
  noise ≈0–5%). One pass over the east wing found six water bands and seven fixture-spur
  verticals mapping one-to-one onto the east fixtures (x=729→WC-12, 785→L-4, 846→WB-1,
  918→DF-4 …).
- **Per-run registration bars**: manifest lines take `minPct` — the bar matches the
  style's dash duty (solid 75+, dash-broken 25–40), printed with each verdict. Finding:
  most east runs then beat their bars ~2× (the ±1 px band bridges dashes better than
  raw duty predicts).
- **East wing traced and landed**: 10 new runs, every one registration-clean; the DF-4
  spur extended from a follow-probe; **connectivity fully clean for the first time —
  every fixture within 6 ft of a run**. Totals: SAN 159.2 · CW 179.3 · HW 86.5 ·
  Gas 22.3 ft; 28 derived fittings; 55 counters + 19 runs across 6 toggle-able layers.
  Open RFI: the east CW/HW pair identities are style-assigned — reviewer confirms.
- PLACEMENT.md workflow now leads with density (locate + duty) before follow/snap;
  M6-v3 results row records the run and its stumbles.
