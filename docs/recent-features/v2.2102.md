# v2.2102 — Vehicle task edit + activity spine (2026-08-22)

Owner request on People → Vehicles: edit a maintenance task and add notes "like on the other screens for checklist items" — so the build reuses those screens' exact patterns rather than inventing new ones. In [`PeopleVehiclesTab.tsx`](../../src/components/people/PeopleVehiclesTab.tsx): (1) **Edit** button per open task → modal with title + note; save updates `vehicle_maintenance_tasks` and, when bridged, rewrites the linked `checklist_items.title` via `maintenanceChecklistTitle` so the assignee's copy never drifts; the note previews in italics under the row title and feeds the assignee's 🚗 vitals card (v2.2094 RPC reads `vmt.note`). (2) Tapping an assigned task's title expands [`ChecklistItemActivity`](../../src/components/checklist/ChecklistItemActivity.tsx) — the same spine as Today/Manage/Review, with `commentInstanceId` pinned to the bridged instance and `onComplete` wired to `completeMaintenanceTask` (both-ways completion + the log-service nudge, unchanged). Unassigned tasks (no checklist bridge) get Edit only — no thread exists yet. Help guide updated. Client-only — no migration.
