# v2.2676 — Dashboard Overhead card + the shared 90-day snapshot loader (2026-09-03)

Owner ask: expose the overhead lenses in the Dashboard finance section, devs and masters only, tastefully. Built as a fourth tile in the money row speaking the neighbours' grammar (money headline → bar → tone line → detail lines), gated exactly like People → Overhead.

## Client only — no migration

- **The one scan, lifted**: [`lib/overheadPoolSnapshot.ts`](../../src/lib/overheadPoolSnapshot.ts) — `loadOverheadPoolSnapshot(inputs)` is the Overhead tab's 90-day KPI/lenses effect body moved verbatim (three paged session queries, parts via the internal-transfer-excluding loader, invoices Chicago-bucketed with test-mode excluded, then every kernel: daily labor, field denominators, hygiene, trailing averages, rate methods, pool trend, lens series, people lines). The tab's effect now just calls it and unpacks; `loadOverheadPoolSnapshotInputs()` fetches the three page-provided inputs (office job id, pay-config wages, users→people links) for consumers outside People. Same code path on both surfaces — no SQL re-implementation to drift.
- [`hooks/useDashboardOverheadSnapshot.ts`](../../src/hooks/useDashboardOverheadSnapshot.ts): runs the loader behind a one-hour per-user, per-company-day `sessionStorage` cache of the JSON-safe slice the card needs; fails soft.
- [`lib/dashboardOverheadCard.ts`](../../src/lib/dashboardOverheadCard.ts) (+6 tests): glance strings (headline `$437/day`, lenses line, trend pill, composition segments/line) and the cache freshness rule.
- [`components/dashboard/DashboardOverheadCard.tsx`](../../src/components/dashboard/DashboardOverheadCard.tsx): self-gating (`role === 'dev' || (master && usePeopleAccess.canAccessPay)` — the tab's `canAccessOverheadTab` rule, so nobody sees a number they can't drill into), IntersectionObserver-deferred load, finance-card shell, composition bar in the aging-bar slot, lens buttons + headline opening `OverheadLensModal`, "Open tab ›" link. `DashboardFinancialsSection` gains an `overheadCard` prop rendered inside its grid; `Dashboard.tsx` passes it for dev/master roles.
- Docs: `DASHBOARD_SECTIONS_ARCHITECTURE.md` role-gating row; help guide `drill-into-the-dashboard-money-cards.md` gains "The Overhead card".
