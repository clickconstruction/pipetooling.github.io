# v2.2739 — Wheels: card purchases only, unattributed cards named, off-card label check (2026-09-04)

Fix on v2.2733 / v2.2735. Live data showed "$37.8k of fuel has no person on it"; $36.7k of that was one ACH to HAJOCA filed under a vehicle-family accounting label. Actual card fuel is 99% attributed (16 of 19 cards carry an auto-assign user).

- [`lib/people/wheels.ts`](../../src/lib/people/wheels.ts) (+2 tests): `splitFuelFamily(rows)` — only rows with a Mercury debit card AND `kind = debitCardTransaction` are fuel; the rest become `offCard { usd, n, top counterparties }`. `unattributedFuelByCard(charges, nicknameByCard)` groups card fuel with no `user_id` by card (nickname, else `card …last4`).
- [`wheelsData.ts`](../../src/lib/people/wheelsData.ts): transactions select adds `kind` + `counterparty_name`; `raw` is fetched only for fuel-family rows (chunked) to read the card id via `mercuryDebitCardIdFromRaw`; nicknames from `mercury_debit_card_nicknames`. Snapshot gains `unattributedCards` and `offCardFuelFamily`.
- Wheels section: the unattributed line lists cards to link (title points at Banking → Sorting → User Card Link); a "Not counted" line names the off-card rows so the label can be fixed.
- Review union loader: the deal's fuel exclusion also requires `kind = debitCardTransaction` (categories select adds `kind`).
- Help guide + GLOSSARY updated.
