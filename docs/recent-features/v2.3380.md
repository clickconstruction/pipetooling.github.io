# v2.3380 — Accounts Receivable refresh, PR 2: the deposit header, the remaining meter, the memo fold, allocation rows (2026-09-13)

Client only; no migration, no edge function. Second of the train in [`to-dos/accounts-receivable-refresh/`](../../to-dos/accounts-receivable-refresh/README.md) (PR 1 v2.3379 the header and list).

## What shipped

- **[`ArDepositHeader`](../../src/components/jobs/ar/ArDepositHeader.tsx)** replaces the *Amount · Remaining to allocate* band and the *Posted* / *Kind* rows: the counterparty as the headline (Returned chip beside it when flagged), *$1,855.70 · check · posted Sep 11, 2026*, the bank's note and memo quoted underneath, then the **remaining meter** — left label (*Nothing allocated yet* · *$1,518.22 applied to jobs* · *Allocating $400.00*), a bar filled by (applied + lines on screen) ÷ the deposit, and the right figure (*Remaining $1,000.00* · *Fully allocated ✓* · *Over by $20.00* in red).
- **Kernel [`arAllocationProgress.ts`](../../src/lib/jobs/arAllocationProgress.ts)** (+4 tests): `arAllocationProgress({ amount, consumed, remainingAvailable, lines })` → allocatedNow · remainingAfter · pctFilled · over; `parseAllocationDollars` is the same lenient parse the inputs use.
- **Allocation rows**: each line is a grid — kind (the Billed line / Payment received toggle when recorded payments exist, else the word) · target picker · amount (right-aligned, tabular) · × — ruled between rows. The payer chips, quick matches, picked-target detail and the link guard stay under the picker for now (PR 3 lifts them into the bill list). *Add Additional Allocation* is now **+ Split across another bill** under the rows.
- **Memo fold**: the textarea leaves the top of the pane; **· Add a note** beside the split link opens it (labelled *Note on this payment — internal, shows on the job's Payments received*); it stays open once it has text and closes on the next deposit.
- The three render suites pass unchanged (the toggle keeps its role and words, the amount input its label). Guide *match bank deposits to the bills they pay* names the split link, the note and the meter.
