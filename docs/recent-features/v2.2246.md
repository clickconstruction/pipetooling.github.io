# v2.2246 — Sub Labor tab: cents rendered smaller on all amounts (2026-08-24)

Owner: "all the '.00' decimals at the end of pay amounts [in] a much smaller font so it is still easy to copy and paste but it's much easier to identify amounts in the thousands."

- **Component** [`AmountSmallCents.tsx`](../../src/components/jobs/AmountSmallCents.tsx): renders `$` + dollars + a `0.7em` span for the `.NN` cents. Plain text nodes — selection/copy still yields the full `$40,000.00`.
- **Kernel** `splitFormattedAmountCents` in [`jobFormatting.ts`](../../src/lib/jobs/jobFormatting.ts) (+ tests): splits a `formatCurrency` string at the decimal point, sign preserved, empty cents when no point.
- **Applied across [`JobsSubLaborTab.tsx`](../../src/components/jobs/JobsSubLaborTab.tsx)**: Sub Labor Due headline, Outstanding-by-contractor table (Total cost / Paid / Outstanding / Total), ledger rows (Total cost, `$… due`, `Over $…`), and the expanded drawer (Total cost · Paid · Backcharges caption, line-item Rate + Cost, payment Amounts). Cell `title` tooltips keep plain full-size strings. Non-zero cents (`.50`) shrink too, for consistency.

Client-only; no schema changes.
