# v2.3063 — Active accounts: the dead Convert-leader section removed; archive reassigns to the company owner (2026-09-07)

Two one-company leftovers found on 2026-09-07 while closing the plan ([`docs/ONE_COMPANY_PLAN.md`](../ONE_COMPANY_PLAN.md)).

- **Convert Leader to Assistant/Subcontractor** (Settings → Active accounts, dev): the form called `supabase.rpc('convert_master_user', …)`, a function that exists only in `supabase/archive/convert_master_user.sql` — never in a migration, never on prod — so every submit failed. Section, its eight state hooks and `handleConvertMaster` removed from `ActiveAccountsPanel` / `useActiveAccountsManagement`. Role changes still happen on the account row.
- **Archive user → customers**: the "Reassign them to another leader" radio no longer opens a leader picker; it reads "File them under the company owner account" and `handleArchiveConfirm` resolves the target with `resolveCompanyOwnerUserId` at submit (the `archive-user` edge function's `reassign_customers_to` is unchanged). `archiveChoiceBlocker` loses the "pick the leader" rule (kernel + test); `eligibleReassignTargets` stays exported for the roster tools. Guide `archive-user-accounts` updated.
