# v2.2209 — Dispatch task modal button order (2026-08-23)

Owner feedback: in [DispatchTaskModal.tsx](../../src/components/DispatchTaskModal.tsx) the footer
had the primary "Send to Dispatch" button on the LEFT with Cancel to its right. Swapped to the
app-wide convention — the row is now `justifyContent: 'flex-end'` with Cancel first (left) and
the blue Send to Dispatch primary on the right. No behavior change.
