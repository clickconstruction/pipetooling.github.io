# v2.2073 — My Schedule card redesign (mockup A) (2026-08-22)

Owner screenshot review of Abraham's phone, round 2 (after the v2.2067 Assigned Jobs cards): the My Schedule card's title row held the call/pictures icons AND the Leave Report button, squeezing "Tovi Pinpoint & Gas Test" into a 4-line ~96px column; "N% done ▲ N today" rendered twice (the v2.1591 right-column stack + the v2.1567 bar legend); the v2.1547 report badge overlapped the button. Mockup (Current + 3 variants) → variant A picked: same anatomy as the Assigned Jobs cards, so the dashboard reads as one system.

[`DashboardMyScheduleSection`](../../src/components/dashboard/DashboardMyScheduleSection.tsx):

- **Title owns the card width** — the right-hand icon/button column is gone; reading order is now *name → time → number·address → dispatch note → sent-back line → utilities → progress → actions*.
- **Utility row** below the info block: 42px call + pictures buttons (v2.2067 atoms from `dashboardJobRowShared`) plus a labeled **reports chip** ("▤ 3 reports" → opens the job's reports; amber-tinted while a report is due). Replaces the overlapping corner badge; `DashboardLeaveReportButton` is no longer used here.
- **One % display** — the right-column % stack is deleted; the bar (unchanged v2.1567 idiom: blue = day start, green = gain, amber = correction) now renders whenever pct is known (previously only on moved days) with a single legend: bold `N% done` left, `▲/▼ N today` / `no change today` right.
- **Action row** — Leave Report (ghost; amber solid "Report due" when the v2.1549 reminder fires) + Update % done (v2.1806 semantics kept: outline all day, solid blue after the block ends) share one 44px row.
- Render smokes updated: single-%-display pins replace the old stack/bar duo pins; help guide (`update-percent-done-from-my-dashboard.md`) copy refreshed.

Verified live as Abraham (login-as-user, 402×874, dark + light). Client-only — no migration. Known follow-up: `DashboardTeamReadyToBillSection` rows (the third row-family sibling) still use the old floating-icon + corner-badge layout.
