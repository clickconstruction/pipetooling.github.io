# v2.1973 — Recently added into the ☰ tools menu (2026-08-21)

The "Recently added" pill (v2.1809's flat last-100 view toggle) floated at
the right of the stage jump strip. It now lives as the FIRST item of the ☰
section-tools menu's Pipeline group (`stagesSectionToolsMenu.ts` gains the
`recently-added` key + a `recentViewOpen` input that flips the item's label
to "Back to board" while the view is open; every role gets it — the pill had
no gate).

Exit affordance kept prominent: the strip pill now renders ONLY while the
flat view is open, as the "Back to board" control — idle boards lose the
button, open views keep an obvious way home. Verified live: menu item first
in the group, opens the view, pill appears/exits, pill absent when idle.
Kernel tests updated (+2, 14 total).
