# v2.2560 — Twin confidence scoreboard (dev) + twin_run_scores (2026-08-31)

Fleet-roadmap deliverable: the per-axis Gate-B view, mockup-first
(design review artifact cf437df2). New dev-gated **Scoreboard** lens in
the Bids robot group (Robot Board · Audits · Shadows · Queue · Scoreboard):

- **Axis cards**: the 5-slot Gate-B bar is the primary element — scored
  runs render as green/red slots carrying their deltas, in-flight shadows
  as dashed slots with their bid numbers; chip states GATE B MET /
  progress n/5 / BLOCKED (last run out-of-band with a note) / AWAITING
  SCORE; a Next line built from streak math, in-flight count, and the
  latest run's note.
- **Pipeline pills**: pending audits (amber when >0 — the bottleneck),
  shadows awaiting score, scored runs on record.
- **Unified run ledger**: backtests + shadows, newest first; VOID runs
  struck-through with their verdict rather than hidden.

Kernel `src/lib/bids/confidenceBoard.ts` (unit-tested; gate rule mirrors
`shadowStory.ts`), component `BidsRobotScoreboardTab` (render smoke).

**Migration `20260901090000_twin_run_scores.sql`**: structured backtest
scores (freeform ledger notes can't feed a UI), seeded with the
BT-6..BT-16 trajectory including the BT-15 VOID row. Staff SELECT;
writes are service-role; all three fence appliers run.
