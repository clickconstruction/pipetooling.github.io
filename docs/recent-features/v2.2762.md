# v2.2762 — People → Users refresh: one row shape, no External groups, Add to roster (2026-09-04)

Owner picked variant A of the Users tab proposal (artifact 1904c46a) and retired the External groups; imitate stays a one-click icon by explicit decision. No migration, no permission changes.

## Client

- **`UsersTabRow`** [`components/people/UsersTabRow.tsx`](../../src/components/people/UsersTabRow.tsx): one row for account and roster-only rows — the two imitate buttons (prod host / localhost) **unchanged** apart from a hover label, attention dot (title = reasons), `PersonNameDoor`, `login` / `no login` chip, contact links, account notes, signal chips, and a ⋯ menu. Menu items: **Open desk**; account rows **Edit name, title & phone** (`canEditUserNotes`); roster rows **Invite as user** (disabled without email), **Edit**, and for the owner or a dev **Link account**, **Combine…**, **Archive**, with "Created by X" as a footer when someone else owns the row. The far-right card icon and the five-button external cluster are retired into the menu. Active projects render under the row for everyone (the old external-only toggle is gone). The dev tags panel still renders under the row.
- **Signals** = the Person tab rail's facts, shared: [`lib/people/loadRailFacts.ts`](../../src/lib/people/loadRailFacts.ts) (sessions waiting per account via the v2.2694 kernel, unsent / expiring / expired paperwork and licenses per pay name, live sub portals) behind [`hooks/useUsersTabSignals.ts`](../../src/hooks/useUsersTabSignals.ts) (focus refetch, fails soft). `PersonDeskPage` now uses the same loader. `buildRailRow` gains structured `signals` (+ portal on as a blue, never-attention chip) — 1 new test. The contract traffic light becomes a chip only when it is not green ("docs waiting" / "docs unsigned"); "no push" shows only on field-kind account rows when push is off.
- **Groups**: still by kind in the house order, each header with "16 · 2 with a login" (`describeGroupCount`) and its Add; rows ordered account-first then alphabetical (`orderUsersTabRows`); roster-only rows beyond six fold behind **+ N more without a login** (`foldNoLoginRows`; a search or the No-login filter opens every fold). Devs render through the same row. Kernel [`lib/people/usersTabRows.ts`](../../src/lib/people/usersTabRows.ts) (+5 tests).
- **Toolbar**: sticky (search stays visible while scrolling); **Team leads**, **Accounts · dev**, **Archived (N)** (toggles and scrolls to the existing archived table), **+ Add to roster**; filter chips **Everyone · No login · Needs attention · Field · Office** (`rowMatchesFilter`); "Nobody matches that filter" when a filter empties every group.
- **Add to roster** (`People.tsx` form): title, the one-line explanation ("A roster row, no login. Their portal, paperwork and pay work from this alone…"), kind as radio pills, email and phone side by side, and two follow-ons — **Also invite them to sign in** (opens the existing invite confirm) and **Open their desk after saving** (default on when the Desk is available). `usePeopleRoster.handleSave` now returns the saved `Person` (null on validation failure) so the wrapper can act on it.
- Verified live as dev: the Subcontractors group with Behar and Michael A (login) followed by roster-only rows with the "no login" chip and the fold; filters; the ⋯ menu; Add to roster → desk opens on the new row (row deleted afterwards).

## Docs

- Help guide `add-someone-to-the-roster.md` (new, replaces the "external sub" confusion); `link-external-person-to-account.md` and `onboard-a-new-subcontractor.md` wording updated for the retired External groups; `PEOPLE_TABS_ARCHITECTURE.md` users row.
