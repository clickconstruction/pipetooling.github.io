# v2.2619 — Lien-release signing: autosave drafts, mint gate, in-app signature (2026-09-02)

PR 2 of the signing loop (v2.2616 laid the schema). **Requires migration `20260902001517` pushed before this deploys** — the modal now writes the lifecycle columns.

- `src/components/jobs/LienReleaseModal.tsx` rework: ✕ close (no Save/Cancel); drafts autosave debounced from the first real edit (`userTouchedRef` — opening never mints; resumed drafts skip the prefill rebuild entirely via `hydratedDraftRef`); **mint gate** — Print for signature / Download PDF / Request signature / Mark issued all record the row first via `ensureMinted` and abort on failure; fields/chips/form-type lock once minted; signature strip (amber awaiting → Sign now for the signer + Cancel request; green signed + audit line); history rows carry lifecycle chips; Copy for email removed (kernel email builders remain — the print doc is built from them).
- New `src/components/jobs/LienReleaseSignModal.tsx`: full document + the shared `ContractAcceptSignatureForm` (which grew optional `heading`/`disclosure`/`agreeLabel`/`submitLabel` props — contract surfaces unchanged). Signing stamps the row first (status/signed_at/printed name/mode/consent, `signer_user_id`), uploads the drawn PNG and the signed PDF to `lien-release-documents` best-effort.
- New kernels: `src/lib/jobs/lienReleaseLifecycle.ts` (status parse, editable/minted, chips, canRequest, audit line — tested) and `src/lib/jobs/lienReleaseDocuments.ts` (bucket + paths). `lienWaiverRelease.ts` grew `LienWaiverSignature` + `buildLienWaiverSignatureHtml`; print HTML and PDF take an optional signature (typed = Great Vibes cursive in HTML / italic serif in PDF + audit line; drawn = embedded PNG) — tested.
- Types: `job_lien_releases` columns hand-patched into `src/types/database.ts` (Relationships regen lands with PR 3's `gen-types:linked`).
- Signer = the job's `master_user_id`; requesting is allowed for the same office set that opens the modal. Inbox/Needs-You routing lanes and the send-to-customer loop are PR 4.
- Help: `give-a-customer-a-lien-release.md` rewritten around autosave + in-app signing.
