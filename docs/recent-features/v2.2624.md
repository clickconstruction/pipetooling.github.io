# v2.2624 — Division 22 rule corrections: full-universe verification sweep (2026-09-01)

Owner ask: "look back through all the names we've added and make sure they are
organized correctly." The green rules (v2.2606) had only been dry-run against
the audit RPC's silently capped top-1,000 names; this sweep classified all
**3,655** (pagination via `?limit&offset` — Range headers do NOT page RPC
POSTs) and re-filed **157 names**, each change dry-run before shipping and
spot-verified against the live ledger after apply.

Migration `20260902015631` (applied): 3 new sections (22 14 26 Facility Storm
Drains — resolves Decision Sheet amber #6; 22 31 00 Softeners; 22 32 00
Filtration), 32 new rules, 1 delete, 2 edits. The bug classes:

- **Seeded-rule precedence**: `WATER` (priority 220) ran before every green
  rule (400+), so any heater/softener/filter/fountain name containing "water"
  filed as piping. New 175–199 carve band fixes the class.
- **Word-boundary false positives**: `our ` matched "hour "/"Pour Back"
  (→ starts_with); `tub` matched STUB/tubes; `sand ` vs Sand-Oil Interceptor
  (ordering); `hose` claimed reels/medical kits (deleted, replaced by
  hosebib/hose drop); `ice m` matched "juice machine"; `SV` matched Schier
  product codes.
- **Rule-order theft**: `wash` (556) beat `sink`/`urinal` (580s), stealing
  wash sinks, the washdown urinal, and Eyewash → moved to 640 + `eyewash` rule.
- **Missing routes**: flush-valve WCs, Reduced Pressure Zone valves, sample
  wells/ports, stub-outs, `demo …` narratives (starts_with → no-code).

Interim calls flagged on the Decision Sheet: water meters → 22 11 19 pending
the owner's bonus answer; `scavenger` → deliberate no-code until the med-gas
ruling re-points it. Real full-universe coverage is ~61% pre-sweep (the 73%
figure was against the capped set) — the audit pagination fix is queued
separately (task chip).
