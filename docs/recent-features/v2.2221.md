# v2.2221 — People → HR: dev-only employee files (2026-08-23)

The client half of the HR files train (schema: v2.2220). New dev-only **HR** tab on People (`?tab=hr`, Scoreboard's no-URL-gate pattern — render site `isDev`-gated): [`PeopleHrTab`](../../src/components/people/PeopleHrTab.tsx), self-contained like `PeopleSubsTab`. Left roster grouped by kind (search, archived collapsed) with per-person freshness dot + entry count from the tested kernel [`personFileFreshness`](../../src/lib/people/personFileFreshness.ts) (green current / amber = entries newer than the summary, with days-behind / grey empty; "covers N of M entries" meta). Right panel: **Summary | Narrative | Raw entries** — curated docs render read-only (agent-maintained via `person_files`), the Raw composer is the only UI write (`entry_date` in `APP_CALENDAR_TZ`, source tag, `created_by`); the log is append-only at the RLS layer so the UI offers no edit/delete. Includes regenerated `database.ts`, render smoke, help guide `keep-private-employee-files`, and the agent writing convention doc `docs/HR_FILES.md` (+ ACCESS_CONTROL/GLOSSARY/PEOPLE_TABS_ARCHITECTURE updates).
