# v2.2519 — Audits tab wired to the one-button finish (2026-08-30)

Closes the Audits-tab train (v2.2516 schema → v2.2517 tab → v2.2518 edge fn):

- `BidsAuditsTab` finish/reopen now call the `audit-finish` edge function (one
  gesture = PT status + ledger stamp + CT project review flip); success toast says
  when the CT leg landed. Unreachable fn (local dev, pre-deploy) falls back to the
  previous PT-side-only writes — the digest sweep reconciles the CT lane later.
- `docs/twins/FEEDBACK_LOOP.md` rewritten to v2: the Audits tab replaces the
  three-channel model; documents the twin's pipeline-end steps (view link →
  audit row → seeded questions), the digest buckets recorded as `digest_outcome`,
  and the receipt contract.

Backfill (data, same day): b405's audit `e7523514…` pending with 4 seeded
questions + CT view link; the twin-can't-finish fence verified live (42501).
