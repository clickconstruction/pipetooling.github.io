# v2.2902 — Papercut batch B19: search RPCs escape wildcards and sort job numbers numerically; Settings search finds itself; live pins can't target a page without a footer (2026-09-05)

Journey map Phase 4, **Tier-3 B19** (`pipetooling-journey-map` → `docs/JOURNEY_MAP.md` row B19 "Search & pins"; families C68 ERROR-UX, C69 DOORS, C70 TEACH; findings J28-F1 / J28-N1 / J28-F3 / J28-F4 / J28-N2 / J28-F10). One migration + a small client change. Skipped by design: the pin-ritual entry cost (J28-F9 → Tier 4) and the lazy evidence-rail reflow (J28-F11, keep).

## Per item

| Item | Finding | Status | What shipped |
|---|---|---|---|
| Search RPCs escape `%` / `_` | J28-F1 | **done** | Migration [`20260905220000_search_rpcs_escape_and_numeric_order.sql`](../../supabase/migrations/20260905220000_search_rpcs_escape_and_numeric_order.sql): new `public.escape_like_pattern(text)` (`\` → `\\`, `%` → `\%`, `_` → `\_`; LIKE's default ESCAPE is backslash) applied inside `search_jobs_ledger`, `search_bids_for_clock`, `search_estimates_for_nav` to the typed text and to the remainder after a `J` / `B` / `E` or configured `ledger_*_prefix` strip. The prefix comparison itself became a literal `left(text, n) = prefix` equality (it was `text LIKE prefix || '%'`). Every caller inherits it — header search, Clock In (`ClockInOutButton`), Dispatch / Estimator task modals, Hours Unassigned, Crew Jobs, Assign Focus, Clock-session actions, Supply-house bills, Mercury allocations. Clients keep passing raw text; the existing client-side `escapeLike` (`unifiedJobBidSearch.ts`) stays on the one PostgREST `.ilike()` customer lookup only — never escape an RPC argument on the client too. |
| Job numbers ordered numerically | J28-N1 | **done** | Same migration: `search_jobs_ledger` orders by (has a number) → first digit run of the effective (HCP-else-Click) number as `numeric` DESC → text DESC. `K-25077-0` > `1200` > `1004` > `999`, click-only rows by their digits, letter-only numbers next, blanks last. `search_bids_for_clock` (by `project_name`) and `search_estimates_for_nav` (by `updated_at`) keep their order. |
| Settings search "Page pins" → the right tab | J28-F3 | **verified** (Tier-2 #17, PR #2630) | `settingsSearch.ts` entry already targets `settings-dashboard` + `#settings-page-pins`; the Settings deep-link effect opens the collapsed group. Not redone. |
| Settings search finds "search" | J28-F4 | **done** | New entry "Search for jobs, bids, customers, and estimates" (keywords `search`, `global search`, `search bar`, `find job/bid/customer/estimate`, `magnifier`, `cmd k`, `ctrl k`) on `settings-guides` with the new `guideSlug: 'global-search'`. `SettingsSearchEntry.guideSlug` + pure `settingsSearchGuideQuery(currentSearch, entry)` → `?tab=settings-guides&g=global-search` (other params kept); `Settings.tsx` `onPick` navigates there (`replace`) so the deep-link effect and `GuideBrowser` open the article together. `GuideBrowser`'s private `'g'` became the shared `HELP_GUIDE_URL_PARAM` (`src/lib/helpGuideUrlParam.ts`). |
| A live pin targets `/dashboard` | J28-N2 | **done** (block + explain) | `/dashboard` left `PINNABLE_PATHS` (`pinnedTabs.ts`), so neither the pin footer nor dev "Pin for someone" can create a self-pin; `PATH_TO_LABEL['/dashboard']` stays for legacy rows. `Layout.tsx`: with Pin Mode on, `/dashboard` renders a one-line `role="note"` footer ("Pins land here on the Dashboard, so the Dashboard itself can't be pinned — open the page you want as a shortcut and its pin footer appears") instead of nothing; the footer's explicit `!== '/dashboard'` guard is gone (redundant now). Existing `/dashboard` rows stay hidden by `filterPinsToShow` / `getMergedFilteredPins` (unchanged) — no data cleanup in this PR. |
| "Pin for someone" lists archived / test accounts | J28-F10 | **verified** (Tier-2 #19, PR #2636) | `Layout.tsx` loads the picker through `activeUsersQuery(..., { includeDev: true })` from `lib/people/fetchActiveUsers`. Not redone. |
| Pin ritual entry cost | J28-F9 | skipped → Tier 4 (G14) | — |
| Evidence rails reflow | J28-F11 | skipped (keep) | — |

## Tests

- `src/lib/pinnedTabs.test.ts`: `/dashboard` not in `PINNABLE_PATHS`; legacy label preserved.
- `src/lib/settingsSearch.test.ts`: "search" ranks the guide entry first (label prefix); every `guideSlug` entry lives on `settings-guides`; `settingsSearchGuideQuery` sets `tab` + `g`, keeps other params, `null` for plain entries.
- Migration validated on a throwaway local Postgres 15 with a mock schema (applied twice, idempotent): `%` / `_` / `\` match literally, `J1004` / `K-25077` / `j99` / `B352` / `BP1005` / `E12` prefix strips still work, numeric order as above, SECURITY DEFINER (jobs, bids) / INVOKER (estimates) and `anon` / `authenticated` / `service_role` EXECUTE preserved.

## Docs

`docs/migrations/20260905220000_search_rpcs_escape_and_numeric_order.md` · `PROJECT_DOCUMENTATION.md` (Search RPCs) · `SETTINGS_TABS_ARCHITECTURE.md` (search `guideSlug`) · `DASHBOARD_SECTIONS_ARCHITECTURE.md` (pins row) · `GLOSSARY.md` (pins) · help `pin-mode.md` (the Dashboard is not pinnable). G14 (Tier 4) still owns the `global-search.md` glyph / match-field edits.

## Deploy

Client first (nothing in it depends on the migration), then **`supabase db push`** — until the migration lands, search behaves as before (unescaped, text order); nothing breaks.

## Rollback

Revert the squash commit for the client. For the RPCs, re-run the previous bodies (`20260619140000_add_jobs_ledger_click_number.sql` for `search_jobs_ledger`; the baseline for the other two) — or leave them, the new bodies are strictly narrower matches with the same shape.
