# v2.2287 — Estimates "Accepted notifications" collapses to the gear (2026-08-25)

Owner call (mobile header was wrapping to two rows). The dev/master-only button on `src/pages/Estimates.tsx` that opens `EstimateAcceptedNotifySettingsModal` drops its text label — now a 36×36 ⚙ icon button; the existing `title`/`aria-label` ("Who gets emailed when a customer accepts an estimate" / "Estimate accepted notification settings") carry the meaning. Help guide `estimate-accepted-notifications` rewritten to point at the gear. Verified on the mobile viewport: ⚙ + New change order + New estimate fit one row; the gear still opens the modal.
