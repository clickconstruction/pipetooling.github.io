# v2.3534 — Stages tab decomposition, PR 4: the jump strip is one component, not five copies (2026-09-16)

Client only, behavior-preserving. PR 4 of the train in `to-dos/engineering-hygiene.md` — region 3 of [`docs/JOBS_STAGES_TAB_ARCHITECTURE.md`](../JOBS_STAGES_TAB_ARCHITECTURE.md). The map's "jump nav + alert chips" region had shrunk since it was written: the three data-gap chips retired with the Old view (v2.2012) and now live in the money card's Fix-ups strip, and their modals were already components. What was left was the strip itself.

## What moved

[`src/components/jobs/JobsStagesJumpStrip.tsx`](../../src/components/jobs/JobsStagesJumpStrip.tsx) — *Waiting → Working → Ready to Bill → Billed Awaiting Payment (→ Collections)*. In the tab this was five copies of the same twenty-line block, differing only in the label, the section key, the noun in the aria-label ("jobs" for the job-only sections, "rows" for the mixed ones), the red colour on Collections, and the rule that Collections renders only while its count is not `0`. The component holds those five facts in one table and renders the same DOM the copies did, arrows between neighbours included. Props: the resolved `counts` (the tab's `jumpStripCounts`, already through `stagesJumpStripCount` so "…" arrives as "…") and `onFocusSection`, which stays the tab's `focusStagesSection` — the section-open state and the scroll are substrate the tab keeps.

The Section tools ☰ menu and the *Recently added* pill sit in the same row and stay in the tab; the ☰ menu is a single-opener region of its own for a later PR.

Numbers: `JobsStagesTab.tsx` 5,557 → 5,443 lines.

## New coverage

[`JobsStagesJumpStrip.render.test.tsx`](../../src/components/jobs/JobsStagesJumpStrip.render.test.tsx): the four fixed sections with counts and nouns; an empty Collections hidden and three arrows drawn; Collections in red with a fourth arrow once it has rows; "…" passed through; clicks report the section key.

## Verified

- Typecheck, lint (0 errors; the same five pre-existing warnings), theme check, full suite.
- Live on prod data at Jobs → Pipeline as dev: the strip reads *Jump to Waiting, 21 jobs · Working, 33 jobs · Ready to Bill, 0 rows · Billed Awaiting Payment, 66 rows · Collections, 8 rows*, with four arrows and Collections in red; the Collections link opens its section (▶ → ▼, rows render).
