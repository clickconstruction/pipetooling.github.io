# v2.3845 — Get contracts signed banner reads quieter (2026-09-25)

Cosmetic pass on the Pipeline's first Today's Money Opportunities card ([`PipelineContractCoverageCard.tsx`](../../src/components/jobs/PipelineContractCoverageCard.tsx), shipped v2.2738, floor line v2.3384). Two complaints, 2026-09-25: the "No floor · change" line under the headline read like a job‑number floor, not a dollar cutoff on job size; and the four filled pills (Waiting 19, Working 24, Billed 50, Collections 7) sat as loud as the primary Start the sweep button beside them.

## The change

- **Pills → segmented text row.** The stage row is now `19 waiting · 24 working · 50 billed · 7 collections` on one line, each count a bare button that hover‑lifts to a soft surface tint and still filters the board to that stage on click. Same `onStageGap` handler; same tooltips. A stage whose gaps are all closed keeps its green ✓ but now sits inline (`✓ waiting`) instead of as its own filled pill.
- **Floor line rephrased.** `formatContractFloor` output is unchanged; the words around it are:
  - floor 0 → *Counting every dollar* · *set a small‑job floor* (dev)
  - floor set → *Skipping jobs under $2,500* · *3 not counted* · *change* (dev)
  - *N marked not needed* still appears when > 0.

No RLS, no RPCs, no migrations — one component file (`PipelineContractCoverageCard.tsx`).

## Verified

Typecheck green. The zero‑gap collapsed row (`missingCount === 0`) is untouched. The floor editor (dev only, `app_settings` write) is untouched.
