# v2.2754 — Guide gif: File a signed contract (2026-09-04)

The "get a job contract signed" help guide's **Already signed? File the Google Doc** section now carries a recording (`public/help/get-a-job-contract-signed-file.gif`): the Pipeline chip opens the Contract modal, **File a signed contract** opens the sheet, a pasted Google Doc link turns into the green "Google Doc linked" line, and **Record as signed** lights up. Recorded on Job 922 as dev; the sheet was cancelled before Record so nothing was filed.

Captured with Playwright video → ffmpeg (Claude in Chrome was disconnected), so this clip has no click overlays — `docs/HELP_MEDIA_PLAN.md` row updated.
