# v2.3677 — Quick time add, PR 5: who gets the door and the daily ceiling, on Settings (2026-09-21)

PR 5 of [`to-dos/quick-time-add`](../../to-dos/quick-time-add/README.md), the second of the three follow-ups queued on 2026-09-21. Client + one migration (`20260921174057_quick_add_settings.sql`, doc in `docs/migrations/`). Push any time after the deploy.

## Why

Owner calls 1 and 2 (who gets the door; the daily ceiling) were taken as defaults on 2026-09-20 and built as a role list in code plus a number in `app_settings`. The to-do's PR 4 said: *only if wanted* — a Settings screen so the owner can change either without a PR. It was wanted.

## The change

- **Kernel** [`quickTimeAdd.ts`](../../src/lib/clock/quickTimeAdd.ts): `QUICK_ADD_ROLE_CHOICES` (the six roles the owner may tick — field roles are not offered), `parseQuickAddRoles` / `serializeQuickAddRoles`, `parseQuickAddDailyCeiling` (5…600, else 120), and `canUseQuickAdd(who, roles)` takes the owner's list. Four tests, one of which reads the new migration and pins every sentence and the default list.
- **Keys** [`appSettingsKeys.ts`](../../src/lib/appSettingsKeys.ts): `quick_add_roles_v1` (new) and `quick_add_daily_ceiling_minutes` (PR 1's row, named here for the first time).
- **[`quickAddSettings.ts`](../../src/lib/clock/quickAddSettings.ts)**: `fetchQuickAddSettings` / `saveQuickAddSettings` — the one reader and the one writer of the two rows.
- **The RPC**: `add_quick_time()` re-created whole; the role gate reads `quick_add_roles_v1` (comma list, whitespace ignored) with the PR 1 list as the fallback. Same sentences.
- **The door** [`ClockInOutButton.tsx`](../../src/components/ClockInOutButton.tsx): loads the role list once and passes it to `canUseQuickAdd`; the kernel's default stands until it loads or when the row is unset.
- **[`QuickAddSettingsBlock`](../../src/components/settings/QuickAddSettingsBlock.tsx)** on Settings → People & teams, under *Assistant hours visibility*: the six role checkboxes, the ceiling, Save (at least one role; 5…600 minutes). Render test: the saved roles come back ticked, the ceiling shows, a save carries the change.
- Guide *add a quick call or email to my hours* gains *Changing who and how much (dev)*; `ACCESS_CONTROL.md` says the gate reads the setting.

## Verified

`quickTimeAdd` (kernel + migration text), `QuickAddSettingsBlock.render`, `appSettingsKeys`, `helpGuideContent`, `releaseNotes` suites; typecheck and lint on the touched files. The migration's SQL is the PR 1 function with a three-line change, checked by the test that every sentence survives.
