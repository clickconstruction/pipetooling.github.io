# v2.3066 — Bids: primaries hold the board and the three customer lenses only (2026-09-07)

Journey map **P-B1 / P-B2 / P-B3** (`partner-checks-position.md`, found on the 2026-09-06 Tier 5 P2 walk): a primary got the whole office Bids page for their trade — Pricing and Cover Letter, all four Followup lenses (other builders' lost bids with dollars and phone numbers), Working, Estimators, the workbench, New Bid, and the board's below-the-fold analytics. Only the trade pill was narrowed. Client-only; no migration.

## What shipped

- `Bids.tsx`: `PRIMARY_BIDS_TABS = ['bid-board', 'rfi', 'change-order', 'lien-release']`. The tab-change effect bounces a primary off any other tab through `roleGateBounce('bids-office-tab', …)` (the v2.2882 superintendent pattern) and lands on the board; the top strip hides Robots / Followup / Working / Bid costs / Estimators and the detail strip hides Counts / Takeoffs / Labor / Pricing / Cover Letter for primaries; `bidsNewBidButton` is null for primaries.
- `BidsBidBoardTab`: new `showEstimatingHealth` prop (default true); Bids passes `myRole !== 'primary' && myRole !== 'superintendent'`, so `BidBoardEstimatingHealthSection` (pulse, weekly chart, everyone-stands, people cards) is office-only (P-B2).
- Guide `start-here-as-a-primary` says what Bids is for a primary. `ACCESS_CONTROL.md` page matrix updated.

Data layer unchanged: bids RLS already scoped primaries (`primary_scope_*`, v2.2174); this closes the surfaces the page still drew around that data.
