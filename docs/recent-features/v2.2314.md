# v2.2314 — Quick Estimate flow v2: kind-first, Back buttons, real CO fields (2026-08-25)

Owner feedback on the shipped wizard (three asks, all built):

1. **Kind-first**: new `kind` stage opens the wizard — 🔧 Change order / 📋 Estimate — replacing the CO-first "schedule opens immediately" design and the estimate side-door link on the job screen. `chooseKind` re-syncs `doc_kind` on an existing draft when the user goes Back and switches (change_order_fields reset to `{}`/`null` accordingly).
2. **Back/forward**: kernel `quickEstimateBackTarget(stage, branch)` (+test) maps every stage to its Back target per branch; a ‹ Back button renders whenever a target exists, autosaving before stepping. All answers live in component state, so nothing is lost in either direction; `ensureDraft` now also *updates* customer_id/doc_kind on revisit instead of early-returning, and the job stage shows "Next — {job}" when returning with a pick.
3. **Real CO fields**: the CO work screen carries the same four fields as the office CO editor — Description of change (voice-nudged textarea), Reason for change, Schedule impact, Answer needed by (date) — saved as the full `change_order_fields` shape (`description_of_change`/`reason_for_change`/`impact_on_schedule`/`response_requested_by`) on every autosave and on send. Estimate branch keeps the single "What's the work?" box.

Live-verified: kind→job→back→estimate→back→CO, fields filled, Next→cost→Back (fields intact)→review, ✕-save; DB row confirmed with all four CO fields in the editor's exact shape. Help guide updated.
