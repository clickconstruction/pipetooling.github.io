# v2.2199 — Vehicle check-ins: cadenced odometer collection with questions (2026-08-23)

Owner-designed (mockup 9ebd178b approved; decisions D1–D5). Quickfill's **Vehicle odometers** station becomes **Vehicle check-ins** (sectionId stays `vehicle-odometers` so marks/banners persist):

- **Cadence** — new kernel [`vehicleCheckinDueList`](../../src/lib/vehicleFleet.ts): vehicles held by a person come due every `assignedDays` (default 7), motor-pool vehicles every `motorPoolDays` (default 30, 0 = skip; rows say "🚶 walk out & check" since there's nobody to call — D3); unassigned vehicles still skipped; never-read first, then stalest. Replaces `staleOdometerCallList` in the section (the old kernel remains for the fleet board's attention counts). Rows carry a cadence pill (`weekly` / `Motor pool · monthly`).
- **Settings** — [`src/lib/vehicleCheckinSettings.ts`](../../src/lib/vehicleCheckinSettings.ts): one `app_settings` key `vehicle_checkin_settings_v1` (`assignedDays`, `motorPoolDays`, `questions[{id,label}]`, defaults make it work with no setup). Edited from the Vehicles board's facts line: **⚙ Check-ins ›** (dev-only, beside Insurance plans ›) opens a modal — two day inputs + questions list (add / edit-in-place / remove; renames never rewrite history because check-ins store the label as asked — D5).
- **Capture** — each Quickfill row renders the questions as checkboxes; checking one reveals a required "What did you see?" note (D4). Save = `vehicle_odometer_entries` insert + a `vehicle_checkins` history row (answers jsonb `[{q, flagged, comment}]`; saved even when all clear — D1; fail-soft if the table isn't migrated yet) + one **Monitor-severity `vehicle_problem_reports` row per checked box** (`"<question> — <comment> (from vehicle check-in)"`) so flags land on the fleet board (D2).
- **History** — vehicle ledger gains kind `checkin` (new **Check-ins** filter pill): "Check-in · all clear — by Grace" or "⚠ Check-in · Any lights on the dash? — “ABS light” · problem report filed — by Grace" (label built via `checkinLedgerBody`). Rows deletable like other ledger rows.
- **Migration** [`20260823210023_vehicle_checkins.sql`](../../supabase/migrations/20260823210023_vehicle_checkins.sql) (doc: [`docs/migrations/20260823210023_vehicle_checkins.md`](../migrations/20260823210023_vehicle_checkins.md)) — table + RLS (pay-access office pool) + both read-only blocks. **Deploy: `supabase db push` after merge**, then gen-types (client uses `as any` casts until then).
- Tests: settings parse/serialize/answers (3), `vehicleCheckinDueList` + ledger kind (in vehicleFleet suite, 43), render smoke updated (motor pool now listed, questions render, note box hidden until checked). Help guides `quickfill` + `manage-company-vehicles` updated; banner default now "Do we have a reading and a check-in on every truck?".
