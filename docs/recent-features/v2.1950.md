# v2.1950 — Fix tech-tree task RLS recursion (2026-08-21)

Migration `20260821160000_fix_tech_tree_task_rls_recursion.sql` — see
`docs/migrations/20260821160000_fix_tech_tree_task_rls_recursion.md`.

- Every `checklist_tech_tree_group_tasks` UPDATE 500'd with 42P17 (the
  UPDATE policy ↔ assignees SELECT policy cycle), breaking task renames from
  the roadmap task card (old Edit fold and the v2.1949 inline rename alike).
- Fixed with the house pattern from v2.1225: SECURITY DEFINER helper
  `user_is_assignee_of_tech_tree_task(uuid)` + policy recreated on it, same
  intended grants.
- Client change: none (v2.1949's `updateTaskInGroup` already skips the title
  PATCH when unchanged; with this applied, changed titles save too).
