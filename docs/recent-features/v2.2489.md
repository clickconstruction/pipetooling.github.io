# v2.2489 — Needs You: roadmap needs-a-person item (banner migration 3/6) (2026-08-29)

Third v2.2339 follow-up: `DashboardRoadmapNeedsNameBanner` (v2.2138) folds
into the Needs You card. Stacked on v2.2488.

- **New hook `src/hooks/useRoadmapNeedsNameNudges.ts`**: the banner's
  farm-mode + `canSeeRoadmapTab` gate and the roadmaps/groups/edges/tasks
  fetch → `buildRoadmapNudges`, extracted verbatim (min-count threshold and
  error-hides behavior unchanged).
- **Kernel**: input `roadmapNudges`; item key `roadmap-needs-person` (amber,
  kicker "Roadmap", action "Open Plan" → first roadmap's
  `/checklist?tab=roadmap&roadmap=<id>&view=plan`). Single-roadmap copy keeps
  the banner's `<title> · N roadmap tasks need a person` + `next:` pick;
  multi-roadmap sums the total and lists up to three titles. Slots between
  `team-reviews` and `job-followups` (old stack order).
- **Both surfaces** wire the hook (no dedicated Quickfill station for this).
- **`DashboardRoadmapNeedsNameBanner.tsx` deleted** — no remaining mounts.
  The card trades the banner's per-roadmap tap targets for one action to the
  top roadmap's Plan; the Roadmap tab lists the rest.
- Tests: 2 new kernel tests. Help guide `needs-you-card.md` updated.
