# v2.2056 — Checklist scheduling overhaul 2/4: rolling horizon + nightly top-up (2026-08-22)

Kills the two-year cliff (B1) and the deep-future noise (B4) from the
anatomy investigation.

- **Add modal**: weekly items now materialize only `MATERIALIZE_HORIZON_DAYS`
  (35) ahead instead of 104 weeks.
- **Nightly top-up**: `send-scheduled-reminders` gains a materialization step
  on its 03:00 CST slot — every active `day_of_week` item is topped back up
  to 35 days ahead (Deno mirror of `checklistMaterialize.ts`; upserts against
  the v2.2055 unique index; created instances copy the item's current
  assignees — so assignee edits self-heal for new occurrences even before
  phase 3). Force with `{"materialize": true}` + cron secret; response
  carries `materialized`. **Manual deploy required**:
  `supabase functions deploy send-scheduled-reminders`.
- **Migration** `20260822005553_prune_far_future_weekly_checklist_instances`:
  prunes untouched weekly occurrences beyond 35 days out (see
  `docs/migrations/` fragment). Apply via `db push` after merge.

Next: phase 3 (edits regenerate the future), phase 4 (modal redesign).
