# v2.2629 — RFQ Phase 1a: quotes store + kernels (2026-09-01)

Phase 1a of `docs/SUPPLY_HOUSE_RFQ_PLAN.md`. No UI.

- **Migration `20260902030531`** (applied): `bid_rfqs` (scope snapshot,
  bid_version pin, token for Phase 2), `bid_quotes` (source lane,
  valid_until, freight, raw_paste provenance), `bid_quote_lines`
  (unit_price_each_cents + price_basis each/ft/per_100/box + basis_qty,
  cant_supply, alternate, confidence, picked), and
  `supply_house_fixture_prices` (name-keyed "last quoted" memory, unique
  per house + lower(fixture)). Read AND write RLS restricted to the
  ledger-writer roles — cost-side data. All three fence appliers.
- **`src/lib/rfq/parseVendorReply.ts`** (+12 tests): basis-first token
  consumption ("box of 50" never reads as a price), $-preferred/last-number
  price extraction (beats "JR smith 2010 - 148"), marker-required size
  rules (prices/model numbers/FD-2 suffixes never enter the size channel),
  strong multi-word synonyms ("floor drains", "carriers"), verbatim-name
  bonus ("GCO/FCO 116 each" → both), size-disagreement veto, no-stock →
  cant_supply, baseline outlier flags.
- **`src/lib/rfq/quoteCompare.ts`** (+5 tests): latest-per-house wins,
  expired quotes visible but excluded from best, D22 section grouping,
  cost-then-last-quoted baseline, snapshot-vs-current quantity drift,
  picked totals at CURRENT quantities, apples-to-apples common-line totals.
