# v2.3337 — Robot icon key: the sealed-envelope explainer, back where new estimators look (2026-09-11)

Client only — no migration, no edge function. The second loose end left after the v2.3225 Robot Board train, after v2.3334 (one-click Paste the plans).

## Why

The Shadows lens carried a "How shadow bids work (30 seconds)" fold — the one plain-words answer to *why can't I see the robot's number*. v2.3222 deleted the lens and folded its rows into the Robot Board mirror; the explanation survived only as the Scoreboard's section caption ("the robot sealed a number before ours existed"), which names the fact and not the reason. A new estimator meets the lock badge on the Bid Board first and looks at the **?** beside Bid #, not at the Scoreboard.

## What shipped

- **[`RobotIconKeyModal.tsx`](../../src/components/bids/RobotIconKeyModal.tsx)**: one block between the intro and "On a live bid" (`data-testid="robot-key-envelope"`), in the seal purple the lock badge already uses — *Why you can't see the robot's number yet*: the robot bids **in secret**, can't peek at ours because ours doesn't exist, seals its price, the envelope opens at send and the score is kept on its own; *5 in a row within 8%* earns first drafts. The figures are `GATE_B_STREAK` / `GATE_B_PCT` from `confidenceBoard.ts`, so the key cannot drift from the gate. Copy is the deleted fold's, trimmed to four sentences.
- The key opens from the table header's **?** and the phone pill row (v2.3200), so both get it. Nothing moves on the Scoreboard.
- Render smoke [`RobotIconKeyModal.render.test.tsx`](../../src/components/bids/RobotIconKeyModal.render.test.tsx) (new): the block, the gate figures, its place above the state rows, Close.
- Help: `let-the-robots-shadow-a-bid` (the icon paragraph now points at the key and carries the one-line answer) and `read-the-bid-board` (the robot-icon section names the key).

## Not changed

The Scoreboard caption and the mirror's sealed rows. `read-the-bid-board`'s robot-icon bullets still describe the grey / yellow / green request states from before v2.3200's always-on shadowing — worth a rewrite on its own, not folded in here.

## Testing

`npm run typecheck`, eslint on the touched files, the new smoke + the help-guide content test, theme-token check. Walked in the dev server: Bid Board → **?** beside Bid # → the key opens with the purple block above "On a live bid".
