# v2.1930 — Payment forecast pay-speeds strip (2026-08-20)

Expected-payment train part 4 (v2.1924 chips, v2.1925 forecast, v2.1926
promised dates): segment-level pay-speed context in the forecast.

- **Pay speeds strip** between the forecast's bucket tiles and its row lists:
  Company / Res / Comm median days-to-pay, each with its sample count
  ("no data" for an empty segment). Hidden entirely while pay speeds are
  unavailable. Tooltips spell out the 12-month median rule.
- **Res/Comm tag on every forecast row** (the Customers surfaces' color
  convention — commercial amber, residential blue), sourced from
  `customers.customer_type` via the pay-speeds RPC so it works even for
  customers with no measurable payments.
- Migration [`20260821120000_pay_speeds_segments.sql`](../migrations/20260821120000_pay_speeds_segments.md):
  `get_billed_customer_pay_speeds()` v2 — adds `segments.residential` /
  `segments.commercial` (same 12-month median rule restricted by
  `customers.customer_type`; untyped customers count toward the company
  median only) and `customerTypes` (every typed customer id → type). Company
  and per-customer outputs and the role gate are unchanged.
- Kernel: `PaySpeedData` gains `segments` + `customerTypes` (v1 payloads
  parse to empty); `ForecastRow` gains `segment`. Modal render smoke added
  (`BilledPaymentForecastModal.render.test.tsx`).
- Help guide `see-when-a-customer-will-pay` forecast section updated.
