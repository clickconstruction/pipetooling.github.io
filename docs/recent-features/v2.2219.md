# v2.2219 — Payment forecast: toolbar Forecast button + section-tools menu item (2026-08-23)

Two new entry points to `BilledPaymentForecastModal` (v2.1925) on Jobs → Pipeline, both gated exactly like the header's `canSeeBilledExpectedPay` (dev / master_technician / assistant-like / primary; hidden otherwise):

- **Forecast toolbar button** (`JobsStagesTab.tsx`): green-tinted pill next to New Job and Follow-ups at the top of the tab, styled like Follow-ups (tint/border/600-weight pattern) using the green money-in tokens.
- **Menu item**: the jump-bar hamburger (`buildStagesSectionToolsMenu`, `src/lib/jobs/stagesSectionToolsMenu.ts`) gains `billed-payment-forecast` in the Billed Awaiting Payment group, between Chart and Paid notifications — the button's relative position on the section header. Kernel test extended with a per-role gate case.

Help guide `see-when-a-customer-will-pay.md` names all three entry points. No schema changes.
