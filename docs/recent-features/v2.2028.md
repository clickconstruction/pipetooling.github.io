# v2.2028 — Count Sheet phase 2: merge-on-rename + drag reorder (2026-08-21)

The two pieces deferred from v2.2024 ([`BidsCountsTab.tsx`](../../src/components/bids/BidsCountsTab.tsx)). **Merge-on-rename**: `sheetSaveRowEdit`'s duplicate branch now opens a confirm ("Merging adds this row's N to it (making M) and removes this row", via `useConfirmDialog`) instead of refusing outright; confirm updates the existing row's count and deletes the edited row (optimistic, full refresh on error), cancel reverts the input. Page/group-tag of the removed row are dropped — counts merge, same as quick-add's merge. **Drag reorder**: new `SheetSortableRow` (module-level, `useSortable` + a ⣿ handle cell, `touchAction: none`) wraps List-mode rows in the sheet's own `DndContext`/`SortableContext` reusing the Old view's `countRowsSensors` + `handleCountsDragEnd` + `saveCountRowsOrder` persistence unchanged; the handle column renders in List mode only (group order is the page grouping). Captions: "same rows as Old, ready to edit"; toolbar hint gains "drag ⣿ to reorder". Verified live on BP298 with temp rows (merge 2+3→5 confirmed end-to-end; drag wiring mirrors the Old view's proven setup — synthetic pointer drags don't fire in the hidden pane). Help guide updated. Client-only — no migration.
