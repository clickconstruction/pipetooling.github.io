# v2.1929 — WAITING ON CUSTOMERS card → "Who owes what" breakdown (2026-08-20)

Owner ask: clicking the Pipeline New view's WAITING ON CUSTOMERS card just opened the aging chart (dev/controller) or jumped to the Billed section — neither answered the card's own question, "who owes this and who do I lean on first?". New kernel [`billedByCustomerBreakdown.ts`](../../src/lib/jobs/billedByCustomerBreakdown.ts) regroups the board's `billedActiveRows` per customer (keyed `customer_id`, falling back to name / "No customer") — total owed, bill count, worst age — groups sorted by total desc, bills oldest-first within. Amounts are the same per-row open remainders the section total sums (`stageRowBilledRemainingAmount`); ages use the est.-bill-date clock the 30/90 aging chips use (`stageRowBilledAgeDays`, null = "no date"). New [`BilledByCustomerBreakdownModal`](../../src/components/jobs/BilledByCustomerBreakdownModal.tsx) (same frame as the Capable breakdown): expandable customer rows with 30+/90+ age chips, per-bill View → invoice-focus jump (job-shell rows focus the job row), footer buttons for the aging chart (dev/controller), the 90+ filter, and the section jump. Card click now opens it for **all** roles (`PipelineOverview` swaps `onOpenAgingChart` for `onOpenBilledBreakdown`; the chart stays reachable from the modal + the ⋯ tools menu). Opening uses the aging chart's retry-until-merged billed-scope fetch (v2.1917 cohort bounds). Kernel tests cover grouping/sorting/remainder-netting/no-date/no-customer; help guide `read-the-pipeline-money-view.md` updated. Verified live: prod data groups 119 bills into a ranked customer list (and instantly surfaced the Take 5- Seguin duplicate pair under Southern Post Construction). Client-only — no migration.
