# v2.2082 — Easter egg screen picker (2026-08-22)

Owner request: target eggs at any screen, not just the hardcoded Followup surface. New pure registry [`easterEggSurfaceTree.ts`](../../src/lib/easterEggSurfaceTree.ts): 27 in-app pages in six groups (Every day / Sales / Work / Money / Field modes / Admin) plus the 16 Bids tabs; surface keys `p:<path>` (page + subpaths, any tab) and `t:/bids:<tab>` (one Bids tab; `bid-board` matches the no-param default). Legacy `followup` normalizes to the two followup tab keys at parse (`normalizeEggSurfaces`) — the live Wendi config self-migrates, no re-entry. `eggActiveFor` now matches through `eggSurfaceMatches`; `EASTER_EGG_SURFACES` is gone.

Settings → Easter eggs "Where" row: chips name real screens ("Bids · Builder Review") and an **+ add screens** button opens [`EasterEggScreenPickerModal`](../../src/components/settings/EasterEggScreenPickerModal.tsx) — grouped expandable tree, Bids opens to its tabs (parent checkbox = anywhere on Bids), footer summary, per-click save via the existing `patch`. **Role handling**: `eggSurfaceVisibleForRole` reuses the router's own `isPathAllowedForRole` (probe paths for prefix pages `/job-mode`, `/workflows`; `allowedOverride` for the in-page dev gates on Templates/Moneyfill/Partnerships/Duplicates) to hang soft amber "hidden for {name}" hints on nodes a targeted person can't reach — informative, never blocking, since an unreachable pick simply never fires; the card echoes the same as a one-line note. 16 kernel tests across the tree + config modules. Client-only — no migration. **Deploy note**: the old client drops the new surface keys as unknown, so don't save from the new UI until this version is deployed.
