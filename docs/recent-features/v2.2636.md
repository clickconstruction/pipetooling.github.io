# v2.2636 — Lane B: the RFQ Desk (2026-09-02)

The plan's deferred lane B, built on owner request (mockup → "is this the
best we can do" revision → build; canvas artifact b731a34b…). Same store,
tokens, and public page as lanes A/C — the desk adds sending and watching.

- **Migration `20260902151658`**: bid_rfqs grows sent_email,
  resend_email_id, viewed_at, last_reminded_at, reminder_count,
  vendor_note; email_send_log gets a scoped SELECT policy (only rows an
  RFQ references, only the five cost-side roles — role check casts to
  public.user_role, `= ANY(text[])` errors on the enum).
- **`send-rfq-email` edge function** (authed: JWT + role, the
  send-bid-pricing-package pattern): modes send (one rfq+token+email per
  house, ≤10/blast, scope snapshot {lines, text}), remind (24h
  server-side throttle; opener varies by viewed_at), resend (bounce fix,
  same token), and **preview — returns the exact email a send would
  produce (same builder, no writes); both the compose flow and nudges
  are gated behind it (owner requirement: preview before ANY send)**.
  reply_to = the sender, so vendor replies reach a human;
  `_shared/resendSendEmail` gains replyTo + returns the Resend id.
- **Viewed signal**: get-rfq-quote-page stamps bid_rfqs.viewed_at on
  first page open — stronger than email-open pixels. Delivered/bounced
  ride the existing resend-webhook → email_send_log rail via the stored
  Resend id.
- **Kernel `src/lib/rfq/rfqDesk.ts`** (+13 tests): deriveRfqTrail
  (email lane 4 steps, copied-link lane 3; page-view implies delivery;
  bounce as bad branch that a quote overrides), canNudge (24h),
  scopeDriftCount, deriveRfqChip (none / quotes-only / waiting /
  bounced / all-in; red > amber > green), coverageFromCompareRows.
- **UI**: RfqComposeModal (multi-house blast, prefilled emails from the
  newest prior request per house, open-request duplicate warning,
  needed-by + vendor note, then the mandatory preview step with
  per-house tabs); RfqDeskModal (trail rows, coverage strip + bare-items
  list, previewed nudges, inline bounce fix + resend, Copy link, manual
  **Close link** — closing the audit gap — and the Compare door);
  PrepareFixtureCopyModal "Send by email…" (scope stays there);
  BidsPricingTab one-chip-five-states swap; QuoteCompareModal now feeds
  snapshotQtyByName from the newest request's scope — the shipped drift
  badge finally has data.
- Live-tested end to end: composed on BP339, previewed, sent a real
  email (self-addressed), webhook marked it delivered within seconds,
  page-open stamped Viewed, trail rendered Sent✓ Delivered✓ Viewed✓,
  nudge throttled with reason, chip amber "RFQs · 1 waiting", Close link
  flipped the public page to closed; test request deleted after.
