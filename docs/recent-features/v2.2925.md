# v2.2925 — Tier 4 bids guides: the board, follow-up, imports, the estimate loop, purchase orders and the quote link read the way the app now works (2026-09-06)

Journey-map Phase 4, **Tier 4 group G-bids** (rows G7 · G8 · G9 · G15 · G21 in the private repo's `docs/JOURNEY_MAP.md`; execution ledger `docs/journeys/_PHASE4.md`). Tier 4 is a **gap audit**: every Tier 1–3 PR shipped its own guide edits (house rule), so this pass reads each named guide on `main` against the shipped fragments (v2.2847 · 2848 · 2851 · 2856 · 2859 · 2873 · 2880 · 2885 · 2886 · 2894 · 2895 · 2897 · 2903 · 2905 · 2906 · 2909 · 2911) and fixes only what was still missing, wrong, or unnamed. **Docs only** — `src/content/help/`, `docs/GLOSSARY.md`, this fragment and the release note. No code, no migration, no edge function.

## Per guide

| Row | Guide | Verdict | What changed |
|---|---|---|---|
| G7 | `read-the-bid-board.md` | **fixed** | Already had "sent Wed 9/2", "No due date (+N)", the loading skeleton, the Won cascade confirm + waiting-undo, "open the job →", the 🤖 Robots tab (B1, #21, #8). Added: **counts follow the trade pill** (Dashboard cards are company-wide — J10-N2; the #20 count kernel is parked, so the scope is taught, not fixed); the Due chip's post-send quiet is by design and the chase lives in **Followup → Call queue** (J10-F9, C16); the **`sent 1/2` badge counts packets** and same-letter GCs are not packets (J13-F6). |
| G7 | `follow-up-with-builders.md` | **fixed** | Added the "board goes quiet on purpose, chase lives here" opener; the Call queue's one-tap chips are the Waiting-to-hear set; a **Won on a multi-GC row asks first** (the cascade sentence, Cancel writes nothing) and **waiting** on the winner undoes it (#21); Waiting to hear's **You won it** strip with Open the job (#8). The multi-GC bullet now says the auto-Lost is confirmed before it is written. |
| G8 | `import-a-takeoff-from-counttooling.md` | **fixed** | Undo (#42) and the unscaled-px fix were already there. Added a short "then pick how the materials get priced" section pointing at the new By Stage / Combined guide; new bids start on Combined (`Bids.tsx` inserts `materials_model: 'rough'`). |
| G8 | **new** `choose-by-stage-or-combined-for-materials.md` | **new** | The fork in trade words — *count each fixture* vs *one parts list* — with the B1 caption quoted (`MATERIALS_MODEL_CAPTION`), the switch confirm's "stored separately, nothing copied", the fill-from-book difference (*Fill from book · N matches* vs *Apply Matching Fixture Assemblies*), per-stage totals and per-stage POs. Roles = everyone who reaches Takeoffs / Labor (incl. primary, superintendent). |
| G9 | `turn-a-won-bid-into-a-job.md` | **verified** (one sentence) | #8 and B5 content (price prompt, Import greyed-with-hint, C# busy hint, Cancel-import closes) all present. Added that the *Which GC gave you this job?* picker's paragraph is the cascade confirm and names a hand-set Lost (#21). |
| G9 | **new** `send-an-estimate-and-turn-it-into-a-job.md` | **new** | The core loop the ten satellite guides never told end to end (J17-F8): New estimate (self-deletes untouched, #14d) → numbered guide → **Send to <email>?** (#42) → the opened / never-opened chip (#34) → **Resend link** (#35) → **No thanks** / **Record a decline** (#34) → Approve + typed/drawn signature → Signed email → **Create job from estimate** / **Link existing job** / Create jobs automatically with the two guardrails (#9) → change orders after. Links every leg to its satellite guide. |
| G9 | `write-a-change-order.md` | **fixed** | Body already covered the confirm sheet + Net change (#14a) and the three button captions + "as typed" readout (B2+B4). **Roles** were `assistant, master_technician` while the estimator start-here routes change orders to estimators (J16-F7) — now `dev, master_technician, assistant, controller, estimator` (all reach `/estimates` and the Bids → Change Order tab). |
| G9 | `start-here-as-an-estimator.md` | **fixed** | Labor skeleton (#10) was there. Added: the bid room starts with **Get the link** (#31); a new **Followup / Call queue** step; **Open the job** on a win is theirs to use (#8); a job opened from Estimates / Customers is the **read-only pane** (#7); PO Builder / Purchase Orders visible, PO Generator office-only; links to the change-order and estimate-loop guides. |
| G15 | **new** `raise-a-purchase-order.md` | **new** | "PO" has two meanings and a tab for each: **PO Generator** (counter code; four fields; the ledger the supply-house invoice PO # is checked against; Dispatch Mode PO) vs **PO Builder → Purchase Orders** (line-item lane; Draft → Finalized; Takeoff per-stage POs feed the bid's exact-materials totals — don't delete them; tally POs). The **three price truths** table (Parts Book cost · takeoff line frozen at assignment · PO `price_at_time` frozen at add, confirm = "still good"). **Price coverage** and the `/duplicates` exact-name default (B14). |
| G15 | `docs/GLOSSARY.md` | **fixed** | New entries: *Call queue*, *Won cascade* (incl. the `sent 1/2` packet rule), *Materials model (By Stage / Combined)*, *"PO" — two lanes*, *Three price truths per part*, *Price coverage*, *Duplicates page*. |
| G21 | `dispatch-mode.md` | **verified** | Badge = open requests only (#24/25), oldest-first + age chips + Needs You item (#40), Dispatch PO default for assistant-like/master (B14) — all present and accurate. No change. |
| G21 | `send-a-supply-house-a-quote-link.md` | **fixed** (one bullet) | Already had: request made when you copy (#14a), Close-link confirm + Reopen (#42), "Supply house prices (RFQ) ▾" (B2), the vendor's footer counts / save promise / Try again / closed-page recap and "Nothing needed — thanks for looking" (B3). Added one bullet: pasted vendor replies match across slashes and plurals (B2's parser fix), pointing at *get supply house prices on a bid*. |

## Conventions checked

Frontmatter (title / category / roles / keywords), slug = filename, `##` headings, lowercase "How do I…" titles, only valid `{{button:…}}` / `{{chip:…}}` variants (`src/lib/helpGuideContent.test.ts`). Roles match `ACCESS_CONTROL.md` / `MATERIALS_TABS_ARCHITECTURE.md` tab gating (estimators reach PO Builder and Purchase Orders, not PO Generator; the new materials-model guide includes primary and superintendent because both have the Takeoff tab).

## Findings for Will (guides revealed, not fixed here)

- **Board counts are still trade-scoped with no label** — the #20 count kernel is parked, so the guide teaches the scope; the pill row itself still says nothing.
- **`/duplicates` is dev-only** while the Parts Book is everyone's — the guide says "dev" plainly; whether assistants should get it is a product call.
- **No staff email on a customer decline** (noted in v2.2873) — the loop guide sends people to the Pipeline's Declined bucket; a "declined" letter would close that gap.
- **`start-here-as-an-estimator` still lists Prospects unconditionally** — it is gated per user (`estimator_prospects_access`); left as is (a guide can't know the flag) but worth a "(if enabled)".
