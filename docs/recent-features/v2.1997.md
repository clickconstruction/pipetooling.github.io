# v2.1997 — Combined-GC cleanup nudge + Bids doc catch-up (2026-08-21)

Bid-recipients train PR 3 (close-out). The pre-recipients workaround was a fake combined customer ("Multiple GC's", "Banyan Construction / 20 Twenty Construction / Fairbanks") that hid the real GCs from every queue — this PR makes those names self-identify and brings `BIDS_SYSTEM.md` up to the merged-Followup reality.

- **`looksLikeCombinedGcName`** ([`bidGcRecipients.ts`](../../src/lib/bids/bidGcRecipients.ts), +2 test blocks): conservative heuristic — `/multiple\s+gc/i` or a spaced `' / '`; plain ampersands stay untouched ("H & I Construction" is a real GC). Both call-mode lenses ([`BidsWhyWeLostLens`](../../src/components/bids/BidsWhyWeLostLens.tsx), [`BidsWaitingToHearLens`](../../src/components/bids/BidsWaitingToHearLens.tsx)) show an amber nudge on flagged builders: set the real primary GC in **Edit Bid** and record the rest under **Also sent to**, after which each real GC gets its own queue entry (v2.1995 expansion).
- **Types regenerated** from linked prod after `20260821230000_bid_gc_recipients.sql` was applied (2026-08-21): `src/types/database.ts` picks up `bid_gc_recipients` (+ concurrently-applied parallel-session schema).
- **Docs drift fix** ([`BIDS_SYSTEM.md`](../BIDS_SYSTEM.md)): the doc last described Builder Review and Submission & Followup as standalone strip tabs — it now records the merged **Followup** tab and its four lenses (By builder / By status / Why we lost / Waiting to hear) with their unchanged deep-link keys, cross-notes atop both legacy sections, the `bid_gc_recipients` schema block, and the multi-GC recipients flow. Client-only — no migration.
