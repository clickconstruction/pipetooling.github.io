# v2.2044 — Call mode: the promise builder (2026-08-21)

Owner-approved "Promise Date Builder" mockup, built whole. The Friday quick-picks in [`PaymentChaseModal`](../../src/components/jobs/PaymentChaseModal.tsx) give way to a three-mode builder matching how the answer comes over the phone: **📅 A date** (exact), **In N days** (chips 7/14/21/30 + free number), **N days after billing** (net-terms chips 15/30/45/60 + free number).

- **Kernel** [`resolvePromiseDates`](../../src/lib/jobs/paymentChase.ts) (+6 tests): per-invoice landing dates; `billed` mode computes from each bill's own reference date (fallback today+N when a bill has no date); the store keeps one promise per job, so a job with diverging bills takes the **earliest** date; `PROMISE_DAY_CHIPS` holds the two chip sets.
- **Live preview**: green landing chips (`→ Sep 14`) render per bill row as the input resolves — the confidence check before committing; the apply button echoes the outcome ("Mark promise → Fri Sep 4" / "Mark 3 promises · Sep 7 – Sep 23") and stays disarmed until the input resolves.
- **Writes**: unchanged plumbing — `set_job_promised_pay_date` per job (now per-job dates) + a `promised` chase touch each; bill session states and the wrap-up detail carry per-date ranges ("promised Sep 7 – Sep 23 (billed + 45d)").
- **Keyboard** (the footer's advertised keys are finally real): 1–4 tap the active mode's chips, Enter applies when resolvable (never from the note box), C can't-reach, → skip. Mode is sticky across the session; days/date clear per customer.
- **Past landings go amber** (live-verify find: net 30 on a 26d-late bill resolves to a date already gone): the chip reads "→ Aug 14 · past" with a tooltip — recording blown terms is honest, and the bill re-queues after the grace.
- **Token fix ridealong**: ten files referenced `--text-green-700` before it existed (texts silently inherited, backgrounds went transparent — found when the armed apply button rendered invisible). [`index.css`](../../src/index.css) now defines it in both palettes (light `#15803d`, dark `#4ade80`).
- New render smoke [`PaymentChaseModal.render.test.tsx`](../../src/components/jobs/PaymentChaseModal.render.test.tsx) (4 tests: shared landing, diverging net-terms landing, date mode + disarmed button, uncheck follows). Help guide updated.

Client-only — no migration, no edge functions.
