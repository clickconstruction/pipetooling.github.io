# v2.1957 — Review summary tiles centered (2026-08-21)

Follow-up polish to the v2.1936 centered Review column (user request).

- `boardTile` drops `flex: 1` for `flex: '0 1 220px'` and the tile row gains
  `justifyContent: 'center'` — the three tiles render compact and centered
  instead of stretching the full column width; they still wrap and shrink on
  narrow screens (`minWidth 110` unchanged).
