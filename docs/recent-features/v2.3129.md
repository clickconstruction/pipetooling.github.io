# v2.3129 — Types regenerated after `esign_consents` (2026-09-08)

`npm run gen-types:linked` after the v2.3118 push (`20260908030946_esign_consents.sql`, applied 2026-09-08): `src/types/database.ts` gains the `esign_consents` table. [`useEsignConsent`](../../src/hooks/useEsignConsent.ts) drops its `as never` cast and reads the table through the typed client. No behavior change.
