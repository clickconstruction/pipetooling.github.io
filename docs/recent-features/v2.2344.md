# v2.2344 — Preview email in View bill; shared preview helper (2026-08-26)

Placement 2 of the owner-approved mockup (placement 1 = Bill Customer,
v2.2340).

- New `src/lib/openInvoiceEmailPreview.ts`: `openInvoiceEmailPreviewInNewTab`
  — the v2.2340 preview logic extracted (same email builders the send uses,
  gray context header, text/html blob tab, 60s URL revoke). Parameterized
  `contextNote` distinguishes the two doors.
- `SendRecordInvoiceModal.tsx` refactors its inline handler onto the helper
  ("Preview — nothing has been sent").
- `DocumentsJobBilledInvoiceModal.tsx`: Preview email button beside "Open
  PDF in new tab", reusing the modal's existing `physicalDoc`
  ("Reconstructed preview — nothing is sent" — like the PDF there, it
  renders from current job data, so the payment card shows today's
  payments).
- Help guide `share-a-customer-their-portal.md` untouched;
  `ready-to-bill-pipeline.md` already documents the preview pair (v2.2340).
