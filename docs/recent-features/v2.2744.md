# v2.2744 — File a signed contract: a Google Doc link first, the scan in a corner (2026-09-04)

Owner-approved mockup (artifact d4abe96d): the office keeps signed contracts in Google Docs, so the Contract modal's filing sheet is rebuilt around a link, with the file upload tucked behind a dotted "Have a scan or photo instead?".

- **Migration `20260904044533`**: `job_contracts.signed_document_url text` (fragment in docs/migrations/).
- **Kernel** (`lib/jobs/jobContractDocument.ts`, tested): `isGoogleDocsUrl` (docs/drive.google.com over https), `isHttpUrl`, `shortDocumentLabel` (`docs.google.com/document/d/1kX9…Qz4`). Coverage rows carry `signed_document_url`; a filed link reads {{chip:green|✍ On file · Google Doc}}.
- **Sheet** — header button renamed **📄 File a signed contract**; the sheet opens on a dashed Google-blue link box ("Share → Copy link in Google Docs, then paste it here", paste-aware) that collapses to a green *Google Doc linked · docs.google.com/…* line with *change*; non-Google links file with an amber note rather than a block; Signed by prefilled, Signed on defaults to today (`todayYmdInAppTz`); the scan/photo input lives behind the corner link and never competes. **Record as signed** stays dim until a link or a file is given (name-and-date-only records are no longer possible).
- **Record**: `JobContractRecordBody` shows **Open the signed Google Doc ↗** (with the short label) for filed links; the Signed agreement view's banner reads *Signed outside the app · filed as a Google Doc*, and Share ▾ gains **Open the signed Google Doc ↗** and **Copy Google Doc link**; *Email a copy…* works for link-only records (hint: *the link*).
- **`share-job-contract`**: a paper record with no upload but a `signed_document_url` emails the link (button + URL) instead of an attachment; `mode: 'pdf_url'` returns `document_url` for it; the `contract_shared` activity row carries `document_url`. Deploy after the migration.
- Help guide: "Already signed on paper?" becomes "Already signed? File the Google Doc".
