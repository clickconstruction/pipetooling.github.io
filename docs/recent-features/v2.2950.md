# v2.2950 — One join key: `normalizeIdentityKey` at every free-text join, plus builder aliases (2026-09-06)

Journey map **Tier 5 X7** (cluster C34 — free-text identity fragmentation: J8-F2 Crew P&L unmatched rows, J14-F5 builder cards split, J21-F8 near-duplicate sites, J29-adj-2 manufacturer facet, J29-F5 same-named assemblies). Tier 5 train T5-07; merges alone. Will's call 2026-09-06: build. One migration (`20260906160000_identity_aliases.sql` — **`supabase db push` after merge**); no edge function.

## What shipped

- **Shared kernel** [`supabase/functions/_shared/identityKey.ts`](../../supabase/functions/_shared/identityKey.ts), re-exported as `src/lib/identityKey.ts` (+6 tests): `normalizeIdentityKey` — exactly the Crew P&L loose form (B8: NFD, lower, non-alnum → space, collapse) plus `&` → `and`; `identityKeysLooselyEqual` (equal keys, or ≥2-token containment on both sides — single tokens never merge alone); `findLooseIdentityPairs`.
- **Adopted at the joins** (each keeps every match it had, gains the ampersand + diacritic cases): `looseCrewPnlName` (Crew P&L, B8's resolver), `normalizeCustomerName` (Customers → Show similar), `manufacturerFacetOptions` / `manufacturerMatches` (B14's facet; the paged `ilike` path stays case-fold — a DB pattern cannot fold punctuation). The portal property key (`normalizeAddressKey`, address-specific) and assembly names (X15, later) are untouched.
- **Why-we-lost lens**: a customer-linked bid keys by the customer as before; a name-only builder keys by `name:<canonical identity key>` — so "H & I" / "H&I" are one card without any data change. The phone/recipient branch compares against the same key.
- **`identity_aliases`** (new table; `is_office_staff()` predicate, new — the banking-staff cohort under a general name; both read-only appliers) + [`identityAliases.ts`](../../src/lib/identityAliases.ts) (`canonicalIdentityKey` follows `merge` rows, `identityPairKept`, load/save) + `useIdentityAliases(kind)`.
- **`BuilderMergePrompt`**: above the cards, the first pair `findLooseIdentityPairs` flags among name-keyed groups that no `keep` row has answered — "These look like the same builder: A and B — N lost bids between them" · **Merge into A** / **Merge into B** / **Keep separate**. Merge writes a `merge` alias (grouping only; bids untouched); Keep writes a `keep` alias so the pair is never offered again. Customer-keyed builders merge through the existing Customers tool.
- Guide `merge-two-spellings-of-the-same-builder` (new). `database.ts` carries the table type by hand until the next gen-types run.

Deliberately not built: a global alias manager UI (un-merge = delete the row, dev), crew-name and manufacturer aliases (the `kind` column is ready; no surface asks yet).

## Verify

Why we lost: two bids with name-only builders "H & I" and "H&I" → one card. Two with "Acme Builders" / "Acme Builders LLC" → the amber prompt → Merge → one card on the next load, `identity_aliases` row `builder / acme builders llc → acme builders / merge`. Keep separate → prompt gone, row `keep`. Crew P&L, Show similar, manufacturer facet: unchanged behaviour on existing data.
