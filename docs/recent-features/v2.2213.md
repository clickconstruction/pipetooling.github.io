# v2.2213 — Cover letter sheets + the $0 rule (2026-08-23)

Third live-iteration batch (mockup 6b3d8c9a). `BidsCoverLetterTab.tsx` + the cover-letter help guide.

- **Preview sheets**: the bundled letter's `<section>`s get preview-only CSS (`.cl-preview section` — border, radius, shadow, gray `h2` label band, gap). The copied Google-Docs document and the print are untouched (print already page-breaks per section).
- **$0 rule** (owner: new versions were landing three "$0.00" letters on every GC): `pricedBundle = bundlePricings.filter(revenueSum > 0)` feeds the letter — `gcPackets`, `letterTotal`, `baseSectionNames`, the summary line and **Mark sent** (unpriced versions get no `bid_version_sends` rows). Flags untouched — self-healing: a version rejoins the letter the moment it's priced. Studio checklist shows amber "unpriced — left off the letter" on checked-but-empty versions and offered $0 alternates; all-unpriced bids read "Nothing priced yet — N bids left off the letter until priced; showing the active bid's letter."
- Known follow-up: the strip's "gets N prices" pill still counts unpriced ★s (the picker has no revenue data).
