# v2.3537 — Stages tab decomposition, PR 7: the partial-invoice decision is a kernel, and its dialog its own component (2026-09-16)

Client only, behavior-preserving. PR 7 of the train in `to-dos/engineering-hygiene.md` — the last inline dialog in the modal tail (region 6 of [`docs/JOBS_STAGES_TAB_ARCHITECTURE.md`](../JOBS_STAGES_TAB_ARCHITECTURE.md)), and the one the map said to Stage-A first because it carries the tab's only direct database write.

## Stage A — the decision

`planPartialInvoice(job, amountInput)` in [`src/lib/jobsStagesBoard.ts`](../../src/lib/jobsStagesBoard.ts), beside the two clamp helpers it builds on, returns one of four plans: `invalid` (blank or non-positive), `nothing_left` (the job is fully carved), `bill_customer` (the whole carvable remainder on a Ready-to-Bill job is *Bill Customer*, not a new line), or `insert` with the clamped dollars, the typed amount when the clamp reduced it (`adjustedFrom`, so the dialog can say so), the next `sequence_order`, and whether the primary remainder draft needs re-syncing afterwards (Ready-to-Bill jobs only). `reclampedPartialInvoiceInput` is the on-blur correction. Seven tests pin the branches, including the clamp on a Working job with two billed lines.

The tab's `createInvoiceFromModal` now reads the plan and does only IO, in the same order as before: the error line for the two refusals, the *Adjusted to remaining unallocated* toast, the Bill Customer hand-off (with the no-customer guard), or the insert plus `ensure_single_ready_to_bill_invoice_for_job` and the reload. Nothing about what gets written changed.

## Stage B — the dialog

[`StagesCreatePartialInvoiceModal.tsx`](../../src/components/jobs/StagesCreatePartialInvoiceModal.tsx) — the job line, the *Remaining* figure, the amount box, the error line and the two buttons, cut verbatim. The tab keeps the amount string, the on-blur re-clamp (`reclampPartialInvoiceAmount`, which also clears the page-global `error` per map quirk 4), `creating`, and the two doors. Two render tests: the remainder and the Create gate on a positive amount; the error line and the busy label.

Numbers: `JobsStagesTab.tsx` 5,241 → 5,217 lines (the creator shrank as much as the dialog moved).

## Verified

- Typecheck, lint (0 errors; the same five pre-existing warnings), theme check, full suite.
- Live: the *Create partial invoice* door renders only on Ready to Bill rows, and that section is empty on today's board, so the dialog could not be opened against prod data; the two render tests and the kernel's seven cover it. The Working and Billed sections still render with the creator rewired.
