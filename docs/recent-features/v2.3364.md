# v2.3364 — Bank transfer card wording: "for bank ACH and wires, not for mail", the check sentence, the softer closing line (2026-09-12)

Client only; owner wording after the v2.3329 live screenshots. Three strings, every copy:

- Address row tail (portal card + Accounts Receivable panel): *for the wire form — not for mail* → **for bank ACH and wires, not for mail**.
- Check box: *Only this address. Checks mailed anywhere else — the bank address is not a mailbox — may need to be re-issued.* → **Checks can only be received at this address. Checks mailed anywhere else need to be re-issued.** The kernel's `checkMailingSentence` (the Accounts Receivable panel) now reads *Checks can only be received at {address}. Checks mailed anywhere else need to be re-issued.*
- Closing line (`bankTransferGuardLine`): the "never change by email" warning → **You can always call (512) 360-0599 before sending anything for clarity.** (no bold prefix; *call our office* when no phone is set).

Help guide `let-a-customer-pay-by-bank-transfer` and the Settings field hint follow. Tests updated.
