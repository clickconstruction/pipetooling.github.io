# v2.1976 — GC Review job rows open Edit Job (2026-08-21)

Owner request: setting a GC from GC Review meant hunting the job down on the board first. New optional `onOpenJob` prop on [`JobsGcReviewModal`](../../src/components/jobs/JobsGcReviewModal.tsx): the Job cell renders as a blue underlined button (rollup rows already carry `jobId`), and the [`JobsStagesTab`](../../src/components/jobs/JobsStagesTab.tsx) call site wires it to `tryOpenEditJob` with the standard `onSaved → loadJobs() + refreshCustomersAfterJobFormSave()` refetch. No stacking work needed — Edit Job's overlay (z 1010) already outranks GC Review's (z 60), and the modal derives its rollup from the `billedActiveRows`/`collectionsRows` props, so the post-save refetch re-renders the report in place with the modal still open. Verified live: clicked a job row inside GC Review → Edit Job opened on top with GC Review still open beneath. Help guide `track-a-general-contractor-on-a-job.md` updated. Client-only — no migration.
