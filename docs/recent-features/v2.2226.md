# v2.2226 — Payment forecast email: Email… button + share modal + deep link (2026-08-24)

Part 3 (client) of the payment_forecast train (schema v2.2223, dispatcher v2.2225). `BilledPaymentForecastModal` header gains **✉ Email…** via a new optional `onEmail` prop — `JobsStagesTab` passes it for dev/master/assistant-like (the Billed Share/Print gate), opening the new [`PaymentForecastShareModal`](../../src/components/jobs/PaymentForecastShareModal.tsx) (BilledReportShareModal sibling minus print: recipient picker filtered by `isPaymentForecastRecipientRole`, Send now / Schedule… + Repeat weekly, Preview / Email-me-a-test, pending list with Cancel) backed by [`paymentForecastEmailClient`](../../src/lib/paymentForecastEmailClient.ts). New `?forecast=1` deep link on the Stages tab (chase-pattern one-shot consume) — the email CTA's landing. `emailScheduleWeek.ts` unions + "Payment forecast" label and a sky `ENTRY_TONES` entry in `SettingsMyEmailScheduleSection`. Regenerated `database.ts` (v2.2223 table). Render smoke: Email button gated on the prop. Help guide `see-when-a-customer-will-pay` gains the "Emailing the forecast" section; REPORT_SUBSCRIPTIONS gains the stream section + inventory row + the lanes-full note.
