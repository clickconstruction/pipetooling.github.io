# v2.2909 — Papercut batch B5: New Job says when it's thinking, Import explains instead of vanishing, honest hours in the delete confirm, one name for Data & recovery, Create new job from the day picker (2026-09-05)

Journey-map Tier-3 row B5 (Job window & New Job form residue — J1-F3, J1-F4, J15-F8, J9-F4, J9-F5, J1-F8). The dirty guard + topmost-only Escape (#42, PR #2637) and Cancel-import-closes (#8, PR #2597) shipped earlier today and are not re-done here. **Client only — no migration, no edge function.**

## Per item

- **J1-F3 — C# suggestion busy hint · done.** [`JobFormModal.tsx`](../../src/components/jobs/JobFormModal.tsx) sets `clickNumberSuggesting` around the `next_job_number_suggestion` RPC; [`JobFormIdentityFields.tsx`](../../src/components/jobs/JobFormIdentityFields.tsx) renders the C# input with placeholder "finding…", `aria-busy`, and a one-line `role="status"` caption under the number row ("C# — finding the next number… (type your own if you already know it)") while it loads. The late suggestion now lands via `setClickNumber((prev) => prev.trim() ? prev : suggestion)`, so a number the office typed while waiting is never overwritten (the race the walk flagged).
- **J1-F4 — Import disabled-with-tooltip · done.** New kernel `newJobImportButtonState({ mode, isEditing, blocked })` + `NEW_JOB_IMPORT_BLOCKED_HINT` in [`lib/jobs/jobFormRows.ts`](../../src/lib/jobs/jobFormRows.ts) (tested). [`JobFormHeaderRow.tsx`](../../src/components/jobs/JobFormHeaderRow.tsx) renders Import for every new-job render; when the sheet has content it is greyed (`aria-disabled`, muted tokens, `cursor: not-allowed`) with the hint as `title`, and a tap calls the new `onImportBlockedClick(hint)` prop → the shell shows the hint as an info toast (phones have no hover). Deliberately not the `disabled` attribute — disabled buttons swallow hover/tap. The gate itself (`newJobFormHasBlockingContent`) is unchanged.
- **J15-F8 — offer the winning packet's value · done.** In `applyPrefillFromBid` the two silent `bids.agreed_value` back-fills (auto-resolved winner; `handleWinningGcPick`) are gone. The prefill now tracks `sentValue` (the forced/picked packet's value, the auto-resolved winner's, or the lone packet's) and, when the bid has an `agreed_value` or a sent value > 0, asks once via `confirmDialog`: **"Start the job at $X?"** — *Carry $X over* sets the job's fixtures to one line ("Bid price", 1 × X, description "B408 · Project — as sent" / "— agreed value") and, only when the bid had no agreed value, writes it there (`.is('agreed_value', null)` guard kept); *Start at $0* writes nothing anywhere. The prompt runs after the second-conversion confirm and the GC pick, before any field is filled, so the discard-guard baseline (armed at the end of the prefill) includes the carried line. `winningGcPick.agreedValue` (now unused) removed from the state type.
- **J9-F4 — "22.773756 hrs" · done.** New pure `approxHoursLabel(hours)` in [`lib/approxHoursLabel.ts`](../../src/lib/approxHoursLabel.ts) (tested): one decimal, "≈" only when rounding hid anything ("≈ 22.8 hrs", "8 hrs", "< 0.1 hrs"). Used by the delete confirm's Team labor line in [`JobFormDeleteMigrateModals.tsx`](../../src/components/jobs/JobFormDeleteMigrateModals.tsx).
- **J9-F5 — "Data & migration" → "Data & recovery" · done (C39 sweep on this surface).** The three delete/migrate-modal sites, plus the two other user-facing strings that said it (`useSettingsPeopleDirectory.ts` person delete confirm, `useSettingsCatalogs.ts` orphaned-prices confirm), four code comments (`DeletedRecordsSection`, `SettingsDataTab`, `BulkDeleteAlertSettingsBlock`, `settingsSearch`) and six live docs (`ACCESS_CONTROL`, `BIDS_PRICING_LABOR_TABS_ARCHITECTURE`, `PROJECT_DOCUMENTATION`, `SETTINGS_TABS_ARCHITECTURE`, `JOB_FORM_MODAL_ARCHITECTURE`, `GLOSSARY`). Outside the frozen `RECENT_FEATURES.md` archive, `grep -rn "Data & migration" src docs` now finds only this fragment.
- **J1-F8 — "Create new job" in the day-view person "+" picker · done.** [`QuickfillScheduleSection.tsx`](../../src/components/quickfill/QuickfillScheduleSection.tsx) (Schedule → Day tab, Quickfill, Dispatch Mode agenda) and [`UserDayScheduleSection.tsx`](../../src/components/userReview/UserDayScheduleSection.tsx) pass `onCreateNewJob` to `ScheduleDispatchAssignJobPickerModal` (the prop the hub grid and Quick Assign already used): snapshot the cell context, close the picker, `jobFormModal.openNewJob({ onCreatedJobId })` → reload the day → open the add-block modal for that person/day with the new job. `useJobFormModal()` is null-safe (no provider → no button). **Host gate:** `UserDayScheduleSection` takes `allowCreateNewJob` — Person Desk (page and drawer, `DESK_Z` 60) turns it on; the User Review modal (z 1200) leaves it off because the New Job form (z 1010) would open behind it. That modal's picker is the one residue of J1-F8; fixing it means re-stacking the review modal, out of scope for a papercut batch.

## Docs

`JOB_FORM_MODAL_ARCHITECTURE.md` + `JOBS_MODALS_ARCHITECTURE.md` (New Job import/prefill notes), help guides `turn-a-won-bid-into-a-job` (what comes over — the price prompt) and `job-window-tabs` (Import stays put, C# hint).
