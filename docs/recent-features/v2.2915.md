# v2.2915 — People papercuts: live and closed sessions are labelled, one roster for every role, chips say what a click does, feedback shows its skips (2026-09-05)

Journey-map **Tier-3 B9** residue (J7 payroll week · J32 crew HR file). Skips what shipped earlier today: Payroll deep-link redirect (#29, PR #2623), salaried / midnight chips (#15, PR #2599), HR pending oldest-first + age (#40, PR #2635), placeholder-inflated counts (#14a/#16), estimator two-tab shell (→ #28), payroll auto-apply default (decision 19, parked). No migration, no edge function, no DB command.

## Per item

| # | Item | Status | Where |
|---|---|---|---|
| 1 | Grid's today column shows closed sessions only while the strip above shows live ones; nothing labels the difference (J7-4) | done | new kernel `src/lib/people/hoursGridLiveByCell.ts` (`buildHoursGridLiveByWorkDate`, `liveDayChipLabel`, `liveDayChipTitle`; 5 tests) folds `activeClockSessions` (the page's pending rows with `clocked_out_at == null`, already loaded for the grid's date range) into one entry per day column through the same `buildHoursGridNameJoin` the pending badge uses. `People.tsx` memoises it; `PeopleHoursGrid` takes a `liveByWorkDate` prop and renders a green **"+N on the clock"** line under the date, tooltip "N people are clocked in right now (X h so far). This column counts closed sessions only — their time lands here when they clock out." Distinct people, not sessions; days and people off the grid are ignored |
| 2 | Dev and assistant see different Hours rosters — assistant had 8 extra zero-hour rows (J7-6) | done — root cause found | Both roles read every `people_pay_config` row (SELECT vs `list_people_pay_flags`), and `get_archived_user_names` is SECURITY DEFINER, so the 2b verifier's "the divergence lives in what pay-config rows each role can read" was wrong: the hours-tab load cycle pushed `loadArchivedUserNames()` **only under `canAccessPay`**, so an hours-only assistant never learned which accounts were archived and their pay-config rows stayed as zero-hour lines. Fixed: the load runs for `canAccessHours || canAccessPay` (the loader itself already allowed it). The row list is now derived once by `src/lib/people/hoursGridRoster.ts` (`buildHoursGridRoster`; 3 tests, one of which pins "empty archived set → archived rows leak") and `showPeopleForMatrixBase` reuses `showPeopleForHours` instead of re-deriving the same list. No "N hidden people" affordance — with one roster there is nothing hidden to disclose. #19's `fetchActiveUsers`/`activeRosterOnly` is not the right tool here: the grid is keyed by pay-config `person_name`, not `users` rows; twins never carry pay config |
| 3 | Grid-cell pending chips say "click to approve" but take two clicks (J7-N1) | done — honest copy | `src/lib/people/hoursGridPendingChipCopy.ts` (`pendingCellChipTitle` → "+9.50 h pending — click to review"; `pendingCellChipAriaLabel` → "… Click to review; approve from the popover."; 2 tests, one asserting the old phrase is gone). Chose honest copy over a one-click approve: the popover is the confirm step for this surface (owner rule 2026-08-19: confirmations are modals), and a one-click write here would re-open J7-8. `ClockSessionStripApproveControl` untouched (#15's confirm stays) |
| 4 | Feedback tab "Enabled · 5 months silent" — show skips or hide (J32-F5) | done — show skips | v2.2835's rebuild already removed the "last collected" sentence (it now shows only when the deck is Off) and gives every person a `Skipped · Nd ago` row state, so the remaining gap was the strip: due / rated / words said nothing about *why* a deck that is On produces nothing. `feedbackStats` (`src/lib/people/feedbackTabRows.ts`) gains `skippedLately` — people whose newest deal was a skip (null when Off) — and `FeedbackStatusStrip` renders it as a fourth stat **"skipped last deal"** with a tooltip explaining that a skip re-arms the full cadence. Test covers skip-after-done, skip-only, done-after-skip. Not hidden: the tab is dev-only and the toggle is real |
| 5 | J32-F1 — Person Desk as assistant: Pay hidden, one-person read | **verified (code)** | `PersonDeskBody` builds `viewer.canAccessPay` from `usePeopleAccess` (assistant → false); `PersonDeskPaySection` both skips every load (`if (!viewer.canAccessPay) return` inside the effect) and `return null`s before render, so no wage / stub / offsets query fires and no Pay section renders for an assistant. The read is per-person: `usePersonDesk` resolves one `users` or `people` row by id (plus a `people_pay_config` name-existence check) and each section loads only its own person's rows; the rail (`PersonDeskPage`) loads the roster once via `activeRosterOnly` (#19). Verdict: **LIKELY-FIXED → FIXED**; the residual is F1's structural cost (13 topic tabs still exist), tracked by the Person page train, not this batch. No live leg (impersonation not required) |
| — | Payroll deep link blank for assistants (J7-5) | skipped | #29, PR #2623 |
| — | Salaried / midnight chips (J7-3) | skipped | #15, PR #2599 |
| — | HR pending oldest-first + age (J32-F7) | skipped | #40, PR #2635 |
| — | Payroll auto-apply default (J7-7) | skipped | decision 19, parked |
| — | Hours → Draft Payroll → Tally pointers (J7-9) | skipped | Tier 5 X3 |

## Docs touched

`docs/PEOPLE_TABS_ARCHITECTURE.md` (hours dossier: the three kernels + the archived-load gate; feedback dossier: `skippedLately`; Person Desk: J32-F1 verdict), help guide `src/content/help/approve-my-teams-hours.md` (the People → Hours line: the grid chip says *click to review*, and the new "+N on the clock" header line is explained; keywords gain "on the clock"). No ACCESS_CONTROL change — no gate moved (the archived-name RPC was always callable by hours viewers; only the client call site was narrower than the gate).
