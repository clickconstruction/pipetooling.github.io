# v2.2098 — Vehicle vitals VIN ellipsis fix (2026-08-22)

Follow-up to v2.2094 caught during live verification: `vinTail()` already prefixes "…", and the modal header added its own — "VIN ……3603". [`VehicleTaskContextModal`](../../src/components/checklist/VehicleTaskContextModal.tsx) now renders `VIN {vinTail(...)}` bare. One line; couldn't ride #1796 because the merge queue had locked the branch.
