# v2.2261 — Stage-context chip on task views + assignee roadmap read (2026-08-25)

The purple roadmap chip on personal task views becomes a **stage-name line**
(owner-directed redesign, mockup artifact 86493bb1): just the stage title —
no "⛰ Farm 1 ·" prefix — rendered on its own line **below the title** on
Today/Outstanding cards, and inline in the Review/Manage row chip clusters.
Truncates at one line (max-width 230, ellipsis); the tooltip carries the full
"roadmap · stage" path; tapping still opens "Where this task fits" (which is
where the roadmap name now lives). Rendered on every task status.

Client (`src/pages/Checklist.tsx`): `roadmapGoalChip` label = stage title
(fallbacks: roadmap title, then the old "⛰ goal"); `title` added to the
`checklist_tech_tree_groups` embed in all five task-view selects (Today,
Outstanding/completed, History, Review outstanding, Manage);
`RoadmapTaskEmbed` gains `title`. Card call sites moved the chip below the
title behind a `roadmap_group_task_id` guard.

**Migration `20260825001101_roadmap_read_for_task_assignees.sql`** — the
"regardless of their status/role" half: `can_select_checklist_tech_tree_roadmap`
(SECURITY DEFINER; backs the SELECT policies on roadmaps/members/groups/
group_tasks/edges) gains a third clause — the caller is an assignee of any
task in the roadmap. Field roles assigned roadmap work previously failed every
tech-tree SELECT and saw a bare "⛰ goal"; assignment now grants structure-title
read, the same assignment-grants-access shape as `project_superintendents`.
Note the deliberate breadth: an assignee can read ALL stage/task titles of
that roadmap, not just their own stage (owner-approved).
