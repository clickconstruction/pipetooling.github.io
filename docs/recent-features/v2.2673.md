# v2.2673 — Overhead pool trend + composition card (2026-09-03)

Owner ask after the overhead-feed repair (v2.2670–v2.2672): "is the 90-day pool trending up or down, and a better ledger of office labor / bid labor / parts." First real piece of the Overhead-tab refresh prototype ("The burn, day by day") to land.

## Client only — no migration

- New pure kernel [`lib/overheadPoolTrend.ts`](../../src/lib/overheadPoolTrend.ts) (+5 tests): `buildOverheadPoolTrend({ laborByDay, partsUsdByDay, startYmd, endYmd })` zero-fills every calendar day of the window with office/bid labor $ + office parts $, computes a trailing 7-day average per day, and a verdict — average $/calendar-day over the last `compareDays` (30) vs the `compareDays` before; `|Δ| < 5%` reads `flat`; no prior cost → `deltaPct: null`, flat.
- Inputs are the SAME maps the tab's 90-day KPI/three-lenses effect already builds (`buildOverheadDailyLabor(...).byDay`, the office-parts $/day loader) — zero new fetches; the card is set right after `mergeOverheadDayTableRows` and nulled in the effect's catch.
- New presentational [`components/people/OverheadPoolTrendCard.tsx`](../../src/components/people/OverheadPoolTrendCard.tsx): verdict pill (amber up / green down / muted flat, tooltip with both averages), composition bar + legend with $ and % per component, SVG stacked-daily chart (purple office labor, blue bid labor, amber parts — the tab's existing series colors) with month ticks, nice-step gridlines, a 7-day line, and per-day `<title>` tooltips. Placed between the three-lenses strip and the maintenance strip.
- Help guide `understand-overhead-numbers.md` gains "Is the pool going up or down?".

Semantics unchanged from the KPIs: approved, wage-priced sessions + office-job parts (internal transfers excluded) — the hygiene strip's caveats apply here too.
