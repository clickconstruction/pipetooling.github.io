# v2.2225 — Payment forecast email stream: dispatcher edge function (2026-08-24)

Part 2 of the payment_forecast train (schema v2.2223; client UI next). New edge fn `payment-forecast-email-dispatch` — a line-for-line sibling of `billed-report-email`: preview / test_send / send_now (staff JWT gate; office-capable recipients incl. primary) + cron dispatch (`X-Cron-Secret`, batch 10, attempts < 5, `repeat_weekly` +7d re-enqueue with the double-insert guard). Bucketing runs in-function via the new Deno kernel port `supabase/functions/_shared/paymentForecastCore.ts` (source of truth: `src/lib/jobs/billedExpectedPay.ts` + `billedPaymentForecast.ts` — deliberate identities documented in its header: raw-`billed_at` UTC slice, ≥3-sample own-median rule, promise override, Sunday weeks). Render (`render.ts`): tile strip → pay-speeds line → buckets **Past expected first** with per-row basis notes and `?jobDetail=` deep links; CTA lands on `?tab=stages&forecast=1`; empty board sends a one-liner. `config.toml`: `verify_jwt = false` (in-function JWT, cron secret). EDGE_FUNCTIONS.md section added.
