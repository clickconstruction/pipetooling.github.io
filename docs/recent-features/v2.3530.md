# v2.3530 — Stages tab decomposition: the three inline modals leave `JobsStagesTab` (2026-09-16)

Client only, behavior-preserving. Item 10 of the owner's ready-to-build train (`to-dos/engineering-hygiene.md`, *Decompose JobsStagesTab / BidsPricingTab*), done as the playbook's region 5 of [`JOBS_STAGES_TAB_ARCHITECTURE.md`](../JOBS_STAGES_TAB_ARCHITECTURE.md) — the lowest-risk single-opener regions, cut and merged while no feature PR was open on the file.

## What moved

| Out of `JobsStagesTab.tsx` | Into | Props |
|---|---|---|
| Billed Awaiting Payment by Job Name dialog | `src/components/jobs/StagesBilledTotalByNameModal.tsx` | `rows`, `expandedName`/`onToggleName`, `onPrint`, `onGoToBilled`, `onClose` |
| Capable of Being Billed — Breakdown dialog | `src/components/jobs/StagesCapableToBillModal.tsx` | `rows` (from `buildCapableToBillBreakdownRowsWithPlans`), `total`, `onView`, `onGoToWorking`, `onClose` |
| Est. bill date for partial invoice dialog | `src/components/jobs/StagesEstBillDateModal.tsx` | `target`, `date`/`onDateChange`, `saving`, `onSave`, `onCancel` |

Stage A first: the Total by Name grouping (group by `job_name`, "—" when empty, total the remaining balance, largest first) is `buildBilledTotalByNameEntries` in `src/lib/jobs/invoiceBilling.ts` with two tests. `JobsStagesTab.tsx` 6,598 → 6,328 lines; three imports it no longer needs dropped.

## What stayed, and why

The three open flags, `billedTotalByNameExpandedName` (its reset-on-close effect lives beside the other effects), `whenInvoiceBillModal` + its date, `printBilledAwaitingPaymentReport`, the View door (`tryOpenEditJob` with the reload callbacks) and both "take me to" section scrolls — each is also written by the imperative handle, the ⋯ menu or the unified table, so per the playbook they stay in the parent and reach the components as callbacks. Preserved quirks: the `#2563eb` View border and `#3b82f6` Save button (saturated action colors, allowed literal), the Save handler's own empty-date guard.

## Verify

`JobsStagesTab.render.test.tsx` still opens Total by Name through the handle (12 tests green); typecheck, lint, theme check clean. Live: Pipeline → ⋯ → Total by Name expands a name and prints; the Working header's capable-to-bill total opens the breakdown and View opens Edit Job.
