# v2.2019 — Payment forecast: bucket tiles center their contents (2026-08-21)

Owner request from a screenshot: the forecast modal's bucket tiles (Past expected / This week / Next week / two-week window / Later) left-aligned their title, sum, and bill count. One-line change in [`BilledPaymentForecastModal.tsx`](../../src/components/jobs/BilledPaymentForecastModal.tsx): the tile button's `textAlign` flips `left` → `center`. Verified live from a worktree dev server (all five tiles compute `text-align: center`; screenshot confirms the centered stack). Client-only — no migration.
