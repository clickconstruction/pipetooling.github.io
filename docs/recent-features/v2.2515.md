# v2.2515 — Robot price research: schedule products → web prices, checkably (2026-08-30)

The fixture schedule already names manufacturer + catalog # for every product; this
release lets a twin research those on the web and record what it finds:

- Migration `20260830210000_robot_price_research.sql`: `material_parts.is_robot`,
  fence v3 (robot parts + prices on robot parts), seeded `🤖 Web Research` supply
  house for provenance.
- Owner requirement: every researched part carries its **source URL in
  `material_parts.link`** — the estimator clicks and checks. Notes carry date +
  price type; "call for pricing" items get a part + link + flag, no invented price.
- Doctrine: new PLACEMENT.md chapter (research protocol; researched prices are
  cost-basis only — sale prices remain the Workbench's margin call). Briefs
  regenerated; twin-mcp redeploy.
- Tested by running the BT-1 (Take 5 6811) schedule through it — see the results
  in the fleet ledger report.
