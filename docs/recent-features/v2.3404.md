# v2.3404 — The sweep: builders entered as both customer and GC become the GC only, and the database keeps it so (2026-09-14)

Migration `20260914160000_job_customer_gc_distinct.sql` ([`docs/migrations/20260914160000_job_customer_gc_distinct.md`](../migrations/20260914160000_job_customer_gc_distinct.md)); no client change, no edge function. The database half of the owner's 2026-09-14 rule — the form half is v2.3403 ("a job's customer and GC are never the same party").

## What shipped

- **Trigger `jobs_ledger_customer_gc_distinct`**: a write naming one customers row as both customer and GC lands as a GC job (customer link and copies cleared, `bill_to_party = 'gc'`). Moves rather than rejects, so `merge_customers`, RPCs, edge functions and older clients all obey it.
- **`job_bill_payer_customer_id`**: a GC with no customer link pays — the SQL twin of the client's `effectiveInvoiceParty` change in v2.3403.
- **The sweep**: the 72 same-party jobs (RMC 23 · Knight 13 · H & I 6 · Palmer 6 · Heron 4 · …; 34 billed · 15 paid · 15 working · 8 waiting) are GC jobs now. 71 already billed the GC (v2.3350); nothing about who pays changed for them, only that the customer row no longer repeats the builder. Property-record links stay.

## Apply

`supabase db push` **after the v2.3403 client has deployed** (its Send-bill gate reads a GC job as billable; the older client's does not), then redeploy the `_shared/billToParty.ts` importers listed in `docs/recent-features/v2.3403.md`.
