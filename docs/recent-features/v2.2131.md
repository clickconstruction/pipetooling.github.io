# v2.2131 — GC Review: "Draft Message", GC pill first, subject copy (2026-08-22)

Owner requests from the GC Review → Certify flow (screenshot of the per-GC statement dialog):

- **Button copy** — [`GcReviewCertifyModal`](../../src/components/jobs/GcReviewCertifyModal.tsx): the blue `Certify & send…` is now **Draft Message** (behavior unchanged: `certify(true)` records the certification and opens the statement email dialog; nothing sends until Send statement).
- **GC pill first + selected** — new kernel [`gcEmailChip(gcName, email)`](../../src/lib/teammateEmailChips.ts) (+ tests) builds the GC's own chip from the customer email (`emailForGc`); [`TeammateEmailChips`](../../src/components/jobs/TeammateEmailChips.tsx) gains a `leading` prop (rendered ahead of teammates, deduped by email). [`JobsGcReviewModal`](../../src/components/jobs/JobsGcReviewModal.tsx)'s per-GC dialog passes it (per-GC grouping only; development groups have no GC email). Since the To field opens prefilled with the GC's email, `chipMatchesValue` lights that pill — no extra selection state. No email on file → no GC chip, teammates as before.
- **Subject** — [`gcStatementEmailSubject`](../../src/lib/jobsDocuments/gcStatementEmail.ts) → `Click Plumbing open balances: <date>` (new `GC_STATEMENT_SUBJECT_NAME`). The "Share all" report subject is unchanged. [`emailLogStreamLink`](../../src/lib/emailLogStreamLink.ts) gains the new prefix so Settings → email log still files these under `gc_statement`. Server mirror: `gcStatementSubject` in [`gc-statement-email-dispatch/render.ts`](../../supabase/functions/gc-statement-email-dispatch/render.ts) (scheduled sends) — **needs `supabase functions deploy gc-statement-email-dispatch --no-verify-jwt`** to take effect; until then scheduled sends keep the old subject (the log matcher accepts both). `docs/EDGE_FUNCTIONS.md` section updated.
- Help: `track-a-general-contractor-on-a-job.md` step 3.

Client + one edge-function copy line; no migration.
