# v2.2497 — DMARC dashboard links in Settings → Advanced (2026-08-29)

Owner request after the Resend sender migration: app-side *visibility* into
DMARC reporting without building ingestion (deliberately rejected — see
DOMAIN_CUTOVER.md; Cloudflare DMARC Management renders the reports for free).

- **`SettingsAdvancedTab`**: new dev-only "Email domain monitoring" card
  inside the collapsible Advanced section — two external links to the
  Cloudflare DMARC Management dashboards (clicktooling.com + pipetooling.com
  zones), with one line of context (should only ever be Resend; read reports
  before tightening the policy). New `isDev` prop; the parent passes
  `myRole === 'dev'`. The account-scoped dashboard URLs are constants in the
  component with a pointer to the runbook.
- No help guide: dev-only infrastructure link, not a user-facing flow.
