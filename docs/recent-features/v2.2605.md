# v2.2605 — Who owes what: physical-invoice resend (2026-09-01)

Last of the three "PDF + send" PRs (v2.2600 PDF tail, v2.2604 Stripe resend).

- `supabase/functions/send-physical-invoice-email` — new `resend: true` mode: requires `status = 'billed'` (instead of `ready_to_bill`) and **skips the row update entirely** — no status flip, no `sent_to_customer_at` bump; the send still lands in the shared email log (`logEmailSendBestEffort`). **Deploy manually** (`supabase functions deploy send-physical-invoice-email`) after merge — see `docs/EDGE_FUNCTIONS.md` § send-physical-invoice-email.
- New `src/lib/resendPhysicalInvoiceEmail.ts` — rebuilds the billed invoice document (`buildPhysicalInvoiceDocumentForBilledInvoice`, same builders as the PDF tail), regenerates PDF + email bodies, and invokes the function with `resend: true`. Recipient = the invoice's `bill_to_email` override, else `jobs_ledger.customer_email` (`physicalResendRecipient`).
- New `src/components/jobs/PhysicalInvoiceResendButton.tsx` — "Email again — PDF attached" with a confirm naming the recipient (+ render smokes).
- `src/lib/jobs/billedByCustomerBreakdown.ts` — bills carry `externalSendChannel` + `billToEmail`.
- `src/components/jobs/BilledByCustomerBreakdownModal.tsx` — the card's evidence line now covers all three channels ("… · stripe", "Emailed with PDF <date> · physical", "Billed in HouseCall Pro — HCP sends its own invoice"); physical bills with a recipient get the resend button, which records the same best-effort `resend` chase touch as the Stripe pairing.
- Help: `read-the-pipeline-money-view.md` covers the per-channel send buttons.
