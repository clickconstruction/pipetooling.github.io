# v2.3502 — Submittals: the room's function URLs read one slash (2026-09-16)

Fix, client only. The live build's `VITE_SUPABASE_URL` ends in a slash; `SubmittalRoom.tsx` concatenated `/functions/v1/…` onto it and every room call (get-submittal-room, submit-submittal-review, open-submittal-pdf) read `supabase.co//functions/…`. Supabase tolerated it, so nothing broke; the page now strips the trailing slash once. Seen on the live-site check after the 4b deploy.
