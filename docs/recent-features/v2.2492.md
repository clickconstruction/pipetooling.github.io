# v2.2492 — Needs You: claim-dev item — migration complete (banner migration 6/6) (2026-08-29)

Final v2.2339 follow-up: `DashboardClaimDevAttemptsBanner` folds into the
Needs You card. Stacked on v2.2491.

- **New hook `src/hooks/useClaimDevAttemptsNudge.ts`**: the refused-attempts
  count query (7-day lookback, focus refetch, dev-only via RLS) + the
  `claimDevAlertDismiss` snooze/dismiss state, extracted from the banner.
  Exports `CLAIM_DEV_LOOKBACK_DAYS` so the kernel copy and the query agree.
- **Kernel**: inputs `claimDevRefusedCount` / `claimDevLookbackDays`; item
  `claim-dev` (red, action "Review accounts" → `/settings?tab=settings-people`,
  secondaries Snooze 24h / "Dismiss until it happens again") — last in the
  stack, mirroring the old banner order. Banner deleted.
- **The v2.2339 migration is done**: all ten attention sources render through
  `buildNeedsYouItems` on both the Dashboard and Quickfill (job-followups and
  GC weekly stay Dashboard-card-only — Quickfill's dedicated stations carry
  them there; GC weekly's green done state renders as `GcReviewWeekDoneNotice`).
  `DashboardPinnedQuickRow` no longer renders any standalone banner.
- Tests: 1 new kernel test. Help guide updated.
