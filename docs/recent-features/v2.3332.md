# v2.3332 — Test reports: the gas paper dates House pressure and hides empty utilities (2026-09-11)

Kernel only (`supabase/functions/_shared/testReport.ts` → `buildTestReportBlocks`); the modal preview, jsPDF and pdf-lib follow the same blocks. No migration. Redeploy `send-test-report` and `auto-send-test-reports` so server-rendered gas papers match (gas is never auto-sent, so the browser is the usual renderer).

Owner, 2026-09-11: "on the gas test move the gas report date into the house pressure section" and "on the gas report, when nothing is reported in the house utilities, hide it from the final report."

- Gas: the *Date* row leads **House pressure** (Date · PSI · in WC · oz/in² · mm WC); the section always prints, the four readings only when a pressure was taken; the location column no longer carries the date.
- Gas: **House utilities** prints only when `gasFixturesTotalBtu(fixtures) > 0` — a quick-pick name with no BTU figure used to print as *Furnace — BTU/hr · Total —*. Named-only rows still list once any row has a figure.
- Hydrostatic (Test details) and pinpoint (Test results) unchanged. Tests pin all three placements and the utilities gate. The help guide's date sentence follows (stacked on v2.3328, PR #3056).
