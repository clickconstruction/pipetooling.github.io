# v2.2504 — Placement engine v0 + fleet calibration + extractor automation + M4 (2026-08-30)

Waves **3.4** (engine v0, counters-first), **3.1** (calibration run), and **1.2**
(extractor automation) of docs/ESTIMATOR_TWIN_PIPELINE_PLAN.md, plus the M4 mission —
the last engineering between the twin fleet and doing takeoffs itself.

- **Coordinate kernel** `src/lib/takeoffPlacement.ts` (+9 tests): raw-px→PDF-point base
  frame, readable-crop→raw (the 90° CW hop), overview-bbox→crop rect; `assembleTakeoff`
  + `validateTakeoff` (mirrors import-takeoff's rejections by name) +
  `countsVsSchedule` (the "counters prove out" self-check).
- **Assembler CLI** `scripts/placement-engine/assemble.ts` (vite-node, imports the
  kernel — one implementation) + `pagesize.sh`. Smoked on the real 55-page LIVSTE set:
  fixture tags read clearly at 300 DPI on P201; a real placement round-tripped to base
  points and the self-check caught a deliberate count miss.
- **`docs/twins/PLACEMENT.md`** — the engine's operating manual (EXTRACTOR.md's
  sibling): the counters-first procedure, coordinate math, self-checks, import loop.
- **Calibration (3.1)** `docs/twins/CALIBRATION.md`: 205 human takeoffs — marks
  p25/50/75 = 14/51/123 (max 1218), lines p50 = 57, ~35 counters defined/project.
  **Load-bearing finding**: humans count valves/cleanouts/hose bibbs the fixture
  schedule never lists — M4 scores per-tag over schedule tags only; the gap list is the
  v1 roster.
- **Extractor automation (1.2)**: `sweep.sh` (all-page overview contact set +
  manifest) and `tiles.sh` (grid of readable crops + raw rects for the kernel) in
  `scripts/substrate-extractor/`; EXTRACTOR.md updated.
- **Mission M4** written (twin does the LIVSTE counters takeoff; Wendi's takeoff is the
  reference; kill/resume ride-along) — briefs regenerated (M1–M5 bundled), twin-mcp
  redeployed.
