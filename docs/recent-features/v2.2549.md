# v2.2549 — Audit cockpit: review the differences, not the bid (2026-09-01)

Owner-approved mockup; rework of `BidsAuditsTab` (data layer + notes/receipts
machinery unchanged):

- **Triage rows + expand-one**: collapsed one-liners (bid, status chip, draft $,
  Δ vs reference, question/note counts, age); first pending auto-expands; Finish
  advances to the next pending audit.
- **Comparison strip** on the open card: robot draft / our reference value (via
  `twin_source_bid_id`) / Δ%. **Seal rule**: an unsent reference (shadow) never
  shows a value or Δ — "Δ sealed 🔒".
- **Biggest rows — tap to judge**: the twin's top-8 priced rows (lazy fetch,
  active-version rules); 🚩 prefills the composer with the row facts and a
  section guess (ft-of → Footage), 👍 is a local ack.
- **One composer with section chips** replaces the five per-section textareas;
  empty sections no longer render.
- Help guide `audit-a-robot-bid.md` rewritten to match.
