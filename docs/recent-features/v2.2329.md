# v2.2329 — One-click invoice PDF tail on Stages billed rows (2026-08-26)

Owner-approved mockup (icon-only variant):
claude.ai/code/artifact/6f24c781-16e8-44e9-bea3-b9222c9260fb.

- New `src/lib/openBilledInvoicePdf.ts`: `openBilledInvoicePdfInNewTab`
  composes the existing builders (`fetchJobWithDetailsById` →
  `buildPhysicalInvoiceDocumentForBilledInvoice` →
  `buildPhysicalInvoicePdfBlob`) and points a new tab at the blob. The tab
  opens synchronously before any await (pop-up blockers), closes itself on
  failure, and the object URL revokes after 60s — same discipline as the
  View bill modal's handler.
- New `ViewBillWithPdfTail.tsx` (+render smoke): split control — View Bill
  (radius squared on the right) + icon-only PDF tail with aria-label/title,
  wait state while the PDF builds, toast on pop-up-block or error.
- `JobsStagesUnifiedTable.tsx`: all three View Bill sites (merged-bundle row,
  single-billed-invoice row, per-invoice row) swap to the split control —
  each already resolves the exact invoice, and the tail targets the same one.
- Help guide `see-when-a-customer-will-pay.md` gains the PDF-tail paragraph.
