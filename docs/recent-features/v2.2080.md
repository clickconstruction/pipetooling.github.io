# v2.2080 — Waiting to hear clarity pass: window removed, one summary line (2026-08-22)

Owner ask on the Followup → Waiting to hear lens: "show all by default, remove the
30/60/90 and the 'sent within the window'" + a general make-it-easy-to-understand pass
(mockup approved before build).

- **Sent-within window removed** (`PENDING_CHASE_WINDOWS`, `PENDING_CHASE_DEFAULT_WINDOW_KEY`,
  `bidSentWithinWindow` deleted from `src/lib/bidPendingChase.ts` + test): the queue is now
  always every sent bid with no outcome yet. The pills, the "Sent within the window, newest
  first · arrows move between bids" caption, and the "widen the window" empty state are gone.
- **Bottom rollup bar folded into the header**: the amber "N sent bids to chase" pill is now
  followed by one muted sentence — `of N still open · $X waiting on an answer · N never
  called, oldest Nd` (same `buildPendingChaseRollup` numbers, one home instead of two).
- **Explainer line** under the header states the queue rules once in plain words: what's in
  the queue, the `PENDING_CHASE_STALE_CONTACT_DAYS` (7-day) needs-a-chase rule, what a tap
  does, and the arrow keys.
- **De-jargoned**: sidebar "fresh" → "all caught up"; panel "1 of 9 fresh" → amber
  "8 of 9 need a chase" (green "all caught up" when done); the bid-pill row gets a visible
  "Their open bids" label; the action chips get a "What happened?" micro-label.
- **Builder rail scrolls independently** (`maxHeight: min(72vh, 42rem)`) — with the full
  queue the rail can run to dozens of builders (~2,000px before; page height ~950px after).
- Help guide `follow-up-with-builders.md` updated to match (window + rollup-bar paragraphs).

Files: `src/components/bids/BidsWaitingToHearLens.tsx`, `src/lib/bidPendingChase.ts`(+test),
`src/content/help/follow-up-with-builders.md`.
