# v2.3454 — Job accounts from the bid: the follow-up (2026-09-14)

Client only; no migration. The optional second PR of the *Job Accounts from the Bid* proposal, plus one gap found while planning the estimator's live test of v2.3451.

## What shipped

- **Mark opened from the bid** — the guide promised it and nothing offered it: every Mark opened door (the house roster, the PO line, the invoice form, the Dispatch card) is office-only. On [`BidJobAccountsRow`](../../src/components/bids/BidJobAccountsRow.tsx) a chip that is not open is now a button opening [`MarkJobAccountOpenedModal`](../../src/components/materials/MarkJobAccountOpenedModal.tsx) for that house with the rep from the bid-side RPC; the estimator's write lands through the v2.3451 policy branch. Open chips stay static.
- **The Dispatch card's byline** — [`JobAccountBidByline`](../../src/components/JobAccountBidByline.tsx) under an `open_job_account` ask: *Bid B398 · Pondhill Building 2 · bid by Tristen · priced with Reece, Ferguson — the estimator knows the rep; a phone call can beat a card.* Reads the job's bid through `job_account_job_identity`, the bid's number / name / author, and `bid_rfqs`. Hand-made jobs show nothing. This is the one-inbox answer to "who owns a bid-linked ask" from the second pass.
- **The Directory** — reps now load `role` and `phone`; a `job_accounts` rep wears a teal *Job accounts · phone* tag; a house whose `job_accounts` is `expects` with no such rep gets an amber line under its reps. `DirectoryHouse.job_accounts` and `DirectoryRep.role / phone` are optional on the kernel types.

## Verified

Typecheck, lint, theme check, tests green. Live — see the PR: the chip → Mark opened as Robert and as an estimator through Imitate, the prompt's email sheet from a won bid, the Directory tag on Ferguson.
