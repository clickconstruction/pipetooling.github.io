# v2.3218 — Bill-truth shadow beacon removed (2026-09-10)

Client only. Closes the v2.2862 "one release only — REMOVE BY the next bill-truth PR, or 2026-09-19" note.

## What changed

- **Deleted** `src/lib/billing/billTruthShadow.ts` — `reportBillTruthShadow`, `billTruthShadowMismatch`, the `bill_truth_mismatch` nav-click control, and the six legacy sums (`legacyDashboardArOwed`, `legacyBilledPinTotal`, `legacyStripBilledTotal`, `legacyHubOpenBalance`, `legacyListOpenBalance`, `legacyFooterLifetime`).
- **Call sites** now compute the kernel figure only: `useDashboardFinancials` (AR card), `useBilledTotal` (Billed pin), `fetchStagesHeaderStats` (Pipeline strip), `BilledAwaitingPaymentSection` (Quickfill), `CustomerDetail` (hub open balance + lifetime footer; its shadow `useEffect` and the `appliedByInvoiceId` / `openBillRowsForJob` imports go with it), `Customers` (list open balance; the per-customer legacy rollup block goes).
- `fetchStagesHeaderStats.test.ts` drops the beacon mock and its assertion. `billTruth.test.ts` is untouched — the kernel is the same.
- Docs: `BILLING_FLOWS.md` / `GLOSSARY.md` no longer describe the beacon as live.

The test-gap passes (v2.3021 / 3022 / 3026 / 3034) had skipped this file as "the one-release beacon slated for removal"; nothing else referenced it. No `ui_nav_clicks` row with control `bill_truth_mismatch` is written from here on — the ones already logged stay queryable.
