# v2.2208 — Workbench batch 2: solver line slimmed, Price modal, no-squish cards (2026-08-23)

Second live-iteration batch with the owner (mockups: solve-split 11c68afc, solo-unpriced 0a627c7c, apply-inline 370f8f3c, price-modal a4133103). `BidsPricingTab.tsx` + the workbench help guide.

- **Stats**: whole dollars (`$20,000`, cents in the tooltip with our-cost), title + value centered.
- **Solve is a split button**: the ▾ menu holds *Price unpriced only* (hint: "fills only rows with no sale price… priced rows are held as-is"); the standalone button and the "→ previewing $X" echo are gone.
- **Apply/Discard inline**: right-pinned on the solver line with *nothing saved yet* beneath; wraps right-aligned on narrow pages; "Previewing N prices —…" sentence removed; label is just **Apply** (target named in the tooltip).
- **Unpriced solo bids**: the "One GC · one price … price below or use the solver" band is gone — the solver card comes first and **＋ Add price** rides its right end (`wbSolverEnd` holder). Keyed on the *viewed* price being empty.
- **Price modal**: ✎ on every card (replaces the inline rename input) → Name + **Delete** (hands off to the existing type-name-to-confirm delete with its 90-day restore note; disabled on the ★ with "the GC's letter is built on this price — make another price the base first") + Cancel/Save. Renames reload pricings + the picker. Delete-confirm copy "price scenario" → "price".
- **Cards**: min 215px / max 300px, footers wrap cleanly — many prices flow to a second row instead of squishing.
- **Progress line**: "1 of 1 priced" (was "1 of 1 costed rows priced — all priced ✓"); the unpriced-cost dollars moved to its tooltip.
- Tour "Solve to a number" step + the workbench guide updated to match (▾, Apply/Discard, Price modal).
