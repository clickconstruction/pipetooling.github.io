# v2.2844 — Job Summary: first-look polish (2026-09-05)

The screenshot pass over the shipped analysis views (plan artifact 9c02d29a) on real data, folded into one PR.

## Client only — no migration

- [`JobSummaryLedgerToolbar.tsx`](../../src/components/jobs/JobSummaryLedgerToolbar.tsx): the "vs Apr 28 → Dec 31" compare caption carries both years and renders only on the row-driven views (Jobs / Months / Cycle) — it had been showing on Capacity, Ahead, Timeline too.
- [`JobSummaryCycleView.tsx`](../../src/components/jobs/JobSummaryCycleView.tsx): y-axis fits the data (floor 8 d, 1.25× headroom) instead of `max(45, …)`; the 30-day line and legend entry appear only when the axis reaches 30, otherwise the legend says every month is under 30.
- [`JobSummaryScatterView.tsx`](../../src/components/jobs/JobSummaryScatterView.tsx): y floor pinned at −50% (`≤−50%` tick) with a pinned count, so jobs at −100% (tiny revenue, real costs) stop compressing the plot.
- [`JobSummaryCapacityView.tsx`](../../src/components/jobs/JobSummaryCapacityView.tsx): weeks with `fieldHours === 0` label "no hours" (muted) instead of amber 0%; the footnote counts them and says they pull utilization down (this year: Jan → mid-Mar has no approved field hours at all); the first month tick is dropped when the next is within two columns ("Dec 2025Jan 2026" collision).
- [`jobSummaryRework.ts`](../../src/lib/jobs/jobSummaryRework.ts): pair `kind` gains `'open'` — an unbilled return whose job isn't finished. It stays in the callback count (on real data every unbilled return this year is "in progress" by the percent rule, so excluding them zeroed the view) but is tagged "still open" in the pairs table and counted in the control row. Test extended.
- Help guide "see how often we go back": the in-progress sentence.
