# v2.1984 — Dashboard Wednesday nudge + GC Review deep link (2026-08-21)

Phase 3 (final) of the Wednesday certify-and-send ritual. New self-gating [`DashboardGcReviewWeeklyBanner`](../../src/components/DashboardGcReviewWeeklyBanner.tsx) in the attention stack (office roles at the mount site, same gate as the follow-ups banner): fetches `gc_review_week_status(week)` once and renders per the kernel's nudge state machine — amber "GC review is due today / still due this week" with a remaining-GCs badge and "N of M certified · K sent" from Wednesday until every outstanding GC is certified AND sent, green "done for the week" for the rest of Wednesday, nothing otherwise. Click-through navigates to `/jobs?tab=stages&gcReview=1`; [`JobsStagesTab`](../../src/components/jobs/JobsStagesTab.tsx) consumes the new `?gcReview=1` param (same consume-once-and-clean pattern as `?followups=1`) and opens GC Review, whose membership in `stagesNeedsAllScopesForModal` loads the board data it needs. Verified live against prod: banner rendered "12 · still due this week · 1 of 13 certified · 0 sent" (RPC counts matching the modal's client rollup exactly), click landed on the Pipeline with GC Review open and the param cleaned. Help guide notes the Dashboard reminder. Client-only — RPC shipped in v2.1980.
