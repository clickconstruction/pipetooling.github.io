# v2.3812 — Fix the Not-coming-in undo RPC role gate (2026-09-24)

Hotfix, reported by Taunya on 2026-09-24: "I need to remove days off if the employee will be coming in after all". Pressing **Mark as coming in** on a Schedule Dispatch chip toasted `Failed to pay staff remove not coming in for user day: function public.is_assistant_of_pay_approved_master() does not exist`.

- `20260831194854_ncns_dispatch_undo_note.sql` (v2.2540) re-created `pay_staff_remove_not_coming_in_for_user_day` from the BASELINE body to widen its note predicate — and carried the baseline's role gate, which names `is_assistant_of_pay_approved_master()`, dissolved and DROPPED on 2026-07-14 (`20260714200000`). Every undo since 2026-08-31 raised at the gate. The self-serve path (a person clearing their own mark) inserts/deletes `user_time_off` directly and was never affected.
- Migration `20260924203922` re-creates the function with the live gate `is_dev() OR is_pay_approved_master() OR is_assistant()` — the dissolve migration's exact substitution. Delete predicate, salary re-sync and return shape unchanged (precedents: `20260805110000`, `20260903030000`).
- Third time the `baseline_function_body_gotcha` shipped (v2.1400, v2.2672, now). `scripts/check-migrations.sh` gains check 4: any migration versioned after the drop whose SQL (comments stripped) names `is_assistant_of_pay_approved_master` fails CI, with the three already-applied offenders allow-listed by version because an applied migration is immutable.

No client change.
