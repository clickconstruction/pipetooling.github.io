# v2.3442 — `src/types/database.ts` regenerated after the demand-letter exhibits push (2026-09-14)

Mechanical; no migration, no edge function. `npm run gen-types:linked` (via `scripts/db-push.sh`) after `20260914243000_job_demand_letters_exhibits.sql` was pushed (drift 575/575): `job_demand_letters.exhibits` / `debtor_party` land in the generated `Row` / `Insert` / `Update` types, plus whatever other sessions' pushes of the day changed — generated files are regenerated, never hand-merged. The v2.3429 client's `as never` casts on the insert can be removed at leisure; the fallback insert shape stays until every client has the new bundle.
