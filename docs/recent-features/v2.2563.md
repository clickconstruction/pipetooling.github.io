# v2.2563 — "Quick Estimate" renamed "Estimate/Change Order" (2026-09-01)

Owner request, label-only. The rendered name changes in the four places a user
sees it — Dashboard button, wizard sheet title + aria-label, the Settings →
Dashboard toggle, and the two help guides ({{button}}/{{chip}} tokens; the
"quick estimate" keywords stay for search). Internal names (QuickEstimateWizard,
quickEstimate kernel, code comments, past release notes) deliberately keep the
original — rename-the-surface-not-the-code, same policy as the ClickTooling
sweep (v2.2526).
