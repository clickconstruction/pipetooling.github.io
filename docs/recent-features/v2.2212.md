# v2.2212 — Partner statement: acknowledgment flow removed + Full-ledger header alignment (2026-08-23)

Owner request (screenshot): remove "the acknowledged and acknowledged confirmation entirely" from /my-statement, and the Full ledger's DATE/POSTING/AMOUNT/BALANCE header looked crooked.

**Acknowledgment removal** — the whole partner-facing flow, since the page was its only home and every indicator would otherwise wait forever:
- [`PartnerStatementPaper.tsx`](../../src/components/partner/PartnerStatementPaper.tsx): the green `acknowledged <date>` stamp + `Acknowledge statement` button (WeekActions keeps Print), the lens "Partner acknowledged / Awaiting…" line, and the D7 "Last statement · awaiting your sign-off" section (with `awaitingStatementCard`, now deleted from `partnerStatementModel.ts`).
- [`PartnerStatement.tsx`](../../src/pages/PartnerStatement.tsx): `acknowledge()` / `acking` / the `acknowledge_partner_statement` RPC call dropped.
- [`PartnerStatementNavLink.tsx`](../../src/components/partner/PartnerStatementNavLink.tsx) + `Layout.tsx`: the amber dot / "sign-off waiting" tag and the `awaitingSignOff` prop.
- [`DashboardPartnerLedgerSection.tsx`](../../src/components/dashboard/DashboardPartnerLedgerSection.tsx): the "Last week's statement is waiting on your sign-off" nudge.
- Help guide `see-your-partner-ledger`: "Acknowledging a statement" → "Printing a statement".

Kept: the DB side (`pay_stubs.partner_ack_at`, the `acknowledge_partner_statement` RPC, existing timestamps) and the office Statements tab's ack tracking — nothing partner-facing writes it anymore. `useIsPartner`'s `awaitingSignOff` field still computes but has no consumers; `invalidatePartnerNavStatus` keeps its other-caller contract.

**Header alignment** — `FullLedgerTable` rendered each row as its own CSS grid with `auto` money columns, so the header's AMOUNT/BALANCE columns sized to the header text while data rows sized to the numbers — right edges drifted per row. The grid is now `72px 1fr 96px 104px` (shared fixed money columns), so the headers sit exactly over the figures. Render tests updated (paper, nav link, dashboard entry card); `awaitingStatementCard` tests removed. Client-only.
