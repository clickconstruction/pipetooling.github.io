# v2.2281 — Goals bar uniform grid (2026-08-25)

Owner feedback on the live v2.2278 wrap: flexbox grew the last row's few
segments (51–54 on Farm 1) to fill the whole line — they read as giant,
more-important stages.

- `GoalsStageStrip` swaps the wrap flexbox for a grid:
  `repeat(auto-fit, minmax(12px, 1fr))`, `gap: 6px 2px`. Uniform columns by
  construction — every segment identical on every row, short last row ends
  early with empty space. `auto-fit` collapses unused tracks, so a
  small-roadmap single row still fills edge-to-edge (old `flex: 1` feel).
- Regression test updated to pin the grid template instead of the flex basis.
- Mockup (round two): https://claude.ai/code/artifact/bdde9680-4fed-4716-8635-11e2bb17f89e
