# v2.2649 — Lien signature lanes atop the Dispatch inbox (2026-09-02)

Real-signer test finding (v2.2621 follow-up): the master's daily surface is `/dispatch-mode/inbox`, where the lanes rendered only inside `DashboardTeamsInboxCard` — below Needs You and the whole My Inbox card, effectively invisible.

- `src/components/dispatchMode/DispatchModeInbox.tsx` — mounts `LienSignatureInboxSection` directly above `DashboardMyInboxCard` (renders nothing when both lanes are empty).
- `src/components/dashboard/DashboardTeamsInboxCard.tsx` — new `hideLienSignatures` prop (Dispatch inbox passes it) so the lanes don't double on that page; Dashboard/Checklist mounts unchanged.
- Help: guide now names Dispatch Mode → Inbox as the master's surface.
