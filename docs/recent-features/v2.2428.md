# v2.2428 — digital twin write-fence + twin_runs (Phase E2, 2026-08-28)

`docs/DIGITAL_TWINS_PLAN.md` Phase E2, PipeTooling half. Migration
`20260828070000_digital_twin_write_fence.sql`:

- **`is_digital_twin()`** helper (STABLE, SECURITY DEFINER) — training-mode's
  `is_read_only()` sibling.
- **`apply_digital_twin_write_blocks()`**: restrictive `digital_twin_write_fence_*`
  policies on every RLS-enabled public table, `(NOT is_digital_twin()) OR (<allowance>)`.
  Allowances: `bids` (INSERT own `created_by`; UPDATE/DELETE created-or-assigned-estimator
  — **assignment is the grant**), any table with a `bid_id` column (owner-bid EXISTS —
  future bid-family tables inherit on rerun), `price_book_entries` via their bid-scoped
  version, `help_feedback` INSERT (the bug-report channel). Everything else FALSE for
  twins. Drop+recreate so reruns converge predicate changes.
- **`twin_runs`** ledger (twin_user_id, mission, notes, started/ended): service-role
  written (the twin-login mint logs here), dev-read; both read-only blocks applied
  (house rule).
- One-directional by design: real users hit `NOT is_digital_twin()` and pass — humans are
  never fenced and can always edit twin work.
- `ACCESS_CONTROL.md` gains the "Digital twins (account flag, not a role)" section.
- No client changes. Push order: normal (`supabase db push` after merge).
