# v2.2385 — Solver folds behind Solve ›; Apply to Bid Value retires; Counts header tidied (2026-08-27)

Three Wendi asks — the first two mockup-approved (five refinement rounds on
the fold), the Counts header directed from her annotated screenshot.

## Cover Letter: Apply to Bid Value sweep (`BidsCoverLetterTab.tsx`)

Mark sent has stamped `bids.bid_value` (per the Settings board-value rule)
along with the sent date since the bundle work — the manual pre-send sync was
dead weight, plus the chrome that managed it. Removed: the button, the
"✓ matches Bid Value" chip, the "✓ Applied successfully" flash, the "Current
Bid Value: $X" drift hint, `applyProposedAmountToBidValue` +
`applyingBidValue` + `bidValueAppliedSuccess`, and the `displayBidValueSynced`
derivation. The headline caption now ends "· Mark sent stamps this as the
bid's value". Settings board-value blurb and the cover-letter help guide
reworded. Kept: Mark sent's write, Custom amount (letter text, unrelated),
the board-value rule itself.

## Pricing: the solver folds (`BidsPricingTab.tsx`)

- `wbSolverOpen` device preference (`bidPricingSolverOpen_v1`, default
  folded). Folded, the strip is a scoreboard — stats, coverage chip, and a
  right-pinned cluster holding the blue **Solve ›** button (plus the
  ＋ Add price door on solo-unpriced bids, and the draft control when one is
  pending — folding can never hide unsaved work).
- Open, everything solver sits inside a **blue ring** (1.5px #3b82f6 +
  soft glow, `--bg-blue-tint` fill): **‹** fold button, Margin with the
  slider back **inline** (20–95, live-drag solving and the 2×/3×/4×/5×
  markup ticks kept), the typed % box, the "or total" unit, and Solve ▾.
  v2.2378's slider-behind-▾ popover is removed — the fold now does its
  space-saving job (`wbMarginSliderOpen`/`wbMarginPopRef` deleted).
- **Draft actions become one segmented control**: amber count chip
  ("N drafts", "· M off" in red when vetoes exist) · **Apply** · **Discard**,
  one shared height, wrapping as a unit, right-pinned even on its own wrapped
  line. The long caption ("saved only when you Apply · waits on this device"
  + the veto sentence) rides the control's hover. The v2.2373 restored-age
  chip renders beside it.
- Label+control pairs ("or total" + box) are single flex units — a wrap can
  never orphan a label from its field.
- `startWorkbenchTour` force-opens the solver so the tour can point at it.
  Tour step + help guide retold.

## Counts: header matches the other tabs (`BidsCountsTab.tsx`)

Old/New pills move up beside the bid title (Cover Letter's bordered pill
pattern — local `countsViewPillStyle` mirroring `studioPillStyle`), the
standalone pills row below the header is gone, **Import from /Tooling** moves
to the header's right end beside the ×, and the **Edit Bid** button retires
(the bid-number link in the title already opens the bid; `onEditBid` prop
dropped from the component and its call site). Desktop's `1fr auto 1fr` grid
becomes a space-between flex row; mobile stacks title+pills over a
right-aligned Import.
