# v2.2710 — Person Desk PR 3: Field, Paperwork, Records, Schedule, and the Person tab (2026-09-03)

Third Person Desk PR (train: v2.2701 spine + drawer, v2.2706 pay + lifecycle). Every row of the proposal's inventory is now reachable from the desk, and the desk has a page. No migration; no new permissions.

## Client only

- **`PersonDeskBody`** [`components/personDesk/PersonDeskBody.tsx`](../../src/components/personDesk/PersonDeskBody.tsx): header + the section registry + the lifecycle flows, extracted from the drawer so the page renders the same thing (two columns when wide via `repeat(auto-fit, minmax(340px, 1fr))`). The drawer is now only the shell. Registry order: Hours & approvals · Portal (subs) · Pay & schedule · Team & alerts · Field · Paperwork · Records · Schedule · Access & account.
- **Field** [`sections/PersonDeskFieldSection.tsx`](../../src/components/personDesk/sections/PersonDeskFieldSection.tsx): vehicles held (`vehicle_possessions` open, `vehicles` embed) with **To motor pool** and a **Hand off…** picker over the fleet — both through `handOffWrites` (ends the vehicle's open possession, inserts the new one, no odometer) — gate `canAccessVehicles`; housing occupancy (`housing_possessions` + `housing_units.address`) with **End occupancy** / **Assign…** — gate pay roles; licenses by pay name with 30-day expiry tones and `PersonLicenseHoursLogModal` — gate `canAccessLicenses`.
- **Paperwork** [`sections/PersonDeskPaperworkSection.tsx`](../../src/components/personDesk/sections/PersonDeskPaperworkSection.tsx) (gate `canAccessContracts`): documents by pay name OR roster id → [`lib/people/paperworkRollup.ts`](../../src/lib/people/paperworkRollup.ts) (+1 test; newest lineage version per lineage, states unsent / sent / signed / expiring / expired, ordered worst-first); per-document **clock-in nag** toggle (`dashboard_prompt_after_clock_in`); **packet** assignment via [`lib/people/materializePacket.ts`](../../src/lib/people/materializePacket.ts) — the Contracts tab's `materializePacketForPerson` lifted verbatim (assignment row + one unsent document per template document, filling signing bodies from the book). Send and Upload signed link to Contracts.
- **Records** [`sections/PersonDeskRecordsSection.tsx`](../../src/components/personDesk/sections/PersonDeskRecordsSection.tsx): HR file freshness (`derivePersonFileFreshness` over the summary row + entry timestamps) and pending `person_reports` (dev; non-devs see a locked "Kept by the dev" row — owner decision 4's existence line waits for the count RPC in PR 5); write-ups count + latest and `attendance_incidents` in the last 90 days (contracts roles); **Rate** → `/prospects?tab=team&stage=review&rate=<userId>`.
- **Schedule** [`sections/PersonDeskScheduleSection.tsx`](../../src/components/personDesk/sections/PersonDeskScheduleSection.tsx): `UserDayScheduleSection` rendered inline behind a "Show today's schedule" button (its own header and close fold the view back). Week / month stay on the header button until the modal retires.
- **People → Person tab** [`components/personDesk/PersonDeskPage.tsx`](../../src/components/personDesk/PersonDeskPage.tsx): `?tab=person&id=u:<users.id>|p:<people.id>`; rail = users ∪ unlinked roster rows, grouped by kind in the house order, with attention from [`lib/people/deskRailAttention.ts`](../../src/lib/people/deskRailAttention.ts) (+3 tests): sessions waiting (one `fetchAllPendingClockSessions` → the v2.2694 kernel), unsent documents and expiring / expired paperwork (`person_contract_documents`, `person_licenses` within 30 days), and "no roster row" for accounts (skipped for devs and primaries); a **Needs attention** group leads. Under 760px the rail and the desk take turns (‹ All people). Registered in `People.tsx` (`PeopleTab` union, the no-URL-gate list, a **Person** button after HR for `canOpenPersonDesk`, render gated the same way).
- `PeopleHrTab` accepts `?person=<people.id>` (the Records row's **Open file**).
- Verified live as dev: the Person tab rail (19 needing attention, red for expired paperwork), Michael A's page with all nine sections (fleet picker, a real housing occupancy, two documents, three packets, one attendance incident), and the drawer from Users still rendering. No writes made.

## Docs

- Help guide `open-a-persons-desk.md` (Person tab, Field, Paperwork, Records, Schedule); `PEOPLE_TABS_ARCHITECTURE.md` (PR 3 line, tab union); `ACCESS_CONTROL.md` (Person tab).
