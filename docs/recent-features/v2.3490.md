# v2.3490 — Submittals stage 4c: the won question (2026-09-16)

Stage 4's last PR ([`to-dos/submittals/README.md`](../../to-dos/submittals/README.md) → PR 4c). One migration (`20260916040000_bids_submittals_not_needed.sql`, two nullable columns on `bids`); no edge function.

## What shipped

- **The prompt** [`JobSubmittalsAfterCreatePrompt`](../../src/components/jobs/JobSubmittalsAfterCreatePrompt.tsx), chained third in `JobFormModalContext` after the contract and job-accounts questions (office roles and estimators). It reads the job's bid through `job_account_job_identity`, the bid's revisions, and the picks (`loadPicksForBid`), and asks only when the bid has something to build from and no revision yet: *Build Rev 1 from the picks* (Rev 1 with `job_ledger_id` set, then the Submittals tab) · *I have the vendor's PDF* (the same build, the tab opens where the PDF drops) · *Later* · *Not needed on this job* (`bids.submittals_not_needed_at/_by`). A bid that already carries a revision gets the job back-filled onto every revision with none (`backfillSubmittalJob`) and no question. No reviewer is asked for — they are not known yet (owner decision 8).
- **Kernel** [`wonQuestion.ts`](../../src/lib/submittals/wonQuestion.ts): `wonQuestionState` (ask · has-revision · not-needed · nothing) and the subline (*4 tags on the schedule · 3 picked lines*). Tests.
- **Shared client** [`firstRevisionClient.ts`](../../src/lib/submittals/firstRevisionClient.ts): `loadPicksForBid`, `createFirstRevisionFromPicks`, `backfillSubmittalJob`, `setSubmittalsNotNeeded` — the Submittals tab now reads its picks through the same loader.
- **The tab**: the empty state carries *Not needed on this job* and, once answered, *Not needed on this job · Sep 16 · undo*.
- **Needs You**: the "won N days, no submittal started" card skips bids marked not needed (`NudgeBid.notNeeded`).
- **Guide**: `turn-a-won-bid-into-a-job.md` gains "The submittals question".

## Verified

`wonQuestion` (3), the prompt's render smokes (3: ask + Build Rev 1 linked to the job and the tab opens; Not needed writes the bid; has-revision back-fills and asks nothing, no bid asks nothing), the nudge's not-needed case, the tab's smokes; typecheck, lint, theme. Live: make a job from a won bid on the dev server and answer the question — the live gate 4c script in the to-do.
