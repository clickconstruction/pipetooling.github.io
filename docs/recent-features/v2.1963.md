# v2.1963 — Partner Full ledger + week-nav buttons (2026-08-21)

Owner requests on the partner card ([`DashboardPartnerLedgerSection.tsx`](../../src/components/dashboard/DashboardPartnerLedgerSection.tsx), also rendered by the /partnerships lens):

- **Full ledger**: a "▸ Full ledger — every posting, all time" toggle under the week card fetches the ledger RPC at `p_weeks: 520` and renders the office-style journal table (Date/Posting/Amount/Balance, newest first) via new kernel fn `partnerStubsToJournal` in [`partnerWeeks.ts`](../../src/lib/partnerLedger/partnerWeeks.ts) (maps the RPC stub payload through `buildPartnerJournal`; 3 tests). Posted rows only — pending-charge detail stays office-side; charges reach the partner through statements. Replaces the "older weeks live in the office ledger — ask" note. Works identically in lens mode (`get_partner_ledger_as`).
- **Week arrows**: the 28px circles at the card's far edges become labeled "‹ Older / Newer ›" buttons grouped around the week title.

Migration `20260821180000_partner_ledger_full_history.sql`: `partner_ledger_payload`'s stub cap rises 26 → 520 weeks (body otherwise verbatim from 20260821150000); both resolvers inherit. Help guide `see-your-partner-ledger` updated.
