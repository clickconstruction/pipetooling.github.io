# v2.3674 — PayTooling's mark redrawn as a wide stub: the Paper / Pay pair separates at 16px (2026-09-21)

The punch-list pass of 2026-09-21 took the *Tooling family marks* to-do to **Do**, and the measurement its `next` line owed was run — from this repo, with the hand-off's studio (`to-dos/tooling-icons/measure.cjs`; Playwright is in `node_modules`, so no CountTooling checkout). Client only: one file under `public/tooling/`, the to-do's record, and PayTooling's own repo.

## What the studio found

Every candidate in `to-dos/tooling-icons/svg/` graded on the hand-off's six per-mark measures and its pairwise gate, against the three shipping marks. The adapter reproduced the hand-off's own numbers on the two marks it had measured (LienTooling refined safeZone 49, Plumbing refined vsShipping 0.694), so the scale is the same.

- Four of the eight new drawings clean: BidTooling, PaperTooling, PayTooling, SubTooling — **alone**.
- **PaperTooling ↔ PayTooling: a pair fail** — IoU 0.713, correlation 0.839 at 16px, closer than the pair the hand-off had caught by eye. Invisible on the board because the two never sit side by side there.
- SignTooling warns (decay 1); SyncTooling, GovTooling and ConnectTooling fail on 16px decay (the bars merge into the arrows; the columns fuse under the seal; six parts become one blob). **The owner's call: leave those three as shipped**; the fails are recorded in the to-do and `audit.json` as accepted debt.

## The redraw

The stub turned landscape — torn edge on the left, the lane line across — so its silhouette is a wide block, not a tall one. Two heights were measured: 240 tall (`pay-6`) is clean alone but in the warn band against the ClickTooling gear; **176 tall (`pay-7`) is clean on all six and flags no pair** with anything the family ships. The owner picked pay-7 on the sheet.

## Where it landed

- [`public/tooling/paytooling.svg`](../../public/tooling/paytooling.svg) — the sign-in page's *More apps* row and preview strip (v2.3626) read it; `toolingFamily.test.ts` still holds (house tile, two colours, no banned elements).
- `clickconstruction/paytooling.github.io` — the four-file set (`icons/favicon.svg`, `icons/icon.svg`, `icons/apple-touch-180.png` full-bleed, `favicon.ico` 16/32/48 as PNG-in-ICO) regenerated from the same SVG with Playwright, PR #5 in that repo; the page's head links and the header's `family-mark` image do not change.
- `to-dos/tooling-icons/`: `picks.json` (pay → pay-7, the note says why), `audit.json` + `verdicts.tsv` (the full pass, 59 candidates), `build-board.mjs` reads chips per candidate, `board.html` rebuilt, `README.md` carries the *Measured — 2026-09-21* table. The folder closes when PayTooling's PR is merged.

## Verified

`measure.cjs`: pay-7 pass on all six, no flagged pair; a contact sheet at 96 / 48 / 32 / 16 px on light and dark strips beside Paper's page and the old stub. `toolingFamily` and `releaseNotes` suites; `npm run check:todos`.
