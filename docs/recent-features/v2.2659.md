# v2.2659 — Email wording PR 3: digests adopt templates server-side (2026-09-02)

- New `supabase/functions/_shared/emailWordingServer.ts`: PostgREST fetch of the `email_templates` row (service key), `{{var}}` rendering (provided keys only; `{{default_subject}}` always available = the built-in subject), returns subject + optional intro (text + escaped `<br>` HTML). Fail-soft: any error → built-in wording.
- Adopted (subject + intro-above-the-data) in nine senders: `paid-job-email` (types `paid_job` + `ready_to_bill`), `money-waiting-email-dispatch`, `billed-report-email`, `payment-forecast-email-dispatch`, `crew-day-email-dispatch`, `weekly-money-email-dispatch` (var `{{week}}`), `weekly-movement-email-dispatch` (`{{week}}`), `schedule-day-email-dispatch` (`{{date}}`). **All nine need redeploys** (batched at stream end).
- Client: nine template types added to `settingsTemplates.ts` (defaults: subject `{{default_subject}}`, a one-line intro) + a "Digest Emails" card group in the Templates tab + catalog rows flip to editable.
- The `[TEST]` prefix and per-role detailed/summary variants are untouched — the prefix wraps the templated subject.
- Still hardcoded (documented in the catalog): the prose staff notifies (portal request, estimate accepted, bid room activity), RFQ/bid emails, GC statements, contract-for-signature (has per-send overrides). PR 4 = polish.
