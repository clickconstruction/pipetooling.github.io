# v2.2671 — Pending hours-approvals Needs You card (2026-09-03)

Second PR of the overhead-feed repair pair (with v2.2670's salary auto-approve): the Aug 2026 approvals stall (3 weeks, 152 sessions / 799h) was invisible outside People → Overhead's maintenance strip. Now a stale approvals queue surfaces on the Dashboard where the day starts.

## DB (migration `20260903020000` — fragment in docs/migrations/)

- `count_pending_clock_session_approvals()` (SECURITY DEFINER, STABLE): closed pending sessions (approved/rejected/revoked all NULL) → `(sessions, total_hours, people, oldest_work_date)`. The gate lives IN the function (`list_bulk_deletion_alerts` precedent): non-approval roles (`is_dev` / `is_pay_approved_master` / `is_assistant_of_pay_approved_master` / `is_assistant` all false) get the zero row, so the hook has no role logic. Counts all origins — `salary_schedule` rows drain via the v2.2670 cron, so with the 3-day age gate they can't trigger the card alone.

## Client

- [`hooks/usePendingHoursApprovalsNudge.ts`](../../src/hooks/usePendingHoursApprovalsNudge.ts) — RPC via the `(supabase as any).rpc` cast precedent (not in generated types yet); computes `oldestAgeDays` against `denverCalendarDayKey(Date.now())`; focus-refetch; fails soft to null. Exports `HOURS_APPROVALS_MIN_AGE_DAYS = 3`.
- [`lib/dashboardNeedsYou.ts`](../../src/lib/dashboardNeedsYou.ts) — new `hours-approvals` item (amber, tier 50, kicker "Time approvals"): shows only when `sessions > 0` AND `oldestAgeDays >= 3` — a normal same-week queue never nags; what it catches is the stall. Figure = session count; action → `/people?tab=hours`. +2 kernel tests (gating matrix, singular copy).
- Wired in `DashboardPinnedQuickRow` (enabled for the office set — dev/master/assistant-like; the RPC re-gates pay approval). `QuickfillNeedsYouSection` passes disabled (people-desk work, not billing).

## Docs

- Help guide `needs-you-card.md` gains the item in "Where the actions go".

Apply order: push the migration any time — until it exists the hook's RPC call fails soft and the card stays quiet.
