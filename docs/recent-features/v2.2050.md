# v2.2050 — Goals strip: stage numbers in the zones, "now:" line removed (2026-08-21)

Owner iteration on the v2.2027 segmented Goals bar (Checklist → Review):

- **Zone numbers**: each segment renders its stage number centered (index+1
  in sort_index order — identical to the Roadmap badges and the stage
  ledger's numbering). Bar height 8 → 13px to fit the digits; complete
  zones show white-on-green, current `--text-700`, locked `--text-faint`.
  Numbers skip for roadmaps over 40 stages (slivers would clip); tooltips
  now lead with the number too.
- **"now: …" line removed** (with its "· N assigned tasks open" tail and the
  all-complete 🎉 variant): redundant since the segmented bar + tap-open
  stage ledger carry the same story. `goalsStripNowSummary` import dropped
  from Checklist.tsx (kernel + tests remain — the function is still exported).

Verified live on Farm 1: 24 numbered zones, stage 20 green with white "20",
prose line gone.
