# v2.2077 — Floaty's daily debut (2026-08-22)

Owner refinement on v2.2074: the first targeted-surface open of each **company day** is a guaranteed visit; later opens keep the 1-in-50 dice. New pure `rollEggAppearance(lastDebutYmd, todayYmd, rand)` in [`easterEggsConfig.ts`](../../src/lib/easterEggsConfig.ts) (+3 tests: debut on day change, dice after, no debut on empty clock). [`EasterEggHost`](../../src/components/FloatingEasterEgg.tsx) stores the debut day in `localStorage` (`easter-egg-debut:{egg}:{userId}`, day = `calendarYmdInAppTzFromIso` — the Chicago wall date per `TIME_AND_ZONES.md`), written only on a debut; private-mode storage failures degrade to dice-only. Settings card copy → "daily debut + 1-in-50 · plays 7s". Verified live end-to-end by temporarily targeting the dev user: first Followup open after a fresh load wrote today's marker and mounted the visit; leaving and re-entering produced no guaranteed appearance; test targeting and marker removed after (config back to Wendi-only). Client-only — no migration.
