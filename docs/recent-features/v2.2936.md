# v2.2936 — Auto-shadow: `next_shadow` dispatcher + coverage, and the learning plan (2026-09-06)

First build from **`docs/twins/LEARNING_PLAN.md`** (new, this PR — the program's forward build list after the 2026-09-05 regression cycle proved the audit loop works: digested lessons land ±8%, undigested rules miss 30–400%). The two scarce resources are the estimator's hours and her ~130-reference corpus; auto-shadow is lever 2 — she generates labeled data every day just by bidding, and a shadow captures it at zero cost to her.

- **`next_shadow(days?)`** (twin-mcp v1.3.7): the shadow dispatcher, mirroring `next_backtest` — claims the next live bid needing a shadow (human-requested first by oldest ask, no age limit; then oldest eligible in the lookback, default 30 days). The `twin_shadow_runs` row is the claim; a same-instant duplicate deletes itself in favour of the earliest run, so parallel agents never share a bid. `axis` seeds from `bids.backtest_axis` when classified. `done: true` = the live board is covered. Shell creation extracted to `createShadowShell`, shared with `open_shadow` (behavior unchanged there).
- **`get_shadow_queue` coverage block**: windowless `{ live_with_plans, shadowed, unshadowed }` — the number the program drives to 100%; a Scoreboard pill for it rides with the scheduled-batch follow-up (plan item 3).
- **`docs/twins/LEARNING_PLAN.md`**: the full 13-item build list — standing-rulings queue, audit triage by doctrine-at-stake, holdout set (owner decision), doctrine-promotion regression gate, record repair, loss-categorization nudge, STG-5 hygiene on human bids, and the rest — each tied to the hours-or-corpus constraint it relieves. `HANDOFF.md` points at it.

Edge + docs only (deploy `twin-mcp` after merge; `briefs.ts` unchanged — neither doc is bundled).
