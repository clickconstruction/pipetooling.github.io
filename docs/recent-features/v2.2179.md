# v2.2179 — Primaries can reach the partner statement (2026-08-23)

Prerequisite for moving Bryan (our partner) from `estimator` to `primary`: `/my-statement`
joins `PRIMARY_PATHS` in [`Layout.tsx`](../../src/components/Layout.tsx) +
[`layoutRouteAccess.ts`](../../src/lib/layoutRouteAccess.ts) (it was sub-like + estimator
only, v2.2157/2165), the partner help guide lists `primary`, `ACCESS_CONTROL.md` row updated.
`useIsPartner` already treats primary as a partner-capable role. Client-only.
