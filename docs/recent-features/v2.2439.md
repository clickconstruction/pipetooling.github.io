# v2.2439 — Twin MCP: two-app mint_session (2026-08-28)

CT bridge Phase 4 (the companion). `twin-mcp`'s `mint_session` gains
`app: 'pipetooling' | 'counttooling'` (default pipetooling):

- The CT path calls CountTooling's `twin-login` with **CT's twin secret held
  server-side** (`CT_TWIN_LOGIN_URL` + `COUNTTOOLING_TWIN_LOGIN_SECRET` function
  secrets) and the twin's CT fleet email (`@twins.counttooling.local` domain swap) —
  partners keep exactly ONE credential per twin. CT per-twin-credential parity stays
  deliberately deferred (noted, not built).
- PT's twin-login guards don't run on the CT path, so the essentials are re-applied in
  the MCP server: twin eligibility was already resolved per-call, and the **6/min rate
  limit** is enforced against the shared `twin_runs` ledger before minting.
- CT mints log to PT's `twin_runs` (`mint via=token:<id> app=counttooling redirect=…`) —
  the fleet console's runs feed shows them like any other sign-in.
- `TWIN_HARNESS.md` notes MCP clients never need CT's raw twin-login; briefs bundle
  regenerated.
- Deploy after merge: set the two secrets, then `supabase functions deploy twin-mcp`.
