# v2.2899 — Banking papercut batch B13: tab captions, Card Review kind filter, honest "reconciliation" wording, nickname nudge (2026-09-05)

Journey map Phase 4, **Tier-3 B13** (`JOURNEY_MAP.md` → "Banking page (after #3(a))"; findings J33-F4 / J33-F5 / J33-adj-2 / J33-N6 in `docs/journeys/banking-reconcile.md`). Builds on #3a / #3c / #13 / #27 (all merged 2026-09-05). No migration, no edge function.

| # | Item | Status |
|---|---|---|
| 1 | Caption row under the tab strip | **Done.** `MERCURY_TAB_CAPTIONS` in [`Banking.tsx`](../../src/pages/Banking.tsx): "User Sort — who spent it · Drag Sort — what kind · Accounting — rules & approvals · Reviews — read-only · Reconciliation — against bank statements, read-only · Jobs are sorted in Job Parts Tally; labels live here." Active tab's caption bolded (Category Review bolds "Reviews"). Mercury product only. |
| 2 | Retire the `user_review` key (J33-F4) | **Done.** `MercuryBankingTab` member and every `?tab=` writer now say `card_review`; element ids `banking-tab-card-review` / `banking-panel-mercury-card-review`. One parser, `mercuryTabFromParam`, replaced three hand-written ladders (`parseBankingView` ×2, `setBankingProduct`, the staff canonicalization effect) and reads `user_review` as an alias; a new effect rewrites a legacy address bar once (`replace: true`) so bookmarks and `banking:<tab>` activity keys converge. Device prefs (`hide empty`, `time window`, `chart view`) moved to `banking_mercury_card_review_*_v1` with `readMigratedStorageItem` copying any value found under the old `user_review` key on first read and deleting it. Not touched: the `user_review_rows` RPC (a DB object; renaming it is a migration), the Dashboard clock-strip "User Review" modal and its `user_review_modal_range_v1` / `user_review_tx_sort_v1` keys (a different feature), `BankingMercuryUserReview*` component filenames. |
| 3 | Unnamed-account nickname nudge (J33-F5) | **Done — dev only, data untouched.** Kernel [`bankingAccountNicknameNudge.ts`](../../src/lib/bankingAccountNicknameNudge.ts) (`unnamedMercuryAccountIds`, `accountNicknameNudgeText`; 6 tests). Amber `role="status"` line under the strip lists the short ids of accounts in the loaded rows with no `mercury_account_nicknames` row + **Name accounts…** → `BankingAccountNicknamesModal`. Targets dev because the table is dev-write only (RLS); the modal also lets dev fix the "Recivables" spelling — the PR deliberately does not edit the data. |
| 4 | Card Review kind filter (J33-adj-2) | **Done.** Kernel [`bankingCardReviewPrefs.ts`](../../src/lib/bankingCardReviewPrefs.ts): `buildCardReviewKindOptions` (All kinds · Card charges only when a card kind is present · each distinct kind by label), `filterCardReviewRowsByKind`, `normalizeCardReviewKindFilter` (a stored kind that left the window falls back to All kinds so the pivot never silently empties); 14 tests incl. the storage migration. Applied to the RPC rows *before* `buildUserReviewPivot` and before the attribution/name maps; persisted under `banking_mercury_card_review_kind_filter_v1`. **Default is All kinds** — nobody's totals move without a click. Tab caption now says the Unassigned row includes transfers/payouts until Kind is narrowed. |
| 5 | "reconciliation" names two things (J33-N6) | **Done.** [`BankingMercuryReconciliationTab.tsx`](../../src/components/banking/BankingMercuryReconciliationTab.tsx): h2 "Reconciliation against bank statements"; button "Check against bank statements" / "Checking statements…" / "Check again"; stamp "checked against bank statements {time} · not saved"; blurb adds "Read-only: it writes nothing and the result is gone when you leave the tab. This is not the 30-minute sync that backfills missed bank transactions"; summary "✓ All N accounts match their bank statements … — every statement transaction is in the books" (states the N4 scope); pills "Matches statement" / "All match statements". Tab name in the strip unchanged (the caption carries the gloss). |
| — | `/tally` header 2-day gloss (J33-F7) | **Skipped** — rides #16 (in flight). |
| — | Accounting auto-approve switch / `label-approvals` Needs-You key | **Not touched** (#27). |
| — | Docs drift J33-N5 | Tier 4; only the lines this PR's renames touched were fixed (`ACCESS_CONTROL.md` Card Review gloss, `BILLING_FLOWS.md` tab list gains `visuals` + alias, `GLOSSARY.md` Card Review entry). |

## Docs / help

- [`docs/BANKING_TABS_ARCHITECTURE.md`](../BANKING_TABS_ARCHITECTURE.md): tab type + alias note, Card Review section renamed to `card_review` with kind-filter + prefs-migration bullets, new "Strip furniture" paragraph (captions + nudge).
- Help guide [`label-bank-transfers.md`](../../src/content/help/label-bank-transfers.md): new "Finding your way around the Banking tabs" section (caption row, Card Review Kind filter, what Reconciliation does and does not do, the dev nickname nudge).
- Release note `src/content/releaseNotes/v2.2899.ts`.

## Gates

`npm run typecheck` · `npm run lint` · `npm test` · `node scripts/theme-tokenize.mjs --check src` — all green before push.
