# v2.3598 — Labor refresh PR 6c: the Old HOURS grid retires; the to-do closes (2026-09-18)

Last of three for PR 6 of the Labor refresh (`to-dos/bids-labor-refresh/`, closed by this PR; the drawing was its `before-after-pr6.html` §05, the Takeoffs precedent v2.3588). Client only.

## What shipped

- **Gone** from [`BidsLaborTab`](../../src/components/bids/BidsLaborTab.tsx): the `laborView` state and its two pills, the Old branch of the HOURS section (the `<select>`, *Apply matching Labor Hours* with `applyLaborBookHoursToEstimate` — quirk #6's primary-name-only matching went with it — the grid, the add-missing-fixture button and modal with `openAddMissingFixtureModal` / `saveMissingFixtureToLaborBook`), `handleLaborBookVersionChange`, `saveLaborRows`, and the three hooks' `laborView === 'new'` gates. [`LaborViewPills.tsx`](../../src/components/bids/) and [`lib/bids/laborView.ts`](../../src/lib/bids/) (+ test) are deleted; the per-device key `bids_labor_view_v1` is abandoned in place — a device that had picked Old lands on the one view, never asked again.
- **Kept**, under the grid as before: the rate row (`laborRateInputRef`, the view's *set a labor rate ↓* still lands on it), the sub-sheet prints, the Labor total, Vehicle Travel, Lodging and Meals, Direct Costs, the Labor book panel. The Breakdown jump (v2.2400) lands on the view's rows (same `laborRowDomId`).
- Guide [`estimate-labor-hours-on-a-bid`](../../src/content/help/estimate-labor-hours-on-a-bid.md) retitled and reframed (no pills; § Under the grid replaces § What is still Old-only); the architecture map marks Region L3 retired and L3b the HOURS section; the owner-decisions list loses its Labor row; the folder `to-dos/bids-labor-refresh/` is deleted and the board regenerated.

## Verified

- `npm run typecheck`, lint, the bids / hooks / content suites; `check:timezone`, the theme check.
- Live on prod (dev server, BP403): no pills beside Print, the view's head, queue and grid render, the rate row and Labor book panel sit under it.

The train, complete: v2.3276 (New view) · v2.3291 (data) · v2.3292 (kernels) · v2.3294–3295 (rate, direct costs) · v2.3307 (calibration) · v2.3310 (New default) · v2.3596 (the fold) · v2.3597 (one book, provenance, who may recalibrate) · v2.3598 (Old retires).
