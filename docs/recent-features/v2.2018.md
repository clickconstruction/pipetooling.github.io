# v2.2018 — Trade pills: Timeline + partner "Your jobs" (2026-08-21)

The two follow-ups deferred from v2.2015 (Job review trade pills): the same `buildServiceTypeTradePill` treatment on the remaining partnership job-number surfaces.

**Timeline** (client-only): [`partnerTimeline.ts`](../../src/lib/partnerLedger/partnerTimeline.ts) — `TimelineEventInputs.confirmedJobs` gains optional `service_type_name`; job rows carry a new `trade` field and drop the "Job #" prefix when a trade is present (label becomes "789 confirmed as partner-majority", the pill carries the rest); no type → the old sentence stands whole (+1 test). [`PartnershipTimelineTab.tsx`](../../src/components/partnerships/PartnershipTimelineTab.tsx) embeds `service_types(name)` in its confirmed-jobs query and renders the pill before the label.

**Partner "Your jobs" card** (migration `20260821235000_partner_jobs_service_type.sql`): `partner_jobs_payload` rows gain `service_type_name` — see the migration fragment. [`DashboardPartnerJobsSection.tsx`](../../src/components/dashboard/DashboardPartnerJobsSection.tsx) parses it and leads each row with the pill (bold plain number, `#` dropped); fail-soft to `#789` until the migration is pushed, and the View-as lens shows the same because it delegates to the same payload.

Deploy order: client first is safe (fail-soft), then `supabase db push` after merge.
