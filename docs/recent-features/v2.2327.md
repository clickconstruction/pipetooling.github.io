# v2.2327 — Cost Timeline fits the container (2026-08-26)

Owner ask (screenshot review of the Bill tab): stop the chart's sideways
scrolling and center the legend.

- `JobChargesTimelineChartView` (shared by the Bill/Detail modals' Parts cost
  section and Job Summary expanded rows): the `overflowX: auto` wrapper and
  the `max(520, buckets × 56)px` min-width are gone — the chart always fits
  the container width (recharts compresses the day buckets) and grows from
  260 to 320px tall for readability.
- The Red/Green legend line and the folded ⓘ marker key are centered.
