# v2.2277 — Dispatch Customers: title moves onto the sort row (2026-08-25)

Owner (screenshot with the centered title and the sort row's dead space circled): "move [the title] over … to the right in line with the search." Mockup with two placements approved as Option A (title right-aligned on the Sort By row): https://claude.ai/code/artifact/c4315333-4aee-4288-927c-eef59e7ea046

- [`DispatchModeCustomers.tsx`](../../src/components/dispatchMode/DispatchModeCustomers.tsx): the centered `<h1>` row is gone; the `Sort By: Name | Interacted` group and the `<h1>Customers</h1>` (`margin-left: auto`, `nowrap`) now share one flex row, so the search box stays full-width and the list starts a row higher. `role="group"` stays on the sort cluster.
- Sibling dispatch screens (Inbox, PO Generator) keep their centered titles — they have no control row to share; align them later if wanted.

Client-only.
