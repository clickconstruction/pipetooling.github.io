# v2.2023 — Portal short origin flips to my.clickplumbing.com (2026-08-21)

Stage 3 of the portal custom-links train (backend v2.2008, modal v2.2014).
One-line flip of `PORTAL_SHORT_ORIGIN` in `src/lib/portal/portalShortOrigin.ts`
from `https://pipetooling.com/p/` to `https://my.clickplumbing.com/`, now that
the DNS is live (set up 2026-08-21 in the Cloudflare clickplumbing.com zone):

- Proxied A record `my` → `192.0.2.1` (dummy target; Cloudflare answers).
- Redirect rule **"portal short links"**: wildcard
  `https://my.clickplumbing.com/*` → 301
  `https://pipetooling.com/p/${1}`, preserve query string.

Verified live: `my.clickplumbing.com/knight-contracting` 301s to
`pipetooling.com/p/knight-contracting` and renders the merged statement.
The modal's hero prefix and Copy link now use the short domain; `/p/` and
`/portal?t=` URLs keep working unchanged (the Direct link row still uses
`window.location.origin`).
