# v2.2000 — Partnership ledger notes (2026-08-21)

Owner-approved working mockup ("Ledger Notes" artifact): dated annotation memos in the partnership ledger — no amount, no balance impact, the chain flows past them.

**DB** (migration `20260821210000_partnership_ledger_notes.sql`): new `partnership_ledger_notes` (partnership_id FK cascade, note_date, memo, partner_visible default false, audit columns; dev-only RLS; both read-only sweeps). Same migration re-creates `partner_ledger_payload` with a `notes` array (partner_visible only) — visibility is enforced server-side, RPC-first.

**Kernel** ([`partnerLedgerJournal.ts`](../../src/lib/partnerLedger/partnerLedgerJournal.ts)): `LedgerNote`, `NoteJournalRow` (kind `note`, null amount/balance), `mergeNotesIntoDisplay` (date interleave; same-date notes render above that date's rows; 2 tests). [`partnerWeeks.ts`](../../src/lib/partnerLedger/partnerWeeks.ts): `parsePartnerLedgerNotes`.

**Office UI** ([`PartnershipLedgerTab.tsx`](../../src/components/partnerships/PartnershipLedgerTab.tsx), now takes `partnershipId`): "+ note" beside the headline opens a composer (date defaults to the company today, memo, "Partner can see"); saved notes interleave as italic rows with a grey right-edge triangle handle and a "partner sees" chip; click to edit/delete; changing the date re-sorts. **Drag-to-date**: dragging the triangle highlights row tops; dropping loads that row's date into the open editor (nothing commits until Save). Fail-soft pre-push (button hidden).

**Partner side** ([`DashboardPartnerLedgerSection.tsx`](../../src/components/dashboard/DashboardPartnerLedgerSection.tsx)): partner-visible notes render as italic rows in the Full ledger. Help guide updated.
