# v2.2307 — Jobs Pipeline: outstanding count on the Follow-ups button (2026-08-25)

Owner: "on the Followups button I would like to see a count of the number of outstanding to followup." Mockup approved: https://claude.ai/code/artifact/7d126652-06bc-4cce-aac3-e3aad8f50312

- **Hook** [`useJobFollowupQueueCount`](../../src/hooks/useJobFollowupQueueCount.ts): same math as the deck — `fetchJobFollowupCandidates` (shared 5-min cache, never `force`) + reviews + settings → `computeJobFollowupQueue(...).length`. Null (no badge) until loaded or on error.
- **Button** ([`JobsStagesTab.tsx`](../../src/components/jobs/JobsStagesTab.tsx)): amber pill (`#f59e0b` on dark-amber text) inside the existing Follow-ups button when count > 0; hidden at zero. `aria-label` carries the count. The deck's `onClose` bumps a refresh key so the badge recounts after a review session.

Client-only.
