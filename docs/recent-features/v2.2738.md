# v2.2738 — Pipeline: the "Get contracts signed" card, and Upload signed contract in the Contract modal's header (2026-09-03)

Owner-approved mockup (artifact 2ad0d625): a prompt on arrival at Jobs → Pipeline showing how many live jobs (every stage but Paid) have an agreement and how many don't, per stage, with the door to go get them; and the paper upload moved out of the send row.

- **Kernel** `lib/jobs/jobContractNudge.ts`: `summarizeContractNudge` gains `byStage` (`waiting · working · ready_to_bill · billed · collections`, Collections = billed + `collections_at`) with `total / missing / revenueMissing`, plus `liveTotal`; `contractStageOf`. `useJobContractsNudge` selects `collections_at` and returns both (the Dashboard's Needs You items are unchanged).
- **`PipelineContractCoverageCard`** — first row of Today's Money Opportunities (Pipeline only, office roles): *✍ Get contracts signed — 58 live jobs without, $412k of work*, the *61 of 119* line, one chip per stage (*Working 15*; a stage with no gap reads *Ready to Bill ✓* and is not a button), **Start the sweep →**. Tapping a stage chip sets the board's *No contract* filter and jumps to that section (`focusStagesSection`). When the gap is zero the card collapses to one green line; when there are no live jobs it renders nothing. Counts come from the company-wide three-query summary, not the lazily loaded rows. `PipelineMoneyOpportunities` / `PipelineOverview` grow optional pass-through props; Quickfill's copy of the block is untouched.
- **`ResponsiveModalShell.headerAction`** — a slot in the sticky title bar before ×. The **Contract modal** renders *⤴ Upload signed contract* there (removed from the footer's send row); it opens a small sheet (signed by · signed on · scan/photo · Record) over the modal instead of unfolding above History.
- Help guide: "Find the jobs without one" describes the card; "Already signed on paper?" points at the header button.
