# v2.3059 — Money rollups read the sheet → job link (2026-09-07)

Owner: "Let's convert them." First PR of the conversion train from [`to-dos/sub-sheet-job-link-followups.md`](../../to-dos/sub-sheet-job-link-followups.md) — sections A (five money RPCs) and E (the one number-only writer). Migration [`20260907130000_money_rollups_read_sheet_link.sql`](../migrations/20260907130000_money_rollups_read_sheet_link.md): each function is its latest body with the join swapped to `people_labor_jobs.job_ledger_id`. Behaviour change worth knowing: `get_weekly_money_movement_payload` was the one site that matched case-sensitively and picked the *oldest* job for a reused number; it now agrees with the rest. SQL only — no client change. **Deploy**: merge → `supabase db push`.
