# v2.3032 — twin-mcp v1.3.10: discipline fence on every claim door + `void_shadow` (2026-09-07)

The first scheduled-era shadow run (b480) claimed **b378, an Electrical-division bid**, locked a $907,590 plumbing estimate against it, and — finding the electrical robot book empty — mirrored 34 plumbing entries into it. The work was honest; the harness handed it the wrong bid. Edge only (deploy `twin-mcp` after merge); the bad run was voided and the book pollution deleted by the operator the same day.

- **Discipline fence**: `plumbingServiceTypeId()` (looked up by name, cached) + `disciplineRefusal()`; applied in `get_shadow_queue` (queries + coverage), `next_shadow`, `open_shadow`, and `openBacktestShell` (covers `open_backtest`/`next_backtest`). Non-plumbing references refuse with "b378 is an Electrical bid — twin-estimator-1 estimates PLUMBING only".
- **`void_shadow(bid, reason)`**: own shells only; scored runs stay scored; sets `twin_shadow_runs.status='void'` (never selected by `score_shadows`) and stamps `[shadow VOID] <reason>`.
- LEARNING_PLAN.md lever 2 gains the lesson; coverage numbers are now plumbing-only.
