# v2.2201 — Outstanding-by-person rows: two lines on phones (2026-08-23)

Owner-approved mock-up (artifact 6afba6c1, built as designed). `OutstandingByPersonSortableRow` in [`Checklist.tsx`](../../src/pages/Checklist.tsx) restructured into classed regions (`.obp-row/grip/check/main/title/meta/sp/expanded`) with one colocated stylesheet in the list component:

- **Desktop (≥720px)**: unchanged single line — grip · ✓ · title · meta cluster (the roadmap chip moved from inline-after-title into the meta cluster next to age/notes; the 🗑 joined the cluster).
- **Phone (<720px)**: grip hidden (`display:none`); ✓ becomes a boxed 34×34 target aligned to the title's first line; title takes `flex: 1 1 100%`; the meta cluster wraps to its own line with a spacer pushing 🗑 to the right edge; the expanded activity panel loses its 1.9rem indent.
- "+ Add task for <name>" restyled as a dashed ghost button.
- Same handlers throughout (✓ complete, 🗑 delete with stopPropagation inside the toggle area, tap-to-expand, notes/age chips).

Verified on the dev build: 375px — grip gone, ✓ 34×34 boxed, title 241px of a 285px row (~85%), meta on line two, four tasks render in 3-line blocks (was 4+); 1280px — grip visible, title+meta one line, ✓ unboxed, expand toggles. Client-only.
