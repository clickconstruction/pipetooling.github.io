# v2.2038 — Customer statement: HVAC tagline, click-away line, days since billed (2026-08-21)

Owner requests from portal-statement screenshots. The "Click Plumbing and Electrical" *company name* in contracts/lien releases/footers is untouched ([`coverLetter.ts`](../../src/lib/bidDocuments/coverLetter.ts)'s never-changed invariant):

- [`CustomerPortal.tsx`](../../src/pages/CustomerPortal.tsx) letterhead subtitle: "Plumbing & Electrical" → "Plumbing, Electrical, and HVAC"; [`DashboardPartnerLedgerSection.tsx`](../../src/components/dashboard/DashboardPartnerLedgerSection.tsx) partner-statement print header matches ("CLICK PLUMBING, ELECTRICAL, AND HVAC").
- [`portalCompany.ts`](../../supabase/functions/_shared/portalCompany.ts) `cityLine` (letterhead right-side line, wire key unchanged): "San Antonio, Texas" → "Your reliable team is just a click away" — **requires `supabase functions deploy customer-portal`** after merge.
- **Days since billed** on statement rows: new kernel `portalDaysSinceBilled` in [`portalPayload.ts`](../../src/lib/portal/portalPayload.ts) (+1 test) renders a quiet sub-line under each Billed date — "today" / "yesterday" / "N days ago" in the faint ink, warming to copper + semibold at 30 days. Future/missing dates stay silent.

Client + one edge-function redeploy — no migration.
