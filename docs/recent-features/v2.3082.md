# v2.3082 — Takeoffs view chooser: a Combined bid asks Old / One at a time / Sheet on open (2026-09-07)

Owner's design, iterated on a clickable mock-up the same evening: "a box in the middle of the screen of the three names and a preview of what they look like, side by side, that they can click on — whenever they go back to that page." Final calls from the mock: no recommended card, no descriptions or "best for" on Old, no "open this way from now on" and no "not now" — a pick is the only way past the box and it opens that view straight away. Client only — no migration, no edge function.

## What shipped

- **[`TakeoffViewChooser.tsx`](../../src/components/bids/TakeoffViewChooser.tsx)** (new): a fixed, centered `role="dialog"` over the tab. Eyebrow (left) `Bids → Takeoffs · BP398 ZZ Test · 3 fixtures, 0 costed`; centered title *How do you want to cost this takeoff?* and *You keep the fixture you're on when you switch.*; three equal cards in `TAKEOFF_VIEWS` order — a drawn CSS preview of each layout with its hotkey in the corner, the name centered beneath (Old is preview + name only; One at a time and Sheet carry one sentence). Previews are drawn in theme tokens (`--border`, `--border-strong`, `--bg-blue-tint`, the action blue), so they never go stale and pass the theme check.
- **Keys 1 · 2 · 3** pick a card (`viewForChooserKey` in [`takeoffView.ts`](../../src/lib/bids/takeoffView.ts), +1 test); the first card takes focus on open so One at a time's own Enter / arrow shortcuts behind the box stay quiet (`focusShortcutApplies` yields inside a dialog).
- **[`BidsTakeoffTab.tsx`](../../src/components/bids/BidsTakeoffTab.tsx)**: `takeoffChooserOpen` flips on every change of the selected bid id while the bid is Combined; a By Stage bid never sees it. A pick calls `switchTakeoffView`, so the device remembers the pick for the pills and the v2.2998 hop still lands the fixture you were on.
- Smokes: the chooser appears for a Combined bid and a click on **Sheet** opens the cost rail and stores `new2`; a By Stage bid never shows it. Guides `cost-a-takeoff-one-fixture-at-a-time` and `see-what-pricing-sees-on-takeoffs` open with the box; `BIDS_SYSTEM.md`, the refresh plan and the retire-Old to-do note that the device default no longer decides what opens.

Deliberately not built (owner's calls on the mock): a remembered "always open this way", a dismiss / Escape, a recommended card, live-rendered previews.

## Verify

Bids → Takeoffs → B398: the box appears over the tab; press **3** → Sheet opens, box gone, pill on Sheet; close the bid and reopen it → the box again; click **One at a time** → opens on the fixture you last touched. B403 (By Stage): no box; it opens as before (Old, or the By Stage notice if the device last picked a new view).
