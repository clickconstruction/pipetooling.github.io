# v2.2316 — Roadmap "needs a name" reworded to "needs a person" (2026-08-25)

Owner confusion report: "56 roadmap tasks need a name" read as "56 tasks are
missing titles" — the count is open unlocked tasks with no assignee
(`assigneeIds.length === 0`), so the copy now says what it means. Wording only;
no logic, identifiers, or thresholds changed (`needsName`/`openNeedsName` and
`DashboardRoadmapNeedsNameBanner` keep their internal names).

- **`DashboardRoadmapNeedsNameBanner`**: banner text ("Farm 1 · 56 roadmap
  tasks need a person") and the group aria-label.
- **`ChecklistRoadmapNextUpPanel`**: the ⚡ Next up lane title "Needs a name"
  → "Needs a person".
- Comment/doc sweep for the old phrase: `dashboardRoadmapNudge.ts`,
  `roadmapNextUp.ts`, `roadmapVisibility.ts`, `DashboardPinnedQuickRow.tsx`,
  help guides `see-what-to-do-next-on-a-roadmap` +
  `staff-and-discuss-roadmap-tasks`, `docs/GLOSSARY.md`. Frozen per-version
  release notes / docs fragments (v2.2129, v2.2138) untouched.
