# v2.3512 — What customers see: the supply house and the law firm, from the sample (2026-09-16)

PR 6 of the train in `to-dos/what-customers-see-journeys/README.md` — the last *Next release* cards go live. Client + five edge functions; no migration. **Deploy after merge:** `get-rfq-quote-page`, `send-rfq-email`, `legal-portal`, `legal-notify-dispatch`, `submit-legal-portal`, plus the other importers of `_shared/customerSampleFixtures.ts` (`customer-portal`, `sub-portal`, `get-estimate-for-customer`, `get-bid-proposal-room`, `get-contract-for-signer`, `get-job-contract`, `get-submittal-room`).

## What shipped

- **Two more shared builders**, the senders' wording moved verbatim and re-exported for the browser: [`_shared/rfqEmail.ts`](../../supabase/functions/_shared/rfqEmail.ts) (`buildRfqEmail`; `send-rfq-email` now calls it with its origin) and [`_shared/legalEmails.ts`](../../supabase/functions/_shared/legalEmails.ts) (`buildLegalConfirmEmail`, `buildLegalNowEmail`, `buildLegalDigestEmail`, the email frame, and the confirm / unsubscribe page bodies; `legal-notify-dispatch` and `submit-legal-portal` now call them). The job-account email already had a client composer (`composeJobAccountEmail`), used as is.
- **Sample fixtures** — `SAMPLE_HOUSE`, `SAMPLE_RFQ_LINES`, `SAMPLE_FIRM`; `sampleRfqQuotePageResponse` (`sample` open, `sample-done` with the house's own last prices on offer, the page's Rung-C door) and `sampleLegalPortalResponse` (the firm, the particulars, the Notifications page with two recipients, **no matters** — a real matter never reaches this frame; the full-matter sample is left open). `get-rfq-quote-page` and `legal-portal` answer the sample tokens before any read; `legal-notify-dispatch` serves `?confirm=sample` and `?unsubscribe=sample` as the two plain pages for Ann Sample without touching a row.
- **`SupplyHouseQuotePage`** and **`LegalPortal`** honour `sampleStateFromToken`: the Sample banner; sending a quote or acting on the portal stays on the page and saves nothing.
- **Nine steps go live**: Quote request email, Quote page, Submitted, Job account email; Confirm email, Confirmed page (a `page` step with `absolute: true` — the frame loads the function's URL), Firm portal, Now email, Digest email.

The count line now reads *39 steps across 5 audiences · 32 rendered live · 5 open as the PDF · 2 sent by another system* — no Next release cards.

## Verified

Typecheck, lint, build; sample-email (8), journeys, registry, person and paper tests. Live before deploy: the five emails render with their subjects. **The four pages need the functions deployed** — verified after the deploy: `/q/sample` shows the banner and five lines; sending moves to the thank-you; `/q/sample-done` offers last prices; `/legal?t=sample` shows the firm's empty portal and its Notifications page; the confirmed page renders in the frame.

## Left open

A full sample matter on the firm's portal; PR 9 (the health row).
