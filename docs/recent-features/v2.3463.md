# v2.3463 — Pipeline: "nobody clocked in" prints as "No hours" (2026-09-15)

Client only; no migration, no edge function. Owner's wording: the printed line under the Progress & payment bar reads **`No hours · 100% Sep 11`** where it read `nobody clocked in · 100% Sep 11`.

## What changed

- [`progressPaymentCell.ts`](../../src/lib/jobs/progressPaymentCell.ts): `crewClause(…, { names: false })` — the printed variant — returns `no hours` for a null crew and for a crew with no clock-ins and no sheet; `capFirst` makes it `No hours` at the front of the line. The `names: true` variant (`words.full`, the tooltip and the bar's accessible name) keeps `nobody clocked in`.

## Verified

Kernel suite and the bar render smoke updated and green; typecheck, lint. Live on the dev server: the Billed rows with no clock-ins read `No hours · 100% Sep 11`.

## Docs

`ready-to-bill-pipeline.md` — the words paragraph's examples.
