# v2.2621 — Lien-release send loop: inbox lanes + email to customer (2026-09-02)

PR 4 of the signing loop (schema v2.2616, modal/sign v2.2619, record surfaces v2.2620).

- New `src/components/jobs/LienSignatureInboxSection.tsx`, mounted in `ChecklistReviewInboxes` (Checklist → Review) and `DashboardTeamsInboxCard`: renders nothing when both lanes are empty. "Awaiting your signature" rows open `LienReleaseSignModal` directly; "Signed — ready to send" rows offer Email to customer (confirm-first), Download PDF (regenerated with typed signature), and two-click Mark sent without emailing (`sent_channel: 'manual'`).
- New kernel `src/lib/jobs/lienReleaseInboxLanes.ts` (+tests): lane split (signer = me / requester = me, live, unsent) and row labels. Hook `src/hooks/useLienSignatureLanes.ts` — one or-query with the job embed, fail-soft.
- New `src/lib/sendLienReleaseEmail.ts`: prefers the stored `signed.pdf` bytes, falls back to snapshot regeneration; pure `buildLienReleaseEmailBodies`.
- New edge fn `supabase/functions/send-lien-release-email` (see `docs/EDGE_FUNCTIONS.md` §send-lien-release-email; `verify_jwt = false` in config.toml). **Deploy manually after merge**: `supabase functions deploy send-lien-release-email`.
- Not in this stream yet (follow-up candidates): Needs-You mirrors of the two lanes, and the amber awaiting-signature tint on the Stages release button.
- Help: guide gains the send-loop section.
