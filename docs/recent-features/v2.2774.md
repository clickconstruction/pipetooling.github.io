# v2.2774 — Takeoffs refresh PR 4: the fixture-history RPC (2026-09-04)

PR 4 of the Takeoffs refresh train (`docs/TAKEOFFS_REFRESH_PLAN.md`, decision 5). The one migration in the train. Nothing calls it yet — New 1 (PR 5) and New 2 (PR 6) do — so the client merges first and `supabase db push` follows.

## Migration `20260904202321_takeoff_fixture_history` (see `docs/migrations/`)

- `public.takeoff_fixture_key(text)` — the fixture-name normalizer, mirrored from `src/lib/bids/takeoffFixtureKey.ts` (keep the two in step).
- `public.takeoff_fixture_history(p_service_type_id, p_keys[], p_exclude_bid_id, p_bids_per_key)` — `STABLE SECURITY INVOKER`: per key, the last N bids (1–10) of the service type whose active-version count row with that key has Combined lines, one best example row per bid, with the lines as jsonb (part / template names joined in). RLS runs as the caller.

## Client

- [`src/types/database-functions.ts`](../../src/types/database-functions.ts): `TakeoffFixtureHistoryParams` / `TakeoffFixtureHistoryLine` / `TakeoffFixtureHistoryRow` (manual types until `gen-types` runs after the push).
- [`src/lib/bids/takeoffFixtureHistoryRpc.ts`](../../src/lib/bids/takeoffFixtureHistoryRpc.ts): `fetchTakeoffFixtureHistory(supabase, {serviceTypeId, keys, excludeBidId?, bidsPerKey?})` — de-duplicates keys, 150-key `.in()`-style chunks, `withSupabaseRetry`.
- [`src/lib/bids/takeoffFixtureHistory.ts`](../../src/lib/bids/takeoffFixtureHistory.ts): pure shaping — `groupFixtureHistory`, `pickSameAsChips` (most recent first, a won bid promoted to the front), `buildCopyFromBidPreview` (which previous bids can fill this bid's uncosted fixtures, best first). 6 tests.

## Deploy sequence

1. Merge (client only; nothing calls the RPC).
2. `supabase db push` from a checkout at or past this commit.
3. `npm run gen-types:linked` → follow-up chore PR if `database.ts` changes.

Live gate (after the push): the RPC returns B383's sink lines for key `s` as Robert and as an estimator; returns nothing for a bid the caller cannot see; `takeoff_fixture_key` agrees with `fixtureKey` on the 15 most common prod names.
