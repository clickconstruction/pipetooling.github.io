# v2.3511 — What customers see: the GC's rooms, from the sample (2026-09-16)

PR 4 of the train in `to-dos/what-customers-see-journeys/README.md`. Client + one edge function (`get-submittal-room`); no migration. **Deploy `get-submittal-room` after merge**, and the other importers of `_shared/customerSampleFixtures.ts` once more (`customer-portal`, `sub-portal`, `get-estimate-for-customer`, `get-bid-proposal-room`, `get-contract-for-signer`, `get-job-contract`).

## What shipped

- **The sample submittal room** — `sampleSubmittalRoomResponse(state, company, todayYmd)` in `_shared/customerSampleFixtures.ts`, typed as the real `SubmittalRoomPayload`: BP482 Cedar Bend, one shared revision, five rows in the customer's four words (two that differ with their reasons, one added, one matching, one not quoted). `sample` is open with two rows waiting on a call; `sample-done` is the same room after **Alex Sample**, the architect, approved one and asked for a revision on the other. `get-submittal-room` answers both before any database read; no view stamp, no person row.
- **`SubmittalRoom.tsx`** honours `sampleStateFromToken`: the Sample banner; *identify* keeps the name on the page; *Send my review* records the count locally and says nothing was saved; the package link is inert.
- **Three GC emails render in the browser** from the real builders ([`customerSampleEmails.ts`](../../src/lib/customerSampleEmails.ts)): the **test report email** through `testReportSampleEmail` over the live Settings → Test reports (the tab now loads those settings alongside the app settings; the card reads *Loading…* until they arrive), the **pricing package** through `buildBidPricingPackageEmailHtml` / `…PlainText` over four sample rows, and the **statement** through `buildGcStatementEmailHtml` / `…Text` over two sample jobs with the sample GC's portal as the pay link.
- Five GC steps go live: Submittal review room, Their call back, Test report email, Pricing package email, Statement email.

## Verified

Typecheck, lint, build; sample-email (7), journeys, registry, person and paper tests. Live before deploy: the three emails render with their subjects. **The two room pages need `get-submittal-room` deployed** — verified after the deploy: `/submittal?t=sample` shows the banner and *2 rows need a call*; identify + decide + Send my review ends in the local thank-you; `?t=sample-done` opens with Alex Sample's calls.

## Left open

PR 6 (the supply house and the law firm) and PR 9 (the health row).
