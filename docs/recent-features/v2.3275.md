# v2.3275 — Robot price matrix, PR 5 of 5: the loop closes — Needs You, the Scoreboard, and what the robot learned (2026-09-11)

Client + `twin-mcp` v1.4.1 (**redeploy after merge**). No migration. Plan: [`docs/PRICE_MATRIX_PLAN.md`](../PRICE_MATRIX_PLAN.md); canvas boards 5, 7.

## Why

PRs 1–4 built the door, the robot, and the grid. What was missing was the loop: Wendi finding out a matrix is ready without watching a chip, the operator seeing whether the robot's picks hold up, and the robot actually reading her corrections. The first live run on ZZ Test b398 (claim → Drive read → structured quote → ask → Settle → re-finish) also surfaced two small things, fixed here.

## What shipped

- **Needs You card** — *Robot pricing · The robot priced BP359 SpaceX BA-2 — 23 picks ready, 4 to settle* ([`dashboardNeedsYou.ts`](../../src/lib/dashboardNeedsYou.ts), key `price-matrix-ready`, revenue tier; amber while rows wait on a choice, blue otherwise; the detail names expired quotes). Fed by [`usePriceMatrixReadyNudge`](../../src/hooks/usePriceMatrixReadyNudge.ts) (ready + unreviewed requests, RLS-scoped) for the office set and estimators; **Review matrix** lands on the bid's Pricing tab where the green chip opens the compare. 3 kernel tests.
- **Scoreboard · Pricing robot** ([`TwinPricerCard`](../../src/components/bids/TwinPricerCard.tsx), scoreboard face): matrices finished, quotes read, rows priced of total, rows it asked about instead of guessing, **agreement** (robot picks the estimator kept, from `fixture_component_corrections` unpick/repick), rules learned. Kernel [`pricingScorecard.ts`](../../src/lib/bids/pricingScorecard.ts) (2 tests).
- **Console · Pricing robot · what it learned** (console face): the active rulebook with provenance — *estimator* (a "remember this") or *robot* (with its mirror note) — times used, a **Retire** door, and the count of corrections the robot has not digested yet.
- **twin-mcp v1.4.1:** `get_component_corrections` (undigested teaching with the bid, row, line and her words); `extend_component_rules` takes `digest_correction_ids` (rules stamp the corrections digested + `rule_id`; an empty rules list with ids = read, one-offs); `finish_price_matrix` clears `reviewed_at` so a re-finish shows up again on the chip and the card.
- **Fleet row:** a pricer no longer shows a TakeoffTooling seat chip (it has no TT seat by design).

## The train, closed

| PR | Version | Shipped |
|---|---|---|
| 1 · schema + kit kernel | v2.3263 | migration `20260911023538` pushed, types regenerated |
| 2 · the door | v2.3267 | live on b398: queue → chip → status face → take back |
| 3 · the pricer | v2.3270 | twin-mcp v1.4.0 + plan-fetch deployed; Twin Pricer 1 minted; verbs run end to end on b398 |
| 4 · the grid | v2.3274 | kit rows, needs-a-choice, Settle, Robot column, corrections |
| 5 · the loop | v2.3275 | this |

## Still open (tracked in the plan)

`move` / `not_a_component` corrections (drag a loose part onto a fixture); cross-bid model-number memory; the rule `times_used` counter is written by nothing yet (the pricer should bump it when a rule decides a placement). Wendi's b359 needs the NWS link pasted on its Price-requests row before the robot can price the real bid.
