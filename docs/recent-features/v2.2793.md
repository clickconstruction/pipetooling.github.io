# v2.2793 — Office phone in the portal company block (2026-09-04)

Owner follow-up to v2.2773: `PORTAL_COMPANY.phone` in `supabase/functions/_shared/portalCompany.ts` was `''`, so the sub portal letterhead/footer, the customer portal letterhead, and the contract email's reach line all hid the number.

- `PORTAL_COMPANY.phone = '(512) 360-0599'` (the format prod already uses in the physical-invoice issuer and the GC statement emails). The constant is served by `customer-portal` and `sub-portal` (`payload.company`) and read by `send-contract-for-signature` — **all three redeployed** after merge.
- The send dialog preview (`PeopleContractsTab`) and the What customers see sample (`buildSampleContractEmail`) now pass `PORTAL_COMPANY.phone` instead of `null`, so they match the sent email: "Questions? Reply to this email to reach <sender>, or call or text the office at (512) 360-0599."
- No client copy changes: `SubPortal.tsx` and `CustomerPortal.tsx` already render `company.phone` when present (letterhead, "This link is private to you · Questions? Call or text the office: …", "or call the office: …" beside Pass).
- No migration.
