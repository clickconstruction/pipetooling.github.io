# v2.2642 — RFQ Round 2, Rung A: plans link + urgency desk (2026-09-02)

Rung A of `docs/RFQ_ROUND2_PLAN.md` (mockup → review → revise → build;
canvas artboard 5).

- **Plans link rides the request**: compose gains an "include the job
  plans link" toggle (shown only when `bids.plans_link` exists — the
  CountTooling link is internal and deliberately never sent). The link
  is stored in the request's scope snapshot (`scope.plansLink`, sanitized
  http(s) ≤500 chars), so the email, every reminder/resend, the preview,
  and the public quote page all carry the same link. Email renders it
  under the CTA; `/q/:token` shows "Job plans ↗" in the header.
- **Urgency-sorted desk**: kernel `rfqUrgency` (+3 tests, 16 total) —
  tiers bounced → needed-by ≤3d/passed → unviewed 2d+ → viewed-silent
  1d+ → fresh → quoted, oldest first inside a tier via
  `sortRfqsByUrgency`. The desk keeps the trail on every row and adds a
  reason chip ONLY on rows needing attention (amber-strong for tiers
  0–1); bounced rows keep their red inline fix instead of a chip.
