# v2.2776 — Fill from book under Combined (2026-09-04)

PR 5 of the Takeoffs refresh train (`docs/TAKEOFFS_REFRESH_PLAN.md`, decisions 2 and 4). `applyTakeoffBookTemplates` refused Combined bids with "Switch to Exact takeoffs…", which made the tab's headline automation inert for 112 of 113 bids since June and gave the book no reason to grow (3 entries). Now, under Combined, the button fills every book-matched fixture that has no lines yet.

## Client only — no migration, no edge functions

- New kernel [`src/lib/bids/takeoffBookFill.ts`](../../src/lib/bids/takeoffBookFill.ts): `planBookFill(countRows, lines, entriesWithItems)` (matcher + coverage → `fillable` in row order, `matched`, `alreadyCosted`), `bookFillMessage(result)`, `fillFromBookLabel(plan, applying, rough)` (Combined: "Fill from book · N matches", disabled at 0 with a hover reason; By Stage: the old label). 7 tests.
- [`useTakeoffRoughLines.fillRowsFromAssemblies(fills)`](../../src/hooks/useTakeoffRoughLines.ts): the quiet bulk form of Add assembly → expand — per fixture, each assembly expands to lowest-price part lines with `source_template_id`, sequenced in book order, persisted per row; returns `{fixturesFilled, linesAdded, partsWithoutPrice, emptyAssemblies}`; one catalog-lowest refresh at the end. `applyRoughAddAssemblyTemplate` is untouched.
- [`BidsTakeoffTab.tsx`](../../src/components/bids/BidsTakeoffTab.tsx): the bid's selected book now loads its entries onto the tab (a small effect keyed on the selected version — the admin section used to be the only loader, and it is left free to browse other books); `bookFillPlan` runs the matcher live; the Combined branch of `applyTakeoffBookTemplates` calls the bulk fill and shows the summary beside the button for 6 s; the button takes its label / disabled / title from the kernel. By Stage behavior and label unchanged.
- Help guide `fill-a-takeoff-from-the-book.md`; `docs/BIDS_SYSTEM.md` → Takeoff Tab → "Apply Takeoff Book Button" rewritten for both models.

Verified from a worktree dev server on the test bid B398 (Combined): with a temporary book holding one entry (`zz test` → an assembly, alias matching the bid's row), the button read "Fill from book · 1 match", filled the fixture with the assembly's priced lines (checked in the database, `source_template_id` set), the summary line appeared, and a second click reported every matching fixture already has lines. Temporary book and lines removed afterwards. Render smoke + bids suites green; `tsc -b` clean; lint 0 errors.
