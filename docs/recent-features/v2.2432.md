# v2.2432 — Settings → Digital twins fleet console (2026-08-28)

Owner ask: everything the twin program needs (keys, names, minting, tokens, MCP) in dev
settings. New dev-only System-zone tab `settings-digital-twins`
(`src/components/settings/DigitalTwinsPanel.tsx`, registered in `settingsGroups.ts`):

- **Endpoints card**: twin-login + twin-mcp URLs with copy buttons, fleet email
  convention, TWIN_HARNESS.md pointer. The master `TWIN_LOGIN_SECRET` **value is
  deliberately absent** (an in-app copy of a session-minting master key would defeat it);
  the rotation command is shown instead.
- **Fleet roster**: every flagged twin with its rung chip (rung 1 read-only / rung 2
  fenced) and a one-click rung toggle.
- **＋ Mint estimator twin**: auto-numbers the fleet email, creates via the existing
  `create-user` edge function (random unused password — twins sign in by mint only), then
  flags `is_digital_twin` + `read_only`.
- **Per-twin tokens**: issue (generated client-side, sha256 stored, plaintext shown ONCE
  with copy) and revoke — riding `twin_credentials` (v2.2429) RLS (dev-only).
- **Recent runs**: last 15 `twin_runs` rows (mints + mission reports).
- Fail-soft everywhere (tables cast until gen-types; panel explains itself pre-migration).
- `settingsGroups.test.ts` updated for the new dev tab id.
