# v2.2114 — bids_count_rows.unit column (2026-08-22)

Stage 2a of the counts-vs-line-feet split (stage 1: v2.2113). Migration [`20260823020216_count_rows_unit.sql`](../migrations/20260823020216_count_rows_unit.md): nullable, default-less `bids_count_rows.unit` (CHECK ea/ft/px/sqft; NULL = infer from the name via `src/lib/bids/countRowUnit.ts`'s `effectiveCountUnit`), backfill of the unambiguous `ft of …` / `px of …` export rows, and `duplicate_bid_to_service_type` re-created to carry `unit` on copy. No client change in this PR — the client that stamps/edits units (stage 2b) merges after this is pushed. Why nullable-without-default, and the write-path audit that found the copy function, are in the migration header.
