# v2.2483 — The agent conversation layer: twin_questions + heartbeat (2026-08-29)

RFI_LOOP_PLAN R3 + master plan Wave 4.5 (+ 4.6's get_work_state middle-coverage):

- **Migration `20260829032237`**: `twin_questions` (the INTERNAL lane, distinct from
  bids_rfis) — bid link deliberately named `about_bid_id` so the fence's bid_id
  auto-detection doesn't block bid-less questions; the fence applier is re-created with a
  `twin_questions` branch (twin INSERT as itself only, never modify) plus RESTRICTIVE
  ask-only policies (status 'open', no pre-filled answers). `twin_runs` gains
  bid_id/stage/state heartbeat columns.
- **twin-mcp v8**: `ask_question` (park, don't stall), `get_answers` (check at run start —
  promoted questions carry their RFI id), `heartbeat` (stage + working|blocked|done).
  `get_work_state` now also returns `middle` coverage (takeoff exact mappings, rough part
  lines, labor estimates, price-book copies), `rfis` statuses, `open_questions_here`, and
  `latest_heartbeat` — the resume read got eyes on the whole pipeline.
- **Fleet console**: a "Twin questions" inbox (answer inline / Promote to RFI draft /
  dismiss, stale-status guarded) and heartbeat rows in the runs feed (PULSE / loud amber
  BLOCKED) via a new `describeTwinRun` branch (+2 kernel tests).

Deploy: client first, then `supabase db push`, then `functions deploy twin-mcp`;
gen-types follow-up.
