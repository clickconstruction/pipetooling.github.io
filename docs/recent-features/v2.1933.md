# v2.1933 — Fix bill lines one-sitting modal (2026-08-20)

Part 2 of the no-bill-line remediation (v2.1931 filter/chip; stepper guard
next): the repair tool for the 64-job / $47.9k shell stock.

- **Fix bill lines…** button on the Billed section's active no-line filter
  status line (dev / master / assistant-like) opens `FixBillLinesModal`: the
  classifier-style one-sitting worklist — every open shell biggest-dollars
  first, each with a bill-out date input (default today, future dates
  blocked) and a **Create line** button; ✓ per fix, "N of M fixed" progress,
  board refetch on close when anything was created.
- Migration [`20260821130000_create_billed_shell_invoice.sql`](../migrations/20260821130000_create_billed_shell_invoice.md):
  `create_billed_shell_invoice(p_job_id, p_billed_on)` — materializes the
  missing billed invoice line for the FULL open remainder
  (revenue − payments_made − Σ RTB line amounts), backdated: `billed_at` is
  supplied on INSERT so the `billed_at` trigger's COALESCE keeps it, and
  `estimated_bill_date` gets the same day. Asserts: office-writer gate,
  status = billed, no existing billed line (the shell definition), amount
  > 0; `FOR UPDATE` on the job row against double-clicks.
- Kernel `src/lib/jobs/fixBillLines.ts` (`buildFixBillLineItems`, +2 tests):
  shells-only, open > 0, biggest-first.
- Fail-soft until the migration applies: Create shows the RPC error inline.
- Help guide `read-the-pipeline-money-view` + `ACCESS_CONTROL.md` updated.
