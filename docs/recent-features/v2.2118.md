# v2.2118 — Manage tab opens on One-offs (2026-08-22)

Owner request: the Manage tab's view pills (v2.2066) defaulted to **All**, which stacks
open one-offs, the repeating library, and the completed fold into one long page. The open
one-offs are what people come to Manage for, so the page now opens on **One-offs**.

- [`Checklist.tsx`](../../src/pages/Checklist.tsx): `manageView` initial state `'all'` →
  `'one_offs'`. Pill order (All · One-offs · ↻ Repeating · ✓ Completed) and every view's
  rendering are unchanged; All is one tap away. Search and the person filter still compose
  with any view. Client-only — no migration.
