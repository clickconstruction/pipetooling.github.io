# v2.2781 — Takeoffs New 2: the cost rail (2026-09-04)

PR 7 of the Takeoffs refresh train (`docs/TAKEOFFS_REFRESH_PLAN.md`; mockup C). The **New 2** pill (v2.2768) stops being a placeholder. Old is untouched and still the default. With this PR the three-view parallel run is complete; what remains is the week of real use (PR 8) and the retirement call (PR 9).

## Client only — no migration, no edge functions (reads the v2.2774 RPC)

- New [`TakeoffCostRailView.tsx`](../../src/components/bids/TakeoffCostRailView.tsx): the full coverage strip (**Fill from book**, **One at a time** → New 1; the Costed / $0 tiles set the filter), a book selector + **All · Uncosted · $0 price** chips, the tab's own line editor with **book suggests ‹assembly› · Apply** inline on every empty matched fixture, and the sticky rail — **What Pricing sees** (materials total, costed N of M, the red "No Takeoffs cost" warning, per-fixture unit costs with **incomplete** flags), **Needs a price** ($0 part lines with their fixture → **Request quotes · N parts**), **Copy fixtures from a previous bid** (up to three candidates from `buildCopyFromBidPreview`, radio pick, **Copy N lines from B…** fills only uncosted fixtures via `copyLinesToRow`), **Book · ‹name›** (how many of this bid's fixtures it knows).
- New kernel [`takeoffCostRail.ts`](../../src/lib/bids/takeoffCostRail.ts): `zeroPriceQueue` (part lines at $0 with their fixture; bundles and orphans skipped), `rfqScopeForZeroPrice` (one scope line per fixture + a note summing parts by name — the shape `RfqComposeModal` takes), `fixtureUnitCosts`. 4 tests.
- New hook [`useTakeoffFixtureHistory.ts`](../../src/hooks/useTakeoffFixtureHistory.ts): the one `takeoff_fixture_history` call per bid, shared by New 1 and New 2 (New 1's inline fetch moved here); runs only while a new view is up on a Combined bid.
- [`BidsTakeoffTab.tsx`](../../src/components/bids/BidsTakeoffTab.tsx): `renderRoughLinesTable(rows, opts?)` gains a `suggestionFor` slot on the empty-row branch (Old passes nothing — unchanged); the **Request quotes** door mounts the Pricing tab's `RfqComposeModal` (plan decision 7) with the bid's open-RFQ houses loaded on open and `bids.plans_link`; `copyFixturesFromBid` loops the hook's `copyLinesToRow` with one summary toast. New 1 gets `history` as a prop.
- Help guide `see-what-pricing-sees-on-takeoffs.md`; `docs/BIDS_SYSTEM.md` → Takeoff Tab gains a "Three views" paragraph.

Verified from a worktree dev server on the test bid B398 (Combined) in New 2: strip and rail totals equal the Old sheet's; a temporary row matched by a temporary book showed **book suggests … Apply** inline and Apply expanded it (database-checked); a seeded $0 line appeared under **Needs a price** and **Request quotes** opened the RFQ modal with the part named in the note (nothing was sent); an uncosted `WC-98` row listed three previous bids under **Copy fixtures from a previous bid** and Copy filled it re-priced; filters and the One at a time hop worked. All temporary data removed. Render smoke + bids suites green; `tsc -b` clean; lint 0 errors; theme check green.
