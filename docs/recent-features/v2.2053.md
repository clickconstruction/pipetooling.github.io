# v2.2053 — Why-we-lost estimator filter (2026-08-21)

Owner request ("show only bids by: <estimator>"; mockup artifact b95f55b4). [`BidsWhyWeLostLens`](../../src/components/bids/BidsWhyWeLostLens.tsx): `estimatorFilter` state + a "bids by" select in the header (after the count chips; hidden when only one estimator exists). Options derive from the lost bids on screen (`estimatorOptions`, sorted, + a "No estimator" bucket) — no extra fetch. The filter sits between `allLensBids` and the expansion/group/rollup pipeline, so the headline, builder rail, dollars, and keyboard queue all speak the filtered scope (deliberate contrast with Waiting-to-hear's search, which is navigation and leaves the rollup alone); selection resets on change and a dedicated "No lost bids by X" state prevents a filtered miss from looking like a clean queue. Also fixed the headline grammar the filter exposed ("1 lost bid needs a reason"). Verified live: Wendi → headline 60 → 1, rail collapsed to her builders, and her one open bid (BP339) showed the note-suggested chip. Help guide updated. Client-only — no migration.
