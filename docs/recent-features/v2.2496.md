# v2.2496 — EMAIL_FROM flip point (Resend sender migration, step 1) (2026-08-29)

Owner kicked off the deferred Resend migration (DOMAIN_CUTOVER.md → "Resend
sender migration"). Step 1 is the zero-behavior-change prep, mirroring the
APP_ORIGIN pattern that made the domain cutover a one-secret flip.

- **New `supabase/functions/_shared/emailFrom.ts`**: `EMAIL_FROM` reads the
  function secret with the Resend-verified pipetooling sender as fallback.
- **Mechanical sweep** (scripted): all 23 hardcoded
  `'PipeTooling <team@noreply.pipetooling.com>'` literals across 15 function
  files → `EMAIL_FROM` (including `_shared/resendSendEmail.ts`, which ~10
  functions route through). Doc-comment mentions updated to "the EMAIL_FROM
  sender". No behavior change until the secret is set.
- **Deploy note**: every email-sending function must be redeployed after this
  merges (shared code bundles at deploy time) — the flip only reaches
  functions deployed with the new code.
- `docs/EDGE_FUNCTIONS.md` sender mentions + `DOMAIN_CUTOVER.md` migration
  steps updated. Steps 2–4 (Resend domain verify, DNS, secret flip + test)
  tracked in the runbook.
