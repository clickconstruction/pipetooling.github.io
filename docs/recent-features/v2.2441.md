# v2.2441 — Click Tooling report button → plumbingtooling.com (2026-08-28)

Stage 1 of the domain shuffle (docs/DOMAIN_CUTOVER.md): the Click Tooling report app
(static GitHub Pages tool, repo `clickconstruction/clicktooling.github.io`) moved to
plumbingtooling.com today (its CNAME commit `9de36bf`), freeing clicktooling.com to
become THIS app's domain later. `buildClickToolingUrl` in
`src/lib/jobs/jobAddressUrls.ts` (+tests) now targets the new domain — the
`name/email/phone/location` query-param contract is unchanged (verified against the
report app's current `js/app-fixed.js`).

Notes from the move:
- The report app's repo had ~1,100 lines of uncommitted local edits on this machine
  (an earlier draft of work already committed remotely) — preserved untouched on its
  branch `local-uncommitted-2026-08-28` for the owner to review/delete.
- The app's own brand is still "Click Tooling — Reports for Licensed Professionals"
  (title tag + README), so the button label stays correct; rebranding the report app
  to match its new domain is a separate, optional decision.
- No redirect from clicktooling.com: no external links ever existed (owner-confirmed).
