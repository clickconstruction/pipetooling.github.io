# v2.3776 — Lien desk: the bar under the notice is one row (2026-09-23)

Punch list **#36** (Grace, 2026-09-23: *"I don't like this footer, it takes up a lot of space and is very wordy"*). No migration, no edge function.

## Why

The draft footer (v2.3662) was four rows on 864 · Michael Palmer: the envelope line with the cover-note switch; the headline *✗ Fix gate 1 · owner of record — then this can go*; a why that repeated it — *Blocked until the owner of record is on the property record. Once it can go: first notice we've sent this GC*; and a three-sentence skip line. Every fact it repeated was already on the pane one glance up (the gate cards, the months card, the skip confirm), and on a phone it was six lines.

## What shipped

- **One row** (`.lienFootRow`): the state and its verb on the left — blocked: *✗ Owner of record missing* (the first blocking gate's label and value) + *Go to gate 1 ▴*; ready (office): *→ Goes to the leader · first notice to this GC* + *Send for approval ▸*, *→ Straight into the run · standing "send" rule* + *Put it in the run ▸*, *→ Parks under the hold rule · re-asks before the deadline*; ready (leader): *→ Approving puts it in the run · they promised Oct 3 — the paper or their word* + *Approve ▸*. The why is a parenthetical (`.lienFootWhy`), never a sentence. Right: *Save draft*, *The leader said to send it…* when it applies, and the primary.
- **Skip** is a quiet dotted link beside *Save draft* — *Skip Jul…* / *Skip Jul + Aug…* — with the cost in its title and in the confirm line it opens (unchanged: *Skipping gives up the lien right on …*, the reason box, *Skip these months* / *Cancel*).
- **The envelope line moved above the paper** (`.lienEnvelope`, still `data-lien-desk-send-line`): *✉ To the owner of record and Loberg Contracting by certified mail · courtesy PDF to …* with the *Include the cover note* switch; the routine-paper explanation is the switch's tooltip. It shows in every state, the switch disabled once the draft has gone for approval, as before.
- **The leader's awaiting footer** is the same one row: *→ Your call on Jul + Aug · keeps the rule "…"* then the hold, back and approve buttons.
- **Phone**: the state takes the first line, the buttons the second (`@media (max-width: 640px)`).
- The held, ready, sent and missed footers were one line already and are unchanged.

## Verified

- `LienDeskModal.render.test.tsx`: the blocked row reads *Owner of record missing* with *Go to gate 1*; the ready row reads *Goes to the leader* with no *Blocked until* and no cost sentence; *Skip Jun + Jul + Aug…* opens the confirm that states the cost; the envelope line above the paper names both recipients and the courtesy PDF; the cover-note switch still toggles page 1.
- Live on 864 (blocked, gate 1) and 891 (to draft) at 1400 px and 375 px.
