# v2.2227 — Payment forecast payload matches the board's row scope (2026-08-24)

Migration `20260824140029` — CREATE OR REPLACE of `get_payment_forecast_email_payload()` only. Live fidelity verification (modal 58 rows/$233,291 vs email 54 rows/$171,187) exposed that v2.2223 copied the billed-report payload's `j.status = 'billed'` job filter, while the board flatMaps billed invoice lines across ALL jobs (`jobsStagesBoard.ts` `billedInvoices` — progressive billing bills break-off lines on Working jobs; live examples 878/650/967) excluding only collections (`status='billed' AND collections_at`). New scope: every billed invoice line on any non-collections job. Pay-speed/promise mirrors unchanged. The billed-report EMAIL has the same drift vs its print sibling — spawned as its own task, not fixed here.
