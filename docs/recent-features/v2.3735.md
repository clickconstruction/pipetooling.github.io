# v2.3735 — Day book PR 4b: Bids → Day book, the estimating side's door (2026-09-22)

PR 4b of [`to-dos/day-book`](../../to-dos/day-book/README.md) (punch list #20). The owner: *"do 4b via Bids."* Client only.

## What was missing

Estimators' lines and the estimating strip shipped in v2.3727 on People → Day book, but `/people` is closed to the estimator role (v2.2920, on purpose), so the tab needed a door on the estimating side. The owner picked Bids over reopening a People route.

## The change

- **`Bids.tsx`** — a `day-book` tab (`?tab=day-book`): the button after Estimators, the select gate (a role the Day book refuses lands on the Bid Board), and the render branch mounting the same `PeopleDayBookTab` People uses, with the viewer's role and the pick allowed by the same gate.
- **[`dayBookAccess.ts`](../../src/lib/people/dayBookAccess.ts)** — `canOpenDayBook(role)`, the one home for the rule (dev · controller for now, v2.3732); `usePeopleAccess`, the Crew Day line and the Bids door all read it.
- Docs: `docs/twins/APP_DIRECTORY.md` (the tab), `BIDS_SYSTEM.md`, the guide's opening line, the to-do (4b shipped).

## Verified

- `dayBookAccess` (1), the Day book and Crew Day render smokes, `appDirectoryCheck`, `releaseNotes`, `helpGuideContent`, `check:todos`; typecheck, eslint.
- Live on the worktree dev server as the dev: Bids → Day book opens the tab with the week list and Month; the URL carries `tab=day-book`.

## Next

Decision 6 (the nightly snapshot under the owner's account).
