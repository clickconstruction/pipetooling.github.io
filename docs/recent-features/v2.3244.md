# v2.3244 — The insurer flag retires (2026-09-10)

Migration `20260910213000_drop_supply_houses_is_insurer.sql` — **push only after this client deploys** (the older client still writes the column on every house save). The last chore from the supply-house-directory train.

## What changed

- `vendor_kind` (v2.3172) is now the only signal for what a `supply_houses` row is. `vendorKindOf` reads it alone and treats an unknown or missing kind as a supply house; `isInsurerFor` is gone.
- The vendor form and the shared house editor stop writing `is_insurer`; the editor also loses its pre-push retry-with-legacy-payload branch, which had no reason to exist once `vendor_kind` was live.
- The supply-house picker reads `vendor_kind = 'supply_house'` straight, with the usual retry wrapper — no flag fallback, no whole-roster fallback.
- Migration drops the derive trigger, its function, and the column. Nothing in RLS, RPCs or edge functions referenced it.

## Docs

`PROJECT_DOCUMENTATION.md` loses the column line; `GLOSSARY.md` → *Price coverage* names vendor kind instead of the flag. Follow-up: `chore(types)` after the push.
