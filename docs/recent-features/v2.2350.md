# v2.2350 — Quickfill "Needs you" station (the shared card) (2026-08-26)

Owner-approved follow-through on the notification audit: Quickfill's last
section is the SAME `DashboardNeedsYouCard` the dashboard renders — one
component, one item list, two homes. As items migrate into the Needs You
model, both surfaces stay complete automatically.

- **`QuickfillNeedsYouSection`** (new, self-contained): the same hooks the
  dashboard quick row uses (`useArBankUnallocatedCount`,
  `useStaleTallyStaffFollowUp`, `useLostBidNudge`, the stale-self tally RPC)
  → `buildNeedsYouItems` → `DashboardNeedsYouCard`, with actions preserved
  1:1 (AR toast+navigate, tally navigate, team follow-up modal — its own
  instance — and why-we-lost navigate). Reports `items.length` up for the
  section header count and freshness tally.
- **`Quickfill.tsx`**: `needs-you` SECTIONS entry at the bottom (after
  Office Leaving), gated like Warnings (office roles; wrapper renders on
  clean days so the ritual stays stampable — the card itself hides when the
  list is empty).
- **Mode preference is shared by design**: the per-user Cards/Walk toggle is
  the same `pipetooling_needs_you_mode_<uid>` key, so the chosen mode
  follows the user between Dashboard and Quickfill.
- **Telemetry for free**: the card records control `needs-you` with
  `from_path` = `/quickfill`, so the Usage dashboard can split engagement by
  surface.
- Verified pre-push live: section at the very bottom, "4 open" header from
  the live item count, all four cards (2 deposits / 89 purchases / team 2 /
  61 lost bids), Walk mode with Skip ("1 of 4"), shared mode persistence,
  no overflow at 375px.
