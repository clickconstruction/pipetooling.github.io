# v2.2858 — Payroll approvals: assistants approve (copy fixed), the strip pill asks first, one Sun–Sat pay-week anchor with "+N earlier weeks", salary and midnight chips (2026-09-05)

Journey-map Phase 4, Tier-1 #15 — payroll-week journey findings J7-1 (stumble, WORSE on main), J7-2, J7-3, J7-8, J7-N1/N2. Owner decision 10 (2026-09-05): **assistants DO approve hours** — delete the contradicting sentence, do not gate.

**The live contradiction (J7-1).** `QuickfillPeopleHoursNewSection.tsx` bolded *"Assistance only makes sure hours are correct, they do not approve!"* while the same page's clock strip, the Hours grid banner, the bulk-approve modal, the per-session Approve buttons, the cell popover, the all-weeks queue, the Users-row hours door (v2.2694/v2.2822) and Moneyfill all approve for `assistant` — and the RPC `approve_clock_sessions` admits `is_assistant()`. Nine doors, one sentence saying none of them exist. Fixed as copy: the notice now reads *"Approve yesterday's and today's sessions here — every approve adds to payroll."* (same amber notice style; no role gate anywhere).

**The one-click write (J7-8, N1).** `ClockSessionStripApproveControl`'s short press (and Enter/Space) called `onApprove()` straight into the RPC — the only unconfirmed single-click payroll write found in the journey. The app has no undo-toast pattern and no revoke-on-undo path, so the fix is the codebase's standard **`useConfirmDialog`** (owner rule 2026-08-19: confirmations are modals): [`DashboardTeamActiveClockStrip.tsx`](../../src/components/DashboardTeamActiveClockStrip.tsx) gains `handleStripPillApprove(sessionId, personLabel, timeRangeLabel)` — `confirmDialog({ message: "Approve <who>'s session (<span>)? This adds the hours to payroll.", confirmLabel: 'Approve' })` → `handleStripSessionApprove(id, 'strip-pill')`. Both pill mounts (Clocked-in-today rows and Jobs-worked-today rows) route through it; keyboard Enter/Space hits the same `onApprove` so it confirms too. The long-press **Session actions** modal's Approve button is unchanged (already a deliberate press) and records as `strip-actions`. Pill titles/aria-labels now say "click to approve (asks first)". `ClockSessionStripApproveControl` itself is behaviour-unchanged — the confirm lives in the host handler so the control stays a dumb press detector.

**One pay-week anchor (J7-2).** New pure kernel [`src/lib/payWeekAnchor.ts`](../../src/lib/payWeekAnchor.ts): `payWeekStart` / `payWeekContaining` (Sun–Sat, via the approvals queue's `weekStartYmd`), `previousCompletePayWeek(now)` (Chicago calendar; = Draft Payroll's default), **`payWeekForCloseWeek(weekMondayYmd)`** (the Sun–Sat week that ends inside a Mon–Sun close week: closing "Aug 24–30" reviews pay week "Aug 23–29"), `formatPayWeekLabel`, and the banner kernel `pendingOutsideVisibleWeek(allWeeks, visibleClosedPending)` + `countClosedPendingSessions` + `describePendingOutsideVisibleWeek(n, rangeEnd, today)` ("+16 sessions in earlier weeks" when the range reaches today, "other weeks" when paged back).
- **Moneyfill "Sessions pending approval"** ([`moneyfillWeekClose.ts`](../../src/lib/moneyfillWeekClose.ts) `fetchPendingApprovalForWeek`) now queries `work_date BETWEEN payWeek.start AND payWeek.end` where `payWeek = pendingApprovalPayWeek(weekMonday)`; the header chip count and the section share the fetcher so they agree by construction. Section copy names the week: "Closed clock sessions in the **Sun–Sat pay week Aug 23–29** … Same week as Draft Payroll; the other queues here keep the Mon–Sun close week." **NOT changed**: the Moneyfill page's `weekMonday` state, `previousCompleteWeekMonday`, and every bank-money / time-no-job / supply / job-flag queue — those pair with the Weekly Money Movement report (WEEKLY_MONEY_PLAN.md invariant). **NOT changed**: Draft Payroll (`getPriorWeekPayStubRangeEnCa`, already prior Sun–Sat) and the Hours grid's initial week (this Sun–Sat).
- **Hours banner** ([`PeopleHoursPendingBanner`](../../src/components/people/PeopleHoursPendingBanner.tsx), new `outsideWeekLine` prop): People.tsx computes `describePendingOutsideVisibleWeek(pendingOutsideVisibleWeek(pendingApprovalsAllWeeks.sessions, countClosedPendingSessions(pendingClockSessions)), hoursDateEnd, today)` from the all-weeks count `usePendingHoursApprovalsNudge` already polls for the Approvals badge (v2.2694) — no new read. Rendered as an underlined button → `onOpenQueue` (the all-weeks queue). Hidden when the RPC returns nothing (team leads get the zero row) or the visible week IS the backlog.

**Chips (J7-3 / N2).** New pure kernel [`src/lib/people/approvalsSessionChips.ts`](../../src/lib/people/approvalsSessionChips.ts): `isSalaryFlatHoursPerson(cfg)` (`is_salary === true`, either flavour — plain-salaried manual punches cost flat too), `isMidnightCappedClockOut(iso)` (company wall clock reads **23:59** — what `auto_clock_out_open_sessions_eod` writes: last microsecond of the work date's Central day), `sessionApprovalChips` / `cellApprovalChips` → `{label, title}`. Labels: **"salary — counts as flat hours"**, **"still clocked in at midnight"**; neutral (muted) chips, deliberately not amber — information, not a warning flag.
- [`PeopleHoursBulkApprovePendingModal`](../../src/components/people/PeopleHoursBulkApprovePendingModal.tsx): new `payConfigFor(personName)` prop (People.tsx passes `payConfig[name]`); chips beside the person name per cell row (midnight once if ANY session in the cell was capped); when any salaried row is present the summary line adds "Salaried rows are session length here; payroll credits their flat salary hours." **Totals are unchanged** (documented) — the cost side already applies `effectiveHoursForCost`.
- [`PeopleHoursApprovalsQueueModal`](../../src/components/people/PeopleHoursApprovalsQueueModal.tsx): `load()` now also reads `people_pay_config(person_name, is_salary, record_hours_but_salary)` in parallel (failure → no chips, queue still loads); chips render after the ⚠ flags on each session row.

**Telemetry — `hours_approved{surface, role, count}`.** New [`src/lib/hoursApprovedTelemetry.ts`](../../src/lib/hoursApprovedTelemetry.ts): `recordHoursApproved(userId, role, surface, count)` → `recordNavClick(userId, role, 'hours_approved', '<surface>?count=<n>')` on the existing `ui_nav_clicks` table (no DDL). Skipped when `approved_count` is 0 (all-skipped batch). Instrumented sites: `strip-pill`, `strip-actions` (strip), `cell-popover` (`PeopleHoursPendingCellPopover`), `bulk-modal`, `approvals-queue` (Hours header / banner / Needs-You deep link), `users-row-queue` (`PeopleUsersTab` door — new `surface` prop on the queue modal), `desk-queue` (`PersonDeskHoursSection`), `moneyfill-queue`, `sessions-list` (`PeopleHoursSessions` per-row Approve). **Not instrumented**: Dashboard My Team "Approve all" / per-session Approve (team-lead surface, outside this row's scope). Query: `select target, role, count(*) from ui_nav_clicks where control = 'hours_approved' group by 1,2`.

**Tests.** `src/lib/payWeekAnchor.test.ts` (Sunday anchor, prior pay week on a Wednesday and on a Sunday, close-week → pay-week mapping Aug 24 → Aug 23–29, labels, the earlier-weeks subtraction incl. floor/null, RPC inclusion rule, earlier vs other wording), `src/lib/people/approvalsSessionChips.test.ts` (salary flavours, the 23:59 Central cap in CDT and CST, 23:59 UTC is not a cap, cell-level once-only midnight), `src/lib/hoursApprovedTelemetry.test.ts` (target format, role null, zero-count skip).

**Docs.** CLOCK_SURFACES_ARCHITECTURE.md (S2 handlers: `handleStripPillApprove` confirm + telemetry), WEEKLY_MONEY_PLAN.md (queue 3d anchored to the Sun–Sat pay week — the documented Mon–Sun divergence now has one exception), PEOPLE_TABS_ARCHITECTURE.md (banner `outsideWeekLine`, bulk-modal `payConfigFor`, queue `surface` + pay-config read), help guides `approve-my-teams-hours.md` (new "Everywhere else you can approve" section — every surface, incl. the strip pill and its confirm), `quickfill.md` (People Hours approves), `clear-the-hours-approvals-backlog.md` (the "+N in earlier weeks" link, chips).

**Verify (as an assistant).** `/quickfill` → People Hours: the notice reads "Approve yesterday's and today's sessions here — every approve adds to payroll." Any clock strip with a pending pill: short-press → "Approve <name>'s session (…)? This adds the hours to payroll." with Cancel/Approve; Enter/Space on the focused pill → same dialog; long-press → Session actions unchanged. `/moneyfill`: "Sessions pending approval" says "Sun–Sat pay week Aug 23–29" (for close week Aug 24–30) and its count equals Draft Payroll's "N clock sessions in this period still need approval" for the same default period; the header "Pending approval" chip matches the section. `/people?tab=hours` with a multi-week backlog: banner ends "+N sessions in earlier weeks" (underlined) → opens the all-weeks queue; a salaried `record_hours_but_salary` person shows "salary — counts as flat hours" in Review & approve and in the queue; a session ending 11:59 PM shows "still clocked in at midnight". **Rollback**: revert the PR — client-only, no migration, no edge function, no data change.
