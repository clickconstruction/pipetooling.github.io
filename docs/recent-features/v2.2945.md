# v2.2945 — "money story →": the door from the daily surface into Job Summary (2026-09-06)

Journey map **Tier 5 X2** (J6-9 gap — "Job Summary is findable but never found": ≈0 h / 30 d vs 132 h on Pipeline; the only entry was the tab strip, `Jobs.tsx`). Tier 5 train T5-02 (`pipetooling-journey-map` → `docs/journeys/_TIER5-TRAIN.md`). **Will's call 2026-09-06: build — hidden from assistants, shown to every other Job Summary role.** Client only — no migration, no edge function.

## What shipped

- **Kernel** [`src/lib/jobs/moneyStoryDoor.ts`](../../src/lib/jobs/moneyStoryDoor.ts) (+8 tests): `canOpenMoneyStory(role)` — a predicate, not a literal array at the call sites (decision 18): the Job Summary audience (`!primary && !superintendent` on `/jobs`; field roles and estimators never reach `/jobs`) minus assistant ⇒ dev · master · controller; `moneyStoryHref(jobId)` = `/jobs?tab=job-summary&job=<id>`; `jobSummaryRowDomId`; `resolveMoneyStoryLanding(param, ledgerIds)` → wait / focus / missing / none; the "not on the list" toast copy.
- **Door 1 — Pipeline activity header** (`JobActivityView`, rendered by the Stages thread panel and its full-screen modal): a `money story →` `Link` after "N% complete" + the provenance chip, gated on `viewerRole`.
- **Door 2 — job window header** (`DetailJobModal`): a `money story →` button next to the trade pill; closes the window and navigates.
- **Landing** (`Jobs.tsx`): `?tab=job-summary&job=<id>` — once `jobSummaryLedgerJobs` is loaded, the job id joins `expandedJobSummaryJobIds`, the main row (`id={jobSummaryRowDomId(job.id)}`, `JobsJobSummaryTab`) scrolls into view, and the param strips itself (`replace`). A job absent from the list (min-HCP floor / "Worked in" window) gets `MONEY_STORY_MISSING_TOAST` instead of silence.
- **Guide** `read-true-profit-on-job-summary` → new "How to get there" section.

Deliberately not done here: replacing Job Detail's duplicated parts-cost block (the row names it as a *candidate* once the door exists) — a separate decision.

## Verify

As dev on Jobs → Pipeline: open a row's activity panel → `money story →` after the % → lands on Job Summary with the row expanded and centred. Job window header shows the same link. Impersonate an assistant: neither link renders; the tab is unchanged. Deep-link `?tab=job-summary&job=<unknown>` → info toast, param cleared.
