# v2.2153 — Portal card copy trim (2026-08-23)

Owner copy on v2.2151's portal card: drop " — pay any bill there by card or ACH" from the HTML card and the plain-text line, in both [`gcStatementEmail.ts`](../../src/lib/jobsDocuments/gcStatementEmail.ts) and the dispatcher's [`render.ts`](../../supabase/functions/gc-statement-email-dispatch/render.ts). Dispatcher redeployed with this version (first deploy of the portal card). Client + edge redeploy; no migration.
