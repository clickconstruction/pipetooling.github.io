# v2.3846 — Get contracts signed floor line reads as one sentence (2026-09-26)

Copy tweak on top of v2.3845 ([`PipelineContractCoverageCard.tsx`](../../src/components/jobs/PipelineContractCoverageCard.tsx)). The floor line ("Counting every dollar · set a small‑job floor") sat in its own div under the description, so it read as a separate stanza. The owner asked for it to flow into the description as one sentence.

## The change

- The FloorLine `<span>` is now inline after the description text, not a following block.
- Its parts join with `, ` instead of ` · `, and the trailing dev link gets a `, ` prefix instead of a bullet.
- Non-editing FloorLine drops its own `fontSize` override so it inherits the parent's `0.75rem`; the editing form (dev only) keeps its input/save/cancel styling.

Result, non-dev: `…Tap a stage to see its gaps. Counting every dollar` (or `…Skipping jobs under $2,500, 3 not counted, 3 marked not needed`). Dev: same, followed by `, set a small‑job floor` (or `, change`). The zero‑gap collapsed status row inherits the same joining.

## Verified

Typecheck green. Live on `/jobs?tab=stages` — the line renders as one sentence and the dev link still opens the input.
