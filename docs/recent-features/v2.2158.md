# v2.2158 — Statement footer: office number is a tel: link (2026-08-23)

Owner request. New `officePhoneTelHref` (US 10/11-digit → `tel:+1…`, else `tel:+digits`) and `gcStatementFooterHtml` in [`gcStatementEmail.ts`](../../src/lib/jobsDocuments/gcStatementEmail.ts) — the HTML footer wraps the configured office number (`physical_invoice_issuer_v1.phone`) in an `<a href="tel:…">`; the plain-text footer is unchanged. Both HTML builders (single statement + Share all) use it; the dispatcher's [`render.ts`](../../supabase/functions/gc-statement-email-dispatch/render.ts) mirrors it. Dispatcher redeploy rides with v2.2153's. Client + edge; no migration.
