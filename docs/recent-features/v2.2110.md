# v2.2110 — Sendable-versions language re-anchor + picker purpose facts (2026-08-22)

Round 3 of the Workbench clarity arc. Owner's goal: versions = **separate bids you can send**; scenarios = **price points**. Key finding: the send machinery already existed (`bid_versions.include_in_submission` + `sort_order` → Cover Letter bundle sections; per-version GC `customer_id` v2.1159; `coverLetterGcPackets.ts`) but no pricing-side surface ever said so. Plan artifact (before/after): claude.ai/code/artifact/7ab1c836-2a4d-4bac-873c-b2b4c3754a62

**Phase A — language (this PR):**
- Workbench solo line: "One takeoff · one price scenario" → **"One bid · one price"**; door button "Try a variant…" → **"＋ New price or version…"**; options renamed to one-liners — 💲 **Another price point** ("Same counts, different price. Star the winner.") / 📦 **Another bid to send** ("A new version — own counts, own GC, its own section in the cover letter.").
- Plural structure bar: "Version — its own takeoff & counts" → **"This bid — one of N in the package, sends on its own"**; scenarios label → **"Its price points — same counts, star the one the customer sees"**.
- `BidVersionPicker`: label "Version:" → **"Bids in this package — each sends on its own"**; "+ New version" → **"＋ Another bid to send…"**; split modal → **"Split into two sendable bids"** (body drops the word "variant"; fields "Name this bid" / "Name the new bid"; checkbox "Start its prices from this bid's").
- Tour step, help guide `price-a-bid-with-the-workbench.md`, and a new GLOSSARY entry (Bid Version / Price scenario) all align to the canonical sentence.

**Phase B — purpose facts (this PR):**
- Every version chip shows **"in cover letter ✓ / not in cover letter"** (reads `include_in_submission`) alongside the existing GC line.
- New **"Send… →"** button on the picker (split bids only) → `onGoToCoverLetter` prop, wired at all three `Bids.tsx` render sites to `selectBidsTab('cover-letter')`.

**Phase C — deferred pending investigation** (per plan): per-version sent dates/values (submission tracking + `bid_pricing_history` are bid-level), board value for multi-version bundles, and a "make this bid a version of another bid" adoption path (the Jakes six-separate-bids failure). Open question found while reading: the Cover Letter bundle prices sections against `pricingCountRows` — verify per-version counts are used for non-active versions before building C.
