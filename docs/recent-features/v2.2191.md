# v2.2191 — Supply Houses: phone aging rows + a real Quickfill metric (2026-08-23)

PR E of the Quickfill tune-up (mockup https://claude.ai/code/artifact/50741c3c-293e-4eae-a897-83076baad9aa, screen E). At 375px the 8-column aging table showed 2½ columns in a cue-less scroller, and the Quickfill section reported "—".

- **Kernel** [`supplyHouseAging.ts`](../../src/lib/supplyHouseAging.ts) (+ tests): `countSupplyHousesPastDue60(matrix)` (houses with 60–90 or 90+ dollars) and `supplyHouseAgingPhoneNote(row)` ("$13,184 at 90+" first, else "most in <bucket>", else "all current").
- **[`SupplyHousesTab`](../../src/components/SupplyHousesTab.tsx)** (shared by /materials and Quickfill): under 640px (`useNarrowViewport640`) the aging table renders as one tappable row per house — name · note (red when 90+ money exists) · a five-segment bar (the table's five aging colors, worst right, ≥3% min segment) · total — same `openHouseFromAging` click. Desktop/tablet unchanged. Hooks sit above the access gate (rules-of-hooks).
- **Metric**: `useReportQuickfillSectionMetric('supply-houses', housesPastDue60, …)` — no-ops on /materials (no provider). The intro line adds "N houses 60+ past due." at every width.
- Verified locally against prod data at 375: "5 open", 9 rows with bars/notes; 1280: table unchanged with the new intro sentence.

Client-only.
