# v2.2510 — Manhattan orthogonalization: no more diagonal shortcuts (2026-08-30)

Owner's second overlay catch: blue diagonals cut corners the architect drew as 90°
turns — the tracer's jog-compression connecting jog-start to jog-end fabricated
geometry, and the fitting deriver then minted phantom 45-ells from the artifact.

- **Kernel** (`orthogonalizePolyline` + `diagonalSegments`, +4 tests → 26): every
  non-axis segment becomes the L-path through a chosen corner; near-axis wobble squares
  exactly; collinear staircase points collapse. `snap.ts` chooses each corner by ink
  coverage of the two candidate legs; `registration.ts` REFUSES undeclared diagonal
  segments — a run that is genuinely diagonal carries `diagonalOk` per line
  (LIVSTE has exactly one: the CO→DF-4 45° tail).
- **Proof by fittings**: SAN 45-ells 16 → 1; 90-ells → 21 (the architect's real offset
  pairs). Derived-fitting distributions are now recognized as an artifact detector —
  a pile of unexplained 45s means the trace is lying.
- **Re-landed**: all gates green (19 orthogonal, registration-clean runs; connectivity
  clean); SAN 168.6 / CW 188 / HW 86.3 / Gas 23.8 ft (L-paths measure honestly longer
  than shortcuts); 60 counters incl. 33 fittings across 6 layers. PLACEMENT.md gains
  the Manhattan rule; M6-v4 results row records the catch.
