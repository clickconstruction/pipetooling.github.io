# v2.2572 — AR Customers view call sheet (2026-09-01)

Mockup Variant B (https://claude.ai/code/artifact/881b39ae-3777-4377-af49-bbd685704ccb)
on the v2.2571 Customers view, with the owner's ordering: bills + line items stay
UP FRONT in the expansion; the call card renders after them.

- **Shared chase write path `src/lib/jobs/paymentChaseIo.ts`**: `addPaymentChaseTouch`,
  `setJobPromisedPayDate`, `recordPromiseForJobs` — `PaymentChaseModal` refactored onto
  the same helpers so the two chase surfaces cannot drift on touch semantics.
  (`SetPromisedPayDateModal` keeps its own call — it also clears dates.)
- **Kernel `src/lib/arCustomerChase.ts`** (unit-tested): `arCustomerChasePill`
  (dispute > broken promise [`BROKEN_PROMISE_GRACE_DAYS`] > promised > snoozed >
  quiet [`TOUCH_QUIET_DAYS`] > past-pace ask), `arLastTouchLine`, `arCallOpener`,
  `buildArCallSummary` (clipboard text, line items indented under each bill).
- **`DashboardArCallCard.tsx`**: opener line, last-5 receipts chips
  (`receiptGapTone` vs company median), last touch, and office-gated actions —
  "They promised…" (date input → `recordPromiseForJobs` over the row's late-bill
  jobs), "Can't reach — snooze 7d", "Copy summary". Fills the view's
  `expansionFooter` slot; `ArChasePillTag` fills `rowBadge`.
- **`ItemsModal`**: fail-soft `list_payment_chase_touches` +
  `list_job_promised_pay_dates` fetches (office roles), refetched after a write.
- No schema/RPC changes; all writes go through the already-shipped chase RPCs.
