# v2.3513 — What customers see: the health row (2026-09-16)

PR 9, the last of the train in `to-dos/what-customers-see-journeys/README.md`. Client + one migration (a read-only RPC); no edge function. **Deploy the client first, then `supabase db push`** — the row hides itself until the function exists.

## What shipped

- **Migration [`20260916170000_journey_health_counts.sql`](../../supabase/migrations/20260916170000_journey_health_counts.sql)** — `journey_health_counts()`, SECURITY DEFINER, office roles only (dev · master · assistant · controller), one jsonb: agreements (signed · sent and unopened 3+ days · sent and waiting · live jobs with no agreement row), bid rooms (open · sent and never opened · signed), customer and sub portals (links · ever visited by an outside viewer, from `public_page_views`), statements (builders with billed work · certified this month · sent this month). Fragment in `docs/migrations/`.
- **Kernel [`src/lib/journeys/journeyHealth.ts`](../../src/lib/journeys/journeyHealth.ts)** (+2 tests): parses the jsonb defensively and words five tiles, each with an `attention` flag and the door that owns the fix (Start the sweep, the Bid Board, Customers, People → Subs, GC Review).
- **[`JourneyHealthRow.tsx`](../../src/components/journeys/JourneyHealthRow.tsx)** under the count line on the tab: five tiles, an amber edge on the ones with something stuck. Missing RPC → the row renders nothing (`isMissingRpcError`); any other error is shown in one line.

## Verified

Typecheck, lint, build; kernel tests. Live before the push: the row stays away (the function is not there yet). **After the push**: five tiles with real counts, the amber edge where the count is stuck, each door landing on its surface.

## Left open

Nothing in the train. Residuals carried in the to-do: a full sample matter on the firm's portal; the signed-copy email as its own step; the Job window's *Their journey* door.
