# v2.3767 — Lien desk: the owner's call, counsel's three piles, and the playbook grid (2026-09-23)

Punch list **#33** PR 3 of 3 — section C of the mock-up (`to-dos/gc-failure-playbook/mockup.html`, in git history at this commit). Client-only; no migration (the owner's answers ride on the first packet's desk item like PR 2's facts). With v2.3753 (the § 53.057 retainage notice, the signer's phone) and v2.3760 (letter two) this closes counsel's memo of 2026-09-22 — the folder is retired and the memo moves to [`docs/COUNSEL_MEMO_GC_ON_NOTICE_2026-09-22.md`](../COUNSEL_MEMO_GC_ON_NOTICE_2026-09-22.md).

## Why

Counsel's grid has four columns the desk did not hold — payment bond, paid-out-to-GC, the 10 percent reserved, the original contract's completion — three of them the owner's own answers to the questions every letter asks them to phone in. With them the affidavit pile sorts into the memo's three piles: A the owner still owes the GC (trap it), B paid but did not reserve the 10 percent (a reserved-funds lien, shared, § 53.105), C paid in full and holds nothing (the property lien; file promptly). The memo also tells the office to build the grid, job by job.

## What changed

- **Kernel [`lienOwnerCall.ts`](../../src/lib/jobs/lienOwnerCall.ts)** — `OwnerCall` (`owesGc`, `owesAmount`, `reserved` held / released / never / unknown, `originalContractCompletedOn`, note, who and when), `parseOwnerCall`, `affidavitPileFor` (A when the owner still owes or holds the 10%, B paid and never reserved, C paid and released), `AFFIDAVIT_PILE_WORDS` (the short words and the next step per pile), `reservationHoldEndsOn` (§ 53.101: 30 days after their contract completed, weekend-rolled — the same rule as the retainage clock), `ownerCallWords`, `ownerCallByJobFrom` (the latest call per job off the notice items), and the grid: `buildPlaybookGridRow` + `PLAYBOOK_GRID_COLUMNS` + `playbookGridHtml` (landscape, the piles' meanings under the table, a homestead row flagged for counsel).
- **The call** — [`LienOwnerCallDialog.tsx`](../../src/components/jobs/LienOwnerCallDialog.tsx): the three questions as segmented picks (a figure when they still owe, a date when their contract is done), the note, and the pile read back live; *Record the owner's call…* in the desk's Sent footer (*The owner called again…* once one is on file) writes it with `noteOwnerCall` onto the **first packet's** item (`fields.ownerCall`; the entry's item may be a later letter two — the footer resolves the first packet through `letterTwoByJob.firstItemId` for the GC's okay and letter two as well). The since-sent line reads *Owner called: … · Pile A*. An owner call also turns letter two off (`lienLetterTwo.ts` reads it from the first packet).
- **The piles** — the Affidavits list row wears the pile chip (green A, amber B, red C); [`LienDeskAffidavitPane.tsx`](../../src/components/jobs/LienDeskAffidavitPane.tsx) gains *The owner's answers*: the call in words, the pile and its next step (or *no call recorded yet* with the door to the notice), the § 53.101 hold's end when their completion date is known, and the **payment bond** line (from the job's `lien_payment_bond`, v2.3753) with *Check the project ›* onto Edit Job → Our contract on this job. `LienDeskData.ownerCallByJob` (both hooks).
- **The grid** — [`gcOnNoticeSteps.ts`](../../src/lib/jobs/gcOnNoticeSteps.ts) gains step 5 *The grid* (`N of M owners answered`, done when all have); [`GcOnNoticeModal.tsx`](../../src/components/jobs/GcOnNoticeModal.tsx) renders it after the decision — one row per job in the run: owner, kind and county (a homestead row carries counsel's caution), last work, unpaid, the § 53.056 months (noticed ✓ and open with their dates), the § 53.057 clock, the affidavit date, the bond, paid out, 10% held, their contract's completion (with the hold's end), letter two, the pile — *Print the grid ↗* opens it for print.
- Guide *send lien notices from the Lien desk* gains *The owner's call and the three piles*; `docs/GLOSSARY.md` → Lien desk; `docs/README.md` indexes the memo; the v2.3753 and v2.3760 fragments now point at the memo's new home.

## Verified

- `lienOwnerCall.test.ts` (the piles, the tolerant parse and the words, the § 53.101 hold, the latest call per job and letter two's off switch, the grid row and its print), `gcOnNoticeSteps.test.ts` (five steps), the desk render smokes (the call recorded on the first packet with the pile read back; the affidavit row's chip, the answers box, the bond door), the GC run smoke (step 5 with its rows), typecheck, lint, `check:todos`.

## Left as is

- The owner's answers are the owner's word, dated and attributed; the affidavit decision stays the master's.
- The owner's contract-completion date is not yet a second trigger on the § 53.057 clock (§ 53.057(b) counts from the original contract's *termination or abandonment*, which the call does not ask). If SQL ever needs the owner's date, it moves to a column then.
- Counsel's one open question (the § 53.056 WARNING block) stays in `to-dos/owner-decisions-pending.md`; counsel reads the drafted § 53.057 form before the first one is mailed.
