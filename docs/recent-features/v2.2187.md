# v2.2187 — Quickfill: Email ×3 → one section (2026-08-23)

PR B of the Quickfill tune-up (mockup https://claude.ai/code/artifact/50741c3c-293e-4eae-a897-83076baad9aa, screen B). `email-inbox` / `email-follow-up` / `email-next-actions` were three registry entries rendering the same `QuickfillEmailInboxSection` with different labels — three chips, three marks, three scrolls, and button labels that didn't match their titles ("Mark Email up to date!" under "Email Inbox").

- New [`QuickfillEmailSection.tsx`](../../src/components/quickfill/QuickfillEmailSection.tsx) (replaces `QuickfillEmailInboxSection.tsx`): three rows (Inbox → Gmail inbox, Follow Up → `#label/Follow+Up`, Next Actions → `#label/Next+Actions`), each a label + Open link + 2-row textarea; one mark button. `composeEmailMarkNote` joins non-empty rows as `Label: text` lines (tested); the "N open" metric counts lines across rows. The mark still requires at least one row (note-first, same as before).
- Registry: `email-inbox` relabeled **Email** (keeps the id → mark history, saved order, `SECTION_INSERT_AFTER` anchors all carry); `email-follow-up` and `email-next-actions` removed — `normalizeQuickfillSectionOrderFromValueText` drops unknown ids from a saved order, their old marks stay in `quickfill_section_marks`.
- Verified locally at 1280 (one Email chip; three rows; banner unchanged — it's the stored custom banner; defaults change in PR C).

Client-only.
