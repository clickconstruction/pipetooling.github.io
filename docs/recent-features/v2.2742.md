# v2.2742 — Add checklist item modal: no backdrop close, × in the header (2026-09-03)

Owner ask, right after v2.2740: "I would also like it to not disappear when I click away on the screen from this modal — this means adding a × button to the top somewhere."

- [`ChecklistAddModal.tsx`](../../src/components/ChecklistAddModal.tsx): the fixed overlay's `onClick={closeModal}` is gone — the draft survives a tap outside. A **×** button (`aria-label="Close"`) joins the header's right-side group after the Go-to-checklist icon. Close paths are now ×, Cancel, and a successful Send. There was never an Escape handler; still none.
- Render test: a click on the backdrop leaves the typed title in place; the × closes.
- Help guide `give-someone-a-task.md`: one sentence on the new behaviour.

Client only.
