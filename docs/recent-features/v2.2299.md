# v2.2299 — Collected card reads 30 days, devs + controllers only (2026-08-25)

Owner call on Jobs → Pipeline's "COLLECTED · LAST 8 WKS" card.

- **Window**: `COLLECTED_WEEKS = 8` (Monday-start weekly buckets) →
  `COLLECTED_DAYS = 30` daily buckets (`collectedByDayFromPayments`;
  `StagesHeaderStats.collectedByWeek` → `collectedByDay`,
  `CollectedWeekPoint` → `CollectedDayPoint`). The bounded payments fetch
  window (`collectedWindowStartYmd`) follows. Label renders
  "collected · last 30 days"; sparkline is 30 daily points.
- **Audience**: `canSeeCollected` in JobsStagesTab is now
  `dev || controller` (was `dev || master_technician`, v2.1916).
- Tests updated across kernel/fetch-parity/render suites.
