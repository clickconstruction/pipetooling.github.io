# v2.2830 — Job Summary: Ahead view (2026-09-05)

PR 8 of the Job Summary analysis plan (artifact 9c02d29a), stacked on v2.2828 (Capacity).

## Client only — no migration

- [`jobSummaryAhead.ts`](../../src/lib/jobs/jobSummaryAhead.ts) (new kernel): `buildAheadSeries({ rows, bids, linkedBidIds, blocks, todayYmd, crewNow, windowRevenueUsd, windowDays, trueMarginPct, targetTrueMarginPct })` → open jobs + `remainingUsd` (contract − earned, unfinished, not billed/paid); won bids with no job (`outcome = 'won'` and no `jobs_ledger.bid_id`), with `wonNoDate` / `wonStartPassed`; eight Monday-keyed weeks from this week: `personDays` (distinct (day, assignee) schedule blocks), `jobsBooked`, `capacityDays` (crew × 5), `bidStarts`; `backlogUsd`, `backlogWeeks` (÷ the window's revenue per week), expected true profit at the window margin and at the target; `bookedDaysNext4` / `capacityDaysNext4`. Four tests.
- [`useAheadData.ts`](../../src/hooks/useAheadData.ts) (new): won bids, linked bid ids, and `job_schedule_blocks` for today → +56 days; errors surface in the footnote.
- [`useJobSummaryView.ts`](../../src/hooks/useJobSummaryView.ts): bundle gains `allRows` (enriched before Show / window / search) — open jobs are open whatever the table shows.
- [`JobSummaryAheadView.tsx`](../../src/components/jobs/JobSummaryAheadView.tsx) (new): tiles (Remaining on open jobs, Won not started, Booked backlog + weeks at pace, Expected true profit, Field days booked next 4 weeks vs crew-days — amber under 60%), the eight-week bars with the capacity line (crew from the Capacity kernel's `crewNow`), won-bid starts as diamonds, this week dashed, and the won-not-started list (no date amber, start passed red). View mode `'ahead'`; ledger-driven controls only.
- Help guide: new `see-what-work-is-coming.md`.
