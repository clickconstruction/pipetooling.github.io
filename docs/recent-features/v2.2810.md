# v2.2810 — Person Desk: section anchors, a section deep link, and a Push section (2026-09-04)

PR 2 of 4 for the People → Users status column (artifact 747a0b27). Every Desk section is now a door target, so the row's chips (PR 3) can open the Desk at the one section that answers them.

- **Section ids** ([`src/lib/people/personDeskSections.ts`](../../src/lib/people/personDeskSections.ts), tested): `hours · portal · work_orders · pay · team · push · field · paperwork · records · schedule · access`. `DeskSection` takes `id` and renders `id="desk-<id>"` with `scroll-margin-top`; all ten existing sections pass theirs.
- **Open at a section**: `PersonDeskOpenArgs.section` — the body scrolls the section into view once the key resolves and rings it for ~2 s. Deep link: `?person=u:<id>&section=paperwork` (`personDeskLink` builds it; `PersonDeskDeepLinkHandler` reads and strips both params; unknown sections open the Desk at the top).
- **Push notifications section** (`PersonDeskPushSection`, office roles — the same `canSeePushStatus` gate as the Users chip): on/off from `push_subscriptions`, device count and latest registration, and the one-line how-to when off. The "no push" fact now has somewhere to land.
- Help guide "open a person's desk" gains the Push section and the section-link note.
