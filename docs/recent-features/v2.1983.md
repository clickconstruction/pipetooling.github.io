# v2.1983 — GC Review certification UI: progress strip, per-bill checklist, chips (2026-08-21)

Phase 2 of the Wednesday certify-and-send ritual (v2.1980 laid the table + kernel; built to the owner's checklist mockup). [`JobsGcReviewModal`](../../src/components/jobs/JobsGcReviewModal.tsx): loads this week's certs on open (`listGcReviewCertifications`, Monday key), renders an amber "This week · due Wednesday · N of M certified · K sent" strip (GC grouping only — certification is per-GC, so the Development grouping hides all cert UI), and per-group state UI: blue **Certify…** (office roles via new `canCertify` prop), green "✓ Certified · name · time" chip (note on hover), amber "Changed since certified · ±$delta" chip + **Re-certify…**, and the "last sent" hint upgrades to a blue "Sent {date}" chip when the send is in-week. New [`GcReviewCertifyModal`](../../src/components/jobs/GcReviewCertifyModal.tsx) (z 70, above GC Review's 60): per-bill check-off with progress bar, ▾ chevron loading the job's latest 4 activity events inline (`fetchJobActivityEventsForJobLedger`), job link → Job Detail on top via the app-level `jobDetailModal` context (z ~1004; checklist keeps its state), optional note, Certify / **Certify & send…** (insert then chain into the existing Email dialog via the extracted `openEmailDialogForGroup`). New IO lib [`gcReviewCertifications.ts`](../../src/lib/gcReviewCertifications.ts). Verified live against prod: strip "0→1 of 13 certified", checklist activity + Job Detail stacking, RLS insert as dev succeeded (one test cert row for Tyler Moore, week of 2026-08-17 — inert once the week rolls). Help guide `track-a-general-contractor-on-a-job.md` gains the ritual section. Dashboard nudge ships next.
