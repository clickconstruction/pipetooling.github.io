# v2.2166 — Jobs Cleanup: row sublines wrap on phones (2026-08-23)

Viewport pass after shipping v2.2145 (desktop / tablet / phone): at 375px the one-line `nowrap` subline in [`QuickfillJobsCleanupSection.tsx`](../../src/components/quickfill/QuickfillJobsCleanupSection.tsx) clipped to "no job with thi…" / "2100 Ind…". The subline now wraps (`overflowWrap: anywhere`); rows grow taller on phones, desktop/tablet unchanged (one line fits). No horizontal overflow at any width; the shared money cards already stack one-per-row on phones. Client-only.
