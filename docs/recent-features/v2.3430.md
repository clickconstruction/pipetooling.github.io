# v2.3430 — Job accounts at the counter, PR 4: the invoice default and the evidence card (2026-09-14)

Migration `20260914233000_job_account_evidence_gaps.sql` (fragment in `docs/migrations/`); no edge function. Fourth of the five-PR train (v2.3423 · v2.3424 · v2.3426).

## Why

Zero invoices carried the v2.2669 flag because nothing set it. And the two v2.3161 hygiene cards asked about packets, which the office rarely sends; the question that matters is *which jobs bought parts at a house that expects an account, with none on record*.

## What shipped

- **The evidence rule** — RPCs `list_job_account_evidence_gaps()` / `count_job_account_evidence_gaps()`: every (job, expecting house) pair with an invoice allocated or a PO code minted in the last 180 days and no `open` / `not_needed` row; dollars, counts, latest evidence, the rep. Office-gated inside.
- **Add / Edit Invoice** (`SupplyHousesTab`): the packet cross-check is replaced by the account at *this* house. A new invoice allocated to a job with an **open** account here starts with **On job account** checked (until the user touches the box); the green line reads *Job account open at Reece · ref R-88214 · opened by phone 9/14*. No account (or requested): an amber line with **Mark opened…** → PR 1's sheet, which flips the default on save. Not needed reads grey with the reason.
- **Needs You** — one item `job-account-missing` (tier 60, gray): *N jobs bought parts at a house with no job account* · `$` · the houses named · pairs when they exceed jobs · **Review them** → `/materials?tab=job-accounts&filter=no_account`. Hook [`useJobAccountEvidenceGapsNudge`](../../src/hooks/useJobAccountEvidenceGapsNudge.ts) replaces `useJobAccountFlagGapsNudge`; the `job-account-unflagged` / `job-account-no-packet` items are gone (kernel + tests).
- **Pipeline Fix-ups** — `buildPipelineFixups` gains `noJobAccount` → the amber chip *No job account · N*; clicking it goes to the Job Accounts filter (the other three open their in-place lists). `JobsStagesTab` reads the same hook.
- **Materials → Job Accounts** — loads the evidence list (`.catch → []`), passes `noAccountByJob` to `buildJobAccountsView` (new row field `missingAccountHouses`, view total `noAccountJobs`, +1 test); filter chip **Bought, no account** (shown when nonzero or active, deep-linkable); expanded rows carry an amber *No job account at Ferguson, Moore Supply* chip. The v2.3161 `needs_flag` / `no_packet` filters stay.
- Types: both RPCs hand-added to `Functions` (regenerate after the push).
- **Fix from the v2.3423 live pass:** the house roster labelled an account row as a raw id when the job had no invoices at that house (the host's label map is built from allocations) — the roster now fetches those identities itself.
- **Fix from the v2.3424 live pass:** on Dispatch Mode → Schedule the strip rendered inside the block's own `<button>` (a button may not nest a button — React's DOM-nesting warning). It now renders as a sibling below the block's button row, indented to the details column.

## Not in this PR

PR 5: the New Job question and same-property reuse. Then the to-do's leftovers: the aging heat map question (still Taunya's) and a bulk flag.

## Verified

Typecheck, lint, theme check, kernel tests green. Live after the push — see the PR.
