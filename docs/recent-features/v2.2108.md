# v2.2108 — Catch-up modals design pass 2 (2026-08-22)

Owner feedback on v2.2106: "those mock-ups do not look phenomenal — another pass." Redesigned mockup republished at the same artifact (22ee9380) and ported into [`VehicleCatchUpModals.tsx`](../../src/components/people/VehicleCatchUpModals.tsx): icon-tile header (🛞 blue / 🧰 emerald) with title + subtitle and a big tabular `2 / 8` done-counter, slim animated progress bar under the readings header, rows on a strict `identity · context · action` grid (no more wrap jitter), monospace VIN pills, holder as a second line with a status dot (blue person / gray pool / amber unassigned), right-aligned tabular miles inputs, emerald ✓ medallion + "just now" on saved rows, `Enter` styled as a kbd chip in the footer. Same props, behavior unchanged — the polish couldn't ride #1805 because the queue merged it mid-edit. Client-only.
