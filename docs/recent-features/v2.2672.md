# v2.2672 — Fix hours-approvals count RPC role gate (2026-09-03)

Hotfix for v2.2671, caught during post-push verification: `count_pending_clock_session_approvals()` copied its approval-powers gate from the BASELINE body of `approve_clock_sessions`, which still referenced `is_assistant_of_pay_approved_master()` — dissolved and DROPPED on 2026-07-14 (`20260714200000`). Every call raised "function does not exist"; the hook fails soft, so the only symptom was the card staying permanently quiet.

- Migration `20260903030000` re-creates the function with the live helper set: `is_dev() OR is_pay_approved_master() OR is_assistant()` (the dissolve migration's exact substitution). Body otherwise unchanged.
- The `baseline_function_body_gotcha` strikes again — the baseline's helper roster is 3 months stale; check helpers against later migrations (or the live catalog) before borrowing a baseline gate.

No client change.
