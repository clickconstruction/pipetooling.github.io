# v2.2140 — Roadmap ★ pin: "this one, now" on the Next up shortlist (2026-08-22)

Next-up phase 3, part 2 of 2 (part 1 = migration v2.2139, [`20260823042146_roadmap_task_pin.sql`](../migrations/20260823042146_roadmap_task_pin.md)). The owner's override for the pick-don't-sort shortlist: pin a task and it leads its lane without touching stage numbers or arrows.

- **Kernel** (`src/lib/roadmapNextUp.ts`, +1 test): `NextUpTask.pinned_at?`; sort key gains a leading `[pinned? 0:1, pin time]` so pinned tasks lead their lane oldest-pin-first; pins are **exempt from the per-stage cap** (a pin never loses its slot); reason `{ kind: 'pinned' }` first, label "★ pinned".
- **Task card** (`ChecklistTechTreeTaskCardModal`): `pinned` + `onTogglePin` props; editors get a ☆/★ button beside ✎ (`aria-pressed`, amber when pinned, disabled while saving); everyone sees a "★ pinned — next up" chip in the header chip row when pinned. +2 render tests.
- **Tab**: `toggleTaskPin` (editors; sets/clears `pinned_at`, reloads); task loader now selects `*` so `pinned_at` arrives once the migration is pushed and the read never 400s before that; `TaskView` spells out `pinned_at` and the UPDATE carries a cast until `database.ts` regenerates (**follow-up after push:** `npm run gen-types:linked`, drop the local type + cast — same pattern as #1810). Map rows show ★ (instead of ⚡) for pinned tasks; `GroupNodeData.tasks[].pinned`.
- **Plan**: `PlanTask.pinned_at?`; Now-list rows show a small ★ before the title; the Next up panel renders the "★ pinned" reason chip via the existing label path.
- Help: `see-what-to-do-next-on-a-roadmap.md` → "Pin a task — this one, now"; `staff-and-discuss-roadmap-tasks.md` → "Pin a task".
- Until the migration is pushed: the ★ toggle errors ("Could not pin task") and nothing is pinned — benign; everything else is unchanged.
