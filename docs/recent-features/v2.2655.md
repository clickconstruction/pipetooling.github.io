# v2.2655 — RFQ Round 2, Rung G: picked prices land on bid costs (2026-09-02)

Owner-approved option (a) (docs/APPLY_PICKS_TO_COSTS_DECISION.md, canvas
artboard 10; migration `20260902181208`).

- **Build correction that mattered**: the workbench row cost is
  labor + materials (NOT materials-only as the doc assumed) — so the
  override replaces the row's **materials component only**, taxed like
  takeoff materials and recomputed against live counts
  (`computeBidPricingRows.materialsOverrideUnitByCountRowId`, +3 kernel
  tests proving labor survives and counts drift through).
- **Lots (the owner's package-pricing design)**: the Plug-in modal
  groups checked lines into a lot with one total (per-line prices
  clear; "lot $2,000 ×" chips ungroup); compare renders lot cells as
  "in lot · $2,000.00", excludes them from unit-price stars, **picks
  them atomically**, and counts the lot total once in picked totals
  (+2 kernel tests).
- **Apply picks to costs** (compare footer): the plan shows takeoff/u
  vs quoted/u per row, lots allocate proportional to takeoff materials
  (fallback per-unit) with editable splits and a running remainder that
  blocks Apply when the package total doesn't hold, and an honest
  margin before/after line. Writes upsert per row with provenance.
- **Workbench**: overridden rows wear a green "Ferguson ↩" tag under
  COST/UNIT — click reverts to takeoff; lot members revert as a group.
- Pipeline: `useBidCustomCosts` → `useBidPricingRows` → kernel; loaded
  bid-level (a negotiated materials price is true in every sale
  scenario).
- Live-tested end to end on BP339: paste with an ALL-IN line → grouped
  a $2,000 lot → atomic pick (picked total $2,580 = lot once + FCO×5)
  → apply (FCO $154.89→$116.00; lot split $0.00 remainder; margin
  94.9%→95.0%) → 3 revert tags → lot revert cleared both members →
  full revert; all test writes deleted.
