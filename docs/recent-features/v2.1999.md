# v2.1999 — Plan focus lenses (2026-08-21)

Built to the approved mockup: the Plan header stats become doors.

- **Kernel** `planFocusRows` (`roadmapPlanView.ts`, +3 tests): per Now-stage,
  open tasks matching the lens ('assigned' | 'unstaffed'); empty stages
  drop (feeding the hidden-count note); done tasks and milestones never
  survive.
- **View**: `focus` state (component-only — never persisted/URL, so the
  full Plan always returns), dotted-underline stat buttons (colors kept),
  amber banner (`--bg-amber-100`/`--border-amber`/`--text-amber-800`) with
  live lens count + "✕ Show everything", amber per-stage count pills in the
  unstaffed lens, Up next/Goals hidden while focused, "{k} stages hidden"
  note, and auto-close when the lens empties (staffing the last unstaffed
  task closes it — the lens is a worklist).
- Staffing flow untouched inside the lens. Guide
  `staff-and-discuss-roadmap-tasks` gains a "Focus on what needs names"
  section.
