# v2.2947 — Bid visits follow the job: "Move them to J1007" on the Won-moment door (2026-09-06)

Journey map **Tier 5 X10** (J15-F10 papercut — bid-anchored schedule blocks never re-anchor to the new job; only the job→bid direction existed, `20260814033856:414-416`). Tier 5 train T5-04. Will's call 2026-09-06: build. One migration (`20260906140000_move_bid_schedule_blocks_to_job.sql` — **`supabase db push` after merge**); no edge function.

## What shipped

- **RPC `move_bid_schedule_blocks_to_job(p_bid_id, p_job_id) → integer`**: `job_schedule_blocks` rows anchored to the bid get `job_id = p_job_id, bid_id = NULL`; `can_edit_schedule_dispatch()` (the table's own write predicate) plus the invariant that the job was opened from this bid (`jobs_ledger.bid_id`). Mirror of the v2.1624 job→bid repoint. Called via the `'name' as never` precedent until the next gen-types run.
- **Kernel** [`src/lib/bids/rehomeBidVisits.ts`](../../src/lib/bids/rehomeBidVisits.ts) (+6 tests): `summarizeBidVisits` (total / upcoming by company-calendar ymd), `rehomeOffer` (line · button · confirm copy, null when nothing to move), `rehomeDoneToast`.
- **`BidVisitsRehomeRow`** (new) rendered inside `BidWonJobActions`'s full variant once a job exists: reads the bid's still-anchored blocks (`job_schedule_blocks` where `bid_id = bid and job_id is null`), and — for `CAN_USE_SCHEDULE_DISPATCH_EDIT_ROLES` — shows "N scheduled visits still sit on the bid (M upcoming)." + **Move them to J1007** → `useConfirmDialog` → RPC → toast → row re-reads and disappears. Never automatic; the compact (board / Followup) variant is untouched.
- Guide `turn-a-won-bid-into-a-job` → "Visits scheduled on the bid".

## Verify

Dev: on a bid with a job (or open one from a won bid), schedule a visit against the bid on the Schedule hub → Edit Bid → Win/Loss row → the line + button appear → confirm → toast "Moved 1 scheduled visit to J####", the hub shows it under the job, the row is gone. Estimator (not in the edit set): chip and buttons as before, no move line. Before the push the button fails soft with the RPC error toast.
