# v2.2217 — Bid Board GC lines: states left + per-GC notes (2026-08-23)

Owner-approved mock-up (artifact fa390fd1); column shipped in v2.2214 (`bids_submission_entries.gc_customer_id`, pushed).

- **Kernel [`bidGcNotes.ts`](../../src/lib/bids/bidGcNotes.ts)** (+3 tests): `countGcNotes` (`bid:gc` → n), `partitionNotesForGc` — a real GC gets scoped notes + whole-bid context; the bid's OWN GC (packet gcId null — a `bids_gc_builders` entity, not a customer) reads the whole-bid notes as its notes.
- **[`BidBoardGcRows.tsx`](../../src/components/bids/BidBoardGcRows.tsx)**: lines reordered to `sent m/d · [pill] · name` (fixed-width date column; shared-letter lines match); names are dotted-underline buttons opening **[`BidGcNotesPopover`](../../src/components/bids/BidGcNotesPopover.tsx)** (scoped notes, greyed whole-bid context, `ContactMethodQuickPicks`, Enter saves; insert bumps `bids.last_contact`, fires `bid-gc-notes-changed`); 💬 count badge per line. Bug found live: the bid's-own-GC packet key is `''` — the open-state test is `!= null`, never truthiness.
- **[`useBidGcPackets`](../../src/hooks/useBidGcPackets.ts)** batches the scoped-note counts alongside packets and listens for `bid-gc-notes-changed`.
- **Feed** ([`UnifiedBidCustomerNotes.tsx`](../../src/components/bidBoard/UnifiedBidCustomerNotes.tsx)): the entries select carries `gc_customer_id` + embedded GC name (`noteCreatorDisplay.ts`); scoped entries wear a GC pill; a `show: Everything · <GC…> · Whole bid` filter renders once scoped notes exist. The "+ bid note" scope selector was deliberately deferred — the popover is the scoped-add path.
- Verified live (dev build, prod data, b365): line order; primary-GC popover = the bid's notes; PlanHub scoped note saved → 💬 1 badge + feed pill + filter row; test entry deleted after. Help: `read-the-bid-board.md`.
