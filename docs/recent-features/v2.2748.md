# v2.2748 — Signed agreements: every master, every signature (2026-09-04)

**Why.** With the role default, Malachi (a master with his own org scope) didn't receive signatures on Robert's estimates — the shared org-scope filter kept him out.

**What.** Migration `20260904030000_signed_agreements_every_master`: masters in the recipient set bypass the org-scope filter (still must be active, non-twin, with an email). Assistants, controllers and devs are scoped as before. Applies to the role default and to an explicit list alike — a master on the list always receives.
