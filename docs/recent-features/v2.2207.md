# v2.2207 — Send-to header drops the ★ price name (2026-08-23)

Owner call. The GC group header in [`BidVersionPicker.tsx`](../../src/components/bids/BidVersionPicker.tsx) showed `★ <price name>` from the group's FIRST version only — with multiple versions it could contradict the board (viewing nljjpjpjj whose ★ is "asda;smfasd" while the header said "★ WENDI copy"). Removed entirely; the header now reads `sent/not sent [· one packet] [· N versions] [gets N prices]`. The version dropdown's per-version `★ <name>` is untouched (there it's unambiguous). Client-only.
