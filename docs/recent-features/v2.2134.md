# v2.2134 — Partnerships Ledger tab: footnote removed (2026-08-22)

Owner request. The muted `<p>` under the office Ledger table in [`PartnershipLedgerTab.tsx`](../../src/components/partnerships/PartnershipLedgerTab.tsx) ("Newest first; each row's balance is the running balance after that posting. Charges count at the date they happened — statements list them later as the paper record. Append-only: reversals are new rows, never edits. Click any row to drill in — labor opens that week's pay report, charges open the editor.") is deleted. Behavior (ordering, charges-at-date, drill-ins) is unchanged — only the caption is gone. Client-only.
