# v2.1953 — Partner ledger: headline caption removed (2026-08-21)

Owner didn't love "what settling up today means · posted +$X · pending −$Y" next to the Ledger headline. Removed in [`PartnershipLedgerTab.tsx`](../../src/components/partnerships/PartnershipLedgerTab.tsx): with pending items the settle-up number now stands alone (the v2.1951 inline pending rows and the footer carry the explanation); with no pending items the quiet "current balance (all postings − payouts)" caption remains. The headline math is unchanged (`netPosition` kernel). Client-only — no migration.
