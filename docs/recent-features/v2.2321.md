# v2.2321 — Data health add-date cell sizes to content (2026-08-25)

Live-verify catch from v2.2319: on `no bill date` payment rows, the fixed
8.6em date column couldn't hold "＋ add date → MM/DD" (127px of content in a
105px cell), so the paid date slid under the amount.

- `PaySpeedDataHealthModal` date cell: `width: 'auto'` whenever the add-date
  button is shown, not just while the editor is open — those rows size to
  content like the editing state already did. Plain billed → paid rows keep
  the fixed column so the list stays aligned.
