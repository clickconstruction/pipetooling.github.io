# v2.2036 — Partner lens: muted ground for light mode (2026-08-21)

Owner report: in light mode the View-as-Partner lens looked lineless — `DashboardPartnerLedgerSection` / `DashboardPartnerJobsSection` are white `DashboardGroupCard`s with hairline `var(--border)` outlines, designed to sit on the dashboard's muted page background. Inside the Partnerships detail card (also white) they vanished; dark mode survived only because its surface/border contrast is higher.

Fix in [`Partnerships.tsx`](../../src/pages/Partnerships.tsx): the lens wrapper gets `background: var(--bg-muted)` + `var(--border)` outline + radius/padding — recreating the dashboard's ground behind the same untouched cards. Verified in both themes (dark checked by forcing `data-theme="dark"`); dashboard surfaces unchanged. Client-only.

Spotted in passing (spawned as its own task): the roster's selected-item `--bg-blue-tint` washes out its text in dark mode.
