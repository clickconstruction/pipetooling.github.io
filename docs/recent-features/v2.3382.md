# v2.3382 — Accounts Receivable refresh, PR 4: the footer sentence, Apply with the amount, Apply & next (2026-09-13)

Client only; no migration, no edge function. Last of the train in [`to-dos/accounts-receivable-refresh/`](../../to-dos/accounts-receivable-refresh/README.md).

## What shipped

- **Kernel [`arApplySentence.ts`](../../src/lib/jobs/arApplySentence.ts)** (+7 tests): `arApplySentence({ lines, targetByKey, paymentById, depositRemaining, validation })` → the footer's words and tone — waiting (*Remaining $1,855.70 — pick a bill, or link a recorded payment.*), ready (*Applies $250.00 to 992 · Done Right Foundation. The bill is settled.* · *… $650.00 stays open on it. $500.00 of the deposit stays unapplied.* · *Applies $2,900.00 across 2 lines — …* · *Links this deposit to the $2,918.22 payment already on 989 · Take 5 Seguin — no new payment is created.*), or warn (the validation message, which left its `<p>` above the footer); `arNextDepositId` picks the row below, else above.
- **Footer**: the sentence on the left, Cancel, **Apply & next ›** (only when there is another deposit and Apply is allowed), and **Apply $X** carrying the total. `submitApply(mode)`: after a successful apply — and after the Stripe auto-close when every close succeeded — `finishApply` either closes (default) or refreshes the list and selects the remembered next deposit; `refreshList` now returns the rows it loaded so the selection can be checked against them.
- Render smoke: picking the exact match flips the sentence from *Remaining …* to *Applies $1,625.00 to 876 · …* and the button to *Apply $1,625.00*. Guide *match bank deposits to the bills they pay* names Apply & next.
