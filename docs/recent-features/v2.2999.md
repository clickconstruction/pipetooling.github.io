# v2.2999 — One company, Phase 5c: the retired grant tables are dropped (2026-09-07)

The last step of [`docs/ONE_COMPANY_PLAN.md`](../ONE_COMPANY_PLAN.md). One migration, `20260907070000_one_company_drop_retired_grant_tables.sql` ([doc](../migrations/20260907070000_one_company_drop_retired_grant_tables.md), which also lists the 18 rows by name for the record) — pushed by the agent after merge; no `CASCADE`, so a surviving dependency fails the push instead of taking anything with it. Will's call 2026-09-07: no quiet week; the views had been serving every read for the day and nothing named the pair.

Plan status after this: **complete** — Phases 1–5 and 5c, v2.2967 → v2.2999. `database.ts` is regenerated in the next PR (the two `retired_*` entries go). Left for ordinary maintenance: the 45 functions that still name `master_assistants` / `master_shares` read the views and can lose those dead branches whenever they are next edited.
