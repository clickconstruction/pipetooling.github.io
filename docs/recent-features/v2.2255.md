# v2.2255 — HCP reconcile importer in Settings (2026-08-24)

Billing Truth Plan Phase 3 (owner-directed entry point: Settings). New dev-only collapsible [`SettingsHcpReconcileSection`](../../src/components/settings/SettingsHcpReconcileSection.tsx) on Settings → Jobs & billing, backed by pure kernels in [`src/lib/settings/hcpReconcile.ts`](../../src/lib/settings/hcpReconcile.ts) (13 unit tests) — the repeatable version of the 2026-08-24 one-off psql backfills.

- **Lane 1 (invoices export)**: `buildInvoiceReconcilePlan` — creates dated `status='paid'` invoices (amount = the job's payment sum, `external_send_note` tagged `hcp-backfill-<runYmd>`) on paid single-HCP-invoice jobs with payments and no app invoice; stamps `billed_at` on undated singles; links unlinked payments on paid singles. Open HCP invoices are never imported (system-of-record ruling); multi-invoice jobs, ambiguous numbers, and paymentless jobs skip with reasons.
- **Lane 2 (payments report + jobs export)**: `buildPaymentReconcilePlan` — resolves payments to jobs via (customer name + job-created timestamp) against the jobs export (**the payments export's "Invoice Number" is a different series than the invoices export's — never join on it**); corrects `paid_on` on job-scoped unique amount matches (note tagged `hcp-paydate-corrected-<runYmd>` — the v2.2248 quarantine's verified marker); splits rollups equal to the sum of ≥2 unmatched export payments (insert-before-delete so a mid-apply failure can't lose money; split rows link to the bill only when dated on/after it). Mercury/Stripe rows untouched; unmatched HCP money never auto-added.
- Plans are idempotent — re-running the same files after an apply plans nothing. Render smoke covers the collapsed/expanded shell. Help guide `reconcile-hcp-billing-history` (dev). Settings.tsx change is one import + one render line.
