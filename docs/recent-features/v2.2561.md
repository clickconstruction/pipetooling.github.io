# v2.2561 — Edge-function ClickTooling copy sweep (2026-09-01)

The server-side half of the v2.2526 text sweep (see that fragment's "Still
outstanding"). Renames every customer/user-visible "PipeTooling" string in
`supabase/functions/` to **ClickTooling**. No client changes, no migrations.

## Renamed (18 files)

- **job-share** — link-page `<title>`, "Open ClickTooling" link, `og:site_name`.
- **Email renders/footers** — billed-report-email ("Open the board in
  ClickTooling" + sender line), payment-forecast-email-dispatch,
  weekly-money-email-dispatch footer, paid-job-email (`render.ts` +
  `readyToBillRender.ts` footers), send-report-email,
  `_shared/recurringJobReportCore.ts` footer (used by the recurring-report +
  schedule-day + bid-pricing senders), recurring-job-report-test-send banner,
  ct-roster-audit ("N ClickTooling people · M CountTooling accounts"),
  accept-estimate ("Open estimate in ClickTooling").
- **send-physical-invoice-email** — 'Invoice from ClickTooling' subject fallback.
- **Stripe error copy** — send-stripe-invoice, reverse-stripe-invoice-out-of-band-payment
  (×3), update-collect-payment-stripe-customer-email.
- **Notifications/notes** — sign-bid-room push text, submit-portal-request
  confirmation, audit-finish ledger note ("Audited in ClickTooling by …").
- **Redirect allowlists** — `ALLOWED_REDIRECT` in **invite-user** and
  **send-sign-in-email** now also accepts `https://clicktooling.com/*`
  (was pipetooling.com-only; rejected redirects fell back to `APP_ORIGIN`, so
  post-cutover links landed on the right domain but always at the default
  path). `docs/EDGE_FUNCTIONS.md` request-shape comments updated.

## Deliberately kept as PipeTooling / pipetooling

- `_shared/emailFrom.ts` fallback sender — mirrors the live `EMAIL_FROM`
  secret (owner flip point, docs/DOMAIN_CUTOVER.md).
- `https://pipetooling.com` `APP_ORIGIN` fallbacks — documented flip points
  (`supabase secrets set APP_ORIGIN=…` at cutover).
- Functional identifiers: `pipetooling_*` Stripe metadata keys,
  `pipetoolingStripeInvoiceNumber` module, geocode/link-check `User-Agent`
  strings, `@twins.pipetooling.local` fleet emails, twin-mcp briefs
  (generated from docs/twins).
- Code comments in functions not otherwise touched (stripe-webhook), and the
  undeployed `test-email/index-with-resend.ts` sample.

## Deploy

Every touched function needs `supabase functions deploy <name>`:
`accept-estimate audit-finish billed-report-email ct-roster-audit invite-user
job-share paid-job-email payment-forecast-email-dispatch
recurring-job-report-test-send reverse-stripe-invoice-out-of-band-payment
send-physical-invoice-email send-report-email send-sign-in-email
send-stripe-invoice sign-bid-room submit-portal-request
update-collect-payment-stripe-customer-email weekly-money-email-dispatch`
— plus the other importers of the shared report core:
`recurring-job-report-dispatch recurring-job-report-preview
schedule-day-email-dispatch send-bid-pricing-package`.
(`create-user` untouched; its `verify_jwt = false` note doesn't apply here.)
