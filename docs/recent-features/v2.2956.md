# v2.2956 — Docs drift audit: the entry docs stop pointing at the frozen changelog, counts and links re-verified (2026-09-06)

Second audit of the Plan B series (RLS coverage was v2.2954). Docs only — no client code, no migration, no edge function; the release note exists because the drift test requires one per fragment.

## What was checked

- A link checker over every living doc (`AGENTS.md`, `README.md`, `CLAUDE.md`, `docs/*.md`, runbooks, twins, to-dos; the frozen `RECENT_FEATURES.md` excluded): markdown link targets, backticked repo paths, `npm run` script names.
- Every numeric claim (tables, edge functions, test files, Postgres version) against the repo and the generated types.
- Every reference to the changelog files against the 2026-08-20 fragments cutover.
- Frontmatter `last_updated` against git.

## What was wrong, now fixed

- **The entry docs still routed agents to the frozen aggregators.** `AGENTS.md` constraint 11 told every agent to add a `docs/RECENT_FEATURES.md` entry plus a `src/content/releaseNotes.ts` entry — the exact files `CLAUDE.md` forbids editing since the cutover. Five more pointers in `AGENTS.md`, four in `README.md`, five in `docs/README.md` and two in `docs/AI_CONTEXT.md` said "grep RECENT_FEATURES.md" as if it were live. All now route to `docs/recent-features/` (+ the frozen archive), `docs/migrations/`, and the per-version release-note files; `docs/README.md` gains rows for both fragment directories and marks `MIGRATIONS.md` frozen.
- **Counts**: "~252 tables" → ~355 (the generated types carry 355); "~61 / ~67 Edge Functions" → ~104; "~440 test files" → ~1,070 (130 render smokes); "PostgreSQL 15" → 17 (the project runs 17.6); `ACCESS_CONTROL.md` frontmatter `tables_with_rls: "50+"` → every public table (the v2.2954 audit proved it).
- **The largest-files inventory in `AI_CONTEXT.md` was two months stale** and inverted: it led with Materials.tsx at 6.9k (now 2.2k) and BidsTakeoffTab at 5.8k (now 2.9k) while missing JobsStagesTab (6.1k) and BidsPricingTab (5.5k); Estimates.tsx had grown from 5.4k to 6.9k. Refreshed to 2026-09-06.
- **Seven links to deleted files**: `BidBoardWeeklySentSection` (folded into the Health section, v2.2215), `CountRow.tsx` (Old Counts view retired, v2.2707), `BidProjectCell` (dead code, v2.1515), `paidSearchChip.ts` (lean search, v2.1825), `BankingUserCardLinkModal` (→ `BankingDebitCardsModal`), `contractSendEmailPreview.ts` (inlined, v2.2773), `EstimateTermsHeaderNotice` (pull-quote, v2.2728). Each now names where the behaviour lives.
- Smaller: `AI_CONTEXT.md` named only one of the two read-only appliers a CREATE TABLE migration must call; the file tree omitted the fragment directories; `RUN_SUBS_PLAN.md` still described `getContactForName` as a planned move (it lives in `stepLifecycleNotifications.ts`); `BILLING_FLOWS.md` named a bulk-delete banner component that is now a hook; a to-do pointed at `src/lib/teamLabor.ts` (it is under `src/utils/`).

## Left alone, on purpose

- 63 backticked paths that do not exist are planned extraction targets in the `*_ARCHITECTURE.md` maps ("Stage A → `src/lib/...`"), template placeholders (`v2.NNNN`, `MyPage.tsx`), gitignored dirs, or a private-repo path. Not drift.
- Architecture maps whose `last_updated` trails their last commit by a month: the 2026-09-05/06 commits were link and rename sweeps, and the line-count inventory is already tracked in `to-dos/engineering-hygiene.md`.

## Verify

`node scripts/…` has no doc linter; the checker lives in the session scratchpad. Re-run by hand: every `[text](path)` in the living docs resolves; `grep -rn "RECENT_FEATURES.md entry" AGENTS.md docs/README.md` returns nothing.
