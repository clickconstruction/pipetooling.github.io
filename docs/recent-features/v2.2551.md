# v2.2551 — Attendance in the write-ups timeline: derived Late rows + 90-day summary (2026-08-31)

Part 2 of "The Late Chip" review (options E + F; part 1 = v2.2550's board chip).
Same principle: **computed, never filed** — one kernel, three renderings.

- **Kernel additions** (`src/lib/scheduleLateness.ts`, +3 tests):
  `latenessLedgerEntries` (per-cell map flattened newest-first),
  `computeAttendanceSummaryForUser` (scheduledDays / clockInDays / onTimeDays /
  lateDays / medianLateMinutes), `fetchScheduleBlocksForRange`.
- **`WriteupsContractsSubTab`** (People → Writeups):
  - loads 90 days of blocks + clock-ins beside the existing writeup/NCNS
    fetches (fail-soft: an error just means no derived rows);
  - `WriteupsTimelineRow` gains `kind: 'late'` — thin rows interleaved by
    work_date: ◔ LATE badge, rounded label + work date, author "clock
    records", status "Derived", the exact receipt in the row title. Included
    under the same filter conditions as NCNS rows, plus a
    **"Show attendance (derived)"** checkbox (default on);
  - when the Subject search narrows to exactly one person, an **Attendance
    summary card** renders above the table (Late n× · median · NCNS n ·
    on-time X of Y scheduled days, "derived from clock records" tag), with an
    **escalation banner** at ≥3 lates in 30 days that opens the write-up
    creator (no subject prefill — the editor doesn't take one yet; candidate
    follow-up along with the mockups' excuse-note annotations, option G).
- Live-verified against prod: Michael A's real NCNS interleaves with derived
  lates; Wendi's card reads Late 47× / median 74m / on time 4 of 51 — which
  also surfaces the known signal caveat: office staff with standing 8:00
  "Office" blocks read as late constantly. Grace/role tuning is a policy knob
  for later; the mockups artifact ("The Late Chip") holds the rationale.
