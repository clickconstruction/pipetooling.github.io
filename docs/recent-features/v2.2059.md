# v2.2059 — Chase card compaction (2026-08-21)

Owner request with screenshot: the chase card ran taller than every neighbor in Today's Money Opportunities. [`PipelineOverview`](../../src/components/jobs/PipelineOverview.tsx) now gives it the grid's standard anatomy — claim · why · button:

- Claim drops the customer count (the amber badge already carries it): "Ask when they'll pay — $101,297".
- The tier chip row is gone; tiers fold into the why line as plain muted text, non-zero only: "11 customers past expected, never asked · 1 broken promise · 2 waiting · 1 dispute".
- Quiet everyone-asked state unchanged. Render smoke updated. Verified live against prod (light + dark).

Client-only — no migration.
