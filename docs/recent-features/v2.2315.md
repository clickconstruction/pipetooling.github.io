# v2.2315 — Data health line items always expanded (2026-08-25)

Owner feedback on v2.2309's tap-to-expand: skip the tapping.

- **Migration `20260826030000`**: `get_payment_line_items_bulk(uuid[])` —
  per-payment line items keyed by id, 200-id cap per call.
- **`PaySpeedDataHealthModal`**: after the transaction list loads, line
  items bulk-fetch in chunks of 150 and every row renders its panel
  permanently (loading placeholder until its chunk lands; "aren't
  available" only after the sweep finishes). The row-click toggle,
  aria-expanded, and hover titles are gone; Exclude / Open job unchanged.
- Kernel `parsePaymentLineItemsBulk` + updated render suite (no-click
  assertions; the expand affordance is asserted absent).
