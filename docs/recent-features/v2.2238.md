# v2.2238 — Payroll pay-reports table: Open | Paid | All segments, defaulting to Open (2026-08-24)

Owner: "I don't like how Hide paid (266) works … have it select by default and make the toggle better." The v2.1795 **Hide paid** button (off by default, session-only, label changed with state) is replaced by a three-segment pill.

- **Control** ([`PeoplePayStubsTab.tsx`](../../src/components/people/PeoplePayStubsTab.tsx)): `Open (N) | Paid (N) | All (N)` segmented group in the header action cluster, styled like the `Pay reports | Ledger` switch. Default **Open** = not fully paid (unpaid + partial, same net-pay math as the rows). Counts follow the person-name search. Session-only state (`paidSegment`), like before — but now the default is the useful view, so nothing needs to persist.
- **Nothing silently missing**: when the segment hides rows, a muted line under the table says `N paid/open reports hidden — show all`. The zero-visible empty states adapt per segment (`All N are paid — show all` / `None … fully paid — show all`).
- **Kernel** [`src/lib/people/ledgerPaidSegments.ts`](../../src/lib/people/ledgerPaidSegments.ts) (+ tests): `paidSegmentCounts`, `filterStubsByPaidSegment`, `hiddenBySegment`. The fully-paid membership set (`ledgerPaidStubIds`) stays in the component.
- Header open-balance summary and the Upcoming payroll line are unchanged — they always describe the whole search-filtered set, not the visible segment.

Client-only; no schema or RPC changes.
