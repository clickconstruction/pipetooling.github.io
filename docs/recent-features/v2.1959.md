# v2.1959 — Jump strip reads the stats spine; Money Opportunities rename (2026-08-20)

Owner-reported: the jump strip ("Waiting (N) → … → Collections (N)") read `stagesBoardLists.*.length` — loaded rows only — so under the v2.1824 scoped first paint, collapsed sections showed (0) until opened, while the section headers below were already correct via the lean stats spine. New kernel [`stagesJumpStrip.ts`](../../src/lib/jobs/stagesJumpStrip.ts) (`stagesJumpStripCount`) applies the same resolution rule as the headers' `sectionHdr`: live rows once the scope is merged (or a search is active), `cacheHeaderStats` count before that, '…' while the first stats load is in flight. All five strip links + aria-labels use it in [`JobsStagesTab`](../../src/components/jobs/JobsStagesTab.tsx); the Collections link's appears-when-nonzero gate now uses the resolved count too, so it shows before the billed scope loads. Since v2.1932 forces a stats refresh on every `loadJobs`, the strip also updates on every move, same as the headers. Rode along: the "Today's money moves" queue header in [`PipelineOverview`](../../src/components/jobs/PipelineOverview.tsx) renamed to "Today's Money Opportunities:" (help guide heading updated). Verified live with all sections collapsed: strip read (20)/(28)/(1)/(64)/(7) matching the headers. Client-only — no migration.
