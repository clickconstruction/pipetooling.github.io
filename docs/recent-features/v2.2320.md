# v2.2320 — Portal recap box itemizes each payment (2026-08-25)

Owner ask on v2.2318: replace the recap's aggregate "Paid to date" line with
the individual payments — date received and amount.

- `portalJobGroups.ts`: groups gain `paymentRowsTotal` (Σ listed payment
  rows) so the renderer can tell whether the rows fully account for the
  cached `totalPaid`.
- `CustomerPortal.tsx` recap box now reads like a little ledger: **Billed to
  date**, then one line per payment ("Paid Jul 31, 2026 · Payment −$6,433.20",
  method quiet, amount PAPER_GREEN), then the ink rule and **Balance on this
  job**. The standalone dashed "Payments received" strip is removed — it
  duplicated the box line for line.
- Honesty fallback: when the rows can't account for the whole total
  (job-remainder bills carry only the cached aggregate, |Σrows − totalPaid| ≥
  0.005) the box keeps the single "Paid to date" line instead of itemizing —
  the on-page arithmetic never lies. Multi-bill unpaid jobs still show
  neither (no "− $0.00" noise).
- Tests: kernel `paymentRowsTotal` assertions; render smokes updated (itemized
  rows + method softening in the box, aggregate fallback, no Paid rows on
  unpaid jobs). Help guide payments section reworded.
