# v2.2290 — Data health drill-down + pay-speed exclusions (2026-08-25)

Owner request: click the Data health strip → a modal on top with all the
transactions, to exclude or go edit.

- **Migration `20260825170000`** (details in `docs/migrations/`): 
  `pay_speed_exclusions` table (payment_id PK, who/when/why, RLS: gate roles
  read, devs+masters insert/delete, both read-only sweeps);
  `get_billed_customer_pay_speeds` **v8** (samples/quarantine/unlinked skip
  excluded payments; quality gains `excluded`); new
  `get_pay_speed_transactions()` — every 12-mo payment with job identity +
  status bucket, plus the all-time undated-bills backlog.
- **Kernel** `src/lib/jobs/paySpeedTransactions.ts` (parse, lens counts,
  filters + tests).
- **`PaySpeedDataHealthModal`**: filter pills (All/Measurable/Unlinked/
  Quarantined/Excluded/Undated bills), search, per-row Exclude ↔ Include
  again (RLS-gated; UI shows for devs+masters via `canExcludePayments`) and
  Open job ›. After a toggle the pay-speeds RPC refetches
  (`refreshBilledPaySpeeds` extracted in JobsStagesTab) so medians update
  live.
- **Strip** is now a button ("see the transactions ›"), shows an excluded
  count when > 0. `PaySpeedQuality` gains `excluded` (0 on pre-v8 payloads).
- Untyped-table/RPC calls use the established `as never` pattern
  (dispatchSwimLanes precedent) — no hand-edit of generated types.
- Mockup: https://claude.ai/code/artifact/010cc1af-5227-4f3e-9c6e-955cdfb46ce5
