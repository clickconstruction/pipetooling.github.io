# v2.1980 — GC certification records: table, status RPC, kernel (2026-08-21)

Phase 1 of the Wednesday certify-and-send ritual (owner-designed; mockups on the "Wednesday GC Certification" canvas). Migration `20260821190000_gc_review_certifications.sql`: append-only `gc_review_certifications` (week_start Monday key, gc_customer_id, certified_by + name snapshot, job_count, total, `snapshot` jsonb of row keys + remainders, note) — office roles insert as themselves, office + primary read; latest row per (week, GC) wins so re-certifying is another insert and history is free. Same migration adds `gc_review_week_status(p_week_start)` (SECURITY DEFINER, role-gated): distinct GCs with outstanding non-collections billed money vs certified vs sent (`gc_statement_emails` in the company-calendar week) — distinct-GC counting only, deliberately none of the client's invoice-bundling row logic. Pure kernel [`gcReviewCertification.ts`](../../src/lib/jobs/gcReviewCertification.ts) (8 tests): Monday week key + weekday in `APP_CALENDAR_TZ`, snapshot build/diff → uncertified / certified / changed-since-certified with dollar delta, weekly progress over real GC groups (No-GC bucket exempt), and the Dashboard nudge state machine (hidden → due Wed+ → done Wed-only). UI ships next (v2.1981 GC Review, then the Dashboard banner).
