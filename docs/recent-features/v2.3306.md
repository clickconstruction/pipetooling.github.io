# v2.3306 — Burn against the bid, PR 5: the backfill list — link the jobs that already match a won bid (2026-09-11)

Client only (calls the v2.3297 `snapshot_job_budget_from_bid`). Rung 5 of [`to-dos/burn-against-the-bid/`](../../to-dos/burn-against-the-bid/README.md) — the last rung of the train; the deferred pair (persist the workbench margin on the bid; earned value by stage) stays in the to-do.

## What shipped

- [`bidJobValueMatches.ts`](../../src/lib/jobs/bidJobValueMatches.ts) (+2 tests): `pairExactValueMatches({ bids, jobs })` — every (unlinked job, won bid) pair within a dollar (agreed value over bid value), ambiguity flagged when a job matches two bids or a bid two jobs; `unambiguousMatches` for Link all.
- [`BidJobBackfillSettingsBlock`](../../src/components/settings/BidJobBackfillSettingsBlock.tsx) on Settings → Data & recovery (dev-only, like the tab): two reads (won bids; unlinked jobs with a price), the paired list with the job's status and value, **Link** per row (confirm) and **Link all N unambiguous** (confirm) → `snapshot_job_budget_from_bid` per pair, then a reload. Settings search: *Link jobs to their bids*.

## Verified

Kernel tests; typecheck; lint. Live on dev against prod: the block read **25 matches · 15 clear · 10 ambiguous** — SPACEX ↔ B375 ($249,715.66), J523 ↔ B66, J372 ↔ B14, J879 ↔ B76, J892 ↔ B118, J650 ↔ B34, J804 ↔ B67, J251 ↔ B12, J258 ↔ B7, J775 ↔ B211, J305 ↔ B3, J843 ↔ B75 … with J363 (B4 or B15), J516 / J864 (B327) and J693 / J724 (B72) flagged ambiguous; no row was linked (the owner's press) — see the PR comment.

## Docs

The to-do's status line (train complete except the deferred pair). Release note `v2.3306.ts`. Dev-only tool — no user guide.
