# v2.1993 — Customer profile v2: jobs list, GC context, activity, public-page slot (2026-08-21)

Owner-approved rebuild of the v2.1322 profile modal (mockup: "Customer Profile v2" canvas). [`CustomerProfileModal`](../../src/components/customers/CustomerProfileModal.tsx): the Jobs pill rail becomes a **money-first list** — new kernel fns in [`customerProfileStats.ts`](../../src/lib/customers/customerProfileStats.ts) (`profileJobRowMoney`, `sortProfileJobsForList`, +5 tests): per-job billed-open on the exact `openBalance` basis (rows + the "+N more · $X open" line reconcile with the money strip by construction), row date/age from est-bill-date **else billed_at** (undated → "billed · no date"), red meta at 90+, unbilled jobs show stage · unbilled with value; sorted billed-open desc → oldest → unbilled desc. Rows link to Job Detail on top (app-level context, keeps the profile open). Header gains a violet "GC on N jobs" chip + "statement last sent" subline ([`fetchCustomerProfile`](../../src/lib/customers/fetchCustomerProfile.ts) adds a `gc_customer_id` count + latest `gc_statement_emails` row, best-effort). New **Recent activity** section merges the newest events across the 4 newest jobs via the role-gated `list_job_activity_events` RPC. Footer reserves a disabled dashed **Public page · soon** button (owner will build the public page last). Mobile-verified at 375px: zero horizontal overflow, rows wrap to two lines. Help guide `see-a-customers-profile.md` updated. Client-only — no migration.
