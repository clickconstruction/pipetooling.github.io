# v2.2296 — Bid tab paste capture: the full ladder (2026-08-25)

Trigger: Burd & Assoc. emailed the complete bid tabs for seven Take 5 bids
(every bidder's number, ours marked "-Click"). The v2.2081 capture keeps only
the four summary numbers; the owner wants full tabs collected for all bids.
Mockup: "Bid Tab Paste Capture" artifact (9335b215).

- **Migration `20260825193140_bid_tab_entries`** (details in
  `docs/migrations/`): one row per bidder amount (`amount`,
  `alternate_amount`, `bidder_name`, `is_ours`), RLS via
  `can_access_bid_for_pricing`. **Summary stays authoritative**: a paste save
  derives and writes `bids.bid_tab_*`, so the Why-we-lost tab analytics,
  Workbench calibration, and tab-gettable queue are untouched.
- **Kernel [`bidTabPaste.ts`](../../src/lib/bids/bidTabPaste.ts)** (+8 tests
  incl. the verbatim Culebra email block): `parseBidTabPaste` ($-amounts or
  whole-line bare numbers — "Take 5" never becomes $5; `(alternate $X)`
  attaches; leftover words = bidder name; first /click/i line auto-flagged
  ours), `deriveTabSummaryFromEntries`, `markEntryOurs`, `buildTabLadder`
  (sorted, ranked, bar-scaled, gap-annotated).
- **[`bidTabEntriesData.ts`](../../src/lib/bids/bidTabEntriesData.ts)**:
  fetch (fail-soft `available:false` pre-migration), replace-wholesale save,
  clear (rides "Remove bid tab").
- **`BidTabCapturePanel`**: "Type the numbers | Paste the tab" pills; paste
  mode shows the parsed ladder with `ours?` correction pills, the derived
  summary line and the existing insight line (our value = the flagged rung).
  `onSave` gains an optional `entries` third arg; `allowPaste={false}` hides
  the mode in the buffered call-session panel. New `BidTabEntriesLadder`
  renders recorded full tabs (OURS highlighted, +$gap on the next rung).
- **Wired in**: Waiting to hear (chase action + ladder under the recorded
  line), Why we lost (saveBidTab + ladder), Call queue (chase + saveTab
  paths). Entries save is fail-soft: summary + history note land, a missing
  table costs only the rungs ("Tab summary saved, but the full bidder list
  could not be stored yet.").
- Verified live in dev on the real BP319 · TAKE 5 - CULEBRA: paste of the
  Burd block parsed 6 rungs, auto-marked ours, derived "#1 of 6 · we were
  the low bid" (by $581), saved summary + note, and surfaced the fail-soft
  message with the table absent.
- Help guide `follow-up-with-builders` documents the paste mode.
