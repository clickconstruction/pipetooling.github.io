# v2.2216 — Add-GC ghosts fixed: version delete cleans its auto-recipient; shared-letter × (2026-08-23)

Owner-reported bug: ＋ Add GC creates a version **and** a `bid_gc_recipients` row (source `'version'`); deleting the version orphaned the recipient, leaving an un-removable "same letter" chip on the Send to strip (seen on BP273 / Achilles).

- [`BidVersionPicker.tsx`](../../src/components/bids/BidVersionPicker.tsx): deleting a version that is the **last** one pointed at its GC also deletes that GC's `bid_gc_recipients` row **where `source = 'version'`** — hand-added (`'manual'`) "Also sent to" records are deliberate and stay.
- Shared-letter chips gain an **×** (`removeSharedLetterGc`): confirm → delete the recipient row (any source), reload, `bid-gc-outcome-changed`. Copy is honest: "Their sends and answers aren't affected — this only removes the record that they got the letter."
- Verified live: full add-GC → delete-version cycle on BP286 leaves zero residue (strip clean, recipients table empty); BP273's orphan removed via the new ×.
