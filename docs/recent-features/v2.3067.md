# v2.3067 — Sub portal: the My statement door, the live address as text, and no more "Null" (2026-09-07)

Three findings from the Tier 5 P1 walk of the Sub Portal (`customer-portal-visit.md` addendum, 2026-09-06). One migration (`20260907140000_my_sub_portal_address.sql`, [doc](../migrations/20260907140000_my_sub_portal_address.md)) — pushed by the agent after merge; `sub-portal` edge function **redeployed** after merge.

- **SP-2 — a signed-in sub reaches their own statement.** New `my_sub_portal_address()` (SECURITY DEFINER, answers only for the caller's roster row) + `useMySubPortalAddress` (+3 tests on the pure URL choice) + a **My statement ↗** door on `DashboardJobModeCard` for subcontractor / helpers; hidden until the office has created the portal. Guide `start-here-as-a-sub` gains the line.
- **SP-1 — the office card shows the live address.** `SubPortalGlobeButton`: once a slug is saved, "Their address" renders `my.clickplumbing.com/<slug>` as text (with a shared / not-yet-shared line) and the editor with the 🎲 tail sits behind **Change address…**; **Copy link** copies the live slug whenever there is one, so the input can never hand out a different tail than what resolves.
- **SP-3 — "Null" in an address.** `cleanPortalAddress` in `_shared/subPortalStatement.ts` (+3 tests) strips standalone `null` / `undefined` tokens and tidies punctuation; applied to sheet cards and Your-days bookings in `sub-portal`.

SP-4 (progress buttons write without a login — by design, the link is the credential) stays as documented.
