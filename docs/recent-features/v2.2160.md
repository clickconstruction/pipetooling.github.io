# v2.2160 — Draft Message CC (2026-08-23)

Owner request (GC Review → Draft Message). Full build (immediate + scheduled), on top of v2.2159's `cc_emails text[]` columns.

- **Kernel [`gcStatementCc.ts`](../../src/lib/gcStatementCc.ts)** (+6 tests): `parseCcEmails(text, to)` — split on comma/semicolon/whitespace, lower-case, validate, dedupe, drop the To address, cap at `GC_STATEMENT_CC_MAX` = 10 (reports `invalid` tokens + `overflow`); `toggleCcEmailInText` / `ccTextIncludes` for the chips.
- **Dialog** ([`JobsGcReviewModal.tsx`](../../src/components/jobs/JobsGcReviewModal.tsx)): a **CC** row under To — the office teammate chips (toggle ✓ in/out of the CC text; the To address's own chip is hidden) plus a free-text field; inline amber note for non-email tokens / overflow; Send and Schedule refuse while a token is invalid. `SendGcStatementPayload.ccEmails`; `buildGcStatementRequestInsert({ ccEmails })` → `cc_emails` (null when empty; +1 test). Share-all dialog unchanged.
- **Edge** — [`send-gc-statement-email`](../EDGE_FUNCTIONS.md#send-gc-statement-email): accepts `cc_emails` (re-validated/capped server-side, never the To), passes Resend `cc`, logs to + cc, audits `gc_statement_emails.cc_emails`. [`gc-statement-email-dispatch`](../EDGE_FUNCTIONS.md#gc-statement-email-dispatch): selects `cc_emails`, passes Resend `cc`, audits, and **carries it onto the next weekly row**. Both redeployed.
- Help: `track-a-general-contractor-on-a-job.md`.

Client + two edge redeploys; migration in v2.2159.
