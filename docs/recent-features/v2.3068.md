# v2.3068 — People → Review keys sub-labor cost by the sheet's job link (2026-09-07)

Conversion train PR 4 — the last row of section D in [`to-dos/sub-sheet-job-link-followups.md`](../../to-dos/sub-sheet-job-link-followups.md).

- [`PeopleReviewTab`](../../src/components/people/PeopleReviewTab.tsx): every `people_labor_jobs` select carries `job_ledger_id`; a module helper `laborRowJobId(row, jobIdByHcp)` reads the link first and the number map only for a sheet with no link. `laborCostByHcp` / `driveCostByHcp` / `personSubLaborCostByHcp` become `…ByJobId` (built after the ledger maps exist, so linked sheets whose job had no crew hours still cost correctly). Linked sheets resolve through `get_jobs_ledger_by_ids[_paid_only]` together with the crew jobs; `get_jobs_ledger_by_hcp_numbers[_paid_only]` is called only for link-less sheets (none in prod since the v2.3055 back-fill). The 2-year lifetime hours pass keys on job id instead of an HCP set.
- [`derivePersonTeamSummary`](../../src/lib/people/derivePersonTeamSummary.ts) + [`teamReviewTypes`](../../src/lib/people/teamReviewTypes.ts): `TeamPeriodLaborRow.job_ledger_id`, `TeamReviewUnion.laborCostByJobId`; the office-job exclusion, paid-only filter and allocation all use the link first. Test: linked sheet with a stale number lands on its job; link-less sheet still resolves by number.

Client-only. No migration.
