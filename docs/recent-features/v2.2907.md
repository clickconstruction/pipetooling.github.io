# v2.2907 — Papercut batch B18: globes show their state, new slugs get a tail, a thumb-sized Approve, one line count on list and rail (2026-09-05)

Journey map Tier-3 **B18** — Estimates list, portals & public pages (J21-F3, J21-F6, J17-F7, J17-F5). Client only; no migration, no edge function. Builds on today's #2593 (globe unminted state), #2615 (Opened N times + `?preview=1`), #2604/#2618 (estimates chips), #2637 (Send confirm).

## Per item

| Item | Finding | Result |
| --- | --- | --- |
| 1 | **Globe tint** — active vs never-minted globes were pixel-identical (J21-F3) | **Done.** One kernel [`src/lib/portal/portalGlobeTint.ts`](../../src/lib/portal/portalGlobeTint.ts) (`portalGlobeTone` / `portalGlobeTint` / `portalGlobeTitle`) maps #14b's `PortalGlobeInitialState` → tone + theme-token colour + tooltip phrase: `unminted` → `var(--text-faint)` "no portal link yet", `active` **and** `legacy-active` → `var(--text-link)` "portal is live", `off` → `var(--text-red-600)` "portal is turned off", not-yet-loaded (`null`) → `var(--text-muted)`. **Both** [`CustomerPortalGlobeButton`](../../src/components/customers/CustomerPortalGlobeButton.tsx) and [`SubPortalGlobeButton`](../../src/components/people/SubPortalGlobeButton.tsx) render from it, through a new shared glyph [`components/shared/PortalGlobeIcon.tsx`](../../src/components/shared/PortalGlobeIcon.tsx) (`currentColor` SVG — the sub globe drops its 🌐 emoji + hue-rotate filter). Each button carries `data-portal-tone`. |
| 2 | **Slug guessability blessed the customer's own name** (J21-F6) | **Done.** New addresses default to name **+ random tail**: `suggestSlugWithTail(name, rng)` in [`portalSlug.ts`](../../src/lib/portal/portalSlug.ts) returns `{ base, slug }`; both modals seed `diceBase` with it so the hero's new 🎲 re-rolls the tail instead of stacking one. `slugGuessabilityDetail(slug, nameSuggestion?)` grades a slug that equals the name-derived suggestion **easy** ("just-their-name") however long it is; reasons `short` / `plain-word` / `just-their-name` / `has-tail` / `composite`, one wording via `slugGuessabilityLabel` in both globes ("⚠ easy to guess — it's just their name", "✓ hard to guess — random tail"). `slugGuessability(slug)` keeps its old verdicts when no name is passed. **Existing saved slugs untouched** — the default only runs when no `customer_portal_slugs` / sub row exists. Minted client-side (the slug is chosen in the modal; the save RPC is unchanged), so nothing to deploy. |
| 3 | **Approve 97×34 px at 375 px** (J17-F7) | **Measured + done.** Re-derived after v2.2728: `primaryBtnStyle` padding `0.5rem 1.25rem` on the 0.875rem / 1.25 line-height button base = 8 + 17.5 + 8 ≈ **34 px** (matches the live measurement). Now `padding: 0.7rem 1.4rem`, `minHeight: 44`, `fontSize: 1rem` in [`EstimateAcceptBody.tsx`](../../src/components/estimates/EstimateAcceptBody.tsx) → **44 px** tall (the sticky-bar variant keeps its tighter padding but inherits the 44 px floor; the modal's Submit acceptance shares the style and grows too). Fixture-measured in Chrome at 375×812 with the app's button base CSS: **before 97×34 px** (reproduces the live J17-F7 figure exactly), **after 108×44 px**; the sticky-bar Accept variant 89×44 px. |
| 4 | **List "1 left: cost lines" vs rail "✓ Line items"** (J17-F5) | **Done.** `countMeaningfulEstimateLines(lines)` exported from [`estimatePipelineRefresh.ts`](../../src/lib/estimatePipelineRefresh.ts) (the existing `lineIsMeaningful` rule on normalized lines; `estimateDraftMeaningfulLineCount` now delegates to it). [`Estimates.tsx`](../../src/pages/Estimates.tsx) `railData` feeds `lineCount: countMeaningfulEstimateLines(lines)` instead of `lines.length` (deps: `lines`). The seeded `$0` "Custom Service Visit" stub now leaves **Line items** at attention on the rail — same verdict as the Pipeline chip. |

**Skipped (done elsewhere / parked / keep):** globe mints only on Share (#14b, v2.2850); staff room views count (#37); "not sent" twice (#20 parked); public-page contact lines (#36 parked); guard copy (keep); Send-to-customer one click (#42 added the confirm); Declined bucket (#34).

## List-level store

[`hooks/usePortalOffStates.ts`](../../src/hooks/usePortalOffStates.ts) and [`useSubPortalOffStates.ts`](../../src/hooks/useSubPortalOffStates.ts) no longer keep a red-only `Set` — they hold the full per-id verdict from the new bulk kernel `computePortalGlobeStates(rows)` in [`portalLinkState.ts`](../../src/lib/portal/portalLinkState.ts) (groups rows, runs `portalGlobeInitialState` per id — so the list globe and the modal it opens can never disagree). API: `usePortalGlobeState(id)` / `setPortalGlobeState(id, state)` (sub: `useSubPortalGlobeState` / `setSubPortalGlobeState`); `null` until the one session query resolves; local updates live in an `overrides` map so a modal action is never clobbered by a late-resolving fetch. `useSubPortalLinkOff` stays as a thin `=== 'off'` shim for Person Desk's compliance line.

## Docs & guides

Help guides `share-a-customer-their-portal.md` / `share-a-sub-their-portal.md` explain the three colours and the tail default; `ACCESS_CONTROL.md` (globe bullet), `PEOPLE_TABS_ARCHITECTURE.md` (Person Desk portal section), `PROJECT_DOCUMENTATION.md` (accept body), `ESTIMATES_TABS_ARCHITECTURE.md` (rail count rule).

## Tests

`portalGlobeTint.test.ts` (every state → tone; active ≠ unminted ≠ off; legacy-active = active; tokens only; titles), `portalLinkState.test.ts` (+`computePortalGlobeStates` agrees with the per-customer verdict; skips id-less rows), `portalSlug.test.ts` (+tail default, base for re-rolls, 60-char cap; reasons incl. the bare name graded easy, name+tail hard, trailing dash), `estimatePipelineRefresh.test.ts` (+`countMeaningfulEstimateLines`), `estimateDraftSteps.test.ts` (+rail ↔ list agreement on the stub: attention / "1 step left: cost lines"; one priced line → done), `CustomerPortalGlobeButton.render.test.tsx` (default address matches `knight-contracting-[tail]`, meter says "random tail", hero 🎲 present).
