# v2.2104 — Workbench collapse-until-plural + the "Try a variant…" door (2026-08-22)

Follow-on to the v2.2013 clarity pass: the owner still found versions/scenarios confusing on solo bids — diagnosis: the two-tier scaffolding renders even when it has nothing to say. Mockup (all three directions picked): claude.ai/code/artifact/038ca133-ccd0-43c0-87f2-92eae0d7a9eb

- **Collapse until plural** (`BidsPricingTab.tsx` Workbench band IIFE): `solo = scenarios.length === 1 && bidVersions.length <= 1` replaces the structure bar + card grid with one line — "One takeoff · one price scenario · ★ customer sees {name} · $rev · margin" (or `No prices yet` + hint), the ☆ button when the sole scenario isn't starred, and the door. Keeps `data-tour="workbench-scenarios"` so the spotlight tour still anchors; tour step 1 copy rewritten to fit both states.
- **The door**: "Try a variant…" (replaces "+ Duplicate scenario" in both states) opens a two-option chooser — 💲 *Same job, different price* → `duplicateWorkbenchScenario()`; 📐 *The plans or scope changed* → dispatches window event `bid-version-picker-open-new`, which `BidVersionPicker` now listens for to open its new-version modal (no prop drilling through Bids.tsx; the picker keeps its own "+ New version" for other tabs).
- **Label hygiene**: legacy shared-pricing fallback card renamed "Current pricing (shared)" → **"Standard prices"**.
- Help guide `price-a-bid-with-the-workbench.md` rewritten around the rule of thumb (*plans changed → new version; price experiment → new scenario*) and the door.

No schema/engine changes; presentation only. Verified live on BP273 (solo legacy bid): line renders, door opens, version option opens the picker's "Split into versions" modal.
