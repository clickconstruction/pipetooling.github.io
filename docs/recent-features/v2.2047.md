# v2.2047 — GC Review unfiltered + one money-truth derivation (2026-08-21)

Owner report: GC Review inherited the Stages search box — a live search narrowed what got certified/sent to a GC. Root cause: the modal read `billedActiveRows`/`collectionsRows` from `stagesBoardLists` (the filtered presentation lists). Client-only.

**Consolidation** ([`JobsStagesTab.tsx`](../../src/components/jobs/JobsStagesTab.tsx)): the file had grown three configurations of `buildJobsStagesBoardLists` — filtered (`stagesBoardLists`), eager-unfiltered (`followupBoardLists`), and lazy-unfiltered (`billedMoneyModalRows`, v2.2035). Now there is exactly one unfiltered derivation, **`unfilteredBoardLists`** (renamed from `followupBoardLists`, moved above its first consumer), carrying the money-never-hides comment (v2.1915) as the single home of the rule: follow-up cards, the chase queue, the aging chart / payment forecast / who-owes-what modals, and **GC Review** all read from it; `billedMoneyModalRows` is deleted (net −1 computation). Any new money surface consumes `unfilteredBoardLists`, never `stagesBoardLists`.

Also: the Section-tools **GC Review menu item's disabled state** now uses unfiltered counts — previously a search matching no billed jobs greyed the item out entirely.

**GC header two-liner** ([`JobsGcReviewModal.tsx`](../../src/components/jobs/JobsGcReviewModal.tsx)): the stats span (`N jobs · $X outstanding · oldest Nd`) moves to a `flexBasis: 100%` second line; line 1 keeps the GC name, the last-sent hint ("last sent Aug 11" = last `gc_statement_emails` send; blue "Sent" pill when within the current certification week), certification chips, and Share.

Verified live with search "dudley" active (board narrowed to 16 billed rows): GC Review showed all 13 GC groups + No-GC, and the two-line headers render.
