# v2.2278 — Goals bar wrap restored + extracted (2026-08-25)

The entire v2.2263 Checklist.tsx changeset (Goals bar wrap, compactTimeAgo
age chips, short fold title) was silently reverted 12 minutes after merge by
v2.2264 (#1969), which was built from a stale checkout. This PR restores all
three and hardens the fragile one:

- **`GoalsStageStrip`** (new, `src/components/checklist/GoalsStageStrip.tsx`):
  the segmented stage bar extracted from `ChecklistOutstandingTab` — flex-wrap
  row, `flex: '1 0 12px'` per segment (grow, no shrink, legible floor),
  `rowGap: 6` clearing the amber current-stage outline. The pre-wrap
  ">40 stages hides numbers" gate is gone: the 12px floor always fits them.
- **`GoalsStageStrip.render.test.tsx`** pins flexWrap + flex-basis/shrink +
  state colors, so the next stale-checkout merge fails CI instead of silently
  shipping the overflow again.
- Restored: `outstandingAgeChip` today-branch (`compactTimeAgo(created_at)` /
  "today" instead of a raw ISO date) and the `Review: completed work` fold
  title.
- Mockup (options considered): https://claude.ai/code/artifact/bdde9680-4fed-4716-8635-11e2bb17f89e
