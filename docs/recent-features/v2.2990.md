# v2.2990 — Takeoffs pills renamed: Old · One at a time · Sheet (2026-09-07)

Owner ask after the 2026-09-07 walkthrough: "they all serve a different purpose, except for Old — Old is just Old." The build-time labels **New 1** / **New 2** (v2.2768) said nothing about what each view does; the hop buttons inside the views and the help-guide titles already used the real names. Client only — no migration, no edge function.

- [`takeoffView.ts`](../../src/lib/bids/takeoffView.ts): `TAKEOFF_VIEWS` labels are now **Old** ("The classic Takeoffs tab — the only view with By Stage"), **One at a time** ("A guided pass, one fixture at a time, with the book and your last bids") and **Sheet** ("The whole sheet, plus the cost rail…"). The ids `old` / `new1` / `new2` are what every device has stored under `bids_takeoff_view_v1`, so they did not change — nobody's remembered view resets. One test pins id → label.
- The By Stage notice reads "One at a time and Sheet work on Combined takeoffs…" and points at the Materials toggle *above* (it sat inside Old before v2.2784); the hop buttons' hover titles drop the "(New 1)" / "(New 2)" tails.
- Guides `cost-a-takeoff-one-fixture-at-a-time` and `see-what-pricing-sees-on-takeoffs` use the new chip labels and name all three pills; keywords keep "new 1" / "new 2" so a search for the old name still lands.
- Living docs (`BIDS_SYSTEM.md`, `docs/README.md`, `to-dos/takeoffs-retire-old.md`, the to-dos index) use the new names; `TAKEOFFS_REFRESH_PLAN.md` keeps its build-time names with a mapping line at the top of Status — the build history and the older fragments still read as written.

## Verify

Bids → Takeoffs → any bid: pills read **Old · One at a time · Sheet**; a device that had New 2 picked opens on Sheet. Settings → Guides → "cost a takeoff one fixture at a time" shows the three chips.
