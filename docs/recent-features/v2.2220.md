# v2.2220 — HR files schema (2026-08-23)

Migration [`20260824025109_person_hr_files.sql`](../migrations/20260824025109_person_hr_files.md): two dev-only tables for per-person HR files — `person_file_entries` (append-only raw log: dated facts with a `source` tag; **no UPDATE/DELETE policies by design**, corrections are new entries) and `person_files` (the two curated docs per person, `kind IN ('summary','narrative')`, `UNIQUE(person_id, kind)`, rewritable). Both keyed on `people.id`, RLS gated on `is_dev()` only. Migration only — the People → HR tab client lands next in the train (`docs/HR_FILES.md` will carry the writing convention).
