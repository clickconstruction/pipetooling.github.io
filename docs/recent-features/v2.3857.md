# v2.3857 — Pipeline: who hears about a returned check is one named rule — Stages Stage-A sweep II, PR 1 (2026-09-26)

Client only; no migration, no edge function, no screen change. Punch list **#46** row 2 (`to-dos/decomposition-queue.md`): the Stages tab's **Stage-A sweep II**, one kernel per PR in the order of the map's candidates table (`docs/JOBS_STAGES_TAB_ARCHITECTURE.md` → Stage-A candidates). This is the first row.

## Why

`JobsStagesTab.tsx` is the most-edited file in the app (198 edits in 90 days). Stage A of its decomposition moved the twelve role gates into `lib/jobs/stagesRoleGates.ts` (v2.3531) with a matrix test that pins every gate for every role — and then v2.3806 (the returned-check badge on the Billed rows) wrote a thirteenth gate inline at the nudge hook: `authRole === 'dev' || authRole === 'master_technician' || isAssistantLike(authRole)`. The same office pool `isStagesOfficeRole` already names, restated.

## What shipped

- **`stagesGates.canSeeBankReturned(role)`** — the office pool (dev · master_technician · assistant · controller): who is told about a deposit the bank returned, the same set that moves jobs between stages, so a returned check reaches whoever can act on the job; off for everyone else, the nudge hook is not armed. Doc comment names both callers' fragments (v2.3795, v2.3806).
- **The matrix row** in `stagesRoleGates.test.ts`: `canSeeBankReturned` admits exactly `OFFICE`.
- **The tab** reads `stagesGates.canSeeBankReturned(authRole)` at `useBankReturnedPaymentsNudge`; the inline chain is gone. Behavior-neutral.
- The map's candidates table strikes the row and notes the Dashboard's `officeEligible` (the same set, folded with the banner and sign-in checks) as a later fold.

## Next in the sweep

Signer fallbacks (`lienToolingSenderFallback` / `lienReleaseSignerFallback` → the existing `lienSigner.ts` kernel — with the map's note that the release modal's fallback reads the release modal's job, to be decided before the swap), then lien focus → Edit Job options, the man-hours folds, the collections note line, round / desk counts, the section header fallback, the deep-link table, the week-dispatch URL.

## Verified

- `stagesRoleGates.test.ts` (the matrix + the new row); eslint; typecheck; `npm test` in full.
