# v2.2222 — Primary dashboard: no dead job-detail door; My Bids gets its card (2026-08-23)

Two owner calls on Bryan's (primary) dashboard, same surface:

- **Recent Reports** ([`DashboardRecentReportsSection.tsx`](../../src/components/dashboard/DashboardRecentReportsSection.tsx)):
  the clickable job name and the **Job detail** chip now render only for non-primary roles —
  primaries reach just their Account-Man jobs (v2.2177 restrictive RLS) while reports span
  every job, so the door dead-ended on "not found" for anything not theirs. Map link and Done
  stay. Render test (`DashboardRecentReportsSection.primary.render.test.tsx`) pins it: primary
  sees plain text, dev keeps the door.
- **My Bids** ([`DashboardMyBidsSection.tsx`](../../src/components/dashboard/DashboardMyBidsSection.tsx)):
  the section sat as a bare ▶ heading between the Recent Reports and Assigned Jobs cards —
  it now wears the same card frame (border, surface, radius, padding), collapse behavior
  unchanged.
