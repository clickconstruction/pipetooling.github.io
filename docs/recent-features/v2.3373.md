# v2.3373 — Pipeline crew names: live accounts, "and N archived", and the crew modal (2026-09-12)

Client only — no migration. Owner (2026-09-12): on Jobs → Pipeline, "instead of Juan, Mario, Jesse, Mike Z, Malachi, Tristen, Trace, Joseph, Michael A, Isiah … Malachi, Tristen, Trace, Michael A, Isiah, and 4 archived", with a modal of everyone and their hours; then, on the mock-up (artifact *Crew Names on Stages*): clicking any name opens the modal, and keep the last-day column. Mission Hills (J523) was the example: ten names, five of them archived accounts (Mario, Jesse, Juan, Joseph, Mike Z); 17 open jobs carried at least one archived name.

## What changed

- **Kernel [`stagesCrew.ts`](../../src/lib/jobs/stagesCrew.ts)** (7 tests): `splitStagesCrew` keeps live accounts in the team list's order and sets archived ones aside (`users.archived_at`); `stagesCrewArchivedTail` words the fold ("and 5 archived", or "5 archived" when nobody is live); `stagesCrewSummary` is the one-line form for the phone sheet; `buildStagesCrewModel` joins the team to the crew-sheet hours by pay name (the breakdown's own key) — live accounts by hours, then archived by hours, each row with hours, worked days and last day — and lists people with hours who are not on the team under `others`.
- **The line** — [`StagesCrewLine`](../../src/components/jobs/StagesCrewLine.tsx) replaces the joined-names div in [`JobsStagesTable`](../../src/components/jobs/JobsStagesTable.tsx) and both places in [`JobsStagesUnifiedTable`](../../src/components/jobs/JobsStagesUnifiedTable.tsx). One dotted-underlined button (the tail in muted italics); it stops propagation so the row's own click stays untouched. Plain text when no opener is provided (outside the Pipeline).
- **The modal** — [`StagesCrewModal`](../../src/components/jobs/StagesCrewModal.tsx): *Everyone on Mission Hills · J523 · 959h total · hours from the crew sheets, the same hours the Costs tab counts*. Fetches `fetchTeamLaborBreakdownForJob` on open (nothing new on the board's load); columns Name (with a bar scaled to the longest), Role (`humanRoleLabel`, hidden on phones), Status (Active · Archived Jul 12 · not on the team), Hours (`h m`), Days, Last day; a grey section *Hours here, not on the crew list*. A name opens the Person desk when the viewer may (`usePersonDeskContext().canOpen`). Escape and the backdrop close it; a phone gets it as a bottom sheet.
- **Opener** — [`StagesCrewModalContext`](../../src/contexts/StagesCrewModalContext.tsx), provided by [`JobsStagesTab`](../../src/components/jobs/JobsStagesTab.tsx) around the board (next to the session-notes opener), which owns `crewModalJob` and mounts the modal with the tab's other modals.
- **Phone cards** — the ⋯ sheet's header reads *Crew: Malachi, Tristen, Trace, Michael A, Isiah, and 5 archived* (same kernel) and gains a **Crew and hours** item after Edit job on both card lists.
- **Data** — `JOBS_LEDGER_TEAM_MEMBERS_EMBED` selects `users(name, archived_at, role)`; `JobWithDetails.team_members[].users` carries the two optional fields.

## Decisions folded in

Archived = `users.archived_at` (benched subs with a live login stay listed by name). Visible names keep the team list's order. Hours are the Costs tab's team hours so the modal total matches the row's 🕒 line. Every Pipeline role sees the link and the modal — hours, not dollars.

## Guides

*ready to bill (Pipeline)* — the Crew & Dates paragraph and the ⋯ sheet list.
