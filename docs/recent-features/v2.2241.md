# v2.2241 — Pay-speed RPC uses the board's bill clock (2026-08-24)

Billing Truth Plan Phase 2c. `get_billed_customer_pay_speeds` v4 (migration `20260824185156`): samples and receipts now date bills by `COALESCE(billed_at AT TZ Chicago, estimated_bill_date)` — the exact rule the board's aging/forecast chips already use (`billedReferenceYmd` / `stageRowBilledAgeReference`). v3 demanded `billed_at`, so a hand-dated bill could age on the board while its payments never fed the medians (11 such payments pre-backfill). No client changes — payload shape unchanged.
