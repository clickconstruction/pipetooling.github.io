# v2.3051 — Quickfill: Unassigned field time becomes a count card that links to the Team board (2026-09-07)

Owner decision after the Jobs → Team train ("I want this done"): the station's day list duplicated the Team board's **No job on the session** row and its exceptions list, and its per-row **Open day audit** was a second, older fix path beside the board's Link / Pick job / Split day.

- **Card**: [`QuickfillUnassignedFieldTimeSection`](../../src/components/quickfill/QuickfillUnassignedFieldTimeSection.tsx) keeps the same loads and the same kernel (`computeUnallocatedFieldRows`, approved-clock-only) but renders a count — *N person-days · X h not on a job across P people · last 14 days* — and one line per company week (`unassignedWeekCards` in [`unassignedFieldTimeCard.ts`](../../src/lib/unassignedFieldTimeCard.ts): rows/hours/people + the newest work date). Window and threshold selects stay. Zero state: **All on a job** with a plain **Open Team board →** link. The `MatchClockSessionsInline` block still renders under the card. The day-group table, the per-row day audit and the crew-map state are gone from this file; the day audit stays on People → Hours.
- **Deep link**: `teamBoardHref({ week, onlyExceptions, jobId })` builds `/jobs?tab=combined-labor&teamWeek=YYYY-MM-DD&teamExceptions=1`. [`JobsTeamTab`](../../src/components/jobs/JobsTeamTab.tsx) takes `focusWeek` / `focusExceptions`: it moves to the company week containing the date, ticks **Only exceptions**, and calls `onFocusConsumed`; [`Jobs.tsx`](../../src/pages/Jobs.tsx) reads both params and clears them together with `teamLaborJob`.
- **Tests**: kernel (`unassignedFieldTimeCard.test.ts`, 5) + render smoke (`QuickfillUnassignedFieldTimeSection.render.test.tsx`: count, week line, href, no "Open day audit").
- **Docs**: guides `quickfill`, `put-clocked-hours-on-the-right-job`, `read-the-team-board`; ACCESS_CONTROL row; GLOSSARY entry. Client-only — no migration. The section id, mark, close-week chip and count badge are unchanged.
