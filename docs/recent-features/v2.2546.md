# v2.2546 — NCNS chip: hub-panel clickability + solid fill on buttons (2026-08-31)

Two fixes surfaced by the first live NCNS run (Michael A, 2026-08-31 —
owner-directed test of v2.2540):

- **`ScheduleDispatchHub.tsx` has its own two chip render sites** (the People
  panel) beside `ScheduleDispatchGrid.tsx`'s two — v2.2540 only widened the
  grid's `variant === 'not_coming_in'` onClick gate to include `ncns`. The hub
  sites now match (click opens the undo confirm with the incident-stays copy).
- **`ScheduleDispatchTimeOffChip`**: the button branch spread
  `buttonResetStyle` (with `background: transparent`) AFTER the variant style,
  so a clickable solid-red NCNS chip rendered white-on-transparent — an empty
  outline. Reset now spreads first, variant second; this also restores the
  intended tinted background on interactive "Not coming in" chips.

Verified live end-to-end: NCNS recorded from the picker footer (incident row
in `attendance_incidents` with details + `scheduled_without_clock`, block
removed, day off with the NCNS note), solid red chip, undo modal shows the
incident-stays copy.
