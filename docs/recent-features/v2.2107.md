# v2.2107 — Partnerships Job review: automatic threshold (2026-08-22)

Owner request (mockup artifact c4c90ecd, approved shape): a bottom-of-tab "automatic threshold" — when the partner's share of a job's approved labor hours reaches N%, the job is confirmed and made visible automatically. Migration `20260822215500_partner_auto_threshold.sql`: `partnerships.auto_threshold_pct` (1–100, NULL = off), `jobs_ledger.partner_confirmed_auto_pct` (the honest stamp — an auto-add records the threshold it fired at, never a person) and `jobs_ledger.partner_auto_exempt_at` (stamped on every explicit clear; **the rule never re-adds a human-cleared job**, enforced in the RPC; a manual re-confirm clears it). `set_job_partner_majority` replaced with a 3-arg version (`p_auto_pct`; old 2-arg dropped — PostgREST named-arg calls from the old client still resolve); auto calls silently skip exempted or already-assigned jobs. `get_partner_job_review_queue` rows gain `confirmed_auto_pct` + `auto_exempt`.

Client ([`PartnershipJobReviewTab.tsx`](../../src/components/partnerships/PartnershipJobReviewTab.tsx)): the rule fires on tab load (the moment shares are computed — shares only move when the office approves sessions), stamps each add, logs one `partnership_events` `config_changed` row (`patch.auto_confirmed` with jobs + shares), and re-fetches so rows render confirmed. Bottom control replaces the static footnote: off → "Set a threshold…" button; editor → 50/60/75 preset chips + number input + live "Right now this would add …" preview (via the same kernel fn); on → purple `≥ N% · k jobs auto-added` chip + Change/Turn off. Auto rows read `confirmed <date> [auto ≥ N%] · visible` (purple chip, `#7c3aed` house accent); qualifying-but-exempt rows read "turned off by hand — won't auto-add". Threshold saves log `config_changed` and reload (firing immediately). Fail-soft: pre-migration the column select errors and the control hides. Kernel: [`jobReviewQueue.ts`](../../src/lib/partnerLedger/jobReviewQueue.ts) gains `isValidThreshold` + `jobsToAutoConfirm` (+ parse fields, 12 tests). Turning the rule off stops future adds; already-added jobs stay until individually cleared.
