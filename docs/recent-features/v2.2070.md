# v2.2070 — Assigned Jobs follow-ups: session-scoped collapse + no Send to Billing for field crew (2026-08-22)

Owner follow-ups on the v2.2067 mobile-card review of Abraham's dashboard:

- **Session-scoped collapse**: [`DashboardGroupCard`](../../src/components/dashboard/DashboardGroupCard.tsx) gains a `sessionScoped` prop — expand/collapse persists in `sessionStorage` instead of `localStorage`, so it survives reloads but every fresh app launch starts back at `defaultCollapsed`. Assigned Jobs opts in; all other group cards keep their sticky localStorage behavior.
- **Send to Billing removed for sub-like roles** (subcontractor + helpers) in [`DashboardAssignedJobsSection`](../../src/components/dashboard/DashboardAssignedJobsSection.tsx), both mobile-card and desktop branches: the 100% report flow already offers Working → Ready to Bill with the same two attestations (MarkJobReadyToBillPrompt), so the second door only added confusion. Office roles (dev/master/assistant-like/primary) and superintendents are unchanged. The guard is `!isSubcontractorLikeRole(role)` (previously `role !== 'helpers'`).
- Render smokes updated: sub/helpers assert the button's absence, a new office case pins its presence, and the collapse seed moved to `sessionStorage`.

Client-only — no migration.
