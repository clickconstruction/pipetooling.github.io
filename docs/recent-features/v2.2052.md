# v2.2052 — Call-lens bid pills switch to project names (2026-08-21)

Owner report (Why-we-lost pills showed street fragments — three identical "s. access rd." on one PlanHub card; Waiting-to-hear had six "S SAGINAW BLVD"; mockup artifact 60b49ceb). New shared kernel [`bidTriagePillLabel`](../../src/lib/bids/bidFormatting.ts) (+2 tests): project name first (trimmed, 26-char ellipsis), street fallback for unnamed bids (old behavior), ledger label last. Replaces the two copy-pasted `streetPillLabel` helpers in [`BidsWhyWeLostLens`](../../src/components/bids/BidsWhyWeLostLens.tsx) and [`BidsWaitingToHearLens`](../../src/components/bids/BidsWaitingToHearLens.tsx); both pills' tooltips now carry `label · project — address`. ✓/active pill states and tap behavior untouched; bids sharing a project name (revision sets) disambiguate via the tooltip. Verified live: the PlanHub card reads "Take 5 Dickinson / Crunch Fitness Potranco / HAWAIIAN BROS #142…" and Knight's SAGINAW row became "Jakes Split Rail…". Help guide's pill sentence updated. Client-only — no migration.
