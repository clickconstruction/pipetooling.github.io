# v2.3859 — Lien screens: every door into Edit Job opens on the same rule — Stages Stage-A sweep II, PR 3 (2026-09-26)

Client only; no migration, no edge function, no screen change. Punch list **#46** row 2, the Stages tab's Stage-A sweep II, one kernel per PR (`docs/JOBS_STAGES_TAB_ARCHITECTURE.md` → Stage-A candidates); PRs 1–2 were [v2.3857](v2.3857.md) and [v2.3858](v2.3858.md).

## Why

Two lien screens hand the Stages tab a door into Edit Job with a *focus* — the Lien desk (v2.3697, the plain-value doors) and Put a GC on notice (v2.3819, the jobs band's chips) — and the tab mapped that focus onto the Job window's options in two inline ternaries that had already diverged: the desk's knew three focuses, the GC run's seven. The next door added to one would have landed differently on the other.

## What shipped

- **[`src/lib/jobs/lienFocusEditJobOptions.ts`](../../src/lib/jobs/lienFocusEditJobOptions.ts)** — `lienFocusEditJobOptions(focus)`: `property-record` → the Property record row; `gc` / `lien-contract` / `status` → that fact row ringed; `pct` → the Bill tab with the % done field ringed; `line-items` → the Bill tab with the line items flashed; `bill` → the Bill tab; nothing → the window as it is. `LienDoorFocus` is the union both desks' doors draw from; the return type is a structural slice of `OpenEditJobOptions` so the kernel stays out of the context module.
- **Both sites** in the tab spread `lienFocusEditJobOptions(focus)` into `tryOpenEditJob` beside `onSaved`; the two ternaries are gone. The desk's three focuses map exactly as before; the GC run's seven map exactly as before.
- **Tests** — `lienFocusEditJobOptions.test.ts` (5): each focus, and no focus.
- The map strikes the row; the desk's narrower `onOpenEditJob` prop type is noted as a one-line follow-up when the desk gains a door.

## Next in the sweep

The man-hours folds (`stagesManHoursByJobId` / `stagesLaborBreakdownByJobId` → `lib/jobs/stagesManHours.ts` + tests), then the collections note line.

## Verified

- `lienFocusEditJobOptions.test.ts` (5); eslint; typecheck; `npm test` in full.
