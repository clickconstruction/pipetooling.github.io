# v2.2066 — Manage view pills: All / One-offs / Repeating / Completed (2026-08-22)

Owner report: "I see one-offs, but I don't see recurring." Diagnosis: no data
problem (9 clean `day_of_week` items) — the REPEATING section simply renders
below all 28 open one-offs, under a wall of scroll.

- **View pills** under the Manage toolbar (`manageView` state, default `all`):
  All · One-offs (count) · ↻ Repeating (count) · ✓ Completed (count). Each
  non-All pill renders just its section; Completed renders unfolded (the
  in-All fold stays). Same pill grammar as Review's range filter. Search and
  the person filter compose with any view.
- **"next" chip** on repeating rows: green `next Mon, Aug 24` (or `due
  today`) from `nextOccurrenceLabel()` (added to `checklistManageGroups.ts`,
  tested) over a `nextOpenByItem` map built from the instances Manage already
  loads — no new queries. Rows with no upcoming open occurrence show no chip
  (the nightly top-up backfills those).
- Mockup artifact: "Manage Tab Views". Verified live in dark mode (evening):
  all four views, counts 28/9/83, next-chips rendering.

Help: `schedule-a-task-or-make-it-repeat.md` gains a pointer.
