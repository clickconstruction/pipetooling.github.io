# v2.1907 — Partnerships nav moves to the gear dropdown (2026-08-20)

Owner request (header icon strip getting crowded): the dev-only Partnerships entry leaves the header icon strip and the hamburger dropdown, and lands in the **gear menu directly above Documents** — labeled entry with the briefcase icon, `role === 'dev' && !farmModeActive` gated, same NavLink pattern as its neighbors. All three previous placements removed from [`Layout.tsx`](../../src/components/Layout.tsx); the route and page are unchanged. Client-only — no migration.
