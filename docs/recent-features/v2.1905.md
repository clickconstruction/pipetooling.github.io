# v2.1905 — Draft editor: the Customer section joins the left edge (2026-08-20)

Owner feedback on the shipped section delineation: the ① Customer block sat centered above the paper while every other section is left-aligned — the odd one out. Its wrapper's `alignItems: 'center'` becomes `'flex-start'` in [`Estimates.tsx`](../../src/pages/Estimates.tsx), so the picker (and its selected-customer card) hugs the same left edge as the document sections below. One line, editing mode only. Client-only — no migration.
