# v2.2760 — What customers see, PR 2: the portals and the contract page go live (2026-09-04)

Completes v2.2758. Every step in the three journeys now renders; no "Next release" cards remain.

- **Customer portal** — `/portal?t=sample` (Sam Sample: one fresh bill with a Pay button, one partly paid, a signed agreement, the request forms, the short-address card) and `/portal?t=sample-gc` (Sample Contracting: the properties they GC, **AS GC** tags with owner names). `customer-portal` gains the sample branch: office JWT required (`_shared/sampleViewer.ts`), fixture from `_shared/customerSampleFixtures.ts` over the live `PORTAL_COMPANY` letterhead; no link, no slug lock, no `public_page_views` row. The request forms flip to *sent* without posting.
- **Sub portal** — `/sub?t=sample` (Sam's Plumbing LLC: two sheets, the payment ledger, an open offer, paperwork with an agreement to sign and a COI expiring in 27 days). `sub-portal` reads the live pay-run settings (`sub_pay_run_day`, `sub_pay_explainer`) and lays them over the fixture. Sign-to-accept and the availability form short-circuit; **Sign** on the agreement opens the sample contract page.
- **Contract signing page** — `/contract/accept?t=sample` (the sample subcontractor agreement, HTML body) and `sample-done` (the 409 the thank-you reads). `get-contract-for-signer` gains the sample branch; no `signer_last_viewed_at` stamp. Signing moves straight to the thank-you.
- **Journeys**: Homeowner ends on the portal; GC ends on the portal as GC; Subcontractor is *Portal link, texted* (external — staff type the message) → Sub portal → Contract to sign → Signed. New sample state `gc` (`SAMPLE_TOKEN_GC`), `SAMPLE_CONTRACT` in the fixture.
- The dev-only `/sub?demo=1` fixture stays as the design mock's stable target; the sample is the customer-facing one.
