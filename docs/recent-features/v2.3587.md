# v2.3587 — Contract sweep: Found in Drive opens to the office set; the to-do closes (2026-09-18)

Client + docs. No migration, no edge function change. Closes [`to-dos/contract-sweep-seen-first/`](https://github.com/clickconstruction/pipetooling.github.io/commits/main/to-dos/contract-sweep-seen-first) (v2.3384–v2.3393), the last open step of that train.

## Why

The Drive pass shipped dev-run (v2.3390) "until it has been right a few times". The first live pass (2026-09-14) filed two contracts and taught the matcher its two fixes (v2.3392 the folder chain and customer-name words, v2.3393 a bare street number is not a job number). The owner's 2026-09-18 clear-out of the punch list took the row: open the door, delete the to-do.

## The change

- [`JobsContractSweepModal.tsx`](../../src/components/jobs/JobsContractSweepModal.tsx): the ⋯ menu's *Look in Drive for signed contracts…* item is gated on `DRIVE_PASS_ROLES` — dev, master_technician, assistant, controller — instead of `authRole === 'dev'`. The set mirrors `officeRoles` in `supabase/functions/drive-contract-scan/index.ts`, which already admitted those roles server-side; the client gate was the only thing narrower.
- [`DriveContractsFoundModal.tsx`](../../src/components/jobs/DriveContractsFoundModal.tsx): the header comment records the opening.
- Guide *get a job contract signed*: "A dev can run" → "Anyone in the office can run".
- `to-dos/contract-sweep-seen-first/` deleted (README + `mockup.html`; both live in git history at the deleting commit). The customer-level (master) agreements decision it raised stays on `to-dos/owner-decisions-pending.md`, whose source pointer now names `docs/recent-features/v2.3392.md`; `to-dos/contract-paper-lane/README.md` says the same. Board and index regenerated.

## Verified

- Typecheck, lint, the help-guide and release-notes suites, `npm run check:todo-drift`.
- Dev server, signed in as dev: the sweep's ⋯ still lists the Drive item (dev is in the set); the guide's line reads the new wording.
