# v2.2622 — Share ▾ menu: Division 22 codes as a child of Supply house list (2026-09-01)

Owner pick (option A of canvas artboard 8, over section-group / inline-link
variants): `PricingShareMenu` items gain `childOfPrevious` — an elbow SVG
connector (stroked `var(--border-strong)`, dark-mode safe), extra left
indent, slightly smaller label, and no divider between parent and child.
Applied to the `d22audit` item under `fixtures`. No behavior change; render
tests unchanged and green.
