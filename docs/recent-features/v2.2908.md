# v2.2908 — Papercut batch B16: human names in Recently deleted, merge reasons shown, clear impersonation exit, paged role-aware release notes, Templates in the nav, bid rooms join the archive (2026-09-05)

Journey map Tier-3 batch **B16 — Settings, admin & Layout** (`pipetooling-journey-map` → `docs/JOURNEY_MAP.md`). One migration (`20260906000000_bid_room_tables_deleted_records.sql`, **`supabase db push` after merge**); no edge-function change. Layout is touched only for the impersonation exit button — the sibling B19 PR owns Settings search + pins.

## Per item

| # | Finding | Status | What shipped |
|---|---|---|---|
| 1 | J9-F6 (C40) — Recently-deleted type filter shows raw table names | **done** | New `describeDeletedBundleKind(kind)` in [`deletedRecordContents.ts`](../../src/lib/deletedRecordContents.ts): one map from the RPC's `kind` word (`job`, `bid`, `pay stub`, `partial`, or a raw head table) to a capitalized label ("Job", "Pay report", "Part of a job or bid", "Job invoices"); raw tables route through `humanizeArchiveTable`, which learned `jobs_ledger_invoices` and the three bid-room tables. Used by the filter `<option>`s and the per-bundle `(kind)` tag in [`DeletedRecordsSection.tsx`](../../src/components/settings/DeletedRecordsSection.tsx). |
| 2 | J27-F4 (C79) — merge-users picker silently omits ineligible accounts | **done** | New `ineligibleAbsorbCandidates(survivor, accounts)` in [`mergeUserAccounts.ts`](../../src/lib/mergeUserAccounts.ts) returns every non-survivor the dropdown leaves out with its `mergeIneligibilityReason` (which now prints human role labels — "Subcontractor vs Estimator" — via `humanRoleLabel`). The Merge users modal in [`ActiveAccountsPanel.tsx`](../../src/components/settings/ActiveAccountsPanel.tsx) renders them in a collapsed `<details>` "Why isn't an account listed? (N left out)" under the merge-away select. The Person Desk's **Manage account…** opens this same modal, so both doors show the reasons. |
| 3 | J26-F6 (C72) — impersonation exit reads as the person's name | **done** | [`impersonationUiLabels.ts`](../../src/lib/impersonationUiLabels.ts): `impersonationExitDisplayLabel` → "Exit impersonation (Bryan)" (email local-part fallback; bare "Exit impersonation" with no identity), `{ compact: true }` → "Exit (Bryan)" for the narrow header; `impersonationExitTitle` → "Stop viewing as … and go back to your own account". Layout passes `compact: isMobile` and widens the button (8.5rem / 18rem); the Settings banner button drops its `=== 'Back'` special case. New `impersonationUiLabels.test.ts`. |
| 4 | J28-F5 · J28-F12 — release notes render ~1,800 blocks in one commit, identical for every role | **done** | [`releaseNotes.ts`](../../src/lib/releaseNotes.ts): optional `roles?: UserRole[]` on `ReleaseNote` (validated: non-empty, known, unique), `releaseNoteVisibleToRole` / `releaseNotesForRole` (no roles = everyone; devs and a not-yet-loaded role see all), `releaseNotesPage(total, shown, 15)` + `describeEarlierUpdatesButton` ("Show 15 earlier updates (1,770 more)" / "Show the last 2 updates"). [`SettingsReleaseNotesSection`](../../src/components/settings/SettingsReleaseNotesSection.tsx) takes a `role` prop (Settings passes `myRole`), pages 15 per click, and says "Showing the N notes that apply to your role (M for other roles hidden)" when the filter bites. Existing notes declare no roles, so every feed is unchanged until a PR opts in. Render test rewritten for the pager. |
| 5 | J28-F14 — `/templates` has no nav entry | **done** | Settings → **Email templates & testing** (dev-only, where the prompt placed it) opens with a "Project workflow templates" block and an **Open Templates →** link ([`SettingsTemplatesTab.tsx`](../../src/components/settings/SettingsTemplatesTab.tsx)). The page itself is dev-only; `/templates` was already pinnable. Nothing in Settings search / `settingsGroups.ts` changed (B19's surface). |
| 6 | J24-F5 (C39) — "ask a dev to resend" | **done** | [`AcceptInvite.tsx`](../../src/pages/AcceptInvite.tsx) `INVALID_LINK`: "Ask a dev to run Invite via email again for your address — that replaces this link with a fresh one." Matches the invite guide's existing "there is no separate resend button" line. |
| 7 | J35-N1 (C73) — bid-room tables outside the deleted-records archive | **done (migration)** | `20260906000000_bid_room_tables_deleted_records.sql` adds `zzz_archive_on_delete` to `bid_proposal_rooms` (group key `bid_id`), `bid_proposal_room_revisions` and `bid_proposal_room_events` (group key `room_id`). Restoring a bid chains bid → room → revisions/events through the restore's `group_key = record_id` recursion; a room deleted on its own lists as a bid-keyed "partial" bundle. No new table → no read-only-block footers. |
| — | J27-F5 probe-code landing | skipped | rides Tier-2 #17 |
| — | J24-F9 Active Accounts role confirm | skipped | rides Tier-2 #39 |
| — | J24-N2 invitation template brand | skipped | data edit in Settings → Templates (Will) |

## Tests

`deletedRecordContents.test.ts` (kind labels), `mergeUserAccounts.test.ts` (ineligible list, partition with the eligible list, human role labels), `impersonationUiLabels.test.ts` (new), `releaseNotes.test.ts` (role filter, roles validation, pager arithmetic and copy), `SettingsReleaseNotesSection.render.test.tsx` (pager + role hide).

## Docs

`docs/migrations/20260906000000_bid_room_tables_deleted_records.md`, `PROJECT_DOCUMENTATION.md` §19 (archive coverage), `SETTINGS_TABS_ARCHITECTURE.md` (§8 Templates link, §11 pager/role props), help guides `release-notes.md`, `merge-user-accounts.md`, `recover-a-deleted-job.md`.
