# v2.3620 — Digital twins: the app directory is checked against the app in CI (2026-09-19)

Item 5 of [`to-dos/robots-residuals.md`](../../to-dos/robots-residuals.md) — the Phase 4 upkeep [`docs/DIGITAL_TWINS_PLAN.md`](../DIGITAL_TWINS_PLAN.md) asked for. Test only; no runtime change.

## The change

- **Kernel** — [`lib/twins/appDirectoryCheck.ts`](../../src/lib/twins/appDirectoryCheck.ts): `appRoutePathsFromSource` (every `<Route path>` in `src/App.tsx`, nested ones made absolute, the catch-all skipped), `docPathsFromMarkdown` (backticked `/…` paths, base path only, `<placeholder>` forms skipped), `helpSlugsFromMarkdown` (`help?g=<slug>`, real slugs only), `pathMatchesRoute` (`/customers/abc` ↔ `/customers/:id`), `directoryProblems` (one line per drift).
- **Test** — [`appDirectoryCheck.test.ts`](../../src/lib/twins/appDirectoryCheck.test.ts) (6): the parsers on fixtures, then the real thing — every `docs/twins/*.md` against the real route table and `src/content/help/`. Green today: 19 distinct paths in `APP_DIRECTORY.md`, 13 in `estimator.md`, every one a route; one help slug, a real guide.
- **The rule** — `CLAUDE.md`: a PR that adds, renames or removes a page updates `docs/twins/APP_DIRECTORY.md`, and this test says when it did not.
