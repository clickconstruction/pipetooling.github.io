# v2.2487 — Needs You: job follow-ups item (banner migration 1/6) (2026-08-29)

First of the v2.2339 follow-ups: the self-gating dashboard banners fold into
the Needs You card one small PR at a time. This one migrates
`DashboardJobFollowupsBanner` (v2.1720).

- **New hook `src/hooks/useJobFollowupNudge.ts`**: the banner's inline
  fetch+compute (candidates/reviews/settings → `computeJobFollowupQueue`)
  extracted; returns `{ count, stageCounts }` — null while loading, 0 on error,
  matching the banner's gating exactly.
- **`jobFollowupBreakdownPhrase`** moved into `src/lib/jobs/jobFollowupQueue.ts`
  (was banner-local STAGE_PHRASES) so the card item and the banner share it.
- **Kernel `dashboardNeedsYou.ts`**: new inputs
  (`jobFollowupsEnabled/Count/StageCounts`) + item key `job-followups`
  (amber, kicker "Quiet jobs", action "Start review" →
  `/jobs?tab=stages&followups=1`), appended after `lost-bids`. Order note:
  later migrations (team reviews, roadmap) insert BEFORE it to preserve the
  old banner-stack order.
- **`DashboardPinnedQuickRow`**: banner render removed; hook enabled for
  office roles (same mount gate); shared `officeEligible` const.
- **Quickfill**: `QuickfillNeedsYouSection` passes `jobFollowupsEnabled: false`
  — its dedicated Job follow-ups station (v2.2347) already carries this queue,
  so the card doesn't double-list it there. The banner component survives for
  that station (now hook-backed).
- Tests: 2 new kernel tests (breakdown copy, office gate, singular/no-breakdown).
- Help guide `needs-you-card.md` updated (quiet jobs in the list + actions).
