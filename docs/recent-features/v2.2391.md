# v2.2391 — Lone-version Alternate disabled with a teaching tooltip (2026-08-27)

Wendi: "When I flip between Base and Alternate … I don't think I see anything
change. What is supposed to happen?" With one version in the letter, nothing —
`planSamePageLetter`'s no-$0-letters fallback promotes the first alternate to
lead when no base exists, so the document renders identically either way.

`BidsCoverLetterTab.tsx` (versions list): `loneInLetter` = this row is the
only `include_in_submission` version in scope (per-GC on multi-GC bids). When
lone and currently Base, the Alternate segment is disabled with the tooltip
"An alternate is offered in lieu of a base — add another bid to send first."
A version already saved as a lone alternate keeps both segments live so Base
is one click away (no silent writes). No letter-planning changes.
