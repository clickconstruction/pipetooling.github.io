# v2.2664 — Email catalog: the § 53.056 notice email registered (2026-09-02)

Gap found while answering "will the new letterhead show in Settings email
previews?": the v2.2656 email-catalog inventory and the v2.2645 lien notice
email crossed in flight, so `send-lien-filing-email` had no registry row.

- `src/lib/emailCatalog.ts`: new `lien_filing_notice` row (group `lien`,
  customer audience, "§ 53.056 notice PDF" attachment, builtWhere `server`,
  sender `send-lien-filing-email`, hardcoded wording until the wording plan
  reaches it; owner/original-contractor recipients folded as variants). The
  `lien` group label renamed "Lien releases" → "Lien paperwork".
- `supabase/functions/send-lien-filing-email/index.ts`: stamps
  `emailType: 'lien_filing_notice'` into the shared email log so per-type
  send stats group these sends (redeployed).
- `emailCatalog.test.ts`: count 32 → 33 + a shape assertion for the new row.
