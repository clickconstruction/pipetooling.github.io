# v2.2912 — Crew P&L papercut batch B8: real rows sort first and estimates say so, one person stays one row, cached figures announce themselves (2026-09-05)

Journey-map Phase 4, Tier-3 row B8 (J8 crew-profitability dossier: J8-F1 / J8-N1, J8-F2, J8-F5; J8-D1 doc drift fixed in passing). **Client only — no migration, no edge function.** Items already shipped and skipped here: the sub $/hr blur-save (J8-F3, Tier-2 #42, v2.2897) and the "Billed (gross)" label (J8-F4, #5b/c, v2.2852). Deliberately not touched: office rows reading negative by construction (J8-F6 — Tier-4 teach).

## Per item

1. **Done — default sorts ranked ≈-estimated rows first (J8-F1 / J8-N1).** `src/lib/crewPnlSummary.ts`: each row now carries `fallbackBilling` (dollars from `billing-fallback` lines) and `estimateLed` = `crewPnlRowIsEstimateLed(fallbackBilling, billing)` (guesses ≥ half the billing — covers the "no hours, no cost, six-figure profit" row and the one-fallback-job outlier at ≈7.5× the next $/hr; sub ≈ equivalent hours is a calibration and never demotes). New exported `compareCrewPnlRows(a, b, key, direction)` with `CrewPnlSortKey`: every numeric key keeps real rows ahead of estimate-led rows in both directions, then the key, then name A→Z; the Name sort is plain alphabetical. The kernel's default order uses it (so `PersonMoneyLedgerModal`'s lookup is unaffected — it finds by name). `JobsCrewPnlTab.tsx` drops its private `sortValue`, sorts with the comparator, badges estimate-led rows "≈ estimated" (amber, tooltip explains the equal split and the ranking) and inserts one amber divider row at the first estimate-led row in numeric sorts. Legend sentence added.
2. **Done — unmatched free-text badge split one person (J8-F2).** `resolveCrewPnlNameLoosely(name, people)` + `looseCrewPnlName` in the kernel: after the exact trim/lower miss, loose equality (NFD-stripped diacritics, punctuation → space, collapsed whitespace), then token containment with ≥2 tokens on the shorter side and single-letter initials ("Jose Garcia" / "Garcia, Jose" / "J. Garcia" ⊆ "José Luis García"; the free text may be the longer side — "Mike Z (sub)"). Accepted only on exactly one candidate; two roster hits ("Tom Reed" vs Sr/Jr) or a lone first name stay free text → `unmatched`. Memoized per normalized name inside `buildCrewPnlPersonResolver`. The unmatched tooltip now says what to do (fix the spelling at the source or add the person to People).
3. **Done — silent fallback to the partial cache (J8-F5).** New kernel `src/lib/jobs/crewPnlJobsUniverse.ts`: `crewPnlJobsUniverseNotice(state, formatTime)` → null when complete; muted "Loading the complete job list — the figures below come from the page cache…" while loading; amber "Showing cached figures from <time> — the complete job list didn't load (<reason>), so these rows use N cached jobs and paid jobs may be missing. Billed and Profit can read low. Refresh to retry." after a page error. The tab tracks `jobsUniverse` state next to `allJobs`, reads the cached count at render (tracks Stages' expand-to-load), and the Refresh button re-runs the paginated load. `role="status"`, theme tokens (`--bg-amber-tint` / `--border-amber` / `--text-amber-800`).
4. **Skipped — "Billing" is gross / office rows negative (J8-F4 / J8-F6):** #5c shipped the label; the office-row explanation is a Tier-4 guide item.

## Tests

`crewPnlSummary.test.ts` +12: `looseCrewPnlName`, loose equality, containment/reorder/initial, ambiguity stays unmatched, single names never merge by containment, resolver keying + display, one-row merge end to end; `crewPnlRowIsEstimateLed` thresholds, row `fallbackBilling`/`estimateLed`, kernel order + rate/profit-asc/hours sorts banded, name sort unbanded, in-band name tiebreak. `crewPnlJobsUniverse.test.ts` (4): complete → null, loading muted, failed warning text verbatim, no-message/singular variants.

## Docs

`docs/CREW_PNL_DATA_FLOW.md` (audience corrected to dev-only — J8-D1; inputs #1 and #5, kernel steps 5 and 7, outputs), help guide `crew-pnl.md` (≈ estimated tag + banded sort, cached-figures example, loose matching + what "unmatched" means).
