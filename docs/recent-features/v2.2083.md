# v2.2083 — Waiting to hear bid-card contrast (2026-08-22)

Owner report: the middle bid card was invisible — `--bg-page` (#fafafa) on a `--surface` (#ffffff) pane. Option A from the mockup ("paper on a stage", artifact 92402faa): the builder detail pane flips to `--bg-muted` (#f3f4f6 light / #374151 dark) and `cardStyle` in [`BidsWaitingToHearLens.tsx`](../../src/components/bids/BidsWaitingToHearLens.tsx) becomes the one white thing on it — `--surface` bg, `--border-strong` border, soft `0 2px 8px` shadow. Open-bid pills switch from transparent/bg-page to `--surface` so they read as buttons on the stage (fresh ones keep the emerald tint). Note: first pass used `--bg-subtle` (#f9fafb) — still a 2% non-difference; `--bg-muted` is the token that actually reads as a stage. Client-only, styling only — no behavior change.
