# v2.2814 — Work Orders tab, PR 1: the job anchor and the document kernel (2026-09-05)

First of three PRs building the owner-approved **Work Orders tab** (mock-up artifact 65bcf6a3): a work order assembled like a bid cover letter, on Jobs and from the job window, that connects a job to sub labor. Owner decisions: the assembler lives on Jobs between Team Labor and Sub Labor; drafts may be unpriced (the master prices them); **signing creates the Sub Labor sheet**.

No visible change in this PR.

- Migration [`20260905050035_work_orders_on_jobs`](../migrations/20260905050035_work_orders_on_jobs.md): `step_commitments.job_id` as a third anchor (back-filled for sheet orders), `amount` nullable while draft, `record_id` + `next_work_order_record_id()` (`WO-<job>-NN`), the access helper's job branch, `create_sheet_for_work_order()` (office set + service role), `respond_to_work_order` and `settle_step_commitment` taught the job anchor.
- Snapshot: `offer_scope_snapshot` gains `facts` (job label/address, customer, trade, record id, issued-on, issuer name/title, sub company, MSA signed-on) and the `job` anchor — `buildSheetWorkOrderSnapshot` / `parseSubWorkOrderSnapshot` in [`subWorkOrder.ts`](../../src/lib/subWorkOrders/subWorkOrder.ts) carry them; legacy snapshots read with empty facts.
- New kernel [`workOrderDocument.ts`](../../src/lib/subWorkOrders/workOrderDocument.ts) (tested): `buildWorkOrderDocument` turns a snapshot + the row + the issuer into the numbered document model (parties, amount/retainage/window/expiry, Scope · Exclusions · Terms · Incorporated by reference · Confirmed at signing, two signature blocks); `renderWorkOrderDocumentHtml` is the one HTML rendering for print / PDF / the portal record (escaped, inline styles); `bidScopeLines` condenses a bid's takeoff rows into one line per stage; `bidSubLaborTotals` sums the bid's sub-labor line per stage.
- New kernel [`workOrderCoverage.ts`](../../src/lib/subWorkOrders/workOrderCoverage.ts) (tested): `buildJobWorkOrderCoverage` (signed › sent › draft › declined, expired offers and unpriced drafts flagged), chip label/tone/title, `workOrderBoardBucket`, `jobsNeedingWorkOrder`.
- New hook [`useJobWorkOrderCoverage`](../../src/hooks/useJobWorkOrderCoverage.ts) (job rows + rows on the job's sheets; refreshes on the `work-order-changed` event; `emitWorkOrderChanged()`).
- `database.ts` hand-aligned (regen after the push lands in PR 2).

Next: PR 2 (the Jobs → Work Orders board + the assembler with its live preview; `submit-sub-portal` creates the sheet on signature), PR 3 (the job window's Sub work order fact row + bill strip, the Needs You item for unpriced drafts, the sheet box and step panel as doors).
