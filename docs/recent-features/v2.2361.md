# v2.2361 — Timeline edge-drag resizes estimates (Tier 2) (2026-08-26)

Client-only follow-up to v2.2358. Drag = SIZE, never schedule: the override
feeds every derived number live (widths, sums, pace, 🎯), release persists,
nothing is ever bound to a date.

- **`ChecklistRoadmapTimelineView`**: new `canEdit` + `onEstimateChange`
  props; pointer-captured right-edge handle on open slots in the expanded
  waterfall (hover-revealed; `role="slider"`; hidden on touch — the card's
  ⏱ stepper is the touch path). Half-day snap, 0.5–90d clamp, live
  "2d → 4.5d" tooltip, Escape cancels, per-task `dragOverride` map flows
  through `effectiveTasks` so avg/rows/pace/🎯 re-derive during the drag;
  the override survives until the parent reload lands (no width snap-back).
- **Tab**: wires `canEdit={canEditStructure}` and the estimate update +
  `load()` reload.
- Help guide gains the "Drag to resize (desktop)" section.
- Verified live: dragged 4.1's edge → snapped to 5d, persisted, caption
  re-derived; cleared via the card stepper afterward.
