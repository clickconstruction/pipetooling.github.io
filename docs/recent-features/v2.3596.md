# v2.3596 — Labor refresh PR 6a: the fold — one book per trade, with provenance (2026-09-18)

Migration `20260919014218_fold_labor_books_into_robot` (**push after merge**, then `npm run gen-types:linked`); no client change in this PR. First of three for PR 6 of [`to-dos/bids-labor-refresh/`](../../to-dos/bids-labor-refresh/README.md); the drawing is [`before-after-pr6.html`](../../to-dos/bids-labor-refresh/before-after-pr6.html). The owner: *"Build it"* (2026-09-18), on the drawing's two defaults — Robot's split wins the three conflicts, and an estimator's calibration Set is a proposal until a leader confirms.

## What the fold found (prod, 2026-09-18)

Seven books: Default (Plumbing, 15 entries, 7 aliases, on no bid), Bill (Electrical, 3, on BP83), Bryan and default (empty), and three 🤖 Robot Default — Plumbing (17 entries, 39 aliases, on BP398 / BP403 / BP404 / BP405), Electrical and HVAC (empty). Default against Robot Plumbing: **3 agree** (Sink, Toilet, Water heater electric), **3 conflict** (Water Fountain 2/3/2 vs 2/2/3, Gas drop 3/1/0 vs 3/0/1, Hose bib 1/1/0.5 vs 1/0.5/1 — the same totals with top-out and trim swapped), **9 only in Default**. Bill's three electrical rows seed the empty Robot Electrical. Fold report: 2 books folded · 3 same · 3 conflict · 12 added · 2 empty books deleted.

## What shipped

The migration, in full in [`docs/migrations/20260919014218_fold_labor_books_into_robot.md`](../migrations/20260919014218_fold_labor_books_into_robot.md): the columns (`archived_at` and the calibration-proposal fields on versions; `origin`, `robot_*_hrs`, `set_by / set_at / set_note` on entries), the fold itself with its notice report, the bid re-pointing and the archive, the mint's book choice (a sent bid with no book now takes its trade's robot book, never an archived one), and the **provenance trigger** — a twin's write lands in `robot_*` and shows only where no person has set the hours; a person's hour change is stamped; restoring the robot's numbers clears the stamp.

Not in this PR: the screens. PR 6b removes the picker and adds the *Hours from* chips, *Reset to robot*, the role rules and calibration proposals; PR 6c retires the Old grid.

## Verified

- The migration run in a rolled-back transaction against prod before merging: the report line and the three conflict notices read as above; the trigger compiles.
- After the push: `check:migration-drift` aligned; `labor_book_versions` reads two archived, two gone, three robot; BP83's `selected_labor_book_version_id` is Robot Electrical; Robot Plumbing holds 26 entries and 46 aliases.
