# v2.2939 — twin-mcp v1.3.8: question topics, report label alias, pairing visibility (2026-09-06)

Groundwork for LEARNING_PLAN.md items 4 and 13, plus a shadow-batch ops note. Migration + edge (push `20260906110000_twin_question_topics.sql` after merge, then deploy `twin-mcp` — the function writes the new column).

- **`twin_questions.topic`** (migration `20260906110000`): the standing-rulings key — one kebab slug per doctrine issue so duplicate robot asks collapse into one ruling. `ask_question` gains the `topic` param (normalized, ≤60 chars, tool description tells agents to reuse existing slugs); `get_answers` returns it.
- **`submit_report` accepts `label`** as an alias for `mission` — three of the 2026-09-05 batch reports filed as "unlabeled" because agents passed `label`.
- **`get_work_state` returns `twin_source_bid_id`** so a shadow/backtest agent can verify its own pairing before heartbeating done (the R2-BT-16 ops note).
