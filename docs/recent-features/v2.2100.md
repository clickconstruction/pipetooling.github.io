# v2.2100 — Partner ledger week card: statement frame (2026-08-22)

Owner report: the "Your ledger" week headline read as accountant-speak — "$967.60 / closing balance · week opened at $717.50" — saying neither whose money the number is nor how the week got from opening to closing. Owner picked Option B ("let the math land at the bottom") from mockup artifact 95ea4646. [`DashboardPartnerLedgerSection.tsx`](../../src/components/dashboard/DashboardPartnerLedgerSection.tsx) replaces the floating headline + caption with a statement frame mirroring the print layout: a quiet muted "Week opened $X" row (signed when negative), the activity lines, then a `2px var(--text-strong)`-ruled total row — label **Week closed** (or **So far this week** on the live week, sub "· updates as hours approve") with a plain-words direction sub-label ("Click owes you" / "you owe Click", from the sign of the total) and the unsigned amount at 1.25rem/800. The live week's total is still the settle-up position via `netPosition` (posted + pending offsets, owner call v2.2009); closed weeks keep their statement closing balance. [`partnerWeeks.ts`](../../src/lib/partnerLedger/partnerWeeks.ts) `buildWeekCards` now skips `day_rates` tiers with zero hours and zero amount (the "Labor · 0.0 h × $0 — 0.00" noise row); new kernel test in `partnerWeeks.test.ts`. Print statement layout unchanged (already statement-framed). Help guide `see-your-partner-ledger.md` updated. Client-only — no migration.
