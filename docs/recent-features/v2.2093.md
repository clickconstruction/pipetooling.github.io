# v2.2093 — Floaty screen picker: Followup lenses named and grouped (2026-08-22)

Owner: the Bids tab checkboxes in the "Where can Floaty appear?" picker (v2.2082)
needed a refresh — "Builder Review" / "Submission Followup" are internal keys,
not what the tab bar says, and the four Followup lenses weren't grouped.

- **`EGG_BIDS_TABS`** (`src/lib/easterEggSurfaceTree.ts`): reordered to the
  app's tab-bar order; labels renamed to the visible ones — the four Followup
  lenses ("By builder", "By status", "Why we lost", "Waiting to hear") carry
  `section: 'Followup'`; "Working" → "Unsent/Working". **Keys unchanged** —
  they're storage (`t:/bids:<tab>`), so existing targeting configs keep
  working untouched.
- **`eggSurfaceLabel`** renders sectioned tabs as `Bids · Followup · By builder`.
- **Picker modal** (`EasterEggScreenPickerModal.tsx`): a full-width FOLLOWUP
  header spans the two-column grid before its four (indented) lenses, with a
  spacer where the section ends.
- Test updated + expanded (section labels, Unsent/Working, plain tabs).
