# v2.2623 — Crew Day report labels drop "Status" (2026-09-02)

Copy tweak (owner request): "Status Report" is the default report template, so its name added no information on Crew Day lines.

- New kernel helper `crewDayReportLabel()` in `src/lib/crewDay.ts` (+test): exactly "Status Report" (case/whitespace-insensitive) and blank names render as "Report"; every other template keeps its full name (the distinction is the point).
- Used by the Crew Day section's report lines and by the `crew-day-email-dispatch` renderer's no-excerpt fallback (inline Deno copy with a keep-in-sync note; function redeployed).
