# v2.2627 — Division 22 as a Needs You item (2026-09-01)

Owner spec: a persistent to-do for uncoded fixture names, visible only to
estimators and devs (mockup: "Division 22 Ledger" canvas artboard 9; home
deliberately the Needs You card, not the dispatch inbox — that's a
person-to-person message surface).

- **Migration `20260902021636`** (applied): RPC `spec_section_uncoded_name_count`
  — server-side count of distinct trimmed names matching NO ledger rule
  (NULL-section rules count as handled, mirroring the kernel). Matching uses
  `position()`/`left()` instead of LIKE so patterns containing `%` (e.g.
  "our 15 %") can never wildcard. SECURITY DEFINER, role-gated inside.
- **Hook `useSpecSectionUncodedCount`** (failure reads as 0 — the dashboard
  never breaks on a ledger hiccup).
- **Builder entry `d22-uncoded`** in `dashboardNeedsYou.ts` (+tests): amber,
  kicker "Division 22", rank 60 (hygiene, beside lost-bids), figure capped
  99+, action "Pin codes". Enabled in `DashboardPinnedQuickRow` for
  `dev || estimator` (the robot-audits gating pattern); Quickfill passes
  `d22UncodedEnabled: false` — it's the billing station.
- **Deep link**: `/bids?tab=pricing&d22audit=1` opens the audit modal
  (role-checked) and strips the param via `history.replaceState` so
  reload/back doesn't reopen it.

Live-verified: dashboard shows "1140 fixture names have no Division 22
code"; Pin codes navigates and auto-opens the audit (now full-universe
thanks to the parallel pagination fix), param stripped.
