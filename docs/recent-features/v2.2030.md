# v2.2030 — One loss-reason system: chips at every door, note suggestions (2026-08-21)

Wendi's double-entry report (marked bids lost in Edit Bid with typed reasons, then had to re-enter them in Followup → Why we lost; mockup artifact 85e15844). Four pieces:

**① Shared chips** — new [`BidLossCategoryChips`](../../src/components/bids/BidLossCategoryChips.tsx) (six `BID_LOSS_CATEGORIES` chips, optional 1–6 key numbers, amber suggestion ring + hint line) replaces the hand-rolled chip rows in [`BidsWhyWeLostLens`](../../src/components/bids/BidsWhyWeLostLens.tsx) and [`BidBoardLostSummaryModal`](../../src/components/bids/BidBoardLostSummaryModal.tsx), and lands in [`BidFormModal`](../../src/components/bids/BidFormModal.tsx)'s Lost section (free text demoted to "what they said (optional)", with a "Don't know yet? Leave it" line). [`useBidEditForm`](../../src/lib/bids/useBidEditForm.ts) gains `lossCategory` (loads from `bids.loss_category`); both Bids.tsx save payloads write `loss_category: outcome === 'lost' ? lossCategory : null` — which is also **④ hygiene**: un-losting a bid now clears the stale category like it clears the note.

**② Suggestion kernel** — `suggestLossCategoryFromNote` in [`bidLossCategories.ts`](../../src/lib/bidLossCategories.ts): case-insensitive synonym map ("gc not awarded" → `gc_lost` is a named test), returns a key ONLY on exactly-one-category match (+4 tests, ambiguity case included). Wired as suggested-chip rings in Edit Bid (live from typed note), the lens (note-on-file bids; **Enter confirms** — new key handler + hint line), and the lost-summary modal. Never auto-applied.

**③ Calibration loop** — migration `20260822003000_bid_pricing_history_loss_category.sql` (DROP + CREATE: return type gains `loss_category`); `BidPricingHistoryRow.loss_category` optional in [`database-functions.ts`](../../src/types/database-functions.ts) so the un-migrated RPC still parses; the Workbench strip counts `loss_category === 'price'` first with the `/price/i` free-text regex as pre-category fallback.

Deliberately excluded: bulk auto-backfill of legacy notes, hard-required category at mark time. Help guide `follow-up-with-builders.md` documents suggestions + chips-everywhere. Verified live: BP335's saved category loads selected in Edit Bid; toggling off shows the amber suggestion from "gc not awarded"; the lens renders shared numbered chips and its queue excludes categorized bids.
