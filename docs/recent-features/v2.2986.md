# v2.2986 — Retire the Crew Jobs / Bids block (Quickfill station) (2026-09-07)

Owner, after the Team train landed: "Retire the Crew Jobs / Bids block on Quickfill too." Client only — no migration, no edge function.

## What shipped

- **Deleted** `src/components/CrewJobsBlock.tsx` (1.3k lines: the per-day Crew Jobs / Bids matrix + the Team Job Labor table + its breakdown / approved-sessions modals) and its only remaining mount, `components/quickfill/CrewJobsSection.tsx`.
- **Quickfill**: the `crew-jobs` station leaves the registry, the render switch and `sectionBanners`. Existing `quickfill_section_marks` rows for it are untouched (history only; no migration).
- Where the block's jobs went: linking hours to a job → Jobs → Team actions (v2.2978), People → Hours' assign window and the day audit (v2.2966); per-job man hours → Pipeline / Job Summary; per-job approved sessions → Job Detail. `utils/teamLabor.ts` stays (Crew P&L, Job Summary, Bids costing still read it).
- Guide `put-clocked-hours-on-the-right-job` rewritten around the three surfaces; `quickfill` loses the station bullet; `read-the-team-board`, `ACCESS_CONTROL`, `JOBS_TABS_ARCHITECTURE`, `SALARY_CLOCK_SESSIONS`, the owner-decisions line, and two code comments updated.

## Verify

Quickfill has no Crew Jobs / Bids station (the station strip and the mark list skip it); Jobs → Team, People → Hours and the day audit are unchanged. `npm run typecheck` finds no reference to `CrewJobsBlock`.
