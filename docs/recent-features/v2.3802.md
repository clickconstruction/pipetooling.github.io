# v2.3802 — Hiring, the helper try-out loop PR 5: Share with… on the column header and the Active accounts line (2026-09-24)

PR 5 of [`to-dos/helper-tryout-loop`](../../to-dos/helper-tryout-loop/README.md) (punch list #24), stacked on PR 4 (v2.3798, the table and the rules). Client only; no migration. PR 6 draws the assistant's trimmed tab.

## The change

- **The column header's ⋯ menu** ([`TeamProspectsTab.tsx`](../../src/components/prospects/TeamProspectsTab.tsx) → `RoleColumn`): *Share with…* and *Delete column* (the ✕ moved in here; the inline confirm and the "delete every candidate first" rule are as they were). The header wears a *shared with N* chip when the column is shared. The virtual Unsorted column has no menu, as it had no ✕.
- **Share with…** ([`ShareColumnDialog.tsx`](../../src/components/prospects/ShareColumnDialog.tsx)): the accounts `shareableAccounts()` lists — prospects staff without the Hiring switch, not archived, not a sample — each with a checkbox; a tick inserts one `team_prospect_role_shares` row, an untick deletes it, no Save; under a ticked name, *shared by Todd, Sep 18*. Full holders are not listed. The reads and writes go through the v2.3798 policies, so the control is inert for anyone who is not a full holder.
- **Kernel [`src/lib/hiring/columnShares.ts`](../../src/lib/hiring/columnShares.ts)** (6 tests): `hasProspectsStaffAccess` (the client twin of the SQL predicate), `shareableAccounts`, `sharedWithChip`, `sharedByLine`, `sharedColumnsLine`.
- **Settings → Active accounts → Edit** ([`ActiveAccountsPanel.tsx`](../../src/components/settings/ActiveAccountsPanel.tsx)): a read-only *Hiring columns shared: Plumber · HVAC Tech — by Todd, Sep 18* line under the Hiring checkbox, read by [`useHiringColumnShares`](../../src/hooks/useHiringColumnShares.ts) (a dev reads the shares and the column names under v2.3798's amended roles policy; the line does not render when there is nothing to say). Unsharing stays on the column.
- Guide: `team-prospects.md` → *Sharing a column with an assistant*. Docs: `ACCESS_CONTROL.md` § Hiring (the control), `PROJECT_DOCUMENTATION.md` (the board entry), the to-do's status.

## Verified

Kernel tests, the tab's render smoke (a fourth case: the chip, the menu, the dialog listing the shared assistant as ticked with the sharer's line, the full holder absent), typecheck, eslint, the theme-token check, the help-guide and release-note validators, `check:todos`. Not walked live: the share table is not on prod until PR 4's migration is pushed, so the dev server's board shows every column unshared and *Share with…* would fail with a plain toast until then.

## Left to PR 6

The assistant's tab: the Hiring pill for a share holder, the shared columns on Screen / Interview / Try-out, the trims (no Hire / Review / Sources / cross-column tells, the Role picker limited, no Hire / Pass / Keep trying / delete / merge), and the live pass with an assistant account holding one column.
