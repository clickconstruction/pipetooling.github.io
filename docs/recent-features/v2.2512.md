# v2.2512 — get_placement_guide: the placement protocol set rides the briefs bundle (2026-08-30)

A cloud-hosted twin without repo access had no way to read `docs/twins/PLACEMENT.md` —
the protocol manual every LIVSTE takeoff pass proved out (doorway calibration,
counters-first placement, registration/snap/density gates, Manhattan rule, branch
sweeps, keyed-note census, printed-total reconciliation).

- `scripts/build-twin-mcp-briefs.mjs` now bundles **PLACEMENT.md + CALIBRATION.md +
  EXTRACTOR.md** into `briefs.ts` as `PLACEMENT_GUIDE` (one string, three
  clearly-headed manuals, ~22 KB).
- New twin-mcp tool **`get_placement_guide`** serves it; `get_ct_guide` step 3½ and
  PLACEMENT.md's frontmatter point at the tool.
- Deployed and verified live: all chapters present in the served text.
