# v2.2371 — Pushed-back markers (Tier A client) (2026-08-26)

Client half of the commitment ledger (migration v2.2366).

- **Kernel `src/lib/checklistDuePushes.ts`** (+7 tests):
  `summarizeDuePushes` (original = first from_due, else first non-null
  to_due, else the item's current due; push count = rows moving later; net
  slip; pushedBack only while current > original), `pushedChipLabel`,
  `originallyDueLine`, `pushedEscalationSuffix`, `dueChangeEntryText`.
- **Manage** (`Checklist.tsx`): ledger loaded alongside the completion
  sweep; amber "pushed ×N" chip beside the due chip.
- **Edit modal**: amber "Originally due … — pushed ×N, +Md so far." line
  under Due by (ledger fetched on open).
- **Activity spine**: `buildManageTimeline` gains `due_change` entries;
  `ChecklistItemActivity` loads the ledger and renders "⟨name⟩ pushed the
  due date Fri, Aug 29 → Mon, Sep 1" lines in amber.
- **Cron `send-scheduled-reminders`** (redeploy required): escalated titles
  carry "(pushed ×N, +Md)" — inline mirror of the kernel, marked
  keep-in-sync. `docs/EDGE_FUNCTIONS.md` updated.
- Types regen. Help guide *schedule a task or make it repeat* → "Pushed
  back" section. Tier B (Manage → Timeline Gantt with slip trails) next.
