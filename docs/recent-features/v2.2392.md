# v2.2392 — Version-less bids: offered price options reach the New letter (2026-08-27)

Wendi: "on New I don't see the Versions that are in old." Root cause: the New
view's letter planner started from `bid_versions` and returned an empty plan
for version-less bids — so a pricing offered as an alternate (G1,
`price_book_versions.include_in_submission`, the Pricing tab's "On their
letter · alternate") never reached the letter. Old showed both pricings only
because its bundling reads the same flag.

`BidsCoverLetterTab.tsx`:

- The plans derivation gains a version-less branch: ★ = the bid's
  `selected_price_book_version_id` (fallback: first unsplit pricing by
  sort_order — mirroring `deriveActivePricingId`'s unsplit rule) as the base
  section, plus each offered non-★ unsplit pricing as an alternate section
  (`offeredPricingId` set). No offers → empty plan, plain single letter as
  before. All downstream machinery (GC packets — null versionId groups to the
  bid's GC — same-page plan, alternates block, headline) flows unchanged;
  `newBundleActive` stays version-gated, and the headline equals the base sum
  either way.
- The solo "One bid — the letter shows ★ …" line now names the offered
  alternates ("plus Default offered as an alternate — manage offers on the
  Pricing tab") instead of claiming alternates need another bid; the
  Same page / Separate pages toggle renders in this branch too.
