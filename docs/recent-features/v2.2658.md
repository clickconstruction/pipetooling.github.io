# v2.2658 — Email wording PR 2: client-built senders adopt templates (2026-09-02)

- New kernel `src/lib/emailWording.ts` (+tests): `renderEmailWording` ({{var}} substitution — provided keys only, unknown tokens stay visible), session-cached `fetchEmailWordingOverride`, `resolveEmailWording` (override → rendered subject/text/html; else built-in fallback, which keeps its richer HTML). Fail-soft: unreachable table = built-in copy sends.
- Adopted: `sendLienReleaseEmail.ts` (type `lien_release_to_customer`; vars project/form_label/amount/signer) and `sendHazmatNoticeEmail.ts` (type `hazmat_notice`; vars job_number/invoice_reference). `settingsTemplates.ts` gains the two types + defaults mirroring the built-ins; Settings → Email templates gains a "Customer Emails" card group; catalog rows flip to `templates`-editable.
- Migration `20260902184612_email_templates_authenticated_read.sql`: authenticated SELECT on `email_templates` (was dev-only) — client senders run as assistants; wording holds no secrets; writes stay dev-only.
- Deliberately NOT adopted yet (still `hardcoded` in the catalog, with reasons): physical-invoice cover note (its sync builder feeds byte-for-byte previews at several call sites — needs a wording-plumb-through, follow-up), supply-house share + GC statement (bodies are data tables; the template contract is subject+body prose). PR 3 covers server-built digests.
