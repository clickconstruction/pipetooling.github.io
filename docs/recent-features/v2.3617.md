# v2.3617 — What customers see: the signed copy is its own step (2026-09-19)

Item 2 of [`to-dos/what-customers-see-residuals.md`](../../to-dos/what-customers-see-residuals.md). Client plus one edge function; reads only; no migration.

## The change

- **One builder** — [`_shared/jobContractEmail.ts`](../../supabase/functions/_shared/jobContractEmail.ts) gains `buildJobContractSignedCopyEmail` (the wording `sign-job-contract` sent inline, moved verbatim; +1 test with the exact text and html); [`sign-job-contract`](../../supabase/functions/sign-job-contract/index.ts) calls it (**deployed**, with `send-job-contract` and `remind-job-contracts`, which bundle the same shared file); [`src/lib/jobContractEmail.ts`](../../src/lib/jobContractEmail.ts) re-exports it.
- **The step** — [`customerJourneys.ts`](../../src/lib/customerJourneys.ts): `job-contract-signed-email` (*Signed copy*, an `email` render, `SampleEmailId` `job-contract-signed-copy`) after *Signed*, whose caption no longer folds the email in. [`customerSampleEmails.ts`](../../src/lib/customerSampleEmails.ts) builds it over the sample job with the PDF attached (+1 test). [`customerSurfaceRegistry.ts`](../../src/lib/customerSurfaceRegistry.ts): `sign-job-contract` places both steps; `share-job-contract` places the new one. The tab builds samples from `SAMPLE_EMAIL_IDS`, which the new id joins — and a new test refuses an email step missing from that list (the live check caught exactly that).
- **The real person** — [`personJourney.ts`](../../src/lib/journeys/personJourney.ts): `CustomerRows.contractEvents` (the `shared` rows of `job_contract_events`, read by the loader for the customer's contract ids; `customerRowsForJob` narrows them with the contracts). The step reads *Sent with the PDF the moment they signed* for an electronic signature, *Emailed <date> · n times* when the office shared, *Not emailed — Share on the signed agreement sends the PDF* with *Open the signed copy* for a paper signature nobody shared (+2 assertions on the J363 fixture).
- **Guides** — [`see-what-customers-see.md`](../../src/content/help/see-what-customers-see.md) chip chains; [`get-a-job-contract-signed.md`](../../src/content/help/get-a-job-contract-signed.md) *The signed copy*. [`docs/EDGE_FUNCTIONS.md`](../EDGE_FUNCTIONS.md) `sign-job-contract` names the builder.

## Verified

Typecheck, lint; journeys (kernel, registry, sample emails, surface registry), strips render smoke, guides, release notes; `npm run check:edge-drift` clean after the deploy. Live: Settings → What customers see → Homeowner shows *Signed copy* rendering the email over the sample; J778's Their journey reads the paper case.

## Left on the residual

Item 1 only — the firm's full sample matter.
