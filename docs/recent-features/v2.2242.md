# v2.2242 — HR Report joins the dashboard SectionDock (2026-08-24)

Owner feedback: the bottom SectionDock had no way to jump to the v2.2235
HR Report card.

- `dockSections` gains `{ id: 'dash-hr-report', label: 'HR' }` between
  Projects and Me (matching page order), visible for dev/master_technician —
  the same gate as the card.
- A `dash-hr-report` anchor div (dockAnchorStyle, same gate) renders directly
  above `DashboardPersonReportCard`.
