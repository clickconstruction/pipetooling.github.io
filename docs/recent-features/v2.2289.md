# v2.2289 — Dead-tap hint on estimate list cards (2026-08-25)

Owner-requested (mockup-reviewed, two refinement passes: whole-card amber ring → inner-zones-only blue; every-tap 2s → dead-taps-only 0.9s). New kernel [`tapHint.ts`](../../src/lib/tapHint.ts): `isDeadTap` (closest-interactive check over `a, button, input, select, textarea, [role="button"]`), `tapHintCss(containerSelector)` (0.9s hold-then-fade outline keyframes in the app's `#2563eb` focus-ring language, `prefers-reduced-motion` gets a steady ring), `TAP_HINT_DURATION_MS` (950ms class window).

Wired in `EstimateListCards` (`src/pages/Estimates.tsx`): card `onClick` → `isDeadTap(e.target)` → per-card `estimate-card--tap-hint` class for the window; CSS appended to `estimatesListPageCss`. No layout shift, no overlay, taps during the fade behave normally; consecutive dead taps inside one window share a burst. Pre-push live verification: dead tap sets the class + `rgb(37,99,235)` computed ring, clears at ~0.95s, real taps (link, thread toggle, customer snapshot) never hint and keep working.
