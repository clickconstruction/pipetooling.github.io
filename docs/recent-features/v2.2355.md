# v2.2355 — Groundwork: roadmap task estimated_days (2026-08-26)

Migration-only PR for the effort-weighted Timeline design (owner request:
"add how long something will take to the gantt + drag edges to resize").

- **Migration `20260826212910`**: `checklist_tech_tree_group_tasks.
  estimated_days numeric NULL` + range check (0 < d ≤ 90). Semantics: a
  task's weight, never its dates — NULL = roadmap-average weight, so
  nothing changes until estimates exist. Details:
  `docs/migrations/20260826212910_roadmap_task_estimated_days.md`.
- Client tiers follow: (1) weighted slot widths + ⏱ card stepper +
  effort-weighted pace/🎯, (2) Timeline edge-drag to resize estimates.
