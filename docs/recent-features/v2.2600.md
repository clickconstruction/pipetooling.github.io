# v2.2600 — Who owes what: PDF tail on the bill cards (2026-09-01)

First of the three owner-approved "PDF + send" PRs for the Who-owes-what modal (round-2 mockup on the Who Owes What Mockups canvas; PR 2 = Stripe resend, PR 3 = physical email resend).

- `src/components/jobs/ViewBillWithPdfTail.tsx` — the v2.2329 split control grew two optional props: `label` (head-button text, default `View Bill`) and `compact` (card-sized paddings). No behavior change at the Stages call site.
- `src/components/jobs/BilledByCustomerBreakdownModal.tsx` — `BillCard` renders `ViewBillWithPdfTail` (`label="View on board"`, `compact`) when the bill is invoice-backed (`invoiceId != null`); the tail opens the freshly rebuilt invoice PDF via `openBilledInvoicePdfInNewTab` (v2.2329 path — works for all three billing channels since `buildPhysicalInvoiceDocumentForBilledInvoice` reconstructs the ClickTooling document from any billed row). Job-shell bills (no invoice row → no document) keep the plain jump button.
- Tests: `ViewBillWithPdfTail.render.test.tsx` covers the relabeled/compact variant (head still jumps, tail still targets the exact invoice).
- Help: `read-the-pipeline-money-view.md` Who-owes-what paragraph mentions the PDF tail.

No schema, no new fetches — the card already had `invoiceId`/`jobId`.
