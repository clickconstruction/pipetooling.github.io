# v2.2766 — Sub portal: drop the availability form (2026-09-04)

Owner request: the "Tell us your availability" tear-off card at the foot of `/s/<slug>` goes away.

- `src/pages/SubPortal.tsx`: the `AvailabilityCard` component and its mount are removed; the page now ends on the short-address card and the footer. `submitToken` still feeds sign-to-accept and sign-link.
- `src/lib/subPortal/subPortalI18n.ts`: the seven strings that only the card used (`tearLabel`, `moreWork`, `moreWorkBody`, `availabilityPlaceholder`, `phonePlaceholder`, `sendToOffice`, `sentToOffice`) are removed.
- Render smoke no longer asserts the card. `manage-subs-end-to-end.md` drops the availability-note sentence.
- Left in place on purpose: the `availability` kind in the `submit-sub-portal` edge function (rate-limited, dispatch intake) — nothing posts to it now; no redeploy needed.
