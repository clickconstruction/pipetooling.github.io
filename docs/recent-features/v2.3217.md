# v2.3217 — Robots group cleanup: Audits filter row, a Robot Board without the human board's furniture, one bid-ref lookup for both question consoles, stale copy and twins-docs drift (2026-09-10)

Client only — no migration, no edge function. A first-look pass over the Bids → 🤖 Robots group after v2.3202/v2.3212 landed; the kickoff / backtest / Desktop prompts were deliberately left alone (another session is testing what works for an agent there). No owner decision reversed: robot bids stay a lens not a wall (v2.2500), nobody asks for a robot (v2.3202), the seal holds, plumbing only.

## What changed

- **Audits filter row.** Kernel [`src/lib/bids/auditListFilter.ts`](../../src/lib/bids/auditListFilter.ts) (+5 tests): `auditKind(projectName)` reads the ZZ convention (`ZZ Shadow …` → shadow, anything else → backtest), `filterAuditList` / `auditFilterCounts` over `{ projectName, openQuestions }`. [`BidsAuditsTab.tsx`](../../src/components/bids/BidsAuditsTab.tsx) renders four `aria-pressed` pills — **All · Backtests · Shadows · Asking you** — above the list (only when more than one audit is listed), each with its count over the same list; the "sorted by what your verdict unblocks" caption moves to the right of the row. The filter narrows and never reorders; the v2.2941 stake order is untouched. An empty filter reads "Nothing under this filter."
- **Robot Board furniture.** [`BidsBidBoardTab.tsx`](../../src/components/bids/BidsBidBoardTab.tsx) gains `showMap?: boolean` (default true) and the jump-pill row now includes a pill only for what renders: no **Map** pill without the map card, no **Health** pill without the Estimating Health section (that second half also fixes primaries / superintendents, who had a Health pill jumping to nothing). [`Bids.tsx`](../../src/pages/Bids.tsx) passes `showMap={activeTab !== 'robot-board'}` and folds `activeTab !== 'robot-board'` into `showEstimatingHealth`, so the Robot Board is the five sections and nothing else — no map of practice bids, no distance rail listing backtest shells as "unsent and due", no pulse, no dev labor-cost table over ZZ rows.
- **One bid-ref lookup for both question consoles.** New hook [`src/hooks/useTwinQuestionBidRefs.ts`](../../src/hooks/useTwinQuestionBidRefs.ts) — the v2.3174 number lookup, `bidNumberById` for a row's `about_bid_id`, and the v2.3187 `twin_source_bid_id` pairing for the "ours b214" link — extracted from `BidsAuditsTab` (Standing rulings) and now also read by the **Twin questions** card in [`DigitalTwinsPanel.tsx`](../../src/components/settings/DigitalTwinsPanel.tsx), which had been passing an empty map (v2.3187's noted divergence). Both surfaces resolve the same links from the same rows.
- **Stale copy.** [`BidsRobotQueueTab.tsx`](../../src/components/bids/BidsRobotQueueTab.tsx): the Requested empty state and the intro line no longer say estimators click a yellow robot (retired in v2.3202) — a bid goes to the front of the line from its robot status sheet. [`BidsRobotShadowsTab.tsx`](../../src/components/bids/BidsRobotShadowsTab.tsx): the empty state says the robots pick up live plumbing bids with readable plans on the weekday batch.
- **Docs drift** (`docs/twins/`): `APP_DIRECTORY.md` describes the 🤖 Robots group and its five lenses, drops the requested-green / ready-yellow model from the Queue entry, notes the filter row and the leaner Robot Board; `HANDOFF.md` lists the current twin-mcp verb families (~35 tools, not ~22) and its front-matter names the 2026-09-04 status section; `estimator.md` stage 4 no longer says `get_work_state` can't show the CT review status yet (it has since v2.2503); `TWIN_HARNESS.md` carries a `last_updated` that matches its v2.3207 body.
- **Help**: `audit-a-robot-bid` mentions the filter row.

## Left alone, on purpose

- **Holdout set** (v2.2942 / v2.2952): the Queue toggles and the axis cards' "no holdout evidence yet" line stay. The harness enforces the flag, so the toggles are the only way to designate holdouts; the pending owner action is picking the 20–25 references, and switching gate denominators is still an owner decision.
- **The prompts** (Copy robot prompt, Copy backtest prompt, Copy Desktop kickoff, the Settings handoff prompt): another session owns them.
- `src/lib/billing/billTruthShadow.ts` is unrelated to robots (bill-truth beacon past its own removal date) — its own PR.

## Testing

`npm run typecheck`, the new kernel tests, `helpGuideContent.test.ts`, `BidsAuditsTab.render.test.tsx`, `standingRulings.test.ts`; walked live on prod data as dev: Robot Board (45 ZZ shells, no map / health), Audits filter pills (30 listed → shadows / backtests / asking-you counts), Queue and Shadows copy, Settings → Digital twins question links.
