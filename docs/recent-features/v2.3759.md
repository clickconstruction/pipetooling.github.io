# v2.3759 — The Lien desk and the GC-run preview show the pay codes page (2026-09-23)

Punch list #35 (`to-dos/lien-notice-pay-codes/`), PR 3 of 4 — the owner's ask to *"be able to preview them in the lien desk"*. Client only, no migration; the page itself shipped in v2.3758.

## The change

- **[`useNoticePayPage.ts`](../../src/hooks/useNoticePayPage.ts)** (2 render tests) — the pay page's rows and codes for one job, for a preview: the job fetched once per id and kept for the life of the component (walking ‹ › through a run never refetches), only the SVG built (a preview has no PDF). The desk's own hook loads no bills, and the run modal already does this fetch when it opens — one request per job either way.
- **[`LienDeskModal.tsx`](../../src/components/jobs/LienDeskModal.tsx)** — the notice pane's stack gains *Page 3 of 3 · pay codes · 5 bills · $28,987.00 still owed · filled from the bills, nothing to type* under the notice, and the page count moves with it (the cover note reads *Page 1 of 3*); the notice's own label reads *the pay codes and the invoice follow it in the packet*. The page carries no "Copy for" strip, as the pane's notice does not. **[`lienNoticePreview.ts`](../../src/lib/jobs/lienNoticePreview.ts)** (+1 test) — the preview window renders it as the last page, labelled, the notice breaking the page before it in print; the desk's pages message carries `payHtml`, so the open preview follows the desk as it does for the other two pages.
- **[`gcNoticePreview.ts`](../../src/lib/jobs/gcNoticePreview.ts)** (+2 tests) — `GcNoticePreviewInput.pay`; the owner's pages end with `{ key: 'pay', label: 'pay codes' }` when the job has unpaid bills, the GC's copy stays the form (`PAY_PAGE_ON_GC_COPY`); `gcNoticeCopyLine` says what each copy carries, in order. **[`GcNoticePreviewModal.tsx`](../../src/components/jobs/GcNoticePreviewModal.tsx)** feeds it from the hook and reads the line under the copy toggle from the kernel.
- Guide *send lien notices from the Lien desk*: the pane, the preview window, the GC window's Preview all.

## Verified

- `useNoticePayPage.render.test.tsx` (2), `gcNoticePreview.test.ts` (+2), `lienNoticePreview.test.ts` (+1), `LienDeskModal.render.test.tsx` (+1: the third page and the count with the hook answering rows; the older label cases unchanged with it answering none), the GC modal smokes; typecheck, lint, theme check.
- Live on the dev server against prod data (dev login as Robert): the Lien desk on a billed job reads *Page 1 of 3 · cover note · Page 2 of 3 · the notice · Page 3 of 3 · pay codes* with the code drawn; Put RMC- Dudley Mason on notice → Preview all shows the Owner's copy as three pages and the GC's copy as one.
