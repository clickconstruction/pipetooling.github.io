# v2.2777 — What customers see: the contract-for-signature email (2026-09-04)

Follow-up to v2.2773 (the "Paper" contract email). Owner asked to see that flow in Settings → What customers see.

- **New step** `sub-contract-email` in the subcontractor strip of `src/lib/customerJourneys.ts`, placed immediately before `sub-contract` (whose sublabel now reads "the link in that email, or Paperwork → Sign"). `SampleEmailId` gains `'contract'`. Reflects: the per-send opening message and subject, the sender (Reply-To), the sub's portal address.
- **Builder** `buildSampleContractEmail(ctx)` in `src/lib/customerSampleEmails.ts`: `buildContractSigningEmail` over `SAMPLE_CONTRACT.documentName`, `SAMPLE_SUB.contact` as the signer, the `/contract/accept?t=sample` link, expiry = today + 14, the viewer as sender, `https://my.clickplumbing.com/sams-plumbing` as the portal address, the default opening line (the real one is typed per send). `buildSampleEmail('contract', …)` routes to it; the tab's email loop includes `'contract'`.
- Tests: `customerJourneys.test.ts` (order + render), `customerSampleEmails.test.ts` (subject, signer, sender, link, expiry, portal band, no-sender fallback). Guide `see-what-customers-see.md` shows the sub strip and names the step.
- No migration, no edge change (the email is built in the browser; the signing page it links to already has its sample branch from v2.2760).
