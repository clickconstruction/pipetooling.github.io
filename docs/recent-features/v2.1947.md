# v2.1947 — Roadmap toolbar row + dark-mode canvas controls (2026-08-21)

Two roadmap chrome fixes (user-reported with screenshot).

- **One toolbar row**: `ChecklistTechTreeRoadmapBar` gains a `trailing` slot;
  the Map/Plan toggle and the editor `TechTreeRoadmapToolbarActions`
  (Order stages / Link Groups, map view only) move into it, and their two
  standalone rows in `ChecklistTechTreeTab` are gone. The row flex-wraps on
  narrow screens.
- **React Flow controls in dark mode**: the library's zoom/fit/lock buttons
  shipped white-on-white in dark theme. Styled via the library's own
  `--xy-controls-button-*` CSS variables (plain rules lose to its
  variable-driven declarations) fed from theme tokens, in `src/index.css`.
