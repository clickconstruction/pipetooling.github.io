# v2.3803 — Robot kickoffs open with the no-Terminal way (2026-09-24)

The sentence [v2.3721](./v2.3721.md) left owed on [`to-dos/mcp-servers.md`](../../to-dos/mcp-servers.md) (#27): the Digital twins card walked the claude.ai / Desktop / phone route (Path 0 of the sign-in spike — *Request headers* on a custom connector), but the kickoff text a redeemed setup code hands back still described the Terminal route as the only one. Docs and a generated bundle; no migration.

## The change

- **[`desktop-operator.md`](../twins/kickoffs/desktop-operator.md)** and **[`pricing-operator.md`](../twins/kickoffs/pricing-operator.md)** — *One-time setup* now opens with **The plainest way — no Terminal**: issue the key on the Fleet card, then in Claude *Customize → Connectors → Add custom connector*, the robot's address (the `{{CONNECTOR_URL}}` placeholder, so the redeemed copy carries the real one), *No sign-in*, and `authorization` under *Request headers* with the card's **Copy header value**; then a new incognito chat. The paragraph ends with when the two Terminal ways are for you: an *Add custom connector* screen with no *Request headers* section. The estimator kickoff's step 2 no longer says Desktop *cannot* send the key header — it can when that section shows.
- **[`_shared/twinKickoffs.ts`](../../supabase/functions/_shared/twinKickoffs.ts)** regenerated (`node scripts/build-twin-mcp-briefs.mjs`). The same run refreshed **[`twin-mcp/briefs.ts`](../../supabase/functions/twin-mcp/briefs.ts)**, whose `DIRECTORY` string had gone stale when [v2.3754](./v2.3754.md) edited `APP_DIRECTORY.md` without regenerating — generated files are regenerated, never left behind.
- The to-do's `next` said PR 6 was still ahead; it now lists what is actually left (three twin-key checks the owner runs, PR 7 when sign-in is needed, composites when the log asks), and notes that *Unknown or revoked dev key* has twice meant `PT_DEV_MCP_TOKEN` was missing from the launching shell.

## Deploys owed (the owner's, after merge)

`supabase functions deploy twin-setup` (the kickoff a setup code hands back) and `supabase functions deploy twin-mcp` (the refreshed directory). Neither changes a verb.

## Verified

`desktopKickoff` (24 — the client splices the connector address into every placeholder, and there are now two per kickoff), `releaseNotes`, `check:todos`, typecheck; the generator's own placeholder guard.
