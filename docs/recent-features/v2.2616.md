# v2.2616 — Lien-release signing: schema + activity foundation (2026-09-02)

PR 1 of the owner-approved 4-PR signing loop (mockups: Lien Release Signing canvas; decisions logged there — no copy-for-email, "Print for signature", autosaving draft + ✕ close, mint-is-the-gate, stored PDFs at mint and signing).

- Migration `20260902001517_lien_release_signing_foundation.sql` — see `docs/migrations/20260902001517_lien_release_signing_foundation.md` for the full column list, the five `job_activity_events` types (`lien_release_issued` / `_signature_requested` / `_signed` / `_sent` / `_voided`, all financial), backfills, and the **out-of-band bucket + storage-policy SQL** (`lien-release-documents`, private, png+pdf, 5 MB) that must run once alongside `db push`.
- Lifecycle on `job_lien_releases.status`: draft → issued → awaiting_signature → signed; `sent_to_customer_at`/`voided_at` orthogonal. Existing rows read as `issued` with `minted_at = created_at`.
- No client changes in this PR — the feed drops the new event types until PR 3 registers them; old clients ignore the new columns.

Next: PR 2 (modal rework: autosave draft, mint gate, request + sign flow), PR 3 (activity registry + Documents rows), PR 4 (ready-to-send lane + send-lien-release-email edge fn).
