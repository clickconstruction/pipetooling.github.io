# v2.2026 — Portal statement footer: QR + short address (2026-08-21)

PR C, the final stage of the portal custom-links train (backend v2.2008,
modal v2.2014, DNS + origin flip v2.2023 — all shipped 2026-08-21).

- **Edge fn `customer-portal`**: payload gains `slug` — the customer's
  `customer_portal_slugs` row, looked up only for audience `'all'` (scoped
  "Separate views" links never advertise the merged address). **Redeploy
  required.**
- **Portal page** (`src/pages/CustomerPortal.tsx`): new
  `PortalShortAddressCard` between the request forms and the sign-off —
  "Your account, any time / See open bills, pay online, or request a visit —
  no login needed", the short address (`my.clickplumbing.com/<slug>`, from
  `portalShortUrl`), and an inline-SVG QR (`qrcode.react`, new dependency,
  level M, ink-on-card) encoding that URL. Renders only when `slug` is
  present; prints cleanly (page pins light, QR is vector).
- `portalPayload.ts` parses `slug` (blank → null) + test; help guide
  `share-a-customer-their-portal.md` updated (footer QR + the guide's address
  example now shows the my.clickplumbing.com form).
