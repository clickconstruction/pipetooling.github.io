# v2.2192 — Light-mode borders darkened one step (2026-08-23)

Owner: "the light mode lines are not dark enough, making it hard to see what sections are." Options mockup: claude.ai/code/artifact/56b0e7e6-c17f-45d8-bd27-e64c31026ce6 (one step chosen).

- `src/index.css` light block only: `--border` `#e5e7eb → #d8dce2`, `--border-strong` `#d1d5db → #b9c0c9`, `--chrome-border` `#e5e7eb → #d8dce2` (nav/footer chrome follows). ~5,000 usages pick it up; dark block untouched; the saturated status borders and the already-darkened `--border-rule` / `--border-job-row` stay as they were.
- Verified in light mode on the dev server: Pricing Workbench (cards / summary strip / solver / price-book pills) and the Bid Board (search, band chips, row rules).
