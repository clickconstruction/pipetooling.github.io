# v2.3621 — Supply houses: the two job-account back-fills (2026-09-19)

Items 2 and 6 of [`to-dos/supply-house-job-account-aging.md`](../../to-dos/supply-house-job-account-aging.md). Client only; no migration; writes only what Edit invoice and Mark opened already write.

## The change

- **Kernel** — [`lib/materials/jobSupplyHouseAccounts.ts`](../../src/lib/materials/jobSupplyHouseAccounts.ts): `RosterInvoiceInput.on_job_account`; `HouseRosterRow.unflaggedInvoiceIds` — invoices allocated to the job *alone* (a job account is per property, so a split invoice never qualifies) not yet flagged (+1 test).
- **Flag n invoices** — [`SupplyHouseJobAccountsRoster.tsx`](../../src/components/materials/SupplyHouseJobAccountsRoster.tsx): on an open row with unflagged invoices; one `update({ on_job_account: true }).in('id', …)`, then `onInvoicesChanged` so the host ([`SupplyHousesTab.tsx`](../../src/components/SupplyHousesTab.tsx)) reloads the house.
- **Mark all N opened…** — the roster header when two or more rows are *bought, no account*; [`MarkJobAccountOpenedModal.tsx`](../../src/components/materials/MarkJobAccountOpenedModal.tsx) gains `bulk` (the job list in place of the Not-needed chip, no Reference field, one upsert of N rows, the toast says to add references from Edit).
- **Guide** — [`open-a-job-account-when-you-win-a-bid.md`](../../src/content/help/open-a-job-account-when-you-win-a-bid.md) *Catching a house up*.

## Verified

Typecheck, lint; kernel tests, guides, release notes. Live on prod data as dev: the house roster shows *Flag n invoices* on open rows with unflagged single-job invoices and *Mark all N opened…* where the amber pile is two or more; neither was clicked on real rows.

## Left on the to-do

Item 4 (Curly's phone and the email typo — an office fix on the house's Contacts) and item 5 (the *Materials → Job Accounts* rename — the owner's call).
