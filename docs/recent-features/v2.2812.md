# v2.2812 — Statement round email redesign: the account man's own account (2026-09-05)

Owner ask, with the v2.2771 email on screen: "for the first time we are beginning to deal with the roles of an account man … we need to improve how this email looks as we're trying to communicate to someone that they need to follow up with this customer and make sure the customer knows they owe us this money. If they don't, it falls on you." Mockup approved.

**DB** (migration `20260905044344`, [doc](../migrations/20260905044344_statement_round_email_payload_v2.md)): `get_statement_round_for_user` 4th cut — per ready GC: `over_90`, `certified_at`, AP contact from `customers.contact_info`, `last_statement_at` (sent mark or app email, any week), `last_word` (newest mark with a note), `last_temperature`, `expected_pay_by`; top level: `deadline` (Friday), `held.items` with reasons (now the sender's own), `contacted_by_me`, `book_total`. The same migration widens the marks table for v2.2813 (`contacted` action, `temperature`, `expected_pay_by`) so the two PRs share one push. Types hand-aligned then regenerated after the push.

**Email** ([`render.ts`](../../supabase/functions/statement-round-email-dispatch/render.ts), rewritten): subject "Malachi, 1 GC is waiting to hear from you — $38,036"; headline by first name; "The standard" block (the owner's sentence, softened one notch: "…that's yours to fix, not the office's"); one card per GC — amount, jobs, certifier + time, chips (oldest bill, red at 90+; $ over 90 days; last statement from you · N days ago, red "no statement on record"), AP contact (red "none on the customer — add one before you call" when missing), last word with its temperature pill (red "nothing on record" when missing), pays-by line, the three-step ask (send · get a pay date in writing · mark it sent with a note, or mark contacted with a temperature), a **Send Knight their statement →** button (`?round=1&gc=<id>`), and "Due by end of day Friday". Held GCs parked as **Coming back to you** with the reason. Scoreboard: your week (sent · contacted) and your book. Empty round: headline says so; held list still shows. Text version mirrors it. Dispatcher tolerates a pre-push payload.

**Deep link**: [`JobsStagesTab.tsx`](../../src/components/jobs/JobsStagesTab.tsx) reads `gc` beside `round=1`; [`JobsGcReviewModal.tsx`](../../src/components/jobs/JobsGcReviewModal.tsx) gains `startInRoundGcId` and the overlay opens on that GC when it is in the user's queue (falls back to the first).

**Help guide** [`run-your-gc-statement-round`](../../src/content/help/run-your-gc-statement-round.md): the email section describes the new shape.

Verified live via the sender card's **Preview Malachi's email** after the push + redeploy (see the PR thread). The over-90 line uses the board's existing 90-day bucket; the standard's wording is a constant in `render.ts` and can also be overridden per the email-templates setting's intro paragraph.
