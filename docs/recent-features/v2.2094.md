# v2.2094 — Vehicle task context modal (2026-08-22)

Owner request: tie Vehicles into checklists so the assignee "can pull up useful information about the vehicle without having to search very far" (mockup artifact e66e8c02, owner-approved). The office half already existed (fleet phase 8: per-vehicle tasks, assignment → one-off checklist item, two-way completion sync); the assignee half was a dead "vehicle" link to `/people?tab=vehicles` — a page field roles can't open, over tables their RLS can't read.

New migration `20260822213000_vehicle_context_for_instance.sql` (see [`docs/migrations/`](../migrations/20260822213000_vehicle_context_for_instance.md)): assignee-scoped SECURITY DEFINER jsonb bundle. New [`VehicleTaskContextModal`](../../src/components/checklist/VehicleTaskContextModal.tsx): your-task banner (note + creator), facts grid (holder/odometer/oil/insurance — oil via the existing `oilStatus` kernel with the vehicle's thresholds, amber when due), open problems with age, recent service (`SERVICE_TYPE_LABELS`), "Open in Vehicles →" for office roles only. `vehicleFleet.ts` gains `VEHICLE_TASK_TITLE_MARKER` / `isVehicleTaskTitle` / `stripVehicleTaskMarker` (+1 test) — the `{{1:vehicle}}` token the assign flow already plants is the detection key; the chip strips it so the old link no longer renders. Chips wired on Checklist → Today (today + outstanding rows, [`Checklist.tsx`](../../src/pages/Checklist.tsx)) and Dashboard → My Inbox (due-today + outstanding rows, [`DashboardMyInboxCard.tsx`](../../src/components/dashboard/DashboardMyInboxCard.tsx)). Help guide updated.
