# Schedule Dispatch Hub Architecture Map

---
file: docs/SCHEDULE_DISPATCH_ARCHITECTURE.md
type: Architecture Map / Decomposition
purpose: Step-0 map (per PAGE_DECOMPOSITION_PLAYBOOK.md) for the Schedule Dispatch hub surface — ScheduleDispatchHub.tsx (4,126 lines, presentational) + ScheduleDispatchHubPage.tsx (3,040 lines, container), treated as one hot ~7.2k-line surface. Inventories every panel/region's state, memos, handlers, supabase tables/RPCs, cross-region coupling and test coverage so extraction can start without re-deriving the strategy. None of the recommended extractions has happened yet; features landed as sibling files while both files grew ~25–28% since the previous map (Hub 3,302 → 4,126, Page 2,384 → 3,040).
covers:
  - src/components/schedule/ScheduleDispatchHub.tsx
  - src/components/schedule/ScheduleDispatchHubPage.tsx
mapped_at: a05cef4c4
audience: Developers, AI Agents
last_updated: 2026-09-25
---

## What this surface is

> **Anchors since v2.1613:** schedule blocks anchor to **exactly one of a job or a bid** (`job_schedule_blocks.job_id` is nullable; `bid_id` + one-anchor CHECK). Hub plumbing carries an opaque **anchor id** — the job uuid, or `bid:<uuid>` — produced by `scheduleBlockAnchorId()` and decoded by `scheduleBlockAnchorFromId()` (both in [`jobScheduleBlocks.ts`](../src/lib/jobScheduleBlocks.ts)); `hubJobTitleById` / `hubJobAddressById` carry `bid:`-keyed entries so every id-keyed lookup just works. The linked-group helpers `updateJobScheduleBlockGroup` / `fetchJobScheduleBlockGroupLegs` are **group-keyed** (no job argument); the `move_job_schedule_block_group` RPC takes a nullable `p_job_id` (NULL = group only) and the drag kernel still passes `block.job_id`. Job-only affordances (Job Detail, job week `?jobId=`, team fetch, week summaries) skip bid anchors. **Identity line (v2.2901, Tier-2 #22):** every block title comes from the pure `scheduleBlockTitle()` kernel ([`scheduleBlockTitle.ts`](../src/lib/scheduleBlockTitle.ts)) — `J<hcp or click number> · name` for jobs, `Bid visit · B408 · Project` for bids; `formatScheduleDispatchHubJobTitle` is a positional wrapper over it. `hubJobTitleById` carries the `bid:` entries for every schedulable bid plus `hubScheduledBidTitleById` (Phase C, `fetchBidTitlesForScheduleBlocks`) for bids that still have a block this week after leaving the schedulable list. `HubPeopleBlockCard` tints bid blocks violet with a `bid` chip and opens the bid from both card buttons; the Jobs panel renders `bidRows` (`blocksToBidWeekMatrixRows`) under the job rows. **Opening a bid block (v2.2861, J18-F1):** `openJobWeekGrid` runs the anchor through `scheduleBlockTarget()` ([`scheduleBlockTarget.ts`](../src/lib/scheduleBlockTarget.ts), pure) and sends a `bid` to the Bids page's Edit Bid deep link (`bidOpenPath` → `/bids?bidId=<uuid>&openBidEdit=1`) instead of writing `?jobId=bid:<uuid>`; the router does the same for any old link still open.

The Schedule Dispatch hub is the week-grid scheduling surface at `/schedule-dispatch`. It is **already split into the playbook's two layers**, but both halves are individually oversized and hot:

| File | Lines | useState · effects · useMemo · useCallback · refs · custom hooks | Commits (90 d) · last | Role |
|---|---|---|---|---|
| [`ScheduleDispatchHubPage.tsx`](../src/components/schedule/ScheduleDispatchHubPage.tsx) | 3,040 (one component, 193–3040) | 66 · 23 · 39 · 52 · 5 · 8 | 46 · 2026-09-24 | **container**: URL router, the `loadHub` data engine + its satellites (lateness, time-off, office ensure, swim lanes, pay gate), the interaction-mode state machine, every mutation handler, ten page-level modals/sheets. Direct supabase: `pay_approved_masters`, `people_pay_config`, RPC `record_ncns_and_reject_sessions_for_day`. |
| [`ScheduleDispatchHub.tsx`](../src/components/schedule/ScheduleDispatchHub.tsx) | 4,126 (6 components, 3 module fns) | 17 · 5 · 18 · 0 · 6 · 10 | 48 · 2026-09-19 | **presentational**: exported orchestrator `ScheduleDispatchHub` + five in-file components (`HubJobsPanel`, `HubPeopleBlockCard`, `HubHiddenBusyPlaceholders`, `HubPeopleDayCell`, `HubPeoplePanel`). **Zero supabase access** (fact sheet: no tables/RPCs) — the exported `Props` type is 90 fields. All state UI-local. |

**Relationship:** `ScheduleDispatchHubPage` owns 100% of the data and mode state and renders `<ScheduleDispatchHub …/>` (90 props at 2693–2814) inside a `DndContext`. The Hub never mutates anything itself; it calls `on*` callbacks. Decomposition here is (a) splitting the Hub file into per-panel component files (pure file moves), and (b) carving the Page's state clusters into hooks.

**Mounts (both must keep working):**
1. [`src/pages/ScheduleDispatch.tsx`](../src/pages/ScheduleDispatch.tsx) (route `/schedule-dispatch`) — thin router: no `?jobId=` → `<ScheduleDispatchHubPage variant="url" />`; `?jobId=bid:<uuid>` → `<Navigate replace>` to the bid (v2.2861; empty id → the hub); any other `?jobId=` → the separate `ScheduleDispatchJobWeek` (NOT this surface).
2. [`QuickfillTomorrowsScheduleSection`](../src/components/quickfill/QuickfillTomorrowsScheduleSection.tsx) — `<ScheduleDispatchHubPage variant="tomorrow" />`: single-day (tomorrow) embed; hides tabs, week nav, weekend toggle, Expected Manpower, phone chrome and Share (`showHubViewTabs` / `showWeekNavigation` / `showExpectedManpower` / `showHideWeekendToggle` / `mobileNewMode` all false; `phonePeopleView` undefined).

The hub view is tab-switched on `hubTab: 'people' | 'jobs' | 'day'` (URL `?hubTab=`, absent or anything else = `people`; local state in the tomorrow variant). The Day tab is **already a thin wrapper** around the extracted [`QuickfillScheduleSection`](../src/components/quickfill/QuickfillScheduleSection.tsx).

### How to read a dossier

**Line numbers are "as of `a05cef4c4`" and rot — search the named symbol; regenerate the fact sheet with `npm run map -- <file>`.** Counts are the fact sheet's. Each dossier lists: render location, **owned local state** (moves with the region), **cross-region/shared state** (stays in the parent), derived memos, handlers, supabase tables/RPCs, sub-components (extracted vs inline), external coupling, test coverage, and extraction status + risk + approach.

### How to maintain this doc

Update the relevant dossier whenever a region is extracted or its state/handlers change; flip its Status and point at the new file. On a refresh, regenerate both fact sheets, re-anchor the ranges and bump `mapped_at`.

---

## Master summary table

| Region | File | Anchor (a05cef4c4) | Lines | Coupling | Risk | Tests | Status |
|---|---|---|---|---|---|---|---|
| Module helpers + style consts | Hub | `hubExpectedManpower*` 87–118; `shortDowLabel` / `hubDayColumnHeaderLabel` / `hubDayColumnHeaderStacked` 126–143; `hubPeopleSalarySuffix` 608–614; `HUB_PEOPLE_TOOLBAR_BTN_H` / `hubPeopleToolbarBtn` / `hubPeopleToolbarIconBtn` 3438–3462 | ~80 | med — `hubPeopleToolbarIconBtn` (declared at 3455, below both panels that use it, just above the shell) is used by Jobs AND People; `hubDayColumnHeaderLabel` by People + Expected Manpower | low | none | inline — **Stage-A prerequisite for every Hub split** |
| Hub shell (tab bar, ⋯ menu, phone header) | Hub | `export function ScheduleDispatchHub` 3464–4126; `Props` 3317–3436 | 663 + 120 | med (fans all props; two `HubPeoplePanel` mounts 3918 / 4019) | low | `ScheduleDispatchHub.render.test.tsx` (5 cases) | inline — stays as coordinator |
| `jobs` tab — `HubJobsPanel` | Hub | `function HubJobsPanel` 161–606 (+ props 145–159) | 446 | **low** (12 props, 4 own states) | **low** | none | inline — extract after the chrome module |
| Card + placeholders + cell | Hub | `HubPeopleBlockCard` 616–1242, `HubHiddenBusyPlaceholders` 1249–1285, `HubPeopleDayCell` 1287–1697 | 1,075 | med (card 26 props, cell 42; dnd ids; mode flags; note context) | low-med | kernels only; no card/cell render | inline — pure file move as a set |
| `people` tab — `HubPeoplePanel` | Hub | `function HubPeoplePanel` 1800–3315 (+ `HubPeoplePanelProps` 1699–1798, 77 fields) | 1,516 | high (75 / 76-prop mounts × 2) | med | kernels + sibling `HubSubsLanes` render test; no panel render | inline — extract after card/cell + Expected Manpower |
| Expected Manpower section | Hub | inside `HubPeoplePanel`: state 1920–1924, memos 1926–2027, effect 2037–2057, JSX 2824–3308 (+ consts 87–118) | ~650 | low-med (~12 inputs, 2 callbacks) | low | calc kernels tested (incl. payroll $); `expectedManpowerDayStats` untested | inline — **highest value ÷ risk split** |
| Phone People board | Hub → sibling | mounted 2209–2257; [`HubPeoplePhoneBoard.tsx`](../src/components/schedule/HubPeoplePhoneBoard.tsx) (820) | — | med (panel props + 6) | — | render test (7) + `phonePeopleBoard.test.ts` | **extracted** (born as own file, v2.3156) |
| `day` tab | Hub | `hubTab === 'day'` 3995–4002 | 8 | low | — | no own test on `QuickfillScheduleSection`; the shell render test's Day case mounts it | **extracted** |
| URL / variant router | Page | 193–360, `placeJob` arming 934–964, nav 2229–2378, gate 2442–2448 | ~420 | high (writes mode state, reads `hubTab`/`weekStart`) | — | day-link / column-focus / edit-roles / block-target kernels | **stays in parent** (playbook rule) |
| `loadHub` data engine + satellites | Page | state 366–395 / 474 / 493 / 1021; memos 434–626; `loadHub` 628–816; trigger 897–903; office ensure 911–927; swim lanes 1095–1103 | ~560 | **highest** (every mutation ends in `loadHub({quiet:true})`; `hubWeekBlocks` has 16 readers) | med | fetchers partly; merge/sort + wage map untested | inline — **seam hook** `useScheduleDispatchHubData` |
| Interaction-mode state machine | Page | state 1013–1050; effects 929–964, 1066–1084, 1105–1124; handlers 818–895, 1033, 1126–1579; banners 2452–2687; multi-cell bar 2821–2856 | ~850 | high (`cardPlacementMode` 15 writers, `hubAssignJobPlacement` 15, `plusMenuBlockId` 14, `linkedCopyMode` 11) | high | linked-copy / drag-end / move / overlap kernels; `insertScheduleDispatchCopiedLeg` + `applyHubMultiCellJob` untested | inline — after data seam |
| Add block modal cluster | Page | state 1000–1010; `openAddBlock` 1135–1179; `closeAdd` 1181–1187; memos 1753–1799; `saveBlockModal` 1801–1875; JSX 2857–2884 | ~250 | med (fed by picker + assign flows) | med | save + timeline kernels, modal render test | inline — hookable |
| Assign-job picker cluster | Page | state 1041–1047; 1052–1058; 1370–1391; 1581–1751; JSX 2902–2983; `hubJobPickerSubline` 144–157 | ~330 | med-high (three intents feed three clusters) | med | order / number-jump / evidence / sort / dup-address kernels; row filters untested | inline — move with modes hook |
| Time-off / not-coming-in / NCNS | Page | `notComingInBusy` 1008; handlers 1879–2182; JSX 2886–2901 | ~310 | low-med | med (NCNS unwinds approved hours) | copy + roles + undo-parse kernels; both write handlers untested | inline — hookable early |
| DnD + delete + note + share + linked-group + move sheet + person day | Page | 314–316, 396, 975–977, 1001–1002, 1015–1017, 1037–1039, 1086–1093, 1327–1353, 2184–2227, 2380–2440, JSX 2816–2820 / 2984–3037 | ~260 | low | low | drag-end kernel; move-sheet / linked-group / person-day render tests | inline — stays (page-level modals) |
| Vestigial job-week residue | Page | `jobId = ''` 201; 362–364; 403–408; `load` 967–969; 979–998 | ~40 | dead branches everywhere | — | — | see quirk #1 — do NOT delete during a move |

---

## Test coverage

Direct tests of the two files: **only** [`ScheduleDispatchHub.render.test.tsx`](../src/components/schedule/ScheduleDispatchHub.render.test.tsx) (150 lines, 5 cases, all shell chrome with empty data: phone header, "+ Schedule" routes each mode to the desktop callbacks, ⋯ menu carries Share + Dispatch settings, desktop tabs keep settings in ⋯, Day view registers Visible hours). `ScheduleDispatchHubPage` has **no render test**, and no `e2e/` spec touches `/schedule-dispatch`. Everything else is covered (or not) through kernels:

| Region | Tested kernels | Untested logic (risk flags in **bold**) |
|---|---|---|
| Shell | render test above | — |
| Jobs panel | `scheduleDispatchColumnFocus` | `filteredRows` / `filteredBidRows` filters |
| Card + cell | `dispatchNoteRequirements`, `schedule/blockGroupCoverage`, `scheduleHiddenBlocks`, `userTimeOffByCell`, `scheduleLateness`, `selfScheduleJobs` (`formatFieldMovedFrom`); `scheduleDispatchDnd` (droppable-id build/parse — the drag contract) **indirectly only**: no own test, one build→parse round trip + a non-cell drop in `scheduleDispatchDragEnd.test.ts` | `scheduleDispatchLinkedGroupPalette`; no card/cell render anywhere |
| People panel | `dispatchSwimLaneSections`, `scheduleDispatch/phonePeopleBoard` (+ board render test), `HubSubsLanes.render.test.tsx` | `usersTabRosterRoleSections`, `scheduleDispatchMobileNamePill`, `afterBlockFilter` / `filteredAssignees` / `peopleDisplayRows` / `missingNoteCount` memos |
| Expected Manpower | `scheduleDispatchExpectedManpower` (8 cases incl. `expectedManpowerJobGroupPayrollEstimate`), `summarizeExpectedManpowerByLane`, `formatManpowerWithHidden` | `expectedManpowerDayStats` reduction |
| Router | `scheduleDispatchDayLink`, `scheduleDispatchColumnFocus`, `scheduleDispatchEditRoles`, `scheduleBlockTarget`, `navClickTelemetry` | `?week` / `?day` / `placeJob` effects, day-first redirect |
| Data engine | `scheduleDispatchHub` (only `jobPickerStatusChip`, `isFinishedJobPickerStatus`, `sortJobPickerRowsFinishedLast`, `findDuplicateJobAddress`, `buildScheduleDispatchHubRoster`), `scheduleBlockTitle`, `salaryPayConfigGate`, `dispatchOfficeRoster`, `subs/subDispatch` + `subDispatchFetch`, `dispatchSwimLanes`, `jobScheduleBlocks` | `parseHubPersonDayKey`, `buildPersonDayBlockMap`, `aggregateWeekSummariesByJob`, `blocksToJobWeekSummaries`, `formatScheduleDispatchHubJobTitle`; `hubMergedRows` sort; **Phase-D wage mapping** (`people_pay_config` by trimmed name → the Expected Manpower $ column) |
| Modes | `scheduleDispatchLinkedCopy`, `scheduleDispatchDragEnd`, `scheduleDispatchMoveBlock`, `jobScheduleOverlap` | **`scheduleDispatchMirrorInsert`** (every copy / linked copy / Copy-to-techs write), **`applyHubMultiCellJob`** (fixed 08:00–16:00 inserts + result copy) |
| Add block | `scheduleDispatchAddBlockSave`, `scheduleDispatchAddBlockTimeline` (+ endDrag), `dispatchAddBlockTime`, modal render test | — |
| Picker | `assignJobPickerOrder`, `jobs/stagesJobNumberJump`, `jobSearchEvidence` | `hubAssignJobPickerRows` 5-field filter, `hubAssignBidPickerRows`, `hubJobPickerSubline` |
| Time-off / NCNS | `scheduleDispatchNotComingInCopy`, `scheduleDispatchEditRoles`, `notComingInTimeOff` (only `parseRemoveNotComingInResult`) | **`recordNotComingInForUserAsStaff`** (unpaid time off + salary sync), **`handleRecordNcnsFromAssignPicker`** (rejects clock sessions / unwinds approved hours, then time off + block deletes — ordering invariant) |
| Modals | `ScheduleDispatchMoveBlockSheet`, `LinkedScheduleGroupModal`, `ManagePersonDayModal`, `ScheduleDispatchAddBlockModal` render tests; `scheduleSharePreview` | — |

---

## Per-region dossiers — ScheduleDispatchHub.tsx (presentational)

### Module helpers + style consts (Stage-A prerequisite)

- **Location:** Expected Manpower table styles `hubExpectedManpowerSrOnly` / `HUB_EXPECTED_MANPOWER_EXPAND_COL` / `HUB_EXPECTED_MANPOWER_JOB_COL_SPAN` / `hubExpectedManpowerSectionTh` / `hubExpectedManpowerRowTd` (87–118, used only in the EM section); exported type `ScheduleDispatchHubMergedRow` (120–124, consumed by the page); day labels `shortDowLabel` (126–129), `hubDayColumnHeaderLabel` (131–133 — People header 2570, EM 1961 / 2880 / 3252), `hubDayColumnHeaderStacked` (136–143 — Jobs header 424 only); `hubPeopleSalarySuffix` (608–614 — used by the panel's person cells 2729 / 2742, not by the card); toolbar styles `HUB_PEOPLE_TOOLBAR_BTN_H` / `hubPeopleToolbarBtn` / `hubPeopleToolbarIconBtn` (3438–3462 — `hubPeopleToolbarIconBtn` is used by HubJobsPanel 251 / 338 and HubPeoplePanel 2268–2463).
- **Why it matters:** every panel split drags one of these along, and two are shared across panels. Move them first into one shared module (labels → lib + test; styles → a consts file) so each later split is a clean cut.

### Hub shell — `ScheduleDispatchHub` (exported orchestrator)

- **Render location:** `export function ScheduleDispatchHub(…)` 3464–4126; `Props` 3317–3436 (90 fields).
- **Owned local state (5):** `dispatchSettingsOpen` (3560), the v2.1240 phone chrome `mobileScheduleMenuOpen` / `mobileMoreMenuOpen` / `mobileQuickAssignOpen` (3567–3569), and `daySettingsApi` (3571, `{ open, windowLabel, dispatchHref } | null`, reported by `QuickfillScheduleSection` via `onDaySettingsApiChange`). The phone layout (`mobileNewMode` = `!isTomorrow && narrowViewport` from the page; the v2.1240 Old/New toggle was removed in v2.1242) sets `newModeHeaderActive` (3572): the shell renders a compact header (3729–3828: segmented Day-first tabs via `mobileTabButton` 3573–3592 + a "+ Schedule" sheet 3741–3824 naming all four creation flows), moves the week nav to shell level for People too, and mounts a shell-level `QuickAssignSheet` (4117–4123) — every action routes through the same page callbacks as the desktop toolbar. The ⋯ menu `moreMenu` (3628–3725) is shared at EVERY width (v2.1243): Visible hours… and "Open in Dispatch week…" (Day view only, from `daySettingsApi`; the trigger tints while a window is active), Dispatch settings… (canEdit), and the Share slot (`weekNavRightSlot`). The page adds a one-shot replace-redirect to `?hubTab=day` on narrow mounts (skipped for explicit `hubTab`/`placeJob` deep links).
- **Derived:** `dispatchSettingsRoster` memo (3561–3564, `allPeopleRows` → `DispatchSettingsModalRosterRow[]`); scroll keys `hubJobsColumnScrollKey` / `hubPeopleColumnScrollKey` (3557–3558, `${weekStart}-${columnFocusDayYmd}-…-${tabForKey}`).
- **Structure (render 3727–4125):** phone header (3729–3828) OR classic tab bar (3829–3899: `role="tablist"` People | Jobs | Day + `moreMenu` as the only right-cluster item); shell-level `ScheduleDispatchWeekNav` (3905–3914) when the embed has no tabs, on the Jobs tab, or on People under the phone header (otherwise People gets it INSIDE the panel via the `weekNav` prop so nav + toolbar share a line); then one of: `HubPeoplePanel` embed branch (3918–3995), `QuickfillScheduleSection` (Day, 3996–4002), `HubJobsPanel` (4004–4017), `HubPeoplePanel` People branch (4019–4107); finally `DispatchSettingsModal` (4108–4114, always mounted; `onSwimLanesChanged` + `onOfficeRosterChanged`) and the phone `QuickAssignSheet`.
- **Quirk:** `HubPeoplePanel` is rendered from **two branches** with hand-duplicated spreads — 75 props (embed) vs 76 (People); the embed omits only `weekNav` at a05cef4c4. Any prop added to the panel must be added in both places.
- **Sub-components (extracted):** `ScheduleDispatchWeekNav`, `DispatchSettingsModal`, `QuickfillScheduleSection`, `QuickAssignSheet`, `HubPeoplePhoneBoard` / `PhonePeopleViewSwitch`, `HubSubsLanes`, `ScheduleDispatchPlusCopyMenu`, `ScheduleDispatchTimeOffChip`, `ScheduleDispatchLateChip` (`QuickAssignSheet` is mounted by both the shell and the panel; the last five only by inner components).
- **Tests:** `ScheduleDispatchHub.render.test.tsx` (see Test coverage).
- **Extraction status + risk + approach:** stays as the coordinator file. Low risk. Once the panels move out, the file is imports + `Props` + the shell (~870 lines — the phone chrome made the shell itself 663 lines). Reducing the duplicated panel spread to one shared props object is a worthwhile behavior-preserving cleanup during that move.

### `jobs` tab — `HubJobsPanel`

- **Render location:** `function HubJobsPanel` 161–606 (props type 145–159, 12 fields), rendered behind `hubTab === 'jobs'` (4004).
- **Owned local state (4):** `search`, `onlyWithBlocks` (default `true`), and the phone (v2.1360) pair `mobileJobsSearchOpen` / `jobsViewMenuOpen` (search behind a magnifier, the checkboxes in a View menu); refs `jobsScrollRef`, `jobsViewMenuRef`.
- **Cross-tab/shared state (props):** `rows: ScheduleDispatchHubMergedRow[]` (page's `hubMergedRows`), `bidRows: ScheduleDispatchHubBidMatrixRow[]` (page's `hubBidMatrixRows` — bids with a block this week, v2.2901; kept out of `hubMergedRows` because that array also feeds the assign picker and `?placeJob=`), `loading`, `jobsError`, `summariesError`, `visibleDayKeys`, `hideWeekend`+`onHideWeekendChange` (shared with People's View menu — the same page state), `onOpenJob`, `scheduleTodayYmd`, `columnFocusDayYmd`, `columnScrollKey`.
- **Derived memos:** `filteredRows` (206–221: search over `hcp_number`/`job_name`/`displayTitle`, then `totalBlocks > 0`); `filteredBidRows` (224–228: search over `displayTitle` only — bid rows exist only because they have blocks).
- **Effects/hooks:** View-menu click-outside + Escape (190–204); `useScrollScheduleDispatchColumnIntoView` (lib hook in `scheduleDispatchColumnFocus`); `useIsMobile`.
- **Render (230–605):** phone toolbar 241–371; table 383–602 (stacked day headers via `hubDayColumnHeaderStacked`); body ternary 431–600 (empty-state row 431–441, else job rows, then bid rows from 521). Row/Open buttons call `onOpenJob(jobId)`; bid rows pass their `bid:<uuid>` anchor through the same callback, which `scheduleBlockTarget` routes to Edit Bid.
- **Supabase:** none. **Tests:** none.
- **Extraction status + risk + approach:** Inline. **Low risk.** Cut/paste to `src/components/schedule/HubJobsPanel.tsx` once `hubDayColumnHeaderStacked` / `shortDowLabel` and `hubPeopleToolbarIconBtn` live in the shared module (see Module helpers). Add a render smoke with one job row + one bid row when it moves.

### Block card + placeholders + day cell — `HubPeopleBlockCard` / `HubHiddenBusyPlaceholders` / `HubPeopleDayCell`

> **Phone move flows (v2.2736):** the card has two tap entry points beside drag — a grip tap arms `cardPlacementMode.variant = 'move'` when `tapGripToMove` (panel `isMobile`) is set, and a press-and-hold (`useLongPress`, fires on release) calls `onRequestMoveBlock` → the page's `ScheduleDispatchMoveBlockSheet`. Both end in `moveScheduleDispatchBlockTo` (the drop kernel). `onRequestMoveBlock` / `tapGripToMove` thread card ← cell ← panel (BOTH mounts) ← hub.

- **Render location:** `function HubPeopleBlockCard` 616–1242 (render 731–1241); `function HubHiddenBusyPlaceholders` 1249–1285 (presentational); `function HubPeopleDayCell` 1287–1697 (render 1439–1696). Rendered only by `HubPeoplePanel` rows (2594–2809).
- **Owned local state:** none in any of the three; the card has one ref (`plusButtonRef` 691). All mode arbitration is derived from props.
- **Cross-tab/shared props (the coupling; card 26, cell 42):** `cardPlacementMode`, `placementSourceWorkDate`, `plusMenuBlockId` + `onPlusMenuBlockIdChange` (page-owned so only one + menu is open across the grid), `linkedCopyMode` (+ `onLinkedCopyToggleBlock`), `hubMultiCellAddActive` + `hubMultiCellAddSelectedKeys` (+ toggle), `hubAssignJobPlacement` (+ `onHubAssignJobCellPick`), `groupMemberCountByGroupId`, `blockCoverageByKey` (v2.3612), `linkedGroupAccentByGroupId` + `highlightLinkedGroups`, `timeOffInfo`, `lateInfo` (v2.2550), `hiddenInfo`, `subBadge`, `getJobDisplayTitle`/`getJobAddress`, `onOpenJob`/`onOpenHubJobDetail`/`onDeleteBlock`/`onRequestEditBlockNote`/`onOpenPersonDay`/`onOpenLinkedGroup`/`onStartCardPlacement`/`onCardPlacementCellPick`/`onRequestMoveBlock`/`onEmptyCellClick`/`onAddJobToScheduleForCell`/`onRequestUndoNotComingIn`/`onMarkNotComingInForCell`, `canEdit`, `scheduleTodayYmd`/`columnFocusDayYmd`, `isBottomRow`.
- **Derived logic:** card — `dragDisabled` (= `!canEdit || hubMultiCellAddActive || linkedCopyActive`), note-requirement colors via `effectiveNoteRequirement`/`editNoteIconColorForBlock`/`surroundingIconColorForRequirement` (past-day gate `block.work_date < scheduleTodayYmd`), linked accent lookup; conditional chrome: bid chip 872–893, amber `unsupervised` pill 956–974 (`coverage`, v2.3612), field-moved chip 977–998 (v2.1568, `formatFieldMovedFrom`), note button 1005–1047, person-day clock button 1048–1084 (v2.1817 → `ManagePersonDayModal`), linked float 1085–1123, −/+ buttons 1124–1206 (`ScheduleDispatchPlusCopyMenu`), linked-copy stage chrome 1207–1239. Cell — `cellBg` mode ladder (drag-over → assign-picking green → linked-wrong-day gray → placement sky → time-off gray, only while picking/placing → multi-select amber, never on a time-off cell), `cellClickable` / `emptyCellClickable` (1412–1421) / `multiSelectCellActive` / `showCellAddJobTriangle` (1429–1437) gates; branches: time-off chip + Late chip 1488–1540 (lateness suppressed while a time-off chip shows), empty cell 1541–1646, corner triangle 1648–1694.
- **Hooks/contexts:** card — `useDraggable` (`id: block.id`), `useLongPress`, `useToastContext` (disabled-drag explainer, `SCHEDULE_DISPATCH_DRAG_DISABLED_READONLY_MESSAGE`), `useDispatchNoteRequirements` (context); cell — `useDroppable` (id from `scheduleDispatchCellDroppableId(workDate, personUserId)`, disabled on time-off).
- **Supabase:** none.
- **External coupling:** dnd-kit ids must stay in sync with the page's `handleHubDragEnd` (`executeScheduleDispatchBlockReassign` parses `over.id` with `parseScheduleDispatchCellDroppableId` and resolves `active.id` against `hubBlockById`) — `scheduleDispatchDnd` has no test of its own; only `scheduleDispatchDragEnd.test.ts` round-trips one id through it.
- **Extraction status + risk + approach:** Inline. **Low-med risk.** Move the three together (cell imports card + placeholders) to `HubPeopleDayCell.tsx` — a verbatim file move; they own no state. Must stay inside the same `DndContext` tree, which they do. Do this before touching `HubPeoplePanel` so the panel move is smaller; add a direct `scheduleDispatchDnd` test first (build/parse edge cases beyond the drag-end test's single round trip).

#### Hidden "busy" placeholders (v2.2892)

`HubPeopleDayCell` takes `hiddenInfo?: ScheduleHiddenCell` (`{count, hours}` from `hiddenByCell.get(hubPersonDayKey(user, day))`). When set, `HubHiddenBusyPlaceholders` renders one dashed grey `busy` box per hidden block (cap 6, then "+N more"; tokens `--bg-muted` / `--border` / `--text-muted`; `data-testid="hub-hidden-busy"`; tooltip "Busy on work outside your projects (N blocks) — details are hidden"). A cell with only hidden blocks is **not** an empty cell: `emptyCellClickable` requires `hiddenCount === 0`, the `+`/`off` bar does not render, and `showCellAddJobTriangle` admits `hiddenCount > 0` so the super can still add their own job via the corner triangle. The page fetches the counts in `loadHub` Phase A **only for `role === 'superintendent'`** (`wantsHiddenBlockCounts`; `fetchScheduleHiddenBlockCounts` → RPC `schedule_hidden_block_counts`), merges the hidden user ids into the roster and into `userIdsWithBlocksThisWeek`, and records `recordNavClick(uid, role, 'schedule_hidden_blocks', '#<count>')` once per non-quiet load.

### `people` tab — `HubPeoplePanel`

- **Render location:** `HubPeoplePanelProps` 1699–1798 (77 fields; `onSwimLanesChanged` is declared but the shell never passes it — the shell hands it to `DispatchSettingsModal`); `function HubPeoplePanel` 1800–3315. Rendered behind `hubTab === 'people'` AND by the `!showHubViewTabs` embed branch.
- **Owned local state (8):** `viewMenuOpen` + `viewMenuRef` (View dropdown: Hide Inactive / Hide weekend / Highlight linked), `mobileSearchOpen` (v2.1357), `search`, `onlyWithBlocksThisWeek`, `quickAssignOpen` (mobile Quick Assign sheet), `personSort: 'alpha' | 'role' | 'lanes'` (default `lanes`; persisted per device, `PEOPLE_SORT_STORAGE_KEY = 'pipetooling_dispatch_people_sort_v1'`, cycled alpha → role → lanes by `cyclePersonSort` 2091–2101), `peopleScrollRef`, plus the Expected Manpower pair (next dossier).
- **Cross-tab/shared state (props, stays in page):** everything in the card/cell dossier, plus `hideWeekend`/`onHideWeekendChange` (shared with Jobs), `highlightLinkedGroups`/`onHighlightLinkedGroupsChange` (page-owned, localStorage-persisted), `allPeopleRows`, `userIdsWithBlocksThisWeek`, `salariedUserIds`, `personDayBlocks`, `hubWeekBlocks`, `hubPeopleNameById`, `roleByUserId`, `swimLanes`, `focusPersonUserId` (from `?focusPerson=`), `weekNav` slot, linked-copy apply callbacks (`onLinkedCopyApplyToPerson`/`onLinkedCopyApplyToLane`, `linkedCopyApplyBusy`), toolbar mode entry points (`onRequestHubAddJob`, `onRequestHubMultiCellAddMode`, `onStartLinkedCopyMode`), `onQuickAssignScheduled`, `userTimeOffByCell`, `latenessByCell`, `hiddenByCell`, `subBadgeByCell` / `subLanes`, `hiddenBlockCounts`, the phone-board six, `showExpectedManpower`/`showHideWeekendToggle` (embed flags).
- **Derived memos (5 + 10 EM):** `afterBlockFilter` (2059–2062, Hide Inactive), `filteredAssignees` (2064–2078: name OR lane match via `personMatchesLaneQuery` OR any visible block's job title), `peopleDisplayRows` (2102–2146: lanes via `buildSwimLaneDisplaySections`, role via `groupRosterUsersByAuthRoleSection`, else flat), `emptyMessage` (2148–2164), `missingNoteCount` (2170–2196: focus/today column, blocks with no note whose `requirementForBlock` ≠ `'skip'`; past days always 0 — quirk #6).
- **Effects (4):** View-menu click-outside + Escape (1883–1897); phone-board reset (1914–1919: when the board activates, clear `search` / `onlyWithBlocksThisWeek` / `mobileSearchOpen` — the board has no filter UI); `?focusPerson` `scrollIntoView` (2030–2035, element id `hub-person-row-${userId}`); Expected Manpower collapse reset (2037–2057).
- **Hooks/contexts:** `useIsMobile` (mobile name pills, Quick Assign ⚡ button), `useScrollScheduleDispatchColumnIntoView`, `useDispatchNoteRequirements` (missing-note count).
- **Render (2198–3314):** error lines; `phoneBoardActive ?` `HubPeoplePhoneBoard` (2209–2257) `:` grid — toolbar 2260–2479 (add-job/Quick Assign 2262–2339, mobile search 2340–2370 / 2444–2468, View menu 2394–2441), table 2480–2811 (day headers + missing-note badge 2560–2590; person rows/cells 2594–2809), `HubSubsLanes` 2812; panel `QuickAssignSheet` 2817–2823 (isMobile && canEdit); Expected Manpower 2824–3308 (renders under both board and grid); `PhonePeopleViewSwitch` 3310–3312 (stands down while `phonePlacementActive`).
- **Supabase:** none.
- **Phone board (v2.3156):** the panel renders [`HubPeoplePhoneBoard`](../src/components/schedule/HubPeoplePhoneBoard.tsx) instead of toolbar + table when `onPhonePeopleViewChange != null && phonePeopleView === 'board'` (per-device, key `pipetooling_dispatch_people_phone_view_v1`; kernel [`phonePeopleBoard.ts`](../src/lib/scheduleDispatch/phonePeopleBoard.ts); an explicit pick wins at any width, unset follows the 640px viewport). It consumes the same props and callbacks, plus six added through the shell — `phonePeopleView` / `onPhonePeopleViewChange`, `onCopyBlockToPeople` (page loop over `insertScheduleDispatchCopiedLeg`), `onCancelCardPlacement`, `onCancelHubAssignJobPlacement`, `onLinkedCopySetStage`. The page's three mode banners stand down while its own `phoneBoardActive` (1032) is true. The switch sits at the bottom of both renderings. The grid's sticky Person cells paint a solid background on phones since the same PR.
- **External coupling:** lane-heading rows double as linked-copy stage-2 "whole crew" apply buttons; person-name cells double as stage-2 per-person apply buttons — both purely via props.
- **Extraction status + risk + approach:** Inline. **Medium risk** — the JSX is big but the state is clean; the cost is the 77-field interface (already declared as `HubPeoplePanelProps`, so the move is mechanical). Extract to `HubPeoplePanel.tsx` AFTER the card/cell set and the Expected Manpower section (the panel itself drops to ~900 lines once EM leaves). All mode state stays page-owned. Remember both render branches in the shell.

#### Subs on the board (v2.2929)

The panel takes `subLanes` (`SubLane[]`) and `subBadgeByCell` (`hubPersonDayKey` → `{count, titles}`), both built in the page (`hubSubLanes` 551 / `hubSubBadgeByCell` 552) from `fetchSubOrdersForRange(weekStart, weekEnd)` (Phase A, degrades to a warning), `fetchTeamMembersByJobId` and `fetchSubOffDaysForRange` — kernel [`lib/subs/subDispatch.ts`](../src/lib/subs/subDispatch.ts) (tested). `HubSubsLanes` renders under the crew table (read-only, chips open the job); `HubPeopleDayCell` shows a green "sub" badge for crew assigned to a job on the days a sub is **picked** there (windows never badge). No schedule block is ever written for a sub — the work order's dates are the schedule. The Day tab mounts `SubsOnSiteForDay` above the crew; its "Add a site visit ›" is the `?placeJob=` deep link.

### Expected Manpower section (inside `HubPeoplePanel`)

- **Render location:** `visibleDayKeys.length > 0 && showExpectedManpower` section 2824–3308: day-tab strip (per-day buttons + "All week"), stats line, lane breakdown line, collapsible per-job table (2925–3293 when a day key is set) with expandable assignee detail rows, week person-hours footer. Its state/memos sit at the top of `HubPeoplePanel` (1920–2027); table styles are module consts 87–118.
- **Owned local state:** `expectedManpowerByJobSectionCollapsed`, `collapsedExpectedManpowerJobIds: Set<string>`, `prevHubExpectedManpowerKeyRef` (drives the collapse-reset effect 2037–2057: switching TO All-week collapses every job; switching between days clears collapses; `null` clears).
- **Cross-tab/shared state:** `hubExpectedManpowerDayKey` + `onHubExpectedManpowerDayChange` — **page-owned** (effect 352–360 keeps it valid across week changes: `HUB_EXPECTED_MANPOWER_ALL_WEEK` sentinel survives → previous → today → first visible day). Reads `hubWeekBlocks`, `visibleDayKeys`, `hiddenBlockCounts`, `getJobDisplayTitle`, `hubPeopleNameById`, `swimLanes`, `canShowExpectedManpowerPayroll`, `hubHourlyWageByUserId`, `onOpenJob`, `scheduleTodayYmd`.
- **Derived memos (10, 1926–2027):** `expectedManpowerWeekPersonHours` (`expectedManpowerPersonHoursTotalForDayKeys`), `expectedManpowerDayRows` (`expectedManpowerRowsForDay` / `expectedManpowerRowsForVisibleDays`), `expectedManpowerSelectionLabel`, `expectedManpowerJobGroups` (`expectedManpowerJobGroupsForDay`), `expectedManpowerDayStats` (1972–1987, inline reduction — Stage-A candidate), `expectedManpowerHiddenSelection` / `expectedManpowerHiddenWeekHours` (`scheduleHiddenManpowerForDayKeys`), `expectedManpowerSelectionHeadline` / `expectedManpowerWeekHeadline` (`formatManpowerWithHidden`), `expectedManpowerLaneRows` (`summarizeExpectedManpowerByLane`).
- **Supabase:** none. Payroll estimate is client-side (3129–3145): `formatCurrency(expectedManpowerJobGroupPayrollEstimate(job.rows, id => hubHourlyWageByUserId.get(id) ?? 0))` gated on `canShowExpectedManpowerPayroll` (dev, or master_technician in `pay_approved_masters`). The estimate kernel is tested; the wage map that feeds it (page Phase D) is not.
- **Hidden blocks (v2.2892):** with `hiddenBlockCounts` non-empty (superintendent only — `[]` for office roles) the stats line reads `**83** person-hours · 38 on your projects · 4 jobs · 6 people · 3 busy elsewhere`, the empty-selection copy becomes "No blocks on your projects for … · N person-hours busy elsewhere", and the week footer gains "· N on your projects". With nothing hidden every string is unchanged. Kernel: [`lib/scheduleHiddenBlocks.ts`](../src/lib/scheduleHiddenBlocks.ts) (tested).
- **Extraction status + risk + approach:** Inline. **Low risk, highest value** — nearly all calc already lives in tested [`lib/scheduleDispatchExpectedManpower.ts`](../src/lib/scheduleDispatchExpectedManpower.ts), [`lib/dispatchSwimLaneSections.ts`](../src/lib/dispatchSwimLaneSections.ts) and `scheduleHiddenBlocks.ts`. Extract to `HubExpectedManpowerSection.tsx` with props (`hubWeekBlocks`, `visibleDayKeys`, `hiddenBlockCounts`, `dayKey` + `onDayKeyChange`, `getJobDisplayTitle`, `hubPeopleNameById`, `swimLanes`, `canShowPayroll`, `hourlyWageByUserId`, `onOpenJob`, `scheduleTodayYmd`); the collapse state, its reset effect, the 10 memos and the style consts move with it (~650 lines out of `HubPeoplePanel`). Keep `hubExpectedManpowerDayKey` in the page.

### `day` tab — extracted

- **Render location:** `hubTab === 'day'` branch 3995–4002: `<QuickfillScheduleSection hideConflictPrompt initialWorkDateYmd={dayTabWorkDateYmd} onBlocksSaved={onDayScheduleChanged} showDaySettings onDaySettingsApiChange={setDaySettingsApi} />`.
- **Coupling:** `onDayScheduleChanged` → page's `loadHub({ quiet: true })` so the People/Jobs week caches refresh after the Day view's dot auto-saves/add-blocks. `QuickfillScheduleSection` (1,885 lines, mapped in [`QUICKFILL_ARCHITECTURE.md`](./QUICKFILL_ARCHITECTURE.md); no render test of its own — only the shell render test's Day case mounts it) does its own data loading (shares lib helpers such as `fetchJobsLedgerForScheduleDispatchHub`, not the page's state).
- **Status:** **Done** — this is the target end-state shape for the other tabs.

---

## Per-region dossiers — ScheduleDispatchHubPage.tsx (container)

State census by cluster (66 `useState`): router/view 5 · data engine 22 · modals 11 · add-block 8 · modes 7 · picker 6 · time-off 4 · vestigial 3. Effects (23): router 8 · data 6 · modes 4 · modals 2 · vestigial 2 · picker 1.

### URL / variant router (stays in parent)

- **Location:** top of the component 193–360 (hooks 194–199, `jobId = ''` 201, `tomorrowYmd` 202, `localHubTab` 203, `hubTabFromUrl` 206–211, `useNarrowViewport640` 216, day-first redirect 220–236, tomorrow tab coercion 238–243, `weekStart` 245–253, `?week` normalize 257–290, `weekEnd` 292, persistence effects 294–312, `visibleDayKeys` 318–321, `columnFocusDayYmd` 325–328, `?day` strip 330–340, `dispatchWeekNavDateRangeOverride` 343–349, `hubExpectedManpowerDayKey` 351–360) + `placeJob` arming effect 934–964 + nav callbacks (`shiftWeek` 2229–2263, `goThisWeek` 2265–2294, `setHubTab` 2296–2350, `openJobWeekGrid` 2352–2378).
- **URL params owned:** `?week=` (normalized to company week start via `companyWeekStartSundayContaining`; invalid → `getDefaultWeekRange().start`), `?day=` (column focus AND, since v2.2890, the Day tab's work date — one resolver, [`scheduleDispatchDayLink.ts`](../src/lib/scheduleDispatchDayLink.ts) (`resolveScheduleDispatchLinkedDay` / `scheduleDispatchDayTabWorkDate`), so `?hubTab=day&day=<ymd>` opens that day; stripped when not in `visibleDayKeys`), `?hubTab=` (`jobs`/`day`; anything else = people), `?placeJob=<jobId>` (arms `hubAssignJobPlacement` once per `${jobId}|${weekStart}` via `placeJobArmKeyRef`, after `hubLoading` clears; on the Jobs tab it first rewrites the URL to the people tab; stripped by `stripPlaceJobFromUrl` on cancel/consume/Esc), `?focusPerson=<userId>` (read inline at the `ScheduleDispatchHub` callsite), `?jobId=` (written by `openJobWeekGrid` for job anchors — hands off to `ScheduleDispatchJobWeek` via the `ScheduleDispatch.tsx` router; records `schedule_block_opened` `#job`/`#bid`).
- **Variant handling:** `variant === 'tomorrow'` pins `weekStart` to tomorrow's week, `visibleDayKeys = [tomorrowYmd]`, `columnFocusDayYmd = tomorrowYmd`, swaps URL tab state for `localHubTab` (coerced back to `'people'` if it drifts), and makes week-nav / open-job actions `navigate('/schedule-dispatch?…')` (leave the embed) instead of `setSearchParams`.
- **localStorage:** `scheduleDispatchHideWeekend` (default hidden; `readScheduleDispatchHideWeekend` 162–172), `scheduleDispatchHighlightLinkedGroups` (174–181); both persisted by effects. `phonePeopleViewPref` (1026, `readPhonePeopleView`/`writePhonePeopleView`) is the page's third per-device view pref.
- **Role gates:** `authLoading` → "Loading…" (2442–2444); `role` not in `CAN_VIEW_SCHEDULE_DISPATCH_ROLES` → `<Navigate to="/dashboard" replace />` (2446–2448). `canEdit` (397) uses `CAN_USE_SCHEDULE_DISPATCH_EDIT_ROLES` (aliased import `CAN_USE_SCHEDULE_DISPATCH`); both sets are dev / master_technician / assistant / superintendent / controller today, kept as separate constants in [`lib/scheduleDispatchEditRoles.ts`](../src/lib/scheduleDispatchEditRoles.ts) so they cannot drift silently. `canTimeOff` (399) and `canRecordNcns` (1965) are narrower — see the time-off dossier.
- **Status:** **stays in the parent permanently** (playbook rule). Any extracted hook receives `weekStart`/`weekEnd`/`visibleDayKeys`/`isTomorrow`/`hubTab` as inputs.

### `loadHub` data engine (the shared substrate) → seam hook `useScheduleDispatchHubData`

- **Location:** state 366–395 + 474 + 493 + 1021 (gate `wantsHiddenBlockCounts` 401); pay-approved-masters effect 410–432; `canShowHubExpectedManpowerPayroll` 434–441; memos 443–626; `refreshHubLateness` 496–512 + effect 514–516; `refreshHubUserTimeOff` 518–529 + effect 531–543; `loadHub` 628–816; trigger effect 897–903; `runOfficeEnsure` 911–927; `refetchSwimLanes` 1095–1098 + effect 1100–1103.
- **Owned state (22 — would move into the hook):** `loadHub` writes 19 — `hubLoading` (+ `hubLoadSeqRef`, monotonic; **quiet refreshes never touch `hubLoading`**), `hubJobsError`, `hubSummariesError`, `hubJobs`, `hubBids` (v2.1613 schedulable bids), `hubScheduledBidTitleById`, `hubWeekBlocks`, `hubHiddenBlockCounts`, `hubTeamMemberUserIds`, `hubSubOrders` / `hubTeamByJobId` / `hubSubOffDays` (v2.2929), `hubRoleByUserId`, `hubPersonById` (v2.3612 `{role, needsSupervision}`), `hubArchivedUserIds`, `hubPeopleNameById`, `hubHourlyWageByUserId`, `hubSalariedUserIds`, `hubUserTimeOffByCell` (+ `hubUserTimeOffPrimedRef`, quirk #3). Satellites write the other 3: `hubPayApprovedMasterIds` (own effect, `pay_approved_masters`), `hubLatenessByCell` (`refreshHubLateness`, v2.2550: `fetchClockInsForUsersInRange` + `computeLatenessByCell`; re-runs whenever `hubWeekBlocks` or `hubJobTitleById` change, i.e. after every quiet reload), `swimLanes` (`fetchDispatchSwimLanes`, per auth user; re-fetched on `onSwimLanesChanged`).
- **Derived (18 memos + 2 getters):** `canShowHubExpectedManpowerPayroll`, `hubSummaryRows` (`blocksToJobWeekSummaries`), `hubPersonDayBlocks` (`buildPersonDayBlockMap`), `hubJobTitleById` (447–464), `hubAllPeopleRows` (466–472: team ∪ assignees, minus archived, name-sorted), `hubVisibleUserIdsSerialized`, `hubUserIdsWithBlocksThisWeek` (+ hidden ids), `hubHiddenByCell`, `hubWeekDayKeys`, `hubSubLanes`, `hubSubBadgeByCell`, `hubBlockById`, `hubGroupMemberCountByGroupId`, `hubBlockCoverageByKey` (`buildBlockCoverageByKey`, v2.3612 → the card's `unsupervised` pill), `hubLinkedGroupAccentMap`, `hubBidMatrixRows`, `hubJobAddressById`, `hubMergedRows` (608–626, quirk #12); getters `getHubJobDisplayTitle`, `getHubJobAddress`.
- **`loadHub` phases (preserve):** **A** (638–711) six in parallel — `fetchJobsLedgerForScheduleDispatchHub`, `fetchJobScheduleBlocksForHubDateRange(weekStart, weekEnd)`, `fetchUsersTabRosterForScheduleDispatchHub(role === 'dev')`, `fetchBidsForScheduleDispatchHub`, `fetchScheduleHiddenBlockCounts` (superintendent only, else `[]`), `fetchSubOrdersForRange`; the subs result then fans out to `fetchTeamMembersByJobId` + `fetchSubOffDaysForRange` → **B** (713–729) `fetchTeamMemberUserIdsForJobIds(jobIds)` (150-id chunks), roster = team ∪ users-tab ∪ hidden ids ∪ assignees → **C** (731–755) `fetchUserNamesForIds` + `fetchArchivedUserIdSetForIds` + `fetchUserTimeOffForUsersInRange` + `fetchBidTitlesForScheduleBlocks` (scheduled-only bids) in parallel → **D** (757–808) `Promise.allSettled([fetchSalariedUserIdSetFromUserIds(rosterIds, { nameByUserId }), wagesPromise])` where `wagesPromise` reads `people_pay_config` by trimmed person_name (only when `canShowHubExpectedManpowerPayroll`). Partial failures degrade (a warning toast, or `hubJobsError` / `hubSummariesError` for the panels' error lines); only an unexpected throw toasts an error.
- **Supabase tables/RPCs:** via lib — `jobs_ledger`, `jobs_ledger_team_members`, `bids`, `users` (roster via `activeUsersQuery`; names via RPC `list_user_display_names`), `job_schedule_blocks`, `user_time_off`, RPC `schedule_hidden_block_counts`, sub tables (`job_stage_windows`, `step_commitments`, `jobs_ledger_fixtures`, `person_availability`), `clock_sessions` (lateness), salary flags (`people`, `users`, RPC `list_people_pay_flags`), `dispatch_swim_lanes` + `dispatch_swim_lane_members`, `dispatch_office_roster` + RPC `ensure_office_schedule_blocks`; direct in page — `pay_approved_masters` (SELECT `master_id`), `people_pay_config` (SELECT `person_name, hourly_wage`). All wrapped in `withSupabaseRetry`.
- **Standing office schedule (v2.1812, past-guard v2.3808):** `runOfficeEnsure` calls RPC `ensure_office_schedule_blocks` once per visible range for editor roles (`officeEnsureRanForRangeRef`; roster edits force a re-run via `onOfficeRosterChanged`) and quiet-reloads when rows were created; the range is first clamped to today onward by `clampOfficeEnsureRange` (`lib/dispatchOfficeRoster.ts`, tested) and the RPC clamps the same way, so a past week is never filled.
- **External coupling:** **every mutation in the file ends with `loadHub({ quiet: true })`** (drag end, move sheet, block save/delete, copies, linked-copy applies, Copy to techs, note save, not-coming-in, NCNS, undo, Quick Assign, Day-tab saves, linked-group and person-day modal changes) — this is the page's equivalent of Bids' `useBidPricingEngine`.
- **Extraction status + risk + approach:** Inline. **Medium risk, do as the first Page step (seam).** Extract to `src/hooks/useScheduleDispatchHubData.ts` taking `{ weekStart, weekEnd, role, authUserId, canEdit, showToast }` and returning one object the page destructures (so downstream references don't change). Keep `canShowHubExpectedManpowerPayroll` inside (input to Phase D and output); preserve that the `loadHub` dep on it re-runs the full load when the pay gate resolves. Lateness must live in the hook (it reads the engine-derived `hubJobTitleById`). Stage-A the Phase-D wage mapping first (money-adjacent, untested).

### Interaction-mode state machine

- **Location:** state `cardPlacementMode` 1013, `linkedCopyMode` 1019, `linkedCopyApplyBusy` 1022, `plusMenuBlockId` 1023, `hubAssignJobPlacement` 1040, `hubMultiCellAddActive` / `hubMultiCellAddSelection` 1048–1049, `placeJobArmKeyRef` 1050; effects: week change clears multi-cell 929–932, `placeJob` arming 934–964, Escape per mode 1066–1084 / 1105–1112 / 1114–1124; handlers `onCancelCardPlacement` 1033, `placementSourceBlock` 1060–1064, `stripPlaceJobFromUrl` 1126–1133, `onStartCardPlacement` 1189–1218, `onCardPlacementPickCell` 1220–1325, `onHubAssignJobCellPick` 1355–1363, `onCancelHubAssignJobPlacement` 1365–1368, `onRequestHubAddJob` 1370–1383, `onRequestHubMultiCellAddMode` 1393–1409, `onHubMultiCellAddToggle` 1411–1419, `onStartLinkedCopyMode` 1422–1436, `onLinkedCopyToggleBlock` 1439–1445, `onLinkedCopySetStage` 1447–1449, `onLinkedCopyApplyToPerson` 1454–1495, `onLinkedCopyApplyToLane` 1500–1541, `onCopyBlockToPeople` 1544–1579 (v2.3156 phone Copy to techs), `applyHubMultiCellJob` 818–895; JSX mode banners 2452–2546 (placement; `move` variant 2469–2526) / 2547–2649 (linked copy) / 2650–2687 (assign placement), each hidden while `phoneBoardActive`; floating multi-cell "Choose job" bar 2821–2856.
- **The invariant:** the modes are **mutually exclusive** — every entry handler (`onRequestHubAddJob`, `onRequestHubMultiCellAddMode`, `onStartLinkedCopyMode`, `onStartCardPlacement`) zeroes the others and strips `?placeJob=`; `openAddBlock` clears placement / + menu / assign placement / picker / multi-cell but leaves `linkedCopyMode` alone and does not strip (its `closeAdd` does). Escape exits whichever is active. `setHubTab('jobs'|'day')` clears placement/multi-cell; `shiftWeek`/`goThisWeek` end placement / move / linked-copy (the phone board would lose its bar and Cancel); a `weekStart` change also clears multi-cell. Writer counts from the fact sheet (distinct writing handlers / effects / render branches, not call sites) show the spread: `cardPlacementMode` 15, `hubAssignJobPlacement` 15, `plusMenuBlockId` 14, `hubMultiCellAddSelection` 12, `linkedCopyMode` 11, `hubMultiCellAddActive` 11.
- **Handlers/writes:** `onCardPlacementPickCell` — `move` variant → `moveScheduleDispatchBlockTo` (the drop kernel); `solo`/`linked` → `insertScheduleDispatchCopiedLeg` (linked copies must stay on the source `work_date`; toast + `loadHub({quiet:true})`); `applyHubMultiCellJob` → per selected `hubPersonDayKey`: `fetchScheduleBlocksForAssigneesOnDay` → `scheduleOverlapsAny` skip → `insertJobScheduleBlock` with fixed 08:00–16:00 and a fresh `newJobScheduleSharedBlockGroupId()`, then a combined added/skipped/failed toast (878–885); linked-copy applies and Copy-to-techs loop `insertScheduleDispatchCopiedLeg` per source (× per member for lanes) and summarize via `summarizeLinkedCopyApply` / `summarizeLinkedCopyLaneApply`.
- **Supabase:** `job_schedule_blocks` (via `jobScheduleBlocks.ts` + `scheduleDispatchMirrorInsert.ts`), RPC `move_job_schedule_block_group` (via the drag kernel).
- **External coupling:** `?placeJob=` router arms `hubAssignJobPlacement`; `hubTab` gates `onStartCardPlacement` (people tab only); the Hub renders the mode UI purely from these props; `phoneBoardActive` (1032) hides the banners.
- **Extraction status + risk + approach:** Inline. **High risk — extract after the data seam**, as `useScheduleDispatchHubModes` (or leave in the page initially). Keep all mode state in ONE owner so the "clear everything else" pattern can't be half-moved. The banners + multi-cell bar could move to a `ScheduleDispatchModeBanners` component (props: the mode states + cancel/stage callbacks + `phoneBoardActive`) — a low-risk JSX-only trim (~270 lines). Test `insertScheduleDispatchCopiedLeg` before touching any copy path.

### Add block modal cluster

- **Location:** `ScheduleDispatchBlockModalState` type 183–185; state 1000 + 1003–1007 + 1009–1010 (`blockModalState`, `addTimeStart`/`addTimeEnd` (defaults 08:00/16:00), `addNote`, `addSaving`, `addError`, `addBlockTimelineSegments`, `addBlockDraftByBlockId`); `openAddBlock` 1135–1179 (clears every mode except `linkedCopyMode`, builds sorted `AddBlockTimelineSegment[]` from the person-day's blocks, seeds the default range via `defaultNewBlockRangeInFirstGap`); `closeAdd` 1181–1187; display memos `blockModalPersonLabel` 1753–1763, `blockModalSupervisionWarning` 1766–1775 (`supervisionWarningFor`, v2.3612), `blockModalJobTitleForModal` 1777–1783, `blockModalWorkDate` 1785–1790, `addBlockModalTimeline` 1792–1799; `saveBlockModal` 1801–1875; JSX `ScheduleDispatchAddBlockModal` 2857–2884 (`warning={blockModalSupervisionWarning}`).
- **Save path (add):** `saveNewScheduleBlockForPersonDay` (lib; validates internally, consumes `addBlockDraftByBlockId` neighbor-time drafts) → toast → `closeAdd` → `loadHub({quiet:true})`. The `kind: 'edit'` branch is **dead on this page** — `saveBlockModal` returns early unless `jobId` is truthy and nothing on this page ever sets `kind: 'edit'` (quirk #1); it delegates to the shared `saveEditedScheduleBlockTimes` kernel (`src/lib/scheduleDispatchAddBlockSave.ts`: range validation, fresh-fetched linked-group legs, per-assignee overlap checks, group/solo update), the same kernel used by `ScheduleDispatchJobWeek`'s live edit path and Dispatch Mode → Schedule's tap-the-time edit.
- **Supabase:** `job_schedule_blocks` via lib helpers.
- **"Move day" row (v2.1377) — opt-in, and this page does NOT opt in.** `ScheduleDispatchAddBlockModal` renders a day-picker row only when a caller passes `onChangeWorkDate` **and** `mode === 'edit'`. Of the five call sites, only Dispatch Mode → Schedule passes it (it is the one surface with no drag-and-drop). Chips come from the pure kernel `src/lib/scheduleBlockMoveDayOptions.ts`; the modal keeps no day state of its own beyond "is the free-date input revealed".
- **"Remove" button (v2.1381) — second opt-in prop.** `onRemove` renders a red-outline Remove button bottom-left of the footer (edit mode only). The modal owns no deletion logic: this page (dormant here — it only wires `onRemove` for `kind: 'edit'`) and `ScheduleDispatchJobWeek` close the modal and hand the block id to their existing `requestDeleteBlock` → `RemoveScheduleBlockConfirmModal` → `deleteJobScheduleBlock` flow; Dispatch Mode → Schedule grew the same confirm-modal flow locally. Single-row delete only — crew-wide removal stays in `LinkedScheduleGroupModal`.
- **Extraction status + risk + approach:** Inline. **Medium risk.** Natural hook `useScheduleDispatchAddBlockModal` returning the modal props + `openAddBlock`; `openAddBlock` writes 15 state atoms across modes/picker/modal, and in the other direction `onStartCardPlacement` (modes) writes `blockModalState` / `addError` to close the modal, so extract alongside or after the modes hook. Do not silently delete the dead edit branch during the move (quirk #1).

### Assign-job picker cluster

- **Location:** state 1041–1047 (`hubAssignJobPickerOpen`, `hubAssignJobPickerSearch`, `hubJobEvidence`, `hubAssignJobPickerNumberQuery`, `hubCellAddContext`, `hubAssignJobPickerIntent: 'toolbar' | 'cell' | 'multi'`); `closeHubAssignJobPicker` 1052–1058; entry points `onRequestHubAddJob` 1370–1383 (toolbar +), `onHubEmptyCellOpenChoice` 1385–1391 (empty cell AND the corner triangle — both wired to it), `onRequestHubMultiCellAddChooseJob` 1581–1588 (floating bar); `onCreateNewJobFromHubJobPicker` 1590–1626 (snapshots intent/ctx/multi keys, opens `jobFormModal.openNewJob`, then after `loadHub()` routes the new job id to multi-add / add-block / placement); memos `hubAssignJobPickerRows` 1628–1644 (digits → `findJobsByNumber`; else 5-field search over hcp/name/title/address/customer; sorted `compareJobsByCreatedAtDesc` then `sortJobPickerRowsFinishedLast`), `hubAssignBidPickerRows` 1652–1682 (bid rows under their violet chip, with `blocksThisWeek` counts), money-rail evidence effect 1685–1710 (`fetchJobSearchEvidence` for ≤30 rows, 250 ms debounce, accumulating, failure-silent), `hubAssignJobPickerDuplicateAddressNotice` 1713–1719 (`findDuplicateJobAddress` while a search narrows to ≤8 rows), `hubEmptyCellChoiceSubtitle` 1721–1725, `hubAssignJobPickerSubtitle` 1727–1751; module fn `hubJobPickerSubline` 144–157 ("Nd Mon D | address", zip stripped); JSX `ScheduleDispatchAssignJobPickerModal` 2902–2983 (incl. `onOpenJobDetail` stacking Job Detail above the picker, the `notComingIn` inline confirm and the `noCallNoShow` option for `'cell'` intent).
- **Coupling:** consumes `hubMergedRows`, `hubBids`, `hubWeekBlocks`, `hubPeopleNameById`, `hubPersonDayBlocks` (data engine); dispatches into `applyHubMultiCellJob`, `openAddBlock`, `setHubAssignJobPlacement`, or the not-coming-in / NCNS handlers depending on intent.
- **Supabase:** job-search evidence reads (`jobs_ledger`, `jobs_ledger_fixtures`, `jobs_ledger_payments`, `job_schedule_blocks` via `fetchJobSearchEvidence` in `jobSearchEvidence.ts`; the page does not use its `bids`-reading `fetchBidSearchEvidence`); writes happen in the dispatched flows.
- **Extraction status + risk + approach:** Inline. **Medium-high coupling** — it is the junction of three modes plus time-off. Move with (or into) the modes hook; do not extract standalone first. The two row filters are Stage-A candidates now.

### Time-off / not-coming-in / NCNS cluster

- **Location:** `notComingInBusy` 1008; `markNotComingInForPersonDay` 1879–1953 (records unpaid time off via `recordNotComingInForUserAsStaff` → RPC `pay_staff_bulk_insert_user_time_off`, then deletes the person-day's blocks via `deleteJobScheduleBlock` with per-failure counts, layered toasts incl. `result.alreadyMarked` and `result.syncWarning`, then `loadHub({quiet:true})` + `refreshHubUserTimeOff()`); `handleMarkNotComingInTodayFromAssignPicker` 1955–1961 (picker `'cell'` path); **NCNS** `handleRecordNcnsFromAssignPicker` 1974–2069 (v2.2540, gate `canRecordNcns` 1965 = dev / master_technician / assistant-like): RPC `record_ncns_and_reject_sessions_for_day` **first** (rejects clock sessions, unwinds approved hours; any refusal stops everything), then `recordNotComingInForUserAsStaff` with `NO_CALL_NO_SHOW_NOTE`, then the same block-delete loop; mark-off confirm `markOffConfirmTarget` 2077 + `onMarkNotComingInForCell` 2080–2091 / `cancelMarkOffForCell` 2092 / `confirmMarkOffForCell` 2093–2098; undo `undoNotComingInTarget` 2103, `undoNotComingInBusy` 2114, `handleRequestUndoNotComingIn` 2116–2130 / `handleCancelUndoNotComingIn` 2132–2135 / `handleConfirmUndoNotComingIn` 2137–2182 → `removeNotComingInForUserAsStaff`; JSX `ConfirmDialog` 2886–2892, `ScheduleDispatchUndoNotComingInModal` 2893–2901.
- **Supabase:** `user_time_off` (via [`lib/notComingInTimeOff.ts`](../src/lib/notComingInTimeOff.ts)); RPCs `pay_staff_bulk_insert_user_time_off`, **`pay_staff_remove_not_coming_in_for_user_day`** (undo; handles authz + salary sync), `record_ncns_and_reject_sessions_for_day` (direct in page); `job_schedule_blocks` (block removal). Cell chips read `hubUserTimeOffByCell` ([`lib/userTimeOffByCell.ts`](../src/lib/userTimeOffByCell.ts) — tested).
- **Confirm + honest undo copy (v2.2919, J18-F3):** the empty-cell `off` button no longer writes on click — `onMarkNotComingInForCell` sets `markOffConfirmTarget` and a page-level `ConfirmDialog` (copy from [`lib/scheduleDispatchNotComingInCopy.ts`](../src/lib/scheduleDispatchNotComingInCopy.ts) `markOffConfirmCopy`) calls `markNotComingInForPersonDay` on confirm. The same module's `undoNotComingInCopy` renders `ScheduleDispatchUndoNotComingInModal`; `undoNotComingInRestoresBlocks()` is `false` (the undo RPC deletes the `user_time_off` row only), which is why the modal carries an unconditional "removed blocks don't come back" line — it cannot tell whether the mark came from `off` (removes nothing), the picker (deletes the day's blocks), or NCNS.
- **Role gate (v2.2892):** the three time-off entry points (`onMarkNotComingInForCell`, `onRequestUndoNotComingIn`, the picker's `notComingIn` option) are passed only when `canTimeOff = canEdit && canWriteTimeOff(role)` — mirrors the RPC gate (dev / master_technician / assistant / controller). `canEdit` alone armed 63 dead `off` buttons for a superintendent (J18-N2). NCNS has its own `canRecordNcns` gate (link visibility only; the RPC enforces payroll access OR team-lead).
- **Extraction status + risk + approach:** Inline. **Low-med coupling, medium risk** (payroll-affecting writes, untested). Good early hook candidate (`useScheduleDispatchNotComingIn`) once the data seam exists — needs `hubPeopleNameById`, `hubPersonDayBlocks`, `hubUserTimeOffByCell`, `loadHub`, `refreshHubUserTimeOff`; the two picker-entry handlers read `hubCellAddContext` + `closeHubAssignJobPicker`. `handleMarkNotComingInTodayFromAssignPicker` is already a thin wrapper; `handleRecordNcnsFromAssignPicker` is not — after the context read it runs the whole NCNS flow and writes `notComingInBusy` — so move its body into the hook as a `(userId, workDate, details)` call and keep only the context read + picker close in the page. Stage-A the duplicated block-delete loop (1894–1905 and 2010–2021) first.

### DnD + delete + note + share + linked-group + move sheet + person-day modals

- **Location:** `sensors` (PointerSensor, 8px activation) 2215 + `handleHubDragEnd` 2217–2227 → [`executeScheduleDispatchBlockReassign`](../src/lib/scheduleDispatchDragEnd.ts) (tested; resolves against `hubBlockById`, success → quiet reload); delete cluster `deleteBlockId`/`deleteBlockBusy` 1001–1002 + `requestDeleteBlock` 2184–2190 / `cancelRequestDeleteBlock` 2192–2195 / `confirmDeleteBlock` 2197–2213 (+ Escape effect 1086–1093) rendering `removeScheduleBlockConfirmModal` (2433–2440 → `RemoveScheduleBlockConfirmModal`); **Move sheet** (v2.2736) `moveSheetBlock`/`moveSheetSaving`/`moveSheetError` 1015–1017 + `saveMoveSheet` 1327–1353 (→ `moveScheduleDispatchBlockTo`; errors land in the sheet) rendering `ScheduleDispatchMoveBlockSheet` 2997–3015; **person day** (v2.1817) `personDayModal` 316 → `ManagePersonDayModal` 3016–3025 (card clock button; `onChanged` → quiet reload); block-note cluster `blockNoteEdit`/`blockNoteBusy`/`blockNoteError` 1037–1039 + `saveHubBlockNote` 2401–2431 (group-aware: `updateJobScheduleBlockGroup` when `shared_block_group_id`, else `updateJobScheduleBlock`) rendering `ScheduleDispatchBlockNoteModal` 3026–3037; `shareModalOpen` 396 + `ScheduleShareModal` 2816–2820 (Share button injected via `weekNavRightSlot` 2711–2728, `canEdit && !isTomorrow`); `linkedGroupModalId` 314 + `LinkedScheduleGroupModal` 2984–2996 (receives `weekStart`/`weekEnd`/`getJobDisplayTitle`/`addPeople`; does its own fetch); `openHubJobDetail` 2380–2399 (Job Detail modal context with `scheduleContext` prefill; bid anchors skip); `closeJobDetail` effect 974–977 (**depends on the stable `closeJobDetail` fn, NOT the context object** — quirk #5).
- **Share modal internals (v2.2919, J18-F8/F9):** `ScheduleShareModal` registers with the Tier-2 #42 modal stack (`useModalStackEntry(open)`) and closes on Escape only when topmost and not sending, ignoring keys the recipient `SearchableSelect` already consumed (`ev.defaultPrevented`). Its Send now tab renders [`ScheduleEmailPreviewPanel`](../src/components/schedule/ScheduleEmailPreviewPanel.tsx) fed by `supabase.rpc('list_schedule_blocks_for_share', { p_viewer: me, p_start, p_end })` — the same call the edge function's instant path makes from the sharer's visibility — shaped by [`lib/scheduleSharePreview.ts`](../src/lib/scheduleSharePreview.ts) (tested; mirrors `_shared/scheduleShareCore.ts`: Include-set filter, `— · Job` / `(Unassigned)` fallbacks, per-person grouping). `ScheduleDayEmailModal` (Dashboard clock strip) shares the panel + Escape handling and previews `list_job_schedule_blocks_for_schedule_email(p_recipient, p_work_date)` flat in time order.
- **Supabase:** `job_schedule_blocks` (delete/update via lib); RPC `move_job_schedule_block_group` (drag / move sheet); read-only preview RPCs `list_schedule_blocks_for_share` / `list_job_schedule_blocks_for_schedule_email` (SECURITY DEFINER, granted to `authenticated` in the baseline).
- **Extraction status + risk + approach:** Page-level modals opened from multiple flows → **stay in the parent** per the playbook. Low risk; the Move sheet trio could become a small `useScheduleDispatchMoveSheet` after the data seam, but the value is low.

### Vestigial job-week residue (read before touching anything)

`const jobId = ''` (201) is a **constant**. The page still carries the old hub+job-week dual-mode skeleton: `jobTitle`/`teamMembers`/`blocks` state (362–364; reset effect 403–408; `jobTitle` is still read by `openAddBlock` and `blockModalJobTitleForModal`'s edit path), `blockById`/`nameByUserId` memos (986–998), a no-op `load()` (967–969), an effect that clears placement when `jobId` is set (979–984), and `jobId ? … : …` branches inside `openAddBlock`, `onStartCardPlacement`, `onCardPlacementPickCell`, `placementSourceBlock`, `blockModalPersonLabel`, `saveBlockModal` (whole edit path), `markNotComingInForPersonDay`, `handleRecordNcnsFromAssignPicker`, `handleConfirmUndoNotComingIn`, `confirmDeleteBlock`, `shiftWeek` / `goThisWeek`, `runOfficeEnsure`, `loadHub`, and the `?week` normalize / load-trigger (897–903) / `placeJob` effects. The live job-week view is [`ScheduleDispatchJobWeek`](../src/components/schedule/ScheduleDispatchJobWeek.tsx), routed by `ScheduleDispatch.tsx`. **Decomposition must preserve these dead branches verbatim** (behavior-preserving rule); deleting them is a separate, later cleanup PR — flag it, don't fold it in.

---

## The shared substrate

There is **no per-record selection pointer** here (no `setSharedBid` analog — nothing like `?id=` selects a block/job across tabs; the closest thing, `?jobId=`, *leaves* this surface entirely). What the tabs share instead:

1. **The week-scoped data engine** (`loadHub` + its 22 state atoms, 18 memos and satellites, dossier above). People and Jobs are two projections of the same `hubWeekBlocks`/`hubJobs`; the Day tab re-fetches independently and pings back via `onDayScheduleChanged`. This engine is the page's `useBidPricingEngine` equivalent, and **building the `useScheduleDispatchHubData` seam is the unlock** for every Page-side hook.
2. **The interaction-mode state machine** (`cardPlacementMode` ∥ `linkedCopyMode` ∥ `hubAssignJobPlacement` ∥ `hubMultiCellAdd*` ∥ `plusMenuBlockId`) — mutually exclusive, page-owned, URL-armed (`?placeJob=`), rendered by the Hub purely as props. It must remain a single owner.
3. **The URL router** (`week`/`day`/`hubTab`/`placeJob`/`focusPerson`) — permanent parent resident.

Consequence for extraction: Hub-file splits are prop-preserving file moves (cheap); Page-file splits are hook extractions where the parent destructures the hook result so downstream references don't change.

---

## Shared infrastructure

- **Contexts consumed:** `useAuth` (page), `ToastContext` (page + Hub card), `JobFormModalContext` + `JobDetailModalContext` (page), `DispatchNoteRequirementsContext` (Hub card + panel — the Hub reads a context directly rather than props; an extracted card keeps doing so). `BidPreviewModalContext` contributes only the `OPEN_BID_EDIT_QUERY` constant.
- **Supabase inventory:** see the data-engine, modes, picker, time-off and modal dossiers; `dispatch_swim_lanes` + `dispatch_swim_lane_members` are still cast `as never` in `lib/dispatchSwimLanes.ts`, although both tables are now in the generated types (leftover casts). No realtime subscriptions anywhere on this surface.
- **Deep-link senders (verify before changing URL params):** Dashboard clock strip (`DashboardTeamActiveClockStrip`) → `?week=&focusPerson=`; `PeopleHoursDayAuditModal`, the Team board (`useTeamBoardActions`), `QuickfillScheduleSection` and the user-review `UserDayScheduleSection` → `?week=&day=`; `UserWeekScheduleSection` / `UserMonthScheduleSection` and `ScheduleDispatchJobWeek`'s back button → `?week=`; `SubsOnSiteForDay` ("Add a site visit ›") → `?placeJob=<jobId>&hubTab=people`; Quickfill tomorrow embed (no URL; its "Open full Schedule Dispatch" link → `?week=&day=`). `?jobId=` senders (Job Detail, Job Mode card, Stages rows, Quickfill) go to the sibling JobWeek surface, as does `openJobWeekGrid` **for job anchors only** — a `bid:` anchor navigates to `/bids?bidId=&openBidEdit=1` (v2.2861) and records `schedule_block_opened` (`#job` / `#bid`) on `ui_nav_clicks`.

## Preserve-quirks list (odd but load-bearing)

1. **`jobId = ''` vestigial dual-mode** — dead branches everywhere; keep verbatim (dossier above). The add-block modal's edit mode and its `onRemove` are dormant on this page for the same reason.
2. **Quiet vs loud loads** — `loadHub({quiet:true})` never sets `hubLoading`; the `hubLoadSeqRef` monotonic guard means an older loud load finishing late cannot clear a newer load's spinner. Every schedule-mutation refresh is quiet; job saves reached through the Job Form / Job Detail contexts (`onCreatedJobId`, `onSaved`, `onEditJobSaved`) reload loud with `loadHub()`.
3. **`hubUserTimeOffPrimedRef`** — skips the standalone `user_time_off` refetch whenever `(weekStart, weekEnd, rosterKey)` still equals what `loadHub` last seeded (the ref is never cleared, so every same-key rerun skips, not just one); a week or roster change refetches.
4. **Two `HubPeoplePanel` mounts** in the Hub shell (embed vs people tab) with hand-duplicated prop spreads (75 / 76 props; only `weekNav` differs).
5. **`closeJobDetail` effect dependency** — must depend on the stable callback, not the `jobDetailModal` context object (context identity changes on open → the modal used to self-close instantly).
6. **Missing-note indicator + card note colors gate on `work_date < scheduleTodayYmd`** — past days never light up red (`effectiveNoteRequirement`); `missingNoteCount` also returns 0 for past focus days.
7. **Multi-cell add uses a fixed 08:00–16:00 window** and a fresh `shared_block_group_id` per inserted block (not one group across the selection); overlapping cells are skipped, not errored.
8. **Linked copies must land on the source block's `work_date`** — enforced in cell-pick (`linkedWrongDay` graying) AND in `onCardPlacementPickCell`; lane/person applies and Copy to techs always use `source.work_date`.
9. **Assign-picker stacking** — Job Detail (and Edit Job from it) stack **above** the picker; closing them lands back on the picker (comment at `onOpenJobDetail`, 2925).
10. **Per-device prefs are split across owners** — `personSort` (`pipetooling_dispatch_people_sort_v1`, default `lanes`) lives in the panel; `hideWeekend` (default **hidden**), `highlightLinkedGroups` and `phonePeopleViewPref` live in the page.
11. **Wages are keyed by trimmed person NAME** (`people_pay_config.person_name` matched against `fetchUserNamesForIds`), not user id; 'Unknown' names are excluded; missing wage renders `$0` with the explanatory tooltip.
12. **`hubMergedRows` sort:** `totalBlocks` desc, then `hcp_number` **descending** with `numeric: true`.
13. **Drag disabled ≠ hidden** — the red drag strip stays visible and clicking it toasts `SCHEDULE_DISPATCH_DRAG_DISABLED_READONLY_MESSAGE`; drag is also disabled during multi-cell add and linked-copy modes.
14. **Escape handling is per-mode effects on `window`** (placement/assign 1066, linked copy 1105, multi-cell 1114, delete-confirm 1086 each have their own listener with different guards, e.g. delete ignores Esc while busy).
15. **Tomorrow variant week-nav escapes the embed** — `shiftWeek`/`goThisWeek`/`openJobWeekGrid` `navigate()` to `/schedule-dispatch?…` instead of mutating local state.
16. **`move_job_schedule_block_group` has NO date predicate** — it moves every leg of a `shared_block_group_id` group (optionally job-filtered) whatever `work_date` each sits on. Since "Add person to crew" (v2.1371) writes one leg per distinct job/date/window, **multi-day groups are routine and moving one day of one collapses the whole group onto a single date**. `saveEditedScheduleBlockTimes` guards this (refuses a day move when the fetched legs span `>1` `work_date`, v2.1377), but **`moveScheduleDispatchBlockTo` still calls the RPC unguarded** — and it now backs three entry points (drag via `executeScheduleDispatchBlockReassign`, tap-to-move placement, the Move sheet). Proper fix is a date-scoped `move_job_schedule_block_group_day(job, group, from_date, to_date)` RPC that every path shares; until then do not add new callers.
17. **Two derivations of "phone board active"** — the page's `phoneBoardActive` (1032: `!isTomorrow && hubTab === 'people' && phonePeopleView === 'board'`) hides the banners; the panel's (`onPhonePeopleViewChange != null && phonePeopleView === 'board'`) swaps board for grid. They agree only because the tomorrow variant passes `undefined` for both phone props — keep it that way.
18. **Lateness refreshes on every quiet reload** — `refreshHubLateness` depends on `hubWeekBlocks` + `hubJobTitleById`, so each mutation also re-fetches the week's clock-ins; a data hook must keep that trigger.
19. **NCNS order** — the incident RPC runs while the day's blocks still exist (with zero clock sessions it only accepts "scheduled, no clock time"); clearing blocks first would make it refuse.
20. **Day-first redirect is one-shot** (`hubMobileDayFirstAppliedRef`, mount-only) and skipped when the URL names `hubTab` or `placeJob`.

---

## Stage-A pure-logic inventory (→ `src/lib/*` + tests before component moves)

Most calc already lives in tested libs (see Test coverage). What remains inline, individually shippable:

| Candidate | Currently | Target |
|---|---|---|
| Day labels `shortDowLabel` / `hubDayColumnHeaderLabel` / `hubDayColumnHeaderStacked` + toolbar styles `HUB_PEOPLE_TOOLBAR_BTN_H` / `hubPeopleToolbarBtn` / `hubPeopleToolbarIconBtn` | module-level Hub 126–143, 3438–3462; used across Jobs / People / EM | one shared module (labels → lib + test; styles → consts) — **prerequisite for splitting the Hub file** |
| Phase-D wage mapping (`wageByName` → `wageByUserId`) | inline in `loadHub` 761–791 | `buildHourlyWageByUserId(rows, rosterIds, nameByUserId)` + test (quirk #11) — **money-adjacent, feeds the EM $ column** |
| Block-delete loop + toast copy for not-coming-in / NCNS | duplicated 1894–1905 and 2010–2021 | `removePersonDayBlocks(ids)` → `{removed, failed}` + a copy kernel next to `scheduleDispatchNotComingInCopy` + test |
| `hubMergedRows` merge + sort | inline memo 608–626 | `buildHubMergedRows(jobs, summaryRows)` in `lib/scheduleDispatchHub.ts` + test (quirk #12) |
| `hubAllPeopleRows`, `hubJobTitleById`, `hubJobAddressById` builders | inline memos 466–472, 447–464, 593–604 | pure fns + tests (archived filter + name sort; `bid:` keys, scheduled-only titles overridden by live bids) |
| `hubJobPickerSubline` | module-level page 144–157 | `lib/scheduleDispatchHub.ts` + test (days-ago + zip strip + join rules) |
| `hubAssignJobPickerRows` / `hubAssignBidPickerRows` filters | inline memos 1628–1644, 1652–1682 | `filterHubJobPickerRows` / `buildHubBidPickerRows` + tests (5-field match, digits path, per-bid block counts) |
| `applyHubMultiCellJob` result summary | inline 878–885 | `summarizeMultiCellAddResult({added, skippedOverlap, failed})` + test (mirrors the tested linked-copy summarizers) |
| `expectedManpowerDayStats` reduction | inline memo `HubPeoplePanel` 1972–1987 | pure fn in `lib/scheduleDispatchExpectedManpower.ts` + test |
| `missingNoteCount` loop | inline memo `HubPeoplePanel` 2170–2196 | `countBlocksMissingNoteForDay(dayYmd, todayYmd, people, personDayBlocks, requirementForBlock)` in `lib/dispatchNoteRequirements.ts` + test (past-day zero rule) |
| `HubJobsPanel.filteredRows` / `HubPeoplePanel.filteredAssignees` filters | inline memos 206–221, 2064–2078 | pure filter fns + tests (assignee filter's job-title-in-week and lane-query branches) |
| Existing **untested** lib code this surface leans on | `lib/scheduleDispatchHub.ts` (`parseHubPersonDayKey`, `buildPersonDayBlockMap`, `aggregateWeekSummariesByJob`, `blocksToJobWeekSummaries`, `formatScheduleDispatchHubJobTitle`), `lib/scheduleDispatchMirrorInsert.ts` (every copy write), `lib/scheduleDispatchDnd.ts` (drag-id contract — exercised only indirectly by `scheduleDispatchDragEnd.test.ts`), `lib/scheduleDispatchLinkedGroupPalette.ts`, `lib/usersTabRosterRoleSections.ts`, `notComingInTimeOff.recordNotComingInForUserAsStaff` | add colocated `*.test.ts` (no code moves needed) |

---

## Recommended extraction order (value ÷ risk)

1. **Stage-A sweep** — the table above; start with the shared chrome module (unblocks every Hub-file split), then tests for `scheduleDispatchMirrorInsert` / `scheduleDispatchDnd` / the untested `scheduleDispatchHub.ts` kernels, then the money-adjacent wage mapping and the duplicated block-delete loop.
2. **Hub file splits (pure moves, no state relocation):**
   a. `HubExpectedManpowerSection` → own file (~650 lines out, ~12 inputs, calc already tested; collapse state + 10 memos + style consts move with it; `hubExpectedManpowerDayKey` stays page-owned as a controlled prop).
   b. `HubJobsPanel` → own file (446 lines, 12 props, 4 own states — smallest; add a render smoke).
   c. `HubPeopleBlockCard` + `HubHiddenBusyPlaceholders` + `HubPeopleDayCell` → own file (1,075 lines, stateless).
   d. `HubPeoplePanel` → own file (~900 lines left; `HubPeoplePanelProps` already exists). Fix nothing else; both shell mounts keep working.
   After (d), `ScheduleDispatchHub.tsx` is imports + `Props` + the shell (~870 lines).
3. **Data seam** — `useScheduleDispatchHubData` (22 atoms + 18 memos + `loadHub` / lateness / time-off / office ensure / swim lanes). Parent destructures the result; zero JSX changes.
4. **Not-coming-in + NCNS hook** — `useScheduleDispatchNotComingIn` (4 atoms, 9 callbacks; the NCNS body moves in, only the two picker entries' context reads stay in the page).
5. **Mode banners** — `ScheduleDispatchModeBanners` (2452–2687 + 2821–2856, JSX-only, ~270 lines).
6. **Modes + picker + add-block modal** — extract together (or in that dependency order) as `useScheduleDispatchHubModes` / `useScheduleDispatchAddBlockModal`. Highest-risk step — the mutual-exclusion invariant (15-writer atoms) must stay in one owner.
7. **Deferred cleanup PR (separate, NOT behavior-preserving):** remove the `jobId = ''` vestigial branches, dedupe the two `HubPeoplePanel` prop spreads, and (separately, with a migration) the date-scoped group-move RPC for quirk #16.

**What must STAY in `ScheduleDispatchHubPage`:** the URL/variant router (`week`/`day`/`hubTab`/`placeJob`/`focusPerson` + tomorrow-variant navigation + day-first redirect), the role gates/redirect, the view prefs (`hideWeekend`, `highlightLinkedGroups`, `phonePeopleViewPref` → `phoneBoardActive`), the `DndContext` + drag-end wiring, all cross-flow modals and sheets (add block, delete confirm, mark-off confirm, undo not-coming-in, assign-job picker, linked group, move sheet, person day, block note, share) and the Job Detail/Job Form context calls, `hubExpectedManpowerDayKey` and its week-validity effect, and ownership of the mode state machine (even if implemented via a hook, the page remains the single mount point).

Definition of done per step, verification gates (`npm run typecheck && npm run lint && npm test` after every move), and anti-patterns: see [`PAGE_DECOMPOSITION_PLAYBOOK.md`](./PAGE_DECOMPOSITION_PLAYBOOK.md).

## The schedule ledger (v2.3726)

Every add, move, reassign and remove on `job_schedule_blocks` writes one `schedule_block_events` row (`change`, the old and new window + assignee, the actor at trigger time) through the SECURITY DEFINER trigger function `job_schedule_blocks_to_ledger()`, fired by three triggers (insert · update of `work_date`, `time_start`, `time_end`, `assignee_user_id` · delete). Nothing on the hub changes; the ledger is what People → Day book reads for *Updated the schedule · N people · N blocks · Thu–Fri* (`docs/recent-features/v2.3726.md`). A write with no `auth.uid()` (a backfill, a service-role function) lands with a NULL actor and is counted on the Day book's day header as a system row, never credited to a person.
