# Page Decomposition Playbook

---
file: docs/PAGE_DECOMPOSITION_PLAYBOOK.md
type: Engineering / Refactor Process
purpose: A repeatable, generic process for breaking a multi-thousand-line "God component" page into per-tab components + shared hooks + tested pure logic, without re-deriving the strategy each time. Generalizes the method proven on Bids.tsx (~18,800 lines at the start) and People.tsx (~21,435). Also the one home for the large-file inventory and the seams that cut across maps.
audience: Developers, AI Agents
last_updated: 2026-09-25
---

## What this is

> Line counts below were re-measured 2026-09-25 at `a05cef4c4`, the commit the 2026-09-25 map sweep re-read every map at. Each map's own `mapped_at:` is the authority for its line numbers.

The repo still has several God components:

**Pages and page-scale surfaces:**

| File | Lines | Map | Notes |
|---|---|---|---|
| `src/pages/Materials.tsx` (+ `SupplyHousesTab` 1,898) | 2,012 | [`MATERIALS_TABS_ARCHITECTURE.md`](./MATERIALS_TABS_ARCHITECTURE.md) | page **decomposition complete** (all 7 tab bodies out, 3 seam hooks); the weight is now `SupplyHousesTab` — Stage A its untested AP money math, then lift the 428-line invoice form into a modal |
| `src/pages/Estimates.tsx` | 6,982 | [`ESTIMATES_TABS_ARCHITECTURE.md`](./ESTIMATES_TABS_ARCHITECTURE.md) | two page components behind a URL router; title/bucket/owner helpers are in lib, every detail region is inline — Stage A the untested draft-line money + `buildDraftPersistPayload`, then the free `EstimateListTable` file move |
| `src/pages/Workflow.tsx` | 4,354 | [`WORKFLOW_PAGE_ARCHITECTURE.md`](./WORKFLOW_PAGE_ARCHITECTURE.md) | region-based (not tabbed); `StepFormModal` + `StepCommitmentPanel` out, no body region moved — Stage A the untested money (unified rows, ledger rail, paste parser), then `WorkflowSuperintendentsStrip` |
| `src/pages/People.tsx` | 4,700 | [`PEOPLE_TABS_ARCHITECTURE.md`](./PEOPLE_TABS_ARCHITECTURE.md) | every tab extracted, but the Payroll (~1,700 lines), Hours-shell (~1,500) and Users (~550) clusters stay in the parent — Stage A the duplicated pay-report assembly first |
| `src/pages/Bids.tsx` | 5,293 | [`BIDS_TABS_ARCHITECTURE.md`](./BIDS_TABS_ARCHITECTURE.md) | all 26 tab keys render extracted children, but the parent regrew with glue (robot layer, Edit Bid controller, URL routing, lens bars) — dead-code sweep, then route/attestation kernels + `BidsLensBar` |
| `src/pages/Prospects.tsx` (+ `TeamProspectsTab` 2,383) | 3,962 | [`PROSPECTS_TABS_ARCHITECTURE.md`](./PROSPECTS_TABS_ARCHITECTURE.md) | no tab moved yet; 16 tested kernels already in `lib/prospects` — move `TeamProspectsTab`'s 7 module components out, then the copy-template kernels, then `ProspectsActivityTab` |
| `src/pages/Banking.tsx` (+ `BankingMercuryAccountingTab` 2,936, `MercuryTransactionAllocationsModal` 1,705) | 2,269 | [`BANKING_TABS_ARCHITECTURE.md`](./BANKING_TABS_ARCHITECTURE.md) | 6 of 8 Mercury tabs out (Ledger, User Sort inline) — point the allocations modal at the tested `lib/mercurySplitMath`, then extract User Sort |
| `ScheduleDispatchHub` + `ScheduleDispatchHubPage` | 4,126 + 3,040 | [`SCHEDULE_DISPATCH_ARCHITECTURE.md`](./SCHEDULE_DISPATCH_ARCHITECTURE.md) | nothing extracted since the first map — Stage A a shared chrome module, then `HubExpectedManpowerSection` |
| `src/pages/Checklist.tsx` (+ `ChecklistTechTreeTab` 2,429) | 3,810 | [`CHECKLIST_TABS_ARCHITECTURE.md`](./CHECKLIST_TABS_ARCHITECTURE.md) | 4 in-file tab components, half of Stage A in `lib/checklist*` — seam PR, then the `ChecklistHistoryTab` file move; the completion engine's off-by-one needs an owner call |
| `src/pages/JobTally.tsx` | 2,403 | [`JOB_TALLY_ARCHITECTURE.md`](./JOB_TALLY_ARCHITECTURE.md) | two nearly independent tabs, no tab extracted (the modals and most kernels are) — test the unlinked-count kernels, then `TallyMaterialsEstimateTab` (kept mounted); payroll-gate defects R1/R2 ship as their own fix |
| `src/pages/Quickfill.tsx` (+ `QuickfillScheduleSection` 1,885) | 2,476 | [`QUICKFILL_ARCHITECTURE.md`](./QUICKFILL_ARCHITECTURE.md) | section bodies out; framework + schedule section inline — Stage A the module helpers, delete the phone code the round made unreachable, then move the section wrapper |
| `src/pages/Jobs.tsx` | 2,274 | [`JOBS_TABS_ARCHITECTURE.md`](./JOBS_TABS_ARCHITECTURE.md) | tab router, every tab extracted — Stage A the untested `jobSummaryData` P&L memo, then the ten Pipeline deep-link effects |
| `src/pages/Settings.tsx` (+ `SettingsDashboardTab` 1,642) | 1,741 | [`SETTINGS_TABS_ARCHITECTURE.md`](./SETTINGS_TABS_ARCHITECTURE.md) | engine campaign done; left is the Your dashboard membrane (109 props) — Stage A the financial-pin money, then make four Dashboard sections self-contained |
| `src/pages/Dashboard.tsx` (+ `DashboardFinancialsSection` 1,824) | 1,852 | [`DASHBOARD_SECTIONS_ARCHITECTURE.md`](./DASHBOARD_SECTIONS_ARCHITECTURE.md) | every section extracted, the parent is glue — Stage A the finance drill-down partition, then move single-consumer state into its section |
| `src/pages/Calendar.tsx` | 2,044 | [`CALENDAR_PAGE_ARCHITECTURE.md`](./CALENDAR_PAGE_ARCHITECTURE.md) | only two modals out, 15 untested module helpers — fix the suspected month-arrow snap-back first, then the `calendarGrid` / `calendarDayItems` kernels |
| `src/pages/Documents.tsx` | 1,926 | [`DOCUMENTS_PAGE_ARCHITECTURE.md`](./DOCUMENTS_PAGE_ARCHITECTURE.md) | four props-clean ledgers already componentized in-file — tests + per-ledger kernels, then verbatim file moves |
| `src/pages/SubPortal.tsx` | 1,563 | [`SUB_PORTAL_ARCHITECTURE.md`](./SUB_PORTAL_ARCHITECTURE.md) | public page, 7 in-file components; payload/i18n kernels in `lib/subPortal` — Stage A `subPortalMoney` + one `postSubPortal` client, then `DocRow` |

**Extracted tabs that regrew into God components:**

| File | Lines | Map | Notes |
|---|---|---|---|
| `src/components/bids/BidsTakeoffTab.tsx` | 3,186 | [`BIDS_TAKEOFF_TAB_ARCHITECTURE.md`](./BIDS_TAKEOFF_TAB_ARCHITECTURE.md) | T0–T9 seams shipped, then features regrew it — one materials-by-stage input builder (Stage A), then `useTakeoffMaterialsByStage` + `useTakeoffViewState` |
| `src/components/people/PeopleReviewTab.tsx` (+ `teamSummary/drilldowns.tsx` 1,625, `buildTeamSummaryHtml.ts` 1,575) | 4,167 | [`PEOPLE_REVIEW_TAB_ARCHITECTURE.md`](./PEOPLE_REVIEW_TAB_ARCHITECTURE.md) | popup builder, kernels and drilldowns out, then it regrew — delete the dead `forTeamSummary` path, then move `loadTeamReviewUnion` to lib |
| `src/components/jobs/JobsStagesTab.tsx` (+ `jobsStagesRowShared` 1,518) | 5,099 | [`JOBS_STAGES_TAB_ARCHITECTURE.md`](./JOBS_STAGES_TAB_ARCHITECTURE.md) | toolbar, dialogs and rows out, but back to 120 `useState` — Stage-A sweep II, then the `useStagesBilledMoneyData` seam; the always-mounted contract is the hazard |
| `src/components/people/PeopleContractsTab.tsx` + `PeopleOverheadTab.tsx` | 3,670 + 2,557 | [`PEOPLE_CONTRACTS_OVERHEAD_TABS_ARCHITECTURE.md`](./PEOPLE_CONTRACTS_OVERHEAD_TABS_ARCHITECTURE.md) | Contracts has no Stage A yet; Overhead mostly out with tests — Stage A both tabs' untested money/row math, then `ContractSendForSignatureModal` |
| `src/components/jobs/JobsJobSummaryTab.tsx` | 3,276 | [`JOBS_JOB_SUMMARY_TAB_ARCHITECTURE.md`](./JOBS_JOB_SUMMARY_TAB_ARCHITECTURE.md) | zero hooks; views, toolbar and Job cell out, the expanded row inline — decide the dead person-filter state, then the person-footer kernel + `JobSummaryAltView` |
| `src/components/bids/BidsPricingTab.tsx` + `BidsLaborTab.tsx` (+ `useBidPricingEngine` 1,819) | 5,122 + 1,451 | [`BIDS_PRICING_LABOR_TABS_ARCHITECTURE.md`](./BIDS_PRICING_LABOR_TABS_ARCHITECTURE.md) | P3–P5, L5, L6 out — Stage A `scenarioPricingRows` (the money adapter is hand-written 4× in Pricing; one copy shipped a $0.00 bug), then a dead-code PR |
| `src/components/bids/BidSubmissionFollowupTab.tsx` | 2,214 | [`BID_SUBMISSION_FOLLOWUP_TAB_ARCHITECTURE.md`](./BID_SUBMISSION_FOLLOWUP_TAB_ARCHITECTURE.md) | nothing extracted since 2026-07-29; Bids-by-GC added untested bucketing — Stage A `bucketSubmissionRows`, then the two PDF builders |
| `src/components/projects/ProjectsForecastSpecificTab.tsx` (+ `ProjectsJobHistoryDayModal` 1,595, stage modal 1,471) | 2,326 | [`PROJECTS_FORECAST_TABS_ARCHITECTURE.md`](./PROJECTS_FORECAST_TABS_ARCHITECTURE.md) | pure logic in tested libs except the money — a `workflowMoneyTotals` kernel shared with Workflow, then day-modal Stage A |
| `src/components/people/PeopleVehiclesTab.tsx` | 2,946 | [`PEOPLE_VEHICLES_TAB_ARCHITECTURE.md`](./PEOPLE_VEHICLES_TAB_ARCHITECTURE.md) | regrew from a thin wrapper to 95 `useState` and 13 inline dialogs — Stage A ledger helpers + fleet money, then the self-contained dialogs; keep panel state in the tab or make `loading` first-load-only (its own PR) before any panel move |
| `src/components/bids/BidsBidBoardTab.tsx` + `BidFormModal.tsx` | 1,983 + 1,695 | [`BIDS_BOARD_FORM_ARCHITECTURE.md`](./BIDS_BOARD_FORM_ARCHITECTURE.md) | neither decomposed internally; the board is hot — Stage A board bucketing + form kernels, then `BidCopyBidDialog` |
| `src/components/bids/BidsCoverLetterTab.tsx` + `BidsSubmittalsTab.tsx` + `BidsBuilderReviewTab.tsx` | 1,840 + 1,511 + 1,506 | [`BIDS_DOCUMENT_TABS_ARCHITECTURE.md`](./BIDS_DOCUMENT_TABS_ARCHITECTURE.md) | nothing moved out — Stage A `planCoverLetter` (untested chain that sets `bid_value`), then Submittals adopts `firstRevisionClient`; Builder Review is really the Followup "By builder" lens |

**Modals and standalone components:**

| File | Lines | Map | Notes |
|---|---|---|---|
| `src/components/jobs/JobFormModal.tsx` | 5,457 | [`JOB_FORM_MODAL_ARCHITECTURE.md`](./JOB_FORM_MODAL_ARCHITECTURE.md) | every section extracted (40 children), but the shell regrew (97 `useState`, highest churn in the repo) — move the labor loader onto the tested `useJobDetailSubLaborCost`, then the three inline overlays and the footer |
| `src/components/DashboardMyTimeDayEditorModal.tsx` | 3,981 | [`MY_TIME_DAY_EDITOR_MODAL_ARCHITECTURE.md`](./MY_TIME_DAY_EDITOR_MODAL_ARCHITECTURE.md) | 13 mounts; cluster renderers, sub-modals and timeline kernels out, the save/dirty kernels and save ladder inline and untested — Stage A, then Not-coming-in + NCNS |
| `src/components/jobs/SendRecordInvoiceModal.tsx` | 3,887 | [`SEND_RECORD_INVOICE_MODAL_ARCHITECTURE.md`](./SEND_RECORD_INVOICE_MODAL_ARCHITECTURE.md) | 13 children out, no tab yet — `buildStripeBillLinePlan` (fee math copied 3×) + `parseEnsurePrimaryResponse`, then the HCP tab |
| `src/components/DashboardTeamActiveClockStrip.tsx` + `ClockInOutButton.tsx` | 3,087 + 2,458 | [`CLOCK_SURFACES_ARCHITECTURE.md`](./CLOCK_SURFACES_ARCHITECTURE.md) | nothing extracted — Stage A into `src/lib/clock/` (untested payroll rules + duration math), then the reject-confirm dialog and the Currently In cluster |
| `src/components/jobs/JobsSubLaborFormModal.tsx` + `DetailJobModal.tsx` + `JobsCombineSeparateModal.tsx` | 2,623 + 2,711 + 1,819 | [`JOBS_MODALS_ARCHITECTURE.md`](./JOBS_MODALS_ARCHITECTURE.md) | only child pieces out — Stage A money kernels (parts total, `lineLaborCost`, combine preview), then the labor-book section and the duplicate-address finder |
| `src/components/jobs/BankPaymentsModal.tsx` + `CollectPaymentModal.tsx` | 2,618 + 1,655 | [`AR_PAYMENT_MODALS_ARCHITECTURE.md`](./AR_PAYMENT_MODALS_ARCHITECTURE.md) | Bank partly split (8 `ar/*` components, 12 kernels) but its apply payload is inline and untested; Collect unsplit with no component test — `buildArApplyAllocations` first |
| `src/components/jobs/JobsGcReviewModal.tsx` | 2,442 | [`GC_REVIEW_MODAL_ARCHITECTURE.md`](./GC_REVIEW_MODAL_ARCHITECTURE.md) | 16 kernels + 5 children out, 56 `useState` and 5 overlays inline — move `ScheduleWhenControls`, then the round-chip and send-payload kernels |
| `src/components/jobs/LienDeskModal.tsx` | 2,002 | [`LIEN_DESK_ARCHITECTURE.md`](./LIEN_DESK_ARCHITECTURE.md) | ~2,600 lines of panes out; notice pane, footer and paper engine inline — Stage A the footer words + sent-packet money, then `useLienDeskRunner` / `useLienNoticeDraft` |

**Edge functions and shell:**

| File | Lines | Map | Notes |
|---|---|---|---|
| `src/App.tsx` + `src/components/Layout.tsx` | 383 + 2,332 | [`APP_SHELL_ARCHITECTURE.md`](./APP_SHELL_ARCHITECTURE.md) | App is the route table (tests parse its `<Route>` literals — they stay) + providers; Layout is one component — Stage A dedupe (`roleHomePath`, sign-out / impersonation), then `PinModeFooter` |
| `supabase/functions/twin-mcp/index.ts` | 3,362 | [`TWIN_MCP_SERVER_ARCHITECTURE.md`](./TWIN_MCP_SERVER_ARCHITECTURE.md) | 48 verbs in one 2,260-line `callTool`; neither type-checked nor linted — `TOOLS` → `tools.ts` with a count/seat-gate test, then adopt `_shared/mcpJsonRpc.ts` |

Each unfinished surface owns dozens-to-hundreds of `useState`s, loaders/handlers, and inline JSX. **As of 2026-09-25, every source file ≥1,500 lines (60 files) is covered by a map that names it in `covers:`** — re-check with `npm run map -- --triage`, which pairs files to maps by `covers:` and flags stale ones. Start any extraction from the map, not from the source file; seams that span maps are in [Cross-surface seams](#cross-surface-seams).

This document is the **process** for shrinking one. The two reference implementations are:

- [`BIDS_TABS_ARCHITECTURE.md`](./BIDS_TABS_ARCHITECTURE.md) — the first completed map (every tab extracted; the parent has since regrown with glue — its order covers the second pass).
- [`PEOPLE_TABS_ARCHITECTURE.md`](./PEOPLE_TABS_ARCHITECTURE.md) — the same method with no single shared pointer (every tab extracted; the Payroll, Hours and Users clusters still sit in the parent).

> **Read this before starting an extraction.** Then create (or update) a `docs/<PAGE>_TABS_ARCHITECTURE.md` map for the page you're working on, and work tab by tab.

---

## Cross-surface seams

Duplications that span maps, de-duplicated from the 2026-09-25 sweep (38 maps). Each row is one shared hook, kernel or component that 2+ surfaces would adopt, ranked by how many adopt it. **★** = untested money math — weigh it above its rank when choosing by value ÷ risk. **Bold** names an existing tested kernel: that seam is adoption, not new code. Each ships as its own PR; a sweep across many files merges alone (`../CLAUDE.md` → *Mechanical sweeps*). File lists name the adopters; the start-from map holds the line ranges.

| # | Candidate | Adopters | Why | Start from |
|---|---|---|---|---|
| 1 | One money-format lib with explicit cents vs dollars | ~90 one-line copies in 92 files (5 in edge functions), 5 output shapes. **No-`$` dollars ×27:** already exported 3× identically as **`formatCurrency`** (`lib/format.ts`, plus the tested `lib/jobs/jobFormMoney.ts` and `jobFormatting.ts`), with private copies named `formatCurrency` (Duplicates, CreateJobFromEstimateModal, MoneyDecimalAmountInput, JobDetailMaterialsCostSection, JobsCombineSeparateModal), `formatMoney` (BankPaymentsModal, BilledPaymentConfirmationModal, HostedStripeBillPanel) and `money` (ArDepositHeader/Row, …). **`$` Intl dollars ×28:** private `formatCurrency` ×9 (allocations + invoice-link modals, TransactionDetailModal, BankingDebitCardRecentTxModal, DashboardStaleTallyStaffFollowUpModal, 2 PartsUnattributed modals, DetailJobModal, JobDetailProfitSection) and `formatUsd` ×6 (Category/User review tabs + ledger modal, DragSortFocusModal, BankingStripeInvoicesPanel, UserMercuryWindowSection), next to the exported Banking-table `formatCurrency` and drag-sort `formatUsd`. **`` `$${…}` `` template ×26** (`money` in the AR/partner/sub-work-order kernels). **Cents ×11:** `formatMoney(cents)` ×5 (Documents, Estimates, BidRoom, 2 estimate components) + 2 more on the browser locale, and 4 pinned to en-US (JobSignedAgreementModal, jobContractDocument, edge jobContract, sign-bid-room). DetailJobModal and Combine share a name but not an output. Documents' `formatBidValueCompact` and BidSubmissionFollowupExpandableDetails' private copy ≡ **`formatCompactCurrency`** | outputs differ ($ sign, `-$` vs `$-` vs `−$`, Workflow's parentheses, browser locale vs en-US, cents vs dollars under one name `formatMoney` — a 100× trap); merge only behind output-format tests | [`BANKING_TABS`](./BANKING_TABS_ARCHITECTURE.md) (the drag-sort ledger already exports `formatUsd`) |
| 2 | Edge-function call helper (`parseEdgeSuccessBody` beside `readEdgeFunctionErrorBody`; neither exists as a tested kernel yet, since `readEdgeFunctionErrorBody` has no test of its own) | ~24 `functions.invoke` sites in 17 files already do token → invoke → `readEdgeFunctionErrorBody` → `body.error`: CollectPaymentModal ×3, SendRecordInvoiceModal ×3, ConvertBillToStripeModal ×2, BankPaymentsModal, HostedStripeBillPanel, StripeInvoiceSendFromStripeButton, DashboardFieldCollectPaymentQueue, SplitBillModal, AgreedWriteDownModal, BilledPaymentConfirmationModal, UndoStripePartPaymentModal, UnwindStripeOobPaymentModal, Dashboard, and in `lib/` sendTestReport ×2, voidStripeInvoiceForRevert ×2, resendPhysicalInvoiceEmail, sendLienReleaseEmail. The generic `success !== true` step appears only in CollectPaymentModal ×3 and StripeInvoiceSendFromStripeButton. HostedStripeBillPanel, DashboardFieldCollectPaymentQueue, ConvertBillToStripeModal and the SendRecordInvoiceModal preview already use the tested `parseStripeInvoiceDetailsResponse` / `parseStripeInvoicePreviewResponse`. BankPaymentsModal checks `error` only, and sendTestReport and Dashboard check `ok`. 12 more files write their own error-body read (`FunctionsHttpError` → `context.json()`): useActiveAccountsManagement, useSettingsTemplatesEngine, useSettingsAccount, PersonDeskLifecycleModal, PersonDeskAccessSection, DigitalTwinsPanel, SettingsAdvancedTab, TwinSetupDialog, People, loginAsUser, people/hireWrites, people/leaveWrites. The token step has drifted: 5 files use `getAccessTokenForEdgeFunctions` (validates, then refreshes) and 10 use a raw `getSession()`. For example, CollectPaymentModal and HostedStripeBillPanel both call `get-stripe-invoice-details` but get the token differently. A separate raw-`fetch` family (jwt + apikey → `!res.ok \|\| !json.ok` → `json.error`; no `FunctionsHttpError`, no `success`) needs its own POST helper: Estimates (`send-estimate-to-customer` ×2), PeopleContractsTab and LienWaiverSendModal (`send-contract-for-signature`), BidRoomPanel, ContractFormOfficeModal ×2, ContractFormPaperEntryModal ×2, PersonContractSignedRecordModal, checkGoogleDriveAttachmentUrl. JobFormModal does not belong here: its `get-stripe-invoice-details` call is a fire-and-forget backfill that reads nothing back | the same token → invoke → error-body → success-check code each time | [`AR_PAYMENT_MODALS`](./AR_PAYMENT_MODALS_ARCHITECTURE.md) |
| 3 | Date keys → **`utils/dateUtils.ts`** (`localCalendarDayKey`, `todayYmdInAppTz` tested; `ymdAddDays`, `calendarYmdInAppTzFromIso` untested; no days-between) | ~26 files, 23 copies: `toLocalDateString` ×4 ≡ `localCalendarDayKey` minus its bad-Date guard (Checklist 9 calls, ChecklistAddModal 8, DashboardMyTimeSection 1, exported from `lib/dailyGoalsGate` to 7 files incl. DashboardCrewDaySection); Calendar `getCentralDateFromUTC` ≈ `calendarYmdInAppTzFromIso`; `shiftYmd` in Calendar + DashboardCrewDaySection ≈ `ymdAddDays`; `todayYmdCentral` ×3 (Forecast Specific tab, stage modal, All-stages tab, browser-local despite the name); `ymdDaysBetween` ×5 (Workflow ≡ stage modal, projectsForecastAlignStages ≡ projectsForecastDragEdit, MercuryBackfillModal); 9 local `todayYmd`, only 4 of them copies (PeopleVehiclesTab, DashboardMyVehicleCard, QuickfillVehicleOdometersSection browser-local; PeopleDayBookTab app TZ), the other 5 already wrap dateUtils; `formatYmdShort` ×4 in 3 shapes (PeopleVehiclesTab ≡ PersonMoneyLedgerModal; supplyHouseInvoiceForm ≈ `formatWorkDateYmdMonthDayShort`; MaterialsJobAccountsTab locale numeric). Not counted: ~14 more private add-days copies (bidPursuit's `shiftYmd` subtracts), ~20 days-between copies, `localYmdFromDate`, `todayIsoDate` ×2, Calendar `formatDateKey` | copies differ on purpose (browser-local vs app TZ; `''` vs `null` vs `NaN` on bad input; `getCentralDateFromUTC` throws on a bad string) — pin each difference in a test before swapping; `todayYmdCentral` → `localCalendarDayKey(new Date())`, not `todayYmdInAppTz`; days-between and the year-when-not-this-year short date have no dateUtils home yet (new code, not adoption) | [`CHECKLIST_TABS`](./CHECKLIST_TABS_ARCHITECTURE.md) (its seam PR) |
| 4 | Maps links → **`googleMapsSearchUrl`** (`lib/jobs/jobAddressUrls.ts`) | 22 files / 27 copies hand-build `google.com/maps/search` (board, form, BidPreviewModal, Builder Review, DetailJobModal, JobModeDetailsSection, AssignedStageCard, 5 Dashboard sections, JobsInspectionsTab, JobsSubLaborTab, Customers pages, Projects, …); 3 are exported lib helpers (`bidBoardMapDirectionsUrl`, `dashboardJobsMapDirectionsUrl`, `jobsMapDirectionsUrl`): make them delegate and keep their names. `bidAddressMapsUrl` (`buildBidPricingPackageHtml.ts`) stays because it returns null on a blank address and is mirrored in `_shared/bidPricingPackage.ts` | adoption only, a mechanical sweep (12 inline copies skip `.trim()`; the kernel trims, which is harmless) | [`BIDS_BOARD_FORM`](./BIDS_BOARD_FORM_ARCHITECTURE.md) |
| 5 | `useJobsLedgerSearch` (+ a job+bid variant) — one debounced picker hook | 22 files, 26 debounced effects. The 24 / 13 non-test src files that name `search_jobs_ledger` / `search_bids_for_clock` include `types/database.ts` and comment-only mentions; 22 / 11 actually call them. Clock job+bid pair ×9: ClockInOutButton, AssignSessionJobPopover, ClockSessionStripActionsModal, AssignFocusModal, HoursUnassignedModal (+ a jobs-only common-jobs search), PeopleHoursDayAuditModal, Dispatch/EstimatorTaskModal, HeaderGlobalSearch. Jobs only: JobsCombineSeparateModal ×3, JobPaymentMoveModal, useJobMigrate (+ a bids-only search), PeopleOverheadTab, SupplyHousesTab, MaterialsPoGeneratorTab, MercuryTransactionAllocationsModal, banking/TransactionDetailModal, DispatchModePo, CreateJobFromEstimateModal, PaidInFullEmailSettingsModal, PeopleHoursGridJobHighlight. Estimates+bids: JobFormImportEstimateOrBidModal | two families, not one. The 280 ms jobs-only family (Combine ×3, payment move, migrate ×2) shares ≥2 chars, top 20/30, a cancelled flag and `withSupabaseRetry`. The 300 ms rest have no cancelled flag, so a stale reply can land last (only CreateJobFromEstimateModal guards); their min length is none / 2 / >2, and HoursUnassigned + the day audit search even an empty box; top-N is none / 8 / 12. Keep on purpose: Mercury swaps to the tally RPCs for self-service; the PO pickers filter or enrich after the query; HeaderGlobalSearch adds estimates + customers. Only 4 pass `buildClockBidsSearchParams` (untested); the other 5 clock pickers search bids unfiltered. Prior art: `ChipsWithSearchPicker` already debounces (250 ms), sets a min length and top-N and aborts around the tested `searchJobsLedgerForDispatchSettings`; the row types live in the tested `utils/unifiedJobBidSearch.ts` | [`CLOCK_SURFACES`](./CLOCK_SURFACES_ARCHITECTURE.md) (B2 picker hook), then [`JOBS_MODALS`](./JOBS_MODALS_ARCHITECTURE.md) |
| 6 | Named role-gate kernels for literal role lists | ~20 files, 6 gates, 4 of them with a tested kernel already (adoption): AR apply (dev · master · assistant-like · primary) → **`canRecordArPayments`** (`lib/jobs/stagesRoleGates.ts`, matrix test): `canRoleApplyBankPayments` (BankPaymentsModal) ≡ `canRoleUseArBankCount` ≡ `canUnlinkMercuryPayment` (`jobFormPaymentPredicates`) ≡ `canOpenAr` (`usePipelineMoneyOpportunities`) ≡ a second `canSeeBilledExpectedPay` (`useJobBilledExpectedPay`); the same literal set on Dashboard (View Reports, Upcoming inspections), Checklist, roadmap and two email-settings modals gates other things and is not this gate; GC office recipients → **`OFFICE_CAPABLE_ROLES`** (`lib/teammateEmailChips.ts`): the 5-role list in GC Review (416), JobsWeeklyMovementModal, SettingsEmailStreamsSection. GC Review's two 4-role lists (633, 954) are the statement-round roster and mirror `statement-round-email-dispatch`'s office-only gate: a second gate, not a copy; prospects staff → **`canAccessProspectPipeline`**: `hasProspectsStaffAccess` (tested twin), Quickfill.tsx, QuickfillProspectsSection, plus 2 identical `.or()` strings (Prospects.tsx, `prospectTeamActivity.ts`); Materials office roles → **`materialsTabs`** kernel: 3 tab bodies (PoGenerator ×3 checks, JobAccounts, SupplyHousesTab). SupplyHousesTab's check matches `supplyHousesPaneFor(role) === 'office'`, not `canOpenMaterialsTab`, which also admits estimators; `KIND_TO_USER_ROLE`: 1 private copy in JobsSubLaborFormModal (no `controller` key) against the export in `peopleUsersTabShared.ts`, which 3 files already import; role-visible service types: Materials ×2, Bids ×2, Settings (estimator only), beside the tested `visibleServiceTypesForJobForm` (`lib/jobs/jobFormServiceTypes.ts`, which differs: it falls back to all trades per role and covers field roles) | the copies drift: Bids' loader falls back to all trades when the filter comes back empty and its render copy does not; Materials' two filters agree only because `loadRole` stores one array per role. The GC 4-role and 5-role lists differ on purpose (two server gates). The 5-role recipient set is also copied in 5 edge functions, which would be a separate `_shared` seam | the map owning each gate: [`AR_PAYMENT_MODALS`](./AR_PAYMENT_MODALS_ARCHITECTURE.md), [`GC_REVIEW_MODAL`](./GC_REVIEW_MODAL_ARCHITECTURE.md), [`PROSPECTS_TABS`](./PROSPECTS_TABS_ARCHITECTURE.md), [`MATERIALS_TABS`](./MATERIALS_TABS_ARCHITECTURE.md), [`JOBS_MODALS`](./JOBS_MODALS_ARCHITECTURE.md) |
| 7 | Bid version sends: `useVersionSends(bidId)` + typed helpers for the `bid-version-sends-changed` / `bid-gc-outcome-changed` window events | ~12: the identical `bid_version_sends` select appears 7× in 6 files (Cover Letter, BidGcSentPanel ×2, BidPackageMapModal, BidPackageSendsDetails, BidVersionPicker, JobFormModal), plus a batched `.in('bid_id')` copy in `useBidGcPackets`; the outcome event fires 10× from 9 files (4 of those + BidBoardGcRows, BidSubmissionFollowupTab, BidsCallQueueTab ×2, BidsWaitingToHearLens, BidsWhyWeLostLens); readers already share **`latestSendByVersion`** (`lib/bids/versionSends.ts`), and the sent-date rule already lives in **`bidSentDate.ts`**. The two mark-sent inserts (Cover Letter, BidGcSentPanel) differ on purpose and stay | an untyped string-event bus: each reader hand-rolls the load, a cancelled flag and a listener, and only JobFormModal sends a `{ bidId }` detail, which no listener reads | [`BIDS_DOCUMENT_TABS`](./BIDS_DOCUMENT_TABS_ARCHITECTURE.md) |
| 8 | One `get_jobs_ledger_by_ids` row loader: ids → `Map<id, row>` with `?? ''` fill (precedent: `fetchJobLabelsByIds` in `lib/dispatchSettingsJobsSearch.ts`, label-formatted, tested, one caller) | 12 files, 19 calls: People.tsx ×3, `lib/draftPayrollPersonBreakdown.ts`, `utils/teamLabor.ts`, HoursUnassignedModal ×3, PeopleHoursDayAuditModal, ReviewHoursModal, DashboardMyTimeSection, DashboardMyTimeDayEditorModal, SupplyHousesTab ×2, AddInspectionModal ×2, NewReportModal, PeopleReviewTab ×2 (fits only with a paged + paid-only option) | each re-types the row and builds the map by hand in its own shape: `{hcp_number, job_name, job_address}` ×5 (People ×3, draftPayroll, teamLabor); the same plus `click_number`/`service_type_id` ×6 (HoursUnassigned ×3, DayAudit, ReviewHours, MyTimeSection); a subset in SupplyHouses; `job_address` only in AddInspection/NewReport; a label in the day editor. 12 calls drop the error (`{ data }` only), 7 throw | [`PEOPLE_TABS`](./PEOPLE_TABS_ARCHITECTURE.md) |
| 9 | Not-coming-in / NCNS kernel: one result → toast helper around `recordNotComingInForUserAsStaff`, `removePersonDayBlocks(ids)`, a typed `record_ncns_and_reject_sessions_for_day` call, one NCNS role gate | 11: the not-coming-in result → toast handler, copied word for word in the 7 parents that pass the Day editor `onMarkNotComingIn` (Dashboard, PeopleHoursDashboardClockStrip, QuickfillPeopleHoursNewSection, QuickfillScheduleSection, User{Day,Week,Month}ScheduleSection); ScheduleDispatchHubPage (block-delete loop ×2 + its own toast variant); the NCNS RPC + row cast in the Hub and DashboardMyTimeDayEditorModal (`runRecordNcns`); the role list as `canRecordNcns` (Hub), `showClockStripScopeToggle` (the 7 parents) and People's `hoursAllowNcnsFromMyTime` (≡ `stripScopeEligible`); the undo handler in the Hub + ScheduleDispatchJobWeek. The Team board's copy (useTeamBoardActions) keeps the blocks on purpose and stays | payroll-adjacent; `recordNotComingInForUserAsStaff` and `parsePayStaffBulkTimeOffResult` are untested (the test covers only `parseRemoveNotComingInResult`); the two NCNS copies disagree on an empty reply (the Hub errors, the Day editor closes as success) and only the Hub marks the day off and clears blocks | [`SCHEDULE_DISPATCH`](./SCHEDULE_DISPATCH_ARCHITECTURE.md), then [`MY_TIME_DAY_EDITOR_MODAL`](./MY_TIME_DAY_EDITOR_MODAL_ARCHITECTURE.md) (its mount table names the 7 parents) |
| 10 | `useStagesBilledMoneyData` (pay speeds, promised dates, promise records, chase touches) + one `canSeeBilledExpectedPay` | 10 files, 21 calls: JobsStagesTab (all 4), DashboardFinancialsSection + usePipelineMoneyOpportunities (3 each), useJobBilledExpectedPay, loadBridgeData, BidBoardCustomerReviewModal, useLegalPacketData (2 each), useLienDeskData + useGcOnNoticeData (promised dates), useCustomerTermsWarning (promise records) | the same `as never` RPC reads load separately, and the promise-record parse → classify → build chain is copied word for word in JobsStagesTab, BidBoardCustomerReviewModal and useCustomerTermsWarning; the gate is defined twice with the same logic, and only the `stagesRoleGates` copy is tested. Keep each read's own gate (office vs office + primary) and the desks' retry. loadBridgeData and the desk hooks load inside their own `Promise.all`, so share plain loaders under the hook | [`JOBS_STAGES_TAB`](./JOBS_STAGES_TAB_ARCHITECTURE.md) |
| 11 | Deep-link hrefs: `scheduleDispatchWeekUrl(jobId, weekYmd)` + the bid-followup href → **`clockedInMapBidHref`** (`lib/clockedInMap.ts`) | ~9 files, 15 hand-built sites. The week URL is built at 7 sites in 6 files (JobsStagesTab, jobsStagesRowShared, JobsStagesCardList ×2, DetailJobModal, DashboardJobModeCard, QuickfillScheduleSection). The hub builds its own `URLSearchParams` copy (ScheduleDispatchHubPage, Tomorrow route), and 9 `?week=` hrefs carry no jobId (PeopleHoursDayAuditModal, User{Day,Week,Month}ScheduleSection, useTeamBoardActions, QuickfillScheduleSection, QuickfillTomorrowsScheduleSection, ScheduleDispatchJobWeek, DashboardTeamActiveClockStrip); those adopt too if the helper takes an optional job or day. `/bids?bidId=…&tab=submission-followup` is hand-built at 8 sites: 4 in the clock strip, DashboardMyBidsSection, and Calendar ×3 (not encoded). The untested `submissionFollowupBidShareUrl` builds the absolute share copy | the week half works out the week at every site (`companyWeekStartSundayContaining(ymd) ?? getDefaultWeekRange().start`, or just this week) and encodes the URL itself; the bid half only needs adoption, because the tested kernel exists but only ClockedInMapModal calls it | [`JOBS_STAGES_TAB`](./JOBS_STAGES_TAB_ARCHITECTURE.md), [`CLOCK_SURFACES`](./CLOCK_SURFACES_ARCHITECTURE.md) |
| 12 | Bid-picker filter → **`lib/bids/filterBidsForPicker`** (Pricing + Labor already use it), then one picker search row | 9 of the 12 tab files that still inline `bidNumberMatchesQuery`: Takeoff, Counts, SubmissionFollowup, BidBoard match the kernel's fields exactly; CoverLetter, Submittals, ChangeOrder, LienRelease, Rfi search `bidDisplayName` (= `project_name`) + customer + GC with no address and an untrimmed blank check (a second shape, or an accepted behavior change). The Call queue, Waiting to hear and Why we lost lenses search derived rows and stay | the Takeoff map never names the existing kernel and calls all 11 other files one shape (only the Pricing/Labor, Document-tabs, SubmissionFollowup and Board maps name it); it returns a copy on a blank query where the four exact-field copies return the same array (Board and SubmissionFollowup key memos on it, so adopt as `query.trim() ? filterBidsForPicker(…) : bids`); its test never exercises a bid-number match and `bidNumberMatchesQuery` has no test | [`BIDS_TAKEOFF_TAB`](./BIDS_TAKEOFF_TAB_ARCHITECTURE.md) (counts the copies; [`BIDS_DOCUMENT_TABS`](./BIDS_DOCUMENT_TABS_ARCHITECTURE.md) A6 names the second shape) |
| 13 | One typed `list_shadow_runs` loader (`useShadowRuns` over **`ShadowRunRow`**, `lib/bids/shadowStory`) + one guarded `twin_questions` answer writer; Bids.tsx's own `useBidRobotLayer` is a separate in-page split | 8 files, 9 `list_shadow_runs` calls: Bids.tsx ×2 (the board map + a fresh re-read in `offerRobotEnvelope`), BidsRobotMirrorTab, BidsRobotQueueTab, BidsRobotScoreboardTab, BidsAuditsTab, BidBestEffortCard (Cover Letter), useBidAuditsPendingCount + useRobotLockedShadows (Dashboard). `twin_questions`: the open→answered UPDATE (0 rows = handled elsewhere) in Bids.tsx, RobotEnvelopeModal, TwinOperatorQuestionsCard, BidsAuditsTab (batch); the open-200 read feeding **`groupStandingRulings`** in BidsAuditsTab ≡ TwinOwnerMemoCard | only the fetch, cast and fail-soft repeat. Each caller picks its own run (latest per reference, first scored, scored > locked > open, `selectRobotLockedShadows`), and those picks stay. BidsAuditsTab and useBidAuditsPendingCount cast inline row shapes. The other `twin_questions` reads filter differently (about-bid 500, one bid 20, all 40). A Bids-page hook (the 330-line R6) cannot reach the Dashboard hooks | [`BIDS_TABS`](./BIDS_TABS_ARCHITECTURE.md) (step 5: the shared source is a later, separate step) |
| 14 | Bid-row kernels for the Followup lenses and the board | ~8: `normalizeBidStaffEmbed` (8 copies in Followup, 4 on the board, 1 in `bidBoardMap`'s `estimatorNameOf`; fetchBidForPreview already has a private `normalizeBidRow` that Bids.tsx copies verbatim, so export that one; `firstOrNull` in BidBoardCustomerReviewModal and `single` in fetchWorkingBoardClockBidPicks are the same logic as private generics); `builderKeyForBid` in `gcOutcomeRows` (Followup, Builder Review — which uses two keys — Call queue, Waiting to hear); `parseStaleDaysThreshold` over the shared `bids_followup_stale_days` key (defaults `''` vs `'14'`); adopt `getSubmissionSectionKey` (`lib/bids/submissionSections.ts`, no direct test yet, so add one) where Bids.tsx inlines it twice | copied word for word; keep the per-caller defaults | [`BID_SUBMISSION_FOLLOWUP_TAB`](./BID_SUBMISSION_FOLLOWUP_TAB_ARCHITECTURE.md) |
| 15 | twin-mcp adopts `_shared`: `mcpJsonRpc.ts` (widen for PDF resource blocks), `ctBridge.callCtManageUser`, `referenceGrade` (moved from `src/lib/bids`, re-exported), `ownsBid` beside `twinMayReadPlans` | 8 functions: twin-mcp, dev-mcp (`mcpJsonRpc`), plan-fetch (`twinSeatGate`), audit-finish, ct-bridge, ct-roster-audit (`callCtManageUser`), archive-user, restore-user (`forwardCtSeatState`) (+ the client grade kernel, whose 5 importers stay put behind the re-export) | twin-mcp keeps its own wire shell, 3 inline CT fetches, 3 untested grade copies (+2 flag copies) and 12 inline bid fences | [`TWIN_MCP_SERVER`](./TWIN_MCP_SERVER_ARCHITECTURE.md) |
| 16 | Wage-lookup loader (`people_pay_config` → `buildHourlyWageLookupByNormalizedName`) | 8 files, 9 loaders: 4 call the tested kernel but each re-types its own `person_name, hourly_wage` select (ProjectsJobHistoryDayModal, ChecklistCostModal, BidBoardLostSummaryModal, BidBoardWeeklyEstimatorLaborDevSection); 4 files (5 copies) hand-roll a case-sensitive name map instead and disagree on a null wage — ScheduleDispatchHubPage's Phase-D block (761–791, by user id, null→0; the Schedule Dispatch map's `buildHourlyWageByUserId` is a proposed name, not code), useDashboardFinancials (228–235, first-wins, null→0), moneyfillWeekClose ×2 (329–332, 438–441, null skipped), and the edge mirror `_shared/recurringJobReportCore.ts` (362–387, by user id, null kept) | payroll-adjacent; the day modal reads pay config for roles that never see labor | [`PROJECTS_FORECAST_TABS`](./PROJECTS_FORECAST_TABS_ARCHITECTURE.md) |
| 17 | Mercury allocation reads → the tested loaders that already exist (**`fetchMercuryJobAllocationsWithAttributionForJob`**, **`mercuryLinesFromRows`**, **`loadMercuryAllocModalDataForTransaction`**) + one paged card-charges-by-job read | ~7 files, 3 shapes. Per-job detail: the Jobs print thunk's cache-miss read is an older, drifted copy of **`fetchMercuryJobAllocationsWithAttributionForJob`** (no paging, no Internal-Transfer filter, no `linkedToSupplyInvoice`), so adopting it changes what that print shows; ProjectsJobHistoryDayModal flattens the object-or-array embed inline (≡ **`mercuryLinesFromRows`**, `_shared/jobMaterialsCostLines.ts`), and so does `fetchOverheadOfficePartsByDay` ×2 (its date-windowed query stays). Per-transaction seed: BankingMercuryTxDetailModal and UserMercuryWindowSection re-read what **`loadMercuryAllocModalDataForTransaction`** loads (the latter drops `note`). Card charges by job: the same paged `job_id, amount, mercury_transaction_id` read in useJobsMercuryAllocations ×2 and PeopleReviewTab ×2. Banking and BankingMercuryVisualsTab already share the tested `fetchAllJobAllocations` / `fetchAllAttributions` | the tested per-job loaders disagree: `fetchJobMaterialsCostSnapshot` (Edit / Detail Job, dev-mcp `get_job`) and the two inline copies neither page nor drop Internal-Transfer rows, but the Job Summary loader does both; Banking reloads every relation after each allocation save | [`JOBS_TABS`](./JOBS_TABS_ARCHITECTURE.md) (parity check), then [`PROJECTS_FORECAST_TABS`](./PROJECTS_FORECAST_TABS_ARCHITECTURE.md) |
| 18 | ★ One guarded writer for `replace_mercury_transaction_splits` (seed guard + Internal-Transfers exclusion + signed rows), after the allocations modal imports **`lib/mercurySplitMath`** (its private copy is identical) | 7: MercuryTransactionAllocationsModal, TransactionDetailModal, BankingMercuryTxDetailModal, mercuryQuickAssignUserAttribution, mercurySetUserAttribution; the Internal-Transfers guard is also hand-written in the Accounting and Drag Sort tabs | only the allocations modal runs `decideSplitSaveGuard` | [`BANKING_TABS`](./BANKING_TABS_ARCHITECTURE.md) |
| 19 | Vehicle writes: `applyHandOffWrites` (the executor for the tested `handOffWrites` plan in `lib/vehicleFleet.ts`), `insertOdometerReading`, one fleet loader | 7: hand-off ×4 — PeopleVehiclesTab, PersonDeskFieldSection ×2 (park + hand-off), PersonDeskLifecycleModal (none atomic); the odometer insert ×7 — PeopleVehiclesTab ×4 (quick entry, hand-off, catch-up, service, whose error is ignored), VehicleOdometerHistoryModal, QuickfillVehicleOdometersSection (needs the new id back), DashboardMyVehicleCard; fleet rows in PeopleVehiclesTab `loadFleet` and Quickfill `loadAll` (the same vehicles → possessions + odometer `.limit(2000)`; Quickfill takes open possessions only), with a holder-scoped variant in DashboardMyVehicleCard and a cost-scoped one in `wheelsData` | ★ pay stubs print raw `weekly_insurance_cost` while the board (`effectiveWeeklyInsuranceCost`, tested) and Wheels (`truckRunningCost`'s inline `onPlan`) zero off-plan vehicles — the owner decides before a `fleetWeeklyCost` kernel | [`PEOPLE_VEHICLES_TAB`](./PEOPLE_VEHICLES_TAB_ARCHITECTURE.md) |
| 20 | ★ Sub-labor costing → `useJobDetailSubLaborCost` (the hook is untested; its row-shaper **`jobSubLaborInputsFromRows`** is tested) + **`laborJobSubCost`** / **`subLaborJobBalance`** / **`sheetMoney`** (`lib/jobs/subPaymentMoveRemove.ts`) | 7 files, 11 copies. Sheet cost: JobFormModal's labor effect (a verbatim copy that already uses the kernel's `?? 0`); PeopleReviewTab's drive expression ×4 (three sit beside a `laborJobSubCost` call and split the drive $ out, which the kernel does not return; 1297 costs `hrs × rate + drive` and ignores line rates and direct $ lines). Balance: JobsSubLaborFormModal ×2 (`editSummary`, the Payments block), `useSubLaborDueTotal`, `buildUnlinkedSubLaborRows`, `sheetMoney`, and the edge copy in `_shared/subPortalStatement` (its `subLineLaborCost` ≡ `lineLaborCost`). JobsSubLaborTab, DashboardSubMoneySection and SheetStoryModal already call `subLaborJobBalance` | the sheet form's rate fallback (`?? first non-zero row ?? 20`, which the tested `sheetMoney` repeats) differs from the kernel's (`?? 0`). The form's two totals can adopt `sheetMoney` unchanged; prove agreement before folding `sheetMoney` into `subLaborJobBalance` | [`JOB_FORM_MODAL`](./JOB_FORM_MODAL_ARCHITECTURE.md), then [`JOBS_MODALS`](./JOBS_MODALS_ARCHITECTURE.md) (quirks #6–#7) |
| 21 | Lien-waiver print args (form fallback + typed signature) → two new helpers (`lienReleasePrintFormType`, `lienReleaseTypedSignature`) in the tested `lib/jobs/lienReleaseLifecycle.ts`, built on its `lienReleaseStatus` / `lienReleaseSignatureAuditLine` | 7 files: the `conditional_progress` fallback in all 7 (Documents, DashboardLienReleaseQueueModal, LienReleaseModal, LienSignatureInboxSection, sendLienReleaseEmail, BillCustomerLienReleaseStrip, LienReleaseSignModal); the typed signature in the first 5 | built by hand in each; BillCustomerLienReleaseStrip passes no signature, so a signed release it previews prints unsigned | [`DOCUMENTS_PAGE`](./DOCUMENTS_PAGE_ARCHITECTURE.md) |
| 22 | A per-job count RPC, plus widening the per-job **`list_reports_for_job_ledger`** that already exists (it has no v2.1546 crew-visibility branch, sorts ASC and has no bid, link or address columns), so callers stop filtering `list_reports_with_job_info` client-side | 6 files, 7 calls. 3 files / 4 calls filter by job: Dashboard badges (count per job), JobReportsModal ×2 (job + optional author; crew open it from AdditionalReportModal), ProjectsJobHistoryDayModal (job + app-TZ day). The rest have different shapes: DashboardRecentReportsSection keeps the newest 8 (`.limit(8)`, no migration), PeopleReviewTab pages every row for one person's period, and JobsReportsTab shows the whole list. JobsReportsListView and `dashboardRecentReports` only name it in comments | For Recent-Reports roles, the Dashboard fetches the whole list twice on mount. Pointing JobReportsModal at the per-job RPC as it stands would hide teammates' reports from crew, so this needs a migration | [`DASHBOARD_SECTIONS`](./DASHBOARD_SECTIONS_ARCHITECTURE.md) |
| 23 | App-shell registries: `lib/appNav.ts` with a nav ↔ `isPathAllowedForRole` parity test, one `shellModeRows` kernel, `usePinnedTabs` | ~6: nav registries in Layout (inline gates) and `phoneDock` (27 dock pages with their own role predicates; DispatchModeFooter's fixed tabs repeat its paths); mode rows built twice in Layout (gear toggles 1323–1600, `phoneDockModes` 284–308) and rendered by PhoneDockMoreSheet; pins listeners in Layout, Dashboard, Settings (only Settings loads them role-filtered, via `getMergedFilteredPins`) | nav visibility and route access already drift (the superintendent Statement link; the dock offers estimators Customers, which the guard refuses, latent while the dock is assistant-only), and desktop and phone nav drift apart (Review, primary Statement) | [`APP_SHELL`](./APP_SHELL_ARCHITECTURE.md) |
| 24 | ★ Tally-line cost → **`tallyLineTotal`** (`lib/partsPerPersonCostSummary.ts`; narrow its param to the four cost fields first — the Jobs.tsx and PeopleReviewTab rows carry no `created_by_*`) | 6 files, 8 copies: Jobs.tsx, JobsJobSummaryTab, JobsPartsTab, PeopleReviewTab ×2, fetchOverheadOfficePartsByDay ×2, jobSummaryCostBreakdown; two tested twins already exist — `tallyPartEventAmount` (jobChargesTimeline, fold it onto the kernel) and `tallyLinesFromRows` (`_shared/jobMaterialsCostLines`, shared with the edge, so it keeps its own copy) | the prerequisite for the Jobs `jobSummaryData` Stage A | [`JOBS_TABS`](./JOBS_TABS_ARCHITECTURE.md) |
| 25 | ★ `lib/bids/scenarioPricingRows.ts` + `loadScenarioInputs` (the `computeBidPricingRows` adapter + the four-table overlay read) | 6 files, 12 adapters: BidsPricingTab (4 adapters, 3 overlay reads), useBidPricingRows ×2, BidPackageMapModal, BidsCoverLetterTab, approvalPdf ×2, pricingPage ×2 (these four also hand-write the overlay read, 5 more; pricingPage keeps a private `submissionHiddenIdsForVersion`); + the Deno mirror in `send-bid-pricing-package` (`deriveRow` + `loadPricing`) to pin, not import | one copy shipped a $0.00 bug; the Workbench revenue effect (and approvalPdf's Margins loop) price every scenario on one version's rows, which is the same class; the kernel, `buildPricingPrintRows` and approvalPdf are tested, but the 8 component/hook copies are not | [`BIDS_PRICING_LABOR_TABS`](./BIDS_PRICING_LABOR_TABS_ARCHITECTURE.md) |
| 26 | ★ `jobPartsTotalFromSnapshot` in `_shared/jobMaterialsCostLines.ts` | 6 files, 4 inline sums: DetailJobModal (adds billed materials), JobsCombineSeparateModal + JobFormModal (don't), JobFormDeleteMigrateModals (its Target column adds up the supply / tally / card preview that useJobMigrate builds); useJobCostSnapshot has its own copy of `tallyPartsTotalFromLines` and already returns the totals that JobFormModal works out again | four untested inline sums. Only DetailJobModal shows no total when a read fails; the other three count a failed read as $0. The migrate modal's Source and Target columns come from two different copies. dev-mcp shares the `_shared` file | [`JOBS_MODALS`](./JOBS_MODALS_ARCHITECTURE.md), then [`JOB_FORM_MODAL`](./JOB_FORM_MODAL_ARCHITECTURE.md) (the migrate modal) |
| 27 | `useOrgSettingSwitch(key, parse)` for boolean `app_settings` flags | 6 files, 6 switches: the Accounting tab's two (`accounting_label_auto_approve_rule_matches` and the AR-applied-income key, which BankPaymentsModal also reads), OwnerAutoConfirmSettingsBlock and HideHcpFieldSettingsBlock (the same optimistic flip and rollback over their own libs), TeamReviewSection's include-crew and DashboardStaleTallyStaffFollowUpModal's hide-dev (write-then-set, no rollback) | each one hand-rolls load / saving / flip / rollback / toast, and only the parse kernels are tested. `overhead_office_job_ledger_id_v1` is not part of this seam: it is a job id chosen in a picker and handled through the tested `overheadOfficeJobSettings.ts` (17 files; PaidProfitChartModal reads it raw). Its Change/Clear leaves `useOverheadOfficeJobId`'s module cache stale, which needs a fix in the lib, not this hook | [`BANKING_TABS`](./BANKING_TABS_ARCHITECTURE.md) (Region D) |
| 28 | A bid-flow header component | 5: Takeoff, Counts, Labor, Pricing, Cover Letter (the `useBidFlowFacts` / `Review` / `Fold` trio, `BidFlowStrip` ×2 (×3 in Counts: full + narrow inline + wide inline), 11 identical `canOpenDoor` / `onOpenDoor` / `reviewStamp` closures, the same `onOpenBidFlowDoor` / `bidFlowDoorAllowed` props); Bid Board shares only facts + review (per-row strip, own door handler with accounts / edit / pricing gate, so not a copy) | identical wiring; the hooks and `useMarkBidReviewed` (writes `bids` + `bids_submission_entries`) have no unit tests (only the Takeoff smoke mounts them; the `deriveBidFlow` / `bidReview` kernels are tested) | [`BIDS_TAKEOFF_TAB`](./BIDS_TAKEOFF_TAB_ARCHITECTURE.md) |
| 29 | A schedule-day loader (day / week / month window) + `lib/scheduleJobPicker.ts` | 5: picker sort/filter (4 memos, `quickfillOrderedSessionJobLedgerIds` → `quickfillAssignJobPickerRows`) in QuickfillScheduleSection + UserDayScheduleSection; load steps (the jobs-ledger title map, the same `clock_sessions` select, then the tested bid-title triple `collectScheduledBidIds` → `fetchBidTitlesForScheduleBlocks` → `addBidAnchorTitles`) in QuickfillScheduleSection + usePerson{Day,Week,Month}ScheduleData. ScheduleDispatchHubPage's picker is the other shape (newest first, finished last, searches address + customer), shared with QuickAssignSheet and the tested `subLaborAssignPickerRows`, so it is not an adopter | the picker memos are copied word for word; the loaders differ only in who (roster or one person) and the date window | [`QUICKFILL`](./QUICKFILL_ARCHITECTURE.md) |
| 30 | Public-portal load + submit (`usePublicPortalLoad`, one POST client) | 5: SubPortal (6 hand-rolled `submit-sub-portal` POSTs), CustomerPortal (4 `submit-portal-request` POSTs: `RequestCard` + `components/portal/PortalPromiseAsk` / `PortalShareAsk` / `PortalStageAsk`), LegalPortal (1, `act`), BidRoom (2), SubmittalRoom (3). The 10 SubPortal + CustomerPortal POSTs send only `Content-Type`, with no apikey | 16 POSTs each carry their own sample short-circuit; the load (token or slug + `?preview=1` + `sample ? publicFunctionHeaders : staffAwarePublicHeaders` + the `error`-body message) is near-verbatim in SubPortal, CustomerPortal and BidRoom, and looser in LegalPortal / SubmittalRoom (token only, always staff-aware) | [`SUB_PORTAL`](./SUB_PORTAL_ARCHITECTURE.md) |
| 31 | Tests for the tally kernels in `lib/mercuryTxRowFromTally.ts` (`tallyRowIsResolved`, `tallyUniqueJobSplitEntries`, `filterTallyRowsToUnlinkedWithOptionalMinPosted`, `mercuryTxRowFromTallyRpc`; no test file yet) | 5: JobTally, ClockInOutButton (through `filterTallyRowsToUnlinkedWithOptionalMinPosted`), TallySortModeCardList, TallySortPurchaseModal (re-inlines `!tallyRowIsResolved`, 91), TallyPreClockOutModal (`mercuryTxRowFromTallyRpc` only); plus DashboardStaleTallyStaffFollowUpModal's `mercuryTxRowFromStaffListRow` (27), a verbatim copy of `mercuryTxRowFromTallyRpc` | they decide the unlinked counts on the page and at the clock-out gate, and the gate never merges `is_payroll`, so rows marked as payroll still count there | [`JOB_TALLY`](./JOB_TALLY_ARCHITECTURE.md) |
| 32 | ★ `taxPercentFromInput` | 4 files, 12 copies of the `parseFloat(x \|\| '8.25')` default on the shared `costEstimatePOModalTaxPercent`: BidsPricingTab 4, useBidPricingRows 2 and BidsLaborTab 1 add `\|\| 0` (junk → 0); BidsTakeoffMaterialsSummarySection 5 don't (junk → NaN in its "with tax" lines), and its PO modal's 3 `parseFloat(x) \|\| 0` sites default to 0 (a preserved quirk); the seed is Bids.tsx `useState('8.25')` | one default, thirteen places to change it | [`BIDS_PRICING_LABOR_TABS`](./BIDS_PRICING_LABOR_TABS_ARCHITECTURE.md) |
| 33 | Salary-eligibility probe → `fetchSelfSalaryClockState`, plus its first tests | 4: ClockInOutButton and Dashboard use it; Calendar and Settings (`useSettingsAccount.refreshSelfPaySalaryForPayName`) probe by `person_name` only | a renamed salaried user loses the Calendar workday layer and the Settings "Salaried workday" section | [`CALENDAR_PAGE`](./CALENDAR_PAGE_ARCHITECTURE.md) |
| 34 | ★ Put the pins' AP / billed totals on the tested kernels: AP → a `sumUnpaidSupplyInvoices` lifted out of `buildApBucket(...).supplyTotal` (`lib/dashboardFinancials.ts`, tested); billed → `computeBillTruth(...).owed` (`lib/billing/billTruth.ts`, tested) | 3 files, 4 hand copies: AP ×3 (SupplyHousesTab `loadSupplyHouseSummary` + header `reduce`, `useSupplyHousesAPTotal` for the Dashboard pin, `useSettingsFinancialPins` AP loader); billed ×1 (`useSettingsFinancialPins.loadBilledTotalAndPinnedUsers`). Already on the kernels: Dashboard AP card (`useDashboardFinancials`), Dashboard Billed pin (`useBilledTotal`), the Jobs Stages Billed board (`stagesHeaderStats`) | the AP copies match `supplyTotal` today but are untested (the tab's per-house sum also drops invoices whose house is not listed); the Settings billed roster still sums `revenue − payments_made` over every `billed` job plus gross billed invoices, so its label disagrees with the Dashboard pin and the board it opens | [`MATERIALS_TABS`](./MATERIALS_TABS_ARCHITECTURE.md) (SupplyHousesTab Stage A), then [`SETTINGS_TABS`](./SETTINGS_TABS_ARCHITECTURE.md) |
| 35 | Per-job team labor → **`fetchTeamLaborBreakdownForJob`** (`utils/teamLabor.ts`) + **`buildJobTeamLaborRow`** for the totals. DetailJobModal (via `useJobDetailTeamLabor`), StagesCrewModal and the cost charts already use it | 3: JobFormModal, useJobMigrate, JobsCombineSeparateModal. Each runs company-wide `loadTeamLaborData` and keeps one row's `jobCost` / `manHours` (and `people.length`). Not Jobs.tsx, whose tabs use every row. Not PersonMoneyLedgerModal, which covers one person across every job | adoption, not new code. The form already loads the per-job copy through `JobCostsTabCharts`, so it fetches team labor twice. Pin two differences first: the per-job loader has no 2-year hours window, and on a query error it returns `[]` where the form needs a throw (error ⇒ force reassign) | [`JOB_FORM_MODAL`](./JOB_FORM_MODAL_ARCHITECTURE.md), then [`JOBS_MODALS`](./JOBS_MODALS_ARCHITECTURE.md) (quirk #40) |
| 36 | `useGcStatementRound` | 3: JobsGcReviewModal, JobsStagesTab, usePipelineMoneyOpportunities (the last two only read certs + marks + senders for the held/ready cards); GcSenderRoundCard's `stateLabel` re-maps the same 6 chip states with its own sent / ready / held labels (that is the `statementRoundChip` kernel, not this hook) | each copy loads the same reads by itself (Stages re-reads all of them on every GC Review open and close to catch up); usePipelineMoneyOpportunities takes account men from lean rows that have no `account_manager_user_id`, so its ready card leaves out GCs whose sender is the account man | [`GC_REVIEW_MODAL`](./GC_REVIEW_MODAL_ARCHITECTURE.md) |
| 37 | ★ A cover-letter content kernel (`planCoverLetter`, `effectiveRevenue`, content fallbacks) | 3: BidsCoverLetterTab, approvalPdf, Bids.tsx's approval-PDF download | the untested chain sets `bids.bid_value` (only approvalPdf's `effectiveRevenue` copy is tested); the PDF has drifted from the letter — the parent's download ignores the org exclusions / terms defaults, approvalPdf drops the org closing and still ties fixtures-per-plan to the plan date | [`BIDS_DOCUMENT_TABS`](./BIDS_DOCUMENT_TABS_ARCHITECTURE.md) |
| 38 | ★ `lib/jobs/draftInvoiceWrite.ts` | 3 files, 5 copies: JobFormModal ×3 (segment pick, typed amount, hazmat fee), the JobsStagesTab partial-invoice path, SplitBillModal (one batch insert of every part) | the same `ready_to_bill` draft row (`sequence_order` = invoice count, no bill date, not the primary bundle); 4 then run ensure RTB when the job is RTB (+ the tested `ensureRemainderResyncOutcome`) and refetch, and 2 link fixture lines by position. SplitBillModal only inserts, then bills each part in Stripe | [`JOB_FORM_MODAL`](./JOB_FORM_MODAL_ARCHITECTURE.md) |
| 39 | Tests for `useJobFormAutosaveSlice` | 3: Edit Bid (Bids.tsx), the Estimates draft, JobFormModal | a shared autosave engine with no test of its own | [`JOB_FORM_MODAL`](./JOB_FORM_MODAL_ARCHITECTURE.md) |
| 40 | One assigned-workflow-steps loader | 3 files, 4 copies: Calendar `loadAssignedSteps` 560–624 (3 round trips), useDashboardBoot ×2 (boot `loadAssigned` 195–254 + reload `loadAssignedSteps` 262–338: RPC `get_assigned_steps_with_projects_for_dashboard`, then legacy RPC / table read + 2 round trips), previewJobModalStages (one project, 4 round trips); related, not a loader: Workflow `loadSteps` subcontractor filter (432) | the copies disagree: Calendar + preview `.eq('assigned_to_name', name)` exactly (case-sensitive, never `assigned_person_id`), while the Dashboard RPC has matched `assigned_person_id` first and the name case-insensitively since v2.1733 (only its missing-function fallbacks `.eq` the name). A step assigned by person id only, or under a differently cased name, shows on the Dashboard but not in the Calendar or the preview. Moving them onto the RPC changes behavior (Calendar quirk #4) | [`CALENDAR_PAGE`](./CALENDAR_PAGE_ARCHITECTURE.md) |
| 41 | One hiring-shares fetch (select + fail-soft `error ? []`) | 3: TeamProspectsTab `load()` and useHiringColumnShares (the same four-column read of every row), useMyHiringShares (own rows, `role_id` only) | the same fail-soft read is written three times and none has drifted yet; TeamProspectsTab batches it into its 8-query `load()`, so the seam is a fetcher, not the hook | [`PROSPECTS_TABS`](./PROSPECTS_TABS_ARCHITECTURE.md) |
| 42 | A Company-Customers Drive folder constant (promote the `CUSTOMER_AND_JOB_FOLDERS_URL` + `CustomerAndJobFoldersLink` that JobFormCustomerSection already exports) | 3 files, 5 copies of the literal: DetailJobModal ×3 inline, EditCustomerForm (private `JOB_FOLDERS_DRIVE_URL`, link hand-rolled ×2), JobFormCustomerSection (the export; JobFormEditFactRows already imports the link) | one hard-coded URL | [`JOBS_MODALS`](./JOBS_MODALS_ARCHITECTURE.md) |
| 43 | ★ `lib/pay/payReportInputs.ts` + tests for `payStubDeductions` and `payReportAssignmentsBreakdown` | 2 files, 4 copies of the report's input fetch (crew rows → job/bid maps via the two RPCs → `computePayReportAssignmentsBreakdown`): People.tsx ×3 (`printPayStub` ≡ `buildPayStubViewHtml`; `generatePayStub` takes its days from `generatePayStubRecord` and shows no payments) and `draftPayrollPersonBreakdown.ts` (the crew half only; its salaried day rows differ on purpose). Not copies: the Leave flow (it only calls `generatePayStubRecord` and builds no report) and the `pay_stub_payments` inserts (payment writes, not report inputs) | the input fetch is copied; `stubNetPay` (27 calls in 9 files, none inline) and the breakdown kernel have no test file | [`PEOPLE_TABS`](./PEOPLE_TABS_ARCHITECTURE.md) |
| 44 | ★ A materials-by-stage input builder | 2: the Takeoffs rail memo (`stageSummary`, which also feeds the tab's schedule-of-values and rough-breakdown prints); `loadMaterialsByStageForBid` (the one door Cover Letter and approval PDF already share) | the same seven inputs (rows, lines, coverage rounding extras, splits, bundle parts, assembly defaults, factor) are assembled twice by hand and the loader is untested; today's differences (the tab passes every version's splits and parses the factor with a bare `Number` instead of `parseSovMaterialFactor` / the unused `effectiveSovFactor`) are inert, so drift comes from the next input added to one side only | [`BIDS_TAKEOFF_TAB`](./BIDS_TAKEOFF_TAB_ARCHITECTURE.md) |

**Two-surface pairs:**

| Pair | Seam | Start from |
|---|---|---|
| Workflow ↔ Projects Forecast | ★ `workflowMoneyTotals` for 3 copies: the Forecast tab's `moneyBalances` + margin/balance chips, and in Workflow both `calculateProjectionsTotal`/`calculateLedgerTotal` (the Projections panel and "Left") and the ledger rail's inline margin/balance. Projections are summed as `Number(p.amount ?? 0)` in one place and `p.amount \|\| 0` in the other two, which gives the same result on the `numeric NOT NULL` column, so pin that in a test. Projects.tsx's money strip sums the same tables per project a third way. Workflow adopts **`projectsForecastStageLineItems`** for its writers, pickers and PO/invoice detail reads, but not for its all-steps loader or its parentheses `formatAmount`. `expectedDatesLinkage.ts`: only Forecast's start handler refuses a negative length | [`PROJECTS_FORECAST_TABS`](./PROJECTS_FORECAST_TABS_ARCHITECTURE.md), [`WORKFLOW_PAGE`](./WORKFLOW_PAGE_ARCHITECTURE.md) |
| Checklist ↔ Dashboard My Inbox | the completion engine through `checklistCompleteInstance` (3 inline copies — Today, Review, `DashboardMyInboxCard` — land a day early in America/Chicago and also notify the completer, which the kernel skips — owner call first), one `ChecklistForwardModal` (3 copies; the card's has drifted), recipient + display-order sort kernels (sort ×4: Today, Review, the card's Outstanding, `useDashboardBoot` — the card's own refetch drops the order) | [`CHECKLIST_TABS`](./CHECKLIST_TABS_ARCHITECTURE.md) |
| Jobs ↔ Dashboard | `lib/jobStatusMutationCore.ts` (new): the `update_job_status` call read through **`updateJobStatusClientFeedback`**, plus the send-back chain (Stripe void → ledger cleanup, or `delete_billed_invoice_on_send_back`, then `syncJobToReadyToBillIfNoBilledInvoicesRemain`), under `useJobsStagesMutations` + `useDashboardBillingInvoices`. Edit Job's `JobFormInvoiceList` writes the chain by hand a third time; its Stripe half is also in `HostedStripeBillPanel` and `SplitBillModal`. `lib/markJobReadyToBill.ts` already wraps the status call (untested), and the send-back confirm handlers in `JobsStagesTab` + `DashboardBillingPipelineSection` write out again the Stripe prep the hooks' `moveJobToReadyToBillWithStripePrep` already does. The wrappers differ on purpose and stay per surface: Jobs serializes, times out at 15 s, patches the jobs cache and reports to the page banner; Dashboard locks per id, reloads its lists and shows toasts | [`JOBS_TABS`](./JOBS_TABS_ARCHITECTURE.md) |
| Layout ↔ Settings | `lib/signOut.ts` + one impersonation return using `IMPERSONATION_ORIGINAL_STORAGE_KEY` | [`APP_SHELL`](./APP_SHELL_ARCHITECTURE.md) |
| Settings ↔ Dashboard | `dashboardButtonDefaults(role)` (Settings carries a third default) + `useDashboardButtonPrefs` | [`SETTINGS_TABS`](./SETTINGS_TABS_ARCHITECTURE.md) |
| Banking Accounting ↔ Drag Sort | one labels loader (both run `ensureDragSortDefaultLabels` and then the same `select('*')` ordered by `sort_order, id`; Accounting adds the usage-count RPC) + one assignment upsert/delete pair (4 identical copies: both tabs, `TransactionDetailModal`, `BankingMercuryTxDetailModal`). Leave the assignment-map loaders apart: they differ on purpose (Accounting reads the whole table in pages with a seq token; Drag Sort reads 400-id chunks and rebuilds `bucketStats`), and the tested `fetchLabelIdByTxId` already does the chunked read. Accounting's `useAccountingLabelsAndAssignments` is a seam inside the Accounting tab that builds on these, not a hook the two tabs share | [`BANKING_TABS`](./BANKING_TABS_ARCHITECTURE.md) |
| Job Summary ↔ Pipeline | `lib/jobThreadLatestActivity.ts` (3 copies; the note-vs-report pick matches, the body text does not — Summary reads `Report: <template> — <preview>`, the Pipeline box strips stamp prefixes) | [`JOBS_JOB_SUMMARY_TAB`](./JOBS_JOB_SUMMARY_TAB_ARCHITECTURE.md) |
| Clock strip ↔ My Team hook | one `sessionDurationSeconds`, the only function the two share (the math matches on purpose; the strip's `formatDurationFromSeconds` / `formatElapsedOpen` / `formatHoursH` have no hook copy). The tested kernel already exists: `dayMixSessionDurationSeconds` in **`lib/dayJobMixPercentages.ts`** has the same signature and math, and `calendarSessionDurationSeconds` in `lib/calendarClockSessionDisplay.ts` is its object-argument twin. Move it to `lib/clock/` and import it. There are 6 clamped copies: strip, hook, the 2 libs, `HoursUnassignedModal` and `PeopleHoursDayAuditModal`. The `ClockInOutButton` / `DashboardMyTimeSection` / `ReviewHoursModal` sums drop `Math.max(0, …)`, so they are not copies | [`CLOCK_SURFACES`](./CLOCK_SURFACES_ARCHITECTURE.md) |
| AR modal ↔ AR count hook; Collect modal ↔ field queue | `bankPaymentsListFilter` (2 copies) + adopt **`canRecordArPayments`** as the one apply gate (`canRoleApplyBankPayments` ≡ `canRoleUseArBankCount` ≡ usePipelineMoneyOpportunities' `canOpenAr`); Collect adopts **`parseStripeInvoiceDetailsResponse`**, which the field queue already uses (`collectPaymentStepFromFlowStatus` is Collect-only, ×2) | [`AR_PAYMENT_MODALS`](./AR_PAYMENT_MODALS_ARCHITECTURE.md) |
| Takeoff ↔ Materials | Takeoff's inline `filterPartsByQuery` / `filterTemplatesByQuery` (1 file, 2 copies, passed as props to 3 children) move onto **`lib/materials/materialsFilters`** (tested; the Materials tabs already use it). Widen the kernel first, because a plain import is not a drop-in: Takeoff's parts carry `part_types` where the kernel reads `part_type` (a bare import compiles but stops matching part-type names), and the kernel's template filter takes an `assemblyTypes` list and also matches the type name, while Takeoff has the `assembly_types.name` join instead. `takeoffTemplatePickerOptions` stays in Takeoff | [`BIDS_TAKEOFF_TAB`](./BIDS_TAKEOFF_TAB_ARCHITECTURE.md) |
| Prospects ↔ Quickfill | `prospectWarmthCounts` rebuilt on `prospectListGrouping` (the list groups by stage now), with a test | [`PROSPECTS_TABS`](./PROSPECTS_TABS_ARCHITECTURE.md) |
| Documents ↔ Workflow + Projects Forecast line items | one supply-invoice search predicate, 3 copies in 3 files: Documents' `documentsSupplyInvoiceRowMatchesSearch` (its `includes('paid')` makes `'unpaid'` match every invoice; it also searches job allocations and the formatted date and amount, which only the ledger needs) and two word-for-word identical invoice pickers in `Workflow.tsx` and `ProjectsForecastStageLineItemsSection.tsx` (exact `q === 'paid'`); no kernel yet, and the pickers' half belongs next to `AvailableInvoiceOption` in the tested **`projectsForecastStageLineItems`** | [`DOCUMENTS_PAGE`](./DOCUMENTS_PAGE_ARCHITECTURE.md) |

---

## Core principles

1. **Two stages per unit of work, always in this order:**
   - **Stage A — Extract pure logic to `lib/*` + tests.** Move calculation, formatting, parsing, CSV/PDF/HTML builders, and any data-shaping that has *no React/JSX* into `src/lib/<domain>/*` (or `src/lib/<domain>Documents/*` for print/PDF). Add a `*.test.ts` next to each. This is safe, reviewable, and independently shippable.
   - **Stage B — Move the component.** Only after the pure logic is out, lift the tab's JSX + its tab-local state/effects/handlers into `src/components/<page>/<Page><Tab>Tab.tsx`. The parent renders a thin `<...Tab .../>` wrapper.

   Doing A before B keeps each Stage-B diff small (mostly a cut/paste move) and means the risky calc lives behind unit tests before any UI moves.

2. **Selection + URL state stays in the parent, passed down as controlled props.** The page owns "which record is selected" and the URL deep-link router. Each extracted tab receives `selectedX` + `onSelectX`/`onClose` as props — it does **not** own its own selection. This preserves cross-tab navigation and `?tab=…&id=…` deep links. (See [Bids: shared bid pointer](./BIDS_TABS_ARCHITECTURE.md#the-shared-bid-pointer).)

3. **Tab-local state moves with the tab; shared state stays and is injected.** If a piece of state/effect/handler is used by exactly one tab, it moves into that tab's component. If it's read or written by 2+ tabs (or by the URL router, or by a shared modal), it stays in the parent and is passed in as a prop/callback.

4. **For high-coupling tab clusters, build a shared hook seam first.** When several tabs share a data engine (e.g. counts → takeoffs → labor → pricing all read/write the same cache and loaders), extract that shared state + loaders + load-effects into a hook (`src/hooks/use<Page><Engine>.ts`) that returns one object the parent destructures. Then each tab in the cluster consumes the engine via props. This is the unlock that lets coupled tabs come out without rewiring everything. (See [Bids: pricing-engine shared layer](./BIDS_TABS_ARCHITECTURE.md#pricing-engine-shared-layer) → `useBidPricingEngine`.)

5. **Extract lowest-coupling tabs first; order by value ÷ risk.** Cheap, isolated tabs first to build momentum and validate the seam; the tightly-coupled cluster last, after its shared hook exists.

6. **Verify after every step.** `npm run typecheck && npm run lint && npm test` must stay green after each extraction (each tab is its own commit). Never batch multiple tab moves into one unverified change.

7. **Behavior-preserving only.** Decomposition is a move, not a redesign. Do not "improve" logic, rename DB columns, or change UX in the same pass. Preserve quirks (even hardcoded constants — note them) so the diff is reviewable as a pure refactor.

---

## The process, step by step

### Step 0 — Map the page (once per page)

Start from the fact sheet, not the source: `npm run map -- <file>` prints every state / effect / memo / handler / render block with its line range, who reads and writes each piece of state, and the tables, RPCs and edge functions each unit touches; open only the ranges that decide a question. `npm run map -- --triage` lists the big files with no map or a stale one. Create `docs/<PAGE>_TABS_ARCHITECTURE.md` (copy the header + section shape from `BIDS_TABS_ARCHITECTURE.md`), with `covers:` (the source files it maps) and `mapped_at:` (the commit its line numbers were read at) in the front matter. For each tab, inventory:

- **Render location** (line range / `activeTab === '…'` gate).
- **Owned local state** — state used *only* by this tab (these move with it).
- **Cross-tab / shared state** — state read/written by 2+ tabs, the URL router, or shared modals (these stay in the parent).
- **Derived memos**, **handlers/functions**, **data dependencies**, **supabase tables**.
- **Sub-components** (already extracted vs inline).
- **External coupling** + an **extraction status + risk + suggested approach**.

Then write a **Master summary table** and a **Recommended extraction order** (value ÷ risk, lowest coupling first). Identify any **shared substrate**: the page's equivalent of Bids' `setSharedBid` selection pointer and/or its `useBidPricingEngine` data engine. Note when a page has *no* single shared pointer (e.g. People keys by `person_name` and gives each tab its own selection — so only *data* is shared, not a UI selection).

### Step 1 — Pick the next tab

Take the lowest-coupling not-yet-extracted tab from the order. If the next-best tab belongs to a high-coupling cluster and the shared hook doesn't exist yet, do **Step 2 (seam)** first.

### Step 2 (only for clusters) — Build the shared hook seam

Extract the cluster's shared state + refs + loaders + load-effects into `src/hooks/use<Page><Engine>.ts`. The parent passes the cluster's selections + page context (`activeTab`, role, `setError`, master loaders) as inputs and **destructures the returned object**, so existing downstream references are unchanged. Put shared types in `src/lib/<page>/<engine>Types.ts`. Leave UI-coupled effects (those that write parent-owned UI state) in the parent for now and revisit per tab.

### Step 3 (Stage A) — Pure logic → `lib/*` + tests

For the chosen tab, identify every pure function it uses inline (calc, format, parse, CSV/PDF/HTML builders, filtering/bucketing). Move each into `src/lib/<domain>/*` and add a colocated `*.test.ts`. Print/PDF builders go to `src/lib/<page>Documents/*` and take an explicit context object (no React, no parent closure). The parent/tab then calls the lib function. Verify green.

### Step 4 (Stage B) — Move the component

Create `src/components/<page>/<Page><Tab>Tab.tsx`. Move into it: the tab's JSX, its owned local state/effects/handlers/memos, and any tab-local sub-components or module-level sortable rows. The parent renders a thin wrapper and passes:

- **Controlled selection:** `selectedX`, `onSelectX`/`onClose`.
- **Shared engine values** (for cluster tabs): destructured from the hook.
- **Shared state read/written by others:** as props + setter callbacks.
- **Master loaders / cross-cutting callbacks:** `onEditX`, `loadX`, `onError`, modal openers.

What **stays in the parent:** the URL deep-link router + any `apply…DeepLink…` glue, shared modals opened from multiple tabs, and any state the router or a sibling tab also touches. Verify green.

### Step 5 — Update the map + commit

Flip the tab's Status to `extracted` in `docs/<PAGE>_TABS_ARCHITECTURE.md`, point it at the new file, and record what stayed in the parent and why. A code comment that points at a map cites the quirk by map and name (`JOBS_STAGES_TAB map, quirk "Page-global error"`), never by number, and a map never renumbers its quirk list — strike a retired quirk through and append new ones. Commit per tab (or per Stage A / Stage B half) with a behavior-preserving message. Repeat from Step 1.

---

## What moves vs what stays — quick reference

| Concern | Moves into the tab component | Stays in the parent |
|---|---|---|
| State used by only this tab | ✅ | |
| State read/written by 2+ tabs | | ✅ (pass as prop + setter) |
| Selected-record pointer | | ✅ (controlled prop `selectedX` + `onSelectX`) |
| URL deep-link router / `apply…DeepLink…` | | ✅ |
| Tab-local effects (autosave, click-outside, reset-on-change) | ✅ | |
| Effects that write parent-owned UI state | | ✅ (revisit later) |
| Pure calc/format/parse/PDF/CSV | ✅→ extract to `lib/*` first (Stage A) | |
| Shared data engine (multi-tab cache + loaders) | | ✅ via `use<Page><Engine>` hook |
| Modal opened from 2+ tabs | | ✅ (page-level; open via callback) |
| Modal opened from only this tab | ✅ | |
| Master loaders (`loadX`, role, `authUser`) | | ✅ (pass down as needed) |

---

## Definition of done (per tab)

- [ ] Parent renders a thin `<...Tab .../>` wrapper; no tab-specific JSX left inline.
- [ ] Selection is a controlled prop; the tab owns no selection state.
- [ ] All pure logic the tab used lives in `lib/*` with at least one colocated test.
- [ ] `npm run typecheck && npm run lint && npm test` are green.
- [ ] No behavior/UX/schema change vs before (diff reads as a move).
- [ ] `docs/<PAGE>_TABS_ARCHITECTURE.md` updated: Status `extracted`, new file linked, "stays in parent" noted.

---

## Anti-patterns (don't do these)

- **Skipping Stage A.** Moving a tab with its calc still inline produces a huge, unreviewable Stage-B diff and ships untested logic into a new file.
- **Letting the tab own its selection.** Breaks cross-tab navigation and deep links. Selection is always a controlled prop.
- **Refactoring logic during the move.** Behavior-preserving only; redesign is a separate, later pass.
- **Extracting a coupled cluster tab before its shared hook exists.** You'll thread dozens of props by hand and likely duplicate the engine. Build the `use<Page><Engine>` seam first.
- **Batching multiple tab moves into one commit.** Keep each tab (and ideally each Stage) independently verifiable and revertible.
- **Pulling shared/router state into a child** because it's "convenient." If a sibling or the URL router also touches it, it stays in the parent.

---

## See also

- [`BIDS_TABS_ARCHITECTURE.md`](./BIDS_TABS_ARCHITECTURE.md) — completed reference (controlled selection, `useBidPricingEngine`, Stage-A `lib/bidDocuments/*` builders, recommended extraction order).
- [`PEOPLE_TABS_ARCHITECTURE.md`](./PEOPLE_TABS_ARCHITECTURE.md) — near-complete reference (no single shared pointer; phased hook extraction).
- `AGENTS.md` (repo root) — project-wide constraints (Supabase, RLS, types).
