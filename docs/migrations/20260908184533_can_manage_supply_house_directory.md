# 20260908184533 — can_manage_supply_house_directory(): one write predicate for supply houses and their reps

v2.3167 (supply-house-directory to-do, PR 2). Adds `public.can_manage_supply_house_directory()` (SECURITY DEFINER, `STABLE`; dev, master_technician, assistant, controller, estimator; `authenticated` only) and re-creates the three write policies on `supply_houses` (`sup_supply_houses_insert` / `_update` / `_delete`, baseline) and the three on `supply_house_contacts` (`supply_house_contacts_insert_office` / `_update_office` / `_delete_office`, `20260902170045`) to call it. SELECT policies on both tables are untouched. Effects: controller may now write houses (it was missing from the baseline arrays), primary and superintendent may not (they were in the baseline arrays; their only door was the Parts Book modal PR 3 retires). Idempotent (`CREATE OR REPLACE` + `DROP POLICY IF EXISTS`). Push **after** the v2.3167 client deploys — the client is safe on the old policies; the old client keeps working on the new ones for every role that still has a door.
