# 20260917003402_day_book_payload.sql (2026-09-16, v2.3542)

Day book, PRs 1+2 — the one read behind People → Day book. No table, no index, no read-only-block calls.

| Object | What |
|---|---|
| `get_day_book_payload(p_from date, p_to date, p_person uuid default null)` → jsonb | SECURITY DEFINER, STABLE. Gate inline on `auth.uid()` like `get_crew_day_payload`: `is_dev() OR has_payroll_access()` → any person, amounts on; `master_technician` (unapproved) / `assistant` / `estimator` → pinned to self, every `amount_usd` NULL; other roles `{error:'forbidden'}`. Range clamped to ≤ 93 days (`range_too_long`). Population: users with role dev / master_technician / assistant / controller. Day bucketing `(ts AT TIME ZONE 'America/Chicago')::date`. Office job resolved the dispatch-roster way (`app_settings.overhead_office_job_ledger_id_v1`, else `get_jobs_ledger_office()`). |
| `events` union | `invoice_billed` and attributed `invoice_sent` → `billed` (amount from `jobs_ledger_invoices`); `payment_added` → `deposit` when the payment row carries a Mercury id, else `payment`; `status_change` → `status`; `job_contract_events` sent/recorded → `contract_sent`/`contract_filed`; `clock_sessions` approvals → `approval` (one row per session, hours in detail); `hours_reviewed`; `dispatch_requests` closes → `dispatch_answered`; `deleted_records_archive` → `deleted`, **aggregated per actor, day and table** (`detail.n`, `detail.restored_n`). |
| `system_counts` | Per day and kind, `job_activity_events` rows with no actor for `invoice_sent` / `payment_added` / `status_change` — outcomes written under the service role, shown as "and N more by the system". |
| Grants | Revoked from PUBLIC and `anon`; `authenticated`, `service_role`. |

Push after the client merges (`supabase db push`), then `npm run gen-types:linked` replaces the hand-carried type in `src/types/database.ts`.
