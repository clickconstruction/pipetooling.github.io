# 20260906100000_gc_stage_asks.sql (2026-09-06, v2.2934)

The GC asks (PR 8 of the three-party scheduling plan, artifact 3a540149).

- **`job_stage_windows.asked_start / asked_end / asked_note / asked_at`** — the GC's ask from their portal; **`answered_at / answer` (`accepted` | `proposed`) / `answer_note`** — the office's reply. Written by `submit-portal-request` (the ask) and the office on Jobs → Subs → Work (the answer).
- **`step_commitments.change_requested_at / change_requested_note`** — the answered window no longer fits the sub's pick; they re-pick from their portal (`pick_dates` clears it and skips the day-before rule while it is set).

No RLS change. Apply order: **client first, then push**, then `supabase functions deploy submit-portal-request customer-portal sub-portal submit-sub-portal`.
