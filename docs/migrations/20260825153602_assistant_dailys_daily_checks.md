# 20260825153602 — quickfill_assistant_dailys_daily_checks (v2.2285)

Per-day shared checkbox state for Quickfill → Assistant Dailys (the any-assistant duties moved out of one assistant's personal recurring checklist). Mirrors `quickfill_office_arriving_daily_checks` field-for-field: `item_id text` + `work_date date` PK, `checked_at`, `checked_by → users ON DELETE SET NULL DEFAULT auth.uid()`, `work_date` index. RLS: SELECT/INSERT/DELETE for `is_dev_or_master_or_assistant()`, INSERT additionally `checked_by = auth.uid()`. Template items live in `app_settings.quickfill_assistant_dailys_items` (covered by the existing dev-manage policy — no new app_settings policy). Deliberately NOT added to the `supabase_realtime` publication (the office table's channel was shed in 20260624160100; the section refreshes on visibilitychange). Ends with both read-only-block calls.
