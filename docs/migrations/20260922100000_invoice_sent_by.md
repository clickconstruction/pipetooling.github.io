# 20260922100000_invoice_sent_by.sql (2026-09-22, v2.3711)

**Purpose**: the invoice send carries who sent it, so the `invoice_sent` activity event is attributed even when the row is written under the service role (Day book PR 1b — the census found 94 % of sends unattributed because `send-stripe-invoice` persists with the service-role client).

**Changes**
- `jobs_ledger_invoices.sent_by_user_id uuid` (nullable, `REFERENCES users(id) ON DELETE SET NULL`), with a column comment.
- `public.jobs_ledger_invoices_to_activity()` re-created with one change: the `invoice_sent` branch stamps `coalesce(auth.uid(), new.sent_by_user_id)`. The three other branches (`invoice_created`, `invoice_billed`, `invoice_write_down`) are as in `20260608010000_job_activity_events.sql`. The trigger definitions are untouched (`after update of billed_at, sent_to_customer_at, agreed_write_down_at` — the send functions write `sent_by_user_id` in the same UPDATE as `sent_to_customer_at`).

**Writers**: `send-stripe-invoice` (service role, stamps the resolved caller) and `send-physical-invoice-email` (as the user). Backfills and imports leave it NULL and stay system rows on the Day book.

**Idempotent**: `ADD COLUMN IF NOT EXISTS`, `CREATE OR REPLACE FUNCTION`. Additive. No new table, so no read-only-block calls.

**Deploy order**: push first, then redeploy the two functions (they write the column).
