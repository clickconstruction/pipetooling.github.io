# 20260908185956 — supply_houses: drop contact_name and email; reps are the only contacts

v2.3170 (supply-house-directory to-do, PR 4). Three steps, idempotent: (1) for a house with a `contact_name` but no `email` and no live rep, append `Contact: <name>` to `notes` (a rep needs an email, so the name had nowhere else to go); (2) insert a rep for any house email not already among its reps (label `primary`, default only when the house had no live rep — the 2026-09-08 probe found zero such rows, so this is a guard); (3) `ALTER TABLE public.supply_houses DROP COLUMN IF EXISTS contact_name, DROP COLUMN IF EXISTS email`. No policy or grant changes; `phone` stays as the main number. **Push after the v2.3170 client deploys** — the pre-v2.3170 form writes both columns on every save and would fail with "column does not exist" until reloaded. Then `npm run gen-types:linked` in a `chore(types)` PR.
