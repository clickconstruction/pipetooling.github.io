# 20260826030000_line_items_bulk.sql — bulk line items (v2.2315)

`get_payment_line_items_bulk(p_payment_ids uuid[])` — same per-payment shape
as `get_payment_line_items` (which stays for other callers) but keyed by
payment id, capped at 200 ids per call (`p_payment_ids[1:200]`). Feeds the
always-expanded Data health drill-down; the client chunks its 545 payments
into ~4 calls. Same gate, anon revoked. CREATE OR REPLACE only.
