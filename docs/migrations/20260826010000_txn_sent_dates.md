# 20260826010000_txn_sent_dates.sql — drill-down rows tell their story (v2.2309)

1. **`get_pay_speed_transactions` v3** (v2 was `20260826000000`): each payment
   row adds `sentYmd` (`jobs_ledger_payments.sent_on`) so the Data health
   drill-down shows sent → paid per row.
2. **`get_payment_line_items(p_payment_id)`** (new, SECURITY DEFINER, same
   gate as the drill-down, anon revoked): the line items behind one payment —
   its bill's `jobs_ledger_fixtures` when linked (`linked: true` +
   `billAmount`), the job's fixtures as context when not. Amounts =
   `count × line_unit_price`. Lazy-loaded on row expand.
Both CREATE OR REPLACE; no table DDL.
