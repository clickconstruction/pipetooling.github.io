# 20260829021122_plan_substrates

`bids_plan_substrates`: immutable per-version jsonb plan substrates keyed to bids
(estimator-twin pipeline Wave 1.4; contract in `docs/twins/SUBSTRATE.md`). Estimating-staff
RLS (read 7 roles / write estimator+, bid_proposal_rooms pattern); ends with both read-only
appliers + `apply_digital_twin_write_blocks()` (fence auto-detects the `bid_id` column, so
twins write substrates only on their own/assigned bids).
