# 20260926025000_supply_invoice_payment_link.sql (2026-09-25, v2.3843)

Adds `supply_house_invoices.payment_link text` (nullable, commented). Supplies → Supply Houses → **Apply Payment** asks for an optional *Payment or receipt link* and used to write it into `link`, which is the invoice's own paper (the scan the invoice form saves; the detail table's **View**) — a typed payment link replaced the scan on every selected bill (v2.3830 already stopped a blank link from nulling it). The owner's call (2026-09-25): keep the scan in `link` and store the payment link here. Additive, `IF NOT EXISTS`, `lock_timeout = 3s`; no RLS, grant or trigger change (the table's policies cover the column; `sync_supply_house_invoice_paid_at` is untouched). The client that writes and shows it ships in v2.3843 after this is pushed.
