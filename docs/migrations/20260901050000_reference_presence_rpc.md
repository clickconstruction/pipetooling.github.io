# 20260901050000_reference_presence_rpc

`list_reference_presence()` (security definer, authenticated): for every decided
human bid, booleans `has_counts` / `has_pricing` — the two grade inputs not on
the bid row itself. Feeds the Bid Board's reference-grade robot-icon badge
(v2.2547). Presence only, no row data. Idempotent; appliers not required.
