# 20260922120000_trial_tally.sql (2026-09-22, v2.3715)

Hiring → the helper try-out loop, PR 3 (`to-dos/helper-tryout-loop`): the tally on the Try-out card. Additive and idempotent; no new table, so no read-only appliers.

- `team_prospects.trial_deferred_at` / `trial_deferred_by` (→ `users`, `ON DELETE SET NULL`) — *Keep trying*: the office's answer "not yet" to the nudge. Written by the client through the board's existing UPDATE policy; the kernel keeps the nudge quiet until a verdict newer than the stamp lands. Hire / Pass ignore it.
- **`team_prospect_trial_tally()`** → `jsonb` — one object per card on trial: the helper's **days** up to today (any date they clocked, were listed on a block, or have a verdict for — `clocked` and `open` say which; the day's job from the clock, else the block), with **who the rule would have asked** each day (`trial_helper_supervisors`, service-role only — hence the definer), **every leader's verdict by name and role**, and the Keep trying stamp with the presser's name. `SECURITY DEFINER`; the gate is inside the query — `is_dev()` or `user_has_team_prospects_access()` — so everyone else gets `[]`. The board cannot read `clock_sessions` (its SELECT policy is pay-approved masters, their assistants, team leads and oneself) and cannot call the rule, which is why the tally is an RPC and not a client join. When the column share (PR 4) exists, its rule joins this gate alongside `create-user`'s door and `end_team_prospect_trial()`.

Not dry-run: Docker was not reachable from this session (`permission denied` on the socket), so the SQL was checked by reading against the v2.3650 migration it mirrors (`trial_helpers_i_led_today` uses the same joins). The RPC is read-only and fail-soft in the client — a missing or erroring RPC leaves the card as it was in v2.3627.

**Apply order:** client first (a missing RPC is swallowed; Keep trying fails with a plain error until the columns exist), then `supabase db push` from a checkout at `main`. Regenerate types afterwards as a `chore(types)` PR; the client calls the RPC and writes the two columns through the untyped door until then.
