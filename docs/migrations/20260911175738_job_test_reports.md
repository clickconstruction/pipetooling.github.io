# 20260911175738_job_test_reports.sql (2026-09-11, v2.3298)

Test reports PR 2 (fragment `docs/recent-features/v2.3298.md`; plan `to-dos/test-reports/README.md`).

- **`job_test_reports`** — one row per hydrostatic / pinpoint / gas test report on a job: `test_type` (CHECK pre_test · post_test · pinpoint · gas), `system` (supply · sewer), `result` (pass · fail), `test_date`, `duration_minutes`, `notes`, the pinpoint trio, `gas_pressure_psi` + `gas_fixtures jsonb`, four per-report text overrides (`system_tested` · `test_method` · `test_pressure` · `conclusion`, NULL = the Settings default), the certifier snapshot (`certifier_name` / `certifier_license`, stamped at send), `source_report_id` → `reports` (PR 4's clock-out draft; unique when set), `status` (draft · sent), `pdf_path` + `pdf_version` (PR 3's stored PDF), the send record (`sent_at` / `sent_to[]` / `sent_cc[]` / `sent_by` / `sent_pay_url`), `created_by`, timestamps (`update_updated_at_column` trigger). Indexes: job + created, drafts, source report.
- **`can_reach_job_for_test_report(p_job_id)`** — the `jobs_ledger_fixtures` job predicate, named: master own · dev · primary · adoption either way · `assistants_share_master`. SECURITY DEFINER, `authenticated` only.
- **RLS**: SELECT dev / assistant-like / master / primary; INSERT + UPDATE dev / assistant-like / master; DELETE the same set **and `status = 'draft'`** — a sent report is a record. All four AND the helper. `authenticated` granted SELECT/INSERT/UPDATE/DELETE.
- **`app_settings` seed** `test_report_settings_v1` (ON CONFLICT DO NOTHING): the certifier and letterhead identity from the external app's literals (Malachi Whites #RMP41130, Click Plumbing, office phone, mailing + TSBPE addresses). The paper's sentences fall back to the kernel defaults until edited on Settings → Jobs & billing → Test reports.
- Ends with both read-only blocks.

Apply order: merge (client), then `supabase db push`, then `npm run gen-types:linked` (the PR ships a hand-aligned `database.ts` block). No edge function. The storage bucket arrives with PR 3.
