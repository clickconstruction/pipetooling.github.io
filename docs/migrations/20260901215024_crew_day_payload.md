# 20260901215024_crew_day_payload

**v2.2602** — `get_crew_day_payload(p_day date)` for the Dashboard Crew Day section.

- SECURITY DEFINER, STABLE, `search_path = public`; EXECUTE granted to anon/authenticated/service_role (the function enforces roles itself).
- Payload (jsonb): `sessions` (clock_sessions on `work_date`, non-revoked/rejected), `blocks` (job_schedule_blocks), `reports` (job-anchored, Chicago-day `created_at`, with template name + field_values), `pct_notes` (`% complete` thread notes on involved jobs), `users` (id/name), `jobs` (label fields + status + pct_complete).
- Scoping: dev/master_technician/assistant/controller company-wide; superintendent limited to involved jobs whose project passes `can_access_project_row` or where they're in `jobs_ledger_team_members` (job-less rows dropped); other roles `{'error':'forbidden'}`.
- No table changes — function only, idempotent (`CREATE OR REPLACE`). No pay data.
- Client: `src/lib/crewDay.ts` + `src/components/dashboard/DashboardCrewDaySection.tsx`.
