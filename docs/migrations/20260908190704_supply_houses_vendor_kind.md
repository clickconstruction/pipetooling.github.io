# 20260908190704 — supply_houses.vendor_kind + a hand seed; is_insurer becomes derived

v2.3172 (supply-house-directory to-do, PR 5). Adds `vendor_kind text NOT NULL DEFAULT 'supply_house'` with a CHECK over `supply_house | insurer | rental_yard | sub_ledger | other` and an index; seeds by name (flagged rows and *Texas Mutual (Workers Comp)* → insurer, *Sunbelt Rentals* → rental_yard, *Outside Subcontractors* → sub_ledger, *Alexander Oil Company* / *Amazon* / *🤖 Web Research* → other, the rest default); installs the `supply_houses_derive_is_insurer` BEFORE INSERT OR UPDATE trigger so `is_insurer = (vendor_kind <> 'supply_house')` from here on, and backfills the flag once. Idempotent (`IF NOT EXISTS` guards, seed `UPDATE`s only touch rows still at the default). **Push after the v2.3172 client deploys**: the client writes `vendor_kind` and retries without it on the unknown-column error, so either order works, but client-first means no retry. Then `npm run gen-types:linked` in a `chore(types)` PR. `is_insurer` is dropped by a later PR once every client reads the kind.
