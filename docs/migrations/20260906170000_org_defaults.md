# 20260906170000_org_defaults.sql (2026-09-06, v2.2951)

Journey map Tier 5 X16 (cluster C27, decision 19), train T5-08 — per-device defaults become org / role defaults; alert dismissals follow the person.

- **`org_defaults`** (new): `(key, role)` PK → `value`, `updated_by`, `updated_at`. `role = '*'` is the org-wide row; a role row overrides it. RLS: SELECT any signed-in user; ALL for `is_master_or_dev()`. Keys today: `jobs.stages.mobile_cards`, `tally.payroll_auto_apply`, `billing.stripe_mode` (registry in `src/lib/orgDefaults.ts`).
- **`user_dismissals`** (new): `(user_id, key)` PK → `state jsonb`; RLS own rows only. `key` is the dismissal kernel's localStorage prefix; the client mirrors every save here and seeds localStorage from it at sign-in.
- Both read-only appliers run (CREATE TABLE rule).

Apply order: either — both tables are read soft (a missing table = no rows), so the client falls back to today's behaviour until the push; Settings → Company → Defaults for everyone errors with a toast until then.
