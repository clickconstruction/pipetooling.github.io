# 20260923230000_driving_distance_cache.sql (2026-09-23, v2.3773)

Creates `public.driving_distance_cache`: the routed-drive cache the `driving-distance` edge function reads before asking the Google Routes API and fills after an answer — one row per origin → destination pair keyed by coordinates to five decimals, `distance_meters`, nullable `duration_seconds`, `source`, `computed_at`; 30-day TTL enforced in the function. RLS on with no policies (service role only); the read-only blocks applied. Ships with v2.3773 (the clocked-in map's Travel times become free on a repeat press, and the bid form's Distance to Office auto-fill shares the cache). Apply order: push any time — the function tolerates the table being absent (a failed lookup is a miss; a failed upsert is ignored) — but deploy the function after the push so the cache is used.
