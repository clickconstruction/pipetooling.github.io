# 20260906200000_one_company_policy_sweep.sql (2026-09-06, v2.2970)

One company, **Phase 2** of [`docs/ONE_COMPANY_PLAN.md`](../ONE_COMPANY_PLAN.md) — the mechanical policy sweep. **Generated** by [`scripts/one-company-policy-sweep.py`](../../scripts/one-company-policy-sweep.py) (Python 3.11 + `pglast`); never hand-edit, re-run the script on a rebase conflict.

- **What it finds**: every *live* permissive RLS policy (CREATE minus later DROP, ALTER folded in, across all migrations in order) whose `USING` / `WITH CHECK` still reasons about per-person ownership or adoption inline — `master_user_id = auth.uid()`, `master_assistants`, `master_shares`, `assistants_share_master()`, `can_see_sharing_master()`, `master_shared_current_user()`. Phase 1 (`20260906190000`) rewired the shared helpers but could not reach these. **135 policies on 47 tables** (45 SELECT, 39 INSERT, 28 UPDATE, 22 DELETE, 1 ALL).
- **What it does**: `DROP POLICY IF EXISTS` + `CREATE POLICY` with the same name, command, roles and expression, wrapped as `USING (public.is_office_staff() OR (<old expression>))` (and the same for `WITH CHECK`). Office roles — dev, master/leader, assistant, controller — pass outright; every field / primary / superintendent / customer-side branch is byte-for-byte what it was (the old owner / adoption branches stay inside the parenthesis, redundant for the office, until Phase 5 retires the grant tables).
- **Skipped**: `users` (its policy calls the Phase 1 helpers), the dormant `master_shares` table, and every restrictive policy (none reference ownership; the script refuses if one appears).
- **Tables**: bids_submission_entries, customer_contact_persons, customer_contacts, customers, developments, estimates, job_collect_payment_flows, job_contract_events, job_contracts, job_demand_letters, job_hazmat_incidents, job_lien_filings, job_lien_releases, job_pct_events, job_schedule_blocks, job_status_events, jobs_ledger, jobs_ledger_fixtures, jobs_ledger_invoice_stripe_email_sends, jobs_ledger_invoices, jobs_ledger_materials, jobs_ledger_payments, jobs_ledger_team_members, jobs_ledger_thread_note_stats_cache, jobs_ledger_thread_notes, jobs_receivables, jobs_tally_parts, labels, material_po_generator_entries, people, people_labels, people_labor_job_items, people_labor_job_payments, people_labor_jobs, project_workflows, projects, prospects, recurring_job_report_dispatch_log, recurring_job_report_schedule_recipients, recurring_job_report_schedules, stripe_oob_payment_reverts, team_prospect_roles, team_prospects, user_labels, workflow_projections, workflow_template_steps, workflow_templates.

**Why the effect is smaller than it looks**: `sync_company_access_grants()` (v2.921) has filled `master_assistants` / `master_shares` with every live office pair since 2026-07-22, so the adoption branches already said yes to every office user; the sweep makes that true *by rule* instead of *by data*, which is what lets Phase 4 stop writing the grant tables. The 22 DELETE policies that were "owner or dev" are now office-wide — deliberate under one company.

Apply: policy DDL takes a brief exclusive lock per table (47 tables, one transaction) — push in a quiet moment; `lock_timeout = '3s'` fails fast otherwise. Order relative to `20260906190000`: either. Verify with the role-matrix probe in the plan doc.
