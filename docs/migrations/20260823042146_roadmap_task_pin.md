# 20260823042146_roadmap_task_pin.sql (2026-08-22, v2.2139)

Adds nullable `checklist_tech_tree_group_tasks.pinned_at timestamptz` (+ column comment): the roadmap **★ pin** — Next-up phase 3. When set, the task leads its lane on the Plan's ⚡ Next up shortlist (oldest pin first) without touching stage numbers or prerequisite arrows. NULL = not pinned.

No policy change: the existing UPDATE policy on the tasks table (structure editors) covers the write. No CREATE TABLE, so no read-only block calls. Additive and idempotent (`ADD COLUMN IF NOT EXISTS`); `SET lock_timeout = '3s'` up top.

**Deploy order:** this PR is migration-only (house pattern from v2.2114). The client that reads/writes the column (v2.2140) selects with `*` so it tolerates the column being absent; until this is pushed the ★ toggle simply errors and no task is pinned. Push after merge with `supabase db push`, then `npm run gen-types:linked` (the client PR carries a local optional type until then — see its docs fragment).
