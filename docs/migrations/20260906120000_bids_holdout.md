# 20260906120000_bids_holdout.sql (2026-09-06, v2.2942)

The holdout-set flag (`docs/twins/LEARNING_PLAN.md` lever 3, item 6).

- **`bids.holdout boolean not null default false`** — marks a reference as reserved for gate measurement: never run as practice, never quoted in doctrine, never named in audits, so Gate-B streaks can measure generalization instead of memorization. Owner designates ~20–25 clean A/B references across axes from the dev Queue lens (Bids → 🤖 Queue → Hold out).
- **`idx_bids_holdout`** — partial index (`WHERE holdout`) for the "which references are held out" lookups (Scoreboard + Queue lens); the set stays ~25 rows.

Plain additive ALTER — no new table, so no RLS/read-only appliers. Apply order: **client first, then push** (the client tolerates the missing column by reading "no holdout designated yet"). The twin-mcp holdout exclusions (dispatchers must never hand a holdout bid to a practice run) are a separate operator change.
