# 20260920033000_helper_trial.sql (2026-09-19, v2.3627)

Hiring → the helper try-out loop, PR 1 (`to-dos/helper-tryout-loop`). Additive and idempotent; no new table, so no read-only appliers.

- `team_prospects.trial_user_id` (→ `users`, `ON DELETE SET NULL`), `team_prospects.trial_started_at`, `users.trial_prospect_id` (→ `team_prospects`, `ON DELETE SET NULL`, partial index). `team_prospects_status_check` gains `trial`.
- `users_guard_privileged_columns()` rewritten from the 20260919151642 body with two additions, and the trigger's column list with them: **`team_prospects_access` is dev-only again** — 20260717250000 made it so, the 20260903191916 rewrite dropped the check and 20260919044528 dropped the column from the trigger, so for sixteen days any prospects-staff account could grant itself the Hiring board through the row-scoped *Users can update own profile* policy; and **`trial_prospect_id`** — a dev or a Hiring-board holder, never one's own row.
- `end_team_prospect_trial(p_prospect_id, p_outcome)` — SECURITY DEFINER, Hiring-board holders only, `hired | passed` on a card whose status is `trial`: sets the status and clears `users.trial_prospect_id` in one call. `trial_user_id` stays as the card's link to the person. Training-mode callers are stopped by the statement blocks already on both tables.

**Apply order:** deploy the client and `create-user` first — the old client never writes `trial`, and the new client reads the new columns fail-soft — then `supabase db push` from a checkout at `main`. Until the push, *Try out* fails cleanly (the function's card read selects a column that does not exist yet) and nothing is created. Regenerate types afterwards as a `chore(types)` PR; the client calls the RPC through the untyped door until then.

**Anyone rewriting `users_guard_privileged_columns()` next:** copy the body from the newest migration that defines it, not from the one you remember — that is how the Hiring guard was lost.
