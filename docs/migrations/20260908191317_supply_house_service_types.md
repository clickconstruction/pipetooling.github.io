# 20260908191317 — supply_house_service_types: the trades a supply house serves

v2.3173 (supply-house-directory to-do, PR 6). New link table `supply_house_service_types (supply_house_id, service_type_id, created_at)` with a composite PK, cascades both ways, and a `service_type_id` index. RLS: SELECT for the six Materials roles (dev, master_technician, assistant, controller, estimator, primary, superintendent); INSERT / UPDATE / DELETE behind `can_manage_supply_house_directory()` (`20260908184533`). `GRANT SELECT, INSERT, UPDATE, DELETE … TO authenticated`. Seeds three rows by house and trade name (CED, Elliott Electric Supply → Electrical; Garner Heating & Air Conditioning → HVAC) with `ON CONFLICT DO NOTHING`; every other house stays untagged, which the client reads as "serves every trade". Ends with both training-mode read-only guards and `apply_digital_twin_write_blocks()`. **Push after the v2.3173 client deploys** (the client treats a 404 on this table as no tags), then `npm run gen-types:linked` in a `chore(types)` PR.
