# 20260911223500_who_pays_readers_follow_the_rule.sql (2026-09-11, v2.3346)

Who pays the bill, PR 2 (fragment `docs/recent-features/v2.3346.md`): `CREATE OR REPLACE` of `get_gc_statement_email_payload(p_group_by, p_entity_id, p_include_collections)` — body verbatim from `20260904201238` except the `entity_id` CASE in `billed_inv` (the invoice's `bill_to_party` pick, else `jobs_ledger.bill_to_party = 'gc'`, never when `bill_to_email` is set; always when `gc_customer_id = customer_id`) and in `shell_jobs` (the job rule), plus the no-entity label `'No GC set'` → `'Not billed to a GC'`. Same signature, same grants (service-role), same row keys and order; the round RPC and the cron dispatcher pick it up on their next call. Idempotent; no table change, no RLS change. Order relative to the client deploy is free; needs PR 1's columns (`20260911221710`) applied first.
