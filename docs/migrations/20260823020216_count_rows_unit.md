# 20260823020216_count_rows_unit.sql (2026-08-22, v2.2114)

Stage 2 of the counts-vs-line-feet split (stage 1 = v2.2113, client-only). Adds **`bids_count_rows.unit text`** — nullable, **no default** (NULL = "infer from the fixture name"; a DEFAULT 'ea' would make every write path that forgets the column lie on a feet row — old cached bundles, copy functions, restores) — with CHECK `unit IN ('ea','ft','px','sqft')`, a column comment, and a backfill that stamps only the unambiguous CountTooling-export forms (`ft of …` → ft, `px of …` → px; optional `[Group] ` prefix). Re-creates **`duplicate_bid_to_service_type`** (body from `20260817160624`) so the count-row copy carries `unit`. Additive + idempotent. **Apply order:** push this BEFORE merging the client PR that writes `unit` (the stage-1 client ignores the column; a client that writes it against a missing column would fail inserts). Client reads through `effectiveCountUnit(row)` in `src/lib/bids/countRowUnit.ts`.
