# 20260908050000 — list_superintendent_jobs_for_dashboard: assigned projects only, active jobs only, status column

v2.3144. `DROP` + `CREATE` of `public.list_superintendent_jobs_for_dashboard()` (return type gains a trailing `status text`, so `CREATE OR REPLACE` alone would refuse; grants re-issued). The `WHERE` keeps only the `project_superintendents` branch — the `master_superintendents` (adoption) branch that v2.2836 (`20260905100000`) removed from every project-access helper was still here, and adoption is company-wide since v2.921, so a superintendent's Dashboard **Superintendent Jobs** section and the v2.3131 **Your jobs on a map** card listed every project-linked job in the company. Also filters `jl.status IN ('waiting','working')` like `list_assigned_jobs_for_dashboard` and returns `jl.status::text`. Function-only; no table, policy or grant-set changes; safe to push before or after the client (the client's row type already has optional `status`).
