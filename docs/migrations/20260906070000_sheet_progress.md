# 20260906070000_sheet_progress.sql (2026-09-06, v2.2931)

Their part, their percent (PR 5 of the three-party scheduling plan, artifact 3a540149).

- **`people_labor_jobs.progress_pct`** (0–100), **`progress_note`**, **`progress_at`**, **`progress_source`** (`portal` | `office`). Written by `submit-sub-portal` on `progress` (0 / 25 / 50 / 75, or a note alone) and set to 100 by `mark_work_done`. No RLS change — the columns ride on the sheet's policies.

Read by the sub portal (the chips' saved state), Jobs → Subs → Work ("50% along" beside the sub), the sheet story (a Work-row fact), and — for step-anchored sheets — mirrored onto `project_workflow_steps.percent_complete`.

Apply order: **client first, then push** (the portal keeps the old done button's behaviour until the columns exist: a report before the push fails with a plain error).
