# 20260907160000_one_company_function_sweep.sql (2026-09-07, v2.3070)

The maintenance item left after the one-company plan ([`docs/ONE_COMPANY_PLAN.md`](../ONE_COMPANY_PLAN.md)): the 45 functions that still name `master_assistants` / `master_shares`.

- **`is_office_or_estimator()`** (new): `is_office_staff() OR is_estimator()` — what an `EXISTS` over the `master_assistants` view says about the caller (its assistant side is assistant / controller / estimator; dev and leader are added because every one of these gates already lets them through a sibling branch). `authenticated` + `service_role`, revoked from `anon`.
- **Live-definition rewrite** (the `20260906010000` role-sweep technique: `pg_get_functiondef` → `regexp_replace` → `EXECUTE`, so nothing an earlier dynamic sweep changed on prod is lost). Five canonical forms only — the three `master_assistants` shapes where one side is `auth.uid()` and the other a plain column / variable → `is_office_or_estimator()`; the two `master_shares` shapes → `is_master_or_dev()`. Dry run against the repo bodies: **32 functions fully rewritten, 2 partially** (`can_modify_people_labor_job`, `user_can_read_labels_for_master` keep a `master_assistants ⋈ master_shares` join), **11 untouched** because they test some other user than the caller (`p_viewer`, `p_recipient`, `viewer_user_id`, `p_user_id`, `NEW.user_id`, `u.id`): `approve_collect_payment_for_terminal`, `can_access_project_row_for_user`, `enforce_user_labels_scope_master`, `estimate_accept_notify_filter_eligible_user_ids`, `insert_material_po_generator_entry`, `is_assistant_of_pay_approved_master`, `jobs_ledger_row_visible_for_tally_assign`, `list_job_schedule_blocks_for_schedule_email`, `list_schedule_blocks_for_share`, `staff_can_view_user_for_tally_followup`. Those keep reading the views and stay correct. Every rewritten body was re-parsed (SQL and PL/pgSQL) before this shipped; the push's `NOTICE` lines name each function and how many references remain.
- **`merge_user_accounts`** is the one function that *writes* the pair (an `UPDATE` / `DELETE` loop over a `VALUES` list of grant tables — an `UPDATE` on a view fails); the two tuples are removed from its list.

Every `EXECUTE` runs inside the migration's transaction, so a pattern that produced invalid SQL fails the push with nothing changed. Idempotent. Apply any time.
