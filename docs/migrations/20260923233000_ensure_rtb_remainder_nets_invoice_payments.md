# 20260923233000_ensure_rtb_remainder_nets_invoice_payments.sql (2026-09-23, v2.3775)

`CREATE OR REPLACE` of `ensure_single_ready_to_bill_invoice_for_job`: `v_allocated` sums `GREATEST(0, amount − Σ jobs_ledger_payments applied to that line)` over the ready_to_bill + billed rows (never-sent primary excluded) instead of their face amounts. `payments_made` already holds the payments applied to a still-open billed line, so a partly paid line was subtracted twice and the remainder clamped to $0 — Taunya's "Nothing left to bill for this job" on job 978 with about $577 unbilled. Every other branch and envelope is byte-identical to `20260828170937`. No DDL on tables, no new table. Push with `supabase db push` after merge; client and migration are safe in either order (the client twin `src/lib/billing/openLineAllocation.ts` nets the same way).
