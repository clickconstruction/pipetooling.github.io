# 20260824141336 — billed_report_payload_scope (v2.2228)

Scope fix for `get_billed_report_email_payload()` (v2.1315's 20260803100000): invoice rows are now **every billed invoice line on any non-collections job** — the board's rule (`jobsStagesBoard.ts` flatMaps billed invoices across all loaded jobs; progressive billing bills break-off lines while jobs are still Working; live examples 878/650/967 on 2026-08-24) — instead of selecting invoices only from `status='billed'` jobs, which made the emailed report miss rows its print sibling showed. The merged `'Billed line'` detail now requires the JOB to be billed with exactly one billed line (the board's merge loop iterates billed jobs only); a Working job's single billed line renders `'Invoice #N'`, matching the board. Job shells, dates, aging buckets, totals, and JSON shape unchanged. Same drift, same fix as the payment-forecast payload (20260824140029, v2.2227). Function replacement only; no tables, no policies, no edge-function redeploy needed.
