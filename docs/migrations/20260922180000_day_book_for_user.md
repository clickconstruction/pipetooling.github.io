# 20260922180000_day_book_for_user.sql (2026-09-22, v2.3733)

**Purpose**: the Crew Day email can read the Day book for its recipient (Day book PR 6b).

**Changes**
- `public.day_book_payload_for(p_uid uuid, p_from date, p_to date, p_person uuid DEFAULT NULL)` — the body of `20260922170000_day_book_access_dev_controller.sql` with `v_uid := p_uid` and the gate `v_role IN ('dev', 'controller')` (read off `users.role`; `is_dev()` only knows `auth.uid()`). SECURITY DEFINER; EXECUTE revoked from PUBLIC, anon and authenticated, granted to service_role (the two doors below are SECURITY DEFINER and reach it as the owner).
- `public.get_day_book_payload(date, date, uuid)` — re-created as a one-line SQL door: `day_book_payload_for(auth.uid(), …)`. Grants unchanged (authenticated, service_role).
- `public.get_day_book_payload_for_user(p_user_id uuid, p_day date)` — `day_book_payload_for(p_user_id, p_day, p_day, NULL)`; service_role only.

**Idempotent**: `CREATE OR REPLACE`. Additive. **Order**: after `20260922170000` (it carries that gate); a push that applies both in one go ends with these three functions.
