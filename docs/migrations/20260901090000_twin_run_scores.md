# 20260901090000_twin_run_scores

Twin confidence scoreboard (v2.2560). One row per backtest run —
run_label (unique), axis, twin/reference bid numbers, locked/reference
totals, delta_pct, counts_note, scope_verdict, gate_eligible, note,
scored_at. Seeded with BT-6..BT-16 (BT-15 gate_eligible=false, the
scope-mismatch VOID). Shadows stay in twin_shadow_runs; the scoreboard
reads both. RLS: staff SELECT only; writes via service role (twin-mcp /
harness at unseal). Ends with all three fence appliers.
