# 20260906010000_role_sweep_predicates.sql (2026-09-06, v2.2920)

Journey-map Phase 4, **Tier-2 #28 — shells admit a role the data refuses** (clusters C21 role-gate contracts broken by literal role lists, C24 robot audiences disagree), plus the RLS gaps the Phase-4 builders verified and queued for this sweep (#7 controller `jobs_ledger`, #32 prospects staff access, #4b `create_sheet_for_work_order`, B21 J31-N4 primary steps, B15 `people_kind_check`). One migration, eight sections, every one idempotent (`DROP POLICY IF EXISTS … CREATE POLICY`, `CREATE OR REPLACE FUNCTION`, `DROP CONSTRAINT IF EXISTS … ADD CONSTRAINT`; the two DO blocks skip anything that already names controller). No new table, so the read-only / twin write-block sweeps are not re-run.

| # | Object | What changed | Why |
|---|---|---|---|
| 1 | **`is_banking_staff()`** (new) | `SELECT public.is_master_or_dev() OR public.is_assistant()` — dev, master_technician, assistant, controller. `GRANT ALL` to anon / authenticated / service_role like its parents. | The Banking audience as one predicate. `is_assistant()` has been assistant-LIKE (assistant OR controller) since v2.662, but only policies that *call* it inherit that; the Mercury tables spelled the list out. |
| 2 | **38 policies on 11 Mercury tables** | Same names, verbs, `TO authenticated`; the `EXISTS (users.role = ANY (ARRAY['dev','master_technician','assistant']))` body becomes `public.is_banking_staff()`. Tables: `mercury_accounting_label_rules`, `mercury_accounting_label_suggestions`, `mercury_drag_sort_labels`, `mercury_transaction_drag_sort_assignments` (4 each, "banking staff"); `mercury_debit_card_user_links`, `mercury_transaction_attributions`, `mercury_transaction_job_allocations` (4 each, "staff"); `mercury_transaction_org_notes` (4); `mercury_transaction_duplicate_dismissals` (SELECT); `mercury_transaction_supply_house_invoice_links` ("mtshil staff select"); `mercury_transaction_ar_returned` (4 — the AR audience was dev/master/assistant/primary; now `is_banking_staff() OR primary`). | A controller opened the assistant Banking shell (`canAccessBanking` = dev / master / `isAssistantLike`) over data that refused them: empty Drag Sort / Accounting queues, all-Unlabeled reviews, refused splits (J33-F6 / J33-N2). `mercury_transactions`, the nickname tables and `mercury_category_tags` already admitted controller — the inventory was inconsistent. |
| 3 | **16 Banking RPC bodies** | A DO block reads each live definition (`pg_get_functiondef`), appends `'controller'` to every `role IN ('dev', 'master_technician', 'assistant')` literal (and the `ARRAY[…user_role]` spellings), and re-executes it. Functions: `upsert_mercury_org_transaction_note`, `replace_mercury_transaction_splits`, `list_users_for_banking_attribution`, `list_people_for_banking_attribution`, `list_people_with_kind_for_banking_attribution`, `replace_mercury_transaction_invoice_links_as_staff`, `replace_mercury_job_splits_for_linked_card_as_staff`, `list_supply_house_invoices_for_tally_link`, `search_jobs_for_tally_mercury_assign_as_user`, `list_unlabeled_mercury_transactions`, `list_user_mercury_review_window`, `backfill_mercury_auto_attributions_for_debit_card`, `bulk_insert_accounting_label_suggestions`, `bulk_approve_accounting_label_suggestions`, `count_pending_accounting_label_suggestions`, `auto_approve_pending_accounting_label_suggestions`. **Loud on drift**: a listed function that is missing, or that carries neither the literal nor `controller`, aborts the migration. Not touched: `list_mercury_drag_sort_label_assignment_counts` (its list already includes primary — a different audience). | These are the SECURITY DEFINER RPCs the Banking shell calls; the same literal refused a controller's splits, notes and label approvals. Bodies are long and `replace_mercury_transaction_splits` has been redefined six times, so the transformation runs on the live text instead of a re-typed copy. |
| 4 | **24 policies on the `jobs_ledger` family** | A DO block over `pg_policies` for `jobs_ledger`, `_fixtures`, `_team_members`, `_invoices`, `_materials`, `_payments`, `_invoice_stripe_email_sends`: every policy whose `qual` / `with_check` names `'assistant'::user_role` and not `controller` is re-created with the same name / verb / permissive flag / roles and `controller` beside `assistant` — array members gain a member, the INSERT policy's `role = 'assistant'` equality becomes `role = ANY (ARRAY[assistant, controller])`. Same rewrite v2.662 used on the users SELECT policy (`20260714213000`). Unrecognised shapes abort. | #7 (v2.2848) verified the baseline SELECT / INSERT / UPDATE / DELETE policies gate on literal arrays that omit controller, so a controller's Job window fetched null and closed itself; v2.2848 parked them on the read-only pane. The inner adoption arms (`master_assistants`, `assistants_share_master`) are role-agnostic and already covered an adopted controller — only the outer array refused them. Client: `isStaffFullJobLedgerDetailRole` and `canCreateJobsLedgerRow` gain controller in the same PR. |
| 5 | **`user_has_prospects_staff_access()`** | Staff branch is `dev / master_technician / assistant / controller` (was three roles). Comment updated. | #32 verified the client `canAccessProspectPipeline` admits controller while every prospects-family policy's gate did not. ACCESS_CONTROL's intent is the four-role staff set, so the function widens (not the client). |
| 6 | **`create_sheet_for_work_order(uuid)`** | Body from `20260905063000` with the gate split: service role and office roles pass up front; a **superintendent** passes only once the row is loaded and `superintendent_can_access_sub_work_order(labor_job_id, job_id)` says the job is theirs; everyone else is refused before the row is read. Comment updated. | #4b (`20260905120000`) scoped superintendents' work orders per row but left this RPC admitting `superintendent` by literal with no row gate — a superintendent who knew a commitment id could trigger sheet creation off their jobs. |
| 7 | **`project_workflow_steps` SELECT** ("Users can see steps for workflows they have access to") | Re-created from `20260817012110:231-245` verbatim plus one branch: `role = 'primary' AND (can_access_project_via_step(id) OR step_assignee_matches_user(assigned_person_id, assigned_to_name, auth.uid()))`. | J31-N4 (B21 verified): no primary branch, so a primary assigned to a step read zero steps — Dashboard **Assigned Stages** (`get_assigned_steps_for_dashboard`, RLS-bound) was structurally empty. `/workflows` stays off `PRIMARY_PATHS` (v2.2836) — a nav decision, not a data one. |
| 8 | **`people_kind_check`** | `DROP CONSTRAINT IF EXISTS` + `ADD CONSTRAINT` with `'controller'` in the list. Comment updated. | B15 verified `PersonKind` and the People → Users kind picker offer controller; the CHECK refused the insert. |

**Not in the migration, on purpose:** `bid_audits`. Its write policy ("Bid pricing users can write bid_audits", `20260830230000`, five roles) is now the single audience source — the client's `ROBOT_AUDIT_ROLES` mirrors it (test names the migration) and the Needs-You card, the Bids 🤖 door and the Audits tab's write controls all read that list.

**Apply order:** any time after `20260905120000` (section 6 calls `superintendent_can_access_sub_work_order`) and `20260905180000` (section 3 lists its two accounting RPCs) — both already applied. Deploy the client first or after; the client changes are strict widenings that fail closed (a controller on the old DB simply keeps the read-only pane / empty queues until push). **Deploy after `db push`: `supabase functions deploy mercury-reconcile get-mercury-account-balances`** — until then a controller's Reconciliation and Balance Sheet cash line still 403.

**Lock footprint:** `DROP/CREATE POLICY` ×63 (catalog-only, `ACCESS EXCLUSIVE` for an instant each), `CREATE OR REPLACE FUNCTION` ×19, one `ALTER TABLE people … ADD CONSTRAINT` (brief re-validation scan of a small table) — all under `SET lock_timeout = '3s'`.

**Verify (after push):** as a controller (none exist yet — Will's decision 3; impersonation or a test account): Banking → Drag Sort / Accounting queues fill; a split saves via `replace_mercury_transaction_splits`; Reconciliation and the Balance Sheet cash line load (after the fn deploys); a job opens the tabbed window; `/prospects` lists load. As a primary assigned to a step: Dashboard → Assigned Stages shows it. As a superintendent calling `create_sheet_for_work_order` with an off-job commitment id: `{"error":"Not authorized"}`. `select conname, pg_get_constraintdef(oid) from pg_constraint where conname = 'people_kind_check'` names controller. `select policyname from pg_policies where tablename like 'jobs_ledger%' and qual || coalesce(with_check,'') like '%assistant%' and qual || coalesce(with_check,'') not like '%controller%'` returns zero rows.

**Rollback:** re-run the baseline / `20260817012110` / `20260905063000` bodies for sections 2, 4, 6, 7 (the DO blocks are one-directional); `CREATE OR REPLACE` the section-5 function with the three-role list; the section-3 RPCs revert the same way (their pre-sweep bodies are in the baseline and the two 2026-08/09 migrations). `DROP FUNCTION public.is_banking_staff()` only after section 2 is reverted. Section 8: re-add the constraint without controller (fails if a controller roster row exists).
