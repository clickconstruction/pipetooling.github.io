# 20260921174057_quick_add_settings.sql (2026-09-21, v2.3677)

Quick time add, PR 5 (`to-dos/quick-time-add`): who gets the door becomes the owner's setting. `add_quick_time()` is **re-created whole** — the same rules and the same sentences as `20260921042405` (the kernel test reads both files and pins every sentence) — with one change: the role gate reads `app_settings.quick_add_roles_v1`, a comma list (`assistant,controller,estimator,primary`), whitespace ignored, and falls back to the PR 1 list (`assistant, controller, estimator, dev`) when the row is missing or blank. The daily ceiling was already `app_settings.quick_add_daily_ceiling_minutes` (PR 1); the Settings block now edits it.

No table change, no seeded row (missing = default), no fence appliers (no CREATE TABLE). Idempotent. Safe to push any time — `CREATE OR REPLACE FUNCTION` takes no lock anyone waits on; `SET lock_timeout = '3s'` is the house rule.

Push order: either side first. Before the push, the client's door follows the setting while the RPC still refuses anyone outside the PR 1 list — a newly ticked primary would see the door and be refused with *Quick time is for office staff* until the push lands, so push soon after the deploy.

Run before merge on a throwaway Postgres 15 (Homebrew `postgresql@15`, `LC_ALL=C`, socket in `/tmp`) with stub `auth.uid()`, `app_settings` and the two roles: applied twice (idempotent), and the role-list expression exactly as the function reads it answered the default list for a missing row and for a blank row, and `{assistant,primary,master_technician}` for `' assistant, primary ,master_technician '` (primary in, controller out). The function body is PR 1's verbatim except the `v_roles` declaration and the `= ANY (v_roles)` gate; `quickTimeAdd.test.ts` reads this file and pins every refusal sentence, the default array and the two setting keys. The first real quick add after the push is the live check — as dev, with the setting unset, then with `primary` ticked.
