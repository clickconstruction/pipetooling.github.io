# 20260829023703_bids_rfis

RFI spine (RFI_LOOP_PLAN R1): per-bid numbered `bids_rfis` with trigger-assigned
`rfi_number` under UNIQUE(bid_id, rfi_number), status machine
draft→approved→sent→answered|withdrawn, per-RFI `sent_to` jsonb. Staff RLS mirrors the
bids family; twins are draft-only via RESTRICTIVE per-command policies (SELECT untouched);
ends with both read-only appliers + the twin write-fence applier (bid_id auto-detected).
