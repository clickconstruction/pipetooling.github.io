# 20260912012359_bills_also_go_to.sql (2026-09-12, v2.3358)

Bills also go to, PR 1 (fragment `docs/recent-features/v2.3358.md`): three additive columns — `customer_contact_persons.gets_bill_copies boolean NOT NULL DEFAULT false` (copied on every bill sent to their customer; Bill Customer pre-ticks them), `jobs_ledger.bill_copy_other_party boolean NOT NULL DEFAULT false` (the party not billed on the job gets a copy of every bill), `jobs_ledger_invoices.copy_emails text[]` (the copy list fixed when a bill goes out — read by `send-stripe-invoice` in PR 2). Idempotent; no CREATE TABLE; each table's existing RLS governs writes. Push as the PR merges (the client's contacts selects and Bill Customer's job select name the first two columns).
