# 20260911043553_twin_setup_codes.sql (2026-09-11, v2.3277)

Robot price matrix, PR 6 ([`docs/PRICE_MATRIX_PLAN.md`](../PRICE_MATRIX_PLAN.md)) — "Set up on this Mac". One-time setup codes the `twin-setup` edge function mints and redeems so a person connects Claude Desktop to a robot with one command and never holds the key.

- New `twin_setup_codes` (`twin_user_id`, `code_hash` unique sha256 of the normalized code, `label` → becomes the credential's label, `created_by`, `created_at`, `expires_at` ≈ 10 minutes, `redeemed_at`, `redeemed_from` hostname, `credential_id` → `twin_credentials`) with a `(twin_user_id, created_at desc)` index.
- RLS: devs read (`is_dev()`); nobody writes through PostgREST — `GRANT ALL … TO service_role` for the edge function. Ends with all three fence appliers (training-mode blocks + statement trigger + the twin write-fence).
- Comments on the table and the hash/credential/hostname columns.

Additive and idempotent (`IF NOT EXISTS`, `DROP POLICY IF EXISTS`). Nothing reads the table until `twin-setup` is deployed; the client's button reports the function's error until then. **Push after the v2.3277 client deploys**, then deploy `twin-setup`. Regenerate types afterwards (untyped client in the meantime — twins pattern).
