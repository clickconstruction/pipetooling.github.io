# 20260910210000_bid_best_efforts.sql (2026-09-10, v2.3234)

The human's blind number before the robot's envelope (fragment `docs/recent-features/v2.3234.md`):

- **`bid_best_efforts`** — one row per bid: `value`, `recorded_at`, `recorded_by`, `note`. RLS: the robot-audit roles (dev, master_technician, assistant, controller, estimator) read and insert (`recorded_by = auth.uid()`); no update or delete policy, so the first record wins; a RESTRICTIVE `NOT is_digital_twin()` policy on every command, so a twin account never sees the number. `bids.bid_value` stays NULL until send, as before.
- **`twin_shadow_runs.reference_kind`** (`'best_effort' | 'sent'`, NULL until scored) — which human number `reference_value` is.
- **`score_locked_shadows(uuid)`** replaced: LEFT JOINs `bid_best_efforts`, prefers the best effort as the reference (else the sent value, the v2.3126 rule), stamps `reference_kind`, teacher = estimator → best-effort recorder → attested sender → creator, and words the `[shadow SCORECARD]` note accordingly.
- Two new triggers into the scorer, both swallowing failures: `bid_best_efforts_score_on_record_trg` (AFTER INSERT on the record) and `twin_shadow_runs_score_on_lock_trg` (AFTER UPDATE OF status WHEN the run locks — the robot may lock after the human recorded; it cannot read the table, so the run is still blind). The v2.3126 send trigger stays as the fallback.
- **`list_shadow_runs()`** dropped and recreated with `reference_kind` as its last column (scored rows only).

Apply order: **client first** (the Cover Letter card and the mirror hide the feature when the table is missing), then `supabase db push`. Twin-mcp's manual `score_shadows` verb still scores against the sent value; the DB path is primary and runs first.
