# 20260907043410_team_board_acks.sql (2026-09-07, v2.2981)

Jobs → Team, third PR of the train — **Looks right** acknowledgements.

- **`team_board_acks`** (new): `id`, `kind` (`over` | `unplanned`), `work_date`, `person_user_id` → `users`, `target_key` (`job:<id>` / `bid:<id>`, the board's own key) with mirror FKs `job_ledger_id` / `bid_id`, `acked_by`, `acked_at`, `note`. Unique `(kind, work_date, person_user_id, target_key)` — one acknowledgement per chip; deleting the row is the undo. RLS: SELECT any signed-in user; ALL for `is_master_or_dev() OR is_controller()`. Both read-only appliers run (CREATE TABLE rule).
- The ran-long threshold is **not** a column here — it is the `org_defaults` key `team.board.ran_long` (registry `src/lib/orgDefaults.ts`, edited on Settings → Company → Defaults for everyone), so no migration for it.

Apply order: either. The loader reads the table soft (a missing table = no acknowledgements), so the board keeps today's behaviour until the push; **Looks right** toasts an error until then.
