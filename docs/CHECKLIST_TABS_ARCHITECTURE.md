# Checklist Tabs Architecture Map

---
file: docs/CHECKLIST_TABS_ARCHITECTURE.md
type: Architecture Map / Decomposition
purpose: Step-0 map for the Checklist surface (per PAGE_DECOMPOSITION_PLAYBOOK.md) — inventory what every tab of src/pages/Checklist.tsx (2,533 lines) and the already-extracted src/components/checklist/ChecklistTechTreeTab.tsx (2,312 lines) touches (state, loaders, handlers, sub-components, supabase tables/edge functions, cross-tab coupling), so extraction can start without re-deriving the strategy.
audience: Developers, AI Agents
last_updated: 2026-07-29
---

## What this surface is

[`src/pages/Checklist.tsx`](../src/pages/Checklist.tsx) (**2,533 lines**) is the task-checklist page, tab-switched on a single `activeTab` state (`type ChecklistTab = 'today' | 'history' | 'roadmap' | 'review' | 'manage'`). Unusually for this repo's God components, the page is **already internally seamed**: each tab is a separate in-file function component (`ChecklistTodayTab`, `ChecklistHistoryTab`, `ChecklistOutstandingTab` = the Review tab, `ChecklistManageTab`) with a small explicit prop surface, and the Roadmap tab is **already extracted** to [`src/components/checklist/ChecklistTechTreeTab.tsx`](../src/components/checklist/ChecklistTechTreeTab.tsx) (**2,312 lines**) — which is itself large enough to need its own sub-decomposition and is mapped here too.

Consequence: most Stage-B moves on this page are near-mechanical **file moves of already-isolated components**, not the cut-a-tab-out-of-a-monolith surgery Bids/Materials needed. The real work is Stage A (pure kernels + tests) and the tech-tree sub-decomposition.

This map is coupling/refactor-oriented; for feature behavior see `PROJECT_DOCUMENTATION.md` and `GLOSSARY.md` (checklists, tech tree / roadmap).

### The parent shell (`Checklist()` — lines ~57–247)

- **Owns:** `role` (from `users.role`), `activeTab`, `loading`, `error`/`setError`, `editItemId`/`setEditItemId` (the one cross-tab selection pointer), `onRoadmapUrlParamChange` (writes `?tab=roadmap&roadmap=<uuid>`).
- **Role gates:** `canManageChecklists` = dev / master_technician / [`isAssistantLike`](../src/lib/subcontractorLikeRole.ts) (shows Review + Manage tabs); `canEditTechTree` = the same **plus `primary`** (mirrors `is_dev_or_master_or_assistant()` in DB per the in-code comment) — passed to the tech tree.
- **URL router:** `?tab=` read/write on every tab button; when no `?tab` and role is known, the **default tab is role-dependent** — `review` for staff (`canManageChecklists` roles), `today` for everyone else — written with `replace: true`. `?roadmap=<uuid>` is read here and passed to `ChecklistTechTreeTab` as `roadmapIdFromUrl`.
- **Shared modal:** [`ChecklistItemEditModal`](../src/components/ChecklistItemEditModal.tsx) (619 lines, extracted) renders at page level behind `editItemId`, opened from **both** Review and Manage. On save it dispatches `window` event `checklist-item-saved`.
- **Layout quirk:** the page container switches padding and becomes a flex column only when `activeTab === 'roadmap'` (the canvas needs `flex: 1; minHeight: 0`).

## The shared substrate (or lack of one)

**There is no shared data engine and no shared record pointer on this page.** Nothing like Bids' `setSharedBid` + `useBidPricingEngine` exists:

1. **Every tab self-loads its own data** (`loadToday`/`loadUpcoming`, `loadHistory`, `loadOutstanding`, `loadItems`, and the tech tree's `useTechTreeData`). No caches are shared between tabs; switching tabs remounts and refetches.
2. The only cross-tab **UI pointer** is `editItemId` → the shared `ChecklistItemEditModal` (opened from Review and Manage). It stays in the parent (playbook rule: modal opened from 2+ tabs is page-level).
3. Cross-tab **freshness** is event-based, not state-based: [`ChecklistAddModal`](../src/components/ChecklistAddModal.tsx) (a **global** modal mounted in [`Layout.tsx`](../src/components/Layout.tsx), opened via [`ChecklistAddModalContext`](../src/contexts/ChecklistAddModalContext.tsx)) and `ChecklistItemEditModal` both dispatch the `checklist-item-saved` window event; Review and Manage each register a listener that calls their own loader via a `loadXRef` pattern. This event contract must survive extraction unchanged.
4. The tech tree is a **separate data universe** (its own five `checklist_tech_tree_*` tables) sharing nothing with the other four tabs except `authUserId`, `setError`, and the URL params.

**What this means for extraction:** no seam hook is needed before moving tabs — each tab's prop surface is already its full contract. The absence of shared caches is why this page is low-risk. The one thing that must NOT move into a child is the `?tab`/`?roadmap` router + `editItemId` + the edit modal.

## Master summary table

| Region | Render location | Lines est. | Status | Coupling | Risk | Recommended action |
|---|---|---|---|---|---|---|
| Parent shell + tab bar + router | `Checklist()` ~57–247 | ~190 | stays | owns `?tab`/`?roadmap`, `editItemId`, edit modal, role gates | — | Keep; end-state is a thin router like Bids.tsx |
| `today` — Today | `ChecklistTodayTab` ~249–868 | ~620 | in-file component | low (props only: `authUserId`, `isDev`, `setError`) | low-med | Extract 3rd → `src/components/checklist/ChecklistTodayTab.tsx` after Stage A |
| `history` — History | `ChecklistHistoryTab` ~870–1125 | ~256 | in-file component | lowest (props: `authUserId`, `canViewOthers`, `canEditHistory`, `setError`) | low | **Extract first** → `ChecklistHistoryTab.tsx` |
| `review` — Review | `ChecklistOutstandingTab` ~1382–2068 (+ dnd rows ~1134–1380) | ~935 | in-file component (+2 module components) | med (`setEditItemId`, `ChecklistAddModalContext`, `checklist-item-saved` listener, renders extracted `ChecklistReviewInboxes`) | med | Extract 4th, moving `OutstandingByPersonSortableRow`/`List` with it |
| `manage` — Manage | `ChecklistManageTab` ~2090–2533 | ~444 | in-file component | low-med (`setEditItemId`, `ChecklistAddModalContext`, `checklist-item-saved` listener) | low | Extract 2nd → `ChecklistManageTab.tsx` |
| `roadmap` — Roadmap (tech tree) | `ChecklistTechTreeTab` (own file) | 2,312 | **extracted** (needs sub-decomposition) | low to page (5 props); high internally (React Flow + dnd-kit + fullscreen) | med (internal) | Sub-decompose: hook → `src/hooks/`, node components → own files, 2 pure fns → lib |
| Review inboxes | inside Review tab | 134 (own file) | **extracted** (`ChecklistReviewInboxes`) | none to page (self-contained via `useDispatchInbox`/`useEstimatorInbox`) | — | Done; moves with Review as a child render |

Page-level pieces that stay in the parent: `?tab=` sync + role-based default, `?roadmap=` param + `onRoadmapUrlParamChange`, `editItemId` + `ChecklistItemEditModal`, `error` display, the `role` fetch. App-level (not this page's problem): `ChecklistAddModal` + its context, `ChecklistItemMuteModal` is per-tab (opened from Today and Manage independently — each keeps its own `muteModalItemId`/`muteModalTitle` pair, so it moves with each tab).

## Per-tab dossiers

Line numbers are as of 2026-07-29 (v2.1088 era) and rot — search the symbol.

### `today` — Today (`ChecklistTodayTab`, ~249–868)

- **Render location:** `activeTab === 'today'` → `<ChecklistTodayTab authUserId isDev={role === 'dev'} setError />`.
- **Owned local state (moves with it):** `todayInstances`, `upcomingInstances`, `upcomingExpanded`, `loading`, `toggleCompleteInFlightRef` (per-instance re-entry guard Set), `notesByInstance` (draft notes keyed by instance id), the Forward-modal cluster (`fwdInstance`, `fwdTitle`, `fwdAssigneeId`, `fwdSaving` + derived `fwdMissingFields`/`fwdCanSubmit`), `users` (dev-only assignee list), `muteModalItemId`, `muteModalTitle`.
- **Cross-tab/shared state:** none. Props only.
- **Derived:** `fwdMissingFields`/`fwdCanSubmit` (computed in render body, not memos).
- **Handlers/loaders:**
  - `loadToday()` — three-step: (1) today's instances via `checklist_instances` with `checklist_items(...)` + `checklist_instance_assignees!inner(user_id)` filtered to the signed-in user; (2) overdue instances of items flagged `show_until_completed` (`completed_at IS NULL`, `scheduled_date < today`); (3) fetches `checklist_item_assignees.display_order` for the merged item set and sorts overdue-first-merged rows by `display_order` (999999 fallback) then `scheduled_date`. Seeds `notesByInstance` from persisted notes.
  - `loadUpcoming()` — next 30 future instances.
  - `toggleComplete(inst)` — optimistic update with `toggleCompleteInFlightRef` guard; un-completing nulls `notes` and drops the draft; revert-on-error restores both; on complete fires `sendCompletionNotifications` and `maybeCreateNextInstance` (fire-and-forget `void`).
  - `sendCompletionNotifications(inst)` — re-reads the item's `notify_on_complete_user_id` / `notify_creator_on_complete` / `created_by_user_id` and invokes edge function **`send-checklist-notification`** per recipient (tag `checklist-<instanceId>`); errors swallowed.
  - `maybeCreateNextInstance(inst)` — the `days_after_completion` repeat engine: reads `repeat_type`/`repeat_days_after`/`repeat_end_date` + item assignees, computes next date from `inst.scheduled_date` (module helper `toLocalDateString`), skips if past `repeat_end_date` or an instance already exists, inserts `checklist_instances` + `checklist_instance_assignees`.
  - `saveNotes(inst)` (onBlur autosave), `isNotificationRecipient(inst)` (gates the 🔕 mute button), `openMuteModal`, `openFwd`, `saveFwd`.
  - `saveFwd()` — the **Forward** flow: clones the source item's notify/reminder fields into a brand-new `checklist_items` row (`repeat_type: 'once'`, `start_date` = the instance's date), inserts a `checklist_item_assignees` row using [`getNextDisplayOrders`](../src/utils/checklistOrder.ts), creates the new instance + instance-assignee, then **deletes the original instance**.
- **Supabase tables / functions:** `checklist_instances` (SELECT/UPDATE/INSERT/DELETE), `checklist_items` (SELECT/INSERT), `checklist_item_assignees` (SELECT/INSERT), `checklist_instance_assignees` (INSERT), `users` (dev-only list); edge fn `send-checklist-notification`.
- **Sub-components:** [`ChecklistTitleWithLinks`](../src/components/ChecklistTitleWithLinks.tsx) (extracted), [`ChecklistItemMuteModal`](../src/components/ChecklistItemMuteModal.tsx) (extracted, this tab's own instance), inline Forward modal (full JSX, ~86 lines — **duplicated in Review**, see below).
- **External coupling:** none beyond the edge fn. Note the same completion engine (notifications + days-after-completion rescheduling) was already duplicated into [`DashboardMyInboxCard`](../src/components/dashboard/DashboardMyInboxCard.tsx) during the Dashboard decomposition — three copies exist in the app.
- **Extraction status + risk + approach:** In-file. **Low-med risk** — self-contained; the only subtlety is preserving optimistic/revert semantics and the fire-and-forget ordering in `toggleComplete`. Stage A first (see inventory): `maybeCreateNextInstance` date math and the notification-recipient derivation are the shared kernels. Then move the whole function component + the Forward modal to `src/components/checklist/ChecklistTodayTab.tsx` verbatim. Preserve quirk: the Forward select has **no placeholder option** — `fwdAssigneeId` starts `''` so the select *displays* the first user while the state stays empty and the "Required: Assignee" hint shows until the user actively picks (Review's copy instead pre-fills `rowUserId`).

### `history` — History (`ChecklistHistoryTab`, ~870–1125)

- **Render location:** `activeTab === 'history'` → `<ChecklistHistoryTab authUserId canViewOthers={canManageChecklists} canEditHistory={role === 'dev'} setError />`.
- **Owned local state:** `instances`, `loading`, `monthsBack` (3/6/12, default 6), `users` (loaded when `canViewOthers`), `selectedUserId` (defaults to self; staff can view others), `editMode` (dev-only checkbox), `cyclingCell` (in-flight cell key), `deletedCells` (Set of optimistically-whitened cells).
- **Cross-tab/shared state:** none.
- **Derived (computed inline in the render body — not memos):** `byItem` map (`itemId → {title, links, dates: {date → 'completed' | 'completed_by_other' | 'incomplete'}}`; `completed_by_other` when `completed_by_user_id` differs from `selectedUserId`), `sortedDates` (only the **last 60** render: `sortedDates.slice(-60)` in both header and rows), `instanceByKey` (`` `${itemId}-${date}` `` → instance).
- **Handlers/loaders:** `loadHistory()` (range = today − `monthsBack` months → today, joined titles + `checklist_instance_assignees!inner` on `selectedUserId`); `handleCycleStatus(itemId, date)` — dev edit-mode cell cycle: **incomplete → delete** the instance (optimistic white via `deletedCells`, then a `setTimeout(2000)` reload + cell un-mark); **completed → delete + reinsert** as incomplete (with `checklist_instance_assignees` row for `selectedUserId`); **not-due → insert** a completed instance. Guarded by `cyclingCell`.
- **Supabase tables:** `checklist_instances` (SELECT/DELETE/INSERT), `checklist_instance_assignees` (INSERT), `users`.
- **Sub-components:** `ChecklistTitleWithLinks` only. Grid cells are raw divs (green `#22c55e` / yellow `#eab308` / red `#ef4444` / near-white `#f9fafb` — literal status colors, per convention).
- **External coupling:** none.
- **Extraction status + risk + approach:** In-file. **Lowest risk on the page — extract first** (the momentum-builder, like `po-generator` was for Materials). Props are already the full contract. Stage A: lift the `byItem`/`sortedDates`/`instanceByKey` derivation into a pure `buildChecklistHistoryMatrix(instances, selectedUserId)` in `src/lib/checklist/` + tests (status precedence, completed-by-other, last-60 slicing left in the component or parameterized). Preserve quirks: the 2-second optimistic `deletedCells` window, and that `loadHistory` errors are silently dropped (only `setLoading(false)`).

### `review` — Review (`ChecklistOutstandingTab`, ~1382–2068, plus module components ~1134–1380)

- **Render location:** `activeTab === 'review' && canManageChecklists` → `<ChecklistOutstandingTab authUserId isDev={role === 'dev'} canManageChecklists setError setEditItemId />`.
- **Owned local state:** `loading`, `byUser` (`Array<{userId, name, count, instances}>`), `expandedUserId`, `dateRange` (`'next_day' | 'next_week' | 'non_repeating' | 'missed'`, default `'non_repeating'`), `remindingUserId`, the second Forward cluster (`fwdInstance`, `fwdTitle`, `fwdAssigneeId`, `fwdSaving`), `users` (dev-only), `deletingInstanceId`, `completingInstanceId`, `reorderingUserId`, `outstandingDeletePending` (delete-confirm modal).
- **Cross-tab/shared state (stays in parent):** `setEditItemId` (opens the page-level `ChecklistItemEditModal` from the row pencil). Uses the app-global `useChecklistAddModal()` context ("Add task" button on the expanded person row, pre-assigned via `openAddModal(userId)`). Listens for the **`checklist-item-saved`** window event (via `loadOutstandingRef`) to refresh after the global add modal or the shared edit modal saves.
- **Derived:** `fwdMissingFields`/`fwdCanSubmit` (same pattern as Today).
- **Handlers/loaders:**
  - `loadOutstanding()` — incomplete instances with `checklist_items(title, links, repeat_type, reminder_scope)` + `checklist_instance_assignees(user_id, users(name, email))`; date-range filters: `missed` = `scheduled_date <` yesterday **and** (client-side) `reminder_scope !== 'today_and_overdue'` (those items already nag daily via the reminder cron); `non_repeating` = (client-side) `repeat_type === 'once'` with no date bound; `next_day` = tomorrow only; `next_week` = tomorrow → +7d. Groups per assignee, fetches `checklist_item_assignees.display_order` for (user × item), sorts each person's list by display_order then date, sorts people by count desc.
  - `markComplete(inst)` — dev row action; `.select('id')` after UPDATE and errors if `updatedRows` is empty (RLS/visibility guard); then **inline near-duplicates** of Today's `sendCompletionNotifications` (body prefix `"Dev completed:"`) and `maybeCreateNextInstance` logic (same `days_after_completion` math, sequential assignee inserts).
  - `saveFwd()` — near-duplicate of Today's `saveFwd` (same clone-as-`once` + delete-original flow; differs only in the follow-up reload).
  - `sendReminder(userId, instances)` — edge fn `send-checklist-notification` with a truncating body builder (1 title / ≤3 joined / "and N more"), tag `task-reminder`.
  - `persistOutstandingOrderForUser(userId, ordered)` — sequential `checklist_item_assignees.display_order = i+1` UPDATEs wrapped in [`withSupabaseRetry`](../src/utils/errorHandling.ts); `onOutstandingDragEnd(userId, instances)` (curried) uses dnd-kit `arrayMove`.
  - `openOutstandingDeleteModal` / `performDeleteOutstandingInstance`, `openFwd(inst, rowUserId)` (pre-fills assignee with the row's user).
- **Supabase tables / functions:** `checklist_instances` (SELECT/UPDATE/DELETE/INSERT), `checklist_items` (SELECT/INSERT), `checklist_item_assignees` (SELECT/UPDATE/INSERT), `checklist_instance_assignees` (INSERT), `users`; edge fn `send-checklist-notification`.
- **Sub-components:** module-level `OutstandingByPersonSortableRow` (~1134–1311; dnd-kit `useSortable` row with dev action buttons: complete ✓, edit → `setEditItemId`, delete, FWD) and `OutstandingByPersonSortableList` (~1313–1380; `DndContext` + `SortableContext`, plain `<ul>` fallback when `!canManageChecklists`) — both **move with this tab**. [`ChecklistReviewInboxes`](../src/components/checklist/ChecklistReviewInboxes.tsx) (**extracted**, 134 lines) renders below the table and is fully self-contained (owns [`useDispatchInbox`](../src/hooks/useDispatchInbox.ts) + [`useEstimatorInbox`](../src/hooks/useEstimatorInbox.ts) + `HelpFeedbackInboxSection` + the dismissed-archive modal; hides itself for assistant-like roles). Inline: delete-confirm modal, Forward modal (the **second copy** of Today's JSX).
- **External coupling:** `ChecklistAddModalContext` (global), `checklist-item-saved` window event, edge fn. `useEstimatorInbox`'s realtime channels are named `checklist-estimator-*` and are also mounted by Dashboard/DispatchMode via the same hook (routes never coexist — noted in RECENT_FEATURES v2.9xx Dashboard extraction #4).
- **Extraction status + risk + approach:** In-file. **Medium risk** — largest tab, but its shared touchpoints are all already props/contexts. Move `OutstandingByPersonSortableRow` + `OutstandingByPersonSortableList` + the tab into `src/components/checklist/ChecklistOutstandingTab.tsx` in one Stage B; `setEditItemId` stays a prop; the `checklist-item-saved` listener moves with the tab (it's tab-local). Stage A first: the `loadOutstanding` grouping/sort and the reminder-body builder (see inventory). Do **not** merge the duplicated Forward/completion logic with Today's during the move (behavior-preserving rule) — record it as the follow-on dedupe below.

### `manage` — Manage (`ChecklistManageTab`, ~2090–2533)

- **Render location:** `activeTab === 'manage' && canManageChecklists` → `<ChecklistManageTab authUserId role setError setEditItemId />`.
- **Owned local state:** `items` (`ChecklistItem[]` — the item/template rows, type declared at ~2070), `users`, `loading`, `filterUserId` (assignee filter; reloads via effect), `manageSearchQuery`, `muteModalItemId`/`muteModalTitle`, `manageDeletePending`/`manageDeleteSubmitting`, `itemCompletion` (`Map<itemId, {total, hasIncomplete}>` derived from instances), `completedOpen` (Complete section collapse).
- **Cross-tab/shared state:** `setEditItemId` (row pencil → page-level edit modal); `useChecklistAddModal()` ("Add checklist item" button); `checklist-item-saved` window-event listener (via `loadItemsRef`).
- **Derived:** `filteredItems` (`useMemo` — title/assignee-name/email substring, the page's only `useMemo`); `isRepeating(item)` (`!show_until_completed && repeat_type ∈ {day_of_week, days_after_completion}`); `isItemComplete(item)` (has instances and none incomplete); three buckets `incompleteItems` / `repeatingItems` / `completeItems` each sorted `byCreatedDesc`; `orderedEntries` (header/none/item row list; Complete section only expands when `completedOpen`); `colCount` = 7 + 1 dev-only Remind column.
- **Handlers/loaders:** `loadItems()` — `checklist_items` full-column select, with `checklist_item_assignees!inner(...)` when `filterUserId` set (plain join otherwise), ordered `start_date desc`; then derives `itemCompletion` from a single unbounded `checklist_instances.select('checklist_item_id, completed_at').in('checklist_item_id', ids)`. `loadUsers()`. `performDeleteChecklistItem(id)` (deletes the item; DB cascade removes instances — dialog copy says so).
- **Supabase tables:** `checklist_items` (SELECT/DELETE), `checklist_instances` (SELECT), `users`.
- **Sub-components:** `ChecklistTitleWithLinks`, `ChecklistItemMuteModal` (this tab's own instance; `onSaved={() => loadItems()}`), inline delete-confirm modal.
- **External coupling:** `ChecklistAddModalContext`, `checklist-item-saved`, [`compactTimeAgo`](../src/lib/subcontractorLastActivityCompact.ts) for the Created column.
- **Extraction status + risk + approach:** In-file. **Low risk — extract second.** Stage A: the bucketing (`isRepeating`/`isItemComplete`/three buckets/`orderedEntries`), the Repeat-column label formatter, and the Start-date formatter (see inventory). Preserve quirks: `DAYS` is Sunday-first; start dates parse with the `'T12:00:00'` noon trick and render `MM-DD` only within the past year (bare year otherwise); the `itemCompletion` query is unbounded `.in()` over every loaded item id.

### `roadmap` — Roadmap / tech tree (`ChecklistTechTreeTab`, own file, 2,312 lines)

Already extracted from the page (thin wrapper: `authUserId`, `canEditTechTree`, `setError`, `roadmapIdFromUrl`, `onRoadmapUrlParamChange`) — the page-level contract is **done and correct** (URL param stays in the parent per playbook). This dossier maps its **internal** structure for the sub-decomposition.

- **In-file data hook — `useTechTreeData(authUserId, roadmapId, setError)`** (~111–222): owns `groups`, `tasks` (`TaskView` = row + `assigneeIds`), `treeEdges`, `users`, `loading`, `load`. Quirk: `blockingLoadOverlayRef` — only the first fetch per (auth, roadmap) shows the blocking loader; refetches keep the graph mounted. Quirk: **edges are loaded unscoped** (`checklist_tech_tree_edges` full select) then filtered client-side to the roadmap's group ids.
- **In-file presentational components:** `TechTreeEmptyGroupDrop` (~254–280, `useDroppable` target using [`techTreeEmptyGroupDropId`](../src/lib/techTreeTaskOrder.ts)); `TechTreeEditableTaskTitle` (~285–358, 500 ms / 10 px long-press-to-edit with click suppression); `TechTreeDndTaskRow` (~360–451, `useSortable` task row); `GroupNode` (~453–691, the React Flow custom node: lock state, collapse, search highlight, per-task checkboxes, edit/add buttons, source/target `Handle`s; registered via module `nodeTypes = { groupNode: GroupNode }`); `RoadmapCanvasSearchPanel` (~727–891, expandable search input rendered twice — `inline` and `fullscreen` variants).
- **Module pure(ish) helpers:** `getAddPrereqLinkBlockReason(canEdit, fromGroupId, toGroupId, treeEdges)` (~83–103 — self-link / duplicate / cycle-guard messages, delegates to [`wouldAddEdgeCreateCycle`](../src/lib/checklistTechTreeGraph.ts)); `clientCoordsForConnectEnd(event)` (~695–712, Touch/Mouse coord normalizer); `assigneeNameMap`; `UUID_RE`.
- **Main component state:** roadmap layer (`roadmaps`, `roadmapMembers`, `membersModalOpen`, `roadmapsLoading`, `loadRoadmaps`, `loadRoadmapMembers`, memos `normalizedRoadmapUrl` → `effectiveRoadmapId` with URL write-back effect, `myRoadmapMemberRole`, **`canEditStructure` = `canEditTechTree || member role 'editor'`**); search (`roadmapSearchQuery` + [`computeRoadmapSearchMatches`](../src/lib/checklistTechTreeSearch.ts) memo + auto-expand-matching-groups effect + `fitViewRoadmapSearchMatches`); collapse (`collapsedGroupIds`, `toggleGroupCollapsed`, one-time collapse-all bootstrap via `collapseBootstrapDoneRef`, new-group/prune reconciliation via `knownGroupIdsRef`); layout (`layoutCacheRef` keyed on `structuralKey` — group ids + edges + collapsed set, so task toggles **don't** re-run dagre; `manualGroupPositions`, `organizeVersion` — the only trigger for `fitView` refits; `newGroupManualPositionFromConnectRef` places a group created by dragging a connection into empty canvas); React Flow (`useNodesState`/`useEdgesState`, `flowNodes` memo hydrating `GroupNodeData`, `onNodeDragStop`, `rfInstanceRef`); dnd (`reorderMode`, `dndSensors`, `onTaskDragEnd` → [`computeTaskReorderUpdates`/`orderedTaskIdsByGroup`](../src/lib/techTreeTaskOrder.ts) then parallel UPDATEs); fullscreen (`isDomCanvasFullscreen` + `cssFullscreen` CSS fallback for iPhone Safari, `canvasShellRef`, `roadmapModalPortalHost` — **modals portal into the canvas shell while fullscreen**, `triggerCanvasResize` double-rAF resize event, Esc + body-scroll-lock effect); modals state (`editingGroupId`, `addTaskModalGroupId`, `editTaskId`, `addGroupModal: AddGroupModalState`, `lineUpModalOpen`, `linksModalOpen` + `linksSearchQuery` + `filteredTreeEdges` memo).
- **Derived memos:** `tasksByGroup`, `completeGroupIds` ([`completeGroupIdsFromTasks`](../src/lib/checklistTechTreeGraph.ts)), `graphEdges`, `allGroupIds`, `unlockedIds` (`computeUnlockedGroupIds`), `taskCountByGroup`, `flowEdgeList`, `structuralKey`, `layoutNodes`/`layoutEdges` ([`layoutTechTreeFlow`](../src/lib/checklistTechTreeLayout.ts)), `flowNodes`, `nameById`, search memos.
- **Handlers (DB-writing):** `onToggleTask` (permission: staff always; otherwise group must be unlocked AND user is an assignee — unassigned tasks are staff-only, see `canActOnTask`), `addTaskToGroup` (max sort_index + 1, sequential assignee inserts), `updateTaskInGroup` (title update + delete-all/reinsert assignees), `insertNewGroup`/`addGroupByTitle`, `addPrereqLink` (toast feedback when `fromConnect`, `setError` otherwise; duplicate-key regex → friendly toast), `handleAddGroupSave` (toolbar vs link-from-group modes), `onPrereqConnect`/`onPrereqConnectEnd` (drag-to-empty-canvas opens the Add-group modal at the drop position via `screenToFlowPosition`)/`isValidPrereqConnection`, `onTaskDragEnd`, `removeEdge`, `handleCreateRoadmap` (**`window.prompt`** for the title; inserts the roadmap **and the creator as an `editor` member**, then navigates via `onRoadmapUrlParamChange`).
- **Supabase tables:** `checklist_tech_tree_roadmaps`, `checklist_tech_tree_roadmap_members`, `checklist_tech_tree_groups`, `checklist_tech_tree_group_tasks`, `checklist_tech_tree_task_assignees`, `checklist_tech_tree_edges`, `users`. All wrapped in `withSupabaseRetry`. RLS is per-roadmap (`can_edit_checklist_tech_tree_structure_for_roadmap` etc., v2.408).
- **Sub-components already extracted (all in `src/components/checklist/`):** `ChecklistTechTreeGroupModal` (183), `ChecklistTechTreeAddTaskModal` (162), `ChecklistTechTreeAddGroupModal` (124), `ChecklistTechTreeLineUpModal` (138), `ChecklistTechTreeLinksModal` (138), `ChecklistTechTreeRoadmapMembersModal` (299), `ChecklistTechTreeRoadmapBar` (94), `TechTreeRoadmapToolbarActions` (89), `ChecklistTechTreeMapActionIconButtons` (228). **Lib kernels with tests already exist:** `checklistTechTreeLayout`, `checklistTechTreeGraph`, `checklistTechTreeSearch`, `techTreeTaskOrder` (all `+ .test.ts`), plus `domFullscreen`.
- **External coupling:** `useToastContext`; `@xyflow/react` + `@dnd-kit/*`; the page's `?roadmap=` param (parent-owned).
- **Extraction status + risk + approach:** Extracted at page level; **medium internal risk** for sub-decomposition (React Flow node identity, layout-cache and fullscreen-portal quirks). Recommended sub-moves, each independently shippable: (1) `useTechTreeData` → `src/hooks/useChecklistTechTreeData.ts` verbatim (keep `blockingLoadOverlayRef` semantics); (2) `GroupNode` + `TechTreeDndTaskRow` + `TechTreeEditableTaskTitle` + `TechTreeEmptyGroupDrop` + `nodeTypes` → `ChecklistTechTreeGroupNode.tsx` (they communicate only through `GroupNodeData` — move the type with them); (3) `RoadmapCanvasSearchPanel` → own file; (4) Stage A: `getAddPrereqLinkBlockReason` → `lib/checklistTechTreeGraph.ts` (colocate with the cycle guard) + tests, `clientCoordsForConnectEnd` → lib + test. The roadmap-selection layer (`roadmaps`/members/`effectiveRoadmapId`) could later become `useChecklistRoadmaps`, but it writes the URL via the parent callback — keep that contract.

## Stage-A candidates (pure logic → `src/lib/*` + tests, before any component moves)

| Candidate | Currently | Target |
|---|---|---|
| `toLocalDateString(d)` | module fn in Checklist.tsx (line ~50) | `lib/checklist/dates.ts` (check for an existing repo equivalent first) + test |
| Next-instance date math in `maybeCreateNextInstance` / `markComplete` (days_after_completion + `repeat_end_date` guard) | duplicated ×2 in Checklist.tsx (+ a 3rd copy in `DashboardMyInboxCard`) | `lib/checklist/nextInstanceDate.ts` `computeNextInstanceDate({scheduledDate, daysAfter, endDate})` + tests |
| Notification-recipient derivation (`notify_on_complete_user_id` + `notify_creator_on_complete` dedupe) | inline ×2 (Today `sendCompletionNotifications`, Review `markComplete`) + Dashboard copy | `lib/checklist/completionRecipients.ts` + test |
| Reminder body builder (1 / ≤3 / "and N more" truncation) in `sendReminder` | inline in Review | `lib/checklist/reminderBody.ts` + test |
| History matrix build (`byItem` status map incl. `completed_by_other`, `sortedDates`, `instanceByKey`) | inline in `ChecklistHistoryTab` render body | `lib/checklist/historyMatrix.ts` `buildChecklistHistoryMatrix(instances, selectedUserId)` + tests |
| Display-order sort (orderMap build + sort by `display_order` → 999999 fallback → date) | duplicated in `loadToday` and `loadOutstanding` (per-user variant) | `lib/checklist/displayOrderSort.ts` + tests |
| Outstanding grouping (assignee grouping, per-user ordering, count-desc people sort, `non_repeating`/`missed` client filters) | inline in `loadOutstanding` | `lib/checklist/groupOutstanding.ts` (takes rows + order rows) + tests |
| Manage bucketing (`isRepeating`, `isItemComplete`, `byCreatedDesc`, three buckets, `orderedEntries` incl. `completedOpen` gating) | inline in `ChecklistManageTab` | `lib/checklist/manageBuckets.ts` + tests |
| Repeat-column label (`Until completed` / `Weekly: Mon, Tue` / `N days after completion` / `Once`) + Start-date short format (MM-DD vs year, noon-parse) | inline JSX IIFEs in Manage | `lib/checklist/manageLabels.ts` + tests |
| `itemCompletion` derivation from instance rows | inline in `loadItems` | pure `deriveItemCompletion(rows)` + test |
| `getAddPrereqLinkBlockReason` | module fn in ChecklistTechTreeTab.tsx | `lib/checklistTechTreeGraph.ts` (already tested module) + tests |
| `clientCoordsForConnectEnd` | module fn in ChecklistTechTreeTab.tsx | `lib/checklistTechTree*/` + test |
| Already done (reference): `checklistTechTreeLayout/Graph/Search`, `techTreeTaskOrder`, `checklistOrder.getNextDisplayOrders`, `domFullscreen` | in `src/lib` / `src/utils` with tests (except `checklistOrder`/`domFullscreen` — add tests if touched) | — |

## Preserve-quirks list (odd but load-bearing — do not "fix" during the move)

1. **Role-dependent default tab** (`review` for staff, `today` otherwise) written with `replace: true` only when `?tab` is absent and `role` has resolved.
2. **Today = overdue-first merge**: overdue instances are only pulled for items with `show_until_completed`, prepended before today's rows, then the whole list is re-sorted by per-user `display_order`.
3. **Un-completing clears notes** (`notes: null`) and drops the local draft; the revert path restores the previous notes only when non-empty.
4. **Forward deletes the original instance** and mints a new single-shot `checklist_items` row (copying notify/reminder fields) — history for the original item loses that date. Today's copy starts with `fwdAssigneeId=''` (placeholder-less select quirk); Review's pre-fills the row's user.
5. **`checklist-item-saved` window event** is the only cross-surface refresh: dispatched by `ChecklistAddModal` (global, in Layout) and `ChecklistItemEditModal`; consumed by Review + Manage via `loadXRef` listeners. Extracted tabs must keep registering it.
6. **History cell cycle** (dev edit mode): incomplete→delete uses a 2 s optimistic `deletedCells` window before reload; completed→delete-then-reinsert (new row id); only the **last 60** distinct dates render.
7. **`missed` filter double-gate**: server `scheduled_date < yesterday` AND client `reminder_scope !== 'today_and_overdue'` (those items self-nag via the reminder cron, so the Review list hides them).
8. **`markComplete` verifies `updatedRows.length`** after UPDATE (`.select('id')`) and errors on 0 rows — an RLS/staleness guard; keep it.
9. **Reorder persists sequentially** (`display_order = i+1`, one UPDATE per row via `withSupabaseRetry`), keyed on (`checklist_item_id`, `user_id`) — instance order is per-person, not per-instance.
10. **Manage completion is derived from ALL instances** of the loaded items in one unbounded `.in()` query; a one-off item is "Complete" only when `total > 0 && !hasIncomplete`.
11. **Tech tree loads all edges unscoped** then filters to the roadmap's groups client-side; `blockingLoadOverlayRef` keeps refetches from unmounting the canvas.
12. **Layout cache keyed on `structuralKey`** (group ids + edges + collapsed set): task add/toggle must NOT re-run dagre; `fitView` refits happen only on `organizeVersion` bumps (Organize / Collapse all / Show all / bootstrap), never on node-count change.
13. **Collapse-all bootstrap** runs once per (auth, roadmap); the group-reconciliation effect auto-collapses newly added groups and prunes removed ids; a group created by dragging a connection into empty canvas keeps its drop position via `newGroupManualPositionFromConnectRef`.
14. **Fullscreen**: DOM Fullscreen API with a CSS `position: fixed` fallback (iPhone Safari), Esc + body-scroll lock in CSS mode, double-rAF `window.dispatchEvent(new Event('resize'))` to make React Flow re-measure, and **all six tech-tree modals portal into the canvas shell while fullscreen** (`roadmapModalPortalHost`).
15. **Task permissions**: `canEditStructure = canEditTechTree || roadmap member role 'editor'`; non-staff can toggle a task only when its group is unlocked AND they are an assignee; **unassigned tasks are staff-only** (`canActOnTask`).
16. **`handleCreateRoadmap` uses `window.prompt`** and inserts the creator as an `editor` member row before navigating.
17. Edge-fn notification failures are swallowed everywhere (`try {} catch { /* ignore */ }`) — completion must never fail because a push failed.

## Recommended extraction order (value ÷ risk)

1. **Stage A sweep** — the pure-logic inventory above; each independently shippable. Highest leverage: `nextInstanceDate` + `completionRecipients` (kills a 3-way duplication incl. Dashboard), `historyMatrix`, `groupOutstanding`, `manageBuckets`.
2. **`ChecklistHistoryTab` → `src/components/checklist/ChecklistHistoryTab.tsx`** — lowest coupling (4 props, no modals shared, no contexts); validates the file-move pattern.
3. **`ChecklistManageTab` → `ChecklistManageTab.tsx`** — brings its own mute/delete modals; `setEditItemId` stays a prop; keeps its `checklist-item-saved` listener.
4. **`ChecklistTodayTab` → `ChecklistTodayTab.tsx`** — after Stage A so the completion engine is lib-backed; the Forward modal moves with it (still duplicated — see 6).
5. **`ChecklistOutstandingTab` (+ `OutstandingByPersonSortableRow`/`List`) → `ChecklistOutstandingTab.tsx`** — largest move; `ChecklistReviewInboxes` is already a child render and needs no change.
6. **Follow-on dedupe (separate pass, not behavior-preserving-move work):** shared `ChecklistForwardModal` component + wiring both tabs (and `DashboardMyInboxCard`) to the Stage-A completion kernels — do this only after 2–5 land, as its own reviewed change.
7. **Tech-tree sub-decomposition** (independent track, any time): `useTechTreeData` → `src/hooks/useChecklistTechTreeData.ts`; node components → `ChecklistTechTreeGroupNode.tsx`; `RoadmapCanvasSearchPanel` → own file; the two module helpers → lib + tests.

**What must stay in `Checklist.tsx`:** the tab bar + `?tab=` sync + role-based default, the `?roadmap=` param + `onRoadmapUrlParamChange`, `editItemId` + the shared `ChecklistItemEditModal`, the `role` fetch and role gates, and `error` rendering. End state: a ~250-line router, which is essentially what lines 57–247 already are.

## Recent churn (from `docs/RECENT_FEATURES.md`, grep-verified)

- Tech tree is the active feature area: v2.407 (canvas icon row / fullscreen header / `ChecklistTechTreeMapActionIconButtons`), v2.408 (multi-roadmap + members + `?roadmap=` + per-roadmap RLS), plus later Add-modal keyboard-path work (2026-07-21 entries) and `ChecklistReviewInboxes` wiring for dispatch link-job-pictures (v2.556 era).
- v2.416 added Manage's search (`manageSearchQuery` + `filteredItems`).
- The Dashboard decomposition (extractions #4/#7) already **reused/duplicated** this page's engines: `useEstimatorInbox` (shared hook, `checklist-estimator-*` channels) and the My Inbox checklist CRUD engine (`DashboardMyInboxCard` + `lib/dashboardMyInbox.ts`) — the strongest argument for the Stage-A shared kernels here.
- Today/History have been stable for months — low churn, safe to move first/third.

Definition of done per tab, verification gates (`npm run typecheck && npm run lint && npm test` after every step), and anti-patterns: see [`PAGE_DECOMPOSITION_PLAYBOOK.md`](./PAGE_DECOMPOSITION_PLAYBOOK.md). Behavior-preserving only.
